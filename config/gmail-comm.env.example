# Gmail connector for Entity Discussion Tracker (comm_* tables)
# Copy to /var/www/maltytask/config/gmail-comm.env on the VPS and fill in.
# This file is safe to commit — it contains no real secrets.

GMAIL_COMM_DELEGATED_USER=kouros@lanebuleuse.ch
GMAIL_SA_KEYFILE=/var/www/maltytask/config/gmail-sa.json
GMAIL_COMM_QUERY=newer_than:180d
