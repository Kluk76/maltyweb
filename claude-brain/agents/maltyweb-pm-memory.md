# maltyweb-pm — Project Manager Knowledge Base (LEAN INDEX)

> This is the maltyweb PM's memory. It is a LEAN INDEX: current build-state, sequencing, RESUME POINT, the BUILD-LAUNCH CHECKLIST, and ONE-LINE load-triggered pointers to thematic files. Detail / completed-arc / any section over ~5 lines is COMPILED OUT into `~/.claude/agents/maltyweb-pm-memory/<topic>.md` (and per-topic subdirectories), loaded ON DEMAND only when a task touches that topic. Keep this index small + current — date load-bearing facts; verify against the live DB/repo before recording. Stable conventions also live in `CLAUDE.md` + the `~/.claude/skills/` skills + the topic memories under `…/memory/`.
>
> **CURRENT-SESSION STATE (2026-06-21):** MIG HEAD **429** applied (`429_comm_entity_registry`; `428_retire_brewing_lookup_page` — folds brewing lookup into wort; `427_ref_suppliers_bc_vendor_fields`; `426_email_order_bc_push_mode` Slice 3b; `425_brewing_lookup_page` SUPERSEDED by 428; 424 recap per-cadence config). Next-free mig = **431** (mig **430** `user_ui_prefs` applied this window — generic per-user UI-pref store, schema_meta class=config; 429=comm-entity-registry). (Hist: 426/428 applied via manual `sudo mysql`/ALTER + manual `schema_migrations` INSERT, not migrate.php.) **ALWAYS re-`migrate.php --status` + `ls db/migrations` at build-start — parallel sessions LEAD the number; next-free is whatever `--status` shows, never a memorized value.**
> ✅ **BREWING/PACKAGING LOOKUP REVISION — SHIPPED+LIVE+VERIFIED 2026-06-21 (3 commits, mig 428; SUPERSEDES the earlier-today standalone-page build).** Net effect: brewing lookup is now an **operator-level "Consulter" tab on `wort.php`** (data-tab=lookup, panel #wort-panel-lookup, operator/production gate), NOT a viewer-level standalone page; the earlier "for everyone/all-7-presets" access is GONE. (1) **`5561145`** — wort.php blank-page bug ROOT-CAUSED + fixed (var-alias lesson, below). (2) **`aef78aa`** — folded the brewing lookup INTO wort.php as a 3rd "Consulter" tab reusing the shared `lookup-panel` partial ($lookupConfig + recipe-options in wort's top PHP block, guarded); API `public/api/brewing-lookup.php` gate repointed `require_page_access('brewing-lookup')`→`('wort')` (endpoint KEPT, page GONE); **mig 428** APPLIED+recorded MANUALLY (audit-tombstoned ref_pages id 739 via action='update'+_tombstone, DELETEd id 739 + its 7 preset grants via CASCADE); DELETED `public/modules/brewing-lookup.php` + `public/css/brewing-lookup.css` (VPS+git); removed the 2 orphaned brewing-lookup Visite-guidée cards → tour-gap-check critical=0/no-orphan. (3) **`61a0130`** — wrapped the existing packaging lookup panel on `packaging.php` in a labelled "Consulter un packaging" header (pkg-lkp- classes, CSS in lookup-panel.css w/ kraft tokens); removed the "+ Saisir un conditionnement" CTA (dedicated saisie page covers it). 🔴 **DURABLE LESSON (wort blank-page root cause) — classic-script shared-global aliasing:** when two classic (non-module) `<script>` tags share globals, alias shared funcs with `var`, NEVER `const`/`let`. `wort-kpis.js` had aliased 6 `window.KpcCharts` primitives (escHtml, fmt, svgEl, MONTHS_FR, buildBarChart, paceTintResult) with top-level `const` → collided with `kpi-charts.js`'s top-level `function escHtml`/`fmt` across classic tags → Chromium "Identifier already declared" → wort-kpis.js died AT PARSE → initTabSwitcher() never ran → whole page dead/no-data. `kpi-charts.js` exposes funcs BOTH as top-level `function` AND on `window.KpcCharts`, so ANY consumer (wort, packaging, mon-tableau — all 3 re-verified clean) MUST use `var` aliases. Trigger "brewing lookup"/"consulter un brassin"/"wort no data"/"wort blank page"/"var alias"/"KpcCharts"/"Identifier already declared"/"packaging lookup"/"saisir un conditionnement CTA" → read-only-lookup-modules.md.
> 5 builds shipped+live+pushed 2026-06-21: brewing-lookup standalone page (`6075fb0`+`8120817`, mig 425 — **SUPERSEDED same day by mig 428: folded into wort.php, standalone page RETIRED; see ✅ banner above**) · packaging daily-recap UNITS (`d4a8861`, no mig) · read-only lookup modules (`e83c4d1`, no mig) · recap per-cadence config (`4dfe7b8`, mig 424 — 🔴 RE-OPENS the one-sub-per-user recap model: recap config now `user_kpi_recap_subs` one-row-per-(user×cadence) + NEW `user_recap_tracker_selections`; dashboard `user_kpi_selections` NO LONGER drives recap content). All deployed surgical per-file rsync (parallel email-orders session held a dirty tree). Per-build narration + the full mig 397→428 ledger → [build-history/changelog-2026-06/](maltyweb-pm-memory/build-history/changelog-2026-06/README.md); recap-model detail → [kpi-tracker-catalog](maltyweb-pm-memory/kpi-tracker-catalog/README.md). 🔴 **DEPLOY-BUT-UNCOMMITTED pile (commit by PATHSPEC, never `-A`):** parser bundle (circea.js/nomoq.js/index.js/parser-supplier-map.ts) + triage_actions.php/triage.php/triage-manual-lines.js + the email-orders Slice-2 bundle (mig 421 + bc_order_match.py + ingest_email_orders.py + generic_vocab.py + email-orders.php/.css). (**Email-orders Slice-3b + Cobra L1 parser now COMMITTED** `99c79d4`/`e36b762` — no longer in this pile.) The verbatim former inline recaps of every banner below are archived in [index-relocated-detail/README.md](maltyweb-pm-memory/index-relocated-detail/README.md) (read only to recover exact former wording).
>
> 🔴 **EMAIL-ORDERS arc (commandes@ → ord_orders → BC):** Slices 1/2/3a/**3b** ALL LANDED. **Slice 3b (validation-accept→BC SalesOrder CREATE) COMMITTED 2026-06-21 (`99c79d4`, Opus-verified before commit) — DISARMED:** mig **426** seeds `system_settings email_order_bc_push_mode='off'`; `email_order_promote()` RETAGGED `source='maltytask'`+`source_ref='mt:<id>'` (existing push→echo machinery rekeys the SAME row to `bc:<No>`; `source_email_id_fk`+`review_status='accepted'` preserved as orthogonal provenance; producer had 0 rows = clean retag); `email-orders.php` gains bc_customer_no NULL guard + force_create dedup re-gate + post-promote BC push block (dry-run in `off` / live POST in `armed`, armed flags ONLY inside the `==='armed'` branch) + Bucket-3 3-state badges (Rapproché BC / Créé — BC #N / En attente d'envoi BC). OPUS-VERIFIED LIVE: OFF-mode E2E wrote one `status='entered'` source='maltytask' row, FG-depletion=0 (FG keys on `o.status`, NEVER `o.source`). **✅ SUPERVISED ARM-TEST EXECUTED + PASSED 2026-06-21** (`--make-test-order` order 238 → real BC POST **ORD210160** → push spine REKEYED 238 in place to `bc:ORD210160` INLINE post-POST (does NOT wait for the */15 echo-leg) → echo-leg `--apply` confirmed 0 new rows, source stayed 'maltytask' → ONE ord_orders row throughout → `--delete-bc` + guarded cleanup = fully clean). **Live BC-write path is PROVEN end-to-end; flag STILL `'off'` (not armed).** **✅ PHASE 1 L1 PDF PARSERS BUILT+DEPLOYED+VERIFIED 2026-06-21 (`fe8266c`):** 4 single-order parsers Nausikraft `[6,8,23,4,24,48,48,24,72]` / Petite Cave `[48,144,144,24]` / Amstein `[9,60,45,9,9,9,90]` / Alloboissons `[1296,648,648,648,600,600,60]` (all born-digital `pdftotext -layout`, HINT-only, match on `env.original_sender` domain+PDF; Alloboissons reconcile gate == Montant HT 12697.04 ±0.10). 🔴 **BLAVIGNAC (id 59) = DOCUMENTED PERMANENT DECLINE** (scanned image + handwritten qty → OCR garbage, re-confirmed via OCR test this session; stays no_match, operator keys manually; NOT a gap, do NOT build an OCR parser for it). **✅ PHASE 2 BOISSONS GDS MULTI-ORDER BUILT+DEPLOYED+SMOKE-VERIFIED 2026-06-21 (Option A atomic-all, DISARMED — 2 commits: Python `ec8c42f`, PHP/JS `47ceea4`):** `boissons_gds.py` returns `list[ParsedOrder]` one-per-PDF, COVERAGE-OR-REFUSE for the whole email (any PDF fails → decline entire email), fixture 98 → 2 orders qty `[[9,30,15],[1,9]]`; connector `_order_to_dict()`+`_serialise_parsed_orders_multi()` emit `_kind:'parsed_order_hints_multi'`/`orders[]` (single shape byte-identical, dispatch detects `list[ParsedOrder]`, no wrapper class); PHP `email_order_promote_multi()` = N ord_orders in ONE txn each `source='maltytask'`/`source_ref='mt:<id>'`, email flips order_created ONCE, idempotency `source_email_id_fk+source` (single `email_order_promote()` BYTE-IDENTICAL); UI N `.eo-suborder` blocks + one "Valider les N commandes" + `validate_multi` handler; JS scope card→`.eo-suborder`; smoke → mt:239/240 status=entered, 2nd call→AlreadyPromoted, 0 residue. **🔴🔴 KNOWN DEFERRED GAP — CLOSE BEFORE ARMING MULTI: the multi-order path has NO BC dedup interstitial and does NOT push to BC even armed (v1). When BC push is armed, the standalone push spine's `load_pending_orders` (source='maltytask' AND bc_no IS NULL) WOULD pick up multi sub-orders and create them in BC with only `Your_Reference=mt:<id>` idempotency — which does NOT dedup against a MANUALLY-entered BC order for the same physical order. Before arming multi: wire a per-sub-order BC dedup gate (`bc_order_match.py --match-one` per sub-card) OR exclude multi from the armed push. Single-order 3a dedup interstitial remains FULLY INTACT.** **🔴 STILL no_match in live DB (parse correctly but flip only on connector `--force --apply` re-run, operator owns): rows 38/47/63/77 (single) + 98 (multi); row 98 now safe to flip (multi UI deployed); connector cron STILL NOT enabled.** **REMAINING: (1) Kouros flips `off→armed` in Données générales when ready (tech proven, no code change); (2) the multi-arming BC-dedup gap above; (3) email-connector cron enable; (4) 🔴 BACKLOG BUG (NOT 3b): `ingest_bc_sales_orders.py` crashes AFTER committing DB work at L1462 `_write_review_csv` — `PermissionError /tmp/bc-orders-unresolved-skus.csv` (stale file owned by another user, www-data can't overwrite); `*/15` cron likely hits every run (DB stays correct, CSV write dies); fix = write under `/var/www/maltytask/data/` or unlink-before-write. Trigger "bc reader crash"/"unresolved-skus.csv"/"PermissionError /tmp".** **L1 Cobra body-parser ✅ COMMITTED (`e36b762`)** — broker order, HINT-only, customer_hint=end-customer string (operator resolves FK via mig-389), coverage-gate-or-decline; corpus now 67 no_match/40 parsed. KNOWN DEBT: force_create match-gate = ~60-line copy-paste of the validate gate (refactor to shared helper). EQUIP coder+sql+ui+webapp-testing / parser-coder+sql. Trigger "email order"/"commandes@"/"per-sender parser"/"Cobra parser"/"email_order_promote"/"dedup vs BC"/"Slice 3b"/"arm-test"/"email_order_bc_push_mode"/"bc_order_match" → READ [email-order-ingestion-arc/](maltyweb-pm-memory/email-order-ingestion-arc/README.md).
>
> 🟢 **SHIPPED/CLOSED this window (one-line each; full detail in the cited topic file):**
> - Circea SA bioCO₂ supplier onboarded; CO₂ routing revised (vrac→MI 197, bonbonne→MI 75, MI 666 deleted). Trigger "Circea"/"CO₂ routing"/"bonbonne" → [derivation-tree-and-schema.md](maltyweb-pm-memory/derivation-tree-and-schema.md) §CO₂ MI ROUTING + supplier-onboarding shape.
> - Stale `doc_invoice_lines` on re-alias FIXED (`ta_update_invoice_line` unconditional `mi_id_fk=?`); OBI inv 221 corrected →HT 32.25; gross-booking systemic finding CLOSED (no further COGS corrections/backfill/CFO restatement); FIX 2a line_index + FIX 2b HT-capture deployed. Trigger "gross vs HT"/"re-alias drift"/"ta_materialize_delivery"/"HT capture" → [ingestion-validation-gate-arc.md](maltyweb-pm-memory/ingestion-validation-gate-arc.md) §SYSTEMIC FINDING CLOSE-OUT.
> - Three Expéditions-card upgrades (`971471c`): doc-externe pill (mig 400 surgical) + per-order comments + single "BL imprimé" tag; `can_write_expeditions()` 403 hole closed. → state-machine-and-eshop-classification (in fulfilment-expeditions-arc).
> - v1→v2 migration-bridge scrap (RawDB-normalized.xlsx + normalize-rawdb.py scrapped; mig 399 schema_meta writer fix) → scrapping-backlog.md #34/#35. 2× DIBB FG-stock data corrections (06-19). Returns reversal-pollution fix (`v_physical_returns`, mig 387, rate 6.60%→1.82%) → fulfilment-expeditions-arc §RETURNS REVERSAL POLLUTION.
> - Recap send-hour (mig 398) + send-hour-ignored bugfix (`197d97e`, canonical `kpi_recap_next_due`); DIBX cage SKU + cage NULL-FK guard (`db422d3`); transfer-leg anchorFloor fix (commit by pathspec `app/fg-stock.php`); cage re-denomination `-X`=1 bottle (mig 392); FG-stock carry-over fix (`5b9858e`); planning multi-process UX P0+P1 (`f0361d4`). STANDING: `php8.1 -l` the DEPLOYED file before every php-fpm reload → conventions-and-helpers.md.
>
> 🟡 **OPEN active arcs (status one-line; READ the topic file + recap unprompted when triggers fire):**
> - **Recipe change-request flow** — P3a-d + **P4 SHIPPED+LIVE; EMAIL CHANNEL NOW LIVE (not dark) 2026-06-21** (`e106e26`; mailer CR-path + `recipe_cr_email_mode` setting). ✅ `recipe_cr_email_mode` flipped **'test'→'live'** (system_settings section='recipes' key_name='recipe_cr_email_mode', rows_updated=1, read-back confirms 'live'). Both templates ('filed' + 'decided') tested E2E in test mode (postfix→IONOS, status=sent → kouros@), operator approved. LIVE recipient resolution: **FILED → active admins w/ email = Kouros only** (sole active admin); **DECIDED → requesting manager** (5 managers have emails: Gonzalo, Yves, Nathan, Louis M., Thierry; 'Smoke Test Manager (bot)' has NO email → its decision notif silently drops, harmless). Backlog STILL STANDS: surface email-mode as a 3-state select in Données générales (recipes) so operator can flip mode without an SQL edit. 🔴 HARD RULE: `$managerAllowedActions` stays EXACTLY `['submit_change_request','withdraw_change_request']` (P3c security incident). Trigger "recipe change request"/"demandes recette"/"sdc_apply"/"P4"/"cr_pending_count"/"recipe_cr_email_mode"/"email go-live"/"managerAllowedActions" → [recipe-ingredients-drift-surface.md](maltyweb-pm-memory/recipe-ingredients-drift-surface.md) §P4 SHIPPED + §P3a-d LANDED.
> - **Planning — serving-tank occupancy rework** PENDING (operator-flagged; the one net-new design open; rounds 1-8 shipped+pushed, migs 401/407/408/411/414). Round-8 default N=14 ZEROES free cuves; real fix = a cuve return/pickup event. CARDINAL: planning = INTENT only, never feeds COGS/COP/WAC/BOM/beer-tax/inventory. Trigger "rework cuves"/"occupation des cuves"/"serving tank occupancy"/"retour cuve"/"serving_tank_turnaround_days"/"fuller planner"/"production planner" → [planning-page-arc.md](maltyweb-pm-memory/planning-page-arc.md) §NEXT/ACTIVE REWORK first. EQUIP coder+sql+webapp-testing(UAT).
> - **Dynamic-financier rebuild + `cogs_fiche_sealed`** (LIVE arc) — move financier path from TS-via-tunnel into in-app PHP compute (`cogs-fiche-compute.php`); published-month immutability becomes structural via append-only `cogs_fiche_sealed`; STEP 1 = byte-for-byte port + parity harness vs TS oracle (gate parity=0). 🔴 RM = naive `final_qty×cost_chf` (do NOT switch to `loadRM`; Hops 29 668.66 = regression tripwire). Trigger "dynamic financier"/"cogs_fiche_sealed"/"sceller"/"restater"/"Clôturé (signé)"/"Calculé (live)"/"parity harness" → [financier-cogs-fiche.md](maltyweb-pm-memory/financier-cogs-fiche.md) §ACTIVE ARC first. EQUIP sql+coder+ui+webapp-testing.
>
> ✅ **SHIPPED-AND-LIVE (immutable/baseline records; one-line — full detail in topic file):**
> - Energy/Utilities COP web-path (mig 396; in-app PHP compute off the stale Node JSON; parity 0.0000 × 5 months). 🔴 STANDING: the financier has TWO boards — COP (÷hlBrewed) AND COGS (÷hlSold); both share GL utilities lines — check BOTH on any COP/COGS-feed work. Trigger "energy"/"utilities COP"/"saisie-energie"/"peak kW"/"compteur" → [energy-cop-webpath.md](maltyweb-pm-memory/energy-cop-webpath.md).
> - Financier-fiche COGS keystone close (May 2026 `423a660`; engine `cogs-monthly-compile.ts`); mig 339 `count_type` ENUM. Next close = JUNE. → [financier-cogs-fiche.md](maltyweb-pm-memory/financier-cogs-fiche.md).
> - Returned-FG arc BC-derived (P0-P3 + Part A/B/C; migs 373/376/380/384/387; disposition restock/scrap/quarantine/rebate). Trigger "return"/"retour"/"avoir de vente"/"disposition"/"oversell"/"ATP"/"return rate" → fulfilment-expeditions-arc §returned-fg-tracking.
> - CFO/Thierry read-only preset (`finance_viewer` id=10, mig 378) + finance-write capability (`manager_scope='finance'`); `can_write_expeditions()` write-leak closed. Trigger "CFO"/"Thierry"/"finance_viewer"/"can_write_expeditions"/"manager_scope finance" → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md) §CFO FINANCE_VIEWER.
> - Topbar consolidation (mig 383, `2f61e52`; 6 drill-down categories + 2 standalone; `page_categories()` canonical accessor). Trigger "topbar"/"category_key"/"page_categories"/"6 categories" → details in [index-relocated-detail/](maltyweb-pm-memory/index-relocated-detail/README.md).
> - Cobra distributor identity (`ref_customer_identity` billing-alias, mig 389; 40 links). 🔴 confirm Cobra-distribution via ledger bill-to test + operator, NEVER guess by name prefix. Trigger "Cobra"/"ref_customer_identity"/"billing_alias"/"bill-to"/"ship-to" → fulfilment-expeditions-arc §COBRA DISTRIBUTOR IDENTITY.
> - Entity discussion tracker (supplier-side two-way mail+doc hub, P0-P3-SEND, mig 420; gmail.send live; `comm_address_pins` = sole email→entity map). ✅ **BC-VENDOR IDENTITY HALF SHIPPED+COMMITTED 2026-06-21 (`a893f99`, mig 427)** — `sync_bc_vendors.py` daily cron, ref_suppliers gains bc_vendor_no+email+address; 130 suppliers human-ratified to FOURN numbers; BC = IDENTITY feed NOT email registry (only 21 emails live on ref_suppliers → sheet stays primary). 🆕 **REGISTRY+RESCOPE+PURGE — mig 429 APPLIED+COMMITTED 2026-06-21 (`a68fc61`):** `ref_entity_email_domains` (XOR-single, **strict `eed_chk_one_party` both-NULL FORBIDDEN — verified AS-BUILT**, match_type domain|address, source enum, is_shared, backfilled_at) + `comm_unknown_domain_seen` counts-only + `purge_status` cols all LIVE. 🔴 **CHECK-vs-is_shared CONTRADICTION RULED (Option B): strict CHECK STAYS, NO mig 430 — consumer/shared domains (gmail/bluewin/…) get NO domain row (both-NULL forbidden + wrong); only per-ADDRESS pins capture them; entity-less review sink stays comm_threads (already both-NULL-permitting); comm_unknown_domain_seen = not-yet-pinned-supplier safety net. CONSUMER_DOMAINS = shared Python const (seed+connector), NOT a settings row. is_shared kept fwd-compat (=0 at seed; reserved = entity-owned-but-don't-auto-resolve, STILL FK'd). Piece-5 step 3 + Piece-2(c) corrected in topic file.** ✅ **SEED (Piece 5) + CONNECTOR RE-SCOPE/BACKFILL (Pieces 2+3) SHIPPED+COMMITTED 2026-06-21** (seed scripts `seed_comm_registry.py`+`comm_domains.py`, CSV NOT committed=PII; 107 pins+151 validated+35 bc-vendor registry rows; connector `2b10508` v2.0.0 store-only-registry-else-DROP + `comm_unknown_domain_seen` + per-domain `newer_than:2y` backfill). 🔴 **LIVE-CUTOVER QUIRK: cron `maltytask-email-comm` (*/15 --apply, ENABLED) auto-backfilled 25 rows to 2y (+~1055 msgs) before session paused rest via `backfilled_at=NOW()` on 161 rows → 25 suppliers @2y, rest @6mo; reversible (NULL backfilled_at); comm_threads now 1645/comm_messages ~3716.** ✅ **PURGE PIECE-4 PHASE-1 (`6449cd8`) + PHASE-2 HARD-DELETE (`7f132bc`) + MANAGER-ACCESS GATE (`f8eeee7`) ALL SHIPPED+COMMITTED 2026-06-21 — ENTIRE registry+rescope+purge+gate slice DONE.** Phase-1 = reversible flag-only 3-way classify of 1161 review threads (`purge_comm_backlog.py`) → KEEP 343→supplier_fk·live / MIGRATE 60→customer_fk·migrated_customer (never deleted) / DELETE-NOISE 752→soft_purged / AMBIGUOUS 6→live both-NULL; +`purge_status='live'` guard on all 4 sf-comm-thread.php reads. **Phase-2 (`--hard-purge` mode, operator authorized COMPLETE purge ahead of 7d window): deleted 752 threads/1647 msgs/1997 comm_message_docs/776 sole-referrer email-only doc_files +776 files (411.9MB freed)/3175 tombstones; 48 docs PRESERVED (shared); ZERO fiscal docs touched; sole-referrer = no surviving comm_message_docs link + zero FK from 7 non-comm tables + path-bounded data/email-attachments; before-snapshot /var/log/maltytask/comm-hardpurge-snapshot.json.** 🔴 **doc_files.source_folder for comm = `'email-comm'` (NOT 'email-comm-attach' as the composer note said).** Post-state: comm_threads **893** (833 live incl 6 ambiguous + 60 migrated_customer), **0 soft_purged**. **Gate:** `can_use_comm_tracker($me)`=is_admin||is_manager in app/auth.php (mirrors can_write_expeditions, NOT a ref_pages row); 403 in sf-comm-thread.php at L27 BEFORE maltytask_pdo L33 (no read/write bypass); UI `window.SF_CAN_COMM`→`CAN_COMM` gates the tab+renderDiscussionSection (section ABSENT for non-managers). STILL OPEN (broader arc, record-only): unknown-domain admin surface +ui (3 high-hit dalumequipment.com/henriot.fr/unitronics.io); operator follow-ups (promote those 3; merge stretchfolienprofi 72/168; 2y-retro-all declined—stay 6mo); customer-side fiche P3-cust DEFERRED (60 migrated_customer threads await it). EQUIP sql+coder+webapp-testing(+ui). 🔴 MDL-HANG: comm_* ALTERs stall on idle-in-trx maltytask_app conn holding SHARED_READ → KILL via `sudo mysql` + ALTER same root session; latent: check maltytask_pdo persistent/idle-read-txn. 🔴 DURABLE: verify entity recency vs invoice NAMES, never supplier_fk aggregates (Durst→Soufflet save). Trigger "discussion tracker"/"comm_threads"/"comm_address_pins"/"ref_entity_email_domains"/"connector rescope"/"comm_unknown_domain_seen"/"two-phase purge"/"send_reply"/"gmail.send"/"BC vendor"/"sync_bc_vendors"/"bc_vendor_no"/"vendor registry" → [entity-discussion-tracker-arc.md](maltyweb-pm-memory/entity-discussion-tracker-arc.md) §REGISTRY+CONNECTOR-RESCOPE+2-PHASE-PURGE.
> - Supplier evaluation / EF-01 (migs 409/410; 6 tables; KO A2+A5; 24 critique seeded; cron deployed disabled). Trigger "évaluation fournisseur"/"EF-01"/"supplier scoring"/"criticité"/"supplier_eval_compute" → [supplier-evaluation-arc.md](maltyweb-pm-memory/supplier-evaluation-arc.md).
>
> 🆕 **PENDING BUILDs (PM-ruled, NOT built; one-line — full ruling in topic file):**
> - Credential-expiry watchdog (`ops_credential_expiry`, mig 381; daily flock'd cron T-30/14/7/1 before BC Entra secret silently dies; N-credential table, NO FK, never feeds COGS). Trigger "credential expiry"/"client_secret"/"ops_credential_expiry"/"watchdog" → [[project-re1-bc-sales-ledger-automation]] §CREDENTIAL-EXPIRY WATCHDOG.
> - Recurring BC → `ref_customers` sync (Kouros "synch les clients"; ships FIRST before resuming BC-orders Phase-1 --apply; DRIFT not dedup; BC-owned vs maltytask-curated split). Trigger "synch les clients"/"customer sync"/"Stoked Mountain"/"drifted bc_customer_no" → [sales-commercial-surface/README.md](maltyweb-pm-memory/sales-commercial-surface/README.md) §RECURRING BC → ref_customers SYNC.
> - BC sales discount → `inv_sales_invoice_lines` + revive #100. Trigger "discount"/"remise"/"escompte"/"#100"/"salesInvoices" → [sales-commercial-surface/README.md](maltyweb-pm-memory/sales-commercial-surface/README.md) §BC SALES DISCOUNT.
>
> ✅ **Objectifs de production** (Salle de contrôle `?sec=objectifs`, mig 346; W/M/Y trackers; canonical actuals from bd_brewing_gravity_v2 Cooling + bd_packaging_v2 vendable) + Mon-tableau widgets #281/#282 (migs 351/352; grouped_bar consumes `result.breakdown[]`, % d'atteinte not absolute hl). Trigger "objectifs de production"/"16700 hl"/"528 brassins"/"objectifs widget"/"grouped_bar" → [kpi-tracker-catalog/README.md](maltyweb-pm-memory/kpi-tracker-catalog/README.md) §OBJECTIFS.
>
> 🟢 **State-machine automation:** #4 (auto `bl_printed` on BC Completely_Shipped, `e82689c`) DELIVERED; #2 (confirmation mail on «Confirmer», DISARMED via `confirmation_email_mode`) build-brief READY/not-launched. ✅ BC↔maltytask operational layer A/B/C + Phase 2 write-back D (D1+D2 complete; migs 354/355/357/366; CMs via BC API v2.0; echo `mt:<id>`; round-trips passed). Trigger "confirmation email"/"Confirmer"/"chantier 2"/"write-back"/"D1"/"D2"/"line_status"/"salesCreditMemos"/"lead time" → [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) §state-machine + §bc-orders-integration.

## 🚦 BUILD-LAUNCH CHECKLIST — read EVERY time a build agent is dispatched (operator standing order)
- **🎯 SKILL-FIRING MATRIX (always-on):** EVERY brief/dispatch/resume the PM produces MUST end with an `EQUIP:` line per [skills-and-build-tooling.md](maltyweb-pm-memory/skills-and-build-tooling.md) §SKILL-FIRING MATRIX. Shorthand: app/backend→`coder`+`sql`; rendering→+`ui`; parsers→`parser-coder`(+`sql`); deployed-page verification→`webapp-testing` smoke BY DEFAULT (read-only vs prod); auth/2FA→coder+`references/php-security.md`; dataviz→ui+`references/dataviz.md`; InnoDB→sql+`references/innodb/`; spreadsheet→`xlsx`; 3rd-party skill→`skill-vetting` FIRST; skill authoring→`skill-creator`; memory compaction→`memory-hygiene`. Mis-equip → PM flags + re-launch.
- **Opus orchestrates, Sonnet codes.** Build agents = `model:"sonnet"`, self-contained prompt (task+constraints+verification+"report back"); Opus reads report + spot-checks diff.
- **🔴 COGS-BEARING CLAIMS: OPUS VERIFIES INDEPENDENTLY** — a sub-agent "gate PASS" is a CLAIM; before any COGS/COP/WAC/BOM number reaches the operator, verify against an independently computed reference (hand-computed per-recipe, WAC bands, before/after invariants). Existence/positivity gates insufficient — assert PLAUSIBILITY. → conventions-and-helpers.md.
- **🔴 "DEPLOYED" IS A CLAIM — md5-VERIFY local↔VPS after every agent deploy, especially COGS-adjacent files** (`ssh maltyweb md5sum …`); pair with an independent behavioural probe (flag-OFF / before-after invariant), not the agent's word. (eshop-repack incident → conventions-and-helpers.md + index-relocated-detail.)
- **Agents leave NO helper scripts in the repo** — use `/tmp`, delete; verify `git status` post-run.
- **🚨 DEPLOY PUSHES THE WORKING TREE, NOT HEAD** — `bin/deploy.sh --apply` rsyncs the ENTIRE working tree; with parallel sessions live, foreign uncommitted changes leak. Commit-before-deploy (or `git status` + confirm nothing foreign dirty). Dirty shared tree → deploy SURGICALLY (file-scoped scp + diff verify) per the `coder` skill ("Dirty shared tree → deploy surgically", `4af2155` example).
- **🚨 COMMIT WITH A PATHSPEC, NEVER BARE `git commit` / `git add -A`** — the shared tree carries others' staged work; bare commit sweeps it in. Always `git commit -- <explicit paths>`; if entangled, `git reset --soft HEAD~1` then re-commit by pathspec.
- **Migration pre-flight** (SQL in scope): `migrate.php --status`; FK type == target PK exactly; MySQL-8 syntax (no IF NOT EXISTS on ALTER, no bare SELECT); FK data integrity; `schema_meta` row per NEW table; selective rsync. `schema_migrations` PK = `filename` (idempotency = filename match); `audit_row_revisions.action` ENUM = `('insert','update')` ONLY (DELETE = `action='update'` + `after_json='{"_tombstone":…}'` + full before_json). `maltytask_app` HAS CREATE VIEW (since 2026-05-26).
- **RULE 1 — architecture cleanliness + scrapping (EVERY build):** log what the change makes redundant in scrapping-backlog.md with a gate; retire only after the replacement covers it AND nothing routes to it.
- **RULE 2 — commit-stage code review is a HARD COMMIT GATE:** fresh-context Sonnet reviewer before EVERY commit auditing (a) correctness/SQL-safety/txn/XSS/auth/CSRF/log_revision/PRG/whitelist AND (b) dead-code/redundancy (feeds RULE 1). Multi-bundle bursts → ONE review over the COMBINED diff before any commit; single-owner rule for shared files (app.css = one agent).
- **RULE 3 — TOUR COVERAGE (when a build touches `ref_pages`):** every operator-visible page needs a Visite-guidée card (predicate `is_active=1 AND (domain IS NULL OR domain<>'admin') AND page_key<>'saisies'`). PM dispatches `maltyweb-tour-steward` (sonnet; EQUIP coder+ui+webapp-testing); it stops + flags `PM-RATIFY:` for the 4 sensitive classes (COGS/COP/WAC/BOM/beer-tax · identity/access · ambiguous · self-flagged). `scripts/tour-gap-check.php` is the auto-advisory inside migrate.php. PM OWNS dispatching the steward. → §VISITE GUIDÉE.
- **Migration-number pre-flight is the FIRST step** — VPS sequence can LEAD local git; re-`migrate.php --status` + `ls db/migrations` (parallel sessions hold un-applied/untracked numbers; the 278 duplicate proved it). Migrations NEVER self-INSERT into `schema_migrations`. Collision-rename = rename + schema_migrations UPDATE in ONE step + `rm` the stale old-name file on VPS (rsync does NOT delete removed files).
- **🔴 STANDING FORM FACT — operator fills saisie/brewing forms by the CORRIGER LOOP, never one pass** → the full POST handler re-runs on EVERY submit against PARTIAL data. (1) design any form POST-path query idempotent + partial-tolerant + NK-preserving; (2) SMOKE forms with the corriger LOOP (header→corriger→corriger), not a single happy-path. Read BEFORE building OR reviewing ANY form. Trigger "corriger"/"modifier"/"saisie form"/"incremental entry"/"Gonzalo"/"form POST"/"re-submit" → [saisie-forms-5-correction-arc/](maltyweb-pm-memory/saisie-forms-5-correction-arc/README.md) §STANDING OPERATIONAL FACT. EQUIP ui+coder+sql(+webapp-testing).
(Full conventions → conventions-and-helpers.md + CLAUDE.md + the skills.)

## ⏭ RESUME POINT (2026-06-05 — fermenting/saisie/tank-sim chain COMMITTED+PUSHED, maltyweb HEAD `815e725` / maltytask HEAD `dd6ca28`)
**🟡 WAITING ON OPERATOR.** The fermenting-correctness + saisie-edit + tank-sim + packaging-parallel-run chain is all SHIPPED+LIVE+PUSHED. Operator is in backlog-data-entry / production-use mode. Several PENDING BUILDs below are PM-blessed but NOT YET BUILT — dispatch when operator unblocks.

**STILL-OPEN / NEXT (one-line each; detail in the cited thematic file):**
BFD. **✅ BREWING-FORM INGREDIENT-LINE RUNAWAY — FIXED + DATA-CORRECTED 2026-06-19 (`5ad8929`).** `form-brewing.php` §5-§7 in `$ownTx` txn; hard-DELETE-then-INSERT kills positional append runaway; Speakeasy 67 header 1137 65→6. ✅ DERIVER ORPHAN-PRUNE LANDED (maltytask `3b21ba8`, `--apply` GATED; dry-run 9 brewing/0 fermenting orphans; Feb-2025 below fiche horizon → silent prune). → [consumption-cogs-derivation-wiring.md](maltyweb-pm-memory/consumption-cogs-derivation-wiring.md) §DERIVER ORPHAN-PRUNE. (Verbatim former detail → [index-relocated-detail/](maltyweb-pm-memory/index-relocated-detail/README.md).)
V1. **🟢 v1 `bd_*` TABLES DECOMMISSION — Stage 1+3 DONE; v1 FROZEN; only Stage 4 (DROP) remains** (gated on ≥1-2wk soak + final both-repo re-grep + operator go). bsf-ingest cron disabled; all readers repointed to `_v2` (COGS repoints Opus-verified). KEEP: bd_packaging_readings/bd_cip_events/bd_tank_readings. Stage-4 re-grep MANDATORY before DROP. → [v1-bd-tables-decommission.md](maltyweb-pm-memory/v1-bd-tables-decommission.md). EQUIP coder+sql(+ui+webapp-testing).
0a. **✅ CAGE ANCHOR LANDED + UNIT-MODEL DECIDED + SCOPE-POLLUTION FIXED — 2026-06-15** (migs 362+363, `e205522`+`80f36b9`; NOT pushed). 8 cages renamed `-X`→`X`; cage qty DECIMAL; cage-detection on canonical `stocktake_scope='cage'`. 🔑 cages tracked in CAGE-UNITS directly (1027 is a nominal label, NEVER a divisor — RETRACTS bottles÷1027). Scope: base 54/single 27/none 93/cage 8. June close needs a `month_end` cage census. → [sku-bom-validation-walkthrough/README.md](maltyweb-pm-memory/sku-bom-validation-walkthrough/README.md) §CAGE ANCHOR LANDED.
0a-cage-scope. **✅ `stocktake_scope='cage'` pollution cleared (mig 362, `e205522`)** — a claude-cli edit had set scope='cage' on 34 non-cage SKUs; DOCB/DOCF→base, 32 Contract→none; cage == exactly the 8 `-X`. Folded into §0a.
0a10. **✅ STOCK DRILL-DOWN render cluster SHIPPED+LIVE+COMMITTED** (`711905c`; segmented Stock/Activité toggle). 🟡 CSS fold-in debt gated on operator's expeditions.css overhaul. → [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) §FLIPPABLE DRILL-DOWN.
0a9. **✅ MULTI-SKU MOUVEMENT INPUT SHIPPED+LIVE+PUSHED** (`badd153`; all-or-nothing N `inv_stock_movements` rows, NO batch table, per-row log_revision). → [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) §MULTI-SKU MOUVEMENT INPUT.
0a6. **✅ TAPROOM SINGLE-UNIT-SKU GAP CLOSED + WEEKDAY-CHIP FIXED** (`afe1dcd`; DGDBU id=315 created; contract beers out of taproom). Shipped-broken nested-fn 500 → fixed by Kouros `ae297c2` (hoist `exp_dow_fr` top-level; DURABLE: PHP nested fns runtime-declared in call order + SMOKE deployed page). OPEN: DOCB base→cage confirm; move DGDB 21u stopgap into DGDBU via FG-stocktake EDIT. → [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) §TAPROOM SINGLE-UNIT-SKU GAP.
0a4. **🆕 WeeklyOrders SHEET → ord_orders INGESTION — PM RULINGS RECORDED (pre-build).** NB SUPERSEDED by 0a3 BC Phase-1 (which eliminates the manual WeeklyOrders importer). Full record → [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) §WeeklyOrders SHEET INGESTION.
0a3. **🆕 BC↔ord_orders ORDER INTEGRATION — PHASE 1 SCOPED + PM-RULED 2026-06-15** (Kouros 'on lance phase 1 pour sûr'). Pull OPEN B2B BC `salesOrders` → ord_orders, eliminates WeeklyOrders importer; ShpfyOrderNo non-empty EXCLUDED (no double-count); new `source='bc'`/`source_ref='bc:<No>'`; CRUX = pull is header/line refresher, NEVER writes status/status_events; SKU = `(Item_No,UoM_Code)` via ref_sku_aliases, refuse-don't-NULL. 🔴 OPUS-VERIFY no-double-deplete fg-stock invariant before done. EQUIP coder+sql+webapp-testing. → [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) §BC↔ord_orders PHASE 1. **NB: now part of the in-flight BC OPERATIONAL-LAYER PROGRAM — see the 🆕 PROGRAM banner above.**
0a8. **✅ THE 4 COGS TILES (#168/#169/#171/#172) LIVE — Option C executed+shipped.** Producer `scripts/build-cop-report.ts` (maltytask, MySQL-native) → `interfaces/cogs-report-data.json`; April COP complete (totalVariables 77169.37 / 88.76 CHF/HL). Durable fallback `scripts/php/ingest-charges-bc-csv.php` (use when Charges BC UI gated). 🔴 OPEN: automation/freshness-chip pending (manual regen, no cron); 2 scripts uncommitted. → [kpi-tracker-catalog/README.md](maltyweb-pm-memory/kpi-tracker-catalog/README.md) §OPTION C EXECUTED.
0a5. **✅ PER-FORMAT MAGNITUDE GUARD SHIPPED** (`f019526`; narrowed to cuv serving-tank fill-volume only; band from canonical ref_serving_tanks 50-30000 L; server re-evaluates per POST, never blocks). Upstream -V revert RESOLVED (see #0 🟢🟢). → [saisie-forms-5-correction-arc.md](maltyweb-pm-memory/saisie-forms-5-correction-arc/README.md) §MAGNITUDE GUARD.
0a11. **✅ SHOPIFY⇄MALTYWEB FULFILMENT INTEGRATION — FULL BUILD COMPLETE** (migs 321/322/323; Phase 0+1+2A LIVE, 2B disarmed; Σcards==Σphysique restored `d9f7976`). Ingest cron VPS-resident `*/1`+flock (`cdb19a5`, not pushed). ✅ DRAFT-ORDER SKIP-FILTER BUG fixed+closed (`b125e90`; historical backfill 351 draft orders, ~CHF 20.3k recovered; mig 406 stable line dedup `ee06469`/`a597ce1`). 🔴 open roadmap: review-bucket triage / orphan-line pruning / eshop SKU-bridge (PACKDEC/PBD). → [shopify-fulfilment-integration.md](maltyweb-pm-memory/fulfilment-expeditions-arc/shopify-fulfilment-integration.md). (Verbatim former detail → [index-relocated-detail/](maltyweb-pm-memory/index-relocated-detail/README.md).)
0a11b. **✅ ESHOP-VIEW HOIST HOTFIX — LIVE + GIT-CONSOLIDATED (`6995146`, not pushed)** — same nested-fn-not-hoisted 500 class; `exp_render_eshop_*` lifted to file scope; deployed surgically; Playwright-smoked. Vindicates the standing 'smoke after any nested/conditional-fn move' + 'deploy surgically under a dirty tree' rules. → [fulfilment-expeditions-arc/fg-stock-compute-bugs.md] / [index-relocated-detail/](maltyweb-pm-memory/index-relocated-detail/README.md) for verbatim.
0a7. **🆕 OPERATOR ATTRIBUTION STABILITY + GLOBAL EVENT-RECAP — PM-RULED (pre-build).** NB ASK1+ASK2 largely SHIPPED via migs 295/298 (see SHIPPED register §OPERATOR ATTRIBUTION). Trigger 'opérateur'/'attribution'/'Corriger'/'journal des saisies' → READ [operator-attribution-and-event-recap-arc.md](maltyweb-pm-memory/operator-attribution-and-event-recap-arc.md).
0a3b. **✅ TASK #10 STOCK PF BOARD SHIPPED** (`24d510a`; per-SKU anchor fix, `fg_stock_location_snapshot()`, `?view=stock` rebuild). Same-day cutoff prod/B2B legs closed (`566b9d3`/`d3fb954` + 2026-06-10 3-tier); eshop Task A (`2192153`). 🔴 CORRECTION 2026-06-17: the TRANSFER leg was NOT actually closed by Task A (its global-anchorDate prefilter survived) → FIXED 2026-06-17 via `$anchorFloor` (see top banner); the ESHOP leg still carries the SAME latent prefilter defect (separate symmetric fix, tracked). 🔴 STALE-INVARIANT note: Σcards==Σphysique now resolved by the 2026-06-12 anchor-tiebreak fix (see 0a11). → [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) §TASK #10 SHIPPED.
0a2. **✅ `inv_fg_stocktake.row_hash` STORED GENERATED column SHIPPED+RECONCILED** (mig 288; both FKs re-added ON DELETE RESTRICT; divergence structurally impossible). → [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) §row_hash divergence.
0. **🔑🏁 KEYSTONE — BOM VALIDATION: ①✅ ②✅ ③ F2 LIQUID✅ ④ DIRECT-SALES TRACKER MVP✅ — ALL FOUR DONE** (④='Tap&Shop', mig 291, `a13a9d0`; F2 applied 147 SKUs/0 parity; 2 conventions LOCKED liquid-CHF-canonical + slot_name=stage). 🟢🟢 COGS COST-SOURCE CUTOVER sheet→mysql FLIPPED+SHIPPED (maltytask `0129bdb`); -V cuv hl_per_unit 1.0→0.01 revert + compiler liner-branch per-unit generalisation CLEARED ZEPV gate (DURABLE: a COGS-bearing compiler line must be gated on the sales-material path not display; per-unit lines scale by SKU's own hl_per_unit, never hardcode). May-2026 mysql: 73 SKUs COGS 59 676.02 / NonBeer 31 617.70 / BeerTax 12 735.39. ④ Phase 2 (cage/COGS engine, #0b) DEFERRED — cage-anchor gate now CLEARED 2026-06-15 (migs 362/363, cages landed+clean, see #0a); now gated ONLY on Shopify pickup-signal (ON HOLD). PARKED residuals: 3 unknown sales SKUs; contract-beer null beer-tax (accountant sign-off). → [sku-bom-validation-walkthrough/README.md](maltyweb-pm-memory/sku-bom-validation-walkthrough/README.md) §F2 LIQUID APPLIED + §COGS COST-SOURCE CUTOVER. EQUIP coder+sql.
0c. **✅ RECETTE TEMPORAL VERSIONING (v2 SCD2 write-side) SHIPPED+PUSHED** (`a1c68dc`, mig 289; STORED gen-col `open_key` + UNIQUE `uq_rri_open`; close-then-insert on the 3 hop write-actions; ≤1 open row/slot + unlimited closed history). → memory/project_recipe_versioning_temporal.md.
1. **Per-recipe historical re-derivation for batches 5/63/169** (mig265 packaging parallel-run reattribution; net Neb HL unchanged). → [packaging-parallel-run-normalizer-defect.md](maltyweb-pm-memory/packaging-parallel-run-normalizer-defect.md).
2. **Tank-sim start_time DD/MM swap in RAW data** still wrong (sim masks via COALESCE). → [tank-simulator-occupancy.md](maltyweb-pm-memory/tank-simulator-occupancy.md).
3. **🔴🔴 Node tank-sim NAME-KEYING PORT DONE + COMMITTED (maltytask `0251394`) — surfaced a FIRST-CLASS COGS-STORE FINDING: `inv_tank_balances` has MASSIVELY OVERSTATED BBT/WIP across the ENTIRE history; NOT yet `--apply`'d, apply-scope WITH OPERATOR.** Root cause: old sim keyed packaging→BBT on `normalizeBeerName(neb_beer)` which passes SKU codes through → SKU-coded packaging never drained its BBT → phantom WIP. Closed months (incl. immutable May) computed on inflated WIP = OPEN CFO restatement item. REC: open-month-only apply + HOLD-AND-QUANTIFY closed overstatement as a CFO memo + harden the seed-closed guard to also refuse `cogs_fiche_monthly` months. → [tank-simulator-occupancy.md](maltyweb-pm-memory/tank-simulator-occupancy.md) §FIRST-CLASS COGS-STORE FINDING + financier-cogs-fiche.md §CLOSED-PERIOD WIP-RESTATEMENT.
3b. **✅ device-classification interstitial + VISITE GUIDÉE tour + tour-chapter expansion + TOUR-STEWARD SYSTEM SHIPPED** (`94d5a0d`/`ca65c22`/`91f8c82`/RULE 3; mig 280). ⚠️ uncommitted-but-live: visite-guidee.php (tap-shop+qa cards) + tour-gap-check.php + migrate.php advisory. STILL-OPEN: form-packaging.php helper-text no-DB-nomenclature violation (~L2451). → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md) §VISITE GUIDÉE.
4. **Auth + KPI backlog — 🏁 PHASE 2b KPI BUILD COMPLETE: 55→158 trackers live / 270 active** (103 built, 112 honest stubs = Phase 2c data-capture backlog). NEXT = Phase 2c data-capture (yeast gen/repitch/harvest + OG target + QC-coverage forms). KPI GATES open: qa_qc threshold validation; WIP #181 + tanks #13-17 + equipment #236 gated on Node tank-sim — CCT-dest fix now DONE+verified (#3) but still gated on the operator-approved `--apply` re-run landing corrected WIP into inv_tank_balances (+ the separate name-keying port); May COP after close. Auth residuals: invites Stéphane/Joelson; EUR→CHF 0.945 centralization; 2FA-TOTP; Tailscale /admin gate; PWA-ify. → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md) + kpi-tracker-catalog/README.md §PHASE 2b BUILD COMPLETE.
5. **Mother-shell atom 11** = operator production smoke + close-out → THEN pilots 5 (packaging) + 6 (brewing). Atom-11 smoke also = visual acceptance of sb-* gravure vessels. → [mother-shell-architecture.md](maltyweb-pm-memory/mother-shell-architecture.md) + [session-model-arc.md](maltyweb-pm-memory/session-model-arc.md).
6. **Validation / strain classification** (SDC Recettes) — operator data-entry, keeps the transfer gate Zepp-only until done. → [recipe-ingredients-drift-surface.md](maltyweb-pm-memory/recipe-ingredients-drift-surface.md) + [yeast-strain-taxonomy.md](maltyweb-pm-memory/yeast-strain-taxonomy.md).
6e. 🟢 **FULL RECIPE MODIFICATION-REQUEST FLOW — PHASE-0 LOCKED + FINAL BUILD PLAN READY 2026-06-19 (NOT built).** Managers FILE requests (admins stay direct); notify = in-app badge + email (test-mode first); scope INCLUDES formats/packaging-BOM (safe — request writes only the 2 request tables; admin approval replays through existing writers, capability gate on APPLY, live COGS delta at decision). New tables `recipe_change_requests`+`_lines` (mig 412). 🔴 KEY: relax `salle-de-controle.php` L128 ONLY for `submit_change_request`, never flip the per-action checks. SINGLE-WRITER file → sequential atoms. Trigger "modification request"/"recipe change request"/"demandes recette"/"recipe_change_requests"/"submit_change_request" → [recipe-ingredients-drift-surface.md](maltyweb-pm-memory/recipe-ingredients-drift-surface.md) §PHASE-0 LOCKED + FINAL BUILD PLAN.
6d. **✅ 'New Yeast' BBT/CCT board placeholder SHIPPED+DEPLOYED** (render-only derive-at-read via `app/yeast-eligibility.php` resolver; refuse-don't-invent; 3 files uncommitted per shared-tree discipline; NO mig). STILL-OPEN (gated): retire bogus ref_yeast_strains id=69 'New Yeast'; alias seeds for historical messy new_yeast. → [yeast-strain-taxonomy.md](maltyweb-pm-memory/yeast-strain-taxonomy.md) §OBSERVED-STRAIN DERIVE-AT-READ.
6b. **✅ BURST-BAG / PACKAGING-LIQUID-LOSS MIS-BOOKING SHIPPED+COMMITTED** (`3d96847`, mig 292; vendable floor + typed `loss_keg_liquid_l` + tank drain; +23.72 HL restored to packaged-HL + beer-tax base; 6 pure-loss rows backfilled). SUPERSEDED in part by #6c loss-model rewrite. → [packaging-losses-convention/burst-bag-liquid-loss-arc.md](maltyweb-pm-memory/packaging-losses-convention/burst-bag-liquid-loss-arc.md).
6c. **✅ LOSS-MODEL CONSISTENCY SHIPPED+COMMITTED** (`7a8b63a`, mig 293; liquid losses no longer subtract from vendable in ANY branch → sum into loss_kpi_hl; +540.8 HL restated to vendable+beer_tax_base). 🔴 CRITICAL SAVE: agent backfill over-reached into 57 contract rows (+858 HL) → Opus REVERTED. FOLLOW-UPS #1/#2 RESOLVED (20 Contract recipes → 28 new ref_skus at standard fmts; 🔴 162-row/2958 HL contract base needs accountant magnitude review before any contract Biersteuer declaration). STILL OPEN: 2 May rows 2198/2200 need alex to name beers. → [packaging-losses-convention/loss-model-consistency-arc.md](maltyweb-pm-memory/packaging-losses-convention/loss-model-consistency-arc.md).
6d. **🟢 PARALLEL-RUN "STIB shows loss, STI4 doesn't" — RESOLVED 2026-06-21 (`461867a`, pushed, LIVE).** Real issue = data-entry + MIS-LABELLED FIELD (`loss_untaxed_full_units` was labelled "Perte liquide autre" → operator typed 4 not 1), NOT a redistribution feature. Data fix id=6802 4→1; relabel →"Unité perdue (pleine)"; display-only recap grouping by (recipe,batch,date,fam) → both 0.18%. Heavyweight redistribute-counts-across-history build DEFERRED. 🔴 DURABLE: confirm FIELD SEMANTICS vs LABELS before building. Trigger "unité perdue"/"loss_untaxed_full"/"redistribute loss"/"parallel run loss"/"STIB STI4"/"recap grouping" → [loss-model-consistency-arc.md](maltyweb-pm-memory/packaging-losses-convention/loss-model-consistency-arc.md) §RESOLVED + §DEFERRED.
7. **Paused arcs:** A-LT2 data-driven-loss-types view-codegen (operator settled option B); packaging-page dashboard build-out. → [packaging-losses-convention/loss-types-and-form-changes-arc.md](maltyweb-pm-memory/packaging-losses-convention/loss-types-and-form-changes-arc.md) + [packaging-page-dashboard.md](maltyweb-pm-memory/packaging-page-dashboard.md).
8. ⚠️ Untracked `app/packaging-loss-types.php` = paused A-LT view-codegen scaffolding, NOT a divergence — leave or fold when A-LT resumes.
9. **🏁 UI/UX QUALITY REWORK ARC — FULLY CLOSED** (Phases 0-3 + C2; gravure = house sketch language; `app/svg-tanks.php` = THE canonical vessel library). OPEN = §RESIDUAL BACKLOG (4): bom-review polish, PF-04 focus-first-invalid-on-PRG, mon-tableau structural, deuteranopia chart-pair pass. → [ui-ux-quality-rework-arc.md](maltyweb-pm-memory/ui-ux-quality-rework-arc.md) §RESIDUAL BACKLOG.
10. **✅ MIG-278 COLLISION REMEDIATED** + all evening/overnight expeditions+auth+CRM arcs PUSHED (CRM bootstrap/Carnet/merge-pipeline/saisie-stock-hints/tour content all shipped). REMAINING = operator runbook (merge decisions click-through → flip is_active=1 → re-anchor). DURABLE: rsync deploy does NOT delete removed files (rm renamed migration's old filename on VPS); migration SQL must NEVER self-INSERT into schema_migrations. → [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) §2026-06-08 OVERNIGHT.

11. **✅ Upload-pipeline operator retry SHIPPED** (`285d5bd`/`121c629`; shared exec helper + flat retry endpoint w/ race-guard 409; ⚠️ not live-fire-tested). Deploy via `bin/deploy.sh --apply-pipeline` → /opt/maltytask-pipeline. → conventions-and-helpers.md §UPLOAD-PIPELINE RETRY.

12. **🏁 Ingestion validation gate + SIE/SIL energy web-path SHIPPED+E2E-VERIFIED** (migs 282/283/284; Drive sourcing RETIRED; NOTHING auto-writes — worker STAGES write-plan to `doc_invoices.delivery_write_plan`, operator validates → `--commit` replays via the one writer). SIE period = consumption-period (21 May → keyed 2026-04). → [ingestion-validation-gate-arc.md](maltyweb-pm-memory/ingestion-validation-gate-arc.md).
12b. **🏁 À-VALIDER → TRIAGE MERGE SHIPPED+LIVE-PROVEN** (`484cc6a`; triage `?tab=valider` tab + shared partial valider-queue.php; Lenzlinger double-write landmine cleared; Tinguely invoice validated end-to-end in prod). BACKLOG: bulk 'Tout valider' fan-out throttle; consolidate the 2 recap-shape sources onto `fetch_recap_for_invoice()`. → [ingestion-validation-gate-arc.md](maltyweb-pm-memory/ingestion-validation-gate-arc.md) §À-VALIDER → TRIAGE MERGE.

### PENDING BUILDs (PM-blessed, NOT YET BUILT — the fermenting-correctness chain's remaining links)
All three fold into the SAME fermenting-correctness build (same files: `fermenting-phase-submit.php` / `session-body-fermenting.php` / `fermenting-phase-*.php`; all share the recipe_id_fk re-key lesson). Full diagnoses + rulings → [saisie-forms-5-correction-arc.md](maltyweb-pm-memory/saisie-forms-5-correction-arc/README.md). NB: most of this chain WAS built+committed under HEAD `815e725` (see line-7 header close-out) — re-verify against live before dispatching; what remains is the cadence-gate + SDC cadence panel below.
- **PURGE-CADENCE GATE breaks edit + backfill** (`fermenting-phase-submit.php` gate) — 3 faults: runs in edit mode; compares `submitted_at` not `event_date` (backfill-hostile); keys `beer_raw` not `recipe_id_fk`. Fix A1-A4 read-side, NO mig (sql skill #28/#29). + Atom 2 = "Cadences de production" SDC panel (clone `update_cip_cadence`, 2 scalars: `fermenting_cadence.purge_after_days` + `packaging.min_days_after_racking`), NO mig. Detail in saisie-forms-5-correction-arc.md.
- **SESSION-PHASE-CACHE LAG** on NULL-linked backfill batches — `op_sessions.phase` cache lags event-derived phase → submit guard rejects. Fix = render-time direct UPDATE phase-sync (NOT `session_advance_phase` — strict single-step throws), guard `status='open'`, side-effect-free (racking eligibility keys on ColdCrash event not session.phase).
- **BEER-IDENTITY FRACTURE** read-side re-key (phase-detect / in-progress `<select>` / CCT firewall) on `(recipe_id_fk,batch)` not `beer_raw` (fragments across event types) + phantom-0-event-session suppression + Cold-Crash → checkbox (not dropdown). 216 (recipe,batch) pairs affected system-wide. Write NK untouched. sql skill #27.
- **🆕 FININGS INPUT FIELD on brewing and/or racking saisie forms** (operator decision at F2 apply, 2026-06-09) — finings (e.g. PROC_NAGARDO) are inconsistently recorded; operator wants them inputted on saisie so they become OBSERVED data (currently gap-filled from recette: DIV 60 g/brassin = 55.72 CHF/brassin). Until built, the recette gap-fill stands. EQUIP coder+sql+ui. → walkthrough §F2 LIQUID APPLIED (operator decision 2).
- **🆕 FAST-FOLLOW (RULE 1) — converge form-brewing's one-off recipe resolver onto canonical `resolve_recipe_id()`.** The 1055 hotfix (✅ SHIPPED+LIVE 2026-06-16, NOT committed — Kouros applied directly; form-brewing.php POST-side resolver: `GROUP BY name HAVING COUNT(*)=1` → plain SELECT + `fetchAll()` count!==1 throw, deterministic, NO ANY_VALUE, refuse-on-ambiguity preserved; surgical single-file rsync, md5 `d7c7cf3…` parity, php -l clean) left a SECOND recipe resolver in the tree. Converge it onto `app/recipe-resolver.php::resolve_recipe_id()` (canonical→alias→normalized, migs 219-225) so a contract-renamed recipe can't resolve in one path but not the other (silent FK fragmentation). EQUIP coder+sql. → saisie-forms-5-correction-arc.md §SAISIE BRASSAGE 1055 HOTFIX.

(Older detailed RESUME-POINT narration — the form-finalisation F-0/F-1 arc (substance lives in rm-retro-prereq-chain.md + user-auth-admin-arc.md, NOT the archive), the mother-shell atom-11 walkthrough checklist, the BSF-exit Phase-D follow-up backlog, the OPEN-ITEMS register of data-quality/RQ/scrap candidates, the WORT-CONTRACT backlog ruling — all COMPILED OUT to [build-history/archive/](maltyweb-pm-memory/build-history/README.md) PART 2 + the cited thematic files. The Phase-D follow-up backlog headline = remaining BSF-exit legs (FG/WIP/Sales/ChargesBC/Consumption_Parsed/BeerTypes/Deliveries-tab still read BSF) + RULE-1 scraps + no_basis cans → [phase-d-cost-source-swap.md](maltyweb-pm-memory/phase-d-cost-source-swap.md) §FOLLOW-UP BACKLOG. **🆕 2026-06-08: build-sales-cogs SKU-cost-map cutover (Sheet→ref_sku_bom, BSF-exit leg #8, operator-approved opt A) SPEC'd — PM VERDICT = GATED on F2 apply (today's liquid is mixed-currency EUR/CHF + un-blessed F2 revaluation; Sheet stale, run-month-close step-3 broken). Plan: write lib/repos/mysql-sku-bom.ts + `--cost-source=diff` NOW [no COGS change], cut over post-F2. → phase-d §build-sales-cogs SKU-cost-map cutover.**)

## 📍 CURRENT BUILD-STATE (one-glance)
- **Canonical store:** MySQL `maltytask` (since 2026-05-12). VPS `/var/www/maltytask` (`ssh maltyweb`); local `/home/kluk/projects/maltyweb`. PDO bootstrap = `/var/www/maltytask/app/db.php` → `maltytask_pdo()`.
- **Migrations:** MIG HEAD = **425** (2026-06-21; parallel sessions LEAD the number). **ALWAYS re-`migrate.php --status` + `ls db/migrations` at build-start** — the VPS sequence can LEAD local git and parallel sessions run live; next-free is whatever `--status` shows, never a memorized value. Full per-migration ledger → [build-history/changelog-2026-06/](maltyweb-pm-memory/build-history/changelog-2026-06/README.md) + [derivation-tree-and-schema.md](maltyweb-pm-memory/derivation-tree-and-schema.md) §SQL BUILD SCHEMA. DURABLE: rsync deploy does NOT delete removed files (rm renamed-migration old filename on VPS); a migration SQL file must NEVER self-INSERT into `schema_migrations`.
- **`ref_sku_bom`:** ✅ **F2 LIQUID APPLIED 2026-06-09** — post-apply census: Brewing/`bom_source='liquid'` **1557 (all currency='CHF', the new F2 lines)** + Packaging/`packaging` 576 + Brewing/`composite_liquid` 204 + Packaging/`composite_packaging` 83 + Brewing/NULL 7 (out-of-scope SKUs the liquid compiler doesn't cover, cosmetic). NULL mi_id still 0 DB-wide; `source='mi_match'` = 0 DB-wide. **Liquid rows now CHF-canonical** (cost=CHF, FIXES the old EUR-summed-as-CHF latent bug); `slot_name` carries the STAGE for liquid ('brewhouse'/'dry_hop'). Apply via `compile_sku_bom_liquid()` apply branch (packaging/composite parity gate 1e-6-or-rollback, idempotent). packaging recompute (`compile_sku_bom_packaging`) + composite fold (`compile_sku_bom_composite`) both LIVE; 30 bindings; 6 `sku-bom-unresolved` + 5 `-4` carton-MI RQ rows. (Prior pre-F2 packaging census 576/2001.4473 = Neb-only byte-stable anchor; 576 packaging rows untouched by the F2 apply.) Detail → walkthrough §F2 LIQUID APPLIED + packaging-bom-model/README.md / sku-decomposition-tree.md.
- **`ref_sku_composite_slots`:** 19 rows / 4 SKUs (PD8/XMAS/PAL/PAC, all member_format_id=5 BU 33cl); compositeness = slot membership (NO `is_composite` col). PACKDEC/PAD → PD8 alias.
- **Commit chain on `main`** (full per-commit narration): → [build-history/](maltyweb-pm-memory/build-history/README.md). All pushed to origin/main through the heads in the header line.
- **volume_hl coverage:** 105 distinct SKUs (DONE) — bot/can/can33 container slots; keg/cuv/composite SKU-level via `v_sku_volume`/member-sum. → volume-dimension.md.
- **Stage-2 / ④ direct-sales tracker** — **Phase 1 MVP ("Tap&Shop") ✅ SHIPPED 2026-06-09 (mig 291, `a1c68dc`→`a13a9d0`); Phase 2 cage/COGS engine DEFERRED, gated on Shopify pickup-signal + cage anchor 2026-06-15.** → RESUME #0b + derivation-tree-and-schema.md + sales-commercial-surface/README.md.

## ✅ SHIPPED-ARCS REGISTER (one-line pointers — full detail in the cited thematic file)
_Each arc below is COMPLETE (shipped+live, mostly committed+pushed). The inline narration was compiled out 2026-06-05; the verbatim former blocks are in [build-history/archive/](maltyweb-pm-memory/build-history/README.md) (consolidated 2026-06-19; PART 1 = last-updated, PART 2 = resume-point) (and the per-session changelogs). Read the thematic file when touching that surface._
- **READ-ONLY LOOKUP MODULES (packaging + brewing) + NEB/CONTRACT SKU FILTER** (`e83c4d1` panels; brewing folded into wort.php Consulter tab mig 428; NO writes, NO fiscal lane) — shared `partials/lookup-panel.{php,js,css}` + per-page `/api/<page>-lookup.php`; brewing matches `(recipe_id_fk,batch)`, OG-not-FG, v2-only. 🟢 **Neb/Contract SKU filter + lookup-card redesign SHIPPED+LIVE+VERIFIED 2026-06-21** (`7c3392c`, origin `f8ed60a`, Playwright): canonical SKU classification = `ref_skus.recipe_id→ref_recipes.classification` (🔴 NOT ref_beer_types — name-keyed beer-tax SoT; 6 composites NULL→'Neb'; LEFT JOIN+COALESCE always); NEW accessor `app/sku_catalog.php`; NEW generic per-user pref store mig 430 `user_ui_prefs`+`app/user-prefs.php`+`public/api/ui-pref.php`; shared 3-way `Toutes|Nébuleuse|Contract` control (default Neb per-user); wired PF board + both lookups; saisie dropdowns NOT filtered; 🔴🔴 DISPLAY-LAYER ONLY never in COGS/compute. Trigger "lookup panel"/"consulter un brassin"/"packaging-lookup"/"brewing-lookup"/"Neb vs Contract"/"sku_catalog"/"user_ui_prefs"/"classification filter" → [read-only-lookup-modules.md](maltyweb-pm-memory/read-only-lookup-modules.md).
- **PLANNING PAGE — `/modules/planning.php` (Phase 1 calendar + Phase 2 eligibility)** (mig 364; `da202a9`; SHIPPED+COMMITTED+LIVE 2026-06-15). Engine `app/planning-eligibility.php::planning_week_eligibility()` is PURE-READ (CARDINAL — zero writes; forward-replay over TankSimulator snapshot + in-plan items; racking unlocks packaging; server re-checks on POST). Trigger "planning"/"eligibility engine"/"pl_plan_days"/"pl_plan_items"/"week calendar"/"forward-replay"/"planifier" → [planning-page-arc.md](maltyweb-pm-memory/planning-page-arc.md).
- **QA / HACCP QUALITY-CONTROLS — `/modules/qa.php`** (migs 358/359/360 source-class tables + 361 page-activate; `0b52f13`…`8734849` + tour `f8478da`; SHIPPED+LIVE 2026-06-15, LOCAL-only). 🔴 NON-FISCAL observation layer — NEVER feeds COGS/stock/WAC/BOM/beer-tax; dropdowns derive by FK from canonical tables. 4th panel "Analyse de l'eau" (CCP-1, migs 403/404/405, `70cc016`) shipped 2026-06-19. Trigger "qa"/"HACCP"/"CCP"/"net content"/"cleaning efficacy"/"bottle reception"/"analyse de l'eau"/"ref_water_parameters"/"qa_water_analysis"/"CAPA-2026-005" → [qa-haccp-controls-arc.md](maltyweb-pm-memory/qa-haccp-controls-arc.md).
- **OBSERVED YEAST ON BBT + CCT CARDS — ✅ SHIPPED + DEPLOYED + SMOKE-VERIFIED 2026-06-11 (render-only; UNCOMMITTED per operator; NO mig, NO writes).** Both boards surface observed yeast strain+gen from `bd_brewing_brewday_v2` as a guarded muted chip "Levure · {strain} (G{gen})", refuse-don't-NULL on empty. INDEPENDENT of the paused strain-classification arc (reads OBSERVED, not `ref_recipes` link; observed-wins). **The earlier verbatim-copy BBT chip was SUPERSEDED 2026-06-11 by the single-homed derive-at-read resolver in `app/yeast-eligibility.php` (`yeast_observed_strain_expr` + `resolve_observed_yeast_strain`) — handles the `yeast='New Yeast'` sentinel via coalesce→new_yeast then `ref_yeast_strains`/`ref_yeast_strain_aliases` resolution; no more "New Yeast" leak. CANONICAL DETAIL → [yeast-strain-taxonomy.md](maltyweb-pm-memory/yeast-strain-taxonomy.md) §OBSERVED-STRAIN DERIVE-AT-READ (and index entry 6d).** → also [packaging-page-dashboard.md](maltyweb-pm-memory/packaging-page-dashboard.md) §OBSERVED YEAST ON BBT CARDS.
- **BBT/CCT CIP-CADENCE INFRA — RESOLVER BUILT, NOT YET ON BBT CARDS** (migs 175/176/177/213/230; verified live 2026-06-11). `app/cip-cadence.php::cadence_for_all_bbts(PDO)` = pure-read per-BBT cadence state (last full/acid CIP, racks-since, recommended action, severity); thresholds in `commissioning_settings` §cip_cadence; surfaced today only as `window.BBT_CIP_CADENCE` in the racking form. GAP = make BBT cards (packaging.php) clickable + inject the resolver + a BBT detail modal mirroring the CCT pattern in tanks.php; NO new table/threshold store. ⚠️ some `bd_cip_events.cip_date` future-dated (input issue). → [cip-module.md](maltyweb-pm-memory/cip-module.md).
- **BC SALES LEDGER LIVE/AUTOMATED — OData CONNECTOR** (maltyweb `c017bc9`, mig 341, cron every 4h; SHIPPED+LIVE 2026-06-12) — `scripts/python/ingest_bc_sales_ledger.py` is the sole going-forward writer for `inv_sales_ledger` (S2S OAuth2, `config/bc.env`), superseding the manual xlsx loader. 🔴 Incremental = ANTI-JOIN on `bc_line_seq`, NOT a MAX(Entry_No) watermark (BC posts non-chronologically — Entry_No holes below the high-water mark). INSERT-only, no COGS valuation coupling. Trigger "BC sales ledger"/"OData connector"/"inv_sales_ledger"/"Entry_No"/"anti-join" → [sales-commercial-surface/README.md](maltyweb-pm-memory/sales-commercial-surface/README.md) §BC SALES LEDGER WENT LIVE/AUTOMATED.
- **SINGLE-SOURCE COGS REPOINT + FINANCIER PAGE v1 + BOARD COGS/COP P&L GRID** (migs 300/301/304/327; `489ab70`/`6a013ad`/`f1ff3a1`/`e72e64e`/`d0c7e60`/`b477565`; SHIPPED+LIVE+PUSHED) — `loadSales()` reads ONLY `inv_sales_ledger` (single source, retiring the bc+orders union); `financier.php` read-only finance hub (id=239, manager/general) consuming COP+COGS JSON, never recomputes; SKU-cost list inlined into Financier tab + sku-costs.php retired (mig 318); board COP/COGS P&L grid (per-GL-line actuals from `inv_charges_bc` + `ref_budget_cogs` 75.76 CHF/HL budget, refuse-don't-guess placeholders). DURABLE: `.fin-panel{display:flex}` overrode `[hidden]` (CSS specificity) → manager-auth full-page SCREENSHOT is the load-bearing UI gate on a role-scoped page. OPEN: /financier tour card (dirty-tree deferred); N-1 2025 GL ingest. → [sales-commercial-surface/README.md](maltyweb-pm-memory/sales-commercial-surface/README.md) §SINGLE-SOURCE REPOINT + §FINANCIER + §BOARD COGS/COP P&L GRID.
- **OPERATOR ATTRIBUTION + JOURNAL DES SAISIES** (migs 295+298; `c8e895c`+`5b918f3`; SHIPPED+LIVE 2026-06-09) — durable FK `submitted_by_user_id_fk` on all 7 bd_*_v2 tables (set-once at INSERT, edit path preserves original submitter) + read-only Journal des saisies page (VIEW `v_saisie_events` UNION over 7 tables, 20s live auto-refresh, `<dialog>` drill-down + audit tail). COGS-neutral + SOT-safe. Trigger "opérateur"/"attribution"/"journal des saisies"/"v_saisie_events" → [operator-attribution-and-event-recap-arc.md](maltyweb-pm-memory/operator-attribution-and-event-recap-arc.md).
- **④ DIRECT-SALES TRACKER MVP "Tap&Shop"** (mig 291, `a13a9d0`; SHIPPED+LIVE+PUSHED 2026-06-09) — read-only logistics page `tap-shop.php` (ref_pages id=186): Shopify+taproom sales per beer/SKU + virtual consumption stock via a single `fg_stock_compute()` call; WRITES NOTHING. Phase 2 (cage/COGS engine) DEFERRED, gated on Shopify pickup-vs-shipped signal + cage anchor. → [sku-bom-validation-walkthrough/README.md](maltyweb-pm-memory/sku-bom-validation-walkthrough/README.md) + [sales-commercial-surface/README.md](maltyweb-pm-memory/sales-commercial-surface/README.md).
- **RECETTE TEMPORAL VERSIONING v2 (SCD2 write-side)** (mig 289 `289_rri_scd2_open_uniqueness.sql`, commit `a1c68dc`; SHIPPED+VERIFIED+PUSHED 2026-06-09) — recette hop edits in `salle-de-controle.php` now close-then-insert into `ref_recipe_ingredients` instead of mutating in place, so gap-fill/COGS resolve recette-at-brew-date. Mig 289 (Option A) added STORED gen-col `open_key` + DROP `uq_recipe_mi_stage` + ADD UNIQUE `uq_rri_open` (the textbook close-then-insert was blocked by the dates-not-in-key UNIQUE → 1062; this was the REQUIRED migration the old plan didn't foresee). `rri_close_version()` helper in `app/recipe-ingredients-loader.php`; read-side predicate unchanged; 164 rows backfilled effective_from='2025-01-01'. Only the 3 hop write-actions write `ref_recipe_ingredients` today; helper reusable for a future generic qty editor. OUT: SDC version-history UI; form-fermenting dry-hop as-of; v3 retrofit. ZERO `ref_sku_bom` impact (2427 untouched). → [project_recipe_versioning_temporal.md](/home/kluk/.claude/projects/-home-kluk-projects-maltytask/memory/project_recipe_versioning_temporal.md).
- **INGESTION VALIDATION GATE + SIE/SIL ENERGY WEB-PATH** (migs 282/283/284; A=maltytask `c80f6f8`/maltyweb `fd1c5ed`, B1a=maltytask `8c991a6`/maltyweb `d894b6f`, B1b=maltyweb `6973316`, B2=maltyweb `e0e580d`; SHIPPED+LIVE+E2E-VERIFIED 2026-06-08) — worker no longer auto-writes inv_deliveries: STAGES the gate's derived write-plan → `doc_invoices.delivery_write_plan` JSON + energy → `energy_extract` JSON; operator validates via « À valider » module (`invoice-validate.php`, ref_pages id=99) OR upload-page recap (`app/recap-lines.php` + triage-upload.js) → new `--commit <uploadId>` worker mode replays the plan through the SAME `appendDeliveryToMysqlWithConn` loop (one writer, zero COGS-fork — the WORKER-COMMIT-MODE ruling held). Drive sourcing RETIRED (ingest-sie-invoice `--local`-only; build-cogs-report hook removed). SIE period convention = consumption-month not billing-date (8906→2026-04). Sudoers www-data→maltytask gap closed. ⚠️ One untested seam: live browser Valider-click. → [ingestion-validation-gate-arc.md](maltyweb-pm-memory/ingestion-validation-gate-arc.md) §SHIPPED RECORD.
- **VISITE GUIDÉE — first-login onboarding tour + TOUR-STEWARD SYSTEM** (`ca65c22` + mig 280 `users.tour_seen_at`; chapter expansion `91f8c82` SHIPPED+LIVE+PUSHED 2026-06-08; **tour-steward governance system SHIPPED 2026-06-09 on ratified terms, NOT YET COMMITTED**) — `/modules/visite-guidee.php` (require_login only, NOT in ref_pages), steps server-built from `ref_pages × user_can_access()`; production presets = 25 steps (Brassage 3/Fermentation 2/Transferts 3/Conditionnement 3 + capstone "La chaîne des pertes"), logistics = 11; content map pure PHP page_key+sub-index (no DB/mig); default landing mon-tableau, exactly-once auto-redirect via tour_seen_at, topbar re-launch link, grid-overlay + compact-dot mode >20 steps; no-DB-nomenclature/threshold vetoes grep-verified on rendered HTML.
  **TOUR-CARD PREDICATE** (what "this page needs a tour card" means, = visite-guidee.php's own filter minus saisies): `is_active=1 AND (domain IS NULL OR domain <> 'admin') AND page_key <> 'saisies'` (admin excluded; `saisies` special-cased into its own opener + form chapters, not a `$PAGE_DESCRIPTIONS` entry).
  **LAYER 1 — DETECTION (NO git hook, NO marker file, by design):** `scripts/tour-gap-check.php` (note: `scripts/` not `bin/` — repo convention mirrors `scripts/reconcile-pages.php`) — read-only drift detector, live `ref_pages` MINUS the THREE content maps in visite-guidee.php (`$PAGE_DESCRIPTIONS` mandatory / `$PAGE_ICONS` recommended / `vg_vignette_for()` optional); block-scoped text parse (file can't be include'd — login side effects). Tiers: CRITICAL=no description / MINOR=no icon / INFO=no vignette / LATENT=inactive-no-card; exit 1 on CRITICAL|MINOR, INFO/LATENT advisory; `--quiet`. Run: `ssh maltyweb "sudo php /var/www/maltytask/scripts/tour-gap-check.php"`.
  **LAYER 1 — AUTO-TRIGGER:** `scripts/migrate.php` post-apply advisory — if any just-applied migration's SQL contains `ref_pages`, passthru-shells `tour-gap-check.php --quiet` and prints a WARNING on gap; NEVER blocks / never changes migrate.php exit code.
  **LAYER 2 — THE AGENT:** `/home/kluk/.claude/agents/maltyweb-tour-steward.md` (sonnet, EQUIP coder+ui+webapp-testing) — codifies the six mechanical copy rules (house voice / no-DB-nomenclature grep-verified / no-thresholds / no-future-promises / bespoke SVG / optional vignette) + the four mandatory-ratification carve-outs (COGS-COP-WAC-BOM-beertax · identity/access · ambiguous-purpose · self-flagged-uncertain). KEY IMPL DETAIL: a subagent can't call another subagent, so the steward does NOT consult PM directly — for a sensitive-class page it DRAFTS, STOPS pre-deploy, returns the draft flagged `PM-RATIFY: <page_key>`; the orchestrator (Opus) routes it to PM and relays the ruling. Non-sensitive pages: steward drafts-to-rules + deploys + smokes autonomously. Includes no-commit + no-dirty-tree-deploy (selective rsync) guards.
  **COVERAGE STATE (live gap-check 2026-06-09):** `critical=0 minor=0 info=8 latent=0`, exit 0. tap-shop (active) + qa (inactive, pre-written) both fully covered. The 8 INFO = active pages w/ description+icon but default vignette (cosmetic backlog, no action). `zeppelin` left as-is per ruling.
  **⚠️ NOT YET COMMITTED (2026-06-09):** three files deployed-live-but-uncommitted in the maltyweb working tree — `public/modules/visite-guidee.php` (tap-shop+qa cards), `scripts/tour-gap-check.php` (NEW), `scripts/migrate.php` (advisory). Agent file is in ~/.claude (separate). Awaiting Kouros's commit decision — this is a live dirty-tree hazard per checklist §line 14: a `bin/deploy.sh --apply` from this tree leaks all three (plus any other session's dirty files).
  **PM OWNS dispatching the tour-steward after any `ref_pages`-touching build** (RULE 3). Standing convention: new ref_pages entry → bespoke tour content row. DEFERRED POLISH: zeppelin per-family depth (low payoff); the 8 INFO default-vignette pages (cosmetic). SEPARATE flagged bug (NOT this work): form-packaging.php ~L2451-2452 helper text leaks raw `prod_total_units`/`qte_unites` to operators. → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md) §VISITE GUIDÉE.
- **FIRST-LOGIN DEVICE-CLASSIFICATION INTERSTITIAL** (`94d5a0d`, ZERO mig, SHIPPED 2026-06-07 night; as-built debrief vs spec pending) + **KPI RECAP CRON ACTIVATED** (`1d8672b`, operator confirmed inbox — user-account arc's last build DONE). → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md).
- **UI/UX QUALITY REWORK — ARC FULLY CLOSED, Phases 0+1+2+3+C2+calibration** (commits `9104273`/`01c99e3`/`9dcd4ca`/`cd96cd4`/`9ed0f6f`/`49981f4`/`c31163f` + Phase 3 `e05b18c`/`e679efa`/`aba178a`, ZERO mig, SHIPPED+LIVE+PUSHED 2026-06-07) — P0–P3 audit (35 findings) + shared-layer fixes site-wide + per-page polish (Phase 3: 3 parallel bundles + combined-diff pre-commit RULE-2 review, 2 P1s fixed pre-commit); svg_cct/svg_bbt + SdM scene + all four `svg_vessel_*` rebuilt GRAVURE DENSE; **`app/svg-tanks.php` = the ONE canonical vessel library** (svg-vessels.php tombstoned → scrapping-backlog #31). Residual = 4-item backlog incl. the bom-review post-F2 carve-out (→ arc file §RESIDUAL BACKLOG); atom-11 smoke = visual acceptance of sb-* gravure. → [ui-ux-quality-rework-arc.md](maltyweb-pm-memory/ui-ux-quality-rework-arc.md).
- **MON TABLEAU v2** (commits `2a8f2b5`→`8800a9e`→`db2da33`→`b66244a`→`e8f5336`, mig 278_kpi_seed_v2_batch, SHIPPED+LIVE 2026-06-07 evening) — dense 4-col grid (viz_type span map, grid-auto-flow dense) + category-grouped dashboard/picker (one $CATEGORY_ORDER source) + fr-CH/unit-aware `fmt()`; 21 new production handlers (racking 7 / packaging 11 vendable-model / tank readings 2 / wort yoy) + REAL MP days-of-supply (v_rm_stock_dynamic ÷ trailing-90d inv_consumption, Opus-verified 147.0 j) + maintenance GL-6100 own series; honesty sweep 28/43 honest, relabels 111+266, 13 stub-mismatches→dr=0 (admin notice clear); **51 trackers live** (census 51/136/81); 15 sub-pages ACL-gated; recap-guard session-payload defect #3 fixed (DB-read convention). Opus V1 verification EXACT to the decimal. → [kpi-tracker-catalog/README.md](maltyweb-pm-memory/kpi-tracker-catalog/README.md) §2026-06-07 EVENING + user-auth-admin-arc.md §2026-06-07 EVENING.
- **P2b — KPI PERSONAL DASHBOARD** (commits 99d5d54 + 2c37c9d, SHIPPED+LIVE 2026-06-06, ZERO mig) — `public/modules/mon-tableau.php` (min_role=viewer, registered in `ref_pages` via idempotent upsert) reads `user_kpi_selections` → `kpi_dispatch()` per tracker → renders via the NEW shared `public/js/kpi-charts.js` (`window.KpcCharts`, 9 viz_types, EXTRACTED from `wort-kpis.js`; wort.php loads kpi-charts BEFORE wort-kpis, both `defer`, wort unchanged). Tracker picker server-filtered domain×role×data_ready; POST rebuilds allowed-set server-side (`mt_build_allowed_set`) + REFUSES out-of-scope (forged viewer→manager-tracker POST rejected); CSRF+log_revision+PRG; NO new compute path. 4 `utilities` data_ready=1 trackers → STUB handler detected/excluded/admin-noticed (constraint #1). Feed-2 WAC-divergence sub-flag added to `bom-review.php` (0 rows today). Role-filter: viewer=0 / manager=30 / admin=39 selectable — **viewers get EMPTY dashboard (no data_ready viewer-tracker; SEED-TUNING flag for next seed-touch, not a bug)**. NEXT = P3 recap emails (WAITS ON ONBOARDING). → [kpi-tracker-catalog/README.md](maltyweb-pm-memory/kpi-tracker-catalog/README.md).
- **P2a — KPI FRAMEWORK** (migs 269/270, commit 0a99b8b, SHIPPED+LIVE 2026-06-06) — `ref_kpi_trackers`(268 seeded: 43 data_ready, 6 hero) + `user_kpi_selections`(sparse, CASCADE FK), schema_meta'd. `app/kpi-handlers.php` = blessed thin-handler registry dispatching on `source_domain` w/ whitelisted params (NO free-SQL-in-DB). IMPLEMENTED: wort (Cooling-row canonical) / cogs (consumes pipeline JSON, never recomputes) / ops_health / rm_procurement; STUBBED return `stub:true` (never error): tanks/racking/packaging/fg_stock/sales/qa_qc/equipment/logistics. Honors all KPI divergence flags. NEXT = P2b dashboard (SHIPPED). → [kpi-tracker-catalog/README.md](maltyweb-pm-memory/kpi-tracker-catalog/README.md).
- **BOM REVIEW SURFACE — ARC CLOSED** (`7730bc4`+`43022d7` + WAC-divergence guard; SHIPPED+LIVE 2026-06-06; `bom-review.php` 2-tab, admin-gated). EDIT ONLY at binding/per-SKU-choice/ref_mi-price altitude (NEVER `ref_sku_bom` — recompile clobbers). Browse tab groups all active SKUs by classification→subtype; snapshotted cost, no recompute on render. ⚠️ `sdc_recompile_recipe_packaging()`+`sdc_flash_bom_result()` canonical home is now `app/sku-bom-compile.php` (function_exists-guarded), required by both pages. → [packaging-bom-model/README.md](maltyweb-pm-memory/packaging-bom-model/README.md) §SHIPPED BROWSE.
- **DIV33C CAN-FIX — durable altitude** (SHIPPED 2026-06-06, ZERO mig) — fixed via Tier-1 per-SKU choice `ref_sku_packaging_choices` id=11 (sku_id=14, slot='can', mi_id_fk=617 PKG_CAN_ALU_33), NOT a binding repoint. Binding id=30 LEFT at 616 (DIV33C is sole Diversion can SKU; flagged to repoint→PKG_CAN_DIV id 626 only if a 50cl Diversion can is ever packaged). inv_consumption corrected (total preserved). +3 WAC-priced PKG_*_NOMOQ. Recompile-idempotent, parity 0. → [packaging-bom-model/README.md](maltyweb-pm-memory/packaging-bom-model/README.md) + [sku-decomposition-tree.md](maltyweb-pm-memory/sku-decomposition-tree.md) (`ref_sku_packaging_choices` is the Tier-1 override layer).
- **USER-ACCOUNT DEEPENING — PHASE 1 PER-PAGE ACCESS CONTROL** (migs 266/267/268, commits c065743/c747006/2466869, SHIPPED+LIVE 2026-06-06) — `ref_pages`(18, retired hardcoded `$modules`)/`ref_access_presets`+`ref_access_preset_pages`+`users.access_preset_id_fk`(3 presets)/`user_page_access`(sparse override); `app/auth.php` `user_can_access()`/`require_page_access()` (admin bypass→role floor→per-user override→preset→NULL-preset fallback; preset carried in session 4-site incl. remember_token); `reglages-generaux.php` access admin (sec=access). Phase 2 = KPI dashboard. → [kpi-tracker-catalog/README.md](maltyweb-pm-memory/kpi-tracker-catalog/README.md).
- **USER-AUTH / ADMIN / MANAGER-SCOPE / EMAIL / SHARED-DEVICE** (migs 261/262/264; HEAD `993d5ae`+post-ship fixes) — prod-vs-logistics `manager_scope`+`manager_can()`; `user_invites`/`set-password.php`/`mailer.php` (Postfix→IONOS); `brewery_identity()` DB-sourced footer; `auth_shared_devices` remember-me suppression. ROBUST LOGIN RESOLVER `auth_resolve_user()` (full-username|first-name|email, accent-insensitive, ambiguous→fail-closed; `d50ec4d`) + both follow-ups shipped (`1fa4bcb`/`2cd2b24`). SALES_MANAGER preset (id=9, mig 324) for Louis Maechler. → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md).
- **SAISIE-FORMS 5-CORRECTION BATCH** (mig 258, commits `68ae0b4`/`039d4e3`/`b42e8b7`/`619b422`) — racking flowmeter→racked_vol; reusable `multi-submit-reads.js` (serialize/sum/average) for co2o2 reads + turbidity avg + all pertes; qa_analyses auto-derived read-only (reversed the never-derive ruling); INVARIANT 13/13 PASS. → [saisie-forms-5-correction-arc.md](maltyweb-pm-memory/saisie-forms-5-correction-arc/README.md).
- **RM PER-PALLET MULTI-LINE FORM** (mig 252, `c2aed24`) — type-ahead MI → bare-qty → instant per-pallet POST → server SUMs → live ledger; `inv_rm_stocktake_lines` child → `rm_recompute_rollup` → `inv_rm_stocktake.counted_qty`. ⚠️ RM-ONLY (brewing/racking/packaging stay single-summed). NULL-vs-0 invariant load-bearing. Archetype detail in this register's snapshot + conventions-and-helpers.md.
- **FORM-RESILIENCE ARC** (`3d7afce`, ZERO mig) — `session-ping.php` (401-JSON keepalive) + `form-resilience.js` (visible-tab keepalive + csrf-rewrite + localStorage autosave) + global topbar wiring. RM form's layers 2/3 later superseded by the per-pallet rewrite. Layers 2/3 for brewing/racking/packaging DEFERRED.
- **PACKAGING PERTES MODEL — CANONICAL** (ruled 2026-06-01) — beer-loss = `ref_packaging_loss_types counts_as_loss=1` dispositions in HL; CONSUMABLES ≠ canonical loss. Dashboard loss-% MUST add the legacy `loss_liquid_other_units/100` term (else 10× understated; the A3 mig-233 view DROPPED it). Beer-loss% ~1-2% from 2023. → [packaging-losses-convention.md](maltyweb-pm-memory/packaging-losses-convention.md) §PERTES-DASHBOARD.
- **UNIFIED PARALLEL-RUN FIX** (migs 250/251, `c355cf8`) — every packaging row (main|parallel) computes vendable from its OWN `prod_total_units`; `special_qty_units` retired to provenance/consumption-key only (NEVER summed into vendable/tax). Reconciled EXACT: keg 2828.2 / bot 1834.72 / can 363.60 / cuv 315.00. "Missing keg / cuv variable-fill" framings RETRACTED. → [packaging-page-dashboard.md](maltyweb-pm-memory/packaging-page-dashboard.md) §PARALLEL-RUN FIX.
- **PACKAGING PAGE DASHBOARD BUILD-OUT** (consulted, dashboard partly built) — `packaging.php` → furnished year-filtered KPI dashboard; canonical source `bd_packaging_v2` ONLY; Neb-vs-Contract = `recipe_id_fk → ref_recipes.classification` (100% cov, NOT sku_id_fk); format = `run_type` (5 vals bot/can/can33/keg/cuv); GROSS output = prod_total×vol (not vendable_hl). → [packaging-page-dashboard.md](maltyweb-pm-memory/packaging-page-dashboard.md).
- **PACKAGING event_date BUG FIXED** (mig 235) — `event_date := DATE(submitted_at)` for the WHOLE bd_*_v2 family (never last_cip_date, never v1-JOIN). → derivation-tree-and-schema.md §EVENT-DATE RULE.
- **V2-ONLY SOURCE ARC** (`08c00de`+`16079a2`) — maltyweb reads `bd_packaging_v2` ONLY; cross-source v1+v2 dedup DROPPED (was the BBT7 double-count). CANONICAL v1/v2/RawDB architecture: v1=still-LIVE old DB (maltytask wired); v2=[v1@migration normalized]+[web-entry backlog]; RawDB.xlsx=FROZEN → re-normalize is OFF the table (clobbers web_entry), fixes are surgical SQL. → [derivation-tree-and-schema.md](maltyweb-pm-memory/derivation-tree-and-schema.md) §V2-ONLY SOURCE RULE + §CANONICAL v1/v2/RawDB ARCHITECTURE.
- **LINER-MI-DROPDOWN ATOM** (mig 239, `63977f6`) — liner client+transport = DROPDOWN of liner MIs (FK→ref_mi via ref_mi_subcategories 'Liner'), not Oui/Non; bool-set=legacy, FK-set=new. Consumption wiring DEFERRED. Lesson: allowed-set must be built in the POST path, not only GET. → loss-types-and-form-changes-arc.md.
- **PACKAGING-FORM 4-CHANGES ARC** (migs 236/237/238) — P1 per-row cuv client/liner; PKG-CLIENT `ref_packaging_clients` (three-client-tables rule holds); P2 `reuses_packaging_id_fk` cuve-réutilisée (view zeroes outputs); mig 236 brewday backfill (21 contract brews). → [packaging-losses-convention/loss-types-and-form-changes-arc.md](maltyweb-pm-memory/packaging-losses-convention/loss-types-and-form-changes-arc.md).
- **IN-TANK / IN-FILLING CO₂/O₂ REMODEL** (migs 241-249/255/257) — `bd_tank_readings`=in-tank DIMENSION (not a loss; shared per beer/batch/day via UNIQUE-as-inheritance); `bd_packaging_readings`=in-filling losses; `co2o2_measures` DROPPED. QA conformance tracker in Salle de contrôle. Broken-bridge lesson (NEVER join v1↔v2 on sheet_row_index) + is_tombstoned gap (flag #8). → [packaging-losses-convention.md](maltyweb-pm-memory/packaging-losses-convention.md) + project_co2o2_intank_infilling_split (orchestrator memory).
- **PACKAGING SAISIE 8-POINT REWORK** — A1+A2+A3 (migs 231/232/233: dispo cols + co2o2 child + loss_untaxed_full) + Atom C white-label ✅ ADDITIVE SHIPPED+PUSHED `40b00be`. Remaining = (a) DATA-DRIVEN LOSS TYPES [DESIGNED, A-LT2 paused]. → [packaging-losses-convention.md](maltyweb-pm-memory/packaging-losses-convention.md) + [packaging-losses-convention/white-label-atom-c-arc.md](maltyweb-pm-memory/packaging-losses-convention/white-label-atom-c-arc.md).
- **GLOBAL SHELL FIX / overflow-x** (`408285e`+`65f1b93`) — fit-guard `.home .main > *{max-width:100%;min-width:0}` + `body.home/auth{overflow-x:auto}` (scroll not clip); `body.home.salle-des-machines{overflow-x:hidden}` exemption MUST be preserved (400% sliding stage). Folded into `ui` SKILL.md #76/#77.
- **RE-EDITABLE PACKAGING SUBMIT** (`f8a9e18`, ZERO mig) — `?edit=<main row id>` prefill of whole session re-POSTed through the FROZEN idempotent handler; shared-in-tank read gate fires only on actual value change; orphan-on-shrink; hors_process round-trip. → saisie-forms-5-correction-arc.md + packaging-losses-convention.md.
- **BREWING FORM ENHANCEMENTS** (ZERO mig — `bd_brewing_timings_v2` pre-existed, mig 226 STILL FREE) — Issue 1 FW/PV/KW gravity+timings UI; REOPEN/PREFILL `?edit=` (closed F-1 P2 sticky-ingredients for free); Issue 3 per-brassin explicit dates (Model B, "two dates two purposes": timings.event_date=brew START, cast-out HL dated by brew END via Cooling-row submitted_at). → saisie-forms-5-correction-arc.md + recipe-ingredients-drift-surface.md.
- **WORT-PRODUCTION KPIs** (`c4d92c9`, PUSHED, ZERO mig) — wort.php "KPIs/Analyse" tab; **CANONICAL WORT METRIC = cooling-anchored** `SUM(bd_brewing_gravity_v2.final_volume WHERE event_type='Cooling' AND is_tombstoned=0)`, recipe via `recipe_id_fk` DIRECTLY (NOT brewday-date-equality join — undercounts ~15%). Archive-subtype bucketing via client_id OR single-lifetime-brew. 2026 YTD: Neb 6271.4 (Core 6168.7+Specials 102.7)+Contract 68.8. Board deck = Windows artifact `Board26Q2-Production.pdf`. → derivation-tree-and-schema.md (if it grows).
- **FERMENTING STATE-GATED SELECTOR + HOP-STAGE** (mig 253, `c38d924`/`8cc1c73`/`e81358e`) — `ref_recipe_ingredients` hop_addition_stage ENUM + boil-time + multi-stage-same-MI UNIQUE; SDC per-hop editor; fermenting card-selector mirrors racking. Gate-2 CIP-cadence REMOVED 2026-06-03 (category error — CCT is by design full of fermenting beer; fermenting = NO-CIP). OR-bridge retirement + operator-smoke open. → [recipe-ingredients-drift-surface.md](maltyweb-pm-memory/recipe-ingredients-drift-surface.md) §FERMENTING-SELECTOR + cip-module.md §FERMENTING = NO-CIP.
- **SDC RECETTES — ATOM B + 4-ITEM PLAN** (migs 219-225, `5e1e1c1`) — `ref_recipes.style` col now EXISTS; `ref_recipe_aliases` (89 rows) + `recipe-resolver.php` resolve canonical→alias→normalized; contract renames are two-hop (alias every intermediate, source = update_recipe_name audit rows NEVER guess); D/A/B/C sequential atoms (Contracts-never-upcoming lifecycle rule, mig 222). → [recipe-ingredients-drift-surface.md](maltyweb-pm-memory/recipe-ingredients-drift-surface.md) + [recipe-lifecycle-and-sdc-list.md](maltyweb-pm-memory/recipe-lifecycle-and-sdc-list.md).
- **MOTHER SHELL Phase 1 + Phase 2-5** (mig 215/216, `96ea0d4` → 10 atoms `3c248bd`…`8ce9222` + fix-forwards) — `op_sessions.form_type='batch'` + self-FK merged_into + blend_share_pct + filtered-UNIQUE open_mother_key + `app/mother-shell.php` resolver; board LIVE at sb-board.php (Concept C diorama, SVG vessels, drill-in, 15s polling, cuve-vide, merge/force-close, Salle de Guerre). Atom 11 = operator smoke (HELD). → [mother-shell-architecture.md](maltyweb-pm-memory/mother-shell-architecture.md) + [op-sessions-schema-spec.md](maltyweb-pm-memory/op-sessions-schema-spec.md) §7.
- **FORM-FINALISATION (backlog data-entry)** — F-access + F-0 (all 4 forms back-datable) + F-1 (brewing MULTI-BREW cooling capture, `89ed1a2`) + Build A (Inventaire RM form) + Build B (RM counted-vs-theoretical comparative, fixed-April-anchor formula). Backlog entry is INDEPENDENT of pilots 5/6. → rm-retro-prereq-chain.md (RM forms / Build A+B) + user-auth-admin-arc.md (F-access).
- **TANK-SIMULATOR occupancy** (2 passes, read-side only, ZERO mutation) — drain only if CCT's current occupant matches `(recipe_id_fk,batch)`; packaging→BBT drain dual-keyed (was last name-keyed path); CCT9 recency-gate; start_time DD/MM swap masked via COALESCE. **✅ RACKING-DEST-TYPE fix DONE+DEPLOYED 2026-06-15 (uncommitted): `loadRackingEvents` now reads typed `racking_destination_type`+`*_number` → routes CCT/YT/BBT in replay (was BBT-only); restored Zepp-213's clobbered BBT8 occupancy as bonus evidence the bug was actively corrupting live state. Node `parse-tank-simulation.ts` SAME gap STILL OPEN (RESUME #3, COGS/WIP).** → [tank-simulator-occupancy.md](maltyweb-pm-memory/tank-simulator-occupancy.md). sql skill #25/#27.
- **EL DORADO dup-MI merge (mig 254) + GL-6285 non-inventoried fix (mig 256)** — both `630d89f`/`62dde37` PUSHED. MI MERGE/TOMBSTONE-AND-RELINK pattern + uniq_row_hash-after-repoint gotcha + `is_inventoried`=counted-as-stock-not-GL-derivable rule → derivation-tree-and-schema.md §SQL BUILD SCHEMA.
- **DIG/QDG recipe consolidation (mig 217, `14f6785`)** — DELETE stub id=77, rename id=27→'Qrew - Diversion Gose' (sku_prefix DIG preserved), aliases migrated; the `audit_row_revisions.action` no-'delete' lesson learned here. CollabIn naming = "BrewerName - BeerName" + non-matching short prefix (Docks/DGD/DIG). RawDB.xlsx source-reconvergence #4. → recipe-lifecycle-and-sdc-list.md.

## 📂 THEMATIC FILES (load on demand)
- [KPI tracker catalog + personal-dashboard (Phase 2 of user-account arc)](maltyweb-pm-memory/kpi-tracker-catalog/README.md) — read when building/touching `ref_kpi_trackers`, `mon-tableau.php`, `user_kpi_selections`, `recap_subs`/`send-kpi-recap.php`, `kpi-charts.js`, the compute_handler registry, the `viz_type` ENUM (now **10 values incl `recap`** — daily-recap composite cards #270 wort / #271 packaging, mig 310), or any KPI tracker. Holds the 250-tracker catalog ref (docs/kpi-catalog.md), the blessed thin-handler/registry model, the params_json-whitelist (no free-SQL-in-DB), the data_ready auto-activation gate, the divergence flags (no COGS/WIP recomputation, cooling-anchored wort, tank-sim port gate), the v1/v1b/roadmap seed plan, and the ⛔-overlaps-existing-arcs map.
- [USER / AUTH / ADMIN / MANAGER-SCOPE / EMAIL / SHARED-DEVICE arc (✅ SHIPPED+LIVE 2026-06-04, migs 261/262/264)](maltyweb-pm-memory/user-auth-admin-arc.md) — read when touching `users`/auth/login/roles, admin user-management (`reglages-generaux.php` §users), the prod-vs-logistics MANAGER SPLIT (`manager_scope` ENUM + `manager_can()`), `user_invites`/`set-password.php`/`app/services/mailer.php` (IONOS relay), `brewery_identity()` (`app/settings-helpers.php` — DB-sourced footer identity, the Neuchâtel hardcode lesson), or the SHARED-DEVICE registry (`app/services/device.php` + `auth_shared_devices` mig 264 + `public/admin/settings/devices.php` — remember-me suppressed on shared stations). Holds the access/email/PWA advisory decisions + the 2FA/Tailscale-gate/PWA/onboarding backlog + the modification-REQUEST/proposal pattern (`ref_supplier_proposals` + `sf-update-field.php` for logistics; production forms write direct) + as-built auth (`app/auth.php` Argon2id, `is_admin`/`is_manager`/`require_*`).
- [Saisie-forms 5-correction arc + fermenting-correctness chain](maltyweb-pm-memory/saisie-forms-5-correction-arc/README.md) — read when touching the packaging/racking saisie forms, the `multi-submit-reads.js` widget, the fermenting EDIT-mode/cold-crash-checkbox/beer-identity-fracture/session-phase-cache/purge-cadence chain, the re-editable packaging submit, brewing form enhancements, or any of the PENDING-BUILD fermenting fixes in the RESUME POINT.
- [Pre-framework PACKAGING + BREWING pass — racking-pattern transfer scoping (🅿️ PARKED)](maltyweb-pm-memory/packaging-brewing-pre-framework-pass.md) — read at pilot 5/6 time for the P1-P19 pattern INVENTORY + per-form transfer map + 3 still-open operator Qs. Do NOT build standalone — patterns re-home into the 3-phase shell at pilot time.
- [op_sessions / op_session_steps — MIGRATION-READY SCHEMA SPEC](maltyweb-pm-memory/op-sessions-schema-spec.md) — read when building session-framework infra OR the BATCH MOTHER SHELL additive ENUM+self-FK (§7) or reasoning about the precise column shape of op_sessions / op_session_steps / additive `session_id_fk` / `bd_cip_events.source_form` ENUM / mother `merged_into_session_id_fk`+`blend_share_pct`+filtered-UNIQUE-via-generated-col + retro-link SQL.
- [BATCH MOTHER SHELL — multi-day batch envelope](maltyweb-pm-memory/mother-shell-architecture.md) — read when building Mother Shell Phase 2-5 / pilots 5-6 / the daily-child→mother auto-link / blend-merge survivor pattern / forward-only retro-link / the "Lots en cours" board / EXPÉDITION Stage-3 placeholder. Holds §AS-BUILT PHASE 2-5, §REFINED OCCUPANCY RULE, §BLEND RULING #4, §ATOM 14/15 MERGE, §ATOM 16/17. Cardinal: mother=batch-grain envelope; children=day-grain; events=truth; mother NEVER pins to a vessel.
- [SESSION-LIFECYCLE READ-POLICY + LOCK-CASCADE + PHASED-SESSION model + WORT-CONTRACT](maltyweb-pm-memory/session-model-arc.md) — read when building/touching ANY downstream consumer of session-grouped events (fiscal reads filter `status='closed'`; `finalize_session($id)` cascade; per-section save = ONE transaction; abandoned-session policy; poll-15s live shells), ANY form restructured into start→in_progress→end→closed sessions, the session live dashboard, the UI STYLE CONSTRAINT (light-kraft palette authoritative, vessel rendering reworked-from-scratch), or the WORT-CONTRACT `process_type ENUM('standard','wort_contract')` BINARY model (cooling-target = continuous param on brewday row, NOT enum branch; phantom-CCT suppression keys off process_type). Holds the C1-C8 re-homing map + the sequenced board arc (ATOM 16 ∥ TS-FIX → ATOM MAT → pilots).
- [RM-retro (Dec reconcile + Jan/Feb/Mar) prereq chain + gate state](maltyweb-pm-memory/rm-retro-prereq-chain.md) — read when reasoning about the RM retro, Dec-2025 stocktake reconciliation, the packaging-consumption gap, the Stage-2 eshop/cage-draw matrix, the Shopify pickup-flag dependency, production-SKU depletion, or "what unblocks the retro". GATED ON commissioning complete; TWO goals + TWO prereqs (Stage-2 matrix + production-SKU depletion) BOTH OPEN.
- [Saisie Transferts + yeast-family defaults](maltyweb-pm-memory/saisie-transferts-and-yeast-family.md) — read when touching the racking/Transferts form (label-only rename), the Recettes/Biochimie surface, `ref_yeast_strains` (strain `type` vs `family`), `ref_yeast_family_defaults`, `ref_recipes` yeast-strain FK + spec overrides, the garde-min transfer gate, or any state-gated lagering-threshold picker. The CIP-convergence design + the superseded transfert-interrompu ruling are now sub-files — see the §CIP CONVERGENCE / §TRANSFERT INTERROMPU pointers inside.
- [Yeast-strain taxonomy + Biochimie SDC — AS-BUILT post mig 228/229](maltyweb-pm-memory/yeast-strain-taxonomy.md) — read when touching yeast strains/families, the SDC Biochimie card, the racking/fermenting garde-min gate, or mig 228/229. ⚠️ FAMILY IS AN ENUM (7 vals), NOT a lookup table — `ref_yeast_families` DOES NOT EXIST; garde JOINs match on the family STRING. Holds the Pinnacle merge (mig 228), the ENUM-extend (mig 229), and the `yeasts.php` hard-DELETE scrapping candidate.
- [Recipe lifecycle + SDC Recettes 3-list + EPH vintage collapse](maltyweb-pm-memory/recipe-lifecycle-and-sdc-list.md) — read when touching SDC Recettes lists, `v_recipe_lifecycle` (FULLY DERIVED, migs 219-222), recipe vintage collapse, the recipe-resolver, or the EPH oldest-vintage split-brain. **✅ mig 263 EPH upstream-event consolidation SHIPPED+PRODUCTION-PROVEN 2026-06-04** (repointed event FKs+op_sessions oldest→operative; the ratification rulings #1/#2 + the hash-scheme-coexist discovery are here).
- [Recipe-ingredients drift surface](maltyweb-pm-memory/recipe-ingredients-drift-surface.md) — read when wiring SDC Recettes ingredients (R1 data-wiring / R2 drift view / R3 editable official), the fermenting-selector + hop-stage scoping, recipe ALIASING + PRG-restore rulings, the CONSOLIDATED 4-ITEM PLAN, or the observed-vs-official cost-safety gate (editing official is cost-AFFECTING for minerals/process-aids/finings/yeast-vit via l/ml gap-fill).
- [Racking REDESIGN WS1-4 (losses / clean-state / multi-lot blend / CIP cadence)](maltyweb-pm-memory/racking-redesign-ws1-4/README.md) — read when building the operator racking redesign: WS1 Pertes section, WS2 event-sourced BBT clean/dirty, WS3 destination auto-blend + multi-lot blend_info, WS4 BBT-CIP cadence. ⚠️ C-CHAIN re-homed into the racking session (zero data-model rework); WS1-4 rulings STAND. C1 SIM-FIX SHIPPED → tank-simulator-occupancy.md.
- [TankSimulator occupancy fix — drain guard + packaging under-drain / CCT9 / start_time-swap (SHIPPED+LIVE 2026-06-04)](maltyweb-pm-memory/tank-simulator-occupancy.md) — read when touching `app/tank-simulator.php`, the CCT/BBT occupancy replay, the loaders/`isTrueBlend`, or porting the lesson to Node `parse-tank-simulation.ts`. Dual-keyed `(recipe_id_fk,batch)` drain; ZERO data mutation; FROZEN return shape. 3 OPEN follow-ups (start_time swap, Node port COGS-impacting, the DIV/45 packaging which is the normalizer defect below). sql skill #25/#27.
- [Packaging parallel -4/-B run normalizer defect — BLESSED FIX (surgical 4-row mig + code-fix) + v2⇄old-form RECONCILIATION AUDIT](maltyweb-pm-memory/packaging-parallel-run-normalizer-defect.md) — read when touching `scripts/normalize-rawdb.py` `process_packaging_data` (Mode A vs B), `bd_packaging_v2` volume/client correctness, the `PackagingForm` old-Google-Form parallel stream, or any tank-drain consumer. Exactly 4 mode_a rows mis-recipe'd (2227 b45→DIVB, 2158 b169→STIB, 2119 b63→SPYB, 2086 b5→DIBB); v1 `second_packaging`=ground truth; NO re-normalize (RawDB frozen). Net Neb HL unchanged. COGS/tax-impacting. **§v2⇄old-form audit (2026-06-10, NO writes): CUTOVER=submitted_at 2026-05-22 16:38:43 (source_sheet_row_index NULL=web/parallel); migration CLEAN (1336/1342 matched, 6 unmatched all prod=0 zero-prod skips); 1 live discrepancy = batch-211 cuv CLIENT mis-attribution v2 id=6739 client_fk 2(Jetée)→should be 19(Blues Rules), volume/tax-NEUTRAL, per-client COGS/recharge only — FIX HELD pending operator confirm, refuse-don't-guess. ROADMAP FLAGS: (1) old Google Form `PackagingForm` STILL filled post-cutover in parallel, NOT ingested → retire-or-parity-check; (2) dedup key can't represent 2 byte-identical same-day events.**
- [Racking / Transferts KPI dashboard section + QC-threshold ruling](maltyweb-pm-memory/racking-kpi-dashboard.md) — read when building/touching racking KPIs or any aggregate over `bd_racking_v2`, the round-3 transferts deltas, OR per-beer FormFramework QC thresholds for ANY form (§QC-THRESHOLDS — metric governance vol=history/co2=operator-spec/o2+pressure=global; `app/qc-thresholds.php` resolver; NOT YET BUILT). KPI section LANDED `c38cea1`; CO₂-correction + re-ingest-gate detail here too.
- [CIP MODULE + config-driven shared-partial family (CIP + LOOKUP PANEL)](maltyweb-pm-memory/cip-module.md) — read whenever a build touches CIP on ANY form OR embeds/builds a **Lookup Panel** OR needs the **config-driven shared-partial pattern** (one partial driven by a caller-set `$config` array, zero DB of its own + a per-page read-only GET endpoint). CIP: `bd_cip_events` DDL, `ref_cip_types`, `app/cip-events.php` writer + `cip-section.php` partial, per-form processes, migs 175/176/177/230, PACKAGING CIP FIX, §FERMENTING = NO-CIP. **Lookup Panel (built 2026-06-21, awaiting operator deploy):** `partials/lookup-panel.php` + `lookup-panel.js/.css` + per-page `/api/<page>-lookup.php`; read-only GET = gate-yes/CSRF-no, `checkdate()` not strtotime, brewing join on `(beer,batch)` natural key (no brewday_id FK), NULL vendable_hl → `—`. The reusable DOCTRINE now lives in the `ui` skill Layer-3. Triggers: CIP/lookup panel/$lookupConfig/$cipConfig/config-driven partial/shared partial/embeddable panel/<page>-lookup.php/read-only endpoint/checkdate.
- [Phase D — COGS/COP cost-source swap (CORE COMPLETE)](maltyweb-pm-memory/phase-d-cost-source-swap.md) — read when touching Phase D, `v_mi_cost`/`v_mi_cost_gap`/`ref_fx`, the COGS/COP cost source, the `loadMI({mysqlCost:true})` overlay, `build-sku-bom.js` cost, or any BSF `MI.price`→MySQL question. Holds the cost-resolver model (wac>catalog>no_basis; `price IS NULL`≠`price=0`), the 32-row gap, the 8-item FOLLOW-UP BACKLOG (BSF-exit legs, RULE-1 scraps, no_basis cans), the dormant mysql-inventory.ts flag.
- [Billing-format / WAC unit-normalization model](maltyweb-pm-memory/billing-format-and-wac-unit-model.md) — read when touching `v_mi_wac`/WAC, `inv_deliveries` unit semantics, `ref_mi_invoicing_units`, pack-size normalization at ingest, catalog-price reliability, any qty×price cost path, OR the brewing-saisie ingredient unit-default (§THREE UNIT COLUMNS — `input_unit`/`pricing_unit`/`consumption_unit`). Holds the canonical-unit contract, the de-drift RULE, the strict WAC audit verdict, catalog-hygiene findings, the total/qty effective-price basis decision, remediation buckets A-E.
- [SKU-by-SKU BOM validation walkthrough — framework + canonical RULES (KEYSTONE correctness arc)](maltyweb-pm-memory/sku-bom-validation-walkthrough/README.md) — read when walking SKUs to validate liquid+packaging+combination BOM with the operator, settling/building the LIQUID-COMPOSITION model + liquid compiler, the 3 scotch/sticker BUCKETS, the eshop-scotch altitude, the intrinsic-BOM-vs-fulfillment-layer split, the DIRECT-SALES STOCK CONSUMPTION TRACKER (= Stage-2 matrix), the 17 missing-liquid SKUs, or the BOM-Review additions. Holds RULE #1 (liquid: malt/hops OBSERVED-ONLY [absent recipe line = drift KPI]; ref_recipe gap-fills non-malt/hops only; basis = VOLUME-WEIGHTED TRAILING avg — DECIDED, F2 unblocked), RULE #2 (3 buckets, exhaustive), **RULE #3 (intrinsic BOM vs channel-dependent fulfillment layer — per-SKU BOM = intrinsic content ONLY; cage XOR box stock states; order-composition eshop pkg = tracker not BOM; 2026-06-07), RULE #4 (draft keg-share = pour_HL/0.2 × collar+safe, channel-exclusive w/ -F, FOLD NOW)**, the 7-item DETERMINISTIC FIX-SET (bottle WAC 0.1466 / TEA line / sticker-qty→1+3-way branding / scotch tape 0.00093 / B12 eshop-scotch / keg-share / F2-needs-WAC), the SEQUENCING RULING (walk-all-then-batch; WAC-switch GATES F2 apply; tracker LAST), the TRACKER design (cage-pull-w/-B-fallback + order-consolidation packing + inv_consumption write), the resolver altitude map, the persistence split. Worked example = Alternative (recipe 6). PACKAGING walk live; liquid Phase C gated on WAC-switch→F2 apply.
- [Contract BOM — Chien Bleu + the contract-SKU/recharge encoding pattern](maltyweb-pm-memory/contract-bom-chien-bleu.md) — read when costing a CONTRACT customer's SKUs (Chien Bleu/BLZ/etc.), the per-material material-responsibility split, contract-COGS/recharge routing, bulk-wort (Moût Chaud) sale, ART-* generic BC sku_code resolution, or whenever a sold contract line "must be computed". Holds the May-2026 Chien Bleu footprint, the real MIs (box 163 PKG_BOX_24_BTL_BRUN, cork 93), the verified live-DB facts (ref_sku_aliases EXISTS but BC resolver ignores it; contract liquid IS F2-derivable for r20/21; r22 Moût Chaud not parsed), the SKU-resolution ruling (mint canonical + alias + patch resolver), the cork-deferral RQ pattern, and the 7-step build sequence. ⚠️ OPEN TODO recorded inside: add cork id 93 to JASPB/BAMB from next bottle run.
- [SKU decomposition tree](maltyweb-pm-memory/sku-decomposition-tree.md) — read when touching composite/multipack/white-label/alias SKUs, the THIRD decomposition axis (per-recipe HL share → ingredient attribution), `ref_sku_composite_slots`/`ref_sku_aliases`/`ref_sku_collab_temporal`/`ref_sku_packaging_choices`, or FR*/BLA4/COLLAB/PACKDEC. COMPOSITE STEP-③ COMPILER-FOLD = DONE + LIVE (composite_liquid=204/composite_packaging=7; 1000× unit-fidelity bug fixed; dead `sku-compiler.php` removed). "CAPACITÉS FORMAT→SKU" surface ALREADY BUILT (`sdc_gated_format_ids()`); #44 ESSENTIALLY CLOSED (migs 185/186/191). → packaging-bom-model/README.md §FORMAT→SKU RESIDUAL-GAP TRIAGE.
- [Derivation tree, SQL schema, cost model](maltyweb-pm-memory/derivation-tree-and-schema.md) — read when reasoning about the Le Zeppelin SOT chain, which canonical table owns a fact, FK/migration discipline, COP/COGS, the FULL per-migration ledger, the V2-ONLY / CANONICAL-v1-v2-RawDB / EVENT-DATE rules, the MI MERGE/TOMBSTONE-AND-RELINK pattern + uniq_row_hash-after-repoint GOTCHA (§SQL BUILD SCHEMA, since mig 254), or the `is_inventoried` semantic-flag rule (since mig 256).
- [Ingestion validation gate + SIE/SIL energy web-path (arc, SHIPPED+LIVE 2026-06-08)](maltyweb-pm-memory/ingestion-validation-gate-arc.md) — read when touching ingest-one-local.ts, upload-document/status/retry.php, triage-upload.js, invoice-validate.php, app/recap-lines.php, doc_invoices/doc_invoice_lines, inv_energydata, the validate-before-write gate, the TRIAGE "Lignes non résolues" CONVERGENCE (queued — recap-lines.php already IS the structured-parser-view surface; doc_invoice_lines.mi_id_fk is already best-candidate+name_confidence semantics, no new col), or Drive-inbox retirement. Holds the no-more-BSF directive, the staging-already-exists discovery, rulings R1-R4, batch-asks, EQUIP lines, the §TRIAGE CONVERGENCE consult.
- [Energy / Utilities COP web-path — LOCKED BUILD DIRECTION (Kouros-decided 2026-06-17)](maltyweb-pm-memory/energy-cop-webpath.md) — read when building/touching the meter-reading saisie (`saisie-energie.php`), the PHP energy estimator (`app/utilities-estimate.php`), `inv_energydata` writes, `ref_utility_tariffs` (mig 395), `ops_utility_closures` (mig 395), the financier COP utilities tile switch off `cogs-report-data.json`, the Node `lib/utilities.js` port/parity, the cumulative-meter-index UNIT TRAP (`gaz_kwh`/`*_kwh` columns store RAW m³/CT-counts, coefficients applied downstream), or anything "energy/gas/water/electricity/peak kW/tariff/meter reading". Holds the ratified architecture (tariff→canonical MySQL table; closures→`ops_utility_closures` not JSON; source ENUM stays 'estimate' no mig), the full ①②③④⑤ build brief, the parity gate (months 2026-04/03/02/01 + 2025-12), and 5 open Kouros decisions. NEXT FREE MIG = 395 (re-`--status`).
- [Fulfilment / Expéditions arc (Le Cockpit; LIVE for the team 2026-06-09)](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) — read when touching `ord_orders`/`_lines`/`_status_events`, `inv_stock_movements` (mig 299), `inv_side_stock_ledger` (mig 303), `inv_repack_events` (migs 336/337), `inv_sales_ledger`/`v_sales_ledger_*` views (325/326/329), `ref_customers`/`ref_transporters`, `app/fulfilment-site.php` (`resolve_fulfilment_site()`), `app/fg-stock.php` (`fg_stock_compute()`+`fg_stock_location_snapshot()`), `app/seasonal-burn.php` (semaines-restantes burn engine), `app/repack.php` (eshop repacking, `repack_depletion_live()` gate), `expeditions.php` (`?view=stock|mouvements|commandes|historique|side-stock|repack`). Holds: Shopify⇄maltyweb integration (Phase 0/1/2A LIVE, 2B disarmed); forward seasonal burn engine; Historique + historical-Commandes lanes; eshop-repacking capture phase (06-15 activation set); the resolver precedence/landmines; Σcards==Σphysique invariant (anchor-tiebreak fix); **the FG-STOCK ANCHOR RULE (`5b9858e`, 2026-06-16): per-LOCATION latest-CENSUS-DATE anchor — NEVER carry stock; a SKU absent from a location's latest census = 0 there; `count_type` NOT filtered on the board (`month_end` is a COGS-only super-tag, COGS path doubly-pinned month_closed+count_type).** Triggers: 'fulfilment'/'expéditions'/'commandes'/'stock PF'/'semaines restantes'/'burn'/'Shopify'/'pickup'/'repack'/'side-stock'/'giveaway'/'historique'/'Σcards'/'carry-over'/'census'/'anchor'/'count_type' → READ this file. **MIG HEAD here always verify `migrate.php --status` (parallel sessions live).**
- [Sales / commercial surface (Le Cockpit)](maltyweb-pm-memory/sales-commercial-surface/README.md) — read when touching the SALES side: Shopify orders ingest, BC monthly sales, `derive-eshop-consumption.ts`, sales-driven COGS, the eshop order→stock model, `ref_clients`, or any commercial question. 3 canonical landing tables; two-source model (Shopify + BC); Stage-2 still planned; the 🔴 Sales BSF-exit leg.
- [Computation-layer architecture](maltyweb-pm-memory/computation-layer-architecture.md) — read when building/changing any computed surface (KPIs, dynamic RM, eshop matrix, live tank volumes, COGS/COP/WAC, SKU/BOM, rollup matrices); the on-write+scheduled rule, per-surface decision rule, the TWO live landmines, Phase 0→3 path.
- [Consumption / COGS / COP derivation wiring — v1 vs v2, module-by-module (VERIFIED LIVE 2026-06-08)](maltyweb-pm-memory/consumption-cogs-derivation-wiring.md) — read for which production version (v1/v2) a computational module reads, why a month's `inv_consumption` is under-populated, the writer chain, the COGS/COP engine location, the May-close gating, or "migrate the computational modules onto maltyweb". 🔴 The COGS inventory-variation FICHE is a SEPARATE table family (mig 330: `ref_cogs_fiche_categories` 12 keys + `cogs_fiche_opening_anchor` April seed Σ431 354), keyed on `category_key` NOT `source ENUM`; `cogs_monthly` is owned by build-cogs-report.js — do NOT hijack it for the fiche. Full detail (the per-script source table, the frozen-brewing-lines gap, the cutover sequence, basis-change disclosure rule) in the topic file.
- [Volume dimension](maltyweb-pm-memory/volume-dimension.md) — read when touching SKU/BOM volume, volume KPIs/trackers, tank/packaging HL, `ref_skus.hl_per_unit`, the cuv/liner model, the `-X` CAGE volume (mig 147), the bot/can/can33 volume_hl backfill, or `bd_packaging_v2.vendable_hl` (canonical formula + units_per_pack + inactive-SKU caveat). Cardinal: additive-cost-vs-OWNED-volume.
- [Packaging BOM model](maltyweb-pm-memory/packaging-bom-model/README.md) — read when touching the packaging BOM, slots, scotch either/or, cuv serving-tank gate, the recompute service, the `-4` 3-layer carton (mig 145), the BLO/ZEP carton-MI gap, the `-X` reusable CAGE (mig 147), the multipack→`-X` stock-pull-by-channel COGS/COP rule, the ZEP6C liquid-only fact + compiler boxing-capacity GATING-GAP, the THREE SKU IDENTITY LAYERS, or §FORMAT→SKU RESIDUAL-GAP TRIAGE (#44).
- [Packaging losses & dispositions convention (KEYSTONE)](maltyweb-pm-memory/packaging-losses-convention.md) — operator-confirmed SINGLE source; read when touching form-packaging.php losses/vendable/dispositions, bd_packaging_v2 loss/QA/keg cols, beer-tax base, general-FG vol, packaged-HL→COGS, the in-tank/in-filling CO₂/O₂ model, or §PERTES-DASHBOARD. The DATA-DRIVEN LOSS TYPES arc + 4-operator-form-changes + the white-label atom-C history are now sub-files (see the pointers inside): [loss-types-and-form-changes-arc.md](maltyweb-pm-memory/packaging-losses-convention/loss-types-and-form-changes-arc.md) + [white-label-atom-c-arc.md](maltyweb-pm-memory/packaging-losses-convention/white-label-atom-c-arc.md).
- [Packaging page dashboard](maltyweb-pm-memory/packaging-page-dashboard.md) — read when building/touching the packaging KPI dashboard or the §PARALLEL-RUN FIX detail; data-source map, KPI→query derivations, atom sequence, divergence flags.
- [Read-only lookup modules + Neb/Contract SKU filter](maltyweb-pm-memory/read-only-lookup-modules.md) — read when touching the shared lookup-panel component, the packaging/brewing lookup endpoints, ANY SKU-list render, or per-user UI prefs. Lookup shape (mig 428): brewing = Consulter tab on wort.php, packaging = labelled section on packaging.php. 🟢 **NEB/CONTRACT SKU-FILTER + lookup-card redesign SHIPPED+LIVE+VERIFIED 2026-06-21** (`7c3392c`, origin `f8ed60a`, Playwright): canonical SKU-level classification = **`ref_skus.recipe_id→ref_recipes.classification`** (🔴 NOT ref_beer_types — that stays name-keyed beer-tax classifier; 6 composites recipe_id NULL→'Neb'; LEFT JOIN + COALESCE(,'Neb') always); NEW accessor `app/sku_catalog.php` `sku_catalog()` (call it, don't re-inline SELECT FROM ref_skus); NEW generic per-user pref store **mig 430 `user_ui_prefs`** + `app/user-prefs.php` + `public/api/ui-pref.php` (reusable); shared 3-way `Toutes|Nébuleuse|Contract` control (`partials/sku-class-filter.php`+js+css, default Neb per-user); wired = PF board + both lookups; saisie dropdowns DELIBERATELY NOT filtered; 🔴🔴 DISPLAY-LAYER ONLY never in COGS/compute; v1 limit = warehouse headline tiles/GL tfoot stay portfolio-wide. Trigger "lookup"/"consulter brassin"/"consulter packaging"/"Neb vs Contract"/"contract SKU filter"/"sku_catalog"/"classification filter"/"user_ui_prefs"/"ui-pref"/"sku-class-filter"/"SKU list clutter".
- [Build history (directory)](maltyweb-pm-memory/build-history/README.md) — read when you need the Stage-1 chronology, the full per-session build-changelog (split by period), the commit chain, resolved divergences, the interrupted-migration lesson, the page inventory, the §8.9 closure backlog, OR the verbatim pre-2026-06-05 index snapshot (full inline arc narration).
- [Scrapping / retirement backlog](maltyweb-pm-memory/scrapping-backlog.md) — read on EVERY build (RULE 1) or when consulted about retiring a surface; the candidate register with gates.
- **📋 DEFERRED BACKLOG (operator-confirmed-WANTED 2026-06-16, not abandoned) — 4 items in their thematic homes:** **★ destructive-scope-flip LEVEL-guard** (Σphysique==Σcards R1 is BALANCE-not-LEVEL → a symmetric base→none scope flip silently zeroes counted stock undetected; PD8/XMASPACK 242-unit near-miss; operator's HIGHEST-interest item) + **Layer B cage-COGS retarget** (=④-Phase 2; gated on Shopify pickup-signal off-hold; NO bundle_bom table, NO inv_consumption write until greenlit) → [sku-bom-validation-walkthrough/README.md §DEFERRED BACKLOG](maltyweb-pm-memory/sku-bom-validation-walkthrough/README.md); **eshop bundle 2×-multiplier/100%-off-promo edge** + **C3 self-service variant-mapping UI** → [fulfilment-expeditions-arc/README.md §DEFERRED BACKLOG](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md). Read the relevant §DEFERRED BACKLOG when the gate clears or when sequencing these.
- [Skills inventory + build tooling](maltyweb-pm-memory/skills-and-build-tooling.md) — **ALWAYS read §SKILL-FIRING MATRIX when equipping/dispatching ANY agent** (work-type→skills table + the PM's 3 standing behaviors: EQUIP: line on every brief, default webapp-testing smoke on deployed pages, proactive mis-equip flagging); also read when planning the post-deploy smoke suite (`webapp-testing` = designated vehicle), starting the 2FA arc (php-security.md ready), heavy dataviz (dataviz.md + deuteranopia flag), SQL perf deep-dives (innodb/ layer, maltytask-conventions-win precedence), or vetting/installing any third-party skill (gap ledger + HELD list).
- [Conventions + house helpers](maltyweb-pm-memory/conventions-and-helpers.md) — read when dispatching/reviewing a build or needing PHP house-helper signatures; the full coding/architecture/UI convention set, maintenance protocol, PM charter, verified helpers + recette/SKU-UI live facts, §COMPUTE-ON-SUBMIT, §AUTH HEADLESS SCREENSHOTS, the STANDING UI/UX DIRECTIVE (site-wide light-kraft palette, single global `:root` token block).

## MAINTENANCE NOTE
Keep THIS index lean — current build-state, RESUME POINT, the CHECKLIST, the SHIPPED-ARCS one-line register, the THEMATIC-FILES pointers. When a section here grows past ~5 lines or becomes historical, COMPILE IT OUT into the relevant thematic file (create a `<topic>/` subdirectory if the file itself crosses ~80KB) and leave a one-line load-triggered pointer. Bump "Last updated" on every change. Verify DB-claims read-only before recording. (Agent note: `maltyweb-pm` registers as a subagent only after a Claude Code restart / `/agents` reload; until then, agents read these files directly.)

**Build-changelog discipline:** the rolling per-session build-changelog (dated narration of LANDED commits/migrations) lives ONLY in [build-history/](maltyweb-pm-memory/build-history/README.md) — APPEND each session's entry to the newest period file there, never to this index. When you record a new build: (1) prepend the dated narration to the build-history changelog; (2) add/refresh a ONE-LINE pointer in the §SHIPPED-ARCS REGISTER; (3) move any now-LANDED RESUME-POINT item out of RESUME POINT, keeping only what is still open/next; (4) update the §CURRENT BUILD-STATE one-glance + the header current-state sentence. The index is the map; the depth lives in the thematic files + build-history.
