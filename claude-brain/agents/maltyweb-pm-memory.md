# maltyweb-pm — Project Manager Knowledge Base (LEAN INDEX)

> This is the maltyweb PM's memory. It is a LEAN INDEX: current build-state, sequencing, RESUME POINT, the BUILD-LAUNCH CHECKLIST, and ONE-LINE load-triggered pointers to thematic files. Detail / completed-arc / any section over ~5 lines is COMPILED OUT into `~/.claude/agents/maltyweb-pm-memory/<topic>.md` (and per-topic subdirectories), loaded ON DEMAND only when a task touches that topic. Keep this index small + current — date load-bearing facts; verify against the live DB/repo before recording. Stable conventions also live in `CLAUDE.md` + the `~/.claude/skills/` skills + the topic memories under `…/memory/`.
>
> Last updated: **2026-06-12** — **maltyweb main session-final HEAD `a0a9965` (PUSHED); maltytask main HEAD `423a660` (PUSHED).** Whole session chain on origin/main; no "NOT pushed"/"deployed-not-committed" flags outstanding.
>
> **✅ FINANCIER-FICHE COGS ARC KEYSTONE CLOSED — May 2026 close SHIPPED + RATIFIED (Kouros) + VERIFIED (Opus gate, all 6 checks PASS) + COMMITTED + PUSHED (`423a660` maltytask main; `origin/main..HEAD` empty; tree clean).** Engine `scripts/cogs-monthly-compile.ts` IS committed (a concurrent session landed it — the deferred commit is DONE, nothing left). 12 May `cogs_fiche_monthly` rows: RM 282 368.74 · WIP 21 814.28 · FG 47 701.56 · TOTAL 351 884.58 · OPENING 394 367.74 · VARIATION −42 483.16 · BASIS_ADJ +1 808.82. Key fiscal invariant: April-seed close 394 367.74 == May opening (EXACT). Engine dry-run reproduces published rows TO THE CENT (HEAD == published, no drift). Yeast RM 1 496.03 (g→kg held; 27 unit-mismatches = diagnostics, NOT applied). Smoke 25/26; banner CSS fix `70efa0e` (maltyweb). **🔴 RM-FORMULA INVERSION (standing): naive `final_qty × cost_chf` IS CORRECT (`inv_rm_stocktake.final_qty` is in the pricing unit) — do NOT "harden via `loadRM`" (lib/inventory.js applies conversionFactor → 1000×-under-values g-input/kg-priced items, Hops 29.67 vs correct 29 668.66). Engine's loader is `loadRmStock` (lib/rm-stock-mysql.js), distinct from the rejected `loadRM`; April yeast 565k = one-off CLOSED-period (seeded, never-recomputed) metadata drift, not a formula bug; permanent non-halting unit-consistency diagnostic added.** Engine May valuation diverges from legacy EXT (RM ≈ −47k) — EXPECTED, engine supersedes EXT; correctness rests on seed tie-out + identities, NOT EXT match. Kouros ratified: seed-April-from-EXT + engine-proves-on-May; WAC/F2 basis = separate "Ajustement de base (non récurrent)" line; closed-periods-never-recomputed = standing policy. **⏭ NEXT MONTHLY CLOSE = JUNE — gated on June FG census at month-end** (operator via Saisies → PF card → `expeditions.php?view=stocktake`); then engine `--apply 2026-06` (opening auto = May 351 884.58) → reconcile → auto-publishes. May now immutable. **✅ C1 (financier month-defaults unified to last-closed + pending-import panel) SHIPPED 2026-06-12, smoke all-PASS, files staged/modified NOT-yet-committed (operator commits) — NEW `app/finance-period.php`::`fin_last_closed_month(PDO):?string` = UNION-MAX(cogs_fiche_seed ∪ cogs_fiche_monthly)=2026-05, the SoT signal (REJECTED a system_settings pointer = parallel store); call the accessor never copy the UNION; `.fin-pending-import[hidden]{display:none}` reset held. ⏭ C2 (comprehensive CFO MULTI-TAB XLSX export, `cogs-comprehensive-xlsx.php`) = NEXT, DEFERRED/not-started; reuses `fin_last_closed_month()`.** **TOUR:** `financier`+`journal-saisies` = pre-existing CRITICAL gaps; new Fiche tab may want a card (financier sensitive → PM-RATIFY). → [financier-cogs-fiche.md](maltyweb-pm-memory/financier-cogs-fiche.md).
>
> **✅ RECAP-EMAIL GRANULARITY OVERHAUL — ARC CLOSED 2026-06-12.** PART (a) `a0a9965` + PART (b) `36d0d70` (maltyweb main, deployed+pushed+verified). `app/kpi-email-render.php` = pure-projection email-safe mirror of the FULL on-screen viz vocab (`kpi_email_render_viz($tracker,$result)`, ZERO DB calls): recap + scalar (a) + 9 renderers sparkline/line/bar/grouped_bar/stacked_bar/donut/flag/table/waterfall + 9 helpers (b). 2 ratified deltas: (1) Outlook-safe **padding-top bar baseline** (not `vertical-align:bottom`); (2) **donut = ranked-% legend NO bar**, distinct from stacked_bar (keeps segmented bar). All pure-HTML/CSS, no SVG, `bgcolor`; caps ~12 +"N autres". **🔴 STANDING DIVERGENCE GUARD: stays a PURE projection — never query/recompute; missing number → add to the HANDLER's `meta`; lockstep via the `kpi_dispatch` contract, not shared code.** Email now accommodates ANY Mon Tableau build (new viz_type → add ONE case). Per-viz table → ui skill `references/email-rendering.md` (Kouros owns; PM not touching). → kpi-tracker-catalog.md §RECAP-EMAIL GRANULARITY OVERHAUL.
>
> **✅ RECETTE DESIGN-TARGETS — SHIPPED+PUSHED `2363c9d`** (mig 335, NO SCD2/schema_meta — existing table): `ref_recipes.{og,fg,ph,abv}_target` NULL DECIMAL via existing `update_recipe_qc` handler + "Cibles" section in recette yeast subtab QC form (attenuation = derived read-out). **🟢 KPI #7 (OG attainment) GATE FLIPPED — fast-follow queued: wire = observed-OG-at-cooling (bd_brewing_gravity_v2 Cooling, °Plato, AVG multi-brew, recipe_id_fk-keyed) ÷ og_target; render "—" on NULL target, NEVER fall back to ref_recipe_profile.avg_og; gated on operator entering targets first; confirm measure framing + scope later.** (Until #7 lands, swap-options = fg_stock #77 OR #12.) → kpi-tracker-catalog.md §RECETTE DESIGN-TARGETS.
>
> **MIG HEAD: applied=335.** RE-VERIFY `migrate.php --status` at build-start (parallel-session collision remains a live class). Prior session arcs ALL COMMITTED+PUSHED on chain `a0a9965`(recap-email)→`2363c9d`(recette targets)→`bc678af`/`cdff109`(tank-occupancy tiles #13/#14/#15/#17, mig 333, read canonical `TankSimulator::run()` via `kpi_tanks_get_sim_state()`, zero writes)→`b6db62c`/`fb2778a`(MON TABLEAU #2 sparkline + #5 grouped_bar mig 331)→`ad9c14b`(financier fiche). REFERENCE PATTERNS: `kpi_wort_hl_brewed`(#1) / `renderKpiGroupedBar` / `renderKpiSparkline` (+meta.prev_series ghost). Earlier 2026-06 landings COMPILED OUT → [build-history/changelog-2026-06.md](maltyweb-pm-memory/build-history/changelog-2026-06.md); pre-2026-06-05 → [build-history/former-index-last-updated.md](maltyweb-pm-memory/build-history/former-index-last-updated.md).
>
> **📦 ALL completed-arc narration is COMPILED OUT — this index only points.** The full per-session build-changelog lives in [build-history/](maltyweb-pm-memory/build-history/README.md); the verbatim pre-2026-06-05 index (every fat arc block inline) is archived at [build-history/index-snapshot-pre-2026-06-05/](maltyweb-pm-memory/build-history/index-snapshot-pre-2026-06-05/README.md) (split into 6 parts). The §SHIPPED-ARCS REGISTER below is the one-line pointer table for completed work; the RESUME POINT carries only what is still open/next.

## 🚦 BUILD-LAUNCH CHECKLIST — read EVERY time a build agent is dispatched (operator standing order)
- **🎯 ALWAYS-ON — SKILL-FIRING MATRIX (operator standing order 2026-06-07):** EVERY build brief / dispatch guidance / resume recap the PM produces MUST end with an **`EQUIP:` line** naming the exact skills per the matrix in [skills-and-build-tooling.md](maltyweb-pm-memory/skills-and-build-tooling.md) §SKILL-FIRING MATRIX — read that section whenever equipping ANY agent. Shorthand: app/backend→`coder`+`sql`; rendering→+`ui` (frontend-design is folded in); parsers→`parser-coder`(+`sql`); **deployed-page verification→`webapp-testing` smoke step BY DEFAULT (READ-ONLY vs prod)**; auth/2FA→coder + name `references/php-security.md`; dataviz design→ui + name `references/dataviz.md`; InnoDB depth→sql + name `references/innodb/`; spreadsheet I/O→`xlsx`; 3rd-party skill→`skill-vetting` FIRST; skill authoring→`skill-creator`; memory compaction→`memory-hygiene`. Mis-equipped dispatch → PM flags proactively + calls re-launch. Kouros never reminds.
- **Opus orchestrates, Sonnet codes.** Spawn build agents as `model: "sonnet"` with a self-contained prompt (task + constraints + verification steps + "report back"). Opus reads the report, spot-checks the diff.
- **🔴 COGS-BEARING CLAIMS: OPUS VERIFIES INDEPENDENTLY (standing arc discipline 2026-06-07 — 3 sub-agent gate-PASS failures in ONE day: TEA lump-sum, dry-hop cross-recipe contamination, per-MI window).** A sub-agent's "gate PASS" is a CLAIM. Before any COGS/COP/WAC/BOM number reaches the operator, Opus verifies against an independently computed reference (hand-computed per-recipe numbers, WAC order-of-magnitude bands, before/after invariants). Existence/positivity gates are insufficient — assert PLAUSIBILITY. → conventions-and-helpers.md + walkthrough §F2 COMPILER VERIFICATION RECORD.
- **Agents leave NO helper scripts in the repo** — use `/tmp`, delete; verify `git status` post-run.
- **🚨 DEPLOY PUSHES THE WORKING TREE, NOT HEAD (divergence hazard, generalized 2026-06-09):** `bin/deploy.sh --apply` rsyncs the ENTIRE local working tree to prod — so with PARALLEL SESSIONS live (since 2026-06-07) ANY uncommitted change from ANOTHER session leaks LIVE on your deploy. PROVEN 2026-06-09: a login-hint deploy carried OTHER sessions' uncommitted `app/auth.php` access-control reorder (per-user override now precedes role floor in `user_can_access()`), `app/tank-simulator.php`, and `public/js/packaging-form.js` to prod — none committed at deploy time. RISK: a clean-checkout deploy (or anyone deploying from a freshly-committed tree) would SILENTLY REVERT live prod behavior to whatever HEAD holds. This is the same live-but-uncommitted smell class as the resolver/parallel-migration cases, now at the deploy-mechanism level. **DISCIPLINE: commit-before-deploy (or at minimum `git status` the tree and confirm nothing foreign is dirty before `--apply`); ideally deploy.sh grows a dirty-tree warning.** When you deploy, your push state = the tree on disk, NOT your commits. **The dirty-tree WORKAROUND (surgical file-scoped scp + `diff` deploy-delta verify vs blanket rsync) is now CODIFIED in the `coder` skill (`~/.claude/skills/coder/SKILL.md`, "Dirty shared tree → deploy surgically", with the `4af2155` réassigner-cuve worked example) — equip `coder` and follow it when deploying off a dirty shared tree.**
- **🚨 COMMIT WITH A PATHSPEC, NEVER BARE `git commit` — same shared-tree class at the COMMIT mechanism (REALIZED + recovered 2026-06-10, commit `711905c`).** The operator routinely leaves files `git add`-STAGED mid commit-prep on this shared tree. A bare `git commit` (or `git add -A` + commit) therefore SWEEPS THEIR STAGED WORK into YOUR commit. PROVEN: the stock-drill commit's first attempt as a bare `git commit` swept in the operator's staged `kpi-handlers.php`/`mon-tableau.css`/`kpi-charts.js`; caught immediately → `git reset --soft HEAD~1` (un-commits, keeps everyone's staging intact) + re-commit via `git commit -- <my explicit paths>` (pathspec form commits ONLY those paths, preserving the operator's exact staging) → clean 3-file `711905c`, bad commit never left local. **DISCIPLINE: ALWAYS `git commit -- <explicit file paths>`; never bare `git commit`, never `git add -A`. If a bare commit already entangled foreign staged files, `git reset --soft HEAD~1` then re-commit by pathspec.** (Reinforces the existing `git add <files>` not `-A` rule — this is the commit-time companion.)
- **Migration pre-flight** (SQL in scope): `migrate.php --status`; FK type matches target PK exactly; MySQL-8 syntax (no IF NOT EXISTS on ALTER, no bare SELECT); FK data integrity; `schema_meta` row per NEW table; selective rsync of only changed files. NB: `maltytask_app` HAS CREATE VIEW + SHOW VIEW (granted 2026-05-26). **`schema_migrations` PK = `filename` VARCHAR (NO `version` col) — idempotency is filename match. `audit_row_revisions.action` ENUM is `('insert','update')` ONLY — DELETE convention = `action='update'` + `after_json='{"_tombstone":"deleted_by_<migN>"}'` + before_json full state.**
- **RULE 1 — architecture cleanliness + scrapping (standing duty EVERY build):** evaluate what the change makes useless/redundant and log it in scrapping-backlog.md with a gate. A superseded surface left in place = the same divergence smell as a parallel data store. Retire only after the replacement covers it AND nothing routes to it.
- **RULE 2 — commit-stage code review is a HARD COMMIT GATE:** before EVERY commit, dispatch a fresh-context reviewer (Sonnet) auditing BOTH (a) coding soundness/correctness (logic, SQL safety, transactions, XSS, auth/CSRF/log_revision/PRG/whitelist) AND (b) dead-code/redundancy scrapping (feeds RULE 1). Address or explicitly-accept findings before the commit lands. **Multi-bundle parallel bursts: ONE fresh-context review over the COMBINED diff BEFORE any commit (not per-bundle, not retroactive) — proven 2026-06-07 ui-ux Phase 3 (caught 2 P1s pre-commit incl. a flow-change a11y gate); pair with single-owner rule for shared files (app.css = one agent only).**
- **RULE 3 — TOUR COVERAGE (standing duty when a build touches `ref_pages`):** every page the operator can see needs a Visite-guidée card. The predicate for "needs a tour card" = `is_active=1 AND (domain IS NULL OR domain <> 'admin') AND page_key <> 'saisies'` (admin domain excluded; `saisies` special-cased by the tour into its own opener + form chapters). After ANY build that adds/activates/renames a ref_pages row, **PM dispatches the `maltyweb-tour-steward` agent** (sonnet; EQUIP coder+ui+webapp-testing) to author house-voice tour copy. Detection is automated: `scripts/tour-gap-check.php` (read-only drift detector, live `ref_pages` MINUS the 3 content maps in visite-guidee.php; tiers CRITICAL=no description / MINOR=no icon / INFO=no vignette / LATENT=inactive-no-card; exit 1 on CRITICAL|MINOR) runs as a non-blocking advisory inside `migrate.php` whenever an applied migration's SQL mentions `ref_pages`. The steward drafts-to-rules + deploys + smokes autonomously for non-sensitive pages, but for the four sensitive classes (COGS/COP/WAC/BOM/beer-tax · identity/access · ambiguous-purpose · self-flagged-uncertain) it STOPS pre-deploy and returns the draft flagged `PM-RATIFY: <page_key>` — a subagent can't call PM, so the orchestrator routes that draft to PM for ratification and relays the ruling. PM OWNS dispatching the steward. → §VISITE GUIDÉE.
- **Migration-number pre-flight is the FIRST step, not the fallback** — the VPS sequence can LEAD local git (the 204→215 mother-shell catch + the 200 near-miss both validated this). Re-check `migrate.php --status` IMMEDIATELY before assigning a number. **With PARALLEL SESSIONS in the repo (live since 2026-06-07), ALSO `ls db/migrations` for un-applied/untracked files holding a number** — the 278 duplicate — BOTH `278_kpi_seed_v2_batch` AND `278_users_tour_seen_at` applied live, tour file untracked — is the proof case (remediation → RESUME POINT #10). **And: migration files NEVER self-INSERT into `schema_migrations` — migrate.php does the bookkeeping** (2026-06-07 incident: self-INSERT → migrate.php reported FAIL on a fully-applied migration). **And: when REMEDIATING a collision by rename, the rename + the `schema_migrations` filename UPDATE land in the SAME step, AND the stale old-name file must be `rm`'d on the VPS — rsync deploy does NOT delete removed files (2026-06-07 mig-280 remediation).**
(Full conventions → conventions-and-helpers.md + CLAUDE.md + the skills.)

## ⏭ RESUME POINT (2026-06-05 — fermenting/saisie/tank-sim chain COMMITTED+PUSHED, maltyweb HEAD `815e725` / maltytask HEAD `dd6ca28`)
**🟡 WAITING ON OPERATOR.** The fermenting-correctness + saisie-edit + tank-sim + packaging-parallel-run chain is all SHIPPED+LIVE+PUSHED. Operator is in backlog-data-entry / production-use mode. Several PENDING BUILDs below are PM-blessed but NOT YET BUILT — dispatch when operator unblocks.

**STILL-OPEN / NEXT (one-line each; detail in the cited thematic file):**
V1. **🟢 v1 `bd_*` TABLES DECOMMISSION — STAGE 1 + STAGE 3 COMPLETE (2026-06-11); v1 tables now FROZEN (no readers, no writer); only Stage 4 (DROP) remains, gated on soak + re-grep + operator. PHASE OF BSF-EXIT.** ✅ **Stage 3 (STOP v1 writer) DONE — commit `0449a95` (maltyweb):** operator CONFIRMED all brewing/ferm/racking/packaging data entry moved to maltyweb saisie forms (→ `bd_*_v2` direct); BSF Google Forms RETIRED → **BSF-exit Phase 6 hard-gate CLEARED for these tables.** DISABLED the `maltytask-bsf-ingest` cron (`/etc/cron.d/maltytask-bsf-ingest`, drove `ingest.py --tab=all --apply` → v1 every 30 min); active line commented w/ dated rationale, deployed, VPS-verified (no active bsf-ingest line). **Pre-stop v1↔v2 reconciliation CLEAN:** v2≥v1 except brewday 835/831, cooling 2322/2286, timings 2306/2271 — v1-only rows ALL 2021-22 contract/collab batch=NULL legacy artifacts (BLZ/Chien Bleu/Bsr du Château/NYL/Combières/MeltingPote/BadFish), no consumer, no real data lost. ✅ **Stage 1 (ALL READERS REPOINTED):** tanks.php (`f398163`+`e063ddf`), sku-bom-compile.php (cooling `aa6fb0f` + ingredients `b17c686`), packaging.php (`aa6fb0f`), beer-tax.js + build-efficiency-data.ts (`262945a`), refresh_recipe_profile.py whole-file (`ed41319`), parse-tank-simulation.ts (`face01f`). COGS-bearing repoints Opus-verified (cooling 0.0°P core OG / 0 tax shifts; ingredients 0 keys lost / 6 = v1 g/kg DATA BUG v2 corrects; batch-HL 753 identical / 8 corrective e.g. Embuscade 236 32.8→128.0 HL). **✅ recipe-profile cron CONFIRMED ENABLED ALL ALONG (commit `b9016d0`, maltyweb, 2026-06-11; VPS-verified) — the earlier "DISABLED pending smoke / operator must re-enable" note was WRONG; action item SCRUBBED.** `/etc/cron.d/maltytask-recipe-profile` runs nightly 03:00 UTC; smoke-passed `refresh_recipe_profile.py` as maltytask cron user (61 recipes, 122 profile rows, 146 malt/133 hop, 0.8s, 0 errors; one --apply primed `ref_recipe_profile`). `b9016d0` DROPPED the cron's obsolete step 1 `parse_bd_ingredients.py --apply` (legacy v1 free-text parser → frozen v1; refresh now reads parsed_v2 per `ed41319`) + fixed stale header (script has NO --dry-run; omit --apply to preview); cron is now refresh-only, still enabled. **STAGE STATUS: 1 ✅ DONE; 2 MOOT (event_date anchor); 3 ✅ DONE (`0449a95`); 4 (snapshot→soak→DROP + remove db-correct v1 branch + validate_fk_candidates refs + **`parse_bd_ingredients.py`** + schema_meta cleanup) GATED on ≥1-2wk soak now ticking + final full both-repo re-grep + operator go-ahead.** Remaining v1 refs NOT consumers: deferred (db-correct.php admin UPDATE on v1 ingredients_parsed.mi_id_fk, validate_fk_candidates.py dev utility, **`parse_bd_ingredients.py` — now referenced by NO cron + NO consumer (only output = v1 bd_brewing_ingredients_parsed, read only by deferred db-correct.php branch); retire at Stage-4 DROP alongside ingest.py/tab_*.py**) + 1 comment (loss-metrics.php:42). **✅ CLOSED — ingest-root ticket: `bd_brewing_timings_v2` NULL recipe_id_fk FIXED (2026-06-11, `2037e2f` maltyweb + mig 334).** Writer `form-brewing.php` built every sibling v2 row with `recipe_id_fk` but OMITTED it from `$timingsRow` → 28 NULL-FK timings rows (2026 core: Zepp 215/216/217, Moonshine 124/125, Embuscade 235/236). Fix = (1) add `recipe_id_fk` to `$timingsRow` (deployed); (2) **mig `334_backfill_timings_recipe_id_fk.sql`** backfills the 28 from the brewday_v2 sibling per (beer,batch), guarded `recipe_id_fk IS NULL` (idempotent); applied via `migrate.php` as `sudo -u www-data` (bare `php migrate.php` as ubuntu FATALS on db.env perms). PM-verified live: NULL 28→0 of 2271. Optional future hardening: re-key the (beer,batch) timings reads onto the now-populated FK — NOT required, (beer,batch) works. ROOT RULE (D): beer_raw flipped ~2026-05-29 to bare-name+batch-col — never reverse-parse 'PREFIX BATCH'; Stage-4 re-grep before DROP is MANDATORY (parse-tank-simulation.ts was a truncated-grep miss). KEEP: bd_packaging_readings/bd_cip_events/bd_tank_readings. Full plan/audit → [v1-bd-tables-decommission.md](maltyweb-pm-memory/v1-bd-tables-decommission.md). EQUIP coder+sql(+ui+webapp-testing for deployed-page smoke); COGS/tax repoints = Opus per-recipe numeric verify.
0a. **✅ cage (-X) form SHIPPED** (`91ce8c0`, deployed 2026-06-07); 8 active -X SKUs now exist (ZEP/MOO/EMB/STI/SPY/DIV/DOA/ALT-X, ids 279-286, PM-verified live 2026-06-09). REMAINING: confirm whether -X SKUs carry `inv_fg_stocktake` anchor rows yet + R2 eshop cage-first netting once they do. Spec → [sku-bom-validation-walkthrough.md](maltyweb-pm-memory/sku-bom-validation-walkthrough.md) §CAGE-FORM BUILD.
0a10. **✅ STOCK DRILL-DOWN render-only cluster SHIPPED + LIVE + NOW COMMITTED 2026-06-10 (3 render-only ships, NO mig; clean 3-file commit `711905c` on main, LOCAL/UNPUSHED = `public/modules/expeditions.php` + `public/js/expeditions-stock.js` + `public/css/expeditions-sku-history.css`; SELECTIVE rsync deploy). ⚠️ PATHSPEC INCIDENT recovered (never left local): bare `git commit` swept in operator's staged kpi-handlers.php/mon-tableau.css/kpi-charts.js → `git reset --soft HEAD~1` + `git commit -- <3 paths>` restored operator staging + landed clean-3. LESSON now in CHECKLIST.** Inside `?view=stock` `exp-stock-ledger` per-SKU drill: (1) "Répartition par site" sub-block (reads existing `$fgLocationSnapshot`, no re-query); (2) "Activité récente" timeline (3 BULK ROW_NUMBER window queries page-wide, NO AJAX/endpoint, packaging predicates MIRROR `fg_prod_since_anchor()` except anchor-cutoff→window — R3 CLOSED); (3) **FLIPPABLE DRILL — `role=tablist` SEGMENTED TOGGLE (not 3D flip) splitting drill into Face "Stock" (default) + Face "Activité"** (timeline in OWN scroll-bounded table max-h 384px/overflow-y — the uncram fix); caps raised 15→50 events / 90→180d; JS = ONE delegated `button[data-evt-tab]` handler scoped `closest('.exp-stock-drill')` (NO per-row listeners) + `resetDrillFace()` on ALL 3 collapse paths + arrow-key nav; new `.exp-evt-tabs/-tab/-pane/--activite` CSS = ≥44px/focus-ring/aria-selected/100% var()/0 hex/0 `.exp-ledger-*`|`.exp-stock-*` redefinitions; a11y real buttons+role tablist/tab/tabpanel+region scroll pane. **BROWSER-VERIFIED 18/18 (Playwright, viewer→operator→reverted exactly, zero residual): independent per-drill state + reset-on-collapse confirmed.** php -l clean, page 200. **🟡 CSS fold-in debt:** `expeditions-sku-history.css` + `exp-ledger-loc-*` + 1 inline `opacity:.7` all gated on operator's in-flight 168-hunk expeditions.css overhaul deploying. EQUIP ui+sql+coder+webapp-testing. → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §FLIPPABLE DRILL-DOWN + §PER-LOCATION PHYSIQUE + §"Activité récente".
0a9. **✅ MULTI-SKU MOUVEMENT INPUT SHIPPED + LIVE + COMMITTED + PUSHED 2026-06-10 (extends ITEM-1 Phase-3 `?view=mouvements` scalar→multi-line; NO mig, HEAD stays 303).** Shared header (origine/destination/date/comment once) + repeatable SKU+Qté grid; `add_movement` reworked to parallel arrays `mov_sku_id[]`/`mov_qty[]`, ALL lines validated BEFORE `beginTransaction()` → **all-or-nothing** (bad line → zero writes, "Ligne N"), one txn / N inserts, **per-row log_revision preserved** (handler expeditions.php:1013-1054). **SINGLE-HOME PRESERVED = N `inv_stock_movements` rows, NO batch table** (multi-SKU = N route-sharing facts, not a new entity). Net-zero 8599==8599 both sides pre/post test. `expeditions-form.js` now also loads on `view=mouvements` (`initMovLines()`). NO new `ref_pages`/tour card (same page). **COMMITTED IN `badd153` (origin/main) — NOT the planned standalone commit: a PARALLEL session bundled it with the order stock-risk modal (`exp-sdm`) + Stock PF header restyle into ONE commit whose MESSAGE omits mouvements (UNDER-DESCRIBED); handler verified intact in HEAD.** ⚠️ **TWO-CONCURRENT-SESSIONS HAZARD today** (HEAD then `e32d472` unpushed + dirty = the OTHER session's mon-tableau work, migs 310/313) — deploy-pushes-working-tree means an `--apply` leaks the other session's uncommitted changes; `git status` before any deploy. EQUIP ui+sql+coder+webapp-testing. → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §MULTI-SKU MOUVEMENT INPUT.
0a6. **✅ TAPROOM SINGLE-UNIT-SKU GAP CLOSED + WEEKDAY-CHIP BUG FIXED 2026-06-09 (no mig; data + `expeditions.php`; commit `afe1dcd`).** (1) DGDBU CREATED `ref_skus` id=315 (recipe 31 / format_id=5 BU, hl_per_unit=0.00330 from format, scope='single', 6/6 BU-sibling-conform, 4-line packaging BOM, liquid line=0 at PHP-recompile like all BU siblings; is_direct_sales=0 flagged to Kouros). (2) Contract beers OUT of taproom — BAMB/BAMF/JASPB/JASPF scope 'base'→'cage'; DECISION: NO contract singles (BAMBU/JASPBU NOT created). (3) Weekday chip now derives via `exp_dow_fr()` (was hardcoded "lun."). ⚠️ **`afe1dcd` SHIPPED BROKEN — correcting an earlier mis-report of it as a clean ship:** `exp_dow_fr()` was placed NESTED inside the stocktake render block, so it was undefined at the EARLIER ~L2824 locations-overview fresh-chip ("✓ compté") call site → today's live taproom count tripped "Call to undefined function exp_dow_fr()" → PROD 500 on the WHOLE PF-stock page. `php -l` passed (valid syntax / conditional decl) and the deployed-page SMOKE WAS SKIPPED, so it shipped broken. **FIXED IN PROD BY KOUROS (not the build agent) = `ae297c2` "hoist exp_dow_fr to top level"** (Kluk76, 2026-06-09 15:57; helper moved to the unconditional top-level date-helper section ~L934; on origin/main, VPS-deployed, php -l clean). Functional outcome stands (weekday derives, 2026-06-09→"mar.") AND the 500 is gone. DURABLE LESSON (also captured in the `coder` skill): PHP nested fn defs are runtime-declared in CALL ORDER — a helper used before its later nested def fatals; keep page helpers TOP-LEVEL, and SMOKE the deployed page (never trust `php -l` alone for a function-existence/runtime fault). → header §EXPÉDITIONS OUTAGE FIX `ae297c2`. OPEN (asking Kouros): DOCB rescope base→cage pending confirm; DEFERRED — move 21/24 DGDB stopgap into DGDBU as 21 units via the FG-stocktake EDIT feature (NOT ad-hoc SQL), pending taproom input complete. → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §TAPROOM SINGLE-UNIT-SKU GAP ✅ SHIPPED.
0a4. **🆕 WeeklyOrders SHEET → ord_orders INGESTION — PM RULINGS RECORDED 2026-06-09 (pre-build; dry-run→match-report→Kouros reviews unmatched→apply).** Scope=KW24 full + ALL future booked, EXCLUDE Feb archive; status=every-reached-stage event (occurred_at=requested_date, comment='import:weeklyorders'); SKIP Taproom/Eshop/samples as ord_orders (R1 double-depletion — but a BC-numbered Marketing/privé sold-to IS a customer order, keep); customer match = BC#→exact→alias, NO-fuzzy-auto-link (flag only), reuse merge/Carnet resolver; Cité festival = N orders (sub-location in comment); **DEDUP = new `ord_orders.source_ref VARCHAR + UNIQUE` (mig, next free ~292 VERIFY — 291 taken by Tap&Shop), NOT a line-hash; the 12 existing `source='web'` KW24 rows are REAL operator orders — never reconcile/replace, flag web-overlap collisions for operator**; off-trade = customer attribute, ignore on order. EQUIP sql+coder+sheets/xlsx-read. Full record → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §WeeklyOrders SHEET INGESTION.
0a8. **✅ THE 4 COGS TILES (#168/#169/#171/#172) ARE LIVE — OPTION C EXECUTED + SHIPPED 2026-06-10.** Producer = **`scripts/build-cop-report.ts`** (maltytask repo, MySQL-native, replaces dead/archived BSF `build-cogs-report.js`) — reuses `lib/cop-indirect.js`/`lib/utilities.js`/`lib/inventory.js` VERBATIM (no divergent COP), adds 4 MySQL loaders (per-SKU cost split `ref_sku_bom×ref_skus×ref_packaging_formats×ref_mi` via `CAT_KEY_MAP`; packaged HL `bd_packaging_v2.vendable_hl` neb-only; brewed HL `bd_brewing_gravity_v2` Cooling final_volume; BC charges `inv_charges_bc`), `withConnection()` over tunnel 13306, **ZERO DB/Sheets writes** → emits local `interfaces/cop-report-data.json`. **Reconcile gate PASSED EXACT** (per-SKU Σ==`ref_sku_bom` Σ to 0.000000: ZEPF/EMBB/ZEPV). Emits SLIM handler-shape only (`cop.totalVariables`/`cop.brewing.{malts,hops,ingredients}.current`/`cop.{packaging,indirect,utilities,rd}.total`); yeast folded into "ingredients" (handler #171 reads malts+hops+ingredients). **Deployed** to `/var/www/maltytask/interfaces/cogs-report-data.json` (handler's hardcoded first candidate; `maltytask:www-data` 644; NO PHP/handler edit, NO mig; kpi_cache per-request). Smoke (www-data kpi_dispatch, period **2026-04** closed): #168=75773.73 / #169=87.16 → **TILES LIVE.** **✅ APR+MAY CHARGES BC INGESTED + COP RE-RUN + REDEPLOYED 2026-06-10 — April COP now COMPLETE: brewing 13928.33 / packaging 33288.24 / indirect 16404.73 / utilities 13548.07 / rd 189.46 → totalVariables 77169.37 / 88.76 CHF/HL. Tiles now #168=77169.37 / #169=88.76 / #171=13.64 / #172 full, all kpi_dispatch-verified** (was 75773.73/87.16 pre-charges). **🔧 NEW DURABLE FALLBACK = maltyweb `scripts/php/ingest-charges-bc-csv.php`** (deployed `/var/www/maltytask/scripts/php/…`, UNCOMMITTED): server-side CLI reusing `charges-bc-upload.php`'s EXACT helpers + row_hash (BYTE-IDENTICAL → future UI re-upload dedups to 0), dry-run default / `--apply` INSERT IGNORE + audit, `last_modified_by='ingest'`. **USE THIS whenever the Charges BC UI is gated** (operator off-Tailscale, blocked by admin gate). Applied Apr 20 / May 22 rows (1 dupe); PM live-verified Apr+May present, all `ingest`, GLs 4302/4500/4510/4600/6100. **📋 OBSERVATION (bookkeeper, non-blocking):** May GL4510 came SUMMARY-ONLY (entry_no=null/0.00 — no May R&D 4510 invoice or absent from export); April 4510=160.04 fine. Flagged to operator. **🔴 OPEN:** (1) **AUTOMATION/freshness-chip STILL pending** — regen MANUAL (local tsx over tunnel + sudo install), NO cron, producer NOT on VPS → as-fresh-as-last-run, against maltyweb-prefers-live (SEQUENCING below); (2) **May COP lands after May CLOSED + re-run** (charges already banked); (3) **TWO scripts UNCOMMITTED** (commit-on-ask): `build-cop-report.ts` (maltytask) + `ingest-charges-bc-csv.php` (maltyweb); (4) **admin Tailscale-gate UX recurs off-Tailscale** — open Q: Charges BC reachable off-Tailscale vs stay gated w/ CLI fallback. EQUIP coder+sql. Full as-built → [kpi-tracker-catalog.md](maltyweb-pm-memory/kpi-tracker-catalog.md) §OPTION C EXECUTED + §FOLLOW-UP 2026-06-10. **This RESOLVES the §THE 4 COGS TILES / step-118(a) / Option-C-ruling thread.**
0a5. **✅ SHIPPED 2026-06-09 — PER-FORMAT MAGNITUDE GUARD (NARROWED to cuv serving-tank FILL VOLUME only).** maltyweb commit `f019526`; files `public/js/magnitude-guard.js` (NEW) + `packaging-form.js` + `form-packaging.php`. Scope narrowed per Kouros ("volume/litre fields only") — counts (keg/bot/can) + loss/taproom litres NOT guarded (no HL-vs-L slip mode; an OOM band on counts would false-flag 6-keg Jasper / 100-bottle Chien Bleu). **Band from canonical `ref_serving_tanks` (5/10/30 HL) → 50–30 000 L; SERVER re-evaluates fresh per POST, no-ops if table absent (never blocks a submit).** Wired via existing `FormFramework.extraWarnings()` + outlier confirm-with-comment dialog; token `magnitude_outlier_confirmed=1` → `bd_packaging_v2.audit_flags`; stored value byte-identical on confirm; edit-mode suppresses re-nag (`data-mag-confirmed-fields`). Tests 28/28 client + 8/8 server-isolation (live DB); live UI round-trip NOT auto-tested (webapp-testing lacked prod role) → 4-step manual handed to Kouros. **Historical audit: 255 cuv rows, 0 out-of-band — the ZEPV 100× was purely `ref_skus.hl_per_unit` config in the sales→COGS/compiler lane, NOT operator input** (separately: 9 cuv rows prod_L=0 / neg vendable_hl, ids incl. 2160 [−10HL], 2217 [−5HL] = tank emptyings/transfers, flagged as a different category). **UPSTREAM ✅ RESOLVED 2026-06-10: -V revert (`ref_skus.hl_per_unit` 1.0→0.01 for EMBV/MOOV/ZEPV) + per-litre recompile + compiler liner-branch fix SHIPPED; ZEPV sales material ≈937 CHF, cutover ZEPV gate CLEARED on technical grounds (flip pending only a commit + the cutover-thread procedure). See RESUME #0 🟢🟢 block.** Brewing/racking magnitude guards = possible future extension via the reusable `MagnitudeGuard` helper, NOT built. → [saisie-forms-5-correction-arc.md](maltyweb-pm-memory/saisie-forms-5-correction-arc.md) §MAGNITUDE GUARD §SHIPPED RECORD.
0a11. **✅ SHOPIFY⇄MALTYWEB FULFILMENT INTEGRATION — FULL BUILD COMPLETE 2026-06-11 (autonomous; Kouros reviews on return).** Phase 0 (mig **321** maltyweb + ingest `bac5501` maltytask): `ref_shipping_methods` 16 rows + `inv_sales_orders.shipping_method_title`/`fulfilment_mode`; ingest captures `shipping_lines`, classifies pickup/delivery/review, line-idempotency bug FIXED, `--until=now`; May+June backfill → 3127 orders (delivery 2587/pickup 488/review 66), FG delta −5. **⚠️ 15-min ingest cron is on the LOCAL WSL box over tunnel 13306, NOT the VPS — single point of failure, VPS-host recommended.** Phase 1 (`e7962ce`): per-order eshop rows + 🏬/🚚/⚠ badges, read-only, NO `ord_orders` twins, FG untouched. Phase 2A (mig **322**, `5508be1`): `inv_sales_fulfilment` + `_events` side tables + `eshop-fulfilment-status.php` API (mode-aware, review-refuses-advance, push-trigger sets `shopify_sync_state=pending`, NO inline Shopify) + chips/badge — **LIVE, XOR preserved, fg-stock untouched**. Phase 2B (`a08a718`): `push-shopify-fulfilment.ts` write-back worker BUILT but **DISARMED + DOUBLE-GATED** (no cron + token has NO write scopes, 403 confirmed) — activation needs team to grant `write_merchant_managed_fulfillment_orders`+`write_fulfillments` + reissue token + `--apply` verify. Phase 3c (mig **323**, `f0c57de`): `ref_skus.is_non_stock` + symmetric exclusion in BOTH fg-stock fns, verified NO-OP (8003=8003, 0 flagged) — T7 infra ready. **DEFERRED:** 2B activation; Phase 3a/3b (gated on team standardizing Shopify shipping-names); Phase 4 cage/box COGS (gated 06-15 anchor). **✅ Σcards≠Σphysique INVARIANT RESTORED — FIXED+DEPLOYED+VERIFIED+COMMITTED 2026-06-12 (maltyweb `d9f7976` "fix(fg-stock): gate depletion legs on stocktake_scope != 'none'"; NO mig).** ROOT CAUSE was NOT §C sales-attribution — it was the 3 depletion legs (B2B Step4/Leg1, eshop Step5/Leg2, taproom Step6/Leg3 in `app/fg-stock.php`) gated ASYMMETRICALLY across `fg_stock_compute()` vs `fg_stock_location_snapshot()`. FIX = gate all 3 legs symmetrically on `stocktake_scope <> 'none'` in BOTH fns (transfer leg untouched); the dead `is_non_stock=0` eshop filter (mig 322, 0 rows) REPLACED by the scope predicate. **Opus independently verified:** gap=0 + per-SKU divergences=0 (both fns agree to the unit); all 13 scope='none' phantoms excluded; B2B compute expedie=1393 == raw scope!=none shipped=1393 (no legit depletion dropped); eshop/taproom INNER JOINs orphan-fk=0; NULL-sku_id_fk rows (722 eshop/497 bc) were already undepletable pre-gate (no-op); taproom=0 legit (latest bc period 2026-05, leg window period>2026-06); the 4 honest-negatives on REAL inventoried SKUs (ALTB/DGDF/DIB4/STI4C) untouched (scope!=none). **Level note: Σphysique now 15386 (was 8177) because a stocktake census was entered 2026-06-09 → anchor_qty=14701 — NOT a regression, explained by census.** **`is_non_stock` column now fully UNUSED/INERT** — left in place; **RULE 1 scrapping decision PENDING OPERATOR: backfill-from-scope OR scrap (non-blocking).** **🔴 CARRIED-FORWARD OPEN PHASE-4 ITEM (flagged to operator, NOT silently closed): eshop-bundle COMPONENT depletion — a B12 box / 4-pack sale consumes real component bottles, but until the cage/box decomposition engine lands (gated on 2026-06-15 cage anchor + the unit-nesting-ladder decision 24↔single↔12↔4 = which SKU is inventory-of-record) that component drawdown is NOT reflected in FG stock. ZEPV/-V cuve needs NO such follow-up (liquid accounted at tank stage per the -V liner arc).** **⚠️ Parallel sessions committing to BOTH repos' main; only `git add <my-files>` + commit, NEVER reset on shared branches (one reset incident caught + restored this run).** MIG HEAD now **323** applied — re-`--status` at next build. EQUIP per-phase coder+sql(+ui+webapp-testing for 2A). Full as-built + Kouros review summary → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §FULL SHOPIFY⇄MALTYWEB FULFILMENT BUILD COMPLETE.
0a7. **🆕 OPERATOR ATTRIBUTION STABILITY + GLOBAL EVENT-RECAP PAGE — PM-RULED 2026-06-09 (pre-build).** Trigger "opérateur"/"saisies récentes"/"attribution"/"Corriger"/"recap saisies"/"event feed"/"journal des saisies"/"who modified" → READ [operator-attribution-and-event-recap-arc.md](maltyweb-pm-memory/operator-attribution-and-event-recap-arc.md) first, recap unprompted. ROOT CAUSE: attribution = `bd_*_v2.email` STRING-COPY of `$me['username']` (NO user FK anywhere on bd_*_v2), re-stamped on EVERY write incl. edit across all 4 forms (~13 sites) → Kouros's Corriger of Gonzalo's racking (id=434, Speakeasy/66) clobbered email→"Kouros Ghavami". Audit ("who/when/what") ALREADY EXISTS + correct in `audit_row_revisions` (log_revision, action insert/update, before/after_json, user_id/username). RULINGS: ASK1(B) stopgap (no mig) = edit path preserves original `email` not re-stamp + fix id=434→Gonzalo (operator-stated, not inferred); ASK1(A) durable (mig) = additive nullable `submitted_by_user_id_fk`→users(id) set-once-never-on-edit, backfill unambiguous-only (refuse-don't-guess), display `users.display_name` (NB username=full-name/display_name=short-name INVERSION). ASK2 = read-only UNION-feed page over bd_*_v2 (NO parallel store), `general` domain sort=15 below sb-board=10 ("Lots en cours"=`sb-board.php`); row identity=(source_table,row_pk); drill-down = live row + `audit_row_revisions WHERE target_table/target_pk`. SEQUENCE: ASK1 first (foundation), then ASK2. EQUIP ASK1(B) coder+sql / ASK1(A) sql+coder+ui / ASK2 ui+sql+coder+webapp-testing. COGS-NEUTRAL/SOT-safe. mig head 292/next free 293 (VERIFY).
0a3. **✅ TASK #10 STOCK PF BOARD SHIPPED 2026-06-09 (maltyweb `24d510a`, LIVE+PM-VERIFIED; the spec request 2026-06-09 was answered "already built").** All 4 build-order items DONE: R4 per-SKU anchor fix (latest count per (sku_id_fk,location_id_fk) via MAX(id), SUM across locations, anchor_date=MAX(counted_at) — global MAX(month_closed) zeroing bug GONE), `fg_stock_location_snapshot()`, `expeditions.php ?view=stock` rebuilt (4 location cards + freshness chips + TOTAL strip + 2 derived lines + display_family-grouped per-SKU table, multipacks split), pinned Commandes stock-strip. LIVE-VERIFIED: anchor 2026-06-08, 34 SKU rows, Σcards==Σanchor 7080==7080, Taproom NULL→graceful zero. **OPEN: uncommitted working-tree change to `expeditions.php` = back-date role gate on stocktake POST (managers/admins only; operators coerced to today; explicit '0' writes a stock-out) — separate in-flight piece, needs its own RULE-2 review + commit.** **✅ TWO PF-STOCK FIXES LANDED 2026-06-09 (committed+pushed+deployed, origin/main afe1dcd..566b9d3, NO mig): (a) OUTAGE `ae297c2` — `exp_dow_fr()` was nested inside the stocktake render fn → undefined at the earlier fresh-chip call site → today's count tripped a 500 on the whole PF-stock page; hoisted to top-level (DURABLE: PHP nested fn defs are runtime-declared in call order, keep page helpers top-level). (b) PRODUCTION-LEG `566b9d3` — the PM-predicted bug: flow windows ran off a single global `$anchorDate` while the anchor SUM is per-(sku,location), so today's partial Taproom count poisoned the cutoff and dropped same-day production-site packaging (DIV4/DIVB invisible). Fix = production site DB-derived (`ref_sites site_type='production'`, set-valued no hardcode) + per-SKU prod cutoff MAX(counted_at) at prod site(s) w/ COALESCE→global fallback + `run_type='cuv'` EXCLUDED from prod (3 guards); SALES legs (expedie/eshop/taproom) STILL global — per-location settlement DEFERRED to the stock-movement-saisie arc. Live-verified DIV4 prod=412.54/DIVB=108/ZEPV absent.** **✅ sales-leg per-location settlement NOW CLOSED (ATTRIBUTION) by ITEM 1 2026-06-09 (mig 299):** `fg_stock_location_snapshot()` attributes sales per site via the single `resolve_fulfilment_site()` + applies `inv_stock_movements` transfers; Σ(cards)==Σphysique=8642. **✅ SAME-DAY CUTOFF — ESHOP + TRANSFER NOW CLOSED by TASK A 2026-06-10 (commit `2192153`, NO mig, `app/fg-stock.php`):** eshop leg made SYMMETRIC (compute Step 5 mirrors snapshot's per-(sku,warehouse-site) `>=`+created_at tiebreak via `fg_site_sku_anchor_map()`; strict-`>` fallback for uncounted SKUs) → Σcards==Σphysique under same-day; transfer leg = atomic per-row UNIT-GATE (`$rowPass=$fromPass||$toPass`, both legs or neither → net-zero, compute stays transfer-exempt). SCOPE NOTE in `fg_stock_compute` rewritten to per-leg table. Divergent-anchor fixture (eshop SKU60 + cross-site transfer SKU39 fromPass=T/toPass=F) 10/10 PASS. ACCEPTED TRADE-OFF: same-day inbound transfer to a same-day-recounted site transiently overcounts until next count (PM-ruled OK for the hard net-zero invariant). **REMAINDER (deferred): per-site same-day TRANSFER precision (unit-gate trade-off) + taproom MONTH-granularity** (B2B = strict-global symmetric, intentional). Prod-leg same-day = closed earlier via `d3fb954` (NO DATETIME mig); Task A had no mig (HEAD stays 299). Full record → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §TASK #10 SHIPPED + §TWO PF-STOCK FIXES + §ITEM 1 SHIPPED. **🔴 STALE-INVARIANT WARNING (2026-06-11): the Σcards==Σphysique equalities asserted above (7080==7080, 8642, 8003) NO LONGER HOLD on the live page — `fg_stock_location_snapshot()` now sums 7981 vs `fg_stock_compute()` 8003 (Δ22), confirmed PRE-EXISTING (predates Phase-3c). OPEN data-integrity item — see 0a11.**
0a2. **✅ SHIPPED + RECONCILED + PROVEN — `inv_fg_stocktake.row_hash` → STORED GENERATED column (mig 288, 2026-06-08; agent conn dropped mid-run → PM live-verified + reconciled; operator FINAL confirmation = arc record matches reality).** `row_hash CHAR(64) GENERATED ALWAYS AS SHA2(CONCAT_WS('|','fgct',sku_id_fk,location_id_fk,counted_at),256) STORED NOT NULL`, UNIQUE uniq_row_hash, 122 rows/122 distinct hashes/0 mismatch (tautology), form deployed. **FK RULING DONE: both `fk_fg_sku` AND `fk_fg_stocktake_location` re-added as ON DELETE RESTRICT** — live-verified `DELETE_RULE='RESTRICT'` on BOTH (fk_fg_sku was SET NULL; agent dropped it for the gen-col, restored as RESTRICT which MySQL 8 allows on a gen-col input; 0 orphans/0 NULLs; both FK cols now FK-enforced RESTRICT + hash inputs). **RECONCILE COMPLETE:** mig 288 file edited to fully describe live (FK drop + RESTRICT re-add), synced to VPS, `INSERT INTO schema_migrations` done → `migrate.php --status` = 288 applied / 0 pending (288 NOT re-run). **`expeditions.php` stocktake INSERT: `row_hash` DROPPED from col list (deployed live — gen-col is auto-computed); `$rowHash` compute KEPT for the before-snapshot SELECT.** **PROOF passed:** in a rolled-back txn, UPDATE `location_id_fk` 1→3 on a June row auto-changed row_hash (760cf472… → 4be4ec73…) — divergence now STRUCTURALLY IMPOSSIBLE. **Committed maltyweb main: `d1a0e40` (NOT pushed; prior multipack commit `f97bcb9` also local-only).** Separate same-day data-corruption repair (earlier): loc1=27/550u, loc2=28/5976u, loc4=26/554u — intact. EQUIP sql+coder. Forward-couples to task #12 admin edit-path (UNIQUE-collision handling). Full record → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §row_hash divergence §SHIPPED.
0. **🔑🏁 KEYSTONE — BOM VALIDATION: ① ✅ + ② ✅ + ③ F2 LIQUID ✅ + ④ DIRECT-SALES TRACKER MVP ✅ SHIPPED 2026-06-09 — ALL FOUR STEPS DONE (④ Phase 2 DEFERRED, gated; see #0b).** ④ shipped as **"Tap&Shop"** (operator-named; page_key='tap-shop', mig 291, commit `a13a9d0`, Opus-verified LIVE). F2 applied (147 SKUs / 1260 deleted / 1557 liquid rows / 0 parity / 0 errors); code maltyweb `9012c8d`+`0956471` (apply branch + parity gate + CLI `--apply`); 2 conventions LOCKED (liquid CHF-canonical; slot_name=stage); operator decisions ratified (DIG YEASTVIT fixed, finings gap-fill + forward-build saisie field, SPY/DIB floors, gate-15); DGD 18 kg/HL cleared. **🟢 F2 follow-ups: finings (`f639632`) DONE, warehouse $glSt cleared; -V cuv costing (`92c9c13`) BROKE sales-COGS (ADJUDICATION REVERSED 2026-06-09) → FIXED + cutover ZEPV gate ✅ CLEARED 2026-06-10 (cuv hl_per_unit revert + liner-branch per-unit generalisation; see the 🟢🟢 block below).** 7 cosmetic out-of-scope NULL rows remain. Full record → [walkthrough §F2 FOLLOW-UPS + §F2 LIQUID APPLIED](maltyweb-pm-memory/sku-bom-validation-walkthrough.md). **NEXT BOM-arc work = ④ Phase 2 (the cage/COGS engine, #0b below) — DEFERRED behind 2 operator-confirmed external deps (Shopify pickup-vs-shipped signal + cage anchor 2026-06-15); recette versioning v2 #0c SHIPPED (mig 289, `a1c68dc`).** **🟢🟢 COGS COST-SOURCE CUTOVER ZEPV GATE ✅ CLEARED 2026-06-10 (Opus live-verified VPS) — flip now GO on technical grounds.** The cuv revert + liner-branch fix shipped: `ref_skus.hl_per_unit` 1.0→**0.01** for EMBV/MOOV/ZEPV(27/46/61) (the only 3 divergent single-format SKUs; découverte composite packs PACKDEC/FRPACKDEC/FRPACKCHEF correctly LEFT ALONE = legit blended composite) + liquid recompile (`--apply --sku 27,46,61`, 0 parity) + **COMPILER DEFECT FIXED** in `app/sku-bom-compile.php` cuv `liner_amortized` branch (was HARDCODED `qty=1.0`/`cost_direct=$cuvLinerCostPerHl` per-HL → after revert, liner stayed per-HL while liquid went per-litre = unit mismatch inside one BOM → `build-sales-cogs.ts:612 materialCHF=units×SUM(cost)` would still mis-cost ZEPV ~7800; FIX = scale BOTH `qty=>$skuHlPerUnit` + `cost_direct=>round($cuvLinerCostPerHl*$skuHlPerUnit,6)`, generalising the hl_per_unit=1.0 special case; deployed + packaging recompiled, 0 parity; CHF/HL DISPLAY unchanged since cost/qty still recovers liner_cost_per_hl). **DURABLE LESSON: 92c9c13's liner-amortization was only ever exercised at hl_per_unit=1.0; it silently broke for any other value — the CHF/HL display gate NEVER caught it (display divides cost/qty), the COGS bug only surfaces in the SUMMED per-unit sales-material path. A COGS-bearing compiler line MUST be gated on the sales-material path, not display/conservation; per-unit lines MUST scale by the SKU's own hl_per_unit, never hardcode a basis.** Live-verified: `Σref_sku_bom.cost` ZEPV 0.096594/EMBV 0.224576/MOOV 0.121062, skuHL=0.01; ZEPV sales material ≈**937 CHF** (~100× phantom GONE); `--cost-source=diff` 2026-05 prints NO cuv per-SKU diff lines (sheet==mysql). Rollback snaps `_snap_refskus_cuv_hlrevert_20260610`/`_snap_skubom_cuv_hlrevert_20260610`. CONSISTENCY RULE STANDS (LOCKED): litre-denominated SKUs carry `ref_skus.hl_per_unit=format.hl_per_unit`, never diverge (JASPL=0.01 canonical). **🏁🏁 CUTOVER FLIPPED + SHIPPED + COMMITTED 2026-06-10 — maltytask `0129bdb` `feat(sales-cogs): cut over default cost-source sheet→mysql (post-F2)`.** `build-sales-cogs.ts` default `?? "sheet"→"mysql"` (~L131; `--cost-source=sheet` legacy fallback, `=diff` audit) + folded `run-month-close.js` step-3 (build-sku-bom) drop from critical-path parallel block. Deps already committed: maltyweb `6b8f6a0` (cuv liner per-unit fix) + `7fbeecb` (mig 302 DB-trigger lock cuv hl_per_unit=0.01) + the cuv revert+compiler-fix above. POST-FLIP VERIFY (regression tripwire): `build-sales-cogs --month 2026-05 --dry-run` NO flag → header `cost-source=mysql`, clean, wrote `interfaces/sales-cogs-data.json`. **May 2026 (mysql): 73 SKUs, COGS CHF 59,676.02, NonBeer 31,617.70, BeerTax 12,735.39 (sales 11,813.43 + invendables 212.03 + contract 709.93 on 46.74 HL); ZEPV 936.96.** RULE-2 GO signed off; drift = benign post-F2 pattern. TWO post-flip RESIDUALS (NOT blockers, BOTH PARKED 2026-06-10): (R-A) 3 unknown sales SKUs (`sales-sku-unknown=3`, pre-existing → triage pass post-close); (R-B) contract beers BAMB/BAMF/JASPB/JASPF/JASPL null beer-tax cat=0 CHF → PARK pending accountant sign-off (tax-liability-routing decision, NOT a code defect; do NOT guess the Cat-per-OG — COGS/tax-impacting). The PRIOR "RE-OPENED" verdict below is HISTORICAL.** Root cause: `mysql-sku-bom.ts` builds `skuHL` from `ref_packaging_formats.hl_per_unit` (=0.01) but `skuCost` from `ref_sku_bom` rows compiled at `×ref_skus.hl_per_unit (=1.0)` → TWO bases, same SKU; `build-sales-cogs` does `units(=litres 9700) × skuCost(9.8873 per-HL) = 95,907 CHF` (~100×). beer-tax stayed sane (uses `units×skuHL`=97 HL). **CANONICAL -V BASIS = PER-LITRE `hl_per_unit=0.01`** — matching format-18 (0.01, '1L liner'), sales uom='L' + hl_resolved (litres/100), and JASPL=0.01 (the contract -V sibling). **CONSISTENCY RULE LOCKED: ref_skus.hl_per_unit MUST EQUAL format.hl_per_unit for litre-denominated SKUs; NEVER diverge.** RESOLUTION ✅ DONE 2026-06-10: **liner session REVERTED hl_per_unit→0.01 for EMBV/MOOV/ZEPV + recompiled -V liquid+liner_amortized at 0.01 (their lane, `app/sku-bom-compile.php`) AND fixed the hardcoded-per-HL compiler defect that the revert exposed; the held flip is now UNBLOCKED on technical grounds (see the 🟢🟢 CLEARED block above).** Then ZEPV skuCost≈0.0989/litre → litres×cost=959.07 CHF ✓ (=97 HL × per-HL 9.8873). The `92c9c13` "phantom GONE" gate-PASS verified only CHF/HL display (9.89) + packaging conservation, NEVER the sales-COGS litre-units path = insufficient COGS gate (the standing failure mode). Packaging-conservation proof (non-cuv 2001.4473→1908.9059, −92.54 cuv liner delta) STILL HOLDS — it just doesn't certify the sales path; the liner amortization NUMBER is correct either basis, only the multiplier flips. **NEXT for the MySQL flip = (after liner-session revert+recompile) re-run `build-sales-cogs --cost-source=diff`, confirm ZEPV ~959 CHF, then flip default sheet→mysql + RULE-2 + final diff** (BLO/BLA not-sold-in-May, off critical path). The F2 finings + warehouse.php $glSt follow-ups are DONE (finings commit `f639632` = discoverability hint + observed-wins auto-retire, racking finings deferred; $glSt CLEARED not-affected). Cosmetic residual: bom-review.php shows MI-102 catalog price on the `liner_amortized` line (display-only). → walkthrough §ADJUDICATION REVERSAL + §COGS COST-SOURCE CUTOVER. EQUIP coder+sql.** *(Below is the now-HISTORICAL handoff-checkpoint narration, superseded by the apply.)* **HISTORICAL: ③ F2 was IN OPERATOR'S HANDS (handoff checkpoint 2026-06-08).** D1 ✅ SHIPPED+VERIFIED (`5186454`/`9fec7d5` — inactive-MI fix + §8.1 mechanism-exclusivity + clear_sku_choice) + D2 ✅ SHIPPED+PUSHED (`9fa60a7` — oldest-invoice/gate-15 into dry-run). **F2 dry-run REGENERATED post-`9fa60a7` (gate-15 included): 142 in scope, 79 gain/49 lose/14 no-change, +800/−588/668 changed lines, 0 errors, 0 unresolved MIs; canonical preview `/var/www/maltytask/data/sku-bom-liquid-preview.json` (REGENERATE at apply — gate-14 staleness).** Engine Opus-re-verified (EMBF DH 566.38 g/HL; DIG/DIP pilsener OI 0.6331; per-HL identical per beer). **Operator review artifact = `/home/kluk/projects/maltytask/reports/f2-liquid-review-digest-2026-06-08.md` (REBUILT off corrected preview; 18 beers; multi-brew malt/hops numbers updated + supersede note, single-brew + dry-hop unchanged).** Operator resumes the walk on corrected numbers → ONE apply dispatch. **✅ MULTI-BREW BASIS BUG FIXED + OPUS-VERIFIED 2026-06-08 (maltyweb commit `9012c8d`, NOT pushed; file `app/sku-bom-compile.php` `compile_sku_bom_liquid`, no mig):** the bug = malt/kettle numerator was ONE per-batch grain bill (= one brew) ÷ `batch_hl=SUM(cool_final_volume_hl)` over ALL N parallel brews → ÷N understatement on every multi-brew beer (DIG/DIP/single-EPH N=1 unaffected). FIX = §2a GROUP BY adds `COUNT(*) AS n_brews`→`$batchNBrews`; §2b malt+hops_kettle accumulators × n_brews per-batch BEFORE MAD/averaging (dry-hop/minerals/gap-fill/oldest-invoice/packaging UNTOUCHED; defaults n_brews=1). **Opus INDEPENDENT verify (preview 2026-06-08 13:03 UTC): apply-gate-16 (a)-(e) ALL PASS** — EMB total malt 21.03 kg/HL (Munich 7.368 + Pilsener 13.662) vs recette 22.83 = ~8% real drift ✓ (EMB b181 hand-check 347×4÷124.5 = 11.1486 ✓); STI 17.52 ✓; **DIG byte-UNCHANGED 10.27 kg/HL = regression-guard PASS**; 0 errors/0 unresolved. CHECK confirmed: saisie enters grain bill per-single-brew + one cooling row per brew; N_brews = cooling-row count reliable (0 zero-cooling batches; dist 18.5% N=1/42% N=3/30% N=4); **the correct N_brews scaling ALREADY EXISTS in `warehouse.php`+`warehouse-export.php` WIP costing — F2 was the LONE omitter** (no shared helper needed; blast-radius §5 resolved). **apply-gate-16 RESOLVED-pending-operator-rewalk** (no longer a build blocker — remaining gate = operator multi-brew re-walk on the rebuilt digest). Scope drift 142→147 / 146→151 = 5 new active SKUs (daily prod drift, NOT a fix artifact — gate-14 regen-at-apply covers it). **🟡 TWO FOLLOW-UPS (logged, NOT part of fix): (1) DGD 18 kg/HL Pilsener is NOT multi-brew (DGD single-brew n_cool=1) → SEPARATE data issue (unit/typo), confirm vs brew record at DGD re-walk; (2) `warehouse.php` `$glSt` FG-valuation query ~L564 may share the one-brew÷batch-HL mismatch for GL cost ratios → SEPARATE review after F2, EQUIP coder+sql.** Detail → walkthrough §MULTI-BREW (FIX LANDED + apply-gate-16). **APPLY PREDICATE NOT YET BUILT** (DELETE source='Brewing' AND bom_source IN(NULL,'liquid') AND mi_id NOT NULL per SKU + reinsert bom_source='liquid' + snapshot + log_revision + idempotent + gates 1-15; EQUIP coder+sql, +ui only if on-page render). **2 MUST-DECIDE-BEFORE-APPLY: (1) 🔴 DIG PROC_YEASTVIT ~22000× recipe-spec scale error — fix `ref_recipe_ingredients` first; (2) 🟠 DGD MALT_PILSENER 18.04 kg/HL implausible — NOW ISOLATED as a single-brew DATA-ENTRY issue (NOT the multi-brew basis bug; DGD n_cool=1) → confirm vs brew record at DGD re-walk.** 9 other anomalies + still-open operator decisions (SPY floor confirm, finings per-beer, gate-15 sign-off, dormant-EPH3 OI, dry-hop data-gap acceptances) → [walkthrough §F2 REVIEW HANDOFF CHECKPOINT](maltyweb-pm-memory/sku-bom-validation-walkthrough.md). **(b) ④ DIRECT-SALES TRACKER — Phase 1 MVP ✅ SHIPPED 2026-06-09 ("Tap&Shop", mig 291, commit `a13a9d0`, Opus-verified LIVE).** New read-only top-bar page `public/modules/tap-shop.php` (page_key='tap-shop', **ref_pages id=186**, domain='logistics', **min_role='operator'** [seeded 'manager', FLIPPED to 'operator' 2026-06-09 — see TOPBAR-VISIBILITY FIX below], sort=105 right after Expéditions; access via ref_access_preset_pages presets 1+3+8). Plane A = Shopify (inv_sales_orders/lines) + taproom (inv_sales_bc channel='taproom') sales per beer/SKU/period with "Non rattaché" bucket for unresolved lines (visible, never dropped), dual-channel bars via KpcCharts, per-beer/SKU table by display_family. Plane B = virtual consumption stock = `physique` from `fg_stock_compute()` — ONE call (L142, the SAME derivation the weekly stocktake uses); page WRITES NOTHING (read-only verified by grep: only comment-mentions of INSERT/UPDATE/DELETE). Forward-compat Phase-2 hooks already in: `vd_load_direct_sales($pdo,$from,$to)` normalizer; SKU set gated on `is_direct_sales`; sku_id carried throughout (no name strings); inert "Consommation directe (à venir)" placeholder panel. Opus verify: read-only ✓ / single fg_stock_compute call ✓ / mig 291 recorded ✓ / ref_pages row correct ✓ / HTTP 200 / ALT4 physique=62 matches fg_stock_compute ✓; pushed. **④ Phase 2 = the cage/COGS engine — DEFERRED, GATED on 2 operator-confirmed external deps: (1) Shopify pickup-vs-shipped signal ON HOLD (team is actively reworking Shopify's shipping/pickup setup — wait so we key on the FINAL mechanism, not a moving target); (2) cage opening anchor — physical count starts MONDAY 2026-06-15, load into `inv_fg_stocktake` then.** Phase-2 scope unchanged: cage-vs-loose routing, `inv_stock_residual` ledger, `inv_consumption` writes (sales_derived + cage draw-down), COGS-into-COP, eshop carton bin-packing. Switch-on once BOTH deps land; Tap&Shop is forward-compatible for it. **Keg-share (draft pours -P25/-P50) handled SEPARATELY (packaging-template batch), NOT in ④ — operator-confirmed.** Operator checkpoints 2026-06-08: cage inventory anchor, keg-sticker count, invoice-feed list. **✅ TOPBAR-VISIBILITY FIX LANDED 2026-06-09 (data-only, no deploy, Opus-verified live): `UPDATE ref_pages SET min_role='operator' WHERE page_key='tap-shop'` (id 186; txn-wrapped + audit_row_revisions before{min_role:manager}→after{min_role:operator}). CONFIRMED LIVE: id 186 min_role='operator', domain='logistics', is_active=1; Olivier=user 19 role='operator' preset 8 with user_page_access granted=1 on 186 (his grant was already correct); `user_can_access('tap-shop',{19,operator,preset 8})`===true via PHP CLI. He sees the link on next refresh (per-request perm cache, no re-login). ROOT CAUSE = the page was SEEDED min_role='manager' (oversight) AND Olivier's per-user grant was set BEFORE min_role was relaxed — `user_can_access` floors on min_role, so the grant alone couldn't surface it. The "step-3-before-step-4" ordering footgun (per-user grants applied while min_role still excludes the role → silently invisible despite granted=1) is NOT addressed in code — left as a separate potential future change (operator-confirmed defer).** **⚠️ 2026-06-08 CHIEN BLEU CONTRACT BUILD touched F2-adjacent state — PM-RECONCILED, NO F2-REVIEW IMPACT:** (i) canonical VPS preview `data/sku-bom-liquid-preview.json` was REGENERATED full-scope at 11:04 (now `skus`=**147** = the digest's 142 in-scope + **5 NEW CONTRACT SKUs** on recipes 20 Bamberg / 21 Jasper — ISOLATED recipes, NONE in the 18-beer digest set; liquid compile deterministic → the 142 existing per-beer numbers are byte-identical superset, NO restore needed, digest at `reports/f2-liquid-review-digest-2026-06-08.md` STILL VALID — it cites scope=142 from the 03:42 UTC run; the apply still REGENERATES per gate-14 anyway). (ii) Contract SKUs added to F2 population: JASPB(312)/BAMB(313)/JASPL(314) bottle/bulk + JASPF(308) keg, all recipe 20/21. **NEXT F2 SESSION: scope is 142 Neb-lineup SKUs; the +5 contract SKUs ride along in the prew but are reviewed on their OWN contract terms, not in the Neb walk.** (iii) **PACKAGING METRIC moved by EXPECTED POPULATION, NOT REGRESSION:** ALL-SKU bom_source='packaging' now **576 rows / 2008.9371 CHF** (was 572 / **2001.4473**); the +4 rows / +7.4898 CHF = EXACTLY JASPB+BAMB bottle(3.3249)+outer_box(0.4200) each — packaging EXCLUDING the 4 contract SKUs is **byte-identical 2001.4473**. **2001.4473 remains the byte-stable Neb-only anchor; 2008.9371 is the new ALL-SKU total incl. contract.** active-only = 1995.29 (population). Next F2/packaging gate-check must use the correct denominator — do NOT alarm on 2008.94.
0c. **✅ RECETTE TEMPORAL VERSIONING (v2 SCD2 write-side) — SHIPPED+VERIFIED+PUSHED 2026-06-09, commit `a1c68dc` + mig 289 (head 289 / NEXT FREE 290). RESUME #0c CLOSED.** The "likely NO migration" guess was WRONG — UNIQUE `uq_recipe_mi_stage` (dates not in key) → 1062 on first edit; resolved Option A (mig 289): STORED gen-col `open_key = CASE WHEN effective_until IS NULL AND is_active=1 THEN 1 ELSE NULL END`, DROP `uq_recipe_mi_stage`, ADD UNIQUE `uq_rri_open(recipe_id,mi_id_fk,stage_key,boil_time_key,open_key)` (≤1 open row/slot, unlimited closed history). Backfill 164 rows effective_from='2025-01-01'/until NULL (0 null_from / 164 open / 164 genesis). Code: `app/recipe-ingredients-loader.php` `rri_close_version(PDO,$id)` helper (read-side L174-175 UNCHANGED); `salle-de-controle.php` 3 hop write-actions → close-then-insert (`set_hop_stage` L1534 / `add_hop_addition` L1659 / `delete_hop_addition` L1882 = CLOSE not tombstone, is_active stays 1), each txn-wrapped; display query L2148 + `AND effective_until IS NULL`. Boundary: from INCLUSIVE / until EXCLUSIVE / CURDATE() server-side. **Opus live-verified: indexes/gen-col/backfill/mig-row/helper ✓; 1062-avoidance + 3-asof boundary PASS; EMB r32 11-ingredient set no-regression ✓; `ref_sku_bom` UNTOUCHED 2427 ✓.** SCOPE: only the 3 hop actions write `ref_recipe_ingredients` today (no generic qty editor) — close helper reusable so future generic editor inherits SCD2 free. STILL OUT (later increments): version-history display UI in SDC; as-of awareness for form-fermenting.php dry-hop EXISTS check; v3 full retrofit. Detail → memory/project_recipe_versioning_temporal.md.
1. **Per-recipe historical re-derivation for batches 5/63/169** — the mig265 packaging parallel-run reattribution; net Neb HL unchanged, only per-recipe splits re-derive. → [packaging-parallel-run-normalizer-defect.md](maltyweb-pm-memory/packaging-parallel-run-normalizer-defect.md).
2. **Tank-sim start_time DD/MM swap in RAW data** still wrong (sim masks it via COALESCE). → [tank-simulator-occupancy.md](maltyweb-pm-memory/tank-simulator-occupancy.md).
3. **🔴 Node `parse-tank-simulation.ts` STILL name-keyed** — COGS/WIP-impacting; port the `(recipe_id_fk,batch)` keying lesson (sql skill #25/#27). → tank-simulator-occupancy.md.
3b. **✅ device-classification interstitial SHIPPED+COMMITTED (`94d5a0d`, ZERO mig; AS-BUILT CORRECTION 2026-06-08: `login.php`-ONLY step — set-password/invite does NOT classify the device, so invite path = set-password → auto-login → tour → mon-tableau with NO device prompt; set-password auto-login does NOT stamp last_login_at, only login.php does; remember-me deferral is login.php-scoped) + ✅ VISITE GUIDÉE tour SHIPPED (`ca65c22`, mig 280) + ✅ tour CHAPTER EXPANSION SHIPPED+LIVE+PUSHED (`91f8c82`, 2026-06-08: 25 steps production / 11 logistics, vetoes grep-verified)** — all LIVE before the 06-08 AM logins. **✅ login.php divergence CLOSED (post-midnight sync 2026-06-07): wiring COMMITTED via `ca65c22` (carries tour-redirect + interstitial wiring) + PUSHED; PM-verified tree clean of it.** Standing convention: new ref_pages entry → add bespoke tour content row, NOW ENFORCED by the **TOUR-STEWARD SYSTEM (SHIPPED 2026-06-09, RULE 3)** — `scripts/tour-gap-check.php` drift detector + `migrate.php` post-apply advisory + `maltyweb-tour-steward` agent (PM dispatches after any ref_pages build; sensitive-class pages return `PM-RATIFY:` for PM ratification). ⚠️ NOT YET COMMITTED: visite-guidee.php (tap-shop+qa cards) + tour-gap-check.php (new) + migrate.php (advisory) deployed-live-but-uncommitted. STILL-OPEN SEPARATE BUG: form-packaging.php ~L2451-2452 helper text shows raw `prod_total_units`/`qte_unites` (no-DB-nomenclature violation on the form itself). → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md) §EVENING CLOSE-OUT + §VISITE GUIDÉE (full §TOUR-STEWARD SYSTEM record there).
4. **Auth + KPI backlog — 🏁 PHASE 2b KPI BUILD COMPLETE 2026-06-10: 55→158 trackers live / 270 active (103 built, 112 honest stubs = Phase 2c data-capture backlog). Full record → kpi-tracker-catalog.md §PHASE 2b BUILD COMPLETE.** Per-domain live/total: rm 25/28 · cogs 18/25 · packaging 17/26 · utilities 16/23 · sales 13/21 · qa_qc 12/24 · wort 12/20 · tanks 11/24 · ops 10/23 · racking 10/11 · fg_stock 9/15 · logistics 4/14 · equipment 1/16. **NEXT KPI ARC = Phase 2c data-capture** (form fields for yeast generation/repitch/harvest + OG design-target + QC-coverage unlock the biggest chunk of the 112 stubs; equipment ~94% / qa micro-sensory-lab / logistics OTIF-freight / cogs forecast-budget-AP/AR / utilities sustainability all need new capture). **KPI GATES still open:** qa_qc threshold validation (operator — reds are threshold/capture ARTIFACTS); WIP #181 + tanks #13-17 + flagship #23/#64 + equipment #236 gated on Node tank-sim port (--write-balances); May COP after May close. (Auth/onboarding residuals: invites Stéphane/Joelson; EUR→CHF 0.945 centralization; salle-fournisseurs ref_pages reg; 2FA-TOTP; Tailscale /admin/* gate; PWA-ify.) → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md) + kpi-tracker-catalog.md.
5. **Mother-shell atom 11** = operator production smoke + close-out (board/retro-link/drill-in/polling/cuve-vide/merge/force-close/Salle de Guerre all wired+live) → THEN pilots 5 (packaging) + 6 (brewing). **NB 2026-06-07 (ui-ux C2): the atom-11 smoke now ALSO serves as VISUAL ACCEPTANCE of the new sb-* gravure vessels + sticky frosted sb-zone__header (`cd96cd4`/`9ed0f6f`) — whoever runs atom 11 must cover the new look in the same pass.** NB 2026-06-07: a repeatable post-deploy smoke suite is now buildable on the `webapp-testing` skill (→ skills-and-build-tooling.md) — candidate companion build when this resumes. → [mother-shell-architecture.md](maltyweb-pm-memory/mother-shell-architecture.md); sequenced board arc (ATOM 16 ∥ TS-FIX → ATOM MAT → pilots) in [session-model-arc.md](maltyweb-pm-memory/session-model-arc.md) + mother-shell-architecture.md.
6. **Validation / strain classification** (SDC Recettes) — operator data-entry, keeps the transfer gate Zepp-only until done. → [recipe-ingredients-drift-surface.md](maltyweb-pm-memory/recipe-ingredients-drift-surface.md) + [yeast-strain-taxonomy.md](maltyweb-pm-memory/yeast-strain-taxonomy.md).
6d. **✅ "New Yeast" BBT/CCT board placeholder — SHIPPED + DEPLOYED + SMOKE-VERIFIED 2026-06-11 (render-only derive-at-read; 3 files UNSTAGED/uncommitted per shared-tree discipline; NO mig, NO raw mutation).** Both boards derive the observed strain via `app/yeast-eligibility.php` (new helpers L264-325): `yeast_observed_strain_expr($alias)`→SQL `IF(<alias>.yeast='New Yeast',<alias>.new_yeast,<alias>.yeast)` (alias whitelisted `[A-Za-z0-9_]`, injection-safe); `resolve_observed_yeast_strain(PDO,$yeast,$newYeast)`→coalesce then precedence (a) exact CI `ref_yeast_strains.name` (b) CI `ref_yeast_strain_aliases.alias` JOIN strain (c) raw + `resolved:false`; NEVER invents. tanks.php CCT query L335-356 uses the expr, resolves post-fetch → `detail.yeast.strain` (cct-detail-modal.js UNTOUCHED, inherits). packaging.php BBT `$bbtYeastStmt` resolves → escaped chip "Levure · {strain} (G{gen})". Live probes: Speakeasy b64→"Pomona", Diversion b46→"Pinnacle Low Alcohol"; render smoke both boards 0 console errors / 0 "New Yeast" leaks (BBT 5 Pomona G7, BBT 2 Pinnacle Low Alcohol G1). **DIVERGENCE FROM PRE-BUILD RULING (working-as-designed, NOT a bug): `apinnacle` does NOT render raw/ambiguous — alias id=34 (`apinnacle`→strain 15 Pinnacle Low Alcohol, operator-curated 2026-05-11 per mig 228) resolves at step 2b; that's a curated decision, not a guess. Lever to show it raw = delete alias id=34 (operator call). NO live in-tank new_yeast renders unresolved today.** **STILL-OPEN FOLLOW-UPS (gated): (1) retire bogus `ref_yeast_strains` id=69 'New Yeast' (no family) — separate migration, gated on apply-time re-verify of 0 live FK refs (v1 FK constraint still live mid-decommission); NOT started. (2) alias seeds for any historical messy new_yeast lacking canonical/alias match — operator-gated, explicit mappings only, NEVER guess; none in-tank, matters only for historical board views.** GIT: 3 files (app/yeast-eligibility.php, public/modules/tanks.php, public/modules/packaging.php) modified+UNSTAGED; the earlier BBT yeast-chip build (packaging.php + app.css `.tank-card__yeast`) is part of this same uncommitted set; tree also carries unrelated parallel-session work (financier.*, saisies.php, mig 330) — deploy-pushes-working-tree, `git status` before any `--apply`. → [yeast-strain-taxonomy.md](maltyweb-pm-memory/yeast-strain-taxonomy.md) §OBSERVED-STRAIN DERIVE-AT-READ.
6b. **✅ BURST-BAG / PACKAGING-LIQUID-LOSS MIS-BOOKING — SHIPPED+COMMITTED 2026-06-09 (maltyweb `3d96847`; mig 292; COGS/WIP/beer-tax-impacting; OPEN months touched).** Compute-fn FLOOR (vendable bccomp-floored at 0 in `compute_packaging_vendable_hl()` keg/cuv branch ~L298, NO-OP on 1015 real runs) + view mirror **mig 292 `292_floor_keg_cuv_vendable_view.sql`** (keg/cuv arm `GREATEST(0,…)`, tax arm derives from it; applied via direct CREATE OR REPLACE VIEW NOT apply-all, to dodge the parallel session's in-flight `291_tap_shop_page.sql`; schema_migrations row inserted manually — **MIG HEAD = 292 / NEXT FREE = 293**) + cuv/keg loss routes to typed `loss_keg_liquid_l` (form field relabelled "Perte liquide / poche éclatée", packaging-form.js dynamic on run_type) + tank-sim drain predicate broadened to `vendable_hl>0 OR loss_kpi_hl>0`, drain=vendable+loss_kpi, PHP guard skips sum≤0 (protects 1303); lost beer now DEPLETES the tank (Kouros approved). **Backfill = 6 pure-loss rows fixed** (4 ZEPV cuv 1297/1320/2160/2217 + 2 keg 1335 EMBF/1998 SPYF — the 2 keg sibling rows folded in); all vendable=0/tax=0/loss_kpi=5/3.5/10/5/0.2/0.02, stored==view confirmed; **+23.72 HL restored to packaged-HL AND beer-tax base** (2024-06 +8.5 / 2024-07 +0.2 / 2025-12 +0.02 / 2026-04 +10.0 / 2026-05 +5.0 — last two OPEN, COGS/tax live). **5 void all-zero cuv rows (1385/1386/1406/1407/1408) HARD-DELETED + tombstoned.** **✅ RESOLVED 2026-06-10 (was a 🔴 CRITICAL FINDING — "held -V cutover #0/#0a5 is URGENT"):** the read-path view `v_bd_packaging_v2_vendable` computes vendable=(prod/units_per_pack)×s.hl_per_unit, so while EMBV/MOOV/ZEPV carried hl_per_unit=1.0 the view recomputed all 245 cuv PRODUCTION rows at 100× (stored cols correct; only the VIEW was hot). The -V revert to 0.01 SHIPPED 2026-06-10 → SELF-RESOLVED. **Opus live-verified: view id 1298 ZEPV vendable_hl=5.0 == stored 5.0** (was 500.0). Cuv view volumes now match stored; burst rows were always immune (prod=0). **✅ Row 1303 (STI4C can) RESOLVED by the loss-model-consistency fix #6c (`7a8b63a`): vendable −23.4→+19.8, loss_kpi 0.69→43.9 — no longer open here.** → [packaging-losses-convention/burst-bag-liquid-loss-arc.md](maltyweb-pm-memory/packaging-losses-convention/burst-bag-liquid-loss-arc.md) §SHIPPED RECORD.
6c. **✅ LOSS-MODEL CONSISTENCY — SHIPPED + COMMITTED 2026-06-09 (maltyweb `7a8b63a`; mig 293 `293_loss_model_no_liquid_subtraction.sql`; COGS/WIP/beer-tax-impacting across history; Opus independently verified).** Canonical model LIVE: liquid losses (`loss_liquid_other_units`, `loss_keg_liquid_l`) NO LONGER subtract from vendable in ANY branch → they sum into `loss_kpi_hl`. bottle/can vendable = good filled units only; keg/cuv vendable = hlGross − taproom (**taproom KEPT — it's a subset of prod, taxed via hlGross; I REFINED the original "drop all three" ruling which would have double-counted taproom**). mig-292 GREATEST/bccomp floor SUPERSEDED — its view logic deleted in the 293 rewrite (292 file lingers on disk, dead/already-applied; do not re-run). Files: `form-packaging.php` (compute fn) + `app/packaging-stats.php` (loss-% double-add removed) + mig 293 (view rewrite). **Backfill: 733 already-computed rows → +540.8 HL restated to vendable AND beer_tax_base, +534 HL newly visible in loss_kpi (2023 +94.4 / 2024 +205.4 / 2025 +156.0 / 2026 +85.1); row 1303 STI4C-can catastrophic run vendable −23.4→+19.8, loss_kpi 0.69→43.9; 0 negative-vendable after; stored==view verified bot/can/keg.** Row 1303 RESOLVED by the model (REMOVE from burst-bag #6b STILL-OPEN). **🔴 CRITICAL SAVE — the build agent's backfill OVER-REACHED:** its recompute hit the compute fn's generic `$isContract` fallback and newly populated 57 previously-NULL CONTRACT rows (BLZ, Abbaye de St-Maurice, MeltingPote, Septentrion, Obrist…), adding +858 HL of contract beer to the tax base (true total +1399, not the dry-run's +554). Opus caught it via independent snapshot verification and **REVERTED those 57 rows to NULL.** → do-not-repeat now in conventions-and-helpers.md §CODING (backfill must not auto-populate contract rows; the magnitude/row-set gate). **FISCAL DE-RISK:** `bd_packaging_v2.beer_tax_base_hl` is read ONLY by packaging dashboards (packaging-stats.php, packaging-loss-types.php); the FILED beer tax is computed by `lib/beer-tax.js` from brewing-side OG×volume, NOT this column → the +540.8 restatement is an internal-metric correction, almost certainly NOT a filed-declaration change (confirm declaration basis w/ accountant; fiscal alarm largely moot). **cuv:** stored correct (verified @0.01); ✅ the -V `hl_per_unit` revert LANDED 2026-06-10 (#0/#0a5) → view reconciled (Opus-verified id 1298 view==stored 5.0), no longer 100×. RULE-2 MEDIUM finding (view bot/can beer_tax omits unsaleable) = FALSE POSITIVE (achieves +unsaleable by not-subtracting; 0 stored-vs-view divergence). Snapshot `bd_packaging_v2_lossfix_snapshot_20260609143950` retained on VPS for rollback. **✅ FOLLOW-UPS #1 + #2 RESOLVED 2026-06-09 (DB-ONLY, NO git, Opus live-verified) — operator directive "mint Néb SKUs at standard formats, Néb pays contract Biersteuer, compute all" then left for the day. (1) 20 Contract recipes → 28 NEW ref_skus at standard fmts (bot fmt1 0.0792/24, keg fmt15 0.20/1, can33 0.0033/1; reused Chien Bleu BAMB/BAMF/JASPB/JASPF), relinked+computed 162 bd_packaging_v2 rows (row_hash=SHA256(recipe_id|format_id|sku_code)). (2) CollabIn DGDB(r31 sku_id=8)+DOCB(r29 sku_id=21) via EXISTING real SKUs (0.0792) — DGDB vendable 10.108 HL (closes April ~10 HL gap), DOCB 3.541; CollabIn taxes via SALES not contract-packaging → NOT in the contract base. 🔴 **MAGNITUDE FLAG (accountant review): Contract bd_packaging_v2 = 162 rows / Σvendable 2958.265 HL / Σloss_kpi +28.247 (live-verified; ALL 162 feed `loadContractPackagedHL` = contract Biersteuer base). ~3.4× the +858 HL over-reach discussed — agent computed ALL 162 NULL contract rows at REAL standard formats (not just the 57 loss-bearing); rule-correct + operator-approved-in-principle, SIZE needs accountant review before any contract Biersteuer declaration consumes it. Nothing auto-files overnight (contract tax on-demand via reports).** NOT a guess — standard formats + explicit operator directive = canonical seeding (supersedes the run_type-fallback guess that left them NULL). Snapshots (live-verified): **`bd_packaging_v2_contractfix_snapshot_20260609_153201`** (162 rows; NOTE underscore before time) + `bd_packaging_v2_lossfix_snapshot_20260609143950`. DEBT (non-blocking): 6 minted codes flagged rename (PALEC/PALEF id326/327 r37, SESSC/SESSF 328/329 r38 = Le Traquenard generic; LAPTB/LAPTF 330/331 r40 truncated); ALL 42 contract recipes have `ref_recipes.sku_prefix=NULL` → SDC activate_format blocked until set. **STILL OPEN: (3) 2 May rows 2198/2200 (alex, May 4, bot, prod 1760/2362) — genuinely unidentifiable, need alex to name the beers (missing from May close); plus the -V hl_per_unit revert (#0/#0a5).** Contract fix = DB-only, no commit. Session commits: `f019526` magnitude-guard / `3d96847` burst-bag / `7a8b63a` loss-model. EQUIP coder+sql+ui. → [loss-model-consistency-arc.md §CONTRACT-PACKAGING GAP CLOSED](maltyweb-pm-memory/packaging-losses-convention/loss-model-consistency-arc.md).
7. **Paused arcs:** A-LT2 data-driven-loss-types view-codegen (G1 failed on `loss_liquid_other_units`, operator settled option B); packaging-page dashboard build-out (operator-confirmed, consulted). → [packaging-losses-convention/loss-types-and-form-changes-arc.md](maltyweb-pm-memory/packaging-losses-convention/loss-types-and-form-changes-arc.md) + [packaging-page-dashboard.md](maltyweb-pm-memory/packaging-page-dashboard.md).
8. ⚠️ Untracked working-tree file `app/packaging-loss-types.php` = paused A-LT view-codegen scaffolding, NOT a divergence — leave it or fold when A-LT resumes.
9. **🏁 UI/UX QUALITY REWORK ARC — FULLY CLOSED 2026-06-07 (Phases 0-3 + C2 + calibration, all PM-verified pushed).** Phase 3 landed `e05b18c`/`e679efa`/`aba178a` (3 parallel bundles, app.css=C-only rule worked, then ONE combined-diff PRE-COMMIT RULE-2 review — 2 P1s caught+fixed before commit [PF-04 preventDefault gate removed; PF-03 invalid listbox-ARIA → role=group + sr-only status]; proven shape for multi-bundle bursts). Gravure = house sketch language; `app/svg-tanks.php` = the ONE canonical vessel library. **OPEN = only §RESIDUAL BACKLOG (4): bom-review polish (the post-F2 carve-out), PF-04 focus-first-invalid-on-PRG, mon-tableau structural (coordinate w/ KPI-tranche session — kpi-handlers.php/mon-tableau.js), deuteranopia chart-pair pass.** Atom-11 smoke still = visual acceptance of sb-* gravure. → [ui-ux-quality-rework-arc.md](maltyweb-pm-memory/ui-ux-quality-rework-arc.md) §Phase 3 + §RESIDUAL BACKLOG.
10. **✅ MIG-278 COLLISION REMEDIATED (PM-verified live 2026-06-07 ~23:30: head = `280_users_tour_seen_at.sql`, applied_at preserved, no stale row; NEXT FREE = 281).** Remediation as ruled: rename→280 + paired `schema_migrations` UPDATE same step + **manual rm of the stale 278 file on the VPS (rsync deploy does NOT delete removed files — a renamed migration's old filename lingers and shows "pending")**. Concurrent-arcs sweep: (a) **✅ PUSH DONE (post-midnight sync 2026-06-07)** — main == origin @ `6f0e773`, ALL evening commits pushed (`ca65c22` tour incl. login.php wiring → RESUME #3b closed; `f7fe151`, `b838cca`, `94d5a0d`, `1d8672b`, `803d3f8`, `91ce8c0`; plus newly-observed `5186454`/`9fec7d5` = the two Dispatch-D1 commits — on main+pushed, data-outcome debrief PENDING: live `ref_sku_bom` packaging census NOW 655 rows/2013.36 CHF ≠ recorded 580/2001.45, and 8 active `-X` cage SKUs already exist — attribution pending parallel-session debrief, do NOT treat 2001.45 as current; `b676c9b`/`dcbce1d` fg-stock, `1b9694a` mig-280 header); mig 281 = CRM contact cols, APPLIED+committed (see §CURRENT BUILD-STATE) — **NEXT FREE = 282**; (b) **✅ EXPÉDITIONS arc — ALL PRE-FLIP BUILDS DONE (2026-06-08 overnight, main==origin @ `6dd5521` PM-verified)** — CRM bootstrap `73879bc` + Carnet clients `ff70261` + merge-decision pipeline `9803849`/`6dd5521` (368→4 buckets xlsx → convert → audited apply CLI; blank decision = NON never merge; «Arches»→«Marchés» 0.92 = the no-fuzzy-links proof case) + saisie live-stock hints `134bbdf` (advisory amber, never blocks — overselling legal) + tour content `a59d94d` (R7 CLOSED) + logistics preset already pre-wired by auth arc (id-based, rename carried — nothing to build); auth smoke DEFERRED (temp-user creation correctly denied by classifier; NB smoketest acct id=16 exists for next time). REMAINING = operator morning runbook ONLY: merge decisions (368) → admin click-through → flip `is_active=1` → 06-08 re-anchor (then R2/R3 `fg_stock_compute()` re-exam) → 3 off-trade ambiguous + R6 link rule → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §2026-06-08 OVERNIGHT; (c) KPI v2-tranche fold into kpi-tracker-catalog.md (partially done via Mon-tableau-v2 record).

11. **✅ Upload-pipeline operator retry SHIPPED 2026-06-07** (maltyweb `285d5bd` deployed + maltytask `121c629` → /opt/maltytask-pipeline; built-to-spec: shared `app/upload-ingest.php` exec helper, flat `api/upload-retry.php` w/ race-guard 409, BOTH writeback guards widened, JS retry resumes polling; ⚠️ not live-fire-tested — first real failed upload exercises it). Deploy fact: `/opt/maltytask-pipeline` syncs via `bin/deploy.sh --apply-pipeline`. → conventions-and-helpers.md §UPLOAD-PIPELINE RETRY (as-built).

12. **🏁 SHIPPED + LIVE + E2E-VERIFIED 2026-06-08 — Ingestion validation gate + SIE/SIL energy web-path (Phases A+B1a+B1b+B2; migs 282/283/284, head 284 / NEXT FREE 285).** NO MORE BSF/Drive (Drive sourcing RETIRED). **NOTHING auto-writes** — worker STAGES the gate's write-plan to `doc_invoices.delivery_write_plan` + energy to `energy_extract`, writes ZERO inv_deliveries; operator validates → `--commit` replays the plan via the IDENTICAL `appendDeliveryToMysqlWithConn` loop (one writer). Commits: A=maltytask `c80f6f8`/maltyweb `fd1c5ed`; B1a=maltytask `8c991a6`/maltyweb `d894b6f`; B1b=maltyweb `6973316`; B2=maltyweb `e0e580d`. SUDOERS gap closed. **SIE period convention: dated 21 May 2026 → keyed 2026-04 (consumption-period); 66 kW / 829.5 kVArh.** List filter `delivery_write_plan IS NOT NULL` = forward-only.
12b. **🏁 À-VALIDER → TRIAGE MERGE SHIPPED + COMMITTED + LIVE-PROVEN 2026-06-08 (maltyweb main `484cc6a` "feat(triage): À-valider queue as triage tab + fix delivery double-write", 8 files: shared partial valider-queue.php, triage.php tab, 3 triage-alias validated_at stamps, invoice-validate.php thin wrapper, overlay-bleach CSS fix, poll JS fix).** Built to the merge verdict: triage `?tab=valider` tab + count badge `$countValider` + body-class toggle; NEW shared partial `public/modules/partials/valider-queue.php` = THE single list renderer; `invoice-validate.php` now a thin wrapper (orphan-page RULE-1 scrap satisfied). **Lenzlinger double-write landmine CLEARED:** Fix A (list query `AND NOT EXISTS … inv_deliveries d WHERE d.file_id_fk=df.id`) + Fix C (triage-alias resolution stamps `validated_at=NOW(),validated_by=<operator id>` at the `$openLeft===0` chokepoint in alias.php/create.php/manual-lines.php — reject.php deliberately NOT stamped) + inv 208 backfilled out. **✅ WATCH-ITEM CLEARED — live Valider PROVED OUT END-TO-END:** operator validated the clean Tinguely invoice (`doc_invoices.id=209`, FAC `01-26050180`) in production → commit worker ran the full chain (recap-lines.php → `/api/invoice-validate.php` → ingest-one-local-commit.sh → `delivery_write_plan` replay) → **7 Active inv_deliveries written (source Invoice-OCR), `validated_at` + `validated_by=1` stamped, HT 641.61 reconciles.** The single commit path (POST `/api/invoice-validate.php`) is now field-proven. **BUG SURFACED + FIXED during the proof (in `484cc6a`):** tsx cold-start can exceed the UI's 2-min poll cap → a *successful* commit showed a false red "✗ Échec"; FIXED in `public/js/invoice-validate.js` = poll cap 2→4 min + a final status re-check on timeout + a neutral "Toujours en cours / Recharger" state instead of red failure. **This is the single-invoice CANARY for the bulk-validate fan-out risk — "Tout valider" fires N concurrent cold-start workers unthrottled; the throttle remains a BACKLOG ITEM.** Also fixed same commit: `.iv-card-overlay` CSS bug (unconditional `display:flex` overrode `[hidden]`, permanently bleaching every card on both the tab and the standalone page) → gated on `:not([hidden])`. **2 STANDING BACKLOG ITEMS:** (1) commit-side precheck "b" DEFERRED (maltytask `commitPlan`/ingest-one-local.ts — defense-in-depth; A+C already close the reachable path); (2) 🔴 TWO RECAP-SHAPE SOURCES — valider-queue.php kept its own BATCH query (vat_rate/gate_failures/aggregate cols) instead of `fetch_recap_for_invoice()` (recap-lines.php = the DESIGNATED canonical); ONE UI renderer preserved but NOT the canonical one → consolidate before a 3rd recap consumer; (3) bulk "Tout valider" fan-out throttle (above). → [ingestion-validation-gate-arc.md](maltyweb-pm-memory/ingestion-validation-gate-arc.md) §À-VALIDER → TRIAGE MERGE — SHIPPED+PROVEN.

### PENDING BUILDs (PM-blessed, NOT YET BUILT — the fermenting-correctness chain's remaining links)
All three fold into the SAME fermenting-correctness build (same files: `fermenting-phase-submit.php` / `session-body-fermenting.php` / `fermenting-phase-*.php`; all share the recipe_id_fk re-key lesson). Full diagnoses + rulings → [saisie-forms-5-correction-arc.md](maltyweb-pm-memory/saisie-forms-5-correction-arc.md). NB: most of this chain WAS built+committed under HEAD `815e725` (see line-7 header close-out) — re-verify against live before dispatching; what remains is the cadence-gate + SDC cadence panel below.
- **PURGE-CADENCE GATE breaks edit + backfill** (`fermenting-phase-submit.php` gate) — 3 faults: runs in edit mode; compares `submitted_at` not `event_date` (backfill-hostile); keys `beer_raw` not `recipe_id_fk`. Fix A1-A4 read-side, NO mig (sql skill #28/#29). + Atom 2 = "Cadences de production" SDC panel (clone `update_cip_cadence`, 2 scalars: `fermenting_cadence.purge_after_days` + `packaging.min_days_after_racking`), NO mig. Detail in saisie-forms-5-correction-arc.md.
- **SESSION-PHASE-CACHE LAG** on NULL-linked backfill batches — `op_sessions.phase` cache lags event-derived phase → submit guard rejects. Fix = render-time direct UPDATE phase-sync (NOT `session_advance_phase` — strict single-step throws), guard `status='open'`, side-effect-free (racking eligibility keys on ColdCrash event not session.phase).
- **BEER-IDENTITY FRACTURE** read-side re-key (phase-detect / in-progress `<select>` / CCT firewall) on `(recipe_id_fk,batch)` not `beer_raw` (fragments across event types) + phantom-0-event-session suppression + Cold-Crash → checkbox (not dropdown). 216 (recipe,batch) pairs affected system-wide. Write NK untouched. sql skill #27.
- **🆕 FININGS INPUT FIELD on brewing and/or racking saisie forms** (operator decision at F2 apply, 2026-06-09) — finings (e.g. PROC_NAGARDO) are inconsistently recorded; operator wants them inputted on saisie so they become OBSERVED data (currently gap-filled from recette: DIV 60 g/brassin = 55.72 CHF/brassin). Until built, the recette gap-fill stands. EQUIP coder+sql+ui. → walkthrough §F2 LIQUID APPLIED (operator decision 2).

(Older detailed RESUME-POINT narration — the form-finalisation F-0/F-1 arc, the mother-shell atom-11 walkthrough checklist, the BSF-exit Phase-D follow-up backlog, the OPEN-ITEMS register of data-quality/RQ/scrap candidates, the WORT-CONTRACT backlog ruling — all COMPILED OUT to [build-history/former-index-resume-point.md](maltyweb-pm-memory/build-history/former-index-resume-point.md) + the cited thematic files. The Phase-D follow-up backlog headline = remaining BSF-exit legs (FG/WIP/Sales/ChargesBC/Consumption_Parsed/BeerTypes/Deliveries-tab still read BSF) + RULE-1 scraps + no_basis cans → [phase-d-cost-source-swap.md](maltyweb-pm-memory/phase-d-cost-source-swap.md) §FOLLOW-UP BACKLOG. **🆕 2026-06-08: build-sales-cogs SKU-cost-map cutover (Sheet→ref_sku_bom, BSF-exit leg #8, operator-approved opt A) SPEC'd — PM VERDICT = GATED on F2 apply (today's liquid is mixed-currency EUR/CHF + un-blessed F2 revaluation; Sheet stale, run-month-close step-3 broken). Plan: write lib/repos/mysql-sku-bom.ts + `--cost-source=diff` NOW [no COGS change], cut over post-F2. → phase-d §build-sales-cogs SKU-cost-map cutover.**)

## 📍 CURRENT BUILD-STATE (one-glance)
- **Canonical store:** MySQL `maltytask` (since 2026-05-12). VPS `/var/www/maltytask` (`ssh maltyweb`); local `/home/kluk/projects/maltyweb`. PDO bootstrap = `/var/www/maltytask/app/db.php` → `maltytask_pdo()`.
- **Migrations:** applied through **298** (live-verified VPS 2026-06-09). **298 = `298_journal_saisies.sql` — Journal-des-saisies arc: VIEW `v_saisie_events` (UNION over the 7 bd_*_v2 tables, 21,887 rows) + submitted_at indexes + ref_pages 'journal-saisies' sort=15; commit `5b918f3` (deployed-not-pushed). 297 + 296 = parallel BBT-vide arc (`296_bd_racking_v2_bbt_vide_scrapped_hl.sql` + `297_pertes_bbt_empty_threshold.sql`) — APPLIED 2026-06-09 as a SIDE-EFFECT of the journal session's deploy.sh/migrate.php run (deploy pushes the working tree; both additive; Kouros's uncommitted BBT-vide PHP/CSS/JS also leaked live in the same deploy). 295 = `295_submitted_by_user_fk.sql` — operator-attribution arc: nullable `submitted_by_user_id_fk INT UNSIGNED → users(id)` on ALL 7 bd_*_v2 tables (NOT 4 — brewing is 4 sub-tables: brewday/gravity/ingredients/timings); commit `c8e895c` (deployed-not-pushed). 294 = `294_ord_orders_source_ref.sql` (parallel: WeeklyOrders ingestion). 293 = `293_loss_model_no_liquid_subtraction.sql` — loss-model-consistency view rewrite, commit `7a8b63a`. 292 = floor view (SUPERSEDED by 293; file dead on disk). 291 = `④ Tap&Shop` direct-sales tracker MVP — ref_pages 'tap-shop' + access-preset rows; commit `a13a9d0`. 290 = `290_marketing_access_preset.sql` (parallel; expeditions is_active 0→1). 289 = `289_rri_scd2_open_uniqueness.sql` — recette SCD2 write-side; commit `a1c68dc`. **299 = `299_fulfilment_site_attribution.sql` ✅ APPLIED 2026-06-09 (ITEM 1: CREATE `inv_stock_movements` + ALTER `ord_orders.fulfilment_site_id_fk` + ALTER `ref_customers.default_delivery_site_id_fk`; commits `3c3eb33`/`49a0386`/`edfcf78` pushed). **300-305 APPLIED+verified live 2026-06-10:** 300=`ref_customers_sale_class` (single-source COGS taxonomy); 301=`inv_sales_ledger` (44,164-row canonical sales ledger 2021→2026); 302=`cuv_hl_per_unit_lock`; 303=`inv_side_stock_ledger` (loose-unit remainder + giveaway); 304=`financier_page` (ref_pages id=239 financier, manager/general/sort=125); 305=`kpi_fg_stock_data_ready` (FG-stock trackers data_ready=1 — swept into Financier commit `6a013ad` by the shared-index concurrency incident, see SHIPPED register). **306-310 APPLIED (parallel sessions, 2026-06-10):** 310=`310_kpi_daily_recap_trackers.sql` — NEW `viz_type='recap'` ENUM value (10th) + the 2 daily-recap composite trackers #270 wort / #271 packaging (NOT yet committed; see kpi-tracker-catalog.md §2026-06-10 RECAP). 306-309 = parallel-session arcs (re-verify owners at build-start). **312 = À-valider topbar dedup (`is_active=0` ref_pages id=99 invoice-validate; commit `1a81a3f`, pushed; 2026-06-10).** 311 = `kpi_utilities_batch6` (parallel mon-tableau session). **313 = `313_packaging_objective_hl.sql` ✅ APPLIED 2026-06-10 (mine — daily-recap Feature C): `bd_packaging_v2.objective_hl DECIMAL(8,3) NULL`, a planning annotation (NOT fiscal); one column per row (parallel runs = separate rows). Leakage audit PASSED — objective_hl appears ONLY in kpi-handlers.php (read), form-packaging.php (write), packaging-form.js, the mig; ZERO in compute_packaging_vendable_hl / v_bd_packaging_v2_vendable / beer_tax_base_hl / loss_kpi_hl. **⚠️ The `objective_hl` WIRING in form-packaging.php + packaging-form.js was ALREADY COMMITTED in the Réassigner-cuve commit `4af2155` (swept in incidentally — noted in that commit body); do NOT re-commit those two files' objective_hl hunks. mig 313 file itself + any mon-tableau-side objective_hl read may still be uncommitted — verify before committing.** **🏁 PHASE 2b KPI BUILD COMPLETE 2026-06-10 added migs 305,306,307,308,309,311,312,314,315,316 (mine) + 310/313/317 (parallel) → MIGRATION HEAD now ≥318. NEXT FREE ≥319** (re-verify `--status` + `ls db/migrations` at build-start — parallel sessions live; the head moved a lot today across 10 atomic batches). **318 = `ref_pages.sku-costs is_active=0` — sku-costs.php retirement (the standalone "Coût par SKU" page absorbed into the Financier tab); maltyweb `e72e64e`, pushed; 2026-06-10. NB the retiring session originally planned 313 but parallel sessions had already taken 313–317.** **317=`317_vendable_view_head_leaf_split.sql` ✅ APPLIED 2026-06-10 — Réassigner-cuve serving-tank reassignment (this was a DISTINCT arc, NOT the KPI parallel session; 316 was the parallel `316_kpi_logistics_equipment_batch10`, which is why this landed as 317): view vendable head→leaf split (vendable MOVES to leaf via `has_child` EXISTS; tax/loss STAY head via `reuses IS NULL`); NO-OP on current data (2270 rows / 0 diff, zero reuse rows pre-feature). **✅ COMMITTED+PUSHED maltyweb `4af2155`** (mig 317 + `form-packaging.php` / `packaging-form.js` / `packaging-form.css` / `app/sku-bom-compile.php` liner-amortization predicate fix). **REAL operator reassignment verified: Blues Rules 6740→Rincette → leaf 6767 (vendable 5/tax 0/reuses 6740/liner reallocated 105/new_liner_transport 0), head 6740 vendable→0/tax 5 stays, batch-211 cuv totals conserved 33.5+33.5 HL, view==stored.** `4af2155` ALSO swept in the uncommitted mig-313 `objective_hl` wiring in form-packaging.php/packaging-form.js (FLAG: daily-recap Feature-C session must NOT double-commit it). 1 Blues tank (6739) still un-reassigned. Full record + follow-ups (multi-tank 2-leg cap, undo stored/view divergence, liner inv_consumption deferral) → [loss-types-and-form-changes-arc.md §RÉASSIGNER CUVE](maltyweb-pm-memory/packaging-losses-convention/loss-types-and-form-changes-arc.md).** Phase-2b commits `6a013ad`/`09006ce`/`d264111`/`4f6a98c`/`598f788`/`a353ea2`/`e32d472`/`ea10fb7`/`7cbedf7`/`b3a1e02` = maltyweb main LOCAL (verify push). Full record → kpi-tracker-catalog.md §PHASE 2b BUILD COMPLETE. **⚠️ ACTIVE PARALLEL-EDIT HAZARD on `app/kpi-handlers.php` (2026-06-10): Phase-2b built ALL domains into this ONE file across 10 batches AND a concurrent recap session shares it → the qa_qc batch (`7cbedf7`) swept the recap session's PHP handlers (mig-310 recap + A/B/C tweaks) into git history. Recap HANDLER code is COMMITTED (matches deploy); recap frontend/form files (kpi-charts.js, mon-tableau.css, packaging-form.js, form-packaging.php) + migs 310/313 stay UNCOMMITTED-but-deployed/applied. `app/kpi-handlers.php` is now the single highest-traffic shared file — coordinate + single-owner before touching it. Prior: **284-282 = ingestion-validation-gate arc (284 = ref_pages id=99 invoice-validate + tour row [`6973316`]; 283 = doc_invoices.delivery_write_plan JSON [`d894b6f`]; 282 = doc_invoices.validated_by FK→users.id + energy_extract JSON [`fd1c5ed`]**; NB the pre-build "mig 282 carries 3 cols" plan was SPLIT 282/283 as built). 281 = ref_customers contact cols. 285-288 = expeditions (display_family 287 / inv_fg_stocktake row_hash gen-col 288). Prior (PM-verified vs live 2026-06-07 LATE NIGHT): **280 = users.tour_seen_at [visite guidée, `ca65c22`]** — the 278-collision REMEDIATED: renamed→280 + paired schema_migrations UPDATE + manual VPS rm of the stale 278 file; 279 = expeditions/orders [parallel session]; 278_kpi_seed_v2_batch = Mon-tableau-v2 seed; 277 = P3 recap; 276 = whirlpool; 275 = liquid window guard; 274 = FIX-7; 273 = slot_name; 272 = BOM step-② data; 271 ALT-b25; 266-270 access+KPI. 281 = ref_customers contact cols + name-UNIQUE→KEY + Transport Express seed (CRM bootstrap arc). (Stale interim markers in this para superseded by **NEXT FREE = 306** stated above — re-verify `--status` + `ls db/migrations` at build-start, parallel sessions live.) **LESSON (2026-06-07): rsync deploy does NOT delete removed files — a renamed migration leaves its OLD filename live on the VPS where it shows as "pending"; rm it manually as part of any rename remediation.** **INCIDENT-CONVENTION (2026-06-07): a migration SQL file must NEVER self-INSERT into `schema_migrations` — migrate.php does the bookkeeping; a self-INSERT collided with migrate.php's own INSERT → FAIL reported after a fully-applied migration.** **MON TABLEAU v2 ✅ LANDED — record → kpi-tracker-catalog.md §2026-06-07 EVENING.** `app/sku-bom-compile.php` staged WAC edits are NOW COMMITTED (① done) — file is clean to edit again. Full per-migration ledger (131→265, what each did, the renumber-drift history) → [derivation-tree-and-schema.md](maltyweb-pm-memory/derivation-tree-and-schema.md) §SQL BUILD SCHEMA + [build-history/](maltyweb-pm-memory/build-history/README.md). Migration collisions are caught + corrected at build-start every time via the `--status` pre-flight (PPB 194→200; mother-shell 204→215; the rule holds).
- **`ref_sku_bom`:** ✅ **F2 LIQUID APPLIED 2026-06-09** — post-apply census: Brewing/`bom_source='liquid'` **1557 (all currency='CHF', the new F2 lines)** + Packaging/`packaging` 576 + Brewing/`composite_liquid` 204 + Packaging/`composite_packaging` 83 + Brewing/NULL 7 (out-of-scope SKUs the liquid compiler doesn't cover, cosmetic). NULL mi_id still 0 DB-wide; `source='mi_match'` = 0 DB-wide. **Liquid rows now CHF-canonical** (cost=CHF, FIXES the old EUR-summed-as-CHF latent bug); `slot_name` carries the STAGE for liquid ('brewhouse'/'dry_hop'). Apply via `compile_sku_bom_liquid()` apply branch (packaging/composite parity gate 1e-6-or-rollback, idempotent). packaging recompute (`compile_sku_bom_packaging`) + composite fold (`compile_sku_bom_composite`) both LIVE; 30 bindings; 6 `sku-bom-unresolved` + 5 `-4` carton-MI RQ rows. (Prior pre-F2 packaging census 576/2001.4473 = Neb-only byte-stable anchor; 576 packaging rows untouched by the F2 apply.) Detail → walkthrough §F2 LIQUID APPLIED + packaging-bom-model.md / sku-decomposition-tree.md.
- **`ref_sku_composite_slots`:** 19 rows / 4 SKUs (PD8/XMAS/PAL/PAC, all member_format_id=5 BU 33cl); compositeness = slot membership (NO `is_composite` col). PACKDEC/PAD → PD8 alias.
- **Commit chain on `main`** (full per-commit narration): → [build-history/](maltyweb-pm-memory/build-history/README.md). All pushed to origin/main through the heads in the header line.
- **volume_hl coverage:** 105 distinct SKUs (DONE) — bot/can/can33 container slots; keg/cuv/composite SKU-level via `v_sku_volume`/member-sum. → volume-dimension.md.
- **Stage-2 / ④ direct-sales tracker** — **Phase 1 MVP ("Tap&Shop") ✅ SHIPPED 2026-06-09 (mig 291, `a1c68dc`→`a13a9d0`); Phase 2 cage/COGS engine DEFERRED, gated on Shopify pickup-signal + cage anchor 2026-06-15.** → RESUME #0b + derivation-tree-and-schema.md + sales-commercial-surface.md.

## ✅ SHIPPED-ARCS REGISTER (one-line pointers — full detail in the cited thematic file)
_Each arc below is COMPLETE (shipped+live, mostly committed+pushed). The inline narration was compiled out 2026-06-05; the verbatim former blocks are in [build-history/index-snapshot-pre-2026-06-05/](maltyweb-pm-memory/build-history/index-snapshot-pre-2026-06-05/README.md). Read the thematic file when touching that surface._
- **OBSERVED YEAST ON BBT + CCT CARDS — ✅ SHIPPED + DEPLOYED + SMOKE-VERIFIED 2026-06-11 (render-only; UNCOMMITTED per operator; NO mig, NO writes).** Both boards surface observed yeast strain+gen from `bd_brewing_brewday_v2` as a guarded muted chip "Levure · {strain} (G{gen})", refuse-don't-NULL on empty. INDEPENDENT of the paused strain-classification arc (reads OBSERVED, not `ref_recipes` link; observed-wins). **The earlier verbatim-copy BBT chip was SUPERSEDED 2026-06-11 by the single-homed derive-at-read resolver in `app/yeast-eligibility.php` (`yeast_observed_strain_expr` + `resolve_observed_yeast_strain`) — handles the `yeast='New Yeast'` sentinel via coalesce→new_yeast then `ref_yeast_strains`/`ref_yeast_strain_aliases` resolution; no more "New Yeast" leak. CANONICAL DETAIL → [yeast-strain-taxonomy.md](maltyweb-pm-memory/yeast-strain-taxonomy.md) §OBSERVED-STRAIN DERIVE-AT-READ (and index entry 6d).** → also [packaging-page-dashboard.md](maltyweb-pm-memory/packaging-page-dashboard.md) §OBSERVED YEAST ON BBT CARDS.
- **BBT/CCT CIP-CADENCE INFRA — RESOLVER BUILT, NOT YET ON BBT CARDS** (migs 175 `ref_cip_types` / 176 `bd_cip_events` / 177 backfill / 213 cct-cadence keys / 230 filler-target; verified live 2026-06-11). Canonical model: **`bd_cip_events`** = one row per CIP (XOR parent FK racking/brewing/packaging; `target_kind`=vessel|machine × `target_code` incl. `bbt`; `target_number`=BBT number; `cip_type_id_fk`→`ref_cip_types`{1 Soude,2 Acide,3 Full CIP,4 Full CIP+rinser}; `cip_date` VARCHAR mixed ISO/US-`%c/%e/%Y`). **A "blend" = a `bd_racking_v2` row INTO a BBT** (`racking_destination_type='BBT'`, `bbt_number`, `is_tombstoned=0 AND interrupted_flag=0`). **Thresholds + reset-type sets ALREADY in `commissioning_settings` section `cip_cadence`** (`cip_cadence_acid_after`=6, `cip_cadence_full_after`=6, `cip_cadence_acid_reset_types`='2', `cip_cadence_full_reset_types`='3,4'). **THE DERIVATION ALREADY EXISTS: `app/cip-cadence.php` → `cadence_for_all_bbts(PDO)` / `cadence_for_bbt(PDO,int)`** = pure-read, per-request-memoised, returns per-BBT {last_full_cip_at, last_acid_cip_at, racks_since_full, racks_since_acid, threshold_full/acid, recommended_action none|acid_recommended|full_recommended, severity ok|warn|critical}; live-verified correct (BBT#8 → 17 racks since full → critical). Currently surfaced ONLY as `window.BBT_CIP_CADENCE` in the RACKING session form (session-body-racking.php:368-392) for the BBT picker. **283 bbt CIP events live; 8 active BBTs.** ⚠️ DATA-QUALITY FLAG: some `bd_cip_events.cip_date` are FUTURE-dated (2026-12-xx) — input/normalization issue, not a resolver bug. **GAP for the BBT-card ask: BBT cards render in `packaging.php` (§"Tanks de garde BBT", `$bbtRef`/`$bbtOccMap`, static `<div class="tank-card">` — NOT clickable). The CCT click-to-modal pattern to MIRROR lives in `tanks.php` (`<button.tank-card data-cct>`, `window.CCT_DETAILS`, `<dialog#cct-detail-modal>`, `public/js/cct-detail-modal.js`).** Build = make BBT cards clickable + inject `cadence_for_all_bbts()` + BBT detail modal; NO new table, NO new threshold store, reuse the resolver verbatim. CSS skeleton `public/css/cip-section.css` exists (racking-form context).
- **SINGLE-SOURCE COGS REPOINT + FINANCIER PAGE v1** (maltytask `489ab70` + maltyweb `6a013ad`, migs 300/301/304; SHIPPED+LIVE 2026-06-10) — (a) `loadSales()` reads ONLY `inv_sales_ledger` (44,164 rows 2021→2026, single source), retiring the bc+orders union; artifact now 66 months; ledger writer `ingest_bc_sales_ledger.py` committed; sign convention EMPIRICALLY VERIFIED (outbound NEG qty/hl; CHF revenue-signed plain-SUM). **SEAM: ledger-only FOR NOW — eshop+taproom still IN the sale_class filter, peel off to Shopify/Lightspeed feeds LATER (isolated behind the filter).** (b) `financier.php` read-only finance hub (ref_pages id=239, manager/general/sort=125) consuming `cogs-report-data.json` (COP) + `sales-cogs-data.json` (COGS) via lazy `api/financier-data.php` per-month slice — NEVER recomputes. **OPEN: /financier tour card NOT dispatched (dirty shared tree — tour-gap-check flags Financier + the FG-stock page CRITICAL; deferred until tree clean).** **🆕 2026-06-10 KOUROS FINANCIER FEEDBACK — PM RULINGS (pre-build; verified live):** (1) **✅ RESOLVED 2026-06-10 — render bug ROOT-CAUSED + FIXED (maltyweb `f1ff3a1`, pushed + live-confirmed).** "All 4 sections don't work" was NOT data, NOT a JS error: `public/css/financier.css` had `.fin-panel{display:flex}` which OVERRODE the HTML `[hidden]` attribute (a `.class{display}` rule beats `[hidden]`'s UA `display:none` on SPECIFICITY) AND `.fin-panel--active` was empty → all four panels (COP/COGS/Marge/SKU) rendered STACKED simultaneously, so clicking tabs did nothing visible. Content + data were ALWAYS correct (manager-auth Playwright smoke verified: COP 77,169 CHF, COP/HL 88.76, Marge 85.8%). FIX = `.fin-panel{display:none}` + `.fin-panel--active{display:flex}` (tab JS already toggles `--active`; HTML sets it on COP). **DURABLE LESSON (reinforces the visual-acceptance gate; added to the `ui` skill rendering-bug catalog as the `[hidden]`-overridden-by-`.class-display` trap, sibling of the `<dialog> display` trap): `php -l` + data-layer checks PASS a stacked/broken page — only a real manager-auth FULL-PAGE SCREENSHOT caught it. The manager-render screenshot is the LOAD-BEARING UI gate on a role-scoped page, not optional.** *(Original static-analysis dead-end recorded for the record: node --check, hoist, tab↔panel data-tab↔fin-panel-* contract, render-target IDs, php-fpm/nginx all 200/clean — all missed it because the fault was CSS specificity, not JS/PHP.)* (2) **Retire standalone SKU cost page** — Kouros assumed BSF-linked; WRONG (verified: `sku-costs.php` reads `ref_skus`/MySQL, only `google` strings = Google Fonts). RULING = **Option (b), NOT full-absorb (a) now**: KEEP `sku-cost-detail.php` (drill-down, 2 callers sku-costs.php L370 + bom-review.php, no ref_pages row — NOT the scrap target); the Financier "Coût par SKU" tab (`fin-panel-sku`, financier.php L432-450) currently LINKS OUT to `sku-costs.php` → repoint/inline the per-SKU LIST into the panel (reuse the SAME `ref_sku_bom` read — NO re-derivation = divergence flag), THEN `is_active=0` ref_pages id=12 sku-costs + log the .php to scrapping-backlog (gate: panel covers list + 0 inbound routes). Full-module absorb = optional later increment. **✅ #2 SHIPPED 2026-06-10 (maltyweb `e72e64e`, pushed) — went FURTHER than the inline-list plan: the Financier "Coût par SKU" tab (`fin-panel-sku`) now renders the FULL per-SKU cost table SERVER-SIDE reusing sku-costs.php's EXACT `ref_sku_bom` query VERBATIM (no re-derivation) — 3 KPIs (active SKUs / recipes / médian CHF·HL) + per-recipe-grouped rows (brewing/packaging/total CHF + CHF·HL) + anomaly flags (keg-inverted/outlier/extreme-bom-line) + BOM freshness stamp + SKU→sku-cost-detail.php links + client-side recipe/format/classification filters (no reload); query try/catch-wrapped (DB hiccup can't fatal the COP/COGS modules). Retirement = **mig 318** (NOT the earlier-noted 313 — parallel sessions took 313–317; head now ≥318) sets `ref_pages.sku-costs is_active=0` AND `public/modules/sku-costs.php` DELETED (operator-authorized, git-recoverable — went past the planned is_active=0-only); `sku-cost-detail.php` KEPT (back-links repointed to /modules/financier.php); 0 dangling hrefs. Live-verified Opus-independent (mig 318 applied, is_active=0, 154 active SKUs carry BOM cost, php -l clean, HTTP 302). → scrapping-backlog **#32 RETIRED**. ⚠️ **Visual-acceptance OPERATOR-PENDING** (build session's local→site browser flaky; manager-render screenshot is the load-bearing UI gate per #1's lesson, not yet eyeballed — same gap pattern).** (3) **✅ À-valider topbar dedup SHIPPED 2026-06-10 (mig 312, commit `1a81a3f`, pushed) — RULING EXECUTED.** `is_active=0` on ref_pages **id=99** invoice-validate (txn + log_revision). NO orphan + NO new flag needed: topbar filters is_active=1 (topbar.php L18) but `_page_registry()`/`user_can_access()` query ref_pages with NO is_active filter (auth.php L370) → is_active=0 IS the hidden-but-accessible pattern (page still reachable by href + access-gated). Queue already lives at `triage.php?tab=valider` (id=4 topbar; shared partial valider-queue.php; invoice-validate.php is a thin wrapper since the 2026-06-08 merge `484cc6a`) — so hiding id=99 leaves exactly Triage carrying À-valider. NB ref_pages has NO `nav_hidden` col; min_role bump is the WRONG lever (restricts reach). **SEQUENCING: #3 + #1 parallel (independent); #2 GATED behind #1 going green (don't absorb into an undiagnosed broken panel). EQUIP: #3 sql+coder+webapp-testing; #1 ui+coder+sql+webapp-testing (manager smoke load-bearing); #2 ui+sql+coder+webapp-testing.** ⚠️ CONCURRENCY INCIDENT: `6a013ad` mixed-boundary — a parallel session's FG-stock KPI suite + mig 305 swept in via the SHARED git index; nothing broken, leave as-is; LESSON = separate clones/worktrees or serialize commits. → [sales-commercial-surface.md](maltyweb-pm-memory/sales-commercial-surface.md) §SINGLE-SOURCE REPOINT + §FINANCIER + §CONCURRENCY INCIDENT. (4) **✅ BOARD COGS/COP P&L GRID — ALL 4 STAGES DONE, ARC CLOSED (SHIPPED+LIVE+PUSHED; maltyweb `d0c7e60` ACTUALS → `b477565` BUDGET, both pushed; remote == deployed).** Built into Financier id=239 per the §BOARD ruling — host EXTENDED, NO new Node script/JSON. `financier-data.php` new modules `cop-grid`+`cogs-grid`: ACTUALS = `inv_charges_bc` net SUM(debit−credit) grouped by `gl_account_no` (is_summary=0), GL→line map mirrors the sheet exactly; `fin_gl_booked_months()`/`fin_ytd_months()` intersect window with booked months so cost numerator + HL denominator cover the SAME months (honest under-read). Denominators: COGS÷HL sold (`inv_sales_bc.hl_resolved`), COP÷HL brewed (FIN_COP `hlBrewed`). Grid card at top of fin-panel-cop/cogs, `renderCopGrid`/`renderCogsGrid` tree tables, chip "mois complets uniquement". **VERIFIED 2026-03 vs independent SQL: Brewing 49.57 / Packaging 49.50 (4206 Labels −5.34 credit-net working) / Indirect 4.69 / Utilities 1.52 (waste only) / R&D 0.21 / TOTAL COGS VARIABLE 105.48 CHF/HL; manager full-page screenshot rendered.** PLACEHOLDERS render "—" (refuse-don't-guess held): BUDGET, N-1/2025 cost, variance%, semi-var labor split, COGS grand total, COP per-type (Keg/Bott/Can/Bulk), Scotch; zero-booked GL = "0.00" (≠ "—"). **COVERAGE GATE: `inv_charges_bc` has only 2026-03 full (411 rows) + 04/05 THIN (20/22) → grid DEFAULTS to 2026-03; thin months can't be auto-distinguished from complete (chip caveat only) — bookkeeper must upload full monthly GL via Le Cockpit.** Commit-hygiene: first commit swept 3 parallel-session files via shared index → reset --soft + recommitted clean 4-file `d0c7e60`, restored the 3 to staged (same shared-index race lesson). **✅ STAGE 4 (BUDGET) SHIPPED+LIVE+PUSHED 2026-06-11 (maltyweb `b477565`, pushed; mig `327`).** `ref_budget_cogs` = 21 GL-line rows, fiscal_year 2026, `budget_chf_per_hl`, schema_meta classified; SEEDED from the operator's board-sheet COGS&YIELDS BUDGET column (total **75.76 CHF/HL**) — seed-from-explicit-statement, every number traces to a cell (not guessing). cogs-grid module loads budget per line → **BUDGET (month+ytd, FLAT ANNUAL RATE) + Vs BUDGET = (actual−budget)/budget %** columns now LIVE on the COGS grid (April proof: Malts −37.3%, Hops −20.6%, **TOTAL COGS VARIABLE −8.0%**). **COP grid UNCHANGED — no budget on its sheet.** **v1 APPROX (documented): Transport + QA/QC budgets are monthly-profiled in the source sheet but seeded as the annual rate.** Manager-auth screenshot verified. ⚠️ **GIT HYGIENE: `b477565` swept a parallel expeditions session's staged DELETION of `public/css/expeditions-sku-history.css` and was pushed before catch — deletion SAFE (file unreferenced; expeditions shipped in `e7962ce`; removal intended) so left, not history-rewritten. 2nd bare-commit sweep in one session → pathspec-commit is now a HARD RULE (CHECKLIST).** **ARC BACKLOG (non-blocking, NOT gating): N-1 cost (2025 GL CSV ingest → N-1/Vs-N-1 cols still "—"); COGS-YTD-starts-March coverage chip; May utilities booking; the dead `fin_ytd_months(rolling6m)` path the COP stage flagged.** **ARC STATUS: COP/COGS board grids fully re-sourced to operational SoT, per-line, budget-comparable, Vue-comptable+CHECK reconciliation — all 4 stages done.** Full as-built → [sales-commercial-surface.md](maltyweb-pm-memory/sales-commercial-surface.md) §BOARD COGS/COP P&L GRID §STAGE 4 BUDGET.
- **OPERATOR ATTRIBUTION + JOURNAL DES SAISIES** (migs 295 + 298; commits `c8e895c` + `5b918f3`; SHIPPED+LIVE 2026-06-09, **deployed-not-pushed — Kouros reviews/pushes 2026-06-10**) — ASK 1: durable FK `submitted_by_user_id_fk → users(id)` on ALL **7** bd_*_v2 tables (set-once at INSERT, edit path omits FK from `bd_upsert $row` + restores original `email` from pre-image → original submitter no longer clobbered by a Corriger edit); recent blocks display `COALESCE(NULLIF(u.display_name,''),email)`; backfill unambiguous-only/idempotent (5 ex-employee strings stay FK-NULL+email); bug-row id=434 corrected to Gonzalo (rev#13003). ASK 2: read-only **Journal des saisies** page (general/sort=15, below "Lots en cours"), VIEW `v_saisie_events` UNION over 7 tables, **20s live auto-refresh** (operator override of the planned poll-on-load), `<dialog>` drill-down → live event row + `audit_row_revisions` tail (whitelisted `table` param). COGS-NEUTRAL+SOT-safe (event-row fact, nothing in SKU/BOM/COGS reads it). RESIDUAL for 2026-06-10: authenticated VISUAL smoke NOT done (no creds; smoketest acct id=16 exists for next pass); brewing ingHeaderRow always-insert edit-FK behavior to eyeball; gravity feed noisy-by-design. → [operator-attribution-and-event-recap-arc.md](maltyweb-pm-memory/operator-attribution-and-event-recap-arc.md).
- **④ DIRECT-SALES TRACKER MVP "Tap&Shop"** (mig 291, commit `a13a9d0`; SHIPPED+Opus-verified LIVE+PUSHED 2026-06-09) — read-only logistics top-bar page `public/modules/tap-shop.php` (page_key='tap-shop', **ref_pages id=186, min_role='operator'** [seeded 'manager', flipped 2026-06-09 topbar-visibility fix — data-only, audit-logged; Olivier=user 19 operator/preset 8 now sees it], sort=105, presets 1+3+8). Plane A Shopify+taproom sales per beer/SKU/period with visible "Non rattaché" bucket + dual-channel KpcCharts; Plane B virtual consumption stock via a SINGLE `fg_stock_compute()` call (L142, same derivation as weekly stocktake). WRITES NOTHING (grep-verified). Forward-compat Phase-2 hooks: `vd_load_direct_sales()`, is_direct_sales gating, sku_id throughout, inert placeholder panel. **Phase 2 (cage/COGS engine: cage-vs-loose routing, inv_stock_residual, inv_consumption sales_derived+cage-drawdown, COGS-into-COP, eshop bin-packing) DEFERRED, gated on Shopify pickup-vs-shipped signal (on hold, team reworking Shopify) + cage anchor (count starts 2026-06-15).** Keg-share (-P25/-P50 draft pours) handled separately via packaging-template batch, NOT here. → RESUME #0b + [sku-bom-validation-walkthrough.md](maltyweb-pm-memory/sku-bom-validation-walkthrough.md) + [sales-commercial-surface.md](maltyweb-pm-memory/sales-commercial-surface.md).
- **RECETTE TEMPORAL VERSIONING v2 (SCD2 write-side)** (mig 289 `289_rri_scd2_open_uniqueness.sql`, commit `a1c68dc`; SHIPPED+VERIFIED+PUSHED 2026-06-09) — recette hop edits in `salle-de-controle.php` now close-then-insert into `ref_recipe_ingredients` instead of mutating in place, so gap-fill/COGS resolve recette-at-brew-date. Mig 289 (Option A) added STORED gen-col `open_key` + DROP `uq_recipe_mi_stage` + ADD UNIQUE `uq_rri_open` (the textbook close-then-insert was blocked by the dates-not-in-key UNIQUE → 1062; this was the REQUIRED migration the old plan didn't foresee). `rri_close_version()` helper in `app/recipe-ingredients-loader.php`; read-side predicate unchanged; 164 rows backfilled effective_from='2025-01-01'. Only the 3 hop write-actions write `ref_recipe_ingredients` today; helper reusable for a future generic qty editor. OUT: SDC version-history UI; form-fermenting dry-hop as-of; v3 retrofit. ZERO `ref_sku_bom` impact (2427 untouched). → [project_recipe_versioning_temporal.md](/home/kluk/.claude/projects/-home-kluk-projects-maltytask/memory/project_recipe_versioning_temporal.md).
- **INGESTION VALIDATION GATE + SIE/SIL ENERGY WEB-PATH** (migs 282/283/284; A=maltytask `c80f6f8`/maltyweb `fd1c5ed`, B1a=maltytask `8c991a6`/maltyweb `d894b6f`, B1b=maltyweb `6973316`, B2=maltyweb `e0e580d`; SHIPPED+LIVE+E2E-VERIFIED 2026-06-08) — worker no longer auto-writes inv_deliveries: STAGES the gate's derived write-plan → `doc_invoices.delivery_write_plan` JSON + energy → `energy_extract` JSON; operator validates via « À valider » module (`invoice-validate.php`, ref_pages id=99) OR upload-page recap (`app/recap-lines.php` + triage-upload.js) → new `--commit <uploadId>` worker mode replays the plan through the SAME `appendDeliveryToMysqlWithConn` loop (one writer, zero COGS-fork — the WORKER-COMMIT-MODE ruling held). Drive sourcing RETIRED (ingest-sie-invoice `--local`-only; build-cogs-report hook removed). SIE period convention = consumption-month not billing-date (8906→2026-04). Sudoers www-data→maltytask gap closed. ⚠️ One untested seam: live browser Valider-click. → [ingestion-validation-gate-arc.md](maltyweb-pm-memory/ingestion-validation-gate-arc.md) §SHIPPED RECORD.
- **VISITE GUIDÉE — first-login onboarding tour + TOUR-STEWARD SYSTEM** (`ca65c22` + mig 280 `users.tour_seen_at`; chapter expansion `91f8c82` SHIPPED+LIVE+PUSHED 2026-06-08; **tour-steward governance system SHIPPED 2026-06-09 on ratified terms, NOT YET COMMITTED**) — `/modules/visite-guidee.php` (require_login only, NOT in ref_pages), steps server-built from `ref_pages × user_can_access()`; production presets = 25 steps (Brassage 3/Fermentation 2/Transferts 3/Conditionnement 3 + capstone "La chaîne des pertes"), logistics = 11; content map pure PHP page_key+sub-index (no DB/mig); default landing mon-tableau, exactly-once auto-redirect via tour_seen_at, topbar re-launch link, grid-overlay + compact-dot mode >20 steps; no-DB-nomenclature/threshold vetoes grep-verified on rendered HTML.
  **TOUR-CARD PREDICATE** (what "this page needs a tour card" means, = visite-guidee.php's own filter minus saisies): `is_active=1 AND (domain IS NULL OR domain <> 'admin') AND page_key <> 'saisies'` (admin excluded; `saisies` special-cased into its own opener + form chapters, not a `$PAGE_DESCRIPTIONS` entry).
  **LAYER 1 — DETECTION (NO git hook, NO marker file, by design):** `scripts/tour-gap-check.php` (note: `scripts/` not `bin/` — repo convention mirrors `scripts/reconcile-pages.php`) — read-only drift detector, live `ref_pages` MINUS the THREE content maps in visite-guidee.php (`$PAGE_DESCRIPTIONS` mandatory / `$PAGE_ICONS` recommended / `vg_vignette_for()` optional); block-scoped text parse (file can't be include'd — login side effects). Tiers: CRITICAL=no description / MINOR=no icon / INFO=no vignette / LATENT=inactive-no-card; exit 1 on CRITICAL|MINOR, INFO/LATENT advisory; `--quiet`. Run: `ssh maltyweb "sudo php /var/www/maltytask/scripts/tour-gap-check.php"`.
  **LAYER 1 — AUTO-TRIGGER:** `scripts/migrate.php` post-apply advisory — if any just-applied migration's SQL contains `ref_pages`, passthru-shells `tour-gap-check.php --quiet` and prints a WARNING on gap; NEVER blocks / never changes migrate.php exit code.
  **LAYER 2 — THE AGENT:** `/home/kluk/.claude/agents/maltyweb-tour-steward.md` (sonnet, EQUIP coder+ui+webapp-testing) — codifies the six mechanical copy rules (house voice / no-DB-nomenclature grep-verified / no-thresholds / no-future-promises / bespoke SVG / optional vignette) + the four mandatory-ratification carve-outs (COGS-COP-WAC-BOM-beertax · identity/access · ambiguous-purpose · self-flagged-uncertain). KEY IMPL DETAIL: a subagent can't call another subagent, so the steward does NOT consult PM directly — for a sensitive-class page it DRAFTS, STOPS pre-deploy, returns the draft flagged `PM-RATIFY: <page_key>`; the orchestrator (Opus) routes it to PM and relays the ruling. Non-sensitive pages: steward drafts-to-rules + deploys + smokes autonomously. Includes no-commit + no-dirty-tree-deploy (selective rsync) guards.
  **COVERAGE STATE (live gap-check 2026-06-09):** `critical=0 minor=0 info=8 latent=0`, exit 0. tap-shop (active) + qa (inactive, pre-written) both fully covered. The 8 INFO = active pages w/ description+icon but default vignette (cosmetic backlog, no action). `zeppelin` left as-is per ruling.
  **⚠️ NOT YET COMMITTED (2026-06-09):** three files deployed-live-but-uncommitted in the maltyweb working tree — `public/modules/visite-guidee.php` (tap-shop+qa cards), `scripts/tour-gap-check.php` (NEW), `scripts/migrate.php` (advisory). Agent file is in ~/.claude (separate). Awaiting Kouros's commit decision — this is a live dirty-tree hazard per checklist §line 14: a `bin/deploy.sh --apply` from this tree leaks all three (plus any other session's dirty files).
  **PM OWNS dispatching the tour-steward after any `ref_pages`-touching build** (RULE 3). Standing convention: new ref_pages entry → bespoke tour content row. DEFERRED POLISH: zeppelin per-family depth (low payoff); the 8 INFO default-vignette pages (cosmetic). SEPARATE flagged bug (NOT this work): form-packaging.php ~L2451-2452 helper text leaks raw `prod_total_units`/`qte_unites` to operators. → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md) §VISITE GUIDÉE.
- **FIRST-LOGIN DEVICE-CLASSIFICATION INTERSTITIAL** (`94d5a0d`, ZERO mig, SHIPPED 2026-06-07 night; as-built debrief vs spec pending) + **KPI RECAP CRON ACTIVATED** (`1d8672b`, operator confirmed inbox — user-account arc's last build DONE). → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md).
- **UI/UX QUALITY REWORK — ARC FULLY CLOSED, Phases 0+1+2+3+C2+calibration** (commits `9104273`/`01c99e3`/`9dcd4ca`/`cd96cd4`/`9ed0f6f`/`49981f4`/`c31163f` + Phase 3 `e05b18c`/`e679efa`/`aba178a`, ZERO mig, SHIPPED+LIVE+PUSHED 2026-06-07) — P0–P3 audit (35 findings) + shared-layer fixes site-wide + per-page polish (Phase 3: 3 parallel bundles + combined-diff pre-commit RULE-2 review, 2 P1s fixed pre-commit); svg_cct/svg_bbt + SdM scene + all four `svg_vessel_*` rebuilt GRAVURE DENSE; **`app/svg-tanks.php` = the ONE canonical vessel library** (svg-vessels.php tombstoned → scrapping-backlog #31). Residual = 4-item backlog incl. the bom-review post-F2 carve-out (→ arc file §RESIDUAL BACKLOG); atom-11 smoke = visual acceptance of sb-* gravure. → [ui-ux-quality-rework-arc.md](maltyweb-pm-memory/ui-ux-quality-rework-arc.md).
- **MON TABLEAU v2** (commits `2a8f2b5`→`8800a9e`→`db2da33`→`b66244a`→`e8f5336`, mig 278_kpi_seed_v2_batch, SHIPPED+LIVE 2026-06-07 evening) — dense 4-col grid (viz_type span map, grid-auto-flow dense) + category-grouped dashboard/picker (one $CATEGORY_ORDER source) + fr-CH/unit-aware `fmt()`; 21 new production handlers (racking 7 / packaging 11 vendable-model / tank readings 2 / wort yoy) + REAL MP days-of-supply (v_rm_stock_dynamic ÷ trailing-90d inv_consumption, Opus-verified 147.0 j) + maintenance GL-6100 own series; honesty sweep 28/43 honest, relabels 111+266, 13 stub-mismatches→dr=0 (admin notice clear); **51 trackers live** (census 51/136/81); 15 sub-pages ACL-gated; recap-guard session-payload defect #3 fixed (DB-read convention). Opus V1 verification EXACT to the decimal. → [kpi-tracker-catalog.md](maltyweb-pm-memory/kpi-tracker-catalog.md) §2026-06-07 EVENING + user-auth-admin-arc.md §2026-06-07 EVENING.
- **P2b — KPI PERSONAL DASHBOARD** (commits 99d5d54 + 2c37c9d, SHIPPED+LIVE 2026-06-06, ZERO mig) — `public/modules/mon-tableau.php` (min_role=viewer, registered in `ref_pages` via idempotent upsert) reads `user_kpi_selections` → `kpi_dispatch()` per tracker → renders via the NEW shared `public/js/kpi-charts.js` (`window.KpcCharts`, 9 viz_types, EXTRACTED from `wort-kpis.js`; wort.php loads kpi-charts BEFORE wort-kpis, both `defer`, wort unchanged). Tracker picker server-filtered domain×role×data_ready; POST rebuilds allowed-set server-side (`mt_build_allowed_set`) + REFUSES out-of-scope (forged viewer→manager-tracker POST rejected); CSRF+log_revision+PRG; NO new compute path. 4 `utilities` data_ready=1 trackers → STUB handler detected/excluded/admin-noticed (constraint #1). Feed-2 WAC-divergence sub-flag added to `bom-review.php` (0 rows today). Role-filter: viewer=0 / manager=30 / admin=39 selectable — **viewers get EMPTY dashboard (no data_ready viewer-tracker; SEED-TUNING flag for next seed-touch, not a bug)**. NEXT = P3 recap emails (WAITS ON ONBOARDING). → [kpi-tracker-catalog.md](maltyweb-pm-memory/kpi-tracker-catalog.md).
- **P2a — KPI FRAMEWORK** (migs 269/270, commit 0a99b8b, SHIPPED+LIVE 2026-06-06) — `ref_kpi_trackers`(268 seeded: 43 data_ready, 6 hero) + `user_kpi_selections`(sparse, CASCADE FK), schema_meta'd. `app/kpi-handlers.php` = blessed thin-handler registry dispatching on `source_domain` w/ whitelisted params (NO free-SQL-in-DB). IMPLEMENTED: wort (Cooling-row canonical) / cogs (consumes pipeline JSON, never recomputes) / ops_health / rm_procurement; STUBBED return `stub:true` (never error): tanks/racking/packaging/fg_stock/sales/qa_qc/equipment/logistics. Honors all KPI divergence flags. NEXT = P2b dashboard (SHIPPED). → [kpi-tracker-catalog.md](maltyweb-pm-memory/kpi-tracker-catalog.md).
- **BOM REVIEW SURFACE — ARC CLOSED** (defect tab `7730bc4` + browse tab `43022d7` + WAC-divergence guard; SHIPPED+LIVE 2026-06-06, ZERO mig) — `public/modules/bom-review.php`(now 2-tab)+css+js; standalone, `require_page_access('bom-review')` (ref_pages: min_role=admin, domain=admin, sort=185). **Defect tab:** 3 feeds; EDIT ONLY at binding/per-SKU-choice/ref_mi-price altitude (NEVER `ref_sku_bom` — recompile clobbers); brewing read-only-w/-provenance; CSRF+log_revision+snapshot+PRG; altitude round-trip tested (DIV33C→617, parity 0); reconcile-pages 0 drift. Feed 2 (unpriced-MI) = `ref_mi.price IS NULL OR =0` ∩ active pkg-BOM = 24 rows, PM-VERIFIED IDENTICAL to `v_mi_cost.cost_basis='no_basis'` → ACCEPTED w/ forward-divergence ticket. **Browse tab "Parcourir les BOM":** all 153 active SKUs grouped classification→subtype (`ref_recipes`, LEFT JOIN, census 99/34/6/7 + 7 composite + 2 format-NULL = no drop), format-filterable (`ref_packaging_formats.display_name`); provenance tier + snapshotted `ref_sku_bom.cost` (no recompute on render); SAME edit actions/recompile path; confirm-blast-list + inline parity + closed-month COGS warning. **⚠️ `sdc_recompile_recipe_packaging()`+`sdc_flash_bom_result()` RELOCATED into `app/sku-bom-compile.php` (function_exists-guarded), required by both pages — canonical home is the lib now, NOT salle-de-controle.php.** → [packaging-bom-model.md](maltyweb-pm-memory/packaging-bom-model.md) §SHIPPED BROWSE.
- **DIV33C CAN-FIX — durable altitude** (SHIPPED 2026-06-06, ZERO mig) — fixed via Tier-1 per-SKU choice `ref_sku_packaging_choices` id=11 (sku_id=14, slot='can', mi_id_fk=617 PKG_CAN_ALU_33), NOT a binding repoint. Binding id=30 LEFT at 616 (DIV33C is sole Diversion can SKU; flagged to repoint→PKG_CAN_DIV id 626 only if a 50cl Diversion can is ever packaged). inv_consumption corrected (total preserved). +3 WAC-priced PKG_*_NOMOQ. Recompile-idempotent, parity 0. → [packaging-bom-model.md](maltyweb-pm-memory/packaging-bom-model.md) + [sku-decomposition-tree.md](maltyweb-pm-memory/sku-decomposition-tree.md) (`ref_sku_packaging_choices` is the Tier-1 override layer).
- **USER-ACCOUNT DEEPENING — PHASE 1 PER-PAGE ACCESS CONTROL** (migs 266/267/268, commits c065743/c747006/2466869, SHIPPED+LIVE 2026-06-06) — `ref_pages`(18, retired hardcoded `$modules`)/`ref_access_presets`+`ref_access_preset_pages`+`users.access_preset_id_fk`(3 presets)/`user_page_access`(sparse override); `app/auth.php` `user_can_access()`/`require_page_access()` (admin bypass→role floor→per-user override→preset→NULL-preset fallback; preset carried in session 4-site incl. remember_token); `reglages-generaux.php` access admin (sec=access). Phase 2 = KPI dashboard. → [kpi-tracker-catalog.md](maltyweb-pm-memory/kpi-tracker-catalog.md).
- **USER-AUTH / ADMIN / MANAGER-SCOPE / EMAIL / SHARED-DEVICE** (migs 261/262/264, HEAD `993d5ae`+post-ship `97aae6d`/`1649fb0`/`2e92de1`) — prod-vs-logistics `manager_scope` + `manager_can()`; `user_invites`+`set-password.php`+`mailer.php` (Postfix→IONOS noreply@maltytask.ch); `brewery_identity()` DB-sourced footer (the Neuchâtel-hardcode lesson → coder+ui skill); `auth_shared_devices` remember-me suppression. **2026-06-09: admin-editable username `2169a81` PUSHED; ROBUST LOGIN RESOLVER `auth_resolve_user()` ✅ SHIPPED+LIVE+COMMITTED+PUSHED (resolver = `d50ec4d`) — `auth_verify()` resolves typed identifier on full-username OR first-name-token OR email (case/accent-insensitive, whitespace-collapsed), AMBIGUOUS→FAIL-CLOSED; fixed the "username→First Last" lockout; soft dep = first-name uniqueness (warn at user-create, backlog). BOTH FOLLOW-UPS NOW SHIPPED+PUSHED: #1 collision warning `1fa4bcb`; #2 static login hint `2cd2b24` (`<small class="auth__hint">Prénom, nom complet ou e-mail</small>` in public/login.php + `.auth__hint` rule in public/css/app.css reusing `--ink-mute`, 0.78rem, margin-top 4px; Playwright-verified renders). RESOLVER ARC + both follow-ups FULLY CLOSED.** **2026-06-11: SALES_MANAGER PRESET (id=9, mig 324 APPLIED+VERIFIED on VPS, file UNTRACKED/uncommitted) for Louis Maechler — 8-page set (mon-tableau/financier/settings/approvisionnement/expeditions/tap-shop/warehouse/rm-comparison), operator-NARROWED from PM's 17-page option-B; intended config role=manager/manager_scope=logistics/preset=9; admin trio (charges-bc/ingest/db-browser) stays `require_admin`-gated (un-grantable to managers by design); (C) capability-refactor BACKLOG. NO ref_pages change → no tour-steward dispatch, BUT post-apply advisory flagged CRITICAL=2 PRE-EXISTING tour gaps (open steward item). PARALLEL-DEVELOPER GUARDRAILS (Louis sales-side onboarding) recorded — 5 rules into his brief. MIG HEAD now 324.** → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md).
- **SAISIE-FORMS 5-CORRECTION BATCH** (mig 258, commits `68ae0b4`/`039d4e3`/`b42e8b7`/`619b422`) — racking flowmeter→racked_vol; reusable `multi-submit-reads.js` (serialize/sum/average) for co2o2 reads + turbidity avg + all pertes; qa_analyses auto-derived read-only (reversed the never-derive ruling); INVARIANT 13/13 PASS. → [saisie-forms-5-correction-arc.md](maltyweb-pm-memory/saisie-forms-5-correction-arc.md).
- **RM PER-PALLET MULTI-LINE FORM** (mig 252, `c2aed24`) — type-ahead MI → bare-qty → instant per-pallet POST → server SUMs → live ledger; `inv_rm_stocktake_lines` child → `rm_recompute_rollup` → `inv_rm_stocktake.counted_qty`. ⚠️ RM-ONLY (brewing/racking/packaging stay single-summed). NULL-vs-0 invariant load-bearing. Archetype detail in this register's snapshot + conventions-and-helpers.md.
- **FORM-RESILIENCE ARC** (`3d7afce`, ZERO mig) — `session-ping.php` (401-JSON keepalive) + `form-resilience.js` (visible-tab keepalive + csrf-rewrite + localStorage autosave) + global topbar wiring. RM form's layers 2/3 later superseded by the per-pallet rewrite. Layers 2/3 for brewing/racking/packaging DEFERRED.
- **PACKAGING PERTES MODEL — CANONICAL** (ruled 2026-06-01) — beer-loss = `ref_packaging_loss_types counts_as_loss=1` dispositions in HL; CONSUMABLES ≠ canonical loss. Dashboard loss-% MUST add the legacy `loss_liquid_other_units/100` term (else 10× understated; the A3 mig-233 view DROPPED it). Beer-loss% ~1-2% from 2023. → [packaging-losses-convention.md](maltyweb-pm-memory/packaging-losses-convention.md) §PERTES-DASHBOARD.
- **UNIFIED PARALLEL-RUN FIX** (migs 250/251, `c355cf8`) — every packaging row (main|parallel) computes vendable from its OWN `prod_total_units`; `special_qty_units` retired to provenance/consumption-key only (NEVER summed into vendable/tax). Reconciled EXACT: keg 2828.2 / bot 1834.72 / can 363.60 / cuv 315.00. "Missing keg / cuv variable-fill" framings RETRACTED. → [packaging-page-dashboard.md](maltyweb-pm-memory/packaging-page-dashboard.md) §PARALLEL-RUN FIX.
- **PACKAGING PAGE DASHBOARD BUILD-OUT** (consulted, dashboard partly built) — `packaging.php` → furnished year-filtered KPI dashboard; canonical source `bd_packaging_v2` ONLY; Neb-vs-Contract = `recipe_id_fk → ref_recipes.classification` (100% cov, NOT sku_id_fk); format = `run_type` (5 vals bot/can/can33/keg/cuv); GROSS output = prod_total×vol (not vendable_hl). → [packaging-page-dashboard.md](maltyweb-pm-memory/packaging-page-dashboard.md).
- **PACKAGING event_date BUG FIXED** (mig 235) — `event_date := DATE(submitted_at)` for the WHOLE bd_*_v2 family (never last_cip_date, never v1-JOIN). → derivation-tree-and-schema.md §EVENT-DATE RULE.
- **V2-ONLY SOURCE ARC** (`08c00de`+`16079a2`) — maltyweb reads `bd_packaging_v2` ONLY; cross-source v1+v2 dedup DROPPED (was the BBT7 double-count). CANONICAL v1/v2/RawDB architecture: v1=still-LIVE old DB (maltytask wired); v2=[v1@migration normalized]+[web-entry backlog]; RawDB.xlsx=FROZEN → re-normalize is OFF the table (clobbers web_entry), fixes are surgical SQL. → [derivation-tree-and-schema.md](maltyweb-pm-memory/derivation-tree-and-schema.md) §V2-ONLY SOURCE RULE + §CANONICAL v1/v2/RawDB ARCHITECTURE.
- **LINER-MI-DROPDOWN ATOM** (mig 239, `63977f6`) — liner client+transport = DROPDOWN of liner MIs (FK→ref_mi via ref_mi_subcategories 'Liner'), not Oui/Non; bool-set=legacy, FK-set=new. Consumption wiring DEFERRED. Lesson: allowed-set must be built in the POST path, not only GET. → loss-types-and-form-changes-arc.md.
- **PACKAGING-FORM 4-CHANGES ARC** (migs 236/237/238) — P1 per-row cuv client/liner; PKG-CLIENT `ref_packaging_clients` (three-client-tables rule holds); P2 `reuses_packaging_id_fk` cuve-réutilisée (view zeroes outputs); mig 236 brewday backfill (21 contract brews). → [packaging-losses-convention/loss-types-and-form-changes-arc.md](maltyweb-pm-memory/packaging-losses-convention/loss-types-and-form-changes-arc.md).
- **IN-TANK / IN-FILLING CO₂/O₂ REMODEL** (migs 241-249/255/257) — `bd_tank_readings`=in-tank DIMENSION (not a loss; shared per beer/batch/day via UNIQUE-as-inheritance); `bd_packaging_readings`=in-filling losses; `co2o2_measures` DROPPED. QA conformance tracker in Salle de contrôle. Broken-bridge lesson (NEVER join v1↔v2 on sheet_row_index) + is_tombstoned gap (flag #8). → [packaging-losses-convention.md](maltyweb-pm-memory/packaging-losses-convention.md) + project_co2o2_intank_infilling_split (orchestrator memory).
- **PACKAGING SAISIE 8-POINT REWORK** — A1+A2+A3 (migs 231/232/233: dispo cols + co2o2 child + loss_untaxed_full) + Atom C white-label ✅ ADDITIVE SHIPPED+PUSHED `40b00be`. Remaining = (a) DATA-DRIVEN LOSS TYPES [DESIGNED, A-LT2 paused]. → [packaging-losses-convention.md](maltyweb-pm-memory/packaging-losses-convention.md) + [packaging-losses-convention/white-label-atom-c-arc.md](maltyweb-pm-memory/packaging-losses-convention/white-label-atom-c-arc.md).
- **GLOBAL SHELL FIX / overflow-x** (`408285e`+`65f1b93`) — fit-guard `.home .main > *{max-width:100%;min-width:0}` + `body.home/auth{overflow-x:auto}` (scroll not clip); `body.home.salle-des-machines{overflow-x:hidden}` exemption MUST be preserved (400% sliding stage). Folded into `ui` SKILL.md #76/#77.
- **RE-EDITABLE PACKAGING SUBMIT** (`f8a9e18`, ZERO mig) — `?edit=<main row id>` prefill of whole session re-POSTed through the FROZEN idempotent handler; shared-in-tank read gate fires only on actual value change; orphan-on-shrink; hors_process round-trip. → saisie-forms-5-correction-arc.md + packaging-losses-convention.md.
- **BREWING FORM ENHANCEMENTS** (ZERO mig — `bd_brewing_timings_v2` pre-existed, mig 226 STILL FREE) — Issue 1 FW/PV/KW gravity+timings UI; REOPEN/PREFILL `?edit=` (closed F-1 P2 sticky-ingredients for free); Issue 3 per-brassin explicit dates (Model B, "two dates two purposes": timings.event_date=brew START, cast-out HL dated by brew END via Cooling-row submitted_at). → saisie-forms-5-correction-arc.md + recipe-ingredients-drift-surface.md.
- **WORT-PRODUCTION KPIs** (`c4d92c9`, PUSHED, ZERO mig) — wort.php "KPIs/Analyse" tab; **CANONICAL WORT METRIC = cooling-anchored** `SUM(bd_brewing_gravity_v2.final_volume WHERE event_type='Cooling' AND is_tombstoned=0)`, recipe via `recipe_id_fk` DIRECTLY (NOT brewday-date-equality join — undercounts ~15%). Archive-subtype bucketing via client_id OR single-lifetime-brew. 2026 YTD: Neb 6271.4 (Core 6168.7+Specials 102.7)+Contract 68.8. Board deck = Windows artifact `Board26Q2-Production.pdf`. → derivation-tree-and-schema.md (if it grows).
- **FERMENTING STATE-GATED SELECTOR + HOP-STAGE** (mig 253, `c38d924`/`8cc1c73`/`e81358e`) — `ref_recipe_ingredients` hop_addition_stage ENUM + boil-time + multi-stage-same-MI UNIQUE; SDC per-hop editor; fermenting card-selector mirrors racking. Gate-2 CIP-cadence REMOVED 2026-06-03 (category error — CCT is by design full of fermenting beer; fermenting = NO-CIP). OR-bridge retirement + operator-smoke open. → [recipe-ingredients-drift-surface.md](maltyweb-pm-memory/recipe-ingredients-drift-surface.md) §FERMENTING-SELECTOR + cip-module.md §FERMENTING = NO-CIP.
- **SDC RECETTES — ATOM B + 4-ITEM PLAN** (migs 219-225, `5e1e1c1`) — `ref_recipes.style` col now EXISTS; `ref_recipe_aliases` (89 rows) + `recipe-resolver.php` resolve canonical→alias→normalized; contract renames are two-hop (alias every intermediate, source = update_recipe_name audit rows NEVER guess); D/A/B/C sequential atoms (Contracts-never-upcoming lifecycle rule, mig 222). → [recipe-ingredients-drift-surface.md](maltyweb-pm-memory/recipe-ingredients-drift-surface.md) + [recipe-lifecycle-and-sdc-list.md](maltyweb-pm-memory/recipe-lifecycle-and-sdc-list.md).
- **MOTHER SHELL Phase 1 + Phase 2-5** (mig 215/216, `96ea0d4` → 10 atoms `3c248bd`…`8ce9222` + fix-forwards) — `op_sessions.form_type='batch'` + self-FK merged_into + blend_share_pct + filtered-UNIQUE open_mother_key + `app/mother-shell.php` resolver; board LIVE at sb-board.php (Concept C diorama, SVG vessels, drill-in, 15s polling, cuve-vide, merge/force-close, Salle de Guerre). Atom 11 = operator smoke (HELD). → [mother-shell-architecture.md](maltyweb-pm-memory/mother-shell-architecture.md) + [op-sessions-schema-spec.md](maltyweb-pm-memory/op-sessions-schema-spec.md) §7.
- **FORM-FINALISATION (backlog data-entry)** — F-access + F-0 (all 4 forms back-datable) + F-1 (brewing MULTI-BREW cooling capture, `89ed1a2`) + Build A (Inventaire RM form) + Build B (RM counted-vs-theoretical comparative, fixed-April-anchor formula). Backlog entry is INDEPENDENT of pilots 5/6. → build-history/former-index-resume-point.md + rm-retro-prereq-chain.md.
- **TANK-SIMULATOR occupancy** (2 passes, read-side only, ZERO mutation) — drain only if CCT's current occupant matches `(recipe_id_fk,batch)`; packaging→BBT drain dual-keyed (was last name-keyed path); CCT9 recency-gate; start_time DD/MM swap masked via COALESCE. → [tank-simulator-occupancy.md](maltyweb-pm-memory/tank-simulator-occupancy.md). sql skill #25/#27.
- **EL DORADO dup-MI merge (mig 254) + GL-6285 non-inventoried fix (mig 256)** — both `630d89f`/`62dde37` PUSHED. MI MERGE/TOMBSTONE-AND-RELINK pattern + uniq_row_hash-after-repoint gotcha + `is_inventoried`=counted-as-stock-not-GL-derivable rule → derivation-tree-and-schema.md §SQL BUILD SCHEMA.
- **DIG/QDG recipe consolidation (mig 217, `14f6785`)** — DELETE stub id=77, rename id=27→'Qrew - Diversion Gose' (sku_prefix DIG preserved), aliases migrated; the `audit_row_revisions.action` no-'delete' lesson learned here. CollabIn naming = "BrewerName - BeerName" + non-matching short prefix (Docks/DGD/DIG). RawDB.xlsx source-reconvergence #4. → recipe-lifecycle-and-sdc-list.md.

## 📂 THEMATIC FILES (load on demand)
- [KPI tracker catalog + personal-dashboard (Phase 2 of user-account arc)](maltyweb-pm-memory/kpi-tracker-catalog.md) — read when building/touching `ref_kpi_trackers`, `mon-tableau.php`, `user_kpi_selections`, `recap_subs`/`send-kpi-recap.php`, `kpi-charts.js`, the compute_handler registry, the `viz_type` ENUM (now **10 values incl `recap`** — daily-recap composite cards #270 wort / #271 packaging, mig 310), or any KPI tracker. Holds the 250-tracker catalog ref (docs/kpi-catalog.md), the blessed thin-handler/registry model, the params_json-whitelist (no free-SQL-in-DB), the data_ready auto-activation gate, the divergence flags (no COGS/WIP recomputation, cooling-anchored wort, tank-sim port gate), the v1/v1b/roadmap seed plan, and the ⛔-overlaps-existing-arcs map.
- [USER / AUTH / ADMIN / MANAGER-SCOPE / EMAIL / SHARED-DEVICE arc (✅ SHIPPED+LIVE 2026-06-04, migs 261/262/264)](maltyweb-pm-memory/user-auth-admin-arc.md) — read when touching `users`/auth/login/roles, admin user-management (`reglages-generaux.php` §users), the prod-vs-logistics MANAGER SPLIT (`manager_scope` ENUM + `manager_can()`), `user_invites`/`set-password.php`/`app/services/mailer.php` (IONOS relay), `brewery_identity()` (`app/settings-helpers.php` — DB-sourced footer identity, the Neuchâtel hardcode lesson), or the SHARED-DEVICE registry (`app/services/device.php` + `auth_shared_devices` mig 264 + `public/admin/settings/devices.php` — remember-me suppressed on shared stations). Holds the access/email/PWA advisory decisions + the 2FA/Tailscale-gate/PWA/onboarding backlog + the modification-REQUEST/proposal pattern (`ref_supplier_proposals` + `sf-update-field.php` for logistics; production forms write direct) + as-built auth (`app/auth.php` Argon2id, `is_admin`/`is_manager`/`require_*`).
- [Saisie-forms 5-correction arc + fermenting-correctness chain](maltyweb-pm-memory/saisie-forms-5-correction-arc.md) — read when touching the packaging/racking saisie forms, the `multi-submit-reads.js` widget, the fermenting EDIT-mode/cold-crash-checkbox/beer-identity-fracture/session-phase-cache/purge-cadence chain, the re-editable packaging submit, brewing form enhancements, or any of the PENDING-BUILD fermenting fixes in the RESUME POINT.
- [Pre-framework PACKAGING + BREWING pass — racking-pattern transfer scoping (🅿️ PARKED)](maltyweb-pm-memory/packaging-brewing-pre-framework-pass.md) — read at pilot 5/6 time for the P1-P19 pattern INVENTORY + per-form transfer map + 3 still-open operator Qs. Do NOT build standalone — patterns re-home into the 3-phase shell at pilot time.
- [op_sessions / op_session_steps — MIGRATION-READY SCHEMA SPEC](maltyweb-pm-memory/op-sessions-schema-spec.md) — read when building session-framework infra OR the BATCH MOTHER SHELL additive ENUM+self-FK (§7) or reasoning about the precise column shape of op_sessions / op_session_steps / additive `session_id_fk` / `bd_cip_events.source_form` ENUM / mother `merged_into_session_id_fk`+`blend_share_pct`+filtered-UNIQUE-via-generated-col + retro-link SQL.
- [BATCH MOTHER SHELL — multi-day batch envelope](maltyweb-pm-memory/mother-shell-architecture.md) — read when building Mother Shell Phase 2-5 / pilots 5-6 / the daily-child→mother auto-link / blend-merge survivor pattern / forward-only retro-link / the "Lots en cours" board / EXPÉDITION Stage-3 placeholder. Holds §AS-BUILT PHASE 2-5, §REFINED OCCUPANCY RULE, §BLEND RULING #4, §ATOM 14/15 MERGE, §ATOM 16/17. Cardinal: mother=batch-grain envelope; children=day-grain; events=truth; mother NEVER pins to a vessel.
- [SESSION-LIFECYCLE READ-POLICY + LOCK-CASCADE + PHASED-SESSION model + WORT-CONTRACT](maltyweb-pm-memory/session-model-arc.md) — read when building/touching ANY downstream consumer of session-grouped events (fiscal reads filter `status='closed'`; `finalize_session($id)` cascade; per-section save = ONE transaction; abandoned-session policy; poll-15s live shells), ANY form restructured into start→in_progress→end→closed sessions, the session live dashboard, the UI STYLE CONSTRAINT (light-kraft palette authoritative, vessel rendering reworked-from-scratch), or the WORT-CONTRACT `process_type ENUM('standard','wort_contract')` BINARY model (cooling-target = continuous param on brewday row, NOT enum branch; phantom-CCT suppression keys off process_type). Holds the C1-C8 re-homing map + the sequenced board arc (ATOM 16 ∥ TS-FIX → ATOM MAT → pilots).
- [RM-retro (Dec reconcile + Jan/Feb/Mar) prereq chain + gate state](maltyweb-pm-memory/rm-retro-prereq-chain.md) — read when reasoning about the RM retro, Dec-2025 stocktake reconciliation, the packaging-consumption gap, the Stage-2 eshop/cage-draw matrix, the Shopify pickup-flag dependency, production-SKU depletion, or "what unblocks the retro". GATED ON commissioning complete; TWO goals + TWO prereqs (Stage-2 matrix + production-SKU depletion) BOTH OPEN.
- [Saisie Transferts + yeast-family defaults](maltyweb-pm-memory/saisie-transferts-and-yeast-family.md) — read when touching the racking/Transferts form (label-only rename), the Recettes/Biochimie surface, `ref_yeast_strains` (strain `type` vs `family`), `ref_yeast_family_defaults`, `ref_recipes` yeast-strain FK + spec overrides, the garde-min transfer gate, or any state-gated lagering-threshold picker. The CIP-convergence design + the superseded transfert-interrompu ruling are now sub-files — see the §CIP CONVERGENCE / §TRANSFERT INTERROMPU pointers inside.
- [Yeast-strain taxonomy + Biochimie SDC — AS-BUILT post mig 228/229](maltyweb-pm-memory/yeast-strain-taxonomy.md) — read when touching yeast strains/families, the SDC Biochimie card, the racking/fermenting garde-min gate, or mig 228/229. ⚠️ FAMILY IS AN ENUM (7 vals), NOT a lookup table — `ref_yeast_families` DOES NOT EXIST; garde JOINs match on the family STRING. Holds the Pinnacle merge (mig 228), the ENUM-extend (mig 229), and the `yeasts.php` hard-DELETE scrapping candidate.
- [Recipe lifecycle + SDC Recettes 3-list + EPH vintage collapse](maltyweb-pm-memory/recipe-lifecycle-and-sdc-list.md) — read when touching SDC Recettes lists, `v_recipe_lifecycle` (FULLY DERIVED, migs 219-222), recipe vintage collapse, the recipe-resolver, or the EPH oldest-vintage split-brain. **✅ mig 263 EPH upstream-event consolidation SHIPPED+PRODUCTION-PROVEN 2026-06-04** (repointed event FKs+op_sessions oldest→operative; the ratification rulings #1/#2 + the hash-scheme-coexist discovery are here).
- [Recipe-ingredients drift surface](maltyweb-pm-memory/recipe-ingredients-drift-surface.md) — read when wiring SDC Recettes ingredients (R1 data-wiring / R2 drift view / R3 editable official), the fermenting-selector + hop-stage scoping, recipe ALIASING + PRG-restore rulings, the CONSOLIDATED 4-ITEM PLAN, or the observed-vs-official cost-safety gate (editing official is cost-AFFECTING for minerals/process-aids/finings/yeast-vit via l/ml gap-fill).
- [Racking REDESIGN WS1-4 (losses / clean-state / multi-lot blend / CIP cadence)](maltyweb-pm-memory/racking-redesign-ws1-4.md) — read when building the operator racking redesign: WS1 Pertes section, WS2 event-sourced BBT clean/dirty, WS3 destination auto-blend + multi-lot blend_info, WS4 BBT-CIP cadence. ⚠️ C-CHAIN re-homed into the racking session (zero data-model rework); WS1-4 rulings STAND. C1 SIM-FIX SHIPPED → tank-simulator-occupancy.md.
- [TankSimulator occupancy fix — drain guard + packaging under-drain / CCT9 / start_time-swap (SHIPPED+LIVE 2026-06-04)](maltyweb-pm-memory/tank-simulator-occupancy.md) — read when touching `app/tank-simulator.php`, the CCT/BBT occupancy replay, the loaders/`isTrueBlend`, or porting the lesson to Node `parse-tank-simulation.ts`. Dual-keyed `(recipe_id_fk,batch)` drain; ZERO data mutation; FROZEN return shape. 3 OPEN follow-ups (start_time swap, Node port COGS-impacting, the DIV/45 packaging which is the normalizer defect below). sql skill #25/#27.
- [Packaging parallel -4/-B run normalizer defect — BLESSED FIX (surgical 4-row mig + code-fix) + v2⇄old-form RECONCILIATION AUDIT](maltyweb-pm-memory/packaging-parallel-run-normalizer-defect.md) — read when touching `scripts/normalize-rawdb.py` `process_packaging_data` (Mode A vs B), `bd_packaging_v2` volume/client correctness, the `PackagingForm` old-Google-Form parallel stream, or any tank-drain consumer. Exactly 4 mode_a rows mis-recipe'd (2227 b45→DIVB, 2158 b169→STIB, 2119 b63→SPYB, 2086 b5→DIBB); v1 `second_packaging`=ground truth; NO re-normalize (RawDB frozen). Net Neb HL unchanged. COGS/tax-impacting. **§v2⇄old-form audit (2026-06-10, NO writes): CUTOVER=submitted_at 2026-05-22 16:38:43 (source_sheet_row_index NULL=web/parallel); migration CLEAN (1336/1342 matched, 6 unmatched all prod=0 zero-prod skips); 1 live discrepancy = batch-211 cuv CLIENT mis-attribution v2 id=6739 client_fk 2(Jetée)→should be 19(Blues Rules), volume/tax-NEUTRAL, per-client COGS/recharge only — FIX HELD pending operator confirm, refuse-don't-guess. ROADMAP FLAGS: (1) old Google Form `PackagingForm` STILL filled post-cutover in parallel, NOT ingested → retire-or-parity-check; (2) dedup key can't represent 2 byte-identical same-day events.**
- [Racking / Transferts KPI dashboard section + QC-threshold ruling](maltyweb-pm-memory/racking-kpi-dashboard.md) — read when building/touching racking KPIs or any aggregate over `bd_racking_v2`, the round-3 transferts deltas, OR per-beer FormFramework QC thresholds for ANY form (§QC-THRESHOLDS — metric governance vol=history/co2=operator-spec/o2+pressure=global; `app/qc-thresholds.php` resolver; NOT YET BUILT). KPI section LANDED `c38cea1`; CO₂-correction + re-ingest-gate detail here too.
- [CIP MODULE — reusable cross-form spec](maltyweb-pm-memory/cip-module.md) — read whenever a build touches CIP on ANY form (racking/brewing/packaging live; future forms extend the SAME contract). Holds `bd_cip_events` DDL, `ref_cip_types`, `app/cip-events.php` writer + UI partial, per-form processes, migs 175/176/177/230, the PACKAGING CIP FIX (filler+KZE pair, mig 230), and §FERMENTING = NO-CIP.
- [Phase D — COGS/COP cost-source swap (CORE COMPLETE)](maltyweb-pm-memory/phase-d-cost-source-swap.md) — read when touching Phase D, `v_mi_cost`/`v_mi_cost_gap`/`ref_fx`, the COGS/COP cost source, the `loadMI({mysqlCost:true})` overlay, `build-sku-bom.js` cost, or any BSF `MI.price`→MySQL question. Holds the cost-resolver model (wac>catalog>no_basis; `price IS NULL`≠`price=0`), the 32-row gap, the 8-item FOLLOW-UP BACKLOG (BSF-exit legs, RULE-1 scraps, no_basis cans), the dormant mysql-inventory.ts flag.
- [Billing-format / WAC unit-normalization model](maltyweb-pm-memory/billing-format-and-wac-unit-model.md) — read when touching `v_mi_wac`/WAC, `inv_deliveries` unit semantics, `ref_mi_invoicing_units`, pack-size normalization at ingest, catalog-price reliability, any qty×price cost path, OR the brewing-saisie ingredient unit-default (§THREE UNIT COLUMNS — `input_unit`/`pricing_unit`/`consumption_unit`). Holds the canonical-unit contract, the de-drift RULE, the strict WAC audit verdict, catalog-hygiene findings, the total/qty effective-price basis decision, remediation buckets A-E.
- [SKU-by-SKU BOM validation walkthrough — framework + canonical RULES (KEYSTONE correctness arc)](maltyweb-pm-memory/sku-bom-validation-walkthrough.md) — read when walking SKUs to validate liquid+packaging+combination BOM with the operator, settling/building the LIQUID-COMPOSITION model + liquid compiler, the 3 scotch/sticker BUCKETS, the eshop-scotch altitude, the intrinsic-BOM-vs-fulfillment-layer split, the DIRECT-SALES STOCK CONSUMPTION TRACKER (= Stage-2 matrix), the 17 missing-liquid SKUs, or the BOM-Review additions. Holds RULE #1 (liquid: malt/hops OBSERVED-ONLY [absent recipe line = drift KPI]; ref_recipe gap-fills non-malt/hops only; basis = VOLUME-WEIGHTED TRAILING avg — DECIDED, F2 unblocked), RULE #2 (3 buckets, exhaustive), **RULE #3 (intrinsic BOM vs channel-dependent fulfillment layer — per-SKU BOM = intrinsic content ONLY; cage XOR box stock states; order-composition eshop pkg = tracker not BOM; 2026-06-07), RULE #4 (draft keg-share = pour_HL/0.2 × collar+safe, channel-exclusive w/ -F, FOLD NOW)**, the 7-item DETERMINISTIC FIX-SET (bottle WAC 0.1466 / TEA line / sticker-qty→1+3-way branding / scotch tape 0.00093 / B12 eshop-scotch / keg-share / F2-needs-WAC), the SEQUENCING RULING (walk-all-then-batch; WAC-switch GATES F2 apply; tracker LAST), the TRACKER design (cage-pull-w/-B-fallback + order-consolidation packing + inv_consumption write), the resolver altitude map, the persistence split. Worked example = Alternative (recipe 6). PACKAGING walk live; liquid Phase C gated on WAC-switch→F2 apply.
- [Contract BOM — Chien Bleu + the contract-SKU/recharge encoding pattern](maltyweb-pm-memory/contract-bom-chien-bleu.md) — read when costing a CONTRACT customer's SKUs (Chien Bleu/BLZ/etc.), the per-material material-responsibility split, contract-COGS/recharge routing, bulk-wort (Moût Chaud) sale, ART-* generic BC sku_code resolution, or whenever a sold contract line "must be computed". Holds the May-2026 Chien Bleu footprint, the real MIs (box 163 PKG_BOX_24_BTL_BRUN, cork 93), the verified live-DB facts (ref_sku_aliases EXISTS but BC resolver ignores it; contract liquid IS F2-derivable for r20/21; r22 Moût Chaud not parsed), the SKU-resolution ruling (mint canonical + alias + patch resolver), the cork-deferral RQ pattern, and the 7-step build sequence. ⚠️ OPEN TODO recorded inside: add cork id 93 to JASPB/BAMB from next bottle run.
- [SKU decomposition tree](maltyweb-pm-memory/sku-decomposition-tree.md) — read when touching composite/multipack/white-label/alias SKUs, the THIRD decomposition axis (per-recipe HL share → ingredient attribution), `ref_sku_composite_slots`/`ref_sku_aliases`/`ref_sku_collab_temporal`/`ref_sku_packaging_choices`, or FR*/BLA4/COLLAB/PACKDEC. COMPOSITE STEP-③ COMPILER-FOLD = DONE + LIVE (composite_liquid=204/composite_packaging=7; 1000× unit-fidelity bug fixed; dead `sku-compiler.php` removed). "CAPACITÉS FORMAT→SKU" surface ALREADY BUILT (`sdc_gated_format_ids()`); #44 ESSENTIALLY CLOSED (migs 185/186/191). → packaging-bom-model.md §FORMAT→SKU RESIDUAL-GAP TRIAGE.
- [Derivation tree, SQL schema, cost model](maltyweb-pm-memory/derivation-tree-and-schema.md) — read when reasoning about the Le Zeppelin SOT chain, which canonical table owns a fact, FK/migration discipline, COP/COGS, the FULL per-migration ledger, the V2-ONLY / CANONICAL-v1-v2-RawDB / EVENT-DATE rules, the MI MERGE/TOMBSTONE-AND-RELINK pattern + uniq_row_hash-after-repoint GOTCHA (§SQL BUILD SCHEMA, since mig 254), or the `is_inventoried` semantic-flag rule (since mig 256).
- [Ingestion validation gate + SIE/SIL energy web-path (arc, SHIPPED+LIVE 2026-06-08)](maltyweb-pm-memory/ingestion-validation-gate-arc.md) — read when touching ingest-one-local.ts, upload-document/status/retry.php, triage-upload.js, invoice-validate.php, app/recap-lines.php, doc_invoices/doc_invoice_lines, inv_energydata, the validate-before-write gate, the TRIAGE "Lignes non résolues" CONVERGENCE (queued — recap-lines.php already IS the structured-parser-view surface; doc_invoice_lines.mi_id_fk is already best-candidate+name_confidence semantics, no new col), or Drive-inbox retirement. Holds the no-more-BSF directive, the staging-already-exists discovery, rulings R1-R4, batch-asks, EQUIP lines, the §TRIAGE CONVERGENCE consult.
- [Fulfilment / Expéditions arc (Le Cockpit christening, LIVE for the team 2026-06-09)](maltyweb-pm-memory/fulfilment-expeditions-arc.md) — **CURRENT MIGRATION HEAD = 329 applied (Shopify fulfilment 321/322/323; seasonal-burn 325; historical-Commandes views 326; weekly-client view 329) — always verify via `migrate.php --status`; parallel sessions live.** **✅ FORWARD SEASONAL BURN ENGINE (semaines-restantes) SHIPPED + LIVE on `?view=stock` 2026-06-11 (mig 325 `kpi_sku_seasonal_index` APPLIED — table + composite `idx_sl_sku_posting` + 11 `system_settings` section='stock' burn_* params; 795 cache rows / 15 families). Single burn path: `app/seasonal-burn.php` (8-fn lib) writes S via `scripts/seasonal-index-cli.php` (`--apply` ran once, cache 11:33); `fg-stock.php` Step 8/9 computes L+forward-sim at render; `expeditions.php`/`.css` triad. Burn=−SUM(qty_signed) ALL doc_types floored ≥0/wk, deny `customs_artifact` ONLY, family=`ref_skus.recipe_id` FK JOIN, NULL-sku excluded+counted, divergence from `v_sales_ledger_order_lines` documented in code. Opus-verified: global peak wk26, ZEPF L=297 vs naive 349, sim 40% shorter into June peak, 53 rows/110ms/0 errors. 🔴 OPEN: cron NOT enabled (in `db/cron/maltytask-seasonal-index.cron`, not in /etc/cron.d — PM recs enable now); authed visual smoke pending; ALL UNCOMMITTED (net-new+fg-stock.php mine via PATHSPEC; expeditions.php/.css entangled w/ parallel session, leave to owner). Pre-existing parallel `Undefined array key "needs_review"` at expeditions.php:2318 = Carnet/CRM not this build. FAST-FOLLOW: Croston's (ADI>1.32), lead-time bands (needs per-family L_repl), overstock ceiling, cron freshness chip. Trigger "semaines restantes"/"burn"/"rythme de base"/"seasonal index"/"kpi_sku_seasonal_index"/"seasonal-burn"/"rythme"/"provisoire chip"/"forecast stock"/"prévision stock" → READ §SEMAINES-RESTANTES / FORWARD SEASONAL BURN ENGINE in this file.** **✅ HISTORIQUE TAB (per-week-per-client historical sales) SHIPPED + LIVE 2026-06-11 (mig 329 `3646e4f` NOT pushed; surgical 5-file scp deploy, authed smoke PASSED KW23=364.80 HL/44 clients/AlloBoissons drill 8 SKUs). 4th read-only VIEW `v_sales_ledger_weekly_client` (6,292 client-week rows, grain (YEARWEEK mode-3, customer), SAME WHERE as the 3 data-lane views) + 8th Expéditions UI view `?view=historique` (week-blocks→client rows HL-desc→per-SKU drill off `v_sales_ledger_order_lines`; read-only, NO chips, NO CHF; new `expeditions-historique.js` drill-collapse). 🔑 GOTCHA: `YEARWEEK(d,3)` vs STR_TO_DATE-derived week_start DISAGREE at some boundaries → filter/join on the INTEGER `iso_yearweek` bucket, NEVER `week_start`; any future consumer of these views MUST key on iso_yearweek. COMPLETES the historical-sales endpoint (only the per-week-per-client AGGREGATE was requested+built; the per-DELIVERY cards remain optional/unbuilt). → §HISTORIQUE TAB.** **✅ HISTORICAL-COMMANDES DATA LANE SHIPPED + LIVE 2026-06-11 (mig 326 `78c231e` NOT pushed; 3 read-only VIEWs over `inv_sales_ledger` — `v_sales_ledger_orders` 7,497 / `_order_lines` 23,529 / `_unresolved` 222·549,387u; read-contract `docs/historical-commandes-read-contract.md`; ZERO `expeditions.php` edits = no UI-overhaul collision). GRAIN: order CARDS = `doc_type='shipment'` DISPLAY grain; `allnet` stays the AUDIT-total basis — display ≠ audit. B2B-scope (`sale_class NOT IN eshop/taproom/customs_artifact/transfer/sample`, eshop/taproom keep their auto-rows, no double-count); cutover 2026-06-08 (forward ≥ / historical <). Year profile 2021 858/2022 1294/2023 1123/2024 1435/2025 1835/2026p 952. OPEN: (1) 214/222 unresolved-SKU review CSV (Kouros, self-heals); (2) COGS-cutover coupling `489ab70` = allnet for qty / invoice lines for CHF; (3) per-DELIVERY card browse still optional/unbuilt (per-week-per-client now wired via Historique). → §HISTORICAL-COMMANDES DATA LANE.** **✅ SHOPIFY⇄maltyweb FULFILMENT INTEGRATION — FULL BUILD COMPLETE 2026-06-11 (Phase 0+1+2A LIVE; 2B DISARMED+double-gated on Shopify write-scopes; 3c is_non_stock NO-OP infra).** (Pickup signal = `shipping_lines[].title`; ingest cron on LOCAL box NOT VPS = SPOF; plan `~/.claude/plans/sleepy-snuggling-brooks.md`.) **✅ Σcards==Σphysique INVARIANT NOW HELD — closed by TWO 2026-06-12 fixes: (1) scope-gate `d9f7976` (3 depletion legs gated `stocktake_scope!='none'`, 13 scope='none' phantoms excluded); (2) ANCHOR TIEBREAK FIX SHIPPED+VERIFIED on prod — `fg_stock_compute()`+`fg_stock_location_snapshot()` anchor = `MAX(counted_at)` tiebreak `MAX(id)` (was bare `MAX(id)` → stale older-dated count inflated anchor). SINGLE-FILE targeted rsync `/tmp/fgstock-hotfix.php`→`app/fg-stock.php` (NOT bin/deploy.sh — dirty cage/repack tree); DOAF 83→45, ZEPF 2502→1256, 26/26 Production SKUs corrected, Σ=8892==8892. ⚠️ LOCAL≠DEPLOYED: local fg-stock.php has the repack `if(repack_depletion_live())` gate TANGLED in (deployed runs repack UNGATED) — do NOT revert the local anchor edit; gated version ships idempotently when repack owner deploys (needs untracked `app/repack.php`). Rollback `/tmp/deployed-fgstock.php`.** 🔴 OPEN (gated, BEFORE any `cogs-monthly-compile --apply --month 2026-06`): WEEKLY-vs-MONTH-END DISCRIMINATOR — mig 338 `inv_fg_stocktake.count_type ENUM('operational','month_end')` + month-end toggle on saisie census form + backfill (May 34→month_end, June weeklies→operational) + COGS query `WHERE month_closed=? AND count_type='month_end'` latest-per-(sku,loc)-then-sum; GATE = May-fiche FG=47 701.56 reproduces TO THE CENT. NOT started. Trigger "Shopify"/"pickup"/"click&collect"/"retrait"/"shipping_lines"/"fulfilment_mode"/"eshop picking"/"push-shopify"/"write-back"/"ref_shipping_methods"/"inv_sales_fulfilment"/"ingest-shopify"/"Σcards" → READ §FULL SHOPIFY⇄MALTYWEB FULFILMENT BUILD COMPLETE + §SHOPIFY ⇄ maltyweb FULFILMENT INTEGRATION in this file first. read when touching `ord_orders`/`ord_order_lines`/`ord_order_status_events`, **`inv_stock_movements`** (inter-site transfers, mig 299), **`inv_side_stock_ledger`** (packaging loose-unit remainder / giveaways, mig 303 — UNIT-COUNTS NOT COGS, distinct from sales-side `sale_class='giveaway'` migs 300/301), `ref_customers`/`ref_transporters`, **`app/fulfilment-site.php` (`resolve_fulfilment_site()` = THE single ship-from resolver)**, `expeditions.php` (`?view=stock|mouvements|commandes|side-stock`)/`api/expeditions-status.php`, `form-packaging.php` (side-stock accrual writer + complete-a-box draw), `app/fg-stock.php` (`fg_stock_compute()` + `fg_stock_location_snapshot()` per-site sales attribution), `reglages-generaux.php ?sec=delivery_sites` (Nausikraft client list), the WeeklyOrders importer, customer bootstrap, or commandes@ email ingestion. Holds mig-279/299/303 schema, the resolver precedence + landmines, the depletion-gap-by-design, go-live record, ITEM 1 SHIPPED + per-site-cutoff remainder, **ITEM 2 (mini side-stock/giveaways) SHIPPED 2026-06-10 (`1c95e23`) — OPEN downstream = year-end-settle cron (documented-not-built; beer-tax JOIN shape in mig 303)**, ITEM 3 parked (Phase-2 cage/COGS, externally blocked), and the R1-R7 risk flags. **Triggers: "side-stock"/"side stock"/"mini side-stock"/"giveaway"/"giveaways"/"remainder"/"loose units"/"year-end offered"/"beer offered" → READ this file.**
- [Sales / commercial surface (Le Cockpit)](maltyweb-pm-memory/sales-commercial-surface.md) — read when touching the SALES side: Shopify orders ingest, BC monthly sales, `derive-eshop-consumption.ts`, sales-driven COGS, the eshop order→stock model, `ref_clients`, or any commercial question. 3 canonical landing tables; two-source model (Shopify + BC); Stage-2 still planned; the 🔴 Sales BSF-exit leg.
- [Computation-layer architecture](maltyweb-pm-memory/computation-layer-architecture.md) — read when building/changing any computed surface (KPIs, dynamic RM, eshop matrix, live tank volumes, COGS/COP/WAC, SKU/BOM, rollup matrices); the on-write+scheduled rule, per-surface decision rule, the TWO live landmines, Phase 0→3 path.
- [Consumption / COGS / COP derivation wiring — v1 vs v2, module-by-module (VERIFIED LIVE 2026-06-08)](maltyweb-pm-memory/consumption-cogs-derivation-wiring.md) — read when reasoning about which production version (v1/v2) a computational module reads, why a month's `inv_consumption` is under-populated, the `inv_consumption` writer chain, the COGS/COP engine location, the May-close gating, or "migrate the computational modules onto maltyweb". Holds: the per-script source-table table (all `derive-*-consumption.ts`+`parse-tank-simulation.ts` already read v2; none on maltyweb; none cron'd); THE REAL GAP = `bd_brewing_ingredients_parsed_v2` FROZEN at 2024-06-17 (brewing lines un-refreshable from v2; live lines in v1 `bd_brewing_ingredients_parsed`); COGS/COP engine NOT on maltyweb (`cop_monthly`/`cogs_monthly` = 0 rows, still Node→BSF); the revised May-close gating list; the full migration sequence. 🔴 **COGS INVENTORY-VARIATION FICHE = SEPARATE table family, NOT `cogs_monthly` (verified live 2026-06-11, mig HEAD 330).** Canonical fiche tables (mig 330, both `policy=allowed`): `ref_cogs_fiche_categories` (12 keys Malt/Hops/Ingredients/Yeast/Cartons/Inliner/Capsules/Verres/Bouteilles/Etiquettes/Canettes/CapsFuts, each w/ `inv_gl`+`charge_gl` from EXT CFO-source, `is_brewing` flag) + `cogs_fiche_opening_anchor` (April-2026 opening ALREADY SEEDED, 12 `category_key` rows, Σ=431 354 CHF — the only externally-anchored month). Fiche keys on **`category_key`**, NOT `source ENUM(rm/fg/wip)`. `cogs_monthly` is OWNED by `build-cogs-report.js` (Node sales-driven COGS, `policy=blocked_with_redirect`, read by `app/kpi-handlers.php`) — DO NOT hijack/ALTER it for the fiche; that's a writer-collision parallel-store smell. CUTOVER = TS `cogs-monthly-compile.ts` reads anchor → writes a NEW `cogs_fiche_monthly` `(month_key,category_key)` (NOT yet built; needs mig + schema_meta writer=cogs-monthly-compile.ts). Provenance is STRUCTURAL (anchor table = seed; computed table = compute) — no provenance enum needed. Basis-change reconciling line: disclose, don't fabricate (FG/F2 delta ≈+1809 clean; RM/yeast g→kg portion "non isolable — base héritée", never a computed number).
- [Volume dimension](maltyweb-pm-memory/volume-dimension.md) — read when touching SKU/BOM volume, volume KPIs/trackers, tank/packaging HL, `ref_skus.hl_per_unit`, the cuv/liner model, the `-X` CAGE volume (mig 147), the bot/can/can33 volume_hl backfill, or `bd_packaging_v2.vendable_hl` (canonical formula + units_per_pack + inactive-SKU caveat). Cardinal: additive-cost-vs-OWNED-volume.
- [Packaging BOM model](maltyweb-pm-memory/packaging-bom-model.md) — read when touching the packaging BOM, slots, scotch either/or, cuv serving-tank gate, the recompute service, the `-4` 3-layer carton (mig 145), the BLO/ZEP carton-MI gap, the `-X` reusable CAGE (mig 147), the multipack→`-X` stock-pull-by-channel COGS/COP rule, the ZEP6C liquid-only fact + compiler boxing-capacity GATING-GAP, the THREE SKU IDENTITY LAYERS, or §FORMAT→SKU RESIDUAL-GAP TRIAGE (#44).
- [Packaging losses & dispositions convention (KEYSTONE)](maltyweb-pm-memory/packaging-losses-convention.md) — operator-confirmed SINGLE source; read when touching form-packaging.php losses/vendable/dispositions, bd_packaging_v2 loss/QA/keg cols, beer-tax base, general-FG vol, packaged-HL→COGS, the in-tank/in-filling CO₂/O₂ model, or §PERTES-DASHBOARD. The DATA-DRIVEN LOSS TYPES arc + 4-operator-form-changes + the white-label atom-C history are now sub-files (see the pointers inside): [loss-types-and-form-changes-arc.md](maltyweb-pm-memory/packaging-losses-convention/loss-types-and-form-changes-arc.md) + [white-label-atom-c-arc.md](maltyweb-pm-memory/packaging-losses-convention/white-label-atom-c-arc.md).
- [Packaging page dashboard](maltyweb-pm-memory/packaging-page-dashboard.md) — read when building/touching the packaging KPI dashboard or the §PARALLEL-RUN FIX detail; data-source map, KPI→query derivations, atom sequence, divergence flags.
- [Build history (directory)](maltyweb-pm-memory/build-history/README.md) — read when you need the Stage-1 chronology, the full per-session build-changelog (split by period), the commit chain, resolved divergences, the interrupted-migration lesson, the page inventory, the §8.9 closure backlog, OR the verbatim pre-2026-06-05 index snapshot (full inline arc narration).
- [Scrapping / retirement backlog](maltyweb-pm-memory/scrapping-backlog.md) — read on EVERY build (RULE 1) or when consulted about retiring a surface; the candidate register with gates.
- [Skills inventory + build tooling](maltyweb-pm-memory/skills-and-build-tooling.md) — **ALWAYS read §SKILL-FIRING MATRIX when equipping/dispatching ANY agent** (work-type→skills table + the PM's 3 standing behaviors: EQUIP: line on every brief, default webapp-testing smoke on deployed pages, proactive mis-equip flagging); also read when planning the post-deploy smoke suite (`webapp-testing` = designated vehicle), starting the 2FA arc (php-security.md ready), heavy dataviz (dataviz.md + deuteranopia flag), SQL perf deep-dives (innodb/ layer, maltytask-conventions-win precedence), or vetting/installing any third-party skill (gap ledger + HELD list).
- [Conventions + house helpers](maltyweb-pm-memory/conventions-and-helpers.md) — read when dispatching/reviewing a build or needing PHP house-helper signatures; the full coding/architecture/UI convention set, maintenance protocol, PM charter, verified helpers + recette/SKU-UI live facts, §COMPUTE-ON-SUBMIT, §AUTH HEADLESS SCREENSHOTS, the STANDING UI/UX DIRECTIVE (site-wide light-kraft palette, single global `:root` token block).

## MAINTENANCE NOTE
Keep THIS index lean — current build-state, RESUME POINT, the CHECKLIST, the SHIPPED-ARCS one-line register, the THEMATIC-FILES pointers. When a section here grows past ~5 lines or becomes historical, COMPILE IT OUT into the relevant thematic file (create a `<topic>/` subdirectory if the file itself crosses ~80KB) and leave a one-line load-triggered pointer. Bump "Last updated" on every change. Verify DB-claims read-only before recording. (Agent note: `maltyweb-pm` registers as a subagent only after a Claude Code restart / `/agents` reload; until then, agents read these files directly.)

**Build-changelog discipline:** the rolling per-session build-changelog (dated narration of LANDED commits/migrations) lives ONLY in [build-history/](maltyweb-pm-memory/build-history/README.md) — APPEND each session's entry to the newest period file there, never to this index. When you record a new build: (1) prepend the dated narration to the build-history changelog; (2) add/refresh a ONE-LINE pointer in the §SHIPPED-ARCS REGISTER; (3) move any now-LANDED RESUME-POINT item out of RESUME POINT, keeping only what is still open/next; (4) update the §CURRENT BUILD-STATE one-glance + the header current-state sentence. The index is the map; the depth lives in the thematic files + build-history.
