# Email-order ingestion arc (`commandes@lanebuleuse.ch` → ord_orders → BC) — a.k.a. "COMMANDES READERS"

> Read when touching: `commandes@` mailbox ingestion, per-sender email-order parsers, `doc_email_messages`/email-order producer, the Phase-1 BC-orders link from email, the logistics mail↔BC validation surface, **"commandes readers"** / "lire les commandes" / "auto-read orders" / "prepare on BC". Companion: [fulfilment-expeditions-arc/README.md](fulfilment-expeditions-arc/README.md) (D1/D2 BC spine, ord_orders schema, fg_stock XOR), [sales-commercial-surface/README.md](sales-commercial-surface/README.md) (ref_customers, BC two-source). PM-SCOPED 2026-06-15. Kouros: "lance".
>
> **🟢🟢🟢 2026-06-19 — REP→INTERNAL-ACCOUNT SLICE IS LIVE + VERIFIED + COMMITTED (`5fecd85` maltyweb main).** The rep-account pre-fill shipped end-to-end. **Migration 418 `ref_internal_order_accounts`** (`sender_email VARCHAR UNIQUE → customer_id_fk` FK→ref_customers, is_active, notes) — applied **MANUALLY** (manual ALTER + manual `schema_migrations` INSERT, NOT migrate.php, because parallel **mig 411** is still pending-applied and migrate.php would sweep it); schema_meta classified reference/admin-managed. **6 SEEDED ROWS (verified live, each → the rep's OWN ACTIVE BC account, never an échantillon dup):** dorian@→**3050** (Fairhall/BC3947) · john@→**2377** (Penman/BC3625) · louis.cardis@→**895** (Cardis/BC2062) · nicolas.berger@→**1164** (Berger/BC2358) · nicolas.cerruela@→**2233** (Cerruela/BC3478) · tania@→**2379** (Schott/BC3629). **CORRECTIONS vs the held proposal:** (a) **Daniel Hersberger was NOT a rep → DROPPED** (the proposed 2400/BC3650 is gone); (b) Nicolas Cerruela added (id **2233**); (c) the note "3478" was a BC number, NOT a ref_customers id — Cerruela's ref_customers.id is **2233**.
> **Parser side (`ingest_email_orders.py`):** injects `_internal_rep` + `_rep_email` into `parsed_json` via an `extra_hints` serialisation path, **ONLY when `customer_hint=='' AND the bare sender email ends with @lanebuleuse.ch`** (forwarded-customer + external From both take precedence → no internal-account dumping). 🔴 **BUG CAUGHT + FIXED IN VERIFICATION (durable):** the first cut tested `from_address.endswith('@lanebuleuse.ch')` on the RAW header — which is `"Name <email>"` form → NEVER matched live (flagged=0). FIX = extract the BARE email FIRST, then test the suffix. After fix: **21 flagged, 0 coexisting with a resolved customer** (precedence holds — flag fires only on the empty-hint internal path).
> **Resolver side (`email-orders.php` + `email-orders.css`):** on `_internal_rep`, a PARAMETERIZED SELECT against `ref_internal_order_accounts JOIN ref_customers` PRE-FILLS the customer typeahead (operator-overridable; htmlspecialchars output). php -l gated before deploy. **customer_hint stays a string→FK-at-validation — the pre-fill is a hint, the operator confirms (never-guess held).**
>
> **🔴🔴 NEW DISCOVERY — TOP OF NEXT-SLICE BACKLOG: the "via Commandes" Google-Groups From-rewrite.** External customers who email the `commandes@` distribution list have their `From` REWRITTEN by Google Groups to `commandes@lanebuleuse.ch` (display name kept, e.g. `"'Ana Rocha' via Commandes"`). CONSEQUENCES: **(1)** the real external customer is LOST — `customer_hint=''` because From is the LIST address (the real sender sits in **`Reply-To` / `X-Original-From` / `X-Original-Sender`**, which the parser doesn't read); **(2)** these get MIS-FLAGGED `_internal_rep` with `_rep_email=commandes@` — HARMLESS for pre-fill (`commandes@` isn't in `ref_internal_order_accounts` → no wrong account binds) but INFLATES the flag count (part of the 21) and BURIES real customers in the review queue. **FIX (next slice, top priority): for list-rewritten messages, resolve the ORIGINAL SENDER from `Reply-To` / `X-Original-From` / `X-Original-Sender` BEFORE the customer cascade** — this recovers a chunk of the currently customer-less parsed orders (Ana Rocha, Nausikraft via Commandes, Tip Top Drinks, …) AND removes the mis-flag. **Also add a guard so system addresses `commandes@` / `production@` NEVER trigger `_internal_rep`** (only real rep mailboxes in the mapping should). This is a HINT-only/customer-recovery fix — still no auto-FK.
>
> **🟢🟢🟢 2026-06-19 — ATTACHMENT-ORDER PARSING SLICE IS LIVE + VERIFIED + COMMITTED (`c31b540` maltyweb main).** The first attachment slice shipped: XLSX (Migros) + PDF (Bevanar) order parsers, fed by connector byte-fetch in BOTH the RFC822 and Gmail paths, registered before `GenericVocabParser`. ZERO new pip deps (XLSX→`openpyxl` already installed; PDF→`pdftotext -layout` shell-out). **What landed (`c31b540`):** (1) `attachment_pdf.py` = `BevanarPdfParser` (`pdftotext -layout`, reconcile gate ≤0.10 CHF line-sum vs stated total); (2) `attachment_xlsx.py` = `MigrosFroidevilleXlsxParser` (openpyxl, deterministic header/grid detection, reconcile gate); (3) connector now FETCHES attachment bytes (closing the meta-only gap that was step 0 of this slice). Prior commit `c31b540`'s predecessor `8f1b397` = the text-core generic_vocab hardening (already recorded). **LIVE `--apply --force` re-parse of the 30-day to:commandes@ window: 34 parsed / 66 no_match out of 100.** Parser breakdown: generic_vocab 31, bevanar_pdf 2 (orders No 32503 + 32529), migros_froideville_xlsx 1 (13 lines, exact total). The 66 no_match = genuine non-orders + reply-only threads + senders without a parser yet (L1 candidates).
> **🔴🔴 KEY BUG + DURABLE LESSON (record for ALL future attachment work):** the first attachment build passed unit tests against standalone .pdf files + the Migros .eml fixture but FAILED LIVE — Bevanar order PDFs arrive as `application/octet-stream` with a `.pdf` filename and **NO `Content-Disposition: attachment` header**, so the connector's disposition-ONLY attachment walk silently dropped them (`attachments_json=[]` → no_match). Migros's XLSX masked the bug because it DID carry the disposition header. FIX = a `_is_attachment_part()` helper that treats a part as an attachment when `get_filename()` is truthy (and content-type isn't text/multipart) OR `disposition=attachment`. **LESSON: verify the REAL email's MIME tree THROUGH THE CONNECTOR, not just the parser against a clean attachment file** — a clean .pdf/.xlsx fixture hides how the part is wrapped in the actual envelope. Real Bevanar `.eml` fixtures were captured to lock this. (Same class as the earlier "verify the real email's envelope, not the parser against a clean file" forwarded-customer bug.)
> **🟢 VOCAB SEEDS NOW LIVE:** the two gaps below are SEEDED — Huguette→EPH2 (recipe 76), découverte/pack découverte→PD8 (sku 137). Vocab is now a 411-term live read.
> **🟢🟢🟢 2026-06-19 — TEXT-CORE PARSER-HARDENING SLICE IS LIVE + PM-VERIFIED. P0 was already PASSED.** The forwarded-envelope + qty-binding + Swiss-NPA-guard work (slice steps 1+2) SHIPPED to the VPS and is verified against real fixtures + live 411-term vocab. **What landed in `scripts/python/email_parsers/generic_vocab.py` (4 changes):** (1) `strip_quoted_history()` cuts the body at the earliest quote-intro marker (drops already-quoted order history → no duplicate parse); (2) `_resolve_customer_hint()` multi-signal cascade — inline client block → forwarded external sender → external From → '' — with `@lanebuleuse.ch` filtered at EVERY level (forwarder/internal never wins); (3) `_ADDRESS_LINE_RE` Swiss-NPA/street/phone/email guard that REMOVES address lines (never re-binds a postal code to a SKU); (4) `_QTY_X_RE` binds the `Nx` prefix to the product. **Live verification (fixtures mode against 9 real .eml + live VPS vocab):** Olivier Corthay order now resolves customer=`Corthay Olivier <olivier_corthay@hotmail.com>` (was the internal forwarder), 3 correct lines ZEPB/ALTB/SPYB qty 2 each, NO phantom "1936 Verbier" postal-code line, DEL-/rabais→notes; sample-02 (order fully quoted) correctly `no_match` (no dup). 4 parsed / 6 no_match across the 10 samples — the no_matches are the Migros XLSX (attachment slice), the Bevanar "3 fûts Huguette" thread, and genuine non-orders. **The four diagnostic bugs from the first live run (phantom postal-code line, dropped `Nx`-prefixed lines, forwarder-as-customer, quoted-history dup) are ALL FIXED.**
> **🟢 DEPLOY/STATUS (2026-06-19) — ATTACHMENT SLICE IS COMMITTED.** `c31b540` (attachment_pdf.py + attachment_xlsx.py + connector byte-fetch + registry) is on **maltyweb main, COMMITTED**; `8f1b397` (text-core generic_vocab hardening) likewise. **`--apply` IS NOW LIVE** — the connector writes `doc_email_messages`; parsed orders flow into the already-shipped `email-orders.php` review queue; no_match segregated into "Non parsé". Re-parse is a MANUAL `--force`; **NO cron yet** (operator enables, per house pattern). `doc_email_messages` no longer empty: 34 parsed / 66 no_match on the 30-day window.
> **🟢 VOCAB GAPS — RESOLVED (seeded as master-data aliases, as designed — NOT parser hardcodes):** **Huguette→EPH2 (recipe 76)** and **découverte / pack découverte→PD8 (sku 137)** are LIVE in the canonical vocab; `generic_vocab.load_vocab()` now picks them up (411 terms). The Bevanar "fûts Huguette" order parses via the PDF parser; découverte resolves to PD8.
> **🟢 INTERNAL SAMPLE-REP ORDERS — NOW PRE-FILLED (rep slice LIVE `5fecd85`).** A rep's own sample order (From a mapped `@lanebuleuse.ch` rep mailbox, customer_hint='') now carries `_internal_rep`+`_rep_email`, and `email-orders.php` PRE-FILLS the rep's active BC account in the customer picker (operator confirms). Still a HINT — never an auto-FK. See the §AS-BUILT for the 6 mappings.
> **🟢 REVISED NEXT-SLICE ORDER (text-core DONE; attachment-parsing DONE; rep-mapping DONE):** **(1) via-Commandes original-sender extraction [NEW, TOP PRIORITY — recovers lost external customers from `Reply-To`/`X-Original-From`/`X-Original-Sender` + removes the `commandes@` mis-flag]** → **(2) L1 per-sender templates [GATED on Kouros forwarding .eml per sender]** → **(3) cron enablement + validation-accept→BC SalesOrder push (D1 hand-off).** EQUIP: **parser-coder + sql** (+ `ui` for any resolver-side surfacing).
> **✅ REP→INTERNAL-ACCOUNT MAPPING — SHIPPED 2026-06-19 (`5fecd85`, mig 418).** See the §AS-BUILT block at the top of this file for the 6 verified mappings, the corrections (Hersberger dropped, Cerruela=2233 added, 3478=BC-not-id), the manual-apply rationale (parallel mig 411 pending), and the raw-header `.endswith` bug that was caught + fixed (flagged 0→21). The earlier held 6-row proposal listing Daniel Hersberger=2400/BC3650 is SUPERSEDED — Hersberger was not a rep.
> **🔴 L1 PER-SENDER TEMPLATES — GATED:** blocked on Kouros forwarding `.eml` per top recurring sender. Roster observed from the live no_match bucket: **Nausikraft, Tip Top Drinks, FC Renens, Cobra Traders, Villvert-via-Louis,** plus gmail/hotmail direct customers.
> **🔴 DIVERGENCE GUARDS for the forwarded-envelope work:** (a) customer_hint stays a STRING HINT — extracting the original sender from a forward chain MUST NOT auto-link a `ref_customers` FK; the human picks the FK at validation (revenue-bearing, never-guess). (b) Multi-address forward chains (A fwd B fwd C): pick the DEEPEST original sender (the innermost forwarded-from), NOT an intermediate forwarder, and NOT an internal @lanebuleuse.ch address — but if ambiguous (multiple external candidates, no clear forwarded-header block), emit customer_hint='' (or the raw chain as a note) → review, NEVER guess a customer. (c) The address/postal-code guard must REMOVE phantom lines, not silently RE-BIND them to a different SKU — a 4-digit token that fails the qty-context test drops OUT of the line grammar entirely (it is an address), it does not become a different quantity. (d) HINT-only invariant: the parser NEVER writes a customer_id_fk/sku_id_fk; all resolution is the operator's at the P3 surface (already enforced — keep it).
> **🟢 SCOPE EXPANDED 2026-06-19 (two Kouros decisions, PM-RATIFIED) — ATTACHMENT-ORDER PARSING + INTERNAL-REP→ACCOUNT MAPPING now IN-SCOPE.** The in-flight text-core coder (forwarded-envelope + qty-binding + NPA guard) is UNAFFECTED — these are the NEXT slice after it lands. **RATIFIED NEXT-SLICE ORDER:** text-core (in-flight) → **attachment-order parsing** (gate: connector must FETCH bytes first) ∥ **rep→internal-account mapping** (gate: Kouros's BC account id) → **L1 per-sender templates** (gate: real .eml per sender).
> - **🔴 GROUND-TRUTH CORRECTION — the 10 .eml samples DIVERGE from the brief (PM-verified the actual files at `/home/kluk/projects/maltyweb/data/email-order-samples/`):** (1) **NO PDF attachment exists in any of the 10 samples.** (2) **sample-10 (18 MB) = MIGROS, not Bevanar** — From `dave.pilloud@gmvd.migros.ch`, subj "commande Froideville", attachment = **`Nébuleuse_Froideville.xlsx` (13.5 MB XLSX)** + an inline signature PNG (122 KB); the 18 MB is base64 bloat of those, not a PDF. (3) **Bevanar "Commande No 32503" = sample-04/05, BODY/THREAD order with NO attachment** (From `ahauray@bevanar.ch` ↔ production@). So the attachment-order surface Kouros means is **XLSX-first (Migros), and PDF is anticipated-but-unsampled.** Build the attachment dispatcher to handle BOTH content-types from day one; the FIRST real attachment parser to write is the Migros XLSX (we HAVE the sample), PDF parser waits on a real PDF sample. (4) Internal-rep samples are samples-01/03/06/07 **Dorian Fairhall**, 08 **Megan Iannone**, 02 **Kouros** — all @lanebuleuse.ch "commande échantillon vendeur" / "client à créer".
> - **🔴 OCR/ATTACHMENT-FETCH LANDSCAPE (PM-verified live on VPS):** the connector's `.venv` has **NO Python PDF/OCR lib** (only `openpyxl==3.1.5`); system binaries **`pdftotext`/`pdftoppm`/`tesseract` ARE present** at `/usr/bin/`. The maltytask Node invoice OCR (`lib/ocr-core.js`) is just `execSync` shell-outs to `pdftoppm`+`tesseract` — **NOT reusable code (Node, wrong repo), but the TECHNIQUE ports trivially.** **CONNECTOR GAP (blocker): `ingest_email_orders.py` currently captures attachment METADATA ONLY** (`{filename, content_type, size_bytes:None, content:None}` in BOTH the RFC822 and Gmail-API walk paths) — it does NOT download attachment bytes. So step 0 of the attachment slice = teach the connector to FETCH bytes (Gmail API `messages.attachments.get` / RFC822 `part.get_payload(decode=True)`). **DECISIONS:** XLSX → `openpyxl` (already installed, ZERO new dep). PDF → `pdftotext` FIRST (digital PDFs have a text layer — most B2B order PDFs are digital, cheap, no OCR); fall back to `pdftoppm`+`tesseract` ONLY if `pdftotext` yields ~empty (scanned PDF). That fallback adds NO Python dep (shell-out), but does need `pdf2image`/`pytesseract` OR raw `subprocess` — PM lean = **raw `subprocess` shell-out (no new pip dep), mirroring `ocr-core.js`'s execSync pattern.** HOOK POINT: a new `email_parsers/` module per content-type (`attachment_xlsx.py`, `attachment_pdf.py`) fed by an attachment-extraction helper in the connector; same `parse()→ParsedOrder|None` contract, registered in the registry. KEEP Model B: attachment parse → `parsed_json` HINTS only, NO ord_orders, NO FK, NO LLM. EQUIP: **parser-coder + sql** (+ `xlsx` skill for the Migros XLSX shape).
> - **🔴 INTERNAL-REP → INTERNAL/SAMPLES ACCOUNT (decision 2) — modeled as a RULE at the resolver, NOT a parser guess.** When From is `@lanebuleuse.ch` AND no external client block resolves (rep places a sample/own order), route customer_hint to a **designated internal/samples BC account** — NOT ''→review, NOT the rep-as-customer. **MAPPING HOME = a small ref table `ref_internal_order_accounts` (sender_email VARCHAR UNIQUE → customer_id_fk INT→ref_customers.id, is_active, notes)** — NOT system_settings (FK integrity + per-rep granularity), NOT a users column (reps aren't all `users`). schema_meta class=reference/allowed. **RESOLUTION LAYER, not parse layer:** parser only SIGNALS "internal-rep order" via a flag in parsed_json (`_internal_rep: true` + `rep_email`) computed from From∈@lanebuleuse.ch AND no external-sender/forwarded-customer block; the PHP validation resolver reads the flag, looks up `ref_internal_order_accounts` by rep_email, and PRE-FILLS the customer picker with the mapped account (operator can still override). customer_hint stays a STRING at parse time → FK at validation, exactly as Model B mandates. **🔴 CRITICAL DIVERGENCE GUARD — the internal account must NOT become a dumping ground:** the rule fires ONLY when From is internal AND NO external customer was named (not forwarded-on-behalf-of an external client). A `@lanebuleuse.ch` From that IS a forward carrying a real external customer (the common forwarded-order case from the text-core slice) MUST resolve to the FORWARDED customer, never the internal account — forwarded-envelope detection (step 1 of the text-core slice) takes PRECEDENCE over the internal-rep rule. **DATA HAZARD (PM-verified live):** `ref_customers` already has a SPRAWL of fuzzy per-rep échantillon rows (4 near-dup "Dorian … échantillons" accounts: ids 2997/2899/2828/3017, plus John/Louis/Nico/Tania variants) — the mapping MUST point to the ONE canonical account Kouros designates, never auto-pick a fuzzy-named échantillon row. `ref_customers.sale_class` ENUM ALREADY carries `'sample'` + `'staff'` — the designated account should be `sale_class='sample'`. **DEL- price prefix / rabais -15% on these orders = OPERATOR-SET at validation, NOT a parser/BC-create field in v1** — ord_orders has no price-tier/discount column and v1 doesn't push price to BC (D1 pushes qty/SKU; pricing is BC-side off the customer's price group). Surface the rep's stated discount as a parsed_json NOTE; operator handles the tier. **Kouros still owes: the actual BC account identifier (`bc_customer_no` / ref_customers.id) for the internal/samples account.** EQUIP: **sql + coder** (+ ui for the resolver pre-fill on email-orders.php). MIG HEAD ≥408 — re-`--status` at build start; `ref_internal_order_accounts` = next-free mig.
>
> **🔑 BC GATE IS OPEN, not blocked.** The active-arc index line "BC API auto-pull gated on Entra admin-consent" is STALE for the WRITE path: Thierry granted Entra admin-consent (RE1, secret on VPS `config/bc.env`), and the D1 order-create + D2 credit-memo write spines are LIVE-PROVEN under `D365 BUS FULL ACCESS` (no fresh consent needed). "Prepare on BC" therefore does NOT block on Entra — it blocks on (a) the live mailbox (P0 — NOW DONE) and (b) the human-validation gate (built). See PRODUCER→D1 HAND-OFF below.

## ✅ AS-BUILT — P3 VALIDATION UI SHIPPED 2026-06-15 (commits `172fd95` + `6ef9df0` on origin; mig 372 applied) — ARC P3 = DONE
**MIG HEAD now 372 (next free = 373).** Model B realized **end-to-end**: parsed candidate → operator-validated → `ord_orders` row → email flipped, all atomic. P3 = ✅ DONE.
- **mig 372 `372_email_orders_page_access.sql` (APPLIED + VERIFIED LIVE).** New `ref_pages` row: page_key=`email-orders`, label "Commandes e-mail", icon 📧, href `/modules/email-orders.php`, domain=`logistics`, min_role=`operator`, is_active=1, sort=26, **`ref_pages.id=480`**. Access-preset grants to `manager` + `logistics_operator` ONLY (NOT production/marketing/sales) — honors the access-preset ship-step (a new ref_pages row is invisible until granted to a preset; idempotent INSERT IGNORE pattern). **RULE 3 satisfied — tour-steward dispatched for page 480.**
- **New standalone module (avoids `expeditions.php` file-contention — the deferred ⚠️ is now MOOT):** `public/modules/email-orders.php` + `public/css/email-orders.css` + `public/js/email-orders.js`, CSS/JS separated with `?v=filemtime` cache-bust, CSRF + PRG. **NO BC push** (D1 stays triple-gated), **NO COGS/fg_stock write.**
- **🔑 PROMOTION CONSOLIDATED INTO A SINGLE HOME (`6ef9df0`):** the promotion transaction was extracted into **`app/email-order-promote.php`** (now tracked) — `email_order_promote(pdo, me, emailId, customerId, requestedDate, lines, comment, allowTwin=false): int`. The module DELEGATES to it (the prior inline ord_orders/lines/status-event INSERT + parse_status flip was REMOVED from the module). The helper carries: FOR UPDATE row-lock on the email row; `source_ref` idempotency pre-check + dup-key (1062 on `uniq_ord_source_ref`) translation to `EmailOrderAlreadyPromotedException`; typed exception hierarchy (NoCustomer / InvalidLine / NotParsed / AlreadyPromoted / Twin); `log_revision` on every write; one atomic txn.
- **Model B end-to-end (the validation action IS the producer):** parsed candidate (`doc_email_messages.parsed_json`) → operator resolves customer + SKU + date (pickers PRE-FILLED from hints but human-picked; **server whitelists every FK against active `ref_customers`/`ref_skus`**; refuse-don't-NULL blocks save) → `email_order_promote()` creates **`ord_orders`** (`order_type='customer'`, `source='email'`, `source_email_id_fk` back-link, `source_ref='email:<message_id>'` UNIQUE idempotency, `review_status='accepted'`, `status='entered'`) + **`ord_order_lines`** (`sku_id_fk`, qty>0) + **`ord_order_status_events('entered')`** + flips `doc_email_messages.parse_status='order_created'` — ALL ATOMIC.
- **🔑 TWIN-CHECK (XOR no-double-deplete) — AS-BUILT VERIFIED COLUMNS:** `inv_sales_orders.source='shopify'` AND `fulfilment_mode='pickup'` AND `customer_email=<ref_customers.email>` AND `created_at` within **±7d** of `requested_date` AND `inv_sales_order_lines.sku_id_fk` overlap. NOTE: `inv_sales_orders` has NO requested_date / customer FK — the correlation is email+pickup+SKU+created_at window; **`source_ref` UNIQUE is the hard structural backstop.** Operator override = `confirm_twin` checkbox → `$allowTwin=true` skips the twin block; UX reveal via `$_SESSION['eo_twin_pending_id']` + `.eo-twin-confirm--visible` after a twin flag. (This is the same predicate the P2-TWIN-CHECK section ratified — now confirmed as-shipped.)
- **Three buckets in the UI:** `parsed` → review card (raw body beside editable candidate); `no_match`+`error` → "non parsé" (raw body + parse_error); `order_created` → archived.
- **Smoke (twice, across both commits):** full validation round-trip created a real `ord_orders` + line + status-event and flipped the email; a second submit raised `AlreadyPromoted` (idempotent — no dup); then full self-clean to zero residue. `md5sum` parity local↔VPS confirmed.
- **Pre-existing JS bug fixed in passing:** customer typeahead dropdown class mismatch (`.eo-cust-dropdown`) that had kept the submit button disabled.
- **STILL GATED (external, unchanged):** (a) P0 live mailbox — Gmail **DWD needs a SUPER admin → Arthur** (Kouros has admin; GCP-side SA/key/enable-Gmail-API doable now). (b) Real per-sender (L1) parsers + L2 grammar tuning — blocked on Kouros forwarding sample `.eml`. (c) D1 hand-off (promote `source='email'`→`'maltytask'` at validation to feed the disarmed BC push) = **deferred/optional, NOT built** (the option-(b) lean from PRODUCER→D1 below — confirm if/when activated).

## ✅ AS-BUILT — P1 (layer-2 generic parser + Model B wiring) SHIPPED 2026-06-15 (commit `0a1f267` on origin; mig 371 applied)
**MIG HEAD now 371 (next free = 372).** mig 371 + Model B + dispatch refinement + layer-2 vocab parser all landed `0a1f267`. STILL NO live mailbox, NO real per-sender (L1) parser.
- **mig 371 `371_doc_email_parsed_json.sql` (APPLIED + VERIFIED LIVE).** `doc_email_messages.parsed_json` JSON NULL column added; `parse_status` ENUM extended to **`('unparsed','parsed','no_match','error','order_created')`** (terminal `order_created` added). **This DISCHARGES the "mig 371 first-task-of-next-build" item — RECONCILIATION B is now DONE.**
- **Model B fully wired in the poller (`ingest_email_orders.py`).** Parse-time writes ONLY `doc_email_messages` — `parse_status` + (on `parsed`) `parsed_json` hints; **NO `ord_orders` write at parse time**; `parse_error` now reserved for REAL errors only (the prior dual-use sentinel hack in `parse_error` is GONE).
- **Dispatch refinement (`email_parsers/__init__.py`) — match-AND-parseable-wins.** `parse()` returning `None` = decline-fall-through to the next parser; `raise` = error; all-declined / none-matched = `no_match`. **`parse()` signature is now `(ctx, env)`** (env carries the loaded vocab + dayfirst); `base.py` + `example_template.py` updated to the new signature.
- **Layer-2 generic vocabulary parser SHIPPED (`email_parsers/generic_vocab.py`, registered LAST).** Vocabulary is a **LIVE read** (`load_vocab`) from canonical tables — `ref_skus`(sku_code, beer_raw), `ref_recipes`(name, recipe_short_name), `ref_beer_types`(beer_name, aliases), `ref_recipe_aliases`, `ref_sku_aliases` — **407 terms on current DB**, NEVER hardcoded (only docstring examples mention beer names). Fuzzy = **stdlib `difflib` token-sort ratio ≥ 0.85** (rapidfuzz absent on both VPS + WSL → difflib chosen, ZERO new dep). Deterministic qty/unit grammar (fût/carton/bouteille/canette/pack/palette, "Nx", "N L", FR decimal comma) + FR date-phrase parser honoring `system_settings` dayfirst; **relative-week phrases ("cette semaine") → None (ambiguous, NOT committed)**. Conservative gate: emits a ParsedOrder ONLY for lines mapping to a known SKU with unambiguous qty; zero confident lines → `None` → no_match → review. `env` carries vocab (read-only, loaded once per run, works in `--dry-run`) + dayfirst; generic parser declines gracefully if vocab is `None` (offline smoke stays green).
- **Live verification (dry-run, real-beer-name fixtures):** "2 fûts de Alternative" → sku_hint **ALT4**; "3 cartons de Diversion" → **DIV33C**; "jeudi prochain" → **2026-06-25**; junk newsletter → **no_match**. `--apply` write-path proven (parsed_json populated, parse_error NULL) then test rows self-cleaned. Confirmed: NO LLM anywhere (logic); NO ord_orders write; NO guessed FK; vocab not hardcoded.
- **Prior (mig 370 substrate, `646e18c`/`0c0bb5e`):** `doc_email_messages` raw RFC822 lane (`message_id` UNIQUE idempotency key; schema_meta class='source'); `ord_orders.source_email_id_fk` FK→…id ON DELETE SET NULL; `ord_orders.source` ENUM already had `'email'` since mig 279 (NO ALTER); poller `--fixtures-dir` offline mode; `--dry-run` default; NO cron; NO BC push.
- **STILL GATED (external, unchanged):** (a) P0 live mailbox — Gmail **DWD needs a SUPER admin → Arthur** (Kouros has admin only; GCP-side SA/key/enable-Gmail-API doable with Kouros now). (b) Real per-sender (layer-1) parsers + layer-2 grammar tuning — blocked on Kouros forwarding sample `.eml` per sender.
- **➡️ P3 VALIDATION UI = ✅ DONE 2026-06-15** (`172fd95`+`6ef9df0`, mig 372) — see the P3 AS-BUILT section at the top of this file. Built as the standalone module `public/modules/email-orders.php` (file-contention with `expeditions.php` AVOIDED). All gated externals (P0 mailbox, L1 parsers, optional D1 hand-off) remain open.

## 🟢 P2+P3 DISPATCH BRIEFED (offline-against-fixtures, mailbox/L1-parser deferred) — PM, 2026-06-15
Operator greenlit P2 (producer/write-primitive) + P3 (validation UI) NOW, offline. PM authored two self-contained Sonnet briefs. **LIVE SCHEMA RE-VERIFIED 2026-06-15** (do not trust stale): line table is **`ord_order_lines` (plural)**, FK `order_id_fk`/`sku_id_fk`, `qty DECIMAL(10,2)` CHECK `qty>0`, `line_status` default `to_fulfil`; `ord_orders` has `ord_orders_chk_exactly_one_party` CHECK + `requested_date NOT NULL` + `uniq_ord_source_ref` UNIQUE + `idx_ord_orders_source_review(source,review_status)`; `doc_email_messages.parse_status` ENUM includes terminal `order_created`; `parsed_json` JSON present; table EMPTY (AUTO_INCREMENT=4 = cleaned test rows). `ref_pages` cols = page_key/label/icon/href/min_role/`domain` ENUM('production','logistics','admin','general')/is_active/`sort` (NOT sort_order). Precedent parked page: `invoice-validate` "À valider" (general, is_active=0). Presets: manager(1)/production_operator(2)/logistics_operator(3)/marketing(8)/sales_manager(9). exact ord_orders+lines+status_event INSERT pattern lives in `public/modules/expeditions.php` L599-655 (source='web' there). parsed_json shape (`_kind:parsed_order_hints`,`_schema_version:1`,customer_hint,requested_date,notes,lines[{sku_hint,qty,raw}]) serialised in `scripts/python/ingest_email_orders.py::_serialise_parsed_order` L414. **KEY RULING surfaced to orchestrator: in Model B P2 has NO parse-time producer — the ord_orders write IS the validation action; so P2 = a SHARED PHP write primitive + its guards, P3 = the UI that calls it. Build P2 first.** Migration template for ref_pages+preset grant = `db/migrations/367_planning_preset_access.sql`. **STILL DEFERRED:** Gmail DWD/live mailbox, real per-sender L1 parsers (Kouros sample .eml). When built, update this arc + flip the "NEXT BUILD" line above.

## 🟢 RATIFIED 2026-06-15 — CANONICAL PRODUCER MODEL = MODEL B (validation-first, NO premature FK). PM ruling.
**The question:** where does a parsed email-order live at PARSE time? **RULING: Model B confirmed** — and it is not a free choice, the LIVE schema FORBIDS Model A for any unresolved order (PM-verified live 2026-06-15).
**Live constraints verified (`SHOW CREATE TABLE ord_orders`):**
- `requested_date` is **`NOT NULL` (column constraint, not a CHECK)** — cannot be deferred without a value.
- There is **NO `customer_id_fk NOT NULL` CHECK.** The ONLY CHECK = `ord_orders_chk_exactly_one_party`: `order_type='customer'` ⟹ `customer_id_fk NOT NULL` (+ internal_channel NULL) **XOR** `order_type='internal'` ⟹ `internal_channel NOT NULL` (+ customer_id_fk NULL). `order_type` is itself `NOT NULL`. So a *customer* order genuinely cannot exist without the customer FK.
- `review_status` ENUM = `('none','pending','accepted')`; `source` ENUM has `'email'`; `source_ref` UNIQUE (`uniq_ord_source_ref`); `source_email_id_fk` FK→doc_email_messages(id) ON DELETE SET NULL. All as-built.
- **Net: a hints-only `ord_orders` row is impossible** (NOT NULL requested_date + the party CHECK both force resolution at INSERT). Model A ⟹ guess the customer FK (never-guess violation) OR hard-fail on every sender-pin miss (drops orders). Model B is the honest shape mig 370 was built for.

**THE CANONICAL MODEL (Model B) — precise contract:**
- **(a) PARSE time** (poller+parser, no human): write ONLY `doc_email_messages` — `message_id` UNIQUE, from/subject/received_at/raw_body/attachments_json/parser_matched, **`parsed_json`** (full ParsedOrder hints: customer_hint, line sku_hints+qty+unit, requested_date_hint), `parse_status` ∈ {`parsed`=parser matched+hints / `no_match`=nobody claimed it incl. layer-2 decline / `error`=parser raised / `unparsed`=fetched-not-dispatched}. **NO ord_orders row. NO customer/sku/date written as FACT — hints are strings/ints in parsed_json, never FKs.**
- **(b) VALIDATION action** (operator confirms/edits): INSERT `ord_orders` fully resolved — `order_type='customer'`, `customer_id_fk` (human), `requested_date` (human), `source='email'`, `source_email_id_fk`=back-link, `source_ref='email:<message-id>'`, `review_status='accepted'`, `created_by_user_id`=validator; INSERT `ord_order_line` rows with resolved `sku_id_fk`+qty (refuse-don't-NULL: unresolved line ⟹ cannot save). Flip `doc_email_messages.parse_status` to a TERMINAL `'order_created'` marker (NEW ENUM val — add in mig 371; do NOT overload `parsed`).
- **(c) state table:** parsed→(ord_orders none)→`order_created`+ord_orders source='email' review_status='accepted'. **NO `pending` ord_orders row in the email path** — the "candidate awaiting validation" state lives entirely in `doc_email_messages.parse_status='parsed'` (RECONCILIATION A's `pending` stays valid for OTHER soft-land paths, email skips straight parsed→accepted).
- **(d) idempotency = TWO layers, both already present:** parse-time `doc_email_messages.message_id` UNIQUE (RFC822 Message-ID; + Gmail `maltytask/processed` label belt-and-suspenders); promotion-time `ord_orders.source_ref='email:<message-id>'` vs `uniq_ord_source_ref` UNIQUE (one message → ≤1 order even on double-fire). The `order_created` flip = human-facing guard; the UNIQUE = structural guard.
- **(e) XOR / no-double-deplete:** email = `ord_orders` leg ONLY (XOR `inv_sales_orders` eshop/Shopify twin). commandes@ senders aren't Shopify pickup → no twin structurally. ENFORCE at validation (don't assume): before creating, check no `inv_sales_orders` twin for same customer+date+SKU from a Shopify-pickup sender; any sender found to also flow Shopify-pickup → review, never auto-promote. A `parsed` row NEVER touches fg_stock; depletion fires only off ord_orders line-status (the 5 gated legs), downstream of human validation → XOR can only break via a created order, and creation is human-gated + twin-checked.

**✅ P2 TWIN-CHECK PREDICATE — PM-RATIFIED AS-BUILT 2026-06-15 (Opus verified live).** `app/email-order-promote.php` is built+offline-verified. The brief's "precise customer-FK + delivery-date equality" twin-check is STRUCTURALLY IMPOSSIBLE: `inv_sales_orders` has NO `customer_id_fk` (only `customer_email`/`customer_external_id` strings) and NO requested/delivery date (only `created_at`=order-placed + `period`; our `ord_orders.requested_date`=delivery, semantically different). Coder did NOT fabricate a proxy — correct. **Structural-XOR CONFIRMED as the real guard, detector is best-effort early-warning only:** (1) WRITE-side — `inv_sales_orders.source` ENUM = `('shopify','lightspeed')`, NO `email`/`web` value → an email order PHYSICALLY CANNOT be written there → no twin can be created. (2) DEPLETION-side (`app/fg-stock.php`) — the 3 FG legs are source-DISJOINT: Leg1 B2B = `ord_order_lines JOIN ord_orders … line_status='to_fulfil'` (the ONLY leg an email-promoted order feeds); Leg2 eshop = `inv_sales_order_lines JOIN inv_sales_orders … channel='eshop'`; Leg3 taproom = `inv_sales_bc`. Same physical order can't appear in both stores → no double-deplete. The SQL detector is NOT the sole thing standing between us and double-booking — it routes genuine human double-entry (a commandes@ sender who's ALSO a Shopify-pickup customer ordering via both) to review. **AS-BUILT predicate RATIFIED:** `email + source='shopify' + fulfilment_mode='pickup' + created_at BETWEEN requestedDate±7d + SKU overlap`; email-NULL→skip (no fabricated match); throws EmailOrderTwinException→P3 review. ±7d band is the RIGHT move (absorbs placed-vs-delivery lag instead of demanding impossible date-equality). Neither false-blocks clean B2B orders nor false-passes a real pickup twin. ACCEPTABLE KNOWN GAP (no change required): keys on `inv_sales_orders.customer_email` string-equality, so a Shopify-email ≠ ref_customers.email evades the ADVISORY only (structural XOR still holds); optional future widening = also match `customer_external_id` — NOT a ship-blocker. DO NOT change code.

**✅ mig 371 DONE (`0a1f267`, applied + verified live):** (1) `doc_email_messages.parsed_json JSON NULL` ADDED — hints moved out of the parse_error sentinel (RECONCILIATION B discharged); (2) `doc_email_messages.parse_status` ENUM extended with terminal `'order_created'`. `parse_error` now reserved for real errors only. Model B is FULLY WIRED in the poller (parse-time writes only doc_email_messages; no ord_orders write).

## 🔴 TWO RECONCILIATIONS (baked into the spec 2026-06-15 — these CORRECT earlier notes in this file)
- **A — `review_status` reality.** The spec elsewhere in this file said email orders land `review_status='needs_review'` — **that value does NOT exist.** The actual `ord_orders.review_status` ENUM is **`('none','pending','accepted')`.** So the validation-first state is **`review_status='pending'`** (= awaiting logistics validation); `accepted` = validated. The D1 hand-off gate (promotion `source='email'`→`'maltytask'` at validation) is UNCHANGED. **Wherever this file says `needs_review` for an email order, read `pending`.**
- **B — ✅ RESOLVED 2026-06-15 (mig 371, `0a1f267`).** The earlier scaffolding stashed `ParsedOrder` hints in `doc_email_messages.parse_error` behind a `_kind:"parsed_order_hints"` sentinel (temp hack). **FIXED:** `parsed_json JSON NULL` added to `doc_email_messages`; hints now live there; `parse_error` reserved for real errors only. The sentinel hack is GONE.

## Verbatim intent (Kouros)
maltytask links to `commandes@lanebuleuse.ch` to **spot, read, write orders to BC**, logistics then validates via a **mail/BC comparison**. EXPLICIT: **deterministic per-sender parsers, NOT LLM** ("Je ne veux pas que ce soit de la lecture via LLM"). Reuse the parser model we already have. This arc supplies the **LIVE PRODUCER** D1/D2 currently lack (the standing OPEN-SCOPING gap: no maltytask-native order producer in prod).

## Ground-truth facts (PM-verified 2026-06-15)
- **MAILBOX PROVIDER = Google Workspace.** `dig MX lanebuleuse.ch` → `smtp.google.com`. SPF = `include:mx.ovh.com include:_spf.google.com` (OVH legacy + Google) + Klaviyo (marketing). Authoritative receiving MX = **Google**, NOT M365. → Graph/Entra-tie theory is DEAD; mailbox access = **Gmail API (Google Workspace)** or IMAP-to-Gmail. This is unrelated to the BC Entra tenant.
- **Parser model already proves the "no-LLM" doctrine.** `lib/invoice-parsers/index.js`: each parser exports `matches(ctx{supplierName,fileName,ocrText})→bool` + `parse(text,miCtx?)→result`; `pickParser()` walks the list, first match wins. **LLM fallback was DROPPED from the dispatcher 2026-05-24 — a no-match is now a no-match (goes to review), never an LLM read.** This is exactly the architecture to clone for email senders. ~70 parsers live.
- **ord_orders is ALREADY email-ingestion-ready** (mig 279): cols `source`, `source_file_id_fk`→`doc_files.id`, `parse_confidence`, `review_status`. **`source` ENUM (VERIFIED AS-BUILT) = `('web','email','import','bc','maltytask')` — `'email'` ALREADY present since mig 279, NO ALTER needed.** `review_status` ENUM = `('none','pending','accepted')` (NOT `needs_review` — RECONCILIATION A). `source_ref` UNIQUE (mig 294) = idempotency key. Order grain = delivery event.
- **D1 selection (verified, fulfilment arc §D):** `push_bc_sales_orders.py` pushes ord_orders where source-native AND `bc_no IS NULL`, only `to_fulfil` lines, GET-before-create idempotent, echo `Your_Reference='mt:<id>'`, rekey `mt:<id>`→`bc:<No>`. Whether `source='email'` is picked up by D1 = SELECTION-PREDICATE question (see Producer wiring below).
- **XOR depletion canon:** an order ∈ `ord_orders` XOR `inv_sales_orders`, never both. Email orders are B2B customer orders → ord_orders leg (no eshop twin) → safe. Watch privé/pickup forward-hazard only if a sender is a Shopify-synced pickup (rare for commandes@).
- **NEVER-GUESS-A-MAPPING** applies HARD (revenue-bearing): customer + SKU resolution that's uncertain → review bucket, never auto-create/auto-link a revenue FK.

## KOUROS-DECISIONS REQUIRED BEFORE P0 (consolidated)
1. **Mailbox access method (#1, blocks everything).** Recommend **Gmail API service-account with domain-wide delegation** impersonating `commandes@` (read-only `gmail.readonly` + `gmail.modify` for label-after-process) — clean OAuth, label/thread-aware, no password storage, matches the existing Google-service-account infra (kouros-sheets-access SA already in the project; may need a new SA + Workspace admin DWD grant for Gmail scopes). Alternatives: IMAP+app-password (simpler, but stores a credential + app-passwords need 2FA-bypass which Workspace may disallow); forward-to-ingest (fragile, loses sender fidelity). **Kouros confirms: Gmail API DWD vs IMAP, and who in Workspace admin can grant the scope.**
2. **Auto-push to BC, or always wait for logistics validation first?** PM STRONG RECOMMENDATION: **parsed email orders land as `ord_orders` for HUMAN validation BEFORE any BC push** (mirrors D1/D2 disarm discipline + Kouros's own "logistics validates via mail/BC comparison" intent). No auto-write to BC without the same green-light gate. Kouros ratifies.
3. **Which senders/formats first + sample set.** Who actually emails orders to commandes@? Need from Kouros a **real sample corpus per sender** (5-10 emails each) to build deterministic parsers against — same as we needed invoice samples. Target the 2-3 highest-volume recurring B2B senders first; everything else → review until its parser exists.

## RECOMMENDED ARCHITECTURE
- **Where the code lives:** STRANGLER RULE → prefer **maltyweb + MySQL + cron**, NOT a new standalone Node script. The mailbox poller is a Python connector on the VPS (sibling to the BC connectors `scripts/python/ingest_bc_*.py` — same cron pattern, same `config/*.env` creds model) that fetches new messages, dispatches to per-sender parsers, and writes `ord_orders` + a raw-email cache table. Parsers: a **PHP or Python per-sender registry mirroring `lib/invoice-parsers/`** — `matches(ctx)→bool` + `parse(body)→{customer, lines[], requested_date}`. PM leans **Python** (the connector is Python; keeps mailbox+parse+write in one VPS process; the JS invoice-parsers stay JS but the PATTERN is copied, not the code). Validation UI = maltyweb PHP.
- **Email body, not OCR:** email orders are text/HTML bodies (or attached PDF/xlsx). Text bodies → regex/structured parse directly (no OCR). PDF/xlsx attachments → reuse the OCR/xlsx path. The parser `ctx` = `{fromAddress, subject, bodyText, attachments[]}` (analogue of the invoice `ctx`).
- **Resolution:** customer via `ref_customers` (exact email→customer map first — store a sender-email→customer_id pin, analogous to `ref_mi_aliases`; then name match; else review). SKU via `ref_sku_aliases`/`ref_skus` (reuse the `(Item_No,UoM)` resolver pattern from BC Phase-1). Confidence gate: all lines resolve ≥ threshold + customer resolved → `review_status='accepted'`-eligible; else **`review_status='pending'`** (= awaiting logistics validation — see RECONCILIATION A; there is NO `needs_review`/`auto` value). Unparseable sender (no `matches`) → review bucket with raw body, NEVER guess.
- **Idempotency/dedup:** key on **RFC822 `Message-ID`** (stable per email) → the `doc_email_messages.message_id` UNIQUE col (AS-BUILT) and `source_ref='email:<message-id>'` on `ord_orders`. Replies/threads = distinct Message-IDs → distinct rows; a forwarded/re-sent same Message-ID = skip. Label processed messages in Gmail (`maltytask/processed`) as a belt-and-suspenders second guard so a label-filter query never re-fetches.
- **Raw store (AS-BUILT, mig 370):** `doc_email_messages` (`id` BIGINT UNSIGNED PK, `message_id` UNIQUE, `from_address`, subject, received_at, raw body, `attachments_json`, `parse_status` ENUM, `parse_error`) — the append-only raw lane (schema_meta class='source'). `ord_orders.source_email_id_fk` → `doc_email_messages(id)` ON DELETE SET NULL (added). 🔴 **`parsed_json JSON NULL` STILL TO BE ADDED (mig 371, first task next build) — see RECONCILIATION B; until then hints live in `parse_error` behind a sentinel (temporary hack).** Refuse-don't-NULL: unresolved line → review row, never a NULL sku_id_fk ord_order_line.

## 🟢 PARSER PLAN — THREE-LAYER DETERMINISTIC STACK (Kouros approved 2026-06-15, "parse the BODY without an LLM"). NO LLM, EVER.
Kouros asked "can we parse the email BODY (not just PDF attachments) without an LLM?" → YES. The registry runs a 3-layer deterministic stack; uncertain → human, never guessed.
- **Layer 1 — per-sender templates.** Same doctrine as the PDF/invoice parsers, applied to `body_text`/`body_html`: HTML-table DOM walk OR aligned plain-text columns. `matches(ctx)→bool` keyed on from_address/subject; `parse(body)→ParsedOrder`. Built against Kouros's real per-sender samples.
- **Layer 2 — GENERIC vocabulary fallback — ✅ SHIPPED `0a1f267` (`email_parsers/generic_vocab.py`, registered LAST).** Runs ONLY after all per-sender parsers decline. Vocabulary = **LIVE read** (`load_vocab`) from CANONICAL tables — **`ref_skus`(sku_code, beer_raw), `ref_recipes`(name, recipe_short_name), `ref_beer_types`(beer_name, aliases), `ref_recipe_aliases`, `ref_sku_aliases`** (407 terms on current DB; **NEVER hardcoded** — only docstring examples name beers). Fuzzy = **stdlib `difflib` token-sort ratio ≥ 0.85** (rapidfuzz absent on VPS + WSL → difflib chosen, ZERO new dep). Deterministic qty/unit grammar (fût/carton/bouteille/canette/pack/palette, "Nx", "N L", FR decimal comma) + FR date-phrase parser honoring `system_settings` dayfirst; **relative-week phrases ("cette semaine") → None (ambiguous, NOT committed).** Conservative gate: emits a ParsedOrder ONLY for lines mapping to a known SKU with unambiguous qty; zero confident lines → `None` → no_match → review. Vocab loaded once per run into `env` (works in `--dry-run`); declines gracefully if `env.vocab is None` (offline smoke stays green). Tune the grammar against real samples when they arrive.
- **Layer 3 — confidence gate.** Anything below the bar → the review bucket (the validation-first surface = `doc_email_messages.parse_status` + the P3 UI). "No LLM" = uncertain orders go to a human, never guessed.
- **🔑 Registry dispatch refinement — ✅ IMPLEMENTED `0a1f267` (`email_parsers/__init__.py`):** match-AND-parseable-wins. `parse()` returning **`None` = decline, try the next parser** (a per-sender parser that matches the SENDER but can't read THIS body falls through to layer-2); **`raise` = error** (→ parse_status='error'); **all-declined / none-matched = no_match** (→ parse_status='no_match'). **`parse()` signature is now `(ctx, env)`** (env carries vocab + dayfirst); `base.py` + `example_template.py` updated. The example_template is the L1 reference; generic_vocab is the L2 generic parser registered last.

## PHASING
- **P0 — Mailbox-read SPIKE + provider decision — ✅ DONE + NOW LIVE-APPLYING.** Gmail API DWD ACTIVE (impersonates production@; commandes@=distribution list into production@). Connector reads real msgs, byte-fetches attachments, dispatches the 3-layer + attachment stack, and (as of the attachment slice, 2026-06-19) **`--apply` is LIVE** — `doc_email_messages` written, orders into email-orders.php, no_match→"Non parsé". NO cron yet.
- **P1 — Parser registry (3-layer) + Model B wiring — ✅ DONE `0a1f267` (mig 371; layer-2 + dispatch + parsed_json all shipped).** Registry runs the 3-layer stack with decline-semantics; L2 generic_vocab.py shipped (canonical-table vocab via difflib ≥0.85 + deterministic qty/unit/date grammar, registered LAST). Parse-time writes `parsed_json` hints into `doc_email_messages` (Model B), NO ord_orders write. **STILL OPEN within P1:** L1 per-sender templates + L2 grammar tuning — both blocked on Kouros forwarding real sample `.eml` per sender.
- **P2 — Producer → ord_orders → D1 wiring.** Flip the validation step to WRITE `ord_orders` (**source='email' — already in the ENUM since mig 279, NO ALTER**; source_ref='email:<msgid>' UNIQUE; lines; review_status). Decide the D1 hand-off (see below). Closes the OPEN-SCOPING producer gap. **mig 371 `parsed_json` already DONE (RECONCILIATION B discharged).** In Model B the ord_orders write happens at VALIDATION (P3), so P2 effectively merges into P3. **EQUIP: sql + coder + webapp-testing.** Disarm: orders land **`review_status='accepted'`** at validation (Model B — there is NO `pending` ord_orders row in the email path; the candidate state lives in `parse_status='parsed'`), **NOT auto-pushed to BC** (Kouros-decision #2); D1 still triple-gated.
- **P3 — Logistics validation UI — ✅ DONE 2026-06-15** (`172fd95`+`6ef9df0`, mig 372). SHIPPED as the standalone module `public/modules/email-orders.php` (+ css/js), NOT on `expeditions.php` (file-contention avoided per the warning). Three `parse_status` buckets (parsed→review / no_match+error→non-parsé / order_created→archived); operator confirm/edit → `app/email-order-promote.php::email_order_promote()` (single promotion home) → atomic ord_orders+lines+status-event + parse_status flip; XOR twin-check (shopify+pickup+email+SKU+±7d) with confirm_twin override; idempotent via source_ref UNIQUE. ref_pages id=480 + preset grant (manager+logistics) + tour card. NO BC push, NO COGS/fg_stock write. EQUIP was coder + sql + ui + webapp-testing.

## PRODUCER→D1 HAND-OFF (P2 crux — decide at P2)
Two clean options:
- **(a) Add `source='email'` to D1's push selection** (the predicate becomes source IN native-set incl. email AND bc_no IS NULL). Email orders then flow through the SAME proven D1 spine untouched.
- **(b) Promote validated email orders to `source='maltytask'` at validation time** so they fall into D1's EXISTING selection with no predicate change.
PM LEAN = **(b)** — keeps D1's selection stable + makes the human-validation step the explicit promotion gate (un-validated email order = source='email' = NOT pushable; logistics accept flips it to maltytask = pushable). This wires the validation UI directly to the producer gate. Confirm at P2.

## DIVERGENCE FLAGS
- **🔴 PARALLEL SESSION IN THE SHARED CLONE (2026-06-19):** an UNRELATED arc is mid-build on `scripts/python/ingest_email_comm.py` (a `comm_threads`/`comm_messages` inbox for info@ correspondence — this is the ENTITY-DISCUSSION-TRACKER arc, see [entity-discussion-tracker-arc.md](entity-discussion-tracker-arc.md)). Left completely untouched/uncommitted by this slice. Serving-tank/expeditions **mig 411** also pending from yet another session. The clone is MULTI-SESSION-DIRTY — commit by PATHSPEC only, never bare/`-A`; deploy surgically per-file.
- **🔴 Google-Groups From-rewrite ("via Commandes") — the From header LIES for list-routed mail.** When an external customer mails the `commandes@` distribution list, Google Groups rewrites `From` to `commandes@lanebuleuse.ch` (display name kept, e.g. `"'Ana Rocha' via Commandes"`). The REAL sender lives in `Reply-To`/`X-Original-From`/`X-Original-Sender`. Two failure modes if unhandled: (1) `customer_hint=''` → the real customer is lost to review; (2) `_internal_rep` mis-fires with `_rep_email=commandes@` (harmless to pre-fill since commandes@ isn't mapped, but inflates the flag count + buries real customers). FIX (next slice): resolve the original sender from those headers BEFORE the customer cascade + guard system addresses (`commandes@`/`production@`) out of `_internal_rep`.
- **🔴 The `_internal_rep` flag must test the BARE email, not the raw `From` header** — the raw header is `"Name <email>"`, so `from_address.endswith('@lanebuleuse.ch')` NEVER matches (the live flagged=0 bug). Extract the bare email first. Fired only when `customer_hint=='' AND bare-email ∈ @lanebuleuse.ch` so forwarded-customer/external From both take precedence (no internal-account dumping).
- **No LLM, ever** — a no-match sender goes to review, never an LLM read (honor the 2026-05-24 dispatcher doctrine + Kouros's explicit instruction).
- **Attachment MIME-tree fidelity** — an order attachment may arrive as `application/octet-stream` with only a filename (no `Content-Disposition: attachment`). The attachment walk must treat `get_filename()`-truthy parts as attachments OR it silently drops them (the Bevanar-PDF live bug). Verify the real envelope through the connector, not the parser against a clean file.
- **No auto-push to BC** without the validation gate (Kouros-decision #2) — same disarm discipline as D1/D2.
- **Never guess customer/SKU** — revenue-bearing FK; uncertain → review, never auto-create.
- **XOR depletion** — email order = ord_orders leg only; verify no inv_sales_orders twin for any pickup sender before writing.
- **Idempotency must be Message-ID-based**, not content-hash (replies/qty-edits would false-merge or false-dup), mirroring the WeeklyOrders source_ref ruling.
- **File-contention on expeditions.php** at P3 (parallel sessions).
