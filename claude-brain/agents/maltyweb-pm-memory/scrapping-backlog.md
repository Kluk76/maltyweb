# Scrapping / retirement backlog (RULE 1 — operator standing order 2026-05-25)

> Load when: doing ANY build (RULE 1 requires evaluating this list every build), or when consulted about retiring a surface. The PM's living register of surfaces/code/schema that have become (or are on track to become) useless or redundant.

**Discipline:** evaluate this list on EVERY build; add new candidates the moment a change supersedes something; retire a candidate only after its replacement covers it AND nothing routes to it. Each item carries its **gating condition** (what must be true before it's safe to scrap). NONE of the seeded items below is to be scrapped now — they are recorded as evaluated candidates with their gate. Status legend: `CANDIDATE` (gated, do not scrap) / `READY` (gate met, safe to scrap) / `SCRAPPED` (done, kept for audit trail).

| # | Surface / code / schema | Superseded by | Gating condition (scrap only when ALL true) | Status |
|---|---|---|---|---|
| 1 | `admin/settings/recipes` page | Recettes (Salle de contrôle) | Recettes covers recipe CRUD incl. names AND nothing routes to `admin/settings/recipes` (grep links + topbar) | CANDIDATE |
| 2 | `admin/settings/skus` page | SKU build (Recettes Formats subtab → `ref_skus` activation) | Formats activation covers SKU create/activate AND nothing routes to it | CANDIDATE |
| 3 | `admin/settings/suppliers` page | `salle-fournisseurs.php` (supplier governance) | salle-fournisseurs covers supplier CRUD/governance AND nothing routes to it | CANDIDATE |
| 4 | `admin/settings/yeasts` page | Biochem + Recettes "Levure & garde" (Salle de contrôle) | **PARTIAL coverage as of 2026-05-27 (Saisie-Transferts landed `2d131de`).** Biochem now owns yeast-FAMILY defaults; the new Recettes "Levure & garde" editor owns recipe→strain linking + inline strain→family classify. BUT `admin/settings/yeasts.php` STILL owns **strain CRUD + supplier linkage** (manages `ref_yeast_strains` rows themselves — "Souches de levure et fournisseurs") which the new surfaces do NOT cover (PM decision: no dedicated strains-management panel built — inline-classify only). It is STILL routed from `admin/settings.php` L23. Scrap ONLY AFTER a Zeppelin surface owns strain CRUD (create/retire/dedup/supplier-link) AND nothing routes to it. DO NOT scrap now. | CANDIDATE (partial — strain CRUD not yet superseded) |
| 5 | `admin/settings/devices` page | — (session/device mgmt) | **STAYS — do NOT retire.** No Zeppelin replacement; recorded so it isn't mistakenly swept with the other settings pages | KEEP |
| 6 | `vessels.php` FILE (the `/admin/settings.php` "Cuves" CARD already retired in `ffd003c`) | Salle des Machines vessel cards (`ref_cct`/`ref_bbt`/`ref_yt`/`ref_serving_tanks`) | Kept as a fallback. Delete the FILE only after confirming nothing includes/routes to `vessels.php` and the Salle des Machines cards fully cover vessel CRUD | CANDIDATE |
| 7 | `ref_packaging_format_mis` table (34 rows) | `ref_packaging_items` (slot model) — the BOM plan flagged folding it in | All consumers re-pointed to `ref_packaging_items`; the 34 rows' facts confirmed represented in the slot model; grep PHP/TS for the table name = 0 live reads. NB: this table is ALSO a primitive in the volume-dimension format→MI bridge (volume-dimension.md) — re-verify it isn't load-bearing there before scrapping | CANDIDATE |
| 8 | `scripts/build-sku-bom.js` (Sheets-based, maltytask) | `app/sku-bom-compile.php` — PACKAGING side only (partial supersession as of 2026-05-25) | Only the packaging side is superseded today. Retire fully ONLY after the compile service also owns the LIQUID side (§8.8 F2 deferred) AND nothing in the maltytask pipeline still calls build-sku-bom.js. Until then it remains the liquid-side source — DO NOT scrap | CANDIDATE (partial) |
| 9 | `FORMAT_SUFFIXES` / `RUN_TYPE_LABELS` hardcoded consts in the packaging form | the dynamic active-SKU picker (SHIPPED `539bad3`) | **Picker SHIPPED but does NOT yet supersede these — the picker writes INTO the FORMAT_SUFFIXES/RUN_TYPE_LABELS selects (they remain the live POST source; the picker is a presentational pre-fill layer).** NOT a scrap candidate as built. Scrap ONLY IF a future picker iteration BYPASSES these selects and drives the POST contract directly (e.g. POSTs `sku_id_fk` straight). Re-evaluate at the picker's browser-QA and at any future picker rework. DO NOT scrap | CANDIDATE (gated on a future picker iteration that bypasses the selects) |
| 10 | `app/sku-compiler.php` (the UNWIRED earlier `compile_sku_bom` draft) | `app/sku-bom-compile.php` (the active `compile_sku_bom_packaging`), once composite decomposition is folded INTO it | **Divergence smell: a 2nd compiler modelling the same fact on a STALE SCHEMA contract** (`s.recipe_id_fk`/`cs.multiple`/`r.recipe_short_name` — none match live; it would error on first run). Only self-referenced (line 362), never required by the live CLI/hooks; last touched in the `d6545fd` mockup commit. It is the ONLY code reading `ref_sku_composite_slots`. Scrap AFTER the active service absorbs the composite-decomposition intent (sku-decomposition-tree.md sequencing step 3) AND grep confirms 0 live requires. **2026-05-26: PM-verified ZERO external callers (`grep compile_sku_bom\b` → only the self-test at line 362; no `require` of `sku-compiler.php` anywhere). Cleared to delete IN THE SAME step-③ build.** DO NOT resurrect as-is | CANDIDATE (gated on composite decomposition landing in `sku-bom-compile.php`) |
| 11 | `body.approv-fournisseurs`-scoped `--kf-*` palette block in `app.css` (~L10128) + the per-body-class dark token re-declarations in `body.home` + `body.auth` | The global `:root` token block introduced by the site-wide light-kraft recolour (`3c57e5b`) | Gate WAS: the `--kf-*` tokens promoted to the shared layer (`:root`) AND the dark per-body-class blocks no longer needed. **2026-05-26: MET + DONE in `3c57e5b` — `--kf-*` promoted to `:root`; dark token lines DELETED from `body.home`/`body.auth` so they inherit `:root`; token-orphan pages fixed for free. Single-source palette achieved.** | SCRAPPED |
| 12 | The OLD `topbar.php` flat module-list nav (the pre-hub flat list of modules: salle-des-machines / salle-fournisseurs / approvisionnement / etc.) | The Le Zeppelin family-switcher IA (`le-zeppelin.php` hub, `e65d0dd`) | **Logged 2026-05-26 from the Le Zeppelin hub build (RULE-1).** Dual-navigation-IA divergence: the family-switcher hub now exists AS the canonical IA, but the old topbar flat module-list still routes to the same pages in parallel (two navigation models for one app). Scrap the flat list ONLY AFTER salle-des-machines / salle-fournisseurs / approvisionnement are migrated onto the family-switcher IA (reachable via their family hub) AND the flat list is provably redundant (every destination reachable through the hub). Until then BOTH coexist — flat list stays. **Gated + deferred (operator confirmed).** | CANDIDATE |
| 13 | `public/index.php` DB-status splash as the `/` landing | The Le Zeppelin hub (`le-zeppelin.php`) as the potential `/` landing | **Logged 2026-05-26 from the Le Zeppelin hub build (RULE-1).** Open IA decision, NOT yet a scrap: the operator chose to leave `index.php` untouched and host the hub at its own route. Fate to decide: should `/` redirect to the hub (making the DB-status splash either a sub-view or retired)? No action until the operator decides direction. NOT to be touched now. **Gated + deferred (operator decision pending).** | CANDIDATE (decision pending, not a scrap) |
| 14 | Flat CIP columns on `bd_racking_v2` (`last_cip_date`/`cip_type`/`cip_bbt_done`/`cip_bbt_type`/`cip_bbt_date`), `bd_brewing_brewday_v2` (`cct_cip`/`cct_cip_date`/`yt_cip_date`), `bd_packaging_v2` (`cip_tank_*`/`cip_machines_*`) + hardcoded `CIP_TYPES` const in form-brewing.php | `bd_cip_events` child table (migration 176) + `ref_cip_types` (175) | **Logged 2026-05-27 from the CIP convergence build (D1=converge all 3 forms). ✅ CONVERGENCE LANDED 2026-05-27j (`8c53e98`+`36b72c5`+`b269eee`+`592641d`): all 3 web forms now write `bd_cip_events` ONLY, STOPPED writing the flat cols, AND removed them from the parent `bd_*_v2` hash basis (PM-confirmed no hash drift) → the flat cols are NOW confirmed READ-DEAD (web) but WRITE-LIVE for legacy Sheets ingest only.** They are KEPT as the **legacy Sheets-ingest write target** (`tab_racking.py`/`tab_brewing.py`/`tab_packaging.py`) — dropping them now breaks ingest. Scrap ONLY AFTER the Sheets ingest is retired OR rewired to write `bd_cip_events` AND grep confirms 0 live reads (already 0 outside ingest). Also retire `CIP_TYPES` hardcode once the brewing form reads `ref_cip_types`. **Gated on ingest rewire/retirement.** | CANDIDATE (gated on Sheets-ingest rewire) |
| 15 | (live bug, not a scrap — tracked here) `warehouse.php` / `warehouse-export.php` g→kg conversion gap | n/a — fix in place via `ref_mi.conversion_factor` on the snapshot surface | Logged 2026-05-27 from the Phase-D Step-3 gate (operator deferred). Values `COALESCE(wac_snapshots.wac_chf, ref_mi.price×fx)×qty` with NO conversion_factor → ~1000× inflated stock DISPLAY for every g/kg MI. Same canonical-source fix as Phase D; keep period-frozen on the snapshot surface (do NOT flip to live `v_mi_wac` — that's the separate Step-2a product decision). NOT this session. | OPEN BUG (deferred) |
| 16 | `data/weighted-prices.json` emit + `lib/pricing.js loadWeightedPrices` read-path (+ possibly the whole `scripts/compute-weighted-prices.js` WAC computation) | `v_mi_cost` = `COALESCE(WAC, catalog)` sourced via the `loadMI({mysqlCost:true})` overlay (maltytask `b7bdab9`) | **Logged 2026-05-27 from Phase-D Commit B (RULE-1).** Commit B made `createPriceResolver` AUTO-BYPASS weighted-prices.json under the overlay; `compute-weighted-prices` still EMITS the json but NO cost builder reads it, AND its `--write-master` BSF writeback was RETIRED in the same commit. So the json is orphaned and the script's WAC computation MAY be fully dead. **VERIFY before scrapping:** grep every reader of `weighted-prices.json` (and any non-cost consumer of compute-weighted-prices output) = 0 live reads. If the whole script is dead, scrap script + json + loadWeightedPrices together. | CANDIDATE (gated on grep-confirming 0 live readers) |
| 17 | `eurBase` figure (`lib/pricing.js` L61) + the `sku-cost-dashboard.html` EUR-rate slider | n/a — obsoleted by CHF-native cost under the overlay (maltytask `b7bdab9`) | **Logged 2026-05-27 from Phase-D Commit B (RULE-1).** Under the `currency='CHF'` overlay lever, `eurBase` collapses to 0 for every MI (costs are already CHF) → the EUR-exposure figure and the dashboard EUR-rate slider (a workaround for the OLD un-converted EUR prices) are now redundant. This is the §COMMIT-B Q4 `eurBase`-zeroes caveat realized. Scrap AFTER confirming no live report depends on `eurBase` for a still-meaningful EUR-exposure metric (it now reads 0). | CANDIDATE (gated on confirming no live eurBase consumer) |
| 18 | `lib/repos/mysql-inventory.ts` (dormant half-cutover) | `lib/rm-stock-mysql.js` (Commit A, maltytask `31af4a9`) for the RM-valuation fact; `v_mi_cost` for cost | **Logged 2026-05-27 from Phase-D Commit A (RULE-1, DO-NOT-WIRE-AS-IS).** Committed, ZERO importers; reads RM qty from MySQL but COST from BSF `miCtx.price` AND carries the SAME g→kg conversion bug Commit A just fixed. Superseded by `lib/rm-stock-mysql.js`. When the pipeline eventually goes TS it MUST adopt `v_mi_cost` + `conversion_factor` — OR be deleted. A future agent wiring it as-is re-introduces BOTH the BSF cost source AND the 1000× value bug. Scrap (delete) once confirmed nothing will adopt it, OR rewrite onto v_mi_cost when the pipeline goes TS. | CANDIDATE (delete-or-rewrite; never wire as-is) |
| 19 | `ref_packaging_formats` id8 (BC, "24-pack can box B variant") | format 7 (C) — the canonical 24-box-cans-bulk suffix, after the EPH BC→C fold (mig 187, `849426d`) | **Logged 2026-05-28 from the EPH BC→C canonical fold (RULE-1).** After mig 187 renamed EPH1-4BC→EPH{n}C + repointed them to format 7, **format 8 (BC) now has 0 active SKUs.** The 4 EPH `…BC` codes survive as `ref_sku_aliases` rows (resolve at sale time) — so dropping the FORMAT row does NOT break alias resolution. Scrap ONLY AFTER confirming: (a) 0 active `ref_skus.format_id=8`; (b) no `ref_packaging_items` / `ref_packaging_format_mis` / `ref_sku_composite_slots.member_format_id` / `ref_packaging_bom_templates` rows reference format 8; (c) no live PHP reads format-id 8. The BC→C convention is now reconciled on the catalog side (`reference_fmt_suffix_bc_folds_to_c`); BC is alias-only. DO NOT scrap until the dangling-ref check is clean. | CANDIDATE (gated on 0-dangling-ref verification) |
| 20 | Sweep cron for `bd_packaging_v2.vendable_hl` (and any future event-derived sweep targeting form-written rows) | `form-packaging.php` COMPUTE-ON-SUBMIT retrofit + `finalize_session` lock-cascade contract | **Logged 2026-05-28 from the session-framework lifecycle ratification (RULE-1).** Operator standing rule ratified 2026-05-28: a derivable column on a form-written row MUST be computed at form-submit time (compute-on-submit; conventions-and-helpers.md §COMPUTE-ON-SUBMIT) and re-run at lock-time via `finalize_session` (session-model-arc.md §LOCK-CASCADE CONTRACT). The hourly sweep cron that was planned to backfill `vendable_hl` is therefore redundant for form rows. Scrap ONLY AFTER: (a) the lock-cascade contract is LIVE (`finalize_session` ships with the racking pilot or later); (b) the form-packaging.php `vendable_hl` compute-on-submit retrofit is LIVE; (c) the python-ingest backstop demonstrably covers `session_id_fk IS NULL` rows arriving from legacy Sheets ingest (the cron's residual role). Until (a)+(b)+(c) are TRUE, the sweep MAY still run as a transitional safety net. **Cardinal:** the sweep is NOT a permanent home for form-derivable columns at any phase — it survives ONLY as the python-ingest catch-all path. | CANDIDATE (gated on lock-cascade live + python-ingest backstop coverage) |

| 21 | `public/modules/form-racking.php` (single-submit endpoint) | Racking session path (P-A + P-B + P-C: `session-body-racking.php` dispatcher + `/api/racking-phase-submit.php` + `/api/racking-session-start.php`) | **Logged 2026-05-28 from the racking pilot P-C build (RULE-1). STRANGLER FIG — do NOT cut over yet.** Session path is additive; `form-racking.php` remains the default entry point from the saisies card's "Saisie classique" link AND the strangler requires operator validation of the session golden path first. Scrap ONLY AFTER: (a) at least 5 successful real racking sessions closed via the session path (operator confirmed, no data anomalies); (b) the saisies card's "Saisie classique" link is removed / redirected; (c) COGS/WAC consumers verified unaffected (§READ-POLICY: filter status='closed'); (d) grep confirms no other routing to `/modules/form-racking.php`. Until ALL four are true, `form-racking.php` MUST stay untouched. | CANDIDATE (gated on 4 conditions above) |
| 22 | (defense enhancement, not a scrap) `scripts/python/ingest_bd_packaging_v2.py` SKU-resolution path missing a `is_packaging_line` safety check | n/a — additive defense to surface mis-routes to `doc_review_queue` | **Logged 2026-05-29 from the F2 vessel-commissioning audit closure (RULE-1, downstream contract).** With migs 210/211 making `is_packaging_line` a canonical flag, the python ingest can use it as a sanity gate: if the resolved `sku_id_fk` for a `bd_packaging_v2` event points to a SKU with `is_packaging_line=0`, emit a `doc_review_queue` row instead of writing — defense-in-depth against the BLO/BLA-style mis-routing bug (operator #2 incident, packaging-bom-model/README.md §THREE SKU IDENTITY LAYERS). This is an ENHANCEMENT (not a scrap and not a live bug); recorded here so the F2 contract isn't lost. Build when: the BLO/BLA literal `sku_code → id` primary lookup fix lands (same file, adjacent edit), OR opportunistically. | ENHANCEMENT (additive defense, no gate) |
| 23 | (placeholder, NOT a scrap — foreshadow) "EXPÉDITION" 5th zone on the mother-shell board (Concept C diorama, `sb-board.php`) — rendered as dashed/grey "Phase 3" placeholder on every closed mother card in v1 | (future) Stage-3 Dispatch / Combat zone activation | **Logged 2026-05-29 from the mother-shell operator review (Q8 expert default kept).** The 5th zone is RESERVED in the v1 layout but NOT WIRED — it carries no dispatch logic, no events, no consumers. Its purpose is to set the visual contract for the Stage-3 activation so the board doesn't need a layout rework later. Activate ONLY when: (a) Stage-3 (Dispatch / Combat) is in scope; (b) the dispatch model is operator-confirmed (Lightspeed integration / Shopify pickup flag / b2b shipment surface — choices TBD); (c) the EXPÉDITION zone's events table + child shell are designed. Until then = visual placeholder only. **NOT a scrap candidate** — this is a forward-looking reservation to prevent a future layout-divergence pass. Recorded here so the "Phase 3" label promise isn't forgotten. | FORESHADOW (placeholder, no gate to scrap) |
| 24 | ✅ **CLOSED 2026-05-29** (TEST DEBT, NOT a scrap — HARD GATE for mother-shell Phase 2-5 now CLEARED) Live UNIQUE enforcement test for `uniq_active_mother (recipe_id_fk, batch, open_mother_key)` on VPS | n/a — verified the as-built filtered UNIQUE (mig 215 §7.4 deviation: CHAR(1) VIRTUAL + composite UNIQUE replaced the planned VARCHAR CASE pattern) | **Logged 2026-05-29 from the mother-shell Phase 1 RULE-2 review (followup F1, deferred). RESOLVED 2026-05-29 same day** — Sonnet executed all 3 scenarios on VPS, all PASS. **Scenario A (direct UNIQUE rejection):** 2nd INSERT for same `(recipe_id_fk=6, batch='TEST_UNIQ_99', form_type='batch', status='open', merged_into=NULL)` rejected with `SQLSTATE[23000] 1062 Duplicate entry '6-TEST_UNIQ_99-1' for key 'op_sessions.uniq_active_mother'` — composite key value `6-TEST_UNIQ_99-1` confirms VIRTUAL `open_mother_key` evaluated to `'1'` correctly (sentinel is `'1'`, not `'Y'`; verified via direct `SHOW CREATE TABLE op_sessions` on VPS 2026-05-29). **Landed GENERATED expression** = 3-condition AND: `CASE WHEN form_type='batch' AND merged_into_session_id_fk IS NULL AND status='open' THEN '1' ELSE NULL END` VIRTUAL — fully self-enforcing for BOTH merge-then-recreate AND close-then-recreate. **Scenario B (merge-then-recreate):** M1 inserted open → `UPDATE M1 SET merged_into_session_id_fk=2` → VIRTUAL re-evaluates to NULL (AND-clause fails on merged_into) → M2 INSERT same (recipe, batch) succeeded. **Scenario C (close-then-recreate):** M3 inserted open → `UPDATE M3 SET status='closed'` → VIRTUAL col became NULL via the GENERATED expression → M4 INSERT same (recipe, batch) succeeded. **Cleanliness:** post-test `SELECT COUNT(*) FROM op_sessions WHERE batch LIKE 'TEST_%'` = 0; temp `/tmp/test_uniq.php` deleted from VPS (per `feedback-test-fixtures-must-self-clean`). **3 schema facts captured during the test (now standing rules — see top-of-file PM-memory header + op-sessions-schema-spec.md):** (1) PDO bootstrap on VPS = `/var/www/maltytask/app/db.php` → `maltytask_pdo()`, NOT `config/bootstrap.php`; (2) `op_sessions.recipe_id_fk` IS FK-constrained to `ref_recipes.id` (sentinel `recipe_id_fk=9999` FK-violates — test used real id=6); (3) `op_sessions.row_hash` has UNIQUE constraint — distinct hash per insert mandatory (`SHA2('T1',256)`, `SHA2('T2',256)`, …). **Phase 2-5 (UI + retro-link + auto-link extensions + sb-board.php + vessel SVG rework) NOW UNBLOCKED.** | RESOLVED 2026-05-29 |

| 25 | (architectural CSS debt, not a scrap) `:root` design-token redeclaration inside `public/css/sb-board.css` | The global `:root` token block in `app.css` (single-source palette since `3c57e5b`, see row 11 SCRAPPED) | **Logged 2026-05-31 from the atoms 14+15 RULE-2 review (deferred P2, NOT introduced by this atom — pre-existing).** `sb-board.css` redeclares `:root` tokens instead of inheriting the shared `:root` layer → divergence smell (two homes for the same palette fact, board can silently drift from the site palette). Fix = scope the board overrides under `body.sb-board` and let everything else inherit `:root` (same pattern row 11 used to converge `body.home`/`body.auth`). P2 architectural; non-blocking; do in a dedicated CSS-hygiene pass or opportunistically on the next sb-board.css edit. | OPEN (CSS debt, scope under body.sb-board) |
| 26 | ✅ **SCRAPPED 2026-05-31 (atom 18, commit `f401f84`)** — the redundant bottom `.sb-batch-list` table on `sb-board.php` (a 2nd rendering of `sb_observed_in_flight()`) + its dead CSS (`sb-batch-list*`, `sb-phase-pill*`, `sb-observed-card*`, `sb-batch-row*`, `sb-observed-badge`, `sb-assemblage-chip`) | The per-zone chip packing (`sb-chips-pack` flex-wrap, all in-flight lots as minimal one-line link chips) — now the SINGLE rendering of in-flight observed lots | **Logged + SCRAPPED 2026-05-31 from the atom 18 build (RULE-1: collapsed TWO renderings of one fact to ONE).** Atom 18 reworked `sb_render_observed_card()` → `sb_render_observed_chip()` (minimal "{recipe} · #{batch} · {vessel}" link chip; provenance/abandoned/assemblage as chip STYLING + ⋈ glyph, no text/badge/heartbeat/meta-dots) packed unbounded per zone (`sb-chips-pack`, no cap/expander — every in-flight lot visible). The zones became the single source of in-flight rendering, so the bottom list table (which re-rendered the same `sb_observed_in_flight()` output) was dead duplication and was deleted along with its now-orphaned CSS. Chips link to `/modules/sb-batch.php?recipe=<id>&batch=<enc>` (atom 19 stub). `sb_observed_in_flight()` gained a one-field `recipe_id` forward (chips need it). Retro-link admin trigger relocated into a `.sb-board-controls` flex row beside the sync pill (no fragile offset). **Gate met at scrap time:** zones now render all in-flight lots; nothing else consumed the bottom list. Kept here as audit trail. | SCRAPPED 2026-05-31 |
| 27 | (P3 backlog note, NOT a scrap — foreshadow) board-level "fully empty / bd_* missing" diagnostic | (future) a board-wide empty-state / data-health banner | **Logged 2026-05-31 from the atom 18 build (RULE-2 acceptable-finding → P3 backlog).** The old global empty-state diagnostic ("données bd_* semblent absentes") lived in the scrapped bottom `.sb-batch-list` table (row 26) and has NO replacement. Reviewer ruled this ACCEPTABLE because each zone now carries its own empty state — but there is no longer a single board-level "everything empty / bd_* source missing" signal. If the operator later wants a board-wide data-health banner (distinguish "no production right now" from "bd_* tables unreadable"), build it as a top-of-board diagnostic reading the same `sb_*()` API. NOT blocking; no gate. | FORESHADOW (P3, no gate) |

| 28 | (correctness debt, NOT a scrap — OPEN bug to fix when chosen) `public/modules/form-brewing.php` parsed-ingredients orphan-on-shrink | n/a — one-surface addition to the EXISTING orphan-delete loop (extend it to cover `bd_brewing_ingredients_parsed` by `line_idx`) | **Logged 2026-05-31 from the brewing REOPEN/PREFILL arc (SHIPPED + LIVE, 2-file targeted deploy, UNCOMMITTED).** The edit-mode reopen (`?edit=<brewday_id>`) makes edit-resubmit common. The existing orphan-delete-on-shrink loop covers `bd_brewing_gravity_v2` + `bd_brewing_timings_v2` BY BREW, but NOT the parsed ingredients table BY `line_idx` → deleting an ingredient line on edit+save leaves a high-`line_idx` orphan row in `bd_brewing_ingredients_parsed`. Latent before reopen (re-entry was rare); reopen surfaces it. **Fix = add the parsed table as a surface to the existing config loop** (same tombstone-before-delete `action='update'`+`_tombstone` pattern, parameterized, guarded on non-empty written set), keyed by `line_idx` rather than brew. NOT blocking backlog entry today (only bites the shrink-an-ingredient-on-edit path; the common backlog path is new-batch entry which has no prior lines to orphan). Build when the orphan starts mattering OR opportunistically on the next form-brewing.php write-path touch (e.g. pilot 6). | OPEN (correctness debt, no gate) |

**Evaluation cadence:** at every commit-stage review (RULE 2), the code-review agent's scrapping half feeds this table — new redundancies it finds get appended here with a gate; existing CANDIDATEs whose gate is now met get promoted to READY and surfaced to the operator. Re-verify a gate (grep routes/includes, confirm replacement coverage) against the live repo/DB before promoting CANDIDATE → READY → SCRAPPED.

#29 — (open, temporary-debt) packaging dashboard INLINE legacy-loss term. The pertes-dashboard (arc A5) must compute beer_loss_hl = v_bd_packaging_v2_vendable.loss_kpi_hl + bd_packaging_v2.loss_liquid_other_units/100, because A3's mig-233 view DROPPED the legacy `loss_liquid_other_units` litres bucket that holds ~99% of historical liquid loss (118.8/150.5/106.4/83.0/107.5/16.2 HL 2021-26). GATE to remove the inline term: A-LT2 view-codegen resumes, resolves loss_liquid_other_units's catalog fate (the G1 blocker), backfills stored loss_kpi_hl, and folds the legacy term into the view's loss arm. Until then the inline term is MANDATORY (omitting it understates loss ~10×). Cross-ref packaging-losses-convention.md §PERTES-DASHBOARD.

#30 — (OPEN latent bug, NOT a scrap — quick fix, low priority) `bd_upsert` UPDATE-path accesses `['id']` when no insert id exists → `PHP Warning: Undefined array key "id" in /var/www/maltytask/app/db-write-helpers.php on line 246`. Surfaced 2026-06-01 during the mig-254 `rm_recompute_rollup` reclaim test (NOT introduced by mig 254 — pre-existing). The upsert STILL SUCCEEDS (operator's 170kg recorded correctly); prod suppresses warnings to logs so the live RM per-pallet form (which calls `rm_recompute_rollup` on every line add) is functionally unaffected. But the warning pollutes logs on EVERY stocktake line add. FIX = guard the `['id']` read on the UPDATE branch (no insert id on an UPDATE). No gate; operator's call on priority. Deferred-cleanup list.

#31 — `app/svg-vessels.php` RETIRED 2026-06-07 (ui-ux arc C2 convergence, commit `9ed0f6f`). Superseded by `app/svg-tanks.php`, now the ONE canonical vessel library — all four `svg_vessel_cct/bbt/kettle/packaging_line` reimplemented there in gravure with signatures + CSS hooks verbatim. The file survives ONLY as a tombstone: header note + `require svg-tanks.php` + `function_exists` no-op stubs (zero-risk fallback for any stale include). **DELETE GATE (both must hold): BSF-exit Phase 6 has landed AND `grep -rn "svg-vessels" public/ app/` returns ZERO hits.** Re-verify the grep against the live repo before deleting. Cross-ref ui-ux-quality-rework-arc.md §C2. | RETIRED (tombstone in place; physical delete gated)

#32 — `public/modules/sku-costs.php` (standalone "Coût par SKU" page, ref_pages id=12) RETIRED 2026-06-10 (maltyweb `e72e64e`, pushed; mig **318** sets `ref_pages.sku-costs is_active=0`). **File DELETED (operator-authorized; git-recoverable), NOT just deactivated** — supersedes the register #2 RULING's "is_active=0 + log the .php" with an outright delete. Superseded by the Financier "Coût par SKU" tab (`fin-panel-sku`, `financier.php`) which now renders the per-SKU cost table **SERVER-SIDE reusing sku-costs.php's EXACT `ref_sku_bom` query VERBATIM (no re-derivation = no divergence)**: 3 KPIs (active SKUs / recipes / médian CHF·HL), per-recipe-grouped rows (brewing/packaging/total CHF + CHF·HL), the anomaly flags (keg-inverted / outlier / extreme-bom-line), BOM freshness stamp, SKU→`sku-cost-detail.php` links, + client-side recipe/format/classification filters (no reload); query try/catch-wrapped so a DB hiccup can't fatal the sibling COP/COGS modules. **`sku-cost-detail.php` KEPT** (drill-down; back-links repointed to `/modules/financier.php`). 0 dangling hrefs. **GATE MET at scrap time:** panel covers the list + 0 inbound routes. Live-verified (Opus-independent): mig 318 applied, is_active=0, 154 active SKUs carry BOM cost, financier.php `php -l` clean, HTTP 302 (no fatal). **⚠️ Visual-acceptance still OPERATOR-PENDING** (build session's local→site browser was flaky; the manager-render screenshot is the load-bearing UI gate — same gap pattern as other role-scoped pages; not yet eyeballed in a manager browser). | RETIRED (file deleted, deactivated; visual-acceptance operator-pending)

#33 — (was STANDING DIVERGENCE — two read-paths over ONE sales fact; **DIVERGENCE NOW CLOSED on the KPI side, 2026-06-12**) **Legacy `inv_sales_bc` as a KPI read-source + its 6-month loader.** Logged 2026-06-12 from the sales Ventes tranche (#272-276); **EXECUTED + VERIFIED + PUSHED same day** (migs 344+345; commits `3b8dead`+`fb61ba5`+`9cc52b3`; HEAD `9cc52b3`; md5 local↔VPS matched). **✅ LOADER SCRAPPED:** `kpi_sales_load_bc_periods` DELETED (PM-verified: function def GONE from VPS `app/kpi-handlers.php`; the 11 residual `inv_sales_bc` string hits are all DOC COMMENTS + the retired #94/#100 stub-messages, ZERO live SELECT callers). All ~18 live sales tiles repointed to the **canonical `inv_sales_ledger`** via two new shared loaders — `kpi_sales_load_ledger_prod()` (L9024, production-scoped via `KPI_SALES_PROD_FILTER`) and the NEW `kpi_sales_load_ledger_all()` (L9058, all-sales no-prod-filter, plain-SUM CHF, negate HL/units). **Per-tranche outcome:**
> - **Tranche 1 (7 prod tiles, stayed data_ready=1):** #88 hl_sold_period, #87 units_sold_sku, #93 top_skus_volume_revenue, #91 revenue_by_family, #96 contract_vs_own_brand, #99 revenue_per_hl_trend, #102 seasonal_demand_curve — repointed to `_prod`; reconcile to prod universe (June MTD 765.84 HL / May 1097.38); #99/#102 now span 66 months (was bc's 6); #87/#93 bar series given `period`=SKU label (the unlabeled-bar fix).
> - **Tranche 2 (3 tiles, dropped→data_ready=0, OPUS-VERIFIED, re-flipped live mig 345):** #86 revenue_month (June all-sales 239,354 CHF, independently matched), #92 top_customers (JOIN `ref_customers.name`; real top-3 Bevanar/Dorga 28,574 · Jardins de Louis 19,926 · Cobra-MANOR 16,503, no NULL names), #187 cogs_pct_revenue (source_domain=cogs; inline bc SELECT repointed to ledger → 11.41% PERIOD-MATCHED June COP 27,306 ÷ June rev 239,354; repoint also FIXED a latent gap — bc had no June revenue so pre-repoint #187 would null for a June COGS month).
> - **Tranche 3:** #90 yoy_pace repointed to `_all` → real N-1 (66 months unlocks YoY); #97 avg_order_value UNCHANGED (already bc-free, reads inv_sales_orders/Shopify).
> - **2 RETIRED (is_active=0):** #94 sales_by_channel (superseded by #275 hl_by_trade_channel on the ledger); #100 discount_rebate_rate (blocked — `discount_amount_chf` not on the ledger; data_ready=0). Both function bodies stubbed (graceful error, no fatal if dispatched).
>
> **✅ KPI-SIDE PARALLEL-STORE DIVERGENCE CLOSED:** the WHOLE sales KPI surface (all ~18 live tiles + the new #272-280) now reads ONE source = `inv_sales_ledger`.
> **🔴 TABLE-DROP STILL PENDING (the one remaining gate):** `inv_sales_bc` the TABLE is RETAINED — **3 operational consumers still read its taproom slice: `public/modules/fg-stock.php`, `public/modules/expeditions.php`, `public/modules/tap-shop.php`** (out of scope this build; PM-grep-confirmed 2026-06-12). Drop the TABLE only after those 3 are migrated/repointed. | LOADER SCRAPPED ✅ 2026-06-12 (table-drop gated on the 3 operational consumers fg-stock/expeditions/tap-shop)

#34 — ✅ **SCRAPPED 2026-06-19 — the v1→v2 migration-BRIDGE artifacts (distinct from the v1-TABLES DROP arc; that = Stage 4, gated, still open).** Executed + verified; commits LOCAL-unpushed (maltytask `10c36c6`, maltyweb `3bb9453`). **(1) DELETED:** `data/RawDB-normalized.xlsx` + `data/RawDB-normalized.xlsx.bak-mig236` (removed BOTH locally in maltytask AND on the VPS at `/var/www/maltytask/data/` — the **live-source hazard is now CLOSED**: a stale normalized RawDB sitting on the VPS could be re-ingested by mistake, the same divergence smell as a parallel data store) + `scripts/normalize-rawdb.py` (`git rm`; the one-time v1→v2 normalizer that fed the mig236 contract-brewday backfill — consumer-less now that the migration landed). **(2) KEPT-AS-HISTORICAL (PM option A, NOT scrapped):** the four `scripts/python/ingest_bd_*_v2.py` — verified `gen_mig236_contract_brewday.py:32` imports `_row_hash,_s,_i,_dt,_date` from `ingest_bd_brewing_v2`, so the v2 ingest helpers + the mig-236 generator stay (still a live import dependency). **(3) schema_meta CORRECTED via mig `399_schema_meta_bd_v2_live_writer.sql`** (maltyweb `3bb9453`, applied on VPS, recorded in `schema_migrations`) — 7 rows still named the dead `normalize-rawdb`/ingest as `writer_script`; repointed to the actual web saisie-form writers: `bd_brewing_brewday_v2`/`_gravity_v2`/`_timings_v2`/`_ingredients_v2` → `form-brewing.php`; `bd_fermenting_v2` → **`form-fermenting.php`** (NB: real filename is `form-fermenting.php`, NOT the earlier-guessed "form-fermentation.php"); `bd_packaging_v2` → `form-packaging.php`; `bd_racking_v2` → `form-racking.php`. `bd_brewing_ingredients_parsed_v2` LEFT as `parse_bd_ingredients.py` (derived, out of scope). **Gate met at scrap time:** bridge artifacts consumer-less + the live-source hazard removed from disk on both sides. **Cross-ref** v1-bd-tables-decommission.md (the v1 TABLES DROP is the SEPARATE Stage-4 arc, still gated on soak + re-grep + operator go-ahead — do NOT conflate). | SCRAPPED ✅ 2026-06-19 (bridge artifacts + live-source hazard; v1 TABLES DROP is separate/still-gated)

#35 — ⚠️ **OPEN ITEM OBSERVED (not a scrap, not mine to touch) — a PARALLEL session has an in-flight uncommitted confirmation-email migration.** During the mig-399 apply (2026-06-19), `migrate.php --status` showed `397_confirmation_email_settings.sql` **PENDING + UNTRACKED** (a parallel session's WIP). I deliberately did **NOT apply 397** — applied only 399 via direct DML + a manual `schema_migrations` INSERT, leaving 397 for its owner. **DO NOT touch / re-number / apply 397** — it belongs to the confirmation-email session. (Mirrors the 397-pending flag already in the index header re mig 398.) Re-`migrate.php --status` at build-start; if 397 is still pending, the owning session hasn't landed it yet. | OPEN (parallel-session WIP; hands-off)

#36 — ✅ **DEFECT-CLOSURE 2026-06-22 — 6 hardcoded `'Europe/Zurich'` sites centralized to `app_timezone()`/`display_local()` (display-timezone centralization, LIVE on VPS).** Per the TIMEZONE ARCHITECTURE ruling (2026-06-22, conventions-and-helpers.md §timezone). Two new helpers in **`app/settings.php`** (co-located with date-format family): `app_timezone():string` (cached) + `display_local(string|DateTimeInterface|null,?string $fmt=null):string` (parse-as-UTC→app_timezone→format; defensive→'' never fatals). Setting `system_settings` general/`app_timezone='Europe/Zurich'` applied SURGICALLY (idempotent upsert, NOT migrate.php — dirty shared tree); mig `db/migrations/438_app_timezone_setting.sql` for provenance + manually recorded in `schema_migrations`. **6 sites rerouted** (ruling anticipated 3, build correctly found 6): kpi-recap-schedule.php L16, production-targets.php L93 (→`app_timezone()`); kpi-handlers.php L1077, mon-tableau.php ×2, financier.php L236, expeditions.php L2416 (display→`display_local`, biz-logic→`app_timezone`). **financier.php L236 was a LATENT +2h BUG** (UTC parsed as Zurich) — FIXED; `…22:19:31Z` now renders `11.06.2026 00:19` CEST, browser-confirmed. VERIFIED: php-l 7 files clean, surgical per-file rsync md5 local==VPS MATCH, chown maltytask:www-data 644, php-fpm reloaded, CLI DST smoke (Jan→CET+1/June→CEST+2) + edge-cases→'', authenticated browser smoke clean. NOT committed (Opus commits); deploy live. **🟡 PENDING FOLLOW-UP (separate sweep, NOT this build):** ~12 PHP surfaces still raw-UTC (charges-bc, bom-review, email-orders, sessions, form-packaging, sb-mother, qa, reglages-generaux last_login_at, salle-de-controle, warehouse) + local date-helper fns (sbb_date_fr/sd_time/wh_date_fr/smh_date_fr — no tz conversion) + 2 JS spots (financier.js sealedAt, salle-fournisseurs.js last_message_at parsed without 'Z'). financier.php COP-freshness `filemtime()` intentionally server-local, left as-is. **DURABLE CONVENTION:** future builds CALL `display_local()`/`app_timezone()`, never hardcode `'Europe/Zurich'` or raw-display a stored UTC value (JS: parse with explicit 'Z'). Cross-ref conventions-and-helpers.md §TIMEZONE ARCHITECTURE (full as-built) + §VERIFIED HOUSE HELPERS (display/timezone family). | DEFECT CLOSED ✅ 2026-06-22 (6 sites centralized + 1 latent +2h bug fixed; raw-UTC sweep = pending follow-up)
