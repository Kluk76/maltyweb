# Racking-form REDESIGN — WS1-4 (operator-driven, 2026-05-27) — CANONICAL RULING

## §TRANSFER NOMENCLATURE — "Transfert(s)" is canonical; NEVER "soutirage"/"mise" (PM consult 2026-06-15)
> House-canonical term for a beer moved tank→tank = **"Transfert" / "Transferts"**. "Soutirage" was EXPLICITLY RETIRED (form-racking.php:6 "renamed Soutirage→Transferts in all user-facing labels"; mon-tableau.php:275 operator ruling 2026-06-07 "soutirage = filling/packaging in their usage — confusing"). **"mise"/"mises" is the PACKAGING verb in this app** ("mise en fût" = filling kegs, tracker #42 `kpi-handlers.php:4683`) — do NOT use it for transfers (collision = the exact ambiguity operator flagged 2026-06-15).
> FIXED 2026-06-15: "Brassage — résumé du jour" widget metric (`app/kpi-handlers.php:3042`) had `unit=>'mises'` on the Transferts count → corrected to `unit=>'transferts'` (label `'Transferts'` was already correct; this is a unit-token fix, NOT a relabel). `$rackCount` = COUNT(*) of bd_racking_v2 rows/day = transfer-EVENTS (not distinct beers; a beer racked twice = 2). Strip convention = label echoes count-noun unit (Brassins/brassins, lots). Recap-email mirror (`kpi-email-render.php:204` group label 'Transferts' already correct) projects units from handler meta → fixing the handler line propagates; never hand-edit the email unit. Pure UI copy, zero COGS/derivation impact.

## §"BBT VIDE" v2 EXPANSION — scrap-forever + process-loss booking + 2.5HL auto-empty threshold (PM RULED 2026-06-09 → ✅ BUILT 2026-06-09 → ✅ DEPLOYED + MIGS 296/297 APPLIED + LIVE IN PROD 2026-06-10, see §BBT VIDE v2 below for AS-BUILT + verified live state; NB revised verdict ACCEPTED a `bbt_vide_scrapped_hl` column, overriding the no-column ruling in lines 5/11)

> **🧪 SMOKE-TEST GATING NOTE (recorded 2026-06-10, Playwright/webapp-testing).** The 4 operator checks span 3 pages with these live gates (VPS-verified): `form-racking.php`→`require_page_access('saisies')` **min_role=operator**; `tanks.php` (Pertes report)→`require_page_access('fermentation')` **min_role=operator**; `salle-de-controle.php?sec=pertes`→`require_login()` only (ANY logged-in user can VIEW the «Seuil BBT vide» row; write gated to manager/admin). **The permanent smoketest acct `users.id=16` is a VIEWER (`role='viewer'`, `access_preset_id_fk=NULL`, no `user_page_access` grants) → it CANNOT reach saisies or fermentation → it can drive ONLY check #3 (the pertes settings VIEW).** Checks #1/#2/#4 need an OPERATOR-or-higher login (operator suffices) — either elevate id=16 to operator for the run (data-only, revert after) or use a real operator account the operator supplies. **Check #1 (edit a BBT-vide racking row → pre-tick + «Talon ignoré : X.XX HL») CANNOT be exercised today: `bbt_vide_scrapped_hl IS NOT NULL` count = 0, so there is NO saved BBT-vide row to edit-prefill** — it needs a real operator to first SAVE a racking event with "BBT vide" ticked (or `bbt_vide_scrapped_hl>0`). Until then #1 is unverifiable via edit-mode; the NEW-tick path can only be exercised by actually saving one. The smoketest creds file is `/home/kluk/projects/maltytask/secrets/maltyweb-smoketest.env` (MALTYWEB_USER/MALTYWEB_PASS, 600, gitignored); WSL network workaround (ssh -L 8443 + Node CONNECT proxy + `--proxy-server`, login path `/login.php`) is in the `webapp-testing` skill reference + user-auth-admin-arc.md §Smoke E2.
> **✅ GATING BORNE OUT (2026-06-10 live smoke — full results in §BBT VIDE v2 below):** all 3 predictions held. #3 (pertes VIEW) PASSED via viewer id=16. #1/#2/#4 required operator-or-higher → id=16 was temporarily elevated viewer→operator then REVERTED to viewer same session (no grant rows; role-floor + NULL preset). Two refinements learned: (a) the «BBT vide» checkbox is **JS-CONDITIONAL** (revealed only after type=BBT + source beer + a blend-candidate card selected; `<label id=rf-bbt-vide-row hidden>`, field `name=bbt_vide`, visible label "BBT vide — ignorer le résiduel suivi") — NOT always-present; (b) the **"Talon ignoré : X.XX HL"** string is the EDIT-MODE prefill message (lives in racking-form.js, shown re-opening a saved BBT-vide row), NOT the checkbox label — so it is only verifiable once a real override row exists. The positive paths (#1 edit pre-tick + persist, #2 sub-2.5HL exclusion from picker, #4 populated «Écart» line) STILL await the operator's first real BBT-vide save.
> Kouros expanded BBT-vide 3 ways. PM rulings (verified live against `app/tank-simulator.php` + `salle-de-controle.php`):
> **(1) SCRAP-FOREVER / persistence.** The sim is EVENT-SOURCED per-event; forcing this row's `blend_hl=0` makes the racking event declare "no prior contents" → the phantom CANNOT survive into future `tank_bbt_composition()` (the destination BBT is re-stated to `$netRacked` only; nothing carries the old residual forward — there is no separate running-balance store). So scrap IS forever AT THE SIM LEVEL already. The ONLY leak is the EDIT-MODE un-scrap (re-select candidate re-fills blend_hl + un-ticks). FIX = make edit-mode prefill respect the stored zero: if a saved row has `blend_hl=0` AND a destination BBT that the live tracker still shows as occupied, prefill the checkbox CHECKED + blend_hl readonly-0 (DERIVE the empty-assertion from the persisted `blend_hl=0`, do NOT add a boolean column). VERDICT: NO first-class boolean col — `blend_hl=0` IS the honest record; a parallel flag = parallel-store smell. Edit-prefill reads the snapshot, not the tracker, for the scrap state.
> **(2) BOOK AS PROCESS LOSSES.** RECONCILES the earlier "no loss event" caveat: the earlier ruling was scoped to MASS-BALANCE physicality (the residual was never really there → don't double-count it as physical loss out of a real volume). Kouros now wants it VOLUMETRICALLY RECONCILED as process loss so the discarded HL is visible, not silently vanished. PM verdict: this is a **VOLUMETRIC reconciliation, NOT a valued WIP write-down. ZERO COGS/WIP monetary impact.** Rationale: the residual is PHANTOM — it was a derivation discrepancy, the liquid did not physically exist, so there is no real brewing-CHF/HL WIP to write down (you cannot expense beer that was never in the tank). It is a derivation-correction loss bucket, DISTINCT from the physical `loss_dest_hl`/Pertes path (which DOES value real spilled liquid). Do NOT route it through `loss_dest_hl` (that decrements a real volume and feeds physical-loss metrics as if liquid was spilled). Surface the scrapped HL in a derivation-correction/phantom-loss line for operator visibility (loss-metrics display only), computed = (tracker residual at capture − 0) i.e. the zeroed blend_hl magnitude; it does NOT touch brewing CHF/HL, COP, or beer-tax. If Kouros insists it appear in the SAME Pertes report as physical losses, it goes in as a SEPARATE labelled cause ("écart de dérivation / BBT vide"), never folded into physical loss_dest.
> **(3) 2.5 HL AUTO-EMPTY THRESHOLD.** `BBT_EMPTY_THRESHOLD_HL = 2.5` ALREADY EXISTS as a hardcoded class const in `app/tank-simulator.php` L45 — the sim ALREADY nulls a BBT when `volume_hl < 2.5` after losses/packaging draws (L470, 589, 608, 652). Kouros's ask = (a) promote it to a SETTING + (b) extend it to the blend-CANDIDATE picker (currently the picker shows any occupied BBT regardless of how small).
>   - **SETTINGS HOME CONFIRMED LIVE:** the "QA/QC losses settings section" Kouros named EXISTS = **`salle-de-controle.php?sec=pertes`** (Salle de contrôle = QA/QC family), reads/writes **`commissioning_settings WHERE section='pertes'`**. It ALREADY holds `pertes_racking_loss_hl` + `pertes_packaging_loss_hl` (the racking/packaging fixed-loss HL settings Kouros referenced) alongside `pertes_*_warn_pct`. ADD a new key **`pertes_bbt_empty_threshold_hl`** the SAME way: (i) mig INSERT one `commissioning_settings` row (section='pertes', key_name='pertes_bbt_empty_threshold_hl', value_num=2.5, label_fr e.g. «Seuil BBT vide (talon)»); (ii) add the key to the `$allowedKeys` whitelist in `update_pertes_config` (salle-de-controle.php L1221); (iii) add the HL-input render row (it's a `_hl` key, joins `$hlKeys` at L4150); (iv) TankSimulator constructor (L232 query) extends the `key_name IN (...)` list + a new private prop replacing the L45 const default-coalesced. THIS IS A MIGRATION (one settings-row INSERT) — the only migration of the three asks.
>   - **CANDIDATE FILTER LOCATION:** gate it in **`tank_bbt_composition()`** (app/tank-simulator.php L1150) — add `if ($totalHl < $threshold) continue;` so sub-threshold BBTs never enter the candidate list AT ALL (the function is the single producer feeding `window.BBT_BLEND_CANDIDATES`; gating here covers form-racking.php's picker AND any future consumer). Threshold must be PASSED IN (it's a free function, no `$this`) — either pass the loaded value as a param or have the caller in form-racking.php filter post-call using the sim's threshold getter. Do NOT gate in racking-form.js (client-side hide leaves the candidate in the payload — refuse-don't-hide).
>   - **CONSISTENCY (1)+(2)+(3):** auto-empty (sub-2.5) == automatic BBT-vide. When a destination's prior residual is sub-threshold, the racking event MUST behave identically to a manual tick: `blend_hl` forced to 0 (so the sim's fresh-fill branch fires) AND the same phantom-loss volumetric line booked. So the SERVER-SIDE coercion in form-racking.php becomes: `if bbt_vide ticked OR (selected-destination's tracker residual < threshold) → blend_hl=0`. The threshold check is the AUTOMATIC arm of the same correction; both produce the identical persisted state (`blend_hl=0`) and the identical phantom-loss booking. No separate code path, no divergence.
> **MIGRATION VERDICT: ONE migration** = a single `commissioning_settings` INSERT for `pertes_bbt_empty_threshold_hl` (NNN_pertes_bbt_empty_threshold.sql; no DDL, no new table → no schema_meta row needed, it's a data row in an existing table; remember migrate.php does schema_migrations bookkeeping, no self-INSERT, no bare SELECT). NO boolean column on bd_racking_v2 (rejected — blend_hl=0 is the record). NO loss table (phantom-loss is display-only/derived). **COGS/WIP VERDICT: ZERO monetary impact** — phantom volume is unvalued; pure volumetric reconciliation. **EQUIP: `sql` (the settings-row mig + the IN-list + whitelist) + `coder` (PHP coercion/threshold-load/edit-prefill) + `ui` (the new HL settings-row render + edit-mode checkbox prefill) + `webapp-testing` (deployed-page smoke: settings save + a sub-threshold candidate disappears from picker, login-gated).** Verify threshold-load coalesces to 2.5 when row absent (safe bootstrap, matches the existing pertes pattern L244).
> **OPEN BEFORE BUILD:** confirm with Kouros whether the phantom-loss line should appear in the Pertes par batch report (tanks.php / loss-metrics.php) as a distinct cause, or stay purely on the racking-form confirmation. That placement is a display choice; it does not change the COGS-zero ruling.

## §BBT VIDE v2 — ✅ DEPLOYED + MIGRATED (296+297 APPLIED) · LIVE IN PROD · awaiting operator smoke + first real override-row (verified 2026-06-10)
> **STATE 2026-06-10 (Kouros-verified live on VPS, ground-truth probe): the deploy gate steps 1–3 are DONE; the feature is FULLY LIVE in production. Only the human/operator smoke verification (gate steps 4–5) remains.** VERIFIED FACTS 2026-06-10: maltyweb HEAD=`281082e`, `main`==`origin/main`; commits `a10431a` (BBT-vide v2) + `281082e` (chore/tour) present+pushed. `public/modules/form-racking.php` ON THE VPS carries the feature (16 BBT-vide markers). **Mig 296** (`bd_racking_v2.bbt_vide_scrapped_hl DECIMAL(8,3) NULL`) APPLIED — column live. **Mig 297** (`commissioning_settings` row `pertes_bbt_empty_threshold_hl`=2.5, section='pertes', is_active=1) APPLIED — row live & confirmed. `migrate.php --status`: 0 pending. **0 rows in `bd_racking_v2` have `bbt_vide_scrapped_hl IS NOT NULL`** → feature live but NOT yet exercised by a real operator save (no first override-row yet). NEXT: operator smoke (the 4 checks in TOMORROW'S REVIEW GATE below) + first real BBT-vide save. (Historical: built+committed+pushed 2026-06-09 evening; the concurrent session finished + committed its own work mid-evening; tree-watcher confirmed ~15min stability; then BOTH workstreams committed + PUSHED.)
> - **a10431a** `feat(racking): BBT-vide v2` — this session's 8 files + migs 296/297. By commit-time `form-racking.php` was foreign:0 vs HEAD (the parallel session's `submitted_by` hunks had ALREADY landed in their own commit c8e895c), so BBT-vide committed cleanly with ZERO intermingling — the entanglement resolved itself by ordering, no manual untangle.
> - **281082e** `chore(tour): auto-check Visite guidée coverage after ref_pages migrations` — 3 loose-end files the parallel session left uncommitted (migrate.php now runs tour-gap-check.php after any ref_pages migration; new scripts/tour-gap-check.php; visite-guidee.php cards). Committed ON THEIR BEHALF to keep the tree clean + attributed; flagged for their review.
> Parallel session's own committed work (mid-evening, c8e895c + others): journal-saisies, submitted_by_user FK, expéditions/fulfilment, migs 298–301.
> Tree now clean except a stray untracked `data/merge-suggestions-2026-06-08.md` (non-code, left alone).
> **⚠️ ONE DELIBERATE DIVERGENCE FROM THE ORIGINAL RULING — ACCEPTED by Kouros's REVISED verdict:** the original ruling (line 5 + line 11) REJECTED a boolean/column on bd_racking_v2 ("blend_hl=0 IS the record"). The **revised verdict ACCEPTED a column** — `bbt_vide_scrapped_hl DECIMAL(8,3) NULL` — storing the SCRAPPED MAGNITUDE (not a boolean). Rationale this is NOT parallel-store smell: blend_hl=0 records the destination-occupancy correction; `bbt_vide_scrapped_hl` records the *magnitude of the phantom that was scrapped* (needed for the volumetric loss-reconciliation line + robust edit-mode prefill of the figure). The other 3 revised-verdict axes also as-built: **beer|batch key** (loss-metrics, NOT recipe_id_fk — see (loss-metrics) below), **unvalued loss** (ZERO CHF), **threshold→setting** (mig 297). NULL = genuine fresh-fill (scrapped 0); >0 = a real BBT-vide scrap.
> **AS-BUILT (2 migrations + 7 PHP/JS/CSS files; all 5 edited PHP `php -l` PASS on VPS):**
> - **Mig 296** `296_bd_racking_v2_bbt_vide_scrapped_hl.sql` — ALTER bd_racking_v2 ADD COLUMN `bbt_vide_scrapped_hl DECIMAL(8,3) NULL CHECK(>=0 OR NULL)`. INSTANT DDL. NOT applied.
> - **Mig 297** `297_pertes_bbt_empty_threshold.sql` — INSERT commissioning_settings section='pertes' key='pertes_bbt_empty_threshold_hl' value_num=2.5 unit_fr='HL' default_num=2.5. NOT applied. (Exactly the "one settings-row INSERT" the ruling predicted.)
> - `app/tank-simulator.php` — L45 const KEPT as fallback; new prop `$bbtEmptyThresholdHl` loaded in ctor (key added to the IN-list); the 4 runtime uses (L470/589/608/652) now read the prop; public getter `bbtEmptyThresholdHl()`; `tank_bbt_composition()` gained a `float $emptyThresholdHl` param + `if($totalHl < threshold) continue;` candidate gate (exactly as the CANDIDATE FILTER LOCATION ruling specified — gated in the producer, threshold passed in since it's a free function).
> - `public/modules/form-racking.php` — SERVER capture: `bbt_vide` ticked OR dest residual < threshold → look up the selected bbt_number's `total_hl` from the server-side sim composition, write to `bbt_vide_scrapped_hl` (NULL when 0 = genuine fresh-fill); `blend_hl=0` unchanged. **EDIT-MODE INVARIANT honored:** preserves the STORED magnitude when a row already had >0 AND the box stays ticked (re-derives only on a NEW tick) — fixes the v1 RESIDUAL-OVERRIDE edit-mode un-scrap leak (§ below). `bbt_vide_scrapped_hl` threaded into $row + $editPrefill + RF_EDIT_PREFILL json.
> - `public/js/racking-form.js` — edit-mode prefill: when `RF_EDIT_PREFILL.bbt_vide_scrapped_hl>0`, pre-tick the box, blend_hl=0 readonly, show note "Talon ignoré : X.XX HL". Pre-tick DERIVED from >0, NOT from blend_hl=0 (correct: blend_hl=0 is ambiguous with fresh-fill).
> - `public/modules/salle-de-controle.php` — `$allowedKeys` + `$hlKeys` include `pertes_bbt_empty_threshold_hl` → editable HL input on QA/QC `?sec=pertes` page (exactly the (ii)+(iii) of the ruling).
> - `app/loss-metrics.php` — `_loss_load_rack_map()` SELECT gained `SUM(COALESCE(r.bbt_vide_scrapped_hl,0))` inside the existing GROUP BY (**beer|batch key, NOT recipe_id_fk** — per the revised verdict); `loss_metrics_for_batches()` + `loss_metrics_per_beer()` expose `sum_bbt_vide_scrapped_hl`. **`rack_loss_pct` numerator UNCHANGED** (still loss_source+loss_dest only → phantom NEVER folded into physical loss). ZERO CHF.
> - `public/modules/tanks.php` — per-batch Pertes report emits a DISTINCT sub-row «Écart de dérivation (BBT vide)» when >0, annotated "réconciliation volumétrique · 0 CHF", NOT summed into physical loss. (Resolves the line-12 OPEN: Kouros chose the distinct-cause-in-Pertes-report placement.)
> - CSS: racking-form.css `.rf-bbt-vide-note`; tankboard-pertes.css `.ppb-row--bbt-vide`.
> **CROSS-SESSION ENTANGLEMENT — RESOLVED CLEANLY (no manual untangle needed).** `form-racking.php` was co-edited by the parallel "journal des saisies / `submitted_by_user_id_fk` identity" session, but that session committed its `submitted_by` hunks FIRST (c8e895c), so by the time the watcher fired this file was foreign:0 vs HEAD and BBT-vide committed clean (a10431a). The two features coexist in the file at non-overlapping line ranges (different columns, different functions). Parallel session's full footprint: migs 295 (applied) + 298–301 (committed/applied), journal-saisies/feed/detail, submitted_by_user FK, expéditions/fulfilment, tour-gap-check.php.
> **MIGRATION COHERENCE — VERIFIED for tomorrow.** migrate.php detects pending PER-FILE (glob + sort_string + "not in schema_migrations"), NOT by high-water mark → 296/297 apply cleanly tomorrow even though 298–301 are already applied/committed. **No out-of-order hazard.** NB: migrate.php ITSELF was modified (committed in 281082e) to invoke tour-gap-check AFTER any ref_pages migration — neither 296 nor 297 touches ref_pages, so that hook will NOT fire for this run.
> **📋 TOMORROW'S REVIEW GATE (2026-06-10) — confirmed, unchanged:** (1) `git status` clean-confirm + `sudo php scripts/migrate.php --status` on VPS (EXPECT: 296,297 pending; 298–301 applied); (2) deploy via `bin/deploy.sh --apply` (tree clean now → no leak risk); (3) `sudo php scripts/migrate.php` → applies 296 then 297; (4) live smoke (login-gated): edit a BBT-vide row → box pre-ticks + «Talon ignoré : X.XX HL» shows; a sub-2.5HL BBT is ABSENT from the blend picker; QA/QC `?sec=pertes` shows the new «Seuil BBT vide» HL setting; Pertes report shows «Écart de dérivation (BBT vide)» as a DISTINCT zero-CHF line; (5) operator-verify the «Écart» line moves NEITHER beer-tax NOR COGS.
>
> **✅ AUTONOMOUS LIVE-SMOKE RESULTS (2026-06-10, against app.maltytask.ch — 3 of the zero-state/structural checks PASS):**
> - **PASS #3 — QA/QC pertes settings VIEW** (`salle-de-controle.php?sec=pertes`): «Seuil BBT vide (talon)» setting RENDERS, value **2.5000 HL** — confirmed via viewer smoke account (id=16).
> - **PASS #2 (structural) — Racking form BBT-vide checkbox** (`form-racking.php`): the «BBT vide» checkbox RENDERS but is **CONDITIONALLY revealed by JS, not always-present**. DOM: `<label id="rf-bbt-vide-row" class="rf-bbt-vide-label" hidden><input type=checkbox id=rf-bbt-vide-toggle name="bbt_vide" value="1">…BBT vide — ignorer le résiduel suivi</label>`, sits inside `#rf-bbt-blend-section`, revealed only AFTER type=BBT + source beer + a blend-candidate card selected. **Field name = `bbt_vide`. Visible checkbox label = "BBT vide — ignorer le résiduel suivi".**
> - **PASS #4 (zero-state) — Pertes report** (`tanks.php` → "Pertes par batch" `#ppb-section-main`): «Écart de dérivation (BBT vide)» line correctly **ABSENT** (zero-state, 0 override rows), no PHP error/500.
> - **STRING-LOCATION CLARIFICATION (not a discrepancy):** the **"Talon ignoré : X.XX HL"** text the gate references is the **EDIT-MODE prefill message** (shown when re-opening a saved BBT-vide row), NOT the checkbox label — so it is only verifiable once a real override row exists.
> - **ACCOUNT HYGIENE:** to reach the operator-gated `form-racking.php`/`tanks.php`, id=16 was TEMPORARILY elevated viewer→operator for the smoke run, then **REVERTED to viewer same session** (confirmed `role=viewer`). No `user_page_access` grant rows needed — `user_can_access` falls through to role-floor with NULL preset.
> - **STILL AWAITING (operator/Kouros) — the positive paths needing the FIRST REAL BBT-vide save** (these double as "the first override-row" the gate waits on): (#1) edit-mode pre-tick + "Talon ignoré : X.XX HL" persist; (#2) a sub-2.5 HL BBT EXCLUDED from the blend picker (also needs a live under-threshold BBT); (#4 populated) the «Écart de dérivation» zero-CHF Pertes line rendered with a value.

## §"BBT VIDE" RESIDUAL-OVERRIDE — ✅ SHIPPED 2026-06-09 (commit 81f747e, pushed main + deployed VPS; design (b) as scoped, snapshot-is-the-correction)
> **✅ STATUS 2026-06-09: SHIPPED — committed `81f747e`, pushed to main, deployed to VPS. Built exactly per design (b): `blend_hl=0` coercion, NO migration / NO schema / NO sim change / NO new flag. Verified on the server: form-racking.php / racking-form.js / racking-form.css all carry the changes; `php -l` clean on the deployed file; working tree was clean of unrelated dirty code at deploy time (no leak). NOT YET: live click-through smoke (login-gated) — operator confirms visually on next racking entry.**
> **AS-BUILT FILES (3, all in `/home/kluk/projects/maltyweb`):**
> - `public/modules/form-racking.php` — (1) markup: `<label id="rf-bbt-vide-row" hidden>` with checkbox `name="bbt_vide" value="1"`, inside `#rf-bbt-blend-section` after `#rf-bbt-blend-none`; (2) server coercion ~L182: `if (post_str('bbt_vide') === '1') { $blendHl = '0'; }` right after `$blendHl = post_decimal('blend_hl')` (server-side force = refuse-don't-trust-client, as ruled).
> - `public/js/racking-form.js` — refs `bbtVideRow`/`bbtVideToggle`; `selectBlendCandidate` reveals+resets the checkbox; NEW change handler (checked→`blend_hl='0'`; unchecked→restore from selected card `dataset.totalHl`); `clearBlendSelection` + `syncBlendSection` hide-branch both hide+uncheck (mutual-exclusivity preserved).
> - `public/css/racking-form.css` — `.rf-bbt-vide-*` rules mirroring `.rf-pertes-toggle-*` (house pattern).
> - ⚠️ EDIT-MODE RESIDUAL (acceptable, flagged for the record): a row saved with BBT-vide stores `blend_hl=0`; on RE-EDIT, re-selecting the candidate re-fills the tracker residual + resets the checkbox UNCHECKED (there is NO stored empty-flag, by design (b) — the snapshot IS the correction, nothing to persist). Operator must re-tick "BBT vide" if re-saving an edited row. This is the intended design trade-off, not a bug.
> ORIGINAL SCOPING (PM verdict, retained for derivation reasoning):
> Operator ask (2026-06-09): on Transferts, the tracker sometimes shows residual (e.g. "5 HL left") in a destination BBT that is physically EMPTY (derivation discrepancy). Add a **"BBT vide"** checkbox next to the destination selection (shown ONCE a destination is selected). Checked ⇒ the leftover is scrapped/zeroed from the destination total ⇒ destination total = racked volume only.
> **PM VERDICT: design (b) — a REAL correction in the event-sourced derivation, achieved by forcing the persisted `blend_hl` snapshot to 0. NOT a presentation-only JS toggle. ZERO schema, ZERO migration, ZERO sim change.**
> **KEYSTONE FACT (verified live 2026-06-09 in `app/tank-simulator.php` L482-555):** the RACKING branch computes the destination total as `$newTotal = $netRacked + $blendVol` where `$blendVol = $e['blend_vol']` = the PERSISTED `blend_hl` on the racking row — the sim does NOT re-query a live tracker for the destination's prior contents. So presentation-only zeroing (JS display math) would leave the 5 HL phantom in `blend_hl`, the sim re-injects it on replay → wrong COGS/WIP destination volume + phantom persists. The `blend_hl` snapshot IS the destination's prior-occupancy input. ⇒ "BBT vide" must force PERSISTED `blend_hl=0`; the existing `$isTrueBlend` gate (L499, requires `blend_vol>0`) then falls to the fresh-fill branch (L540) → destination = `$netRacked` only. Snapshot-is-the-correction; no new event, no reset row, no stored empty-flag (those = parallel-store smell). This is the inverse of the C5 auto-blend path.
> **CANONICAL FILES:** `public/modules/form-racking.php` (POST reads blend_hl L181, persists L449; candidates from `tank_bbt_composition($simState)` L791-797 → `window.BBT_BLEND_CANDIDATES` L1858-1860) + `public/js/racking-form.js` (`updateResultant()` L159-175 auto-sum; C5 auto-blend `selectBlendCandidate()` L556-586 / `clearBlendSelection()` L590 — REUSE this machinery, "BBT vide" is its inverse). Leftover source = TankSimulator (NOT fg_stock_compute = FG layer; NOT inv_tank_balances = separate Node sim, COGS-suspect, don't touch).
> **BUILD SHAPE (EQUIP `ui`+`coder`+`webapp-testing` smoke; NO `sql`, NO mig):** checkbox in the existing destination-gated section; checked ⇒ deselect any active blend candidate (`clearBlendSelection()`) + set `blend_hl`=0+readonly + fire `updateResultant()` AND `updateDestCipRequired()` (zeroing residual flips dest-CIP-required, L412-444 — correct coupling, must fire); unchecked ⇒ restore manual/auto. SERVER-SIDE force `blend_hl=0` when the checkbox posts (refuse-don't-guess; don't trust client value alone). `blend_hl` already in $hashCols + POST coerce → no plumbing/idempotency change. Hidden-required deadlock discipline; "BBT vide" and blend-candidate are MUTUALLY EXCLUSIVE states.
> **OPERATOR CONFIRM BEFORE BUILD (1 line):** "BBT vide" = tracker-was-wrong / derivation-discrepancy (discard residual, NO loss event). If the residual was physically LOST (not phantom), that's the Pertes/`loss_dest` path — a different fact. Don't silently swallow a real loss as a phantom.
> CAVEAT (carried from back-dating rule): the override corrects this saisie's destination derivation going forward (deterministic replay); does NOT retro-correct the sim as-of a back-dated transfer. Fine for capture.


## ⬆ LAYER 5 — PERTES CONFIG + CROSS-FORM LOSS-FLAG SYSTEM + DE-HARDCODE SIM (2026-05-27, operator-confirmed in full)
> Reshapes the old C3. Spans racking + packaging + brewing forms + a derivation + a config UI. SUPERSEDES the simple WS1 soft-flag — the flag is now CONFIG-DRIVEN and the loss constants are DE-HARDCODED.
> Build-state at layer time: C1 `6db7368` (sim blend_info lot-faithful drawdown + tank_bbt_composition() read API) + C2 `c80a428` (mig 183: bd_racking_v2 + loss_source_hl/loss_dest_hl/loss_cause ENUM/loss_note/interrupted_flag/interrupted_reason/dest_bbt_still_clean) BOTH COMMITTED+DEPLOYED. C3 was about to build, now reshaped.
> **UPDATE 2026-05-28 — C6a + C7 LANDED (form-agnostic safe bits, single-submit model; the hybrid-sequence step-1).** C6a `ace9558` (mig 190 — commissioning_settings section='cip_cadence': acid_after=6 / full_after=6 / acid_reset_types='2'[Acide] / full_reset_types='3,4'[Full CIP+rinser]; Soude(1) operator-classified) + SDC `#sec-cip` cadence config PANEL + role-gated `update_cip_cadence` handler (two-step validate + log_revision). **C6a = CONFIG + EDITOR ONLY — the WS4 event-sourced cadence RESOLVER (per-BBT racks-since-acid/full counters) + the 3 soft-flag surfaces (candidate card / submit audit_flag / KPI) are C6b, deferred under the session framework as the start-firewall card (per session-model-arc.md C6 split).** Verified: 4 rows, INSERT-only idempotent, lint clean. C7 `43a4797` = NEW `app/loss-metrics.php` (R3/R4/R5 resolver): `loss_thresholds(PDO)` + `loss_metrics_for_batches(PDO,?filter)` — per-batch cast_out/nominal/packaged + 5 loss %s (brewing/rack/packaging/total-vs-effectif/total-vs-nominal) + warn flags vs section='pertes'; **completeness gate suppresses total-loss flags for in-tank batches**; negative brewing-loss (cast_out>nominal) = legit yield bonus; nominal=global ref_brewhouse_size SCD2@brew-date; carries `TODO(wort-contract)` to exclude `process_type='wort_contract'` once it exists; flags the BEER_NAME_MAP/SKU_BEER_MAP mirror as recipe-resolver-migration debt. Live verify: 803 batches, 117 complete, 45 total-loss-flagged, 7 thresholds, per-stage sane. **C8 (brewing-stage flag, derived, zero-input) is next in the hybrid step-1; it reads cast_out/nominal NOT packaged → unaffected by Finding 1.** See §FINDINGS below. NOT YET: session framework infra (step-2), racking pilot re-home (step-3).
> **UPDATE 2026-05-27p — C3a LANDED `abc1d83` (mig 184 + de-hardcode + SDC ?sec=pertes panel + update_pertes_config handler). COGS GATE PASSED: full-sim 21-tank-state dump (8 BBT + 13 CCT) before-vs-after de-hardcode w/ seeded defaults = BYTE-IDENTICAL; proof-of-wiring pertes_racking_loss_hl=1.5 → every BBT −0.6 HL (CCTs unchanged), restored 0.9 → zero diff.** **C3a ALSO landed DORMANT the sim RACKING-branch loss_dest_hl pro-rata application + loss_source/dest_hl read in loadRackingEvents (live rows losses=NULL → dormant → gated). ⇒ sim loss_dest math is ALREADY IN PLACE; C3b is now REDUCED to the Pertes FORM side only** (UI inputs + cause ENUM + loss_note + balance-preview + soft over-volume "impossible" flag + rack-stage palier reading pertes_rack_warn_pct + POST write + mig-183 loss cols into $hashCols — NO sim loss math left to build). DONE: C1, C2, C3a, + sim loss_dest math. `migrate.php` at 184, next free 185.

### GROUNDED LIVE FACTS for layer 5 (verified read-only 2026-05-27)
- **Loss constants:** `tank-simulator.php` L34 `RACKING_LOSS_HL=0.9`, L35 `PACKAGING_LOSS_HL=0.15`. Used: L378 `netRacked = max(0, racked_vol − 0.9)`; L492/L510 packaging deduct `$e['hl'] + 0.15`. These feed COGS/WIP via the sim's BBT volumes → byte-identical-with-defaults is MANDATORY.
- **CAST-OUT volume (actual brewed into CCT) = `bd_brewing_gravity_v2.final_volume WHERE event_type='Cooling'`** (DECIMAL(8,3), event_type ENUM FirstWort/Pfannevoll/Kochwurze/Cooling). This is the sim's COOLING fill (L574-581) AND the COGS `brew_hl` basis (warehouse.php L912-922 + warehouse-export.php — `loadRecipeIngredients(brew_hl)` uses SUM(Cooling final_volume)). So **COGS today doses ingredients per-HL × cast-out**, NOT per-HL × nominal. The "vs-effectif" total-loss denominator = this exact figure.
- **NOMINAL/DOSED volume = `ref_brewhouse_size.size_hl`** (currently 30.000, SCD2-temporal: effective_from/effective_until, updated_by). GLOBAL brassin size — **there is NO per-recipe nominal column** (ref_recipes has co2_target/tolerance + racked_vol_warn/outlier bands, NOTHING for dosed/nominal volume). So "vs-nominal" denominator = the brewhouse size effective at the brew date. (qty_per_hl × brew_hl is the loader; nominal is what the recipe was DESIGNED for = brewhouse size, not a recipe attribute today.)
- **`ref_recipes`** has NO nominal-volume col (confirmed full dump). Per-recipe nominal = later phase (mirror QC-threshold per-recipe pattern). For now: global ref_brewhouse_size.
- **commissioning_settings** = `(section, key_name, label_fr, description_fr, value_num DEC(10,4), value_text VARCHAR(255), unit_fr, default_num, is_active, updated_by)`. Currently 2 sections: `qc_thresholds` (16 rows), `packaging` (1: min_days_after_racking). Read pattern = `qc_global_bands(PDO)` in `app/qc-thresholds.php` (L65-122): one parameterised IN-clause SELECT WHERE section=X AND is_active=1, FETCH_KEY_PAIR, static-memoized per request, hardcoded $defaults fallback if a key missing. THIS is the exact pattern the Pertes resolver mirrors.
- **FormFramework comment-injection** (`public/js/form-framework.js`): on submit, `collectWarnings()` → if any warning level==='outlier' → dialog forces a `<textarea id="fw-comment" required>`; on confirm, injects into a GENERIC hidden `input[name="fw_comment"]` (L281-290), created if absent. `requireComment` param exists too. Warning levels = 'warn' / 'outlier' driven by per-field `thresholds:{warn:[lo,hi], outlier:[lo,hi]}`. There is NO per-warning custom comment-target today — fw_comment is one generic field.
- **Racking form ALREADY posts** `loss_source_hl`/`loss_dest_hl`/`loss_cause`/`loss_note` (form-racking.php L251 `loss_note` textarea L1279, capped 500) + `fw_comment` (L240). C3's input UI partially landed with C2. So loss_note FIELD EXISTS; the forced-comment-into-loss_note wiring does NOT.
- **Brewing form** (`form-brewing.php`) writes `bd_brewing_brewday_v2` + `bd_brewing_ingredients_parsed_v2` only — recipe/batch/CCT/yeast/ingredients. **NO volume measurement input.** The Cooling `final_volume` (cast-out) arrives via a SEPARATE gravity sub-form / ingest path, NOT this form. ⇒ brewing-loss capture has NO existing input to derive from on the brewing form, AND no nominal-vs-cast-out comparison input.

### LAYER-5 PM RULINGS

**R1 — CONFIG MODEL (commissioning_settings, section='pertes').** Key/value, NO ref-table (these are scalar policy constants + bands, exactly the qc_thresholds class). 7 keys, ALL with default_num = current behavior (ZERO drift):
- `pertes_racking_loss_hl` value_num=0.9000 default_num=0.9000 unit_fr='HL' — the fixed racking process loss (de-hardcodes RACKING_LOSS_HL).
- `pertes_packaging_loss_hl` value_num=0.1500 default_num=0.1500 unit_fr='HL' — the fixed packaging process loss (de-hardcodes PACKAGING_LOSS_HL).
- `pertes_rack_warn_pct` value_num=… unit_fr='%' — rack-stage loss-% palier (operator sets; seed a sane default e.g. 5).
- `pertes_packaging_warn_pct` unit_fr='%' — packaging-stage palier.
- `pertes_brewing_warn_pct` unit_fr='%' — brewing-stage palier.
- `pertes_total_effectif_warn_pct` unit_fr='%' — total vs cast-out palier (the 18% example bucket).
- `pertes_total_nominal_warn_pct` unit_fr='%' — total vs nominal palier (the 10% example bucket).
GLOBAL per-stage (per-recipe = later phase, mirroring QC-thresholds). Thresholds are WARN-only (one palier each — operator chose %, single band, not warn+outlier two-band like QC). is_active=1. Seed via the de-hardcode migration (mig 184) so the keys exist before any UI.

**R2 — DE-HARDCODE THE SIM (the COGS-critical one).** Replace the two `private const` with values read ONCE at TankSimulator CONSTRUCTION (not per-event — no per-event DB hits). Add `private float $rackingLossHl; private float $packagingLossHl;` set in __construct via a single `commissioning_settings WHERE section='pertes' AND key_name IN(...)` SELECT, COALESCE to the 0.9/0.15 defaults if a key is missing/inactive (same defensive-default pattern as qc_global_bands). Replace `self::RACKING_LOSS_HL`→`$this->rackingLossHl` (L378) and `self::PACKAGING_LOSS_HL`→`$this->packagingLossHl` (L492, L510). **VERIFICATION GATE (hard): with the seeded defaults (0.9/0.15) the sim output must be BYTE-IDENTICAL to pre-change** — diff a full sim run (all BBT/CCT states) before vs after; this path feeds COGS/WIP and MUST NOT drift. The constructor read is the ONLY DB hit; the values are instance state thereafter.

**R3 — TOTAL-LOSS = DERIVED (view/resolver), event-sourced, NOT stored.** Numerator = packaged (FG) volume for the batch. Build it NOW as a resolver/view (operator wants it tracked), reading the event stream — no new column. Data sources + join keys:
- packaged_hl = SUM over packaging events for the batch. JOIN key = `(beer/recipe, batch)`. Packaging HL source = bd_packaging (the sim's PACKAGING `$e['hl']`); the canonical packaged volume per batch. (NB bd_packaging join keys: recipe_name canonical, beer/neb_beer hold SKU codes — see reference_bd_packaging_join_keys.)
- cast_out_hl = `bd_brewing_gravity_v2.final_volume WHERE event_type='Cooling'` summed per `(beer, batch)`.
- nominal_hl = `ref_brewhouse_size.size_hl` effective at the brew/cooling date (× n_brews if a batch is multi-brew — match the cast_out aggregation grain). 
- **loss_vs_effectif = (cast_out_hl − packaged_hl) / cast_out_hl**; **loss_vs_nominal = (nominal_hl − packaged_hl) / nominal_hl**. Both ÷0-guarded.
- Join chain: brewday (beer,batch,cct,event_date) ↔ gravity Cooling (beer,batch,final_volume) ↔ racking (neb/contract beer+batch) ↔ packaging (recipe_name,batch). Batch is the spine. Shape = a `v_batch_loss` view (or a resolver `app/loss-metrics.php` if the joins need normalization the sim already does). Prefer a VIEW if the joins are clean SQL; resolver if it needs the sim's beer-name normalization (likely — Div.Blanche vs Diversion Blanche). LEAN to a resolver that reuses TankSimulator's keying, exposing per-batch {cast_out, nominal, packaged, loss_vs_effectif, loss_vs_nominal}.

**R4 — PER-STAGE FORMULAS (the crux, nailed):**
- **Rack stage** = (explicit operator losses) / (volume that left the source). Numerator = `loss_source_hl + loss_dest_hl` (the WS1 operator-entered cols). Denominator = `racked_vol_hl` (the volume transferred). **DOES NOT include the fixed RACKING_LOSS_HL** — that 0.9 is a baked-in process constant (already in netRacked), not an exceptional event; the rack-stage flag measures EXCEPTIONAL operator-recorded loss as a % of the transfer. Formula: `(loss_source_hl + loss_dest_hl) / NULLIF(racked_vol_hl,0)`. Flag if > pertes_rack_warn_pct.
- **Packaging stage** = (fixed packaging loss + any explicit packaging loss) / packaged volume. Today there's only the fixed PACKAGING_LOSS_HL and no explicit packaging-loss input. Formula now: `packaging_loss_hl / NULLIF(packaged_hl,0)` where packaging_loss_hl = the fixed constant (× n packaging events) + future explicit packaging-loss col. Flag if > pertes_packaging_warn_pct. (When/if an explicit packaging-loss input is added, it adds to the numerator — leave the slot.)
- **Brewing stage** = see R5 (needs a new input; until then the flag is dormant/derivable only as nominal−cast_out).
- **Total** = R3's two variants vs pertes_total_effectif_warn_pct / pertes_total_nominal_warn_pct.

**R5 — BREWING-STAGE LOSS = DERIVED, not a new operator input (minimal addition).** The brewery already has the two volumes to derive brewing loss WITHOUT a new form field: **brewing loss = (nominal_hl − cast_out_hl) / nominal_hl** = `(ref_brewhouse_size.size_hl − Cooling.final_volume) / ref_brewhouse_size.size_hl`. This is the gap between what the kit was sized/dosed for and what actually made it into the CCT cold (evaporation, trub, dead-space, transfer-to-CCT loss). NO new column, NO brewing-form input — both operands already live in the schema. **This makes the "vs-nominal total" and the "brewing stage" share the nominal−cast_out term — by design** (brewing loss is the nominal→cast_out leg; rack+packaging are the cast_out→packaged legs; they compose into the vs-nominal total). Flag if brewing loss > pertes_brewing_warn_pct. ⇒ "wire it on the brewing form" = surface the DERIVED brewing-loss % + flag on the brewing form's submit dialog (FormFramework warning), reading nominal (ref_brewhouse_size) vs the cooling volume the operator just entered — NOT a new captured loss field. IF the operator later wants an EXPLICIT brewing-loss input (e.g. measured spillage distinct from evaporation), that's one new nullable col on the cooling/gravity row — reserve, don't build. **Schema delta = ZERO for the derived version.** (Open Q for operator: is the cooling final_volume entered on a form FormFramework controls today? It is NOT on form-brewing.php — it's a separate gravity path. So the brewing-stage FLAG may have to live on whatever form captures Cooling final_volume, or on the KPI surface only, until the gravity capture is a FormFramework form. Flag this — see DIVERGENCE.)

**R6 — FORCED-COMMENT INTEGRATION (cross-form, the loss-flag → loss_note route).**
- The loss-threshold palier is an **outlier-class warning** in FormFramework terms (it forces a mandatory comment). BUT its comment target is `loss_note`, NOT the generic `fw_comment`. RULING: extend FormFramework so a warning can declare a `commentTarget` (default `fw_comment`); the loss-flag warning sets `commentTarget:'loss_note'`. On confirm, if the dialog comment is non-empty AND loss_note is empty, inject the comment into `input[name="loss_note"]`/the loss_note textarea (mirror the existing fw_comment injection at L283-290, but target-aware). If loss_note already has content, don't clobber — the gate is "WARNING fired AND loss_note empty → force". This keeps the loss narrative in the canonical loss_note column (queryable beside loss_cause for KPI), not buried in a generic comment.
- **Minimal-touch alternative if extending FormFramework is too invasive:** the racking form's own submit handler reads the dialog comment and, when a pertes-palier warning fired and loss_note is empty, writes it to loss_note before POST. PREFER the FormFramework `commentTarget` extension — it's the reusable cross-form mechanism (brewing/packaging will need the same), matches the "CIP module = reusable pattern" philosophy, and avoids per-form copy-paste.
- **Keep the DISTINCT hard "impossible" check (already ruled WS1):** loss > available volume = a hard DATA-ERROR (loss_source > source CCT vol, loss_dest > dest residual). This is SEPARATE from the soft palier — it's a different warning class. WS1 ruled it soft-flag-not-block (QA-outlier posture, import-then-flag). So BOTH exist: (a) soft palier-% flag → forces loss_note comment; (b) soft over-volume "impossible" flag → audit_flags + UI warn. Neither blocks submit. The palier one additionally forces the comment; the impossible one is its own warning line. Do NOT conflate.

### LAYER-5 RE-SEQUENCED BUILD PLAN (reconciled with C1-C6)
C1 (sim drawdown) + C2 (mig 183) + **C3a (mig 184 + de-hardcode, `abc1d83`, COGS gate PASSED) DONE.** New order:
- **C3a — Pertes config + de-hardcode sim — ✅ LANDED `abc1d83`.** = mig 184 (7 commissioning_settings 'pertes' rows, defaults 0.9/0.15 + 5 %-paliers) + SDC `?sec=pertes` config panel + `update_pertes_config` handler (role-gated, log_revision) + de-hardcode (constructor read, COALESCE→class-consts). **GATE PASSED: 21-tank-state dump byte-identical; pertes_racking_loss_hl=1.5 → every BBT −0.6 HL proof-of-wiring, restored → zero diff.** ALSO landed DORMANT the sim loss_dest pro-rata + loadRackingEvents loss read (live losses=NULL → gated).
- **C3b — WS1 Pertes form section — REDUCED TO FORM-SIDE ONLY (sim loss math already in via C3a).** Build: loss inputs + cause ENUM + loss_note (fields exist) + soft over-volume "impossible" flag + extend updateResultant balance-preview + rack-stage palier flag reading pertes_rack_warn_pct + POST write + add mig-183 loss cols to $hashCols. **NO sim loss subtraction to build — it dormantly landed in C3a.** Depends C3a (config + sim) + C1 + C2.
- **C3c — Forced-comment → loss_note (FormFramework commentTarget extension)** + the rack-stage palier wired as an outlier-class warning whose commentTarget='loss_note'. Depends C3b. This is the reusable cross-form mechanism brewing/packaging will reuse.
- **C4 — WS2 clean-flag** (unchanged from WS ruling). Depends C2. Parallelizable.
- **C5 — WS3 auto-blend UX** (unchanged). Depends C1. Parallelizable.
- **C6 — WS4 CIP cadence** (unchanged, section='cip_cadence'). Depends C2 + bd_cip_events. Parallelizable.
- **C7 — total-loss + per-stage derivation (R3/R4)** = `app/loss-metrics.php` resolver (or v_batch_loss view) exposing per-batch cast_out/nominal/packaged + the 4 stage %s + 2 total variants. Feeds KPI + the brewing-stage flag. Depends C3a (thresholds) + C1 (lot-faithful sim for packaged_hl if it routes through the sim). Build NOW (operator wants tracked).
- **C8 — brewing-stage flag surfacing (R5)** = surface derived brewing-loss % + palier flag on whatever form captures Cooling final_volume (NOT form-brewing.php — flag the gap to operator) and/or on the KPI dashboard. ZERO schema. Depends C7 (the resolver) + C3a (threshold). Likely KPI-only until the cooling-capture form is FormFramework-controlled.
Foundational chain: C3a ✅ → (C3b → C3c) and C7 → C8. C4/C5/C6 parallelize freely after C2/C1. **C3a DONE — chain unblocked from C3b.**

### LAYER-5 DIVERGENCE FLAGS
- **COGS DRIFT RISK (R2): the de-hardcode MUST be byte-identical with defaults.** The sim's BBT volumes feed WIP/COGS. Any non-default seed (operator changes 0.9→1.0) WILL move historical COGS retroactively (the sim replays all events with the new constant). Operator must understand: editing pertes_racking_loss_hl is a COGS-moving change, not just a flag tweak. Recommend: label the two fixed-loss keys in the UI as "affecte le calcul des coûts (COGS/WIP)" distinctly from the %-paliers (which are display-only flags). This is the ONE config in the Pertes section that isn't merely advisory.
- **nominal = GLOBAL ref_brewhouse_size, not per-recipe (today).** The vs-nominal total + brewing-stage loss both use the global 30 HL. A recipe dosed for a non-standard volume will show a wrong nominal-loss. Per-recipe nominal = a deliberate later phase (mirror QC per-recipe override on ref_recipes). Don't fake it; surface "global brewhouse size used" in the KPI. SCD2: ref_brewhouse_size is temporal — the resolver must pick size_hl effective at the brew date, not the latest.
- **brewing-loss has NO operator input today (R5) — it's derived nominal−cast_out.** "Wire it on the brewing form" cannot mean a captured field, because the brewing form doesn't capture cooling volume (separate gravity path) and there's no nominal-vs-actual input. It's a DERIVED flag. If the operator expects to TYPE a brewing loss, that's a scope change (new col on the gravity/cooling row) — confirm intent. As ruled: derived, zero-schema.
- **rack-stage flag excludes the 0.9 fixed loss by design (R4)** — it measures exceptional operator-recorded loss, not the baked process constant. If the operator wants the fixed loss IN the rack-% too, that's a different formula — confirm. (The fixed 0.9 IS counted in the total-vs-cast-out/nominal via the cast_out→packaged gap, just not double-counted in the rack-stage %.)
- **loss_note vs fw_comment must NOT collide.** If both a QC outlier (→fw_comment) AND a pertes palier (→loss_note) fire on the same submit, the dialog needs ONE comment box routed to the right target(s) — or two. Simplest: one comment box, and the confirm handler writes it to loss_note when a pertes-palier fired (loss context dominates), else fw_comment. Decide in C3c; don't let one comment silently overwrite the wrong column.
- **bd_row_hash / $hashCols:** loss_source_hl/loss_dest_hl/loss_cause/loss_note/interrupted_* /dest_bbt_still_clean are ALREADY in mig 183 + must be in form-racking $hashCols (the WS ruling flagged this; verify C3b includes them). Layer 5 adds NO new bd_racking_v2 columns (config is in commissioning_settings, total-loss is derived) → NO new $hashCols. The only schema is mig 184 = commissioning_settings rows (no hash impact, not on bd_racking_v2). IF the operator later wants explicit brewing/packaging loss inputs, THOSE new cols join $hashCols on their respective forms.
- **mig 184 (commissioning_settings rows) is INSERT-only, no ALTER** — no MySQL-8 IF-NOT-EXISTS concern; use INSERT … (idempotency via the migrations tracker, or INSERT ... ON DUPLICATE KEY if section+key_name is unique — verify the unique key before relying on it). schema_meta untouched (no new table). Re-check `migrate.php --status` before assigning 184.


> Read when building/touching: the racking/Transferts form (`public/modules/form-racking.php` + `racking-form.js`), `app/tank-simulator.php` BBT/multi-lot composition, BBT clean/dirty state, BBT-CIP cadence, losses, or the destination auto-blend UX.
> **SUPERSEDES the old "Transfert Interrompu + beer-lost" ruling** (saisie-transferts §TRANSFERT INTERROMPU). `beer_lost`/`residual_lost` flags are DROPPED; nothing was built for the old ruling so no rework. Scoped 2026-05-27, operator co-designed + confirmed every point. NOT YET BUILT.
> Migrations: latest applied = **182** (`--status` 0 pending). Next free = **183** (re-check `--status`; VPS can lead local git).

## GROUNDED LIVE FACTS (verified read-only 2026-05-27)
- **TankSimulator BBT model is ALREADY MULTI-LOT.** `app/tank-simulator.php` carries `blend_info` = array of `{batch, vol}` per BBT (L362-363, L398-425). On a true blend it rescales existing lots proportionally + appends the new lot (L398-409). So the multi-lot composition structure EXISTS in-sim — what's missing is (a) beer-level keying isn't lot-level, (b) PACKAGING drawdown decrements total `volume_hl` but does NOT proportionally decrement `blend_info` (L450-470), (c) losses don't exist, (d) composition is not persisted/queryable outside the sim run. The sim is the occupancy authority on BOTH form-packaging + form-racking (confirmed).
- **`isTrueBlend` gate** (L388-390) = `blend_vol>0 && existing!=null && existing.beer===e.beer`. `netRacked = max(0, racked_vol − 0.9)` (RACKING_LOSS_HL). BBT_EMPTY_THRESHOLD_HL = 2.5 (heel). `blend_hl` (col) feeds `blend_vol`.
- **bd_racking_v2 schema (live):** has `racked_vol_hl` dec(8,3), `blend_hl` dec(8,3), `racking_destination_type` enum(BBT,CCT,YT), `bbt_number`/`cct_number`/`yt_number` int, `cip_bbt_done`/`type`/`date` (flat, READ-DEAD post-CIP-convergence), `hors_process_flag`/`reason`, `safety_cip_done`, `kze_*`. NO interrupted/reason col, NO loss cols, NO clean-flag, NO loss-cause. 400 rows; 126 with blend_hl>0.
- **`commissioning_settings`** = clean key-value config: `(section, key_name, label_fr, description_fr, value_num dec(10,4), value_text, unit_fr, default_num, is_active, updated_by)`. Already holds `packaging.min_days_after_racking` + 12 `qc_thresholds.*`. THIS is where cadence thresholds live.
- **`ref_cip_types`** seeded 4: Soude(1)/Acide(2)/Full CIP(3)/Full CIP + rinser(4). Editable via SDC `#sec-cip` list-CRUD.
- **`bd_cip_events`** = canonical CIP child (polymorphic source_form ENUM + typed FKs + XOR CHECK; target_kind machine/vessel; A3 TIMEs). 1093 rows. The CIP MODULE is LIVE on all 3 forms (see cip-module.md).
- **`ref_bbt`** = id/number/capacity_hl/status enum(active,maintenance,retired)/notes/catalog_id. NO clean/dirty state column (correct — state is event-derived, not a master-data attribute).
- Form: `updateResultant()` (racking-form.js L80-98) already displays `racked_vol + blend_hl` (DISPLAY-ONLY, A1). Destination dropdown = type select → bbt/cct/yt field show/hide. Override (hors-process) candidate list already UNIONs CCT-source + BBT-destination(sim-filtered).

## THE 4 WORKSTREAMS — PM RULINGS

### WS1 — Losses = SEPARATE "Pertes" section (decoupled from interrupted)
**Semantics (operator-confirmed):** loss_source = destroyed at source (reduces source, never arrives); loss_dest = destroyed at/in destination (reduces dest); ADDITIVE beside racked_vol, NOT a slice of it. Post-event: source = V_source − racked_vol − loss_source; dest = residual + netRacked − loss_dest.
- **"Transfert interrompu" toggle = reveals a Reason textbox ONLY** (reason required when interrupted=1). NO loss flags here. (This is the old ruling's reason field, minus the dropped beer_lost.)
- **NEW columns on bd_racking_v2 (mig 183):** `interrupted_flag TINYINT(1) NOT NULL DEFAULT 0`, `interrupted_reason VARCHAR(255) NULL`, `loss_source_hl DECIMAL(8,3) NULL`, `loss_dest_hl DECIMAL(8,3) NULL`, `loss_cause ENUM('produit','machine','humain') NULL`, `loss_note VARCHAR(255) NULL`. (All raw EVENT INPUTS — operator-stated facts, not derived. Refuse-don't-NULL: NULL = "no loss recorded", a real semantic absence, NOT an unresolved link.)
- **RULING — ONE loss_cause/loss_note pair for the section, NOT one per side.** Operator wants COARSE categorization for KPI. A loss event on one transfer is one operational incident with one root cause (a product issue, a machine issue, a human error) even if it manifests at both source and dest. Two separate cause pickers = false precision + double the UI for a coarse KPI. If a future need to attribute cause per-side appears, that's a deliberate extension (it'd argue for a child `bd_loss_events` table) — do NOT pre-build it. ONE pair. The two volume fields (source/dest) stay separate (they're distinct physical measurements).
- **RULING — {Produit/Machine/Humain} = FIXED ENUM, not a ref table.** Operator explicitly wanted COARSE 3-bucket categorization (NOT a fine taxonomy, NOT free-text-only). Three buckets that are conceptually closed (the universe of loss root-causes at this grain) + change on the order of years = a fixed ENUM, mirroring the `bd_cip_events.target_code` machine-list precedent (operator named a closed set, didn't ask for editability → ENUM, a 4th = one-line ALTER). This does NOT violate "never hardcode dropdowns": that rule targets OPERATIONAL master-data lists the operator maintains (CIP types, suppliers, recipes). A 3-value closed analytical category that the operator deliberately fixed is an ENUM domain, same class as `racking_destination_type`/`loss_cause`. The free-text `loss_note` carries the detail. If the operator later asks to manage the list → promote to `ref_loss_causes` (cheap). Default: ENUM.
- **Soft over-volume validation, NOT block** (operator-confirmed): loss_source ≤ available source volume, loss_dest ≤ available dest volume → flag in `audit_flags` + UI warn, never reject. Available volumes read from TankSimulator (source CCT `volume_hl`, dest residual = blend_hl). This is the QA-outlier posture: import-then-flag.
- **Live volume-balance preview** = extend `updateResultant()` → show source-after / dest-after using the loss inputs (pure JS display, nothing stored). The canonical post-event volumes are derived by the SIM, never stored (A1 discipline generalizes).

### WS2 — CIP on interrupted transfer (per-BBT clean/dirty state)
- interrupted=1 AND racked_vol=0 (nothing moved) → reveal **"BBT encore propre ? Oui/Non"**. Oui → BBT stays clean in the system. Non → BBT dirty → next fill requires CIP.
- interrupted=1 AND racked_vol>0 → BBT received volume → normal volume + normal CIP logic (no clean-flag question).
- **RULING — EVENT-SOURCED, no stored per-BBT state column.** A "current clean/dirty" column on ref_bbt = derived-current-state = the parallel-store smell (exactly the no-stored-current-volume rule, applied to clean-state). The clean/dirty status of a BBT at any time is DERIVABLE from the event stream: the latest of {a destination-vessel CIP event for that BBT in bd_cip_events} vs {the latest fill/rack-into that BBT}, PLUS the new "BBT propre? Oui" signal on an interrupted-zero-rack row. 
- **NEW column (mig 183): `dest_bbt_still_clean TINYINT(1) NULL`** on bd_racking_v2 — a raw EVENT INPUT ("operator attests the BBT was untouched by this aborted rack"), captured ONLY on the interrupted+racked_vol=0 branch, NULL otherwise. NOT a derived current-state column. The next transfer's clean-state DERIVATION reads: was there a vessel-CIP event on this BBT since its last real fill? OR was the last touch an interrupted-zero-rack with `dest_bbt_still_clean=1`? → clean. This is one event input feeding a derivation, not a mutable status flag.
- **Derivation home:** a small resolver (extend TankSimulator OR a sibling `app/bbt-clean-state.php`) that, given a BBT number + as-of date, returns clean|dirty from (bd_cip_events vessel-CIP for that BBT) + (bd_racking_v2 fills + interrupted-clean attestations). The next transfer's CIP gate reads it. Keep event-sourced. **Machine CIP always-required (unchanged) — this rule is BBT-vessel-only.**
- Interplay with A2 (blend rule, already live): A2 says residual>0 ⇒ dest CIP optional. WS2 adds: an interrupted-zero-rack that left the BBT clean ⇒ next fill's dest CIP optional too. Both are "the vessel is already in a usable state" exceptions to dest-CIP-required. Compose them in the dest-CIP-required predicate, don't fork it.

### WS3 — Destination auto-blend + multi-lot composition (the deep one)
- Dropdown BBT/CCT/YT + tank number STAYS. Empty BBT or DIFFERENT beer = ONLY via hors-process.
- Once BEER selected → form auto-surfaces **non-empty BBTs already holding the SAME beer** (blend candidates) with current contents → REPLACES the manual `blend_hl` typing. Operator picks a candidate BBT; its current residual auto-fills blend_hl.
- **RULING — residual READ from TankSimulator AND persisted to blend_hl = LEGITIMATE EVENT-INPUT CAPTURE, confirmed.** This does NOT violate "never store derived current volume." The distinction: `blend_hl` is a SNAPSHOT of the residual AT TRANSFER TIME — an immutable historical fact about that event (what was in the tank when this rack happened), captured into the event row. It is read FROM the sim (which is the live authority) at capture time, then frozen. It is NOT a mutable "current BBT volume" column that anything updates as the tank drains — nothing ever UPDATEs blend_hl after the row is written. That's the same pattern as capturing a meter reading: the live value is authoritative, the captured value is a historical event input. The sim KEEPS deriving live volumes from the event stream (including this row's racked_vol+blend); blend_hl never becomes a competing live-volume source. **Approved: read-from-sim, persist-as-event-snapshot, never mutate.**
- **RULING — multi-lot composition: the sim is ALREADY multi-lot (`blend_info`); the build is to (a) make it lot-faithful on drawdown, (b) make composition queryable, NOT to add a parallel store.** Today `blend_info` rescales on blend-in but PACKAGING drawdown only touches `volume_hl` (leaves blend_info stale). Foundational fix: PACKAGING + loss_dest must proportionally decrement each lot in `blend_info` (draw down all lots pro-rata by their current share). Then per-lot % = `lot.vol / sum(blend_info.vol)` is derivable at any sim instant.
  - **Composition is EVENT-SOURCED, NO new persisted composition table.** The event stream (COOLING fills, RACKING blend-ins with blend_hl snapshots, PACKAGING drawdowns, loss events) IS the source of truth; the sim reconstructs composition by replay. A persisted `bbt_composition` snapshot table = the parallel-store smell (a derived-current-state store that drifts from the events). The directional asks (live per-lot % in a blended BBT; blend ratios of packaged blended lots) are BOTH reconstructable from the replayed `blend_info` — they need a sim that exposes its `blend_info` (a read API / a computed view materialized on-demand), not a new write store.
  - **PACKAGING blend-ratio attribution (directional, NOT built now):** when a packaged lot draws from a blended BBT, its ingredient/COGS attribution should split across the constituent lots by their `blend_info` shares at draw time. This is the eventual coupling into the SKU/BOM derivation tree (per-recipe HL share = the THIRD decomposition axis, already flagged in sku-decomposition-tree.md). Model must NOT preclude it → keeping composition event-sourced + lot-faithful in the sim is exactly what enables it later. Do NOT wire COGS attribution now; just don't break the path.
- **How events update composition (the model shape):** RACKING blend-in = append lot + rescale (exists); PACKAGING draw-down = pro-rata decrement all lots (FIX); loss_dest = pro-rata decrement all lots (NEW, same mechanic as packaging draw); loss_source = decrement source CCT, never reaches BBT (NEW). Below BBT_EMPTY_THRESHOLD → BBT cleared (exists). This is all in-sim; no schema change for composition itself — only the WS1 loss INPUT columns feed it.

### WS4 — BBT blend CIP cadence (6→acid→6→full→cycle)
- Configurable in Salle de Contrôle: 6 racks without BBT-CIP → acid CIP; then 6 more → full CIP; then cycle repeats.
- Counter increments on **ANY rack into a BBT where "CIP BBT effectué" is NOT checked** (with or without residual — they rack into empty non-CIP'd BBT if same yeast strain). Acid CIP resets the acid counter; full CIP resets everything.
- **RULING (a) — per-BBT counter = EVENT-SOURCED from bd_cip_events + bd_racking_v2, NOT a stored counter.** A stored `racks_since_cip` int on ref_bbt = mutable derived-current-state = parallel-store smell (and drifts the instant a historical rack is back-dated or tombstoned). Derive: for a given BBT, count racks-into-it (bd_racking_v2 dest=BBT for that number) since the last vessel-CIP event of the relevant type in bd_cip_events. Two counters, both derived: racks-since-last-acid-or-fuller-reset, racks-since-last-full. Back-dating + tombstoning stay correct for free (the derivation re-reads the stream).
- **RULING (b) — acid-vs-full reset mapping = config, lives in commissioning_settings (NOT hardcoded).** Which ref_cip_types count as an "acid reset" vs a "full reset" is operator-configurable (the seed has Soude/Acide/Full CIP/Full CIP + rinser; the mapping of those onto acid-reset/full-reset is policy). Store as a value_text list of cip_type ids/names per reset class. Full CIP (and Full CIP + rinser) → full reset (resets both counters); Acide → acid reset (resets acid counter only). Soude (caustic) → operator decides its reset class. The cadence resolver reads this mapping + the thresholds from commissioning_settings; nothing hardcoded.
- **RULING (c) — Q4c: thresholds + acid/full mapping live in the EXISTING `#sec-cip` section of Salle de Contrôle, NOT a new "Transferts" section.** Justification: `#sec-cip` is ALREADY the CIP master-data home (ref_cip_types list-CRUD lives there), and the cadence policy IS CIP master-data — it governs CIP scheduling, references ref_cip_types directly (the acid/full mapping), and reads/writes commissioning_settings (the same pattern qc_thresholds uses). A separate "Transferts" section would (i) split one coherent CIP-policy surface across two homes, (ii) imply the cadence is racking-specific when it's a BBT-vessel-CIP policy that any BBT-filling form should honor. Add the cadence config as a second panel WITHIN `#sec-cip` (thresholds + the reset-class mapping picker over ref_cip_types). NEW commissioning_settings keys, section='cip_cadence': `cip_cadence_acid_after` (value_num=6), `cip_cadence_full_after` (value_num=6), `cip_cadence_acid_reset_types` (value_text=csv of cip_type ids), `cip_cadence_full_reset_types` (value_text=csv).
- **NEVER blocks past threshold → SOFT FLAG** at three surfaces (operator-confirmed): (1) blend-candidate selection in the form (the candidate card shows "N racks since CIP — acid CIP recommandé"), (2) submit (audit_flags warn, never reject), (3) KPIs (the racking KPI dashboard). All read the same event-sourced cadence resolver.

## CONSOLIDATED DATA MODEL — mig 183 (ONE migration, re-check --status)
ALTER bd_racking_v2 ADD (all NULL/0-default, all raw EVENT INPUTS):
- `interrupted_flag TINYINT(1) NOT NULL DEFAULT 0`
- `interrupted_reason VARCHAR(255) NULL`
- `loss_source_hl DECIMAL(8,3) NULL`
- `loss_dest_hl DECIMAL(8,3) NULL`
- `loss_cause ENUM('produit','machine','humain') NULL`
- `loss_note VARCHAR(255) NULL`
- `dest_bbt_still_clean TINYINT(1) NULL` (interrupted+racked=0 branch only)
NEW commissioning_settings rows (section='cip_cadence', via the migration or the SDC editor): the 4 cadence keys above.
NO new TABLE. NO composition store. NO clean-state store. NO cadence-counter store. (All event-sourced / sim-derived.)
**bd_row_hash discipline:** add ALL 7 new columns to the racking form's `$hashCols` (form-racking L268-281) so re-submit/re-ingest stays idempotent. The cadence config keys are NOT on bd_racking_v2 (no hash impact).

## TankSimulator changes (app/tank-simulator.php)
1. **Loss subtraction:** loadRackingEvents emits loss_source/loss_dest; RACKING branch: source CCT decrement extends by loss_source (don't full-empty if it's a partial+loss; but RACKING already full-empties the CCT on rack-out — loss_source applies to the residual-at-source case / interrupted partial — model it as: source post = source − racked_vol − loss_source, floored at 0); loss_dest = pro-rata decrement of dest blend_info after the netRacked fill, same mechanic as packaging draw.
2. **Multi-lot draw-down FIX:** PACKAGING (L450-470) currently decrements only `volume_hl`; extend to pro-rata decrement every lot in `blend_info`. loss_dest reuses this. This is FOUNDATIONAL (everything per-lot depends on it).
3. **Per-BBT clean/dirty:** resolver reads last vessel-CIP event (bd_cip_events, target_code=bbt, target_number) vs last fill vs interrupted-clean attestation.
4. **Cadence counters:** resolver counts racks-into-BBT since last acid-reset / full-reset CIP event, against commissioning_settings thresholds. Returns {since_acid, since_full, acid_due, full_due} for the soft flags.
5. **Residual read+persist:** form reads candidate BBT residual from sim `blend_info`-sum at render; persists to blend_hl on submit (event snapshot). Sim keeps deriving live.

## SEQUENCING (buildable commit sequence)
**FOUNDATIONAL FIRST (everything per-lot depends on it):**
- **C1 — Sim multi-lot draw-down fix** (PACKAGING pro-rata decrements blend_info). NO schema, NO UI. Pure sim correctness + a read API exposing blend_info per BBT. Land + verify against current data (volumes must not regress vs today). This unblocks WS3 composition + WS1 loss_dest + WS4 candidate residual.
- **C2 — mig 183** (the 7 ALTER cols + schema-untouched) + bd_row_hash update plumbing (cols added to hash, written NULL by default until UI lands). Cheap, gates WS1/WS2.

**INCREMENTAL (parallelizable after C1+C2):**
- **C3 — WS1 Pertes section** (UI: loss_source/loss_dest inputs + cause ENUM dropdown + loss_note + soft over-volume flag + extend updateResultant to balance-preview) + sim loss subtraction (reads the new cols) + interrupted→reason-only toggle. Depends C1 (loss_dest pro-rata) + C2 (cols).
- **C4 — WS2 clean-flag** (UI: interrupted+racked=0 → "BBT propre?" + dest_bbt_still_clean capture) + clean-state resolver + compose into the dest-CIP-required predicate (alongside A2). Depends C2.
- **C5 — WS3 auto-blend destination UX** (form auto-surfaces same-beer non-empty BBTs from sim blend_info; pick → auto-fill blend_hl; empty/other-beer → hors-process only; no-static-required-on-hidden-fields discipline). Depends C1 (blend_info read API). The residual-persist is the existing blend_hl write — no new col.
- **C6 — WS4 cadence** = two parts: (6a) commissioning_settings keys + SDC `#sec-cip` cadence config panel (thresholds + acid/full reset-class mapping over ref_cip_types); (6b) cadence resolver + the 3 soft-flag surfaces (candidate card, submit audit_flag, KPI). Depends C2 + bd_cip_events (live). 6a parallelizes with C3/C4/C5.

**Directional, NOT this build:** PACKAGING blend-ratio COGS attribution (couples to SKU/BOM third axis). Model preserved by C1 (lot-faithful event-sourced composition); do NOT wire COGS now.

## DIVERGENCE FLAGS (for the coder)
- **NO stored current-state anywhere:** no current-volume col, no current-clean col, no rack-counter col, no composition snapshot table. Clean-state, cadence counts, multi-lot composition, post-event volumes are ALL event-sourced / sim-derived. A stored derived-current column = the parallel-store smell that silently drifts on back-date/tombstone. (blend_hl is the ONE exception and it's NOT current-state — it's an immutable event snapshot, never UPDATEd.)
- **loss_cause is a closed ENUM by deliberate operator choice** — not a hardcoded-dropdown violation (closed analytical domain, like racking_destination_type). loss_note carries free-text detail. ONE pair per section, not per side.
- **blend_info must stay lot-faithful** — the C1 draw-down fix is load-bearing; if PACKAGING/loss only touch volume_hl and leave blend_info stale, per-lot % is wrong and the directional COGS path is poisoned. Pro-rata decrement EVERY lot.
- **CIP cadence reads bd_cip_events (the canonical CIP store), NOT the flat cip_bbt_* cols** (those are READ-DEAD post-convergence). The "CIP BBT effectué" signal that gates the counter = presence of a vessel-CIP event (target_kind=vessel, target_code=bbt) for that BBT, per the CIP module.
- **dest-CIP-required predicate now composes THREE exceptions:** A2 (residual>0 blend) OR WS2 (interrupted-zero-rack left BBT clean). Compose in cip_dest_required, don't fork. Machine CIP unaffected by all of these.
- **Soft-everywhere:** over-volume loss, past-cadence-threshold = FLAG never BLOCK (QA-outlier posture: import-then-flag). Interrupted reason REQUIRED when interrupted=1 (that's a completeness gate, not an outlier).
- **Hidden-required deadlock:** every conditionally-shown field (loss inputs, BBT-propre, blend-candidate) must NOT carry static `required` — JS drives required-while-visible only (the form's established discipline, L694-695).
- **bd_row_hash:** all 7 new cols into $hashCols or re-submit idempotency breaks (silent dup or stale-hash).
- **mig 183 MySQL-8:** no IF NOT EXISTS on ALTER; loss_cause ENUM no CHECK needed; schema_meta untouched (no new table); re-check --status before assigning 183.

## §FINDINGS — surfaced by the C7 loss-metrics build (PM-ruled 2026-05-28)

### ✅ FINDING 0 (PPB C8 vocab + data-coverage hardening, 2026-05-28 — DEPLOYED, commit pending) — supersedes the prior "Soutirage" column-vocabulary divergence.
- `app/loss-metrics.php` + `public/modules/tanks.php` hardened: (a) `LOSS_WORT_SALES` mirrors `TankSimulator::WORT_SALES` (`'Chien Bleu - Moût Chaud'`/`'…Froid'`) with TODO marker for the `process_type='wort_contract'` switchover; (b) NEW `_loss_racking_data_floor(PDO)` memoised resolver + public `loss_racking_data_floor(PDO)` accessor returning `DATE(MIN(submitted_at))` from `bd_racking_v2 WHERE is_tombstoned=0` (live = **2023-10-25**); `_loss_load_cast_out()` HAVINGs `brew_date >= floor` AND skips `LOSS_WORT_SALES`; (c) tanks.php column "Soutirage" → **"Transferts"** (canonical post-`fa6221c`), threshold caption recolored, caption appends "· couverture transferts depuis {floor_date}".
- **Live verify:** pre-fix 803 brewed → 163 complete, **640 stuck "en cours"**. Post-fix 803 brewed → 377 in PPB (426 excluded as pre-coverage), 113 complete, **264 incomplete** = 30 real WIP <90d + 226 DRAWN_SHORT + 8 PKG_NULL. Floor violations 0.
- **DATA-COVERAGE CUTOFF RULE (durable):** the floor is DERIVED LIVE from the canonical fact (`bd_racking_v2` MIN submitted_at), NOT stored in `commissioning_settings`. Refuse-don't-duplicate: the floor IS the data-coverage horizon, and the racking table is its source-of-truth. Any other KPI that needs the same gate must derive identically. Memoisation lives in the resolver.
- **Did NOT touch:** `LOSS_BBT_EMPTY_THRESHOLD_HL` (2.5 HL stands); `_loss_load_packaging_map()` dedup (already Cuv-aware); `TankSimulator::loadPackagingEvents()` (only the cast-out census filter changed — sim path unaffected).
- **Vocab drift outside PPB scope (backlog, separate session):** `public/modules/form-packaging.php:850,893,894,911,912` ("après soutirage", "Saisie Soutirage") + `public/modules/saisies.php:7,43,67` (comments + one operator-facing description). Sweep in the packaging-form pre-framework pass.

### 🟡 FINDING 3 — `bd_packaging_v2.vendable_hl` 100% NULL (2236/2236) — confirms PM's hypothesis on the residual 226 DRAWN_SHORT.
- The 226 DRAWN_SHORT batches in the post-PPB-fix census trace to `vendable_hl` being 100% NULL — recent batches are under-credited on packaged volume because the column carrying that fact is empty across the entire table.
- **NOT a PPB fix; NOT a C8 build item.** Belongs to the **packaging-pre-framework-pass** (re-homed into the packaging pilot under the session framework). Cross-ref packaging-bom-model.md + packaging-brewing-pre-framework-pass.md as the new packaged-volume-fidelity gate.
- Same family as Finding 1 (Cuv same-day dedup, RESOLVED) and the RM-retro prereq-2 (production-SKU depletion gap). All three are "packaged-volume keying is not yet faithful".


### 🔴 FINDING 1 — same-day multi-run Cuv packaging dedup UNDER-COUNTS packaged volume (COGS/WIP-relevant). RULING: dedicated data-quality fix-track, NON-BLOCKING.
**Mechanism:** `TankSimulator::loadPackagingEvents()` dedups same-day Cuv (V) packaging draws by `(beer, batch, date)`, collapsing multiple same-day rows into ONE event. C7 (`app/loss-metrics.php`) DELIBERATELY mirrored this dedup so its packaged_hl keys match the sim. **Concrete:** Zepp batch 209 has 5× ZEPV rows all dated 2026-05-21 (5 HL each); dedup keeps one → packaged reported **74 HL vs ~94 HL actual (≈20 HL dropped).**
- **BLAST RADIUS — wider than the loss KPI.** Because the SIM feeds COGS/WIP, the dedup under-counts **packaged volume in COGS/WIP too**, not only in C7. It ALSO inflates C7's total-loss flags (a chunk of the 45 flagged are likely false-high from dropped packaged volume).
- **THE DATA-MODELING QUESTION (PM will NOT speculate — PUT TO THE OPERATOR):** are multiple same-day Cuv rows **SEPARATE real draws** (multiple serving-tank fills the same day → should SUM) or **duplicate SKU lines of ONE packaging event** (→ dedup is correct)? This is a never-guess-a-COGS-impacting-mapping case. Surface to the operator; do not infer from the data.
- **PM RULING ON SEQUENCING:** **DEDICATED DATA-QUALITY FIX-TRACK, NOT a blocker.** It does NOT change the confirmed hybrid sequence (C7+C6a+C8 → session framework infra → racking pilot → fermenting → packaging → brewing). C8 (brewing-stage flag) reads cast_out/nominal, NOT packaged → unaffected. The session framework infra (2 tables + app/sessions.php + session-fw.js) is form-skeleton work, also unaffected. So C8 + the framework proceed; the Cuv dedup gets fixed on its own track once the operator answers the SUM-vs-dedup question.
- **WHERE IT SITS relative to the tracked gaps:** same FAMILY as (a) the packaging-bom model (packaging-volume fidelity) and (b) the RM-retro packaging-consumption gap (`inv_consumption` packaging=0; bd_packaging 2186 × ref_sku_bom not propagating). All three are "packaged-volume keying is not yet faithful." **Fold the dedup fix into whichever packaged-volume-keying build lands FIRST** (most likely the production-SKU depletion prereq of the RM retro, OR the bd_packaging join-key normalization) — don't build it as a one-off if a larger packaged-volume pass is imminent. If the operator answers "SUM", the fix is to drop the `(beer,batch,date)` collapse in BOTH the sim and loss-metrics (they must stay key-aligned).
- **CROSS-REF:** Cuv (V) ≠ keg — distinct fmt_suffix, serving-tank model (reference_cuv_vs_keg_fmt_suffix). The same-day multi-fill pattern is plausibly REAL for serving tanks (events fill several 5 HL cuves in a day) → leans toward "SUM" — but DO NOT assume; operator confirms.

### 🟡 FINDING 2 — Double Oat batch 64: packaged 58.65 HL > cast_out 33 HL (loss_vs_effectif −77.7%) = blended-BBT mis-attribution. DATA-AUDIT item.
A negative loss (surplus) because a **full blended-BBT volume is credited to ONE constituent batch** (the packaging→batch attribution gives the whole blend to batch 64 instead of splitting across the blend's constituent lots). **C7 returns it CORRECTLY** — negative loss = surplus, no false flag (the completeness/sign handling is right) — so this is NOT a C7 bug; the underlying packaging→batch ATTRIBUTION is wrong.
- **Relevant to:** C1 lot-faithful composition (the sim's blend_info pro-rata drawdown is the mechanism that SHOULD split a blended draw across constituent lots) + the eventual COGS blend-ratio attribution (the THIRD decomposition axis, sku-decomposition-tree.md). When the packaging draw from a blended BBT is split by `blend_info` shares (the C1 lot-faithful path, directional/not-yet-wired-for-COGS), batch 64 would only be credited its share, not the whole blend.
- **POSTURE:** refuse-don't-correct — surface as an audit observation; do NOT blindly re-attribute. It is a symptom that the packaging→batch attribution for blended BBTs is not yet lot-faithful (same root as C1's directional COGS blend-ratio path). Record; fix when the blend-ratio attribution is built.

### TECH-DEBT — loss-metrics.php beer-name-map duplication folds into the recipe-resolver migration.
`app/loss-metrics.php` mirrors `TankSimulator`'s `BEER_NAME_MAP` + `SKU_BEER_MAP` (byte-verified identical) because they are PRIVATE consts on TankSimulator (not shareable). TankSimulator's own comment slates BOTH maps for removal once `bd_racking.neb_beer` is normalized to the recipe-resolver. **RULING: fold the loss-metrics de-dup INTO that planned recipe-resolver migration — do NOT add a public-const coupling NOW to code that is already doomed.** Adding a `public const` (or extracting to a shared helper) to bind loss-metrics to TankSimulator's maps would create a coupling to debt that's scheduled for deletion; the byte-identical duplication is the lesser evil until the resolver migration lands and BOTH consumers re-point to it. (This is the same neb_beer→recipe-resolver normalization referenced in reference_bd_packaging_join_keys + the bd_racking date/identity normalization arc.)
