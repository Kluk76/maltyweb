# Sales-commercial — CFO COGS fiche ruling, single-source COGS repoint, financier page v1, RE-SOURCE arc, per-module canonical-source ruling, board COGS/COP grid, sku-costs retirement

## 🔴 2026-06-11 — CFO INVENTORY-VARIATION COGS FICHE — PM RULING RECORDED (pre-build; Kouros "this is what my CFO needs"; EXT 2026APRIL D3:L16 "Fiche de Calcul COGS")
**This is a DISTINCT computation from the shipped board grids — NOT the per-HL COGS.** The board grid (§BOARD COGS/COP P&L GRID, `b477565`) is a FLOW/unit-cost view (consumption×WAC ÷ HL, per GL line). The CFO fiche is an INVENTORY-BALANCE VARIATION view (RM/WIP/FG *closing stock value* per category; COGS movement L = ΔTotal Inventaire). Complementary, no collision WITH the grid. Target table = one row per inventory category (Malt/Hops/Ingrédients/Levures/Cartons/Inliner/Capsules/Verres/Bouteilles/Etiquettes/Canettes/Caps Futs) × cols D(cat)/E(Comptes Inv 1200.x)/F(Cptes Charge 41xx)/G(RM clo)/H(WIP clo)/I(FG clo)/J(G+H+I)/K(Compta opening)/L(J−K). April acceptance test = **J=394,367.74, L=−36,986.26**.
**LIVE-VERIFIED STATE (2026-06-11):** `cogs_monthly` = **the purpose-built shell** (UNIQUE `(month_key,category,subcategory,source['rm'|'fg'|'wip'])` + opening/inflows/outflows/closing_value_chf) — **0 rows, NO writer anywhere** (legacy `build-month-closure.js` wrote BSF tabs, NOT this table; archived to `bsf-dashboards/`). So a maltyweb writer is the FIRST+ONLY writer → strangler landmine(a) two-writers does NOT bite. `cop_monthly` also 0. Source tables READY: `inv_rm_stocktake` (Apr 239/May 125 active, ×`v_mi_cost`); `inv_tank_balances` (Apr 18/May 16 rows, volume_hl×brew_cost_per_hl, thru 2026-05); `inv_fg_stocktake` (Apr 41 @ month_closed='2026-04' / May 109 @ '2026-06'); `ref_sku_bom` (per-SKU liquid+packaging lines, mi_id+cost+bom_source).
**THE 7 RULINGS:**
1. **Compute home = maltyweb-native writer to `cogs_monthly`** (PHP `app/cogs-monthly.php` + CLI backstop), SCHEDULED-REFRESH mode (period-keyed, closed-period immutable). NOT a revived `build-month-closure.js` (would resurrect a kill-list artifact reading BSF). Page READS `cogs_monthly`, never recomputes (mirror board-grid pattern).
2. **🚫 NEVER-GUESS HARD — inventory GL (col E, 1200.x) has NO canonical home.** Verified: `ref_mi_categories.default_gl_account` = CHARGE GL only (→col F); no inv-GL column on ref_mi/categories/subcategories; `inv_charges_bc` has ZERO 12xx rows (it's P&L charge export, 2026-03→05 only). ⟹ source the 1200.x→category map from the EXT sheet (canonical-statement seed, traces to a cell — like `ref_budget_cogs`), NEVER infer from row order. New table `ref_cogs_fiche_categories` = fiche-row master (fiche_label, inventory_gl E, charge_gl F, sort, + category/subcat→fiche-row map). Operator ratifies any unmapped cat.
3. **WIP (col H) = brewing categories ONLY** (Malt/Hops/Ingrédients/Levures), blank for packaging (liquid-in-tank carries no packaging). `inv_tank_balances.volume_hl × brew_cost_per_hl` per tank, split to category via OBSERVED brewing consumption ratio per `(recipe_id_fk,batch)` (NOT beer name; NOT flat-spread). ⚠️ inherits the `bd_brewing_ingredients_parsed_v2`-frozen-at-2024-06 gap go-forward (April OK); unallocatable tank → surface as "WIP unallocated", never NULL-split or dump-into-Malt.
4. **FG (col I) = `inv_fg_stocktake` qty × `ref_sku_bom` decomposition** (liquid lines→brewing cats, packaging lines→Cartons/Bouteilles/… → spreads across ALL cats incl packaging — why FG fills every row, WIP only brewing). Value at month-close BOM, stamp computed_at; FG liquid line uses prev-month WIP CHF/HL (verify BOM liquid basis vs EXT April).
5. **K opening = EXTERNALLY-ANCHORED, not self-chained (for now).** Not DB-derivable (charges_bc no 12xx, starts 2026-03). April: take K from EXT/bookkeeper as given (guessing it corrupts L). Go-forward: `cogs_monthly.opening = prior-month closing` (self-chain) seeded from bookkeeper anchor + a CHECK line (self-chain vs Compta), Vue-comptable+CHECK pattern (`30d7adf`); never overwrite anchor with chain silently.
6. **EXT write-back = TEMPORARY CFO BRIDGE, one-way, flagged.** EXT is on the BSF-exit kill-list, NOT a keeper. Canonical = maltyweb fiche page + CSV. EXT D3:L16 writes the SAME numbers the page renders (page+sheet both read `cogs_monthly`; sheet = write-only projection, never read back). scrapping-backlog gate: drop once CFO accepts maltyweb+CSV.
7. **IA = 5th tab in `financier.php`** (id=239, manager) "Variation de stock / Fiche COGS", lazy per-month slice `financier-data.php?module=cogs-fiche&month=`. ⚠️ label distinctly from existing per-HL "COGS & Ventes" tab (two things called COGS on one page = confusion): "COGS — variation de stock" vs "COGS — ventes (CHF/HL)".
**SEQUENCING:** (1) `ref_cogs_fiche_categories` master + EXT seed [GATES all] → (2) `cogs_monthly` writer April-only → (3) **ACCEPTANCE GATE: Opus hand-reconciles April to J=394,367.74 / L=−36,986.26 BEFORE Kouros** (COGS-bearing-claim discipline; sub-agent PASS = a claim) → (4) Financier tab → (5) CSV → (6) EXT bridge (flagged) → (7) closed-period immutability + freshness.
**EQUIP:** sql + coder + ui + webapp-testing (manager-auth FULL-PAGE screenshot = load-bearing UI gate on this role-scoped page) + xlsx (EXT write-back). Mig pre-flight: `--status` first + `ls db/migrations` (parallel sessions live) + schema_meta row for `ref_cogs_fiche_categories`.

## ✅ 2026-06-10 — SINGLE-SOURCE COGS REPOINT — SHIPPED (maltytask `489ab70`, PM-verified live)
**SHIPPED RECORD (supersedes the ruling below):** `lib/repos/mysql-sales.ts` `loadSales()` now reads ONLY `inv_sales_ledger` (44,164 rows, 2021-01→2026-06), retiring the `inv_sales_bc` (6mo) + `inv_sales_orders` (Shopify) union. 2026-04 b2b/taproom/eshop reconcile to ledger 0.00%. Artifact `interfaces/sales-cogs-data.json` regenerated = **66 months** (was 6), deployed to VPS. The canonical ledger writer `scripts/python/ingest_bc_sales_ledger.py` committed (was untracked) in the same arc. COGS numbers shifted (now complete ledger — 2026-05 salesCOGS ~69,136) = expected + more correct.
**⚠️ EVOLUTION vs the ruling (the seam): ledger-only FOR NOW.** The shipped `sale_class` filter = {b2b, eshop, taproom, event, giveaway, staff} (excl customs_artifact/transfer/sample/other) — i.e. eshop + taproom ARE included from the ledger today (NOT excluded as the ruling's line 126 planned). Reason: ledger is the single source NOW; eshop→Shopify and taproom→Lightspeed (custom API requested) peel off to dedicated feeds LATER, isolated behind the `sale_class` filter — flip those two classes off the ledger when their feeds land. **Sign convention VERIFIED EMPIRICALLY:** outbound = NEGATIVE `qty_signed` AND `hl_resolved` (both negated → positive units/HL); `sales_amount_chf` is revenue-signed (credits negative → plain SUM nets, NEVER negate). SKU resolved sku_id_fk → ref_sku_aliases → raw (unresolved surface as `unknownSKUs`, never guessed).

### Prior ruling (2026-06-10, now realized — kept for the overlap proof + sequencing rationale)
Kouros ruled the COGS sell-side must read the SAME single source as sales going forward (2021+). PM verdict + the overlap proof:
- **`inv_sales_ledger` IS the canonical single sales source — a SUPERSET that ALREADY CONTAINS the BC sales, NOT a parallel store.** PROVEN LIVE 2026-06-10: ledger 2026-05 `sale_class='b2b'` net = **433,760 CHF** == `inv_sales_bc` 2026-05 b2b **434,008 CHF** (Δ 248 / 0.06%); ledger taproom 14,132 == bc taproom 14,132 EXACT. ⟹ pointing `build-sales-cogs` at the ledger AND keeping the `inv_sales_bc + inv_sales_orders` union = **DOUBLE-COUNT.** loadSales must read the ledger and DROP the bc+orders union (the union becomes the legacy 6-month path).
- **doc_type aggregation = naive `SUM(sales_amount_chf)` across ALL doc_types nets correctly** — PROVEN: credits carry NEGATIVE CHF natively, so a plain SUM nets shipment+invoice+credit+return_receipt with NO per-doc_type CASE logic. **DO NOT sum qty_signed expecting positive (outbound is NEGATIVE) — units = `−SUM(qty_signed)`; but for CHF revenue/COGS the sign is already revenue-oriented, just SUM.** shipment vs invoice are DISTINCT document rows (920 ship + 180 inv in Apr) — do NOT add gross+gross; the net-by-customer view is the lens (and a plain SUM already gives net).
- **COGS lens = filter `sale_class` (on ref_customers, inherited via customer_id_fk JOIN), NOT doc_type.** Outbound product-sales COGS = sale_class IN (b2b, event, giveaway, staff) [= counts_as_unit_sale TRUE]; EXCLUDE eshop+taproom (BC 1080/3822 postings — Shopify/Lightspeed own them, double-count: ledger May eshop 44,879) and customs_artifact+transfer+sample (not-a-sale). eshop sale comes from `inv_sales_orders` (Shopify), taproom from its own feed — NOT the ledger eshop/taproom rows.
- **Unresolved sku_id_fk = 10,960 (CHF 2,791,066 / units 703,798) — POLICY: cost resolved rows, surface unresolved as a visible "non rattaché" bucket, NEVER block & never guess.** The tail is MOSTLY legitimately NON-FG: CAUF(5561)/CAUAL/CAU-T = deposits→GL1302, CO2-FO, ART-000xxx generic, ECO30, SEPI. But REAL FG hide there needing ref_sku_aliases: **ZEP6C, BLO4C, BLO4, BLA4, PACKDECX10, PBD.** Resolution path = `data/bc-ledger-sku-review.csv` (214 codes, operator classifies FG_ALIAS→`ref_sku_aliases` / NON_FG_EXCLUDE); loader self-heals on re-run. This is the SAME surface-don't-guess tail as §2026-06-10. NB build-sales-cogs re-resolves `sku_code_raw → ref_skus.sku_code` at READ time (its own JOIN) — once an alias lands the sale costs automatically; the stored NULL is irrelevant (§CONTRACT-SKU (4) discipline).
- **SEQUENCING (PM ruling): repoint loadSales → inv_sales_ledger FIRST as its own COGS-bearing change, THEN build Financier ON the unified artifact.** Confirmed Kouros's instinct. The repoint is COGS-bearing → Opus-verified, `--cost-source` parity unaffected (cost side already cutover 06-10), but run a SALES-side parity check: ledger-COGS for a closed month (e.g. 2026-04) must reconcile to the bc+orders-union COGS the old path produced (expect b2b match within rounding; eshop now via Shopify; the DELTA is the previously-missing pre-2025-12 history + the credits/returns netting). Financier page then reads the ONE artifact (`interfaces/sales-cogs-data.json` or, if the page is maltyweb-native, the ledger directly via a PHP read — but ONE source either way).
- **The just-run all-months regen (bc+orders, 6mo) is MOOT — HOLD/discard that artifact.** Do not deploy it; it's the about-to-be-superseded partial source.
- **OPEN dependency:** the ledger loader `scripts/python/ingest_bc_sales_ledger.py` is UNCOMMITTED (§2026-06-10) — commit it as part of this arc so the canonical source has a tracked writer. And the BC item ledger is broad-item (carries non-FG); the FG-velocity/COGS surface filters to resolved-FG + counts_as_unit_sale, per §2026-06-09 architecture rule.

## ✅ 2026-06-10 — FINANCIER PAGE v1 — SHIPPED+LIVE (maltyweb `6a013ad`, mig 304)
Read-only financial-analytics hub `public/modules/financier.php` (page_key='financier', **ref_pages id=239, min_role='manager', domain='general', sort=125, is_active=1** — PM-verified live; manager preset). CONSUMES canonical artifacts, **NEVER recomputes** (SOT-safe):
- **COP** ← `cogs-report-data.json` via `kpi_load_cogs_json` (5-section + COP/HL + brewing split + month-picker + trend).
- **COGS** ← `sales-cogs-data.json` (THE single-source ledger artifact, §SINGLE-SOURCE above) via NEW `kpi_load_sales_cogs_json` + `kpi_sales_cogs_month_slice`. Totals + per-SKU table + beer-tax-by-cat + non-rattaché bucket.
- **Marge/ASP** derived from the COGS slice. Coût-par-SKU links OUT to `sku-costs.php` (no dup).
- **Architecture:** the 5.2MB/66-month artifact is NOT injected into the page — `public/api/financier-data.php` is a manager-gated LAZY per-month-slice endpoint (~43KB HTML). CSS/JS separated + cache-busted (`?v=filemtime`) + fr-CH fmt. Per-module freshness stamp (manual-regen; **freshness CRON = deferred fast-follow**).
- **VERIFIED:** data fns artifact-exact (COP 2026-04 = 88.76 CHF/HL; COGS 2026-05 salesCOGS = 69,135.53); `php -l` clean; auth gates 302/401/403, viewer denied (screenshot); XSS-safe.
- **✅ 2026-06-10 — RENDER BUG ROOT-CAUSED + FIXED (maltyweb `f1ff3a1`, pushed + live-confirmed). The manager VISUAL smoke is now DONE and it caught a real bug.** Symptom: "all 4 sections don't work." ROOT CAUSE was CSS, not data and not JS: `public/css/financier.css` had `.fin-panel{display:flex}` which OVERRODE the HTML `[hidden]` attribute (a `.class{display}` rule beats `[hidden]`'s UA `display:none` on specificity) AND `.fin-panel--active` was empty → all four panels (COP/COGS/Marge/SKU) rendered STACKED at once, so clicking tabs did nothing visible. Content + data were ALWAYS correct (manager-auth Playwright smoke verified COP 77,169 CHF / COP-HL 88.76 / Marge 85.8%). FIX = `.fin-panel{display:none}` + `.fin-panel--active{display:flex}` (tab JS already toggles `--active`; server-rendered HTML sets it on COP). **DURABLE LESSON (now in the `ui` skill SKILL.md rendering-bug catalog as the `[hidden]`-overridden-by-`.class-display` trap, mirror of the `<dialog> display` trap): `php -l` + data-layer CLI checks PASS a stacked/broken page — only a real manager-auth FULL-PAGE SCREENSHOT caught it. On a role-scoped page the manager-render screenshot is the LOAD-BEARING UI gate, not optional.** *(Earlier static-analysis dead-end — node --check, hoist, data-tab↔fin-panel-* contract, render-target IDs, all-200 logs — missed it because the fault was CSS specificity.)*
- **⚠️ STILL OPEN — Visite-guidée tour card for /financier NOT dispatched** (tour-gap-check flags Financier + the FG-stock page CRITICAL; tour steward won't deploy from the dirty shared tree — dispatch once the tree is clean).

## ✅ 2026-06-11 — RE-SOURCE SHIPPED + LIVE + COMMITTED (Stages 1-5, ARC FULLY COMPLETE) — the ruling below was CORRECT against live data; Explore's "expected/correct" read was WRONG, Kouros confirmed
**STATE OF RECORD (2026-06-10/11): the PER-MODULE CANONICAL-SOURCE re-source is DONE — Financier COP+COGS grids now read the OPERATIONAL feed, booked GL DEMOTED to a "Vue comptable (GL booké)" secondary + CHECK line. PM-VERIFIED LIVE 2026-06-11 against the deployed feed.**
- **Stage 1 — maltytask `dfcd878`:** `build-cop-report.ts` now emits **`cop.glLines`** per month = a MAP keyed by canonical `ref_mi.gl_account` (21 board GL accounts: brewing 41xx, packaging 42xx, indirect 43xx, transport 46xx, utilities 47xx incl. **4701 waste added to the BC pull**, R&D 4500/4510). Operationally sourced (consumption×WAC + packaging volumes + utility invoices), grouped to GL for PRESENTATION. Feed redeployed to VPS `interfaces/cogs-report-data.json` (backup `.bak-preglines`). **PM-VERIFIED 2026-06-11 (live deployed feed, period 2026-04): glLines present (21), Σ glLines = 77,358.84 CHF; `cop.totalVariables.total` = 77,169.37 (legacy field, EXCLUDES rd); difference = 189.47 ≈ 4500(29.42)+4510(160.04)=189.46 R&D. hlPackaged=869.4 / hlBrewed=1021.4. 77,358.84÷869.4 = 88.98 (grid headline); 77,169.37÷869.4 = 88.76 (tile). Both internally consistent — the grid≠tile gap IS the rd line, by design (see denominator + subtotal notes below).**
- **Stage 2 — maltyweb `30d7adf`:** Financier `cop-grid` module (`financier-data.php`) re-sourced booked-GL→operational `cop.glLines` ÷ **HL PACKAGED** (matches the COP tile → no two-numbers-one-fact). April TOTAL COP VARIABLE **88.98 CHF/HL**, all 21 lines reconcile, Apr+May complete. Booked GL DEMOTED to "Vue comptable (GL booké)" secondary block + **CHECK line = opérationnel − comptable** (+87.16 April, since GL still unbooked-thin). Manager-auth screenshot verified render.
- **Stage 3 — maltytask `f5482a0` + maltyweb `9d0569c`:** `build-sales-cogs.ts` emits per-GL-line COGS (material = sold units × per-GL unit cost reusing `costByGl`; overheads LIFTED from the COP feed's glLines — **single source, no re-derivation**; +`hlSold` +beer tax). `cogs-grid` module re-sourced ÷ **HL VENDU**. Material reconciles to `totals.material_CHF` within 0.4%, zero unmatched sold SKUs. April TOTAL COGS VARIABLE **69.70 CHF/HL** + **taxe bière 13.08** as its own line; May populated. Same Vue comptable + CHECK pattern. Manager-auth screenshot verified (smoke acct id=16 elevated→shot→reverted to viewer).
- **Stage 4 (BUDGET) — maltyweb `b477565` (pushed; mig `327`) — ARC CLOSED, all 4 stages done.** NEW canonical **`ref_budget_cogs`** = 21 GL-line rows, `fiscal_year=2026`, `budget_chf_per_hl`, schema_meta classified. **SEEDED from the operator's board-sheet COGS&YIELDS BUDGET column** (canonical-statement seed, every number traces to a cell — NOT guessing), **total 75.76 CHF/HL**. cogs-grid module loads budget per line → **BUDGET (month + ytd, FLAT ANNUAL RATE) + Vs BUDGET = (actual−budget)/budget %** columns now LIVE **on the COGS grid only** (April proof: Malts −37.3%, Hops −20.6%, **TOTAL COGS VARIABLE −8.0%**). **COP grid UNCHANGED — no budget on its source sheet.** **N-1 / Vs-N-1 still render "—"** (2025 GL not ingested — backlog). Manager-auth screenshot verified. **v1 APPROXIMATION (documented, not silent): Transport + QA/QC budgets are monthly-profiled in the source sheet but seeded as the annual rate** — a known simplification to revisit if monthly-profile budget matters.
  - **⚠️ GIT HYGIENE INCIDENT (2nd bare-commit sweep in ONE session):** `b477565` swept a PARALLEL expeditions session's staged DELETION of `public/css/expeditions-sku-history.css` and was pushed before catch. Deletion was SAFE (file unreferenced; expeditions work already shipped in `e7962ce`; removal was intended) → left as-is rather than rewrite pushed history. REINFORCES the pathspec-commit hard-rule: bare `git commit` on the shared maltyweb tree is now treated as a HARD ERROR; always `git commit -- <explicit paths>` (operator hardened the git memory accordingly).
  - **✅ ALL COMMITS NOW PUSHED — remote == deployed:** maltytask `a08a718`, maltyweb `b477565`. (Clears the §OPEN(4)/§198 "push sweep owed" debt for the whole board-grid arc.)
  - **ARC BACKLOG (non-blocking, does NOT gate the arc — recorded for resumption):** (i) **N-1 cost** = 2025 GL CSV ingest → lights N-1 / Vs-N-1 cols (currently "—"); (ii) **COGS-YTD-starts-March coverage chip** (sales-cogs feed starts 2026-03, so COGS YTD isn't true Jan-start — chip caveat, §OPEN(1)); (iii) **May utilities booking** (gas/elec invoices not yet booked, §OPEN(2)); (iv) **dead `fin_ytd_months(rolling6m)` path the COP stage flagged** — unreferenced code path to clean up.
  - **ARC STATUS: COP/COGS board grids fully re-sourced to operational SoT, per-line, budget-comparable, Vue comptable + CHECK reconciliation — ALL 4 STAGES DONE.**
- **Stage 5 (N-1 / prior-year 2025) — maltyweb `a9e4024` (SHIPPED + LIVE + PUSHED, 2026-06-11) — completes the board-grid arc; the Stage-4 backlog item (i) N-1 cost is now DONE.** N-1 (2025) + Vs N-1 columns lit on BOTH grids, sourced **OPERATIONALLY from the 2025 months already in the feeds — NOT a booked-2025-GL ingest.** 🔑 KEY DECISION (architecturally load-bearing, PM endorses): I checked first and the operational 2025 data was already present (COP feed back to 2023, sales-cogs back to 2021), so ingesting a 2025-GL CSV was **unnecessary AND would have re-created the ledger-vs-operational mismatch the re-source just fixed** — i.e. lighting N-1 from booked GL while ACTUALS read operational = a two-sources-one-comparison divergence. Kouros OFFERED the GL; I wired existing operational data instead and he agreed. **This SUPERSEDES the earlier ruling (§229/§230/§234 Gate-0, §178(i), §184(3)) that N-1 cost requires a 2025 BC GL CSV ingest — that path is now OPTIONAL, see backlog (1) below.**
  - **COP grid:** N-1 = 2025 `cop.glLines` ÷ HL packaged. Columns now **ACTUALS / N-1 / Vs N-1** (NO budget — correct for the COP sheet; any stray budget col removed). April: N-1 **56.74 CHF/HL**, Vs N-1 **+56.8%**.
  - **COGS grid:** N-1 = 2025 sales-cogs `glLines` ÷ HL sold. All **10 board columns now LIVE** (ACTUALS / BUDGET / N-1 / Vs BUDGET / Vs N-1). April: N-1 **57.73 CHF/HL**, Vs N-1 **+20.7%**.
  - **Vs N-1 formula = (actual − n1)/n1 %; renders '—' when n1=0** (honest gap, not 0%). **Coverage chip:** 2025 COP overhead lines (CO2 / utilities / R&D) only ~12/21 populated historically → those N-1 lines read 0 (the material lines are complete; the gap is honest + chipped).
  - **VERIFY:** manager-auth screenshots verified BOTH tabs; smoke account (id=16) elevated → shot → reverted to viewer.
  - **ARC STATUS (FINAL): the Financier COP/COGS board-grid arc is FULLY COMPLETE — both grids operational-sourced per-GL-line, budget + YoY (N-1) comparison live, Vue comptable + CHECK reconciliation. ALL COMMITS PUSHED — maltytask `a08a718`/`dfcd878`/`f5482a0`, maltyweb through `a9e4024`. Kouros closed the session here.**
  - **REMAINING NON-BLOCKING BACKLOG (resumption points, none gate the arc):** (1) **booked-2025 GL ingest** IF Kouros later wants a *comptable* N-1 inside the Vue-comptable block specifically (the operational N-1 above is the headline — this would be a books-bridge N-1 only; OPTIONAL, not the default path); (2) **COGS-YTD-starts-March coverage chip** (sales-cogs feed starts 2026-03); (3) **May utilities booking** (gas/elec invoices not yet booked); (4) **dead `fin_ytd_months(rolling6m)` path** — unreferenced, clean up.
- **🟢 DENOMINATOR VERDICT (operator overrode my pre-build ruling — operator was RIGHT, ruling RATIFIED):** my pre-build ruling said COP ÷ HL **BREWED**. The build used HL **PACKAGED**, and that is the CORRECT choice — the operational COP feed is **packaging-driven** (consumption attributed via `bd_packaging_v2.vendable_hl`), so ÷ HL brewed would make the grid headline ≠ the COP tile (88.76) = a two-numbers-one-fact violation, exactly the divergence smell I enforce against. ÷ HL packaged keeps **grid == tile**. COGS ÷ HL sold (correct, unchanged). NO divergence — endorse the build's choice; my original "brewed" ruling is SUPERSEDED for this grid. (HL brewed remains the right denom for a true unit-COP-of-production view IF one is ever built separately; for the P&L grid that mirrors the tile, packaged is canonical.)
- **🟢 SUBTOTAL SOURCING VERDICT (sound):** grid subtotals computed PURELY from glLines, NOT the feed's legacy `.total` fields — because those legacy fields carry convention mismatches (totalVariables EXCLUDES rd; the utilities `.total` excludes waste 4701). Summing glLines avoids both → the grid is internally consistent and rd/waste are visible. This is WHY grid headline 88.98 > tile 88.76 (tile reads the rd-excluding legacy total). Architecturally clean — glLines is the single grouped truth.
- **🟢 SINGLE-SOURCE OVERHEAD VERDICT (sound):** COGS overheads are LIFTED from the COP feed glLines (not recomputed) → one source of overhead truth feeding both grids. Correct — no parallel overhead derivation.
- **booked GL (`inv_charges_bc`) is now reserved for the Vue comptable / CHECK reconciliation view ONLY — never the headline.** Marge + Coût par SKU were already correctly sourced (sales-cogs / live `ref_sku_bom × v_mi_cost`), no change.
- **OPEN (post Stage-5; arc FULLY COMPLETE, these residuals remain, non-blocking):** (1) **COGS-YTD coverage chip** — sales-cogs feed starts 2026-03 → COGS YTD isn't true Jan-start yet (shows available-months); COP feed has full history. Both honest; chip note still owed. (2) **May utilities = 0** (gas/elec invoices not booked) — operational feed gap; May utilities booking owed. (3) ~~N-1 cost~~ **✅ DONE Stage 5 (`a9e4024`, 2026-06-11)** — N-1 / Vs-N-1 lit OPERATIONALLY from 2025 feed months on BOTH grids (NOT a GL ingest — that's now the OPTIONAL comptable-N-1 path, backlog (1) above). (4) **dead `fin_ytd_months(rolling6m)` path** the COP stage flagged — unreferenced, clean up. **✅ Stage 4 BUDGET SHIPPED 2026-06-11 (mig 327, `b477565`); ✅ Stage 5 N-1 SHIPPED 2026-06-11 (`a9e4024`, no mig); ✅ push debt CLEARED — maltytask `a08a718` / maltyweb through `a9e4024`, remote == deployed.** → cross-ref kpi-tracker-catalog/README.md §OPTION C + the BOARD GRID record below (now superseded by this re-source).

## 🟥 2026-06-10 — PER-MODULE CANONICAL-SOURCE RULING (Kouros pushback on the GL grid: "you have Apr/May COP data; data sourced from wrong DB") — ✅ EXECUTED, see re-source record above
**RULING (verified live 2026-06-10): the GL grid sources COP/COGS ACTUALS from BOOKED GL (`inv_charges_bc`), which is THIN for Apr (20 rows)/May (22 rows) — only March full (411). But COP is an OPERATIONAL metric and the operational data for Apr/May IS rich in MySQL: `inv_consumption` Apr 1022 rows/74 MIs, May 287; `bd_packaging_v2.vendable_hl` Apr 869.4 / May 1399.6 HL; `inv_energydata` Apr invoice present. So the grid reads the WRONG database for an operational metric. Kouros is right.**
**THE TWO-LAYER MODEL (the architectural reconciliation):** there are TWO legitimately distinct COP/COGS computations and they must NOT be conflated:
1. **OPERATIONAL COP/COGS** = consumption × WAC + packaged/brewed HL + utility invoices. Canonical = **`scripts/build-cop-report.ts` → `interfaces/cogs-report-data.json`** (the COP feed; reuses `lib/cop-indirect.js`/`lib/utilities.js`/`lib/inventory.js`) + **`build-sales-cogs.ts` → `sales-cogs-data.json`** (COGS). This is the management/decision metric. **It HAS Apr/May (deployed feed regenerated 2026-06-10 08:57, Apr total 77,169 CHF / 88.76 CHF·HL).** This is what the tiles + Financier COP/COGS panels read.
2. **ACCOUNTING/PNL-BRIDGE** = booked GL net (`inv_charges_bc` SUM(debit−credit) by `gl_account_no`) ÷ HL, with escompte/taxe-bière/CHECK/PNL bridge. This TIES TO THE BOOKS, lags the bookkeeper, and is the GL grid I built. Legit — but it is the RECONCILIATION view, NOT the primary ACTUALS.
**PER-MODULE CANONICAL SOURCE (the ruling Kouros asked for):**
- **A. COP cost tree:** Brewing (malt/hop/yeast/ingredients) + Packaging materials → OPERATIONAL: `inv_consumption × v_mi_cost` (WAC) + `ref_sku_bom × bd_packaging_v2` volumes (the COP feed already computes this). Indirect/Utilities/R&D → the COP feed ALREADY folds them via `cop-indirect.js` (actual spend) + `utilities.js` (`inv_energydata`, invoice>estimate per month). So COP ACTUALS canonical = **the operational COP feed, NOT booked GL.** GL labels (4101 etc.) are a PRESENTATION mapping over the operational categories. **April/May COP are NOT gated on the bookkeeper** — they exist now.
- **B. COGS:** canonical = `sales-cogs-data.json` (`inv_sales_ledger` sold units × `ref_sku_bom` material cost + beer tax via `ref_beer_types`). The COGS&YIELDS GL structure + PNL bridge (escompte/taxe bière/CHECK/PNL) = the ACCOUNTING view → cost lines from the OPERATIONAL COGS, bridge lines (escompte/taxe/CHECK) from `inv_sales_ledger` + `inv_charges_bc` reconciliation.
- **C. Marge/ASP:** canonical = `sales-cogs-data.json` (revenue − COGS per SKU). Confirmed.
- **D. Coût par SKU:** canonical = `ref_sku_bom × ref_skus × v_mi_cost` (live MySQL). Confirmed (the Financier SKU panel already reuses sku-costs' verbatim ref_sku_bom query).
- **E. The booked-GL grid:** KEEP, but RE-LABEL + DEMOTE. It is the **accounting/PNL-bridge / CHECK view**, not the primary ACTUALS. Both views coexist on the SAME Financier COP/COGS tabs: primary number = OPERATIONAL COP feed (covers Apr/May); a clearly-labelled secondary "Vue comptable (GL booké)" block = the GL grid + CHECK line that ties operational to books WHEN the month is fully booked. `inv_charges_bc` is reserved for the bridge/reconciliation + the indirect/utilities GL detail, NOT for the headline COP/HL.
- **F. Live MySQL vs stale JSON:** YES — this is the deeper fix. `cogs-report-data.json` is a manually-regenerated snapshot (the LOCAL copy was 2026-05-27 stale; the DEPLOYED copy is fresh but only because Option-C re-ran it by hand). Per maltyweb-prefers-live + strangler-consume-the-host: the COP/COGS feeds should be **live MySQL reads (PHP views/handlers over `inv_consumption`/`v_mi_cost`/`bd_packaging_v2`/`inv_energydata`)**, not a snapshot JSON regenerated off-VPS over a tunnel. Interim is acceptable (cron the producer + freshness chip); the durable fix is a maltyweb-native COP read. This is the real architecture point.
- **G. Budget:** Kouros will provide. Land in NEW canonical **`ref_budget_cogs`** (mig + schema_meta). Granularity = **per GL-line-key × month × metric(CHF total + CHF/HL)** mirroring the sheet's BUDGET column grain, so the grid's BUDGET + Vs-BUDGET cols light up by a straight JOIN. Seed ONLY from the operator's explicit budget tab (xlsx read, every cell traced) — that is canonical-statement seed, NOT guessing.
**DIVERGENCE FLAG:** showing the GL-booked thin-month number AS the headline COP ACTUAL is a SILENT UNDER-READ (Apr would read ~5k vs the real ~77k) — exactly what tripped Kouros. The fix is not more GL data; it is sourcing the headline from the operational feed and reserving GL for the bridge.
**SEQUENCING TO CORRECT:** (1) Repoint Financier COP/COGS HEADLINE ACTUALS from the GL-grid module to the OPERATIONAL feed (COP feed + sales-cogs) — the tiles already do this; align the grid's top line to match. (2) Re-label the GL grid as "Vue comptable (GL booké)" secondary + add the CHECK line (operational − booked) so the divergence is visible, not hidden. (3) Make the COP feed live (maltyweb-native read or cron+freshness-chip) — F's durable fix. (4) Budget table (G) when Kouros provides. (5) N-1 cost from 2025 GL ingest when provided. **EQUIP: (1)(2) ui+sql+coder+webapp-testing (manager full-page screenshot load-bearing); (3) coder+sql; (4) sql+coder+xlsx.** Cross-ref phase-d-cost-source-swap.md §two-layer (operational vs GL) + kpi-tracker-catalog/README.md §OPTION C.

## ✅ 2026-06-10 — BOARD COGS/COP P&L GRID — SHIPPED+LIVE (ACTUALS-only phase 1) | ⚠️ SUPERSEDED 2026-06-11 by the RE-SOURCE (top of file)
**⚠️ SOURCE NOW CHANGED:** this v1 read ACTUALS from BOOKED GL (`inv_charges_bc`) ÷ HL brewed (COP) / HL sold (COGS). As of 2026-06-11 (Stages 1-3, commits maltytask `dfcd878`/`f5482a0` + maltyweb `30d7adf`/`9d0569c`) the grid headline reads the OPERATIONAL feed (`cop.glLines`) ÷ **HL PACKAGED** (COP) / HL sold (COGS); booked GL is DEMOTED to the "Vue comptable (GL booké)" + CHECK secondary. The denom shifted brewed→packaged (ratified — see RE-SOURCE record). Record below retained for the GL→line map + helper history only.**
**STATE OF RECORD (2026-06-10): SHIPPED + LIVE + COMMITTED `d0c7e60` on maltyweb main — LOCAL, NOT PUSHED. Built into Financier page (id=239) per ruling — host EXTENDED, no new Node script / no new JSON. ACTUALS columns only ("build first, settle comparison sources after").**
WHAT SHIPPED:
- `public/api/financier-data.php`: new manager-auth modules **`cop-grid` + `cogs-grid`**. ACTUALS computed server-side from `inv_charges_bc` grouped by `gl_account_no`, **net = SUM(debit)−SUM(credit), is_summary=0**. Line→GL map mirrors the sheet EXACTLY (Ready Beer 4100, Malts 4101, Hops 4102, Yeast 4103, Other Ingr 4104; Bottles inc TEA 4200+4201, Cans 4202, Reg.cardboard 4203+4207, Special 4204, Plastic 4205, Labels 4206, Keg 4209, External 4208; CO2 4300, Chemical 4301, Small equip 4302, Transport 4600+4602; Gaz&Water 4700, Electricity 4702, Waste 4701; QA/QC 4500, Purchases 4510). New helpers **`fin_gl_booked_months()` + `fin_ytd_months()`** intersect the YTD/6M-rolling window with months that actually have booked GL → numerator (cost) + denominator (HL) cover the SAME months (honest under-read, no phantom dilution).
- Denominators: COGS grid ÷ **HL sold** (`inv_sales_bc.hl_resolved` by period); COP grid ÷ **HL brewed** (reuses FIN_COP feed's `hlBrewed`). (Per-type KEG/BOTT/CAN/BULK HL split = placeholder, not yet wired — see below.)
- `public/modules/financier.php`: grid card at TOP of `fin-panel-cop` + `fin-panel-cogs`, reused month picker (offers ONLY booked months), source chip "mois complets uniquement".
- `public/js/financier.js` + `public/css/financier.css`: `renderCopGrid`/`renderCogsGrid` tree table (indented lines, bold subtotals/totals), `.fin-`-prefixed classes extending `.fin-table`.
**VERIFIED (independent SQL, 2026-03): Total Brewing 49.57, Total Packaging 49.50 (4206 Labels = −5.34, a CREDIT month — credit-net working), Total Indirect 4.69, Total Utilities 1.52 (waste only — 4700/4702 gas/elec NOT booked in March GL), Total R&D 0.21, TOTAL COGS VARIABLE 105.48 CHF/HL, YTD 105.48 (March = only booked month in window). Manager-auth FULL-PAGE screenshot confirmed render (not blank — the load-bearing UI gate satisfied). `php -l` clean; endpoint 401 (auth-gated, not fataling).**
**PLACEHOLDERS — render "—" (the OPEN follow-on, no canonical source yet): BUDGET (all), N-1/2025 cost (all), variance % (derived from those), semi-variable WORK brewing/packaging labor split, COGS OVERALL grand total, COP per-packaging-type block (Kegs/Bottles/Cans/Bulk), Scotch line. Zero-booked accounts render "0.00" (DISTINCT from "—").** Discipline held: refuse-don't-guess on every absent source.
**DATA-COVERAGE GATES (operator-facing, Kouros to action):**
- `inv_charges_bc` holds ONLY **2026-03 (full, 411 rows)**, 2026-04 (THIN, 20 rows), 2026-05 (THIN, 22 rows). Only March is a complete GL month → grid **defaults to 2026-03**. YTD/rolling stay honest (intersect-with-booked) but UNDER-READ until bookkeeper uploads full monthly GL via Le Cockpit. **April/May "thin" can't be auto-distinguished from complete** — covered only by the chip caveat (a known soft gap; no per-month completeness flag exists yet).
- BUDGET + N-1 sourcing DEFERRED by Kouros ("after we finished building"). Both = fixed history. **Candidate sources found in board sheet `1jbin-zHrhNrs0b6k7Xl9sECn_eqgxnmgkgS-Y08N_AA`: tabs "2026_ SALES MONTHLY BUDGET", "BUDGET MONTHLY 2025", "25BUDGET SKUs" carry monthly series** → seed `ref_budget_cogs` + a 2025-N-1 cost table. Awaiting Kouros's decision next session.
**COMMIT HYGIENE: `d0c7e60` is CLEAN (4 files only). First commit accidentally swept 3 staged parallel-session files (`app/kpi-handlers.php`, `public/css/mon-tableau.css`, `public/js/kpi-charts.js`) via the shared index → `reset --soft` + recommitted only the 4 Financier files, then restored those 3 to staged. Parallel sessions' staged index intact.** (Same shared-index race lesson as `6a013ad`/mig-305 below — confirms: separate clones/worktrees or serialize commits.)
**⏭️ NEXT (obvious follow-on, PM-endorsed): the BUDGET/N-1 SEED-FROM-SHEET pass** — ingest the 3 board-sheet tabs into `ref_budget_cogs` (mig + schema_meta) + a 2025 cost history table, then wire BUDGET / N-1 / variance% columns (steps 2–4 of the ruling's sequencing). EQUIP: budget→sql+coder+xlsx; 2025-cost→sql+coder; variance→coder+ui. **Seeding from the operator's explicit budget tab is NOT guessing (canonical-statement seed) — but every number must trace to a sheet cell, never a pattern.** Gate: confirm with Kouros WHICH tabs are authoritative before reading.
**`git push` of `d0c7e60` still owed** (currently local-only on main).

---
### PM RULING (pre-build, retained for rationale) — 2026-06-10
Kouros wanted to rebuild his Google-Sheet board report (id `1jbin-zHrhNrs0b6k7Xl9sECn_eqgxnmgkgS-Y08N_AA`, tabs "COGS & YIELDS" A9:K64 + "COSTS OF PRODUCTION" A10:G67) — a **structured GL-line P&L grid** (CHF/HL tree per GL account: 4100 Ready Beer / 4101 Malts / … / 4209 Keg / 4300 CO2 / 4700 Gaz&Water / R&D 4500+4510, then escompte/taxe bière/CHECK/PNL bridge), with columns ACTUALS·BUDGET·N-1·Vs-BUDGET%·Vs-N-1% (COGS tab) and ACTUALS·N-1·Vs-N-1% over month+6M-rolling (COP tab; NO budget col there).
**WHERE (ruling): EXTEND the existing Financier page (id=239) tabs — NOT a new page, NOT two pages.** Financier already owns finance reporting (read-only, manager/general) + already has COP + COGS&Ventes tabs + already consumes the 2 canonical artifacts. Sheet "COSTS OF PRODUCTION"→COP tab, "COGS & YIELDS"→COGS tab. A new page = parallel surface (RULE-1 divergence). This is a RENDERING UPGRADE of the existing tabs (compact-trend → full GL-line grid), same page+`financier-data.php` lazy-slice pattern.
**🔴 LIVE-VERIFIED SOURCE TRUTH (2026-06-10, the two load-bearing facts):**
- `inv_charges_bc` (booked GL cost actuals) holds **ONLY 2026-03/04/05** (+98 NULL-date). Keys `gl_account_no`+`period_text` (free-text "Période : 01.04.26..30.04.26"), `debit_amount`/`credit_amount`. **NO 2025/2024 GL → N-1 COST COLUMNS HAVE NO SOURCE.**
- **NO budget table exists** (no `*budget*`/`*plan*`/`*target*`). **BUDGET COLUMNS HAVE NO SOURCE.**
- `inv_sales_ledger` = full **2021→2026** (`posting_date`/`qty_signed`/`sales_amount_chf`/`hl_resolved`) → N-1 on REVENUE/HL/MARGIN exists today; only the cost lines are blocked.
**DATA-SOURCE MAPPING (ruling):**
- ACTUALS cost lines = **`inv_charges_bc` SUM(debit−credit) grouped by `gl_account_no`** ÷ HL (NOT the consumption/WAC COP feed — the grid is a P&L bridge w/ CHECK/PNL that must tie to BOOKED GL). Encode ONE canonical GL→line_key map (multi-GL lines: Bottles 4200+4201, Transport 4600+4602, Reg.cardboard 4203+4207); unmapped GL → surfacing catch-bucket (refuse-don't-drop, or CHECK won't tie). Works TODAY on the 3 months present.
- BUDGET = **new canonical `ref_budget_cogs`** (mig + schema_meta), seeded ONLY from operator's explicit 2026 budget tab (xlsx read); until then render "—". Inventing a budget = guessing a COGS number (forbidden).
- N-1 cost = ingest 2025 BC GL CSV into `inv_charges_bc` via existing `ingest-charges-bc-csv.php` (append-only/row_hash-dedup; gated on operator producing the CSV) ELSE render "—". N-1 revenue/HL/margin = `inv_sales_ledger` today.
- ABSENT-source columns render **"—", never 0/guess.**
**HL DENOMINATORS (confirm w/ operator, mirror sheet): COP tab = per HL BREWED** (`bd_brewing_gravity_v2` Cooling final_volume, neb-only — `build-cop-report.ts`'s `hlBrewed`) for ALL variable sections; **COGS tab = per HL SOLD/PACKAGED** + per-type HL (KEG/BOTT/CAN/BULK). NEVER reuse one HL across both tabs (silent CHF/HL distortion). `build-cop-report.ts` already emits hlBrewed + hlPackaged separately.
**vs KPI TILES: COEXIST, no retirement.** Tiles (#168/#169/#171/#172/#178/#184) = consumption/WAC COP at-a-glance; grid ACTUALS = booked GL. They WILL differ (that IS the CHECK-line variance) → LABEL tiles "COP (consommation)" vs grid "Comptabilité (grand-livre)"; don't force-equal.
**SEQUENCING:** Gate-0 (operator) = will he provide (a) 2025 BC GL CSV [unlocks N-1 cost] + (b) 2026 budget tab [unlocks BUDGET]? Build grid ACTUALS-first regardless (works on 3 months). (1) GL→line map + ACTUALS grid COP-then-COGS [single-owner `financier.php`/`.css`]; (2) N-1 cols (revenue now / cost after 2025 ingest); (3) BUDGET (`ref_budget_cogs` + ingest); (4) variance % (trivial derivation last). **EQUIP step1 sql+coder+ui+webapp-testing (manager full-page screenshot = LOAD-BEARING gate — the `[hidden]` CSS bug on THIS page proves it); 2025-GL ingest sql+coder; budget sql+coder+xlsx; variance coder+ui.**
**DIVERGENCE FLAGS:** N-1 cost has no source (don't backfill from feed); no budget table (don't hardcode/pattern); ACTUALS must be 100% booked-GL not blended; HL-brewed vs HL-sold cross-contamination; unmapped GL silently dropping from grid.

## 🟢 2026-06-10 — sku-costs RETIREMENT (#2) — NOW UNBLOCKED (not yet built)
Gate (#1 Financier going green) is satisfied by the `f1ff3a1` render fix. PM picking it up next; only remaining blocker = a flaky local→site browser network for the verify screenshot. Plan UNCHANGED: inline `sku-costs.php`'s per-SKU LIST into the Financier `fin-panel-sku` panel reusing the SAME `ref_sku_bom` read (NO re-derivation = divergence flag), KEEP `sku-cost-detail.php` (drill-down, 2 callers, NOT the scrap target), then `is_active=0` ref_pages id=12 sku-costs + log the .php to scrapping-backlog (gate: panel covers the list + 0 inbound routes).
