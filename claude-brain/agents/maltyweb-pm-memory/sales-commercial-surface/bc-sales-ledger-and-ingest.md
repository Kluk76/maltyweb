# Sales-commercial — BC sales ledger + ingest (recurring customer sync, discount→canonical, sale_class taxonomy, item-ledger load, OData connector, ingest path, contract-SKU recipe)

## 🆕 2026-06-15 — RECURRING BC → ref_customers SYNC — PM DESIGN RULED (pre-build; live-verified). Trigger "synch les clients"/"customer sync"/"BC customer"/"drifted bc_customer_no"/"Stoked Mountain"/"client sync".
> **SHIPS + RUNS FIRST, BEFORE resuming the BC-orders Phase-1 `--apply`** (clean customers → clean order resolution; ORD210051 then resolves to corrected Stoked record + collision-detects vs order id=49).
>
> **🟢 LIVE-VERIFIED 2026-06-15 (supersedes the canary's premise on one point):** `ref_customers` total=3060, with bc_no=2688, is_active=0 → 369, merged_into tombstones=301, off_trade=64, needs_review=1. **`bc_customer_no` IS UNIQUE (`uq_ref_customers_bc_no`, varchar(32)) with ZERO dup groups live** → the recurring failure mode is **STALE/DRIFTED numbers, NOT duplicates**; the connector's value is drift detection/reconciliation, not dedup. NO `bc_last_synced`/`merged_into` COLUMN (merge uses `notes` breadcrumb); **NO `phone` column** (BC exposes it → add in mig if phone in scope). Schema curated cols: trade_channel ENUM(on/off_trade), sale_class ENUM(10), is_private, default_transporter/delivery_site_id_fk, needs_review, notes(text). BC-mappable cols: address_line1/2, postal_code, city, country_code, email.
>
> **CANARY (live-confirmed):** id=3056 "Stoked Mountain Experience AG" bc=**3954**(phantom) has order id=49; id=3060 same name bc=**3951**(real) needs_review=1 NO orders; id=3057 "Stocked Mountain" tombstone merged_into:3056. NB BOTH 3056+3060 created 2026-06-09 from WeeklyOrders import "confirmed by Kouros" — so 3954 was the import's number, 3960 the build-agent's later add. End-state = ONE row bc=3951 w/ order id=49.
>
> **RULINGS:**
> 1. **FIELD OWNERSHIP.** BC-OWNED (sync overwrites): address_line1/2, postal_code, city, country_code, email, phone(new col), is_active←blocked, bc_customer_no(reconcile-path-only, #2). MALTYTASK-CURATED (sync NEVER touches): trade_channel(64 off-trade, cobra-rule), sale_class(COGS/ASP discriminator), is_private, default_transporter/delivery_site, needs_review, notes(append-only — carries merged_into:/alias:), canton. **NAME = operator-curation WINS, BC does NOT overwrite** (Salle-des-Machines rule; live proof id=1572 Blind Tigers trimmed "- Le Vestibule" — a name-clobber would silently revert audited corrections). NO customer-alias table (verified) so name edits orphan no resolution path. BC displayName → surface as drift signal, never auto-apply.
> 2. **DRIFTED bc_customer_no = strict hybrid (c), NEVER blanket auto-correct.** Auto-reconcile ONLY when ALL: (1) BC's number absent from ref_customers.bc_customer_no, (2) the candidate row's OWN bc number is PROVABLY ABSENT from the full BC customers pull (= phantom; the deterministic anchor that converts a guess into a fact — THIS is what licenses automation under never-guess), (3) name + (postcode OR city) exact-match (normalised). Then: update bc_customer_no + `notes` breadcrumb `bc_no_corrected: old→new` + log_revision. Name-matches-but-old-number-still-live-in-BC, or close-but-not-exact, or multi-candidate → REVIEW item (b), never auto-touch. **Watch UNIQUE(bc_customer_no): if both numbers exist (Stoked 3951/3954), null-the-old-FIRST then set, else review.**
> 3. **STOKED specifically = one-time Carnet merge cleanup, NOT the recurring sync's auto-path** (it's a pre-existing dup, don't make the sync untangle blind): keep id=3056 canonical (holds order+provenance), correct its bc 3954→3951, merge-tombstone id=3060 into 3056 (null 3060's bc=3951 FIRST to dodge UNIQUE, then set 3056=3951, is_active=0 + notes merged_into:3056, reassign 0 orders=moot) — operator-confirmed, audited via existing client_merge path.
> 4. **MERGE-TOMBSTONE RESPECT = inherit the DURABLE RESOLVER RULE verbatim** (fulfilment-arc §DURABLE RESOLVER RULE): follow notes `merged_into: N` to canonical active (depth-capped), REFUSE is_active=0 matches. Apply BC updates to CANONICAL not stub; never reactivate a merge-tombstone; blocked→inactive never resurrects. **Biggest divergence risk — name it in brief.**
> 5. **NEW BC customer absent from maltytask → INSERT needs_review=1** (Carnet "à valider", curated cols at defaults). **maltytask customer absent from BC (372 NULL-bc rows, sheet-only privé/marketing) → UNTOUCHED** (sync keys on bc_customer_no, NULL-bc invisible by construction). Both confirmed.
> 6. **Blocked in BC → is_active=0** — FKs point at id not is_active, order/ledger links intact (369 inactive already prove it); idempotent; guard = never touch a merge-tombstone; un-block→is_active=1 ONLY if not a tombstone.
> 7. **CADENCE = daily VPS cron, FULL-SCAN upsert** (3k trivial; full-scan self-heals drift like the ledger's "holes-below-watermark" lesson — do NOT use lastModifiedDateTime incremental). Reuse `ingest_bc_sales_ledger.py` `_load_bc_env()`/`_get_token()`/`_odata_base()`/nextLink-paging (NO `$top` — truncation trap) VERBATIM; BC `customers` standard entity; user maltytask, db.env localhost, bc.env creds. Cron shipped DISABLED (recipe-profile/seasonal convention), manual `--apply` after dry-run drift report reviewed. `--dry-run` default → drift report buckets (matched-clean / phantom-drift-auto / name-drift-review / new-needs-review / blocked-deactivate / tombstone-skipped).
> 8. **MIG (re-`--status`+`ls db/migrations` at dispatch — parallel-session class):** `ALTER ref_customers ADD bc_last_synced_at TIMESTAMP NULL` (+ `phone VARCHAR(40) NULL` if in scope). **NO new table, NO new identity store.** New BC customers → needs_review=1 (existing Carnet). Identity-drift candidates below auto-bar → new `doc_review_queue` type `bc-customer-identity-drift` (self-sufficient: BC#, BC name+city, candidate id, reason) — confirm ENUM has room. Update the `ref_customers` schema_meta row notes to record the 2nd writer + BC-owned/curated split. **EQUIP: sql + coder + ui + webapp-testing.**
> **DIVERGENCE FLAGS:** clobbering curation (name/trade_channel/sale_class/notes silently revert audited ops decisions) · identity-guessing on drift (phantom-absence test is the ONLY auto license) · dup creation (resolve-through-merge-graph before INSERT) · resurrecting tombstones · UNIQUE(bc_customer_no) collision · customer fragmentation by location (NOT a sync concern — De Siebenthal location split lives on ORDER External_Document_No, BC `customers` returns ONE row; just never read order-level location into customer identity) · double-writer race (BC-owned/curated split IS the contract, record in schema_meta).

## ✅ 2026-06-15 — BC SALES DISCOUNT → CANONICAL STORE + #100 REVIVAL — SHIPPED + LIVE + VERIFIED + PUSHED. Trigger "discount"/"remise"/"escompte"/"rabais"/"#100"/"discount_rebate_rate"/"salesInvoices".
> **✅ AS-BUILT (recorded 2026-06-15) — design below realized to spec, with definition LOCKED.** NEW canonical table **`inv_sales_invoice_lines` (mig 348)**: per-LINE, idempotent on `bc_line_id` (BC line GUID); `document_type` invoice|credit (credit discount stored **NEGATIVE** so plain SUM nets); `posting_date` basis; `discount_amount_chf` + `invoice_disc_alloc_chf` (separate cols); `line_amount_excl_tax_chf` reconcile-guard; `sku_code`; `source`. schema_meta row class=`source`/policy=`blocked_with_redirect`. **17,127 lines, 2025-01-03→2026-06-12, 4,871 docs.** NEW connector **`scripts/python/ingest_bc_sales_invoice_lines.py`** (BC STANDARD API v2.0 `companies(NEB)/salesInvoices` + `salesCreditMemos`, `$expand` lines; reuses ledger connector auth/paging/upsert; `--apply`/`--floor`/`--verify`; company resolves dynamically by "NEB" substring → actual "La Nebuleuse SA"). **DEFINITION LOCKED (Opus-verified):** discount granted = net incl. credit memos = `SUM(discount_amount_chf + invoice_disc_alloc_chf)`; `invoice_disc_alloc_chf` is ~0 across the WHOLE dataset (all signal in `discount_amount`). May 2026 = **17,954.71 CHF** vs legacy bc-xlsx 17,957.83 → Δ 3.12 (0.02%) — **BC-direct reconciles to the operator's historical figure almost exactly; BC-direct is the truth.** **#100 (id=99 `discount_rebate_rate`, handler `kpi_sales_discount_rate`) REVIVED LIVE** (mig 350): viz=`line` per-month rate trend, manager-role, is_active=1/data_ready=1/readiness=live. Numerator = net discount from the new table; denominator = ledger ALL-SALES gross via `kpi_sales_load_ledger_all()` — SAME universe both sides (guard honored). May rate **3.64%** (gross 492,770.68 ties to reference); series 18 months range 1.59–6.79%; June MTD 3.38% green. **Migs 348 + 350.** Commits **6ec3fb7** (ingest+handler) + **5ba1def** (go-live flip), PUSHED; md5 local↔VPS matched. **Scrapping #33:** discount was the LAST analytics fact stranded in legacy `inv_sales_bc`/xlsx — now canonical OData-sourced; the bc TABLE drop still gates on fg-stock/expeditions/tap-shop taproom slice (unchanged).
> **⚠️ ONE LOOSE END — REMAINING STEP (not yet done):** the discount connector is **NOT yet wired into a cron — it is manual-run only.** Initial backfill is done, but it will NOT auto-refresh. PM design required it to **FOLD INTO the ledger connector's scheduled cron** (both BC sale-side facts refresh together, no freshness drift). **TODO = co-schedule `ingest_bc_sales_invoice_lines.py` with `ingest_bc_sales_ledger.py`.** The cron source-of-truth is **`db/cron/maltytask-bc-sales.cron`** in the maltyweb repo → deployed to `/etc/cron.d/maltytask-bc-sales` via `bin/deploy.sh --apply` (sudo install -m 0644; manual edits to /etc/cron.d/* are OVERWRITTEN on deploy — edit the repo file). Current line runs the LEDGER connector every 4h: `0 0,4,8,12,16,20 * * * maltytask cd /var/www/maltytask && /usr/bin/python3 scripts/python/ingest_bc_sales_ledger.py --apply >> /var/log/maltytask/bc-sales.log 2>&1`. **Add a second command line in the SAME cron file (same user `maltytask`, same `/var/log/maltytask/bc-sales.log`, absolute `/usr/bin/python3`)** running `ingest_bc_sales_invoice_lines.py --apply`. Either a sibling line at the same 4h cadence, or a sequencing one-liner (`… ledger.py --apply && … invoice_lines.py --apply`) so they refresh in lockstep. Commit the .cron file by pathspec, deploy, then `cat /etc/cron.d/maltytask-bc-sales` on VPS to confirm both lines present. EQUIP coder (deploy/cron) — trivial, no schema.
> --- design ruling preserved below (now realized) ---
> **CONTEXT (live-probed 2026-06-15):** discount has NO canonical home. `inv_sales_ledger` (item-ledger grain, 44,347 rows) carries gross `sales_amount_chf` but NO discount field (PM-confirmed via SHOW COLUMNS — and ZERO `%discount%/%remise%/%escompte%` tables exist). BC STANDARD API v2.0 `salesInvoices?$expand=salesInvoiceLines` exposes LINE-level `discountAmount`/`discountPercent`/`invoiceDiscountAllocation` (header discount=0 in samples → discount is at LINE level), reachable with the SAME `config/bc.env` creds as the ledger connector. The "#100" tile = `ref_kpi_trackers.id=99` / `tracker_no=100` / slug `discount_rebate_rate` / handler `kpi_sales_discount_rate` (kpi-handlers.php:9677, dispatch :8966) — currently RETIRED (`is_active=0,data_ready=0,readiness='compute'`). (NB: `id=100` is a DIFFERENT tile = `days_sales_outstanding`; "#100" in briefs means tracker_no, = id 99.)
> **RULING — legitimate NEW canonical fact, NOT a parallel store** (discount has no other home; bc-xlsx retiring; HELPS `inv_sales_bc` retirement = kills the xlsx col-35 discount_chf dependency → log scrapping-backlog #33 family). New sale-side leaf BESIDE the ledger (both BC-fed, different API/grain), joined only by period.
> **(1) GRAIN = per-invoice-LINE table `inv_sales_invoice_lines`** (NOT monthly rollup = derived parallel store that diverges on backdated/credit; NOT header = discount is line-level → lossy). Mirrors ledger's self-healing drillable discipline. Cols: id PK / posting_date (date basis, NOT invoiceDate) / invoice_date (audit) / bc_invoice_no / **bc_line_id (salesInvoiceLine.id) = UNIQUE upsert NK** / doc_type ENUM('invoice','credit_memo') / customer_id_fk+bc_customer_no (refuse-don't-NULL→review CSV) / sku_code_raw + sku_id_fk (ref_skus+ref_sku_aliases, NULL=visible bucket) / **line_amount_excl_tax_chf = RECONCILE-GUARD only, NOT rate denom** / **discount_amount_chf** / **invoice_disc_alloc_chf** (stored SEPARATELY) / discount_percent (raw) / source_file / imported_at. schema_meta: class='source', policy='blocked_with_redirect' (1 row, mandatory). Gross for the RATE comes from `inv_sales_ledger` (canonical) — table does NOT store gross as the denom; the excl_tax col is a per-period drift guard only.
> **(2) CONNECTOR = new `scripts/python/ingest_bc_sales_invoice_lines.py` (maltyweb repo, user maltytask, config/db.env localhost).** Reuse ledger connector's `_load_bc_env`/`_get_token`/`_odata_base` VERBATIM; nextLink paging NO `$top`; ON DUPLICATE KEY on uq_bc_line_id; review-CSV refuse-don't-NULL. **PULL BOTH salesInvoices AND salesCreditMemos** (credit discount reverses sale discount → store credit-memo discount NEGATIVE so plain SUM nets; verify sign vs 1 known memo first). **DATE BASIS = postingDate** (ledger keys posting_date; rate divides by ledger gross; Financier close is posting-date → never mix calendars; invoiceDate-basis is part of the 3.1% xlsx gap). Incremental = anti-join on bc_line_id (self-heals backdated; no Entry_No-hole pathology on standard API). First-run floor = sane horizon (~2025-01), NOT 2021.
> **(3) "DISCOUNT GRANTED" = discountAmount + invoiceDiscountAllocation** (allocation = line's share of a whole-invoice discount; exclude it → under-report order-level rebates). Store both cols, sum at read. **BC-DIRECT IS THE NEW TRUTH — matching the bc-xlsx (17,957.83) is NOT required** (operator export, weaker source; gap = invoiceDate-vs-posting + credit handling + alloc). Connector `--verify` prints 4 candidate sums (disc-only/+alloc × inv-only/+credits) for ONE month to decompose the gap with Kouros, THEN lock + move on, delta documented. 🔴 COGS-adjacent (feeds Financier escompte line) → OPUS INDEPENDENTLY VERIFIES the component-sum vs a hand-pulled BC sample BEFORE data_ready flips.
> **(4) #100/id=99 HANDLER = revive `kpi_sales_discount_rate` stub.** Numerator = SUM(discount_amount_chf+invoice_disc_alloc_chf)/period. Denominator = ledger gross via the SAME loader the sales surface uses. 🔴 **numerator+denominator MUST share ONE universe** — recommend ALL-SALES (kpi_sales_load_ledger_all) both sides (rebates land on eshop-pack/CAUF SKUs the prod-filter drops; prod-gross × all-discount would inflate). Rate = disc÷gross×100 (plain, NOT gross-up; ledger sales_amount is already gross — verify vs excl_tax guard col). **viz_type = `line` per-month TREND** (creeping-rebate margin signal), latest-month rate as `value`, sub-line = abs CHF + gross + invoice_count in `meta`. min_role=manager, source_domain/category='sales'.
> **(5) DOCTRINE:** legit canonical fact ✅; helps bc-xlsx retirement ✅. GUARDS: same-universe num/denom; reconcile-guard col CHECKED not decorative; NEVER a materialized monthly rollup (if monthly wanted → thin `v_sales_discount_monthly` VIEW over the line table, SoT = line table); posting-date throughout.
> **🟢 BUILD-READY FACTS (PM live-verified 2026-06-15, supersede stale recollections):** (a) **MIG HEAD = 347, next free = `348`** (`--status` 0 pending; 346=prod-targets settings, 347=ord_orders source_bc — both parallel-session landings since the 345 note). **Re-`--status` AGAIN at build-start (parallel drift is a live class).** (b) **schema_meta sibling to MIRROR = the `inv_sales_ledger` row** (cols: `table_name`/`table_class`/`writer_script`/`corrections_policy`/`upstream_hint`/`notes`/`updated_at`): `table_class='source'`, `corrections_policy='blocked_with_redirect'`, `writer_script='ingest-bc-sales-ledger.py'`, `upstream_hint`="Raw BC … Fix in BC system + re-export → re-ingest. Dedup on (…).". For the new table mirror: `writer_script='ingest_bc_sales_invoice_lines.py'`, policy `blocked_with_redirect`, upstream_hint = "BC Standard API salesInvoices/salesCreditMemos $expand=…Lines. Fix in BC + re-pull. Upsert on bc_line_id." (c) **`kpi_sales_load_ledger_all(PDO $pdo): array` EXISTS** (kpi-handlers.php:9058) — returns ASSOC keyed by period 'YYYY-MM', each `{period, chf:SUM(sales_amount_chf), hl:−SUM(hl_resolved), units:−SUM(qty_signed)}`, **NO production filter** (full revenue universe incl. sku_id_fk NULL), cached `sales_ledger_all_periods`. `chf` IS the all-sales gross denom for the rate — use it directly. (d) **Tracker LIVE STATE confirmed:** `id=99 / tracker_no=100 / slug=discount_rebate_rate / label='Taux remises / rabais' / compute_handler='discount_rebate_rate' / is_active=0 / data_ready=0 / readiness='compute' / viz_type='kpi_number' / min_role='manager' / source_domain='sales' / category='sales'`. **`id=100 / tracker_no=101 = days_sales_outstanding` (different tile — do not touch).** Cols are `compute_handler`+`label` (NOT `handler`/`title`). (e) **DISPATCH ALREADY WIRED** — kpi-handlers.php:8966 `'discount_rebate_rate' => kpi_sales_discount_rate(...)`; the revive = (1) rewrite the function BODY at :9677 (currently a `kpi_error_result` stub, comment "#100 RETIRED 2026-06-12 — discount_amount_chf absent…") + (2) tranche-2 mig flips is_active=1/data_ready=1/readiness='live'/viz_type='line'. NO dispatch edit. (f) **HANDLER EXEMPLARS for the `line` trend pattern:** `kpi_sales_revenue_period` (#86, :9107) = canonical `line`-with-series shape — `array_merge(kpi_empty_result($label,'%'), ['value'=>latest, 'delta'=>MoM, 'tint'=>green/red, 'series'=>[{period,value}], 'meta'=>{period,period_label,source}])`; `kpi_sales_seasonal_curve` (#102, :9685) = full-history series build. Use `kpi_sales_latest_period()`/`kpi_sales_prior_period()` helpers (:9120). For the rate: build `series` of `{period, value:disc÷gross×100}` over the all-sales periods, `value`=latest-month rate, sub-line numbers (abs disc CHF / gross / invoice_count) in `meta`. (g) **CONNECTOR to mirror = `scripts/python/ingest_bc_sales_ledger.py`** (maltyweb deploy `/var/www/maltytask`, 53KB, user maltytask) — reuse VERBATIM: `_load_bc_env()` (:108, parses `/var/www/maltytask/config/bc.env`), `_get_token()` (:141, OAuth2 `grant_type=client_credentials`), `_odata_base()` (:168); paging = follow `@odata.nextLink` with **NO `$top`** (:188 comment "$top caps + suppresses nextLink → silent truncation"); OR-chained `$filter` batched at ~50 ids (BC OData 501 on `in`). The discount connector swaps the item-ledger OData entity for `salesInvoices?$expand=salesInvoiceLines` + `salesCreditMemos?$expand=…` and the anti-join key from `bc_line_seq`(Entry_No) to `bc_line_id`(salesInvoiceLine.id). (h) **DERIVATION-TREE POSITION:** `inv_sales_invoice_lines` is a NEW sale-side LEAF beside `inv_sales_ledger` — both BC-fed sources, DIFFERENT API/grain (ledger=item-movement Entry_No grain w/ gross; this=invoice-line grain w/ discount), **joined ONLY by period**, NEVER FK-joined line-to-line (no shared line key; the ledger has no invoice-line id). Both feed the rate KPI: this table = numerator (discount), ledger = denominator (gross). NOT upstream/downstream of each other; siblings under the BC sale-side. Neither feeds COGS valuation directly (the rate is a margin SIGNAL; the Financier escompte line is the COGS-adjacent consumer that gates Opus-verify).
> **(6) MIG + SEQ + CRON.** Re-`--status` FIRST (head=347 verified 2026-06-15, next free=348; re-check at build-start for parallel drift), assign next free. ONE mig: create table + schema_meta (tracker flip SPLIT into tranche-2 mig AFTER Opus-verify, the 344→345 two-step pattern). SEQ: --status→connector+`--verify`(lock components w/Kouros)→mig create→dry-run→apply→OPUS-VERIFY rate→rewrite handler→tranche-2 flip(active/data_ready/readiness=live/viz=line)→wire into ledger's cron invocation+deploy+webapp smoke→scrapping-backlog log. **CRON = NOT standalone monthly — FOLD into the ledger connector's existing scheduled invocation** (both BC sale-side facts refresh together, can't drift in freshness; user maltytask on VPS). No tour card (KPI tile, no ref_pages row). Deploy: md5 local↔VPS (COGS-adjacent), commit by pathspec. **EQUIP: coder+sql+ui+webapp-testing** (no parser-coder — OData JSON not OCR).

## 🔴 2026-06-10 — `sales_weekly_normalised` CONTRACT RULED (PM, live-verified) — IT DOES NOT EXIST AS A PROD OBJECT; THE INTERFACE IS THE EXISTING `inv_sales_ledger` (raw dated lines) — DO NOT BUILD A WEEKLY TABLE/VIEW
**The historical-data lane asked for the canonical "normalised weekly deliverable" contract. PM ruling, after live prod verify:**
- **`sales_weekly_normalised` is a CONCEPT-ONLY name** that crept into this file's §2026-06-09/2026-06-10 design prose + this file's `> Load when` header. **It is NOT a prod table or view** (verified 2026-06-10: `information_schema` shows ONLY `inv_sales_ledger` [BASE TABLE]; no `*weekly*`/`sales_*`/`v_sales*` object exists). **Build nothing named `sales_weekly_normalised`.** The header reference is stale-aspirational; treat the ledger as the surface.
- **The historical deliverable was ALREADY DELIVERED = the `inv_sales_ledger` load itself** (44,164 rows 2021-01-05→2026-06-09, signed qty, customer 100%, SKU 33,204/10,960 NULL) PLUS the single-source COGS repoint (`mysql-sales.ts loadSales()` reads the ledger, artifact `interfaces/sales-cogs-data.json` = **66 months**, monthly×SKU grain). There is NO separate weekly normalisation step owed before the parallel UI chat can consume sales history.
- **THE PARALLEL "Activité récente" UI DOES NOT READ THE LEDGER — verified in deployed `expeditions.php`.** Its timeline reads OPERATIONAL recent-activity sources: `inv_sales_orders` (eshop), `inv_sales_bc` (taproom), `bd_packaging_v2` (production ↑), `inv_stock_movements` (transfers ↔), via 3 bulk `ROW_NUMBER` window queries, 180d/50-cap. **It NEVER touches `inv_sales_ledger`** and needs nothing from the historical lane. The two lanes are decoupled at the source — there is no shape the historical lane must hand the UI chat. (grep proof: only maltytask `lib/`/`scripts/` reference `inv_sales_ledger`; ZERO maltyweb PHP does.)
- **IF a weekly historical series is later wanted** (the original arc intent "normalise vs LogTrack BY WEEK"), the architecturally-correct surface = **a thin MySQL VIEW `v_sales_weekly` over `inv_sales_ledger`** (NOT a materialised table — the ledger is the SoT, a view never diverges; `maltytask_app` has CREATE VIEW). Grain = `(YEARWEEK(posting_date,3) ISO, sku_id_fk, sale_class)` → `−SUM(qty_signed)` units, `−SUM(hl_resolved)` HL, `SUM(sales_amount_chf)` CHF (revenue-signed, plain SUM nets credits). Filter to resolved-FG (`sku_id_fk IS NOT NULL` + `is_direct_sales`/FG predicate) for an FG-velocity lens; surface unresolved as a visible bucket, never drop. **But this is NOT currently requested/needed by the UI chat — build it only if Kouros asks for a weekly analytic, not as a UI interface.**
- **Sign + netting rules (same as the COGS lane, LOCKED):** outbound = NEGATIVE `qty_signed` AND `hl_resolved` → negate for positive units/HL; `sales_amount_chf` is revenue-oriented (credits native-negative) → plain SUM nets, NEVER negate CHF. doc_type needs NO CASE (plain SUM nets shipment+invoice+credit+return). sale_class filter for an outbound-sales lens = the loadSales set `{b2b,eshop,taproom,event,giveaway,staff}` today (eshop/taproom included from the ledger FOR NOW per the §SINGLE-SOURCE seam; flip off when Shopify/Lightspeed feeds peel them).
- **214-SKU review CSV = NOT a gate on any weekly build.** `inv_sales_ledger` already holds all 44,164 rows; the loader self-heals SKU resolution on re-run as aliases land (read-time JOIN re-resolves `sku_code_raw`). Build any FG-velocity surface over the 33,204 resolved rows now; it self-heals. The 10,960 NULL stay a visible "non rattaché" bucket. Do NOT block on the CSV.
- **LogTrack cross-check = a post-hoc RECONCILIATION REPORT, NOT a gate, NOT a prod object.** LogTrack is a legacy BSF/Sheets tab (gid 1775198692) — there is no prod table. The cross-check is: compare the ledger's weekly/period totals to the legacy LogTrack tab, basis-defined-with-Kouros, AFTER the ledger is loaded (it is). It validates; it does not block the load or any downstream build. STILL PENDING (basis TBD w/ Kouros) per §2026-06-10.

## ✅ 2026-06-12 — SALES-DOMAIN "VENTES" KPI TRANCHE SHIPPED off the canonical ledger (#272-276) + bc→ledger DIVERGENCE logged
**5 manager-role `source_domain='sales'` Mon-Tableau trackers SHIPPED+LIVE+VERIFIED+PUSHED (mig 340), all reading the canonical `inv_sales_ledger` — the FIRST KPI surface to consume the ledger directly.** #272 HL/mois (line, 66-mo) · #273 HL par SKU (bar) · #274 unités par SKU MoM (grouped_bar, prior-month ghost) · #275 HL par canal on/off/**non_classé** (stacked_bar, NULL never dropped) · #276 HL par recette (bar, via `rs.recipe_id`). All **production-scoped** via the single-sourced PHP const `KPI_SALES_PROD_FILTER` (`app/kpi-handlers.php` L9049: `rs.recipe_id IS NOT NULL AND rs.units_per_pack<100`, NO `is_active` filter) + shared loader `kpi_sales_load_ledger_prod()` → drops CAUF / eshop packs (PD8/XMAS/PAL/PAC/COLLAB*) / -X cages; the 5 reconcile to ONE production-beer universe. **Locked sign+netting (same as the COGS lane):** `−SUM(hl_resolved)`, `−SUM(qty_signed)`, plain-SUM nets credits, NO doc_type/sale_class filter. Opus-verified BEFORE flip: 2026-05 prod HL=1097.38 (matched canonical ref); latest MTD 2026-06=458.15, #273/#275/#276 each Σ458.15. Snapshot tiles show latest month PRESENT (MTD), `meta.period_label`. Full as-built → kpi-tracker-catalog/README.md §SALES-DOMAIN VENTES TRANCHE. **🔴 STANDING DIVERGENCE (scrapping-backlog #33): the EXISTING Phase-2b sales handlers #86-105 still read the LEGACY `inv_sales_bc` (3,071-row 6-month window) — two read-paths over one sales fact; the bc tiles silently miss pre-Dec-2025 + the broader channel universe. REPOINT #86-105 → ledger + retire the bc 6-month loader; NOT done this build.** This is the first concrete realization of the §`sales_weekly_normalised` CONTRACT's "ledger is THE canonical sales surface" ruling on the KPI side.

## ✅ 2026-06-09 — BC ITEM-LEDGER ARC: `sale_class` TAXONOMY + 6-ACCOUNT CLASSIFICATIONS DECIDED (Kouros, captured + ratified by PM)
The BC item-ledger ingest arc (full Business-Central item-movement ledger → `inv_sales_ledger` + `sales_weekly_normalised`, broader than the monthly `inv_sales_bc` sales-only feed). Two halves to its gate: the customer-side `sale_class` taxonomy is now **DECIDED**; the SKU-side classification is still **PENDING** (operator CSV annotation). PM ratified the design — no objection.

**DESIGN DECISION — `internal_channel` COLLAPSES into a single `sale_class` discriminator ON `ref_customers`** (supersedes the originally-proposed `ord_orders.internal_channel`/`is_internal_channel` flags AND PM's own earlier `internal_channel ENUM('eshop','taproom','transfer','sample')` proposal). Driver = **ASP (average sale price) is a tracked KPI**, so giveaways / staff-price sales must be TRACKED (they drag ASP), not dropped — this turned a binary exclude-flag into a price-type taxonomy.
- **Column:** `ref_customers.sale_class ENUM('b2b','eshop','taproom','event','giveaway','staff','customs_artifact','transfer','sample','other') NOT NULL DEFAULT 'b2b'`. Classification lives on `ref_customers` (set on EXISTING rows only — respects the CRM-roster-is-truth rule, NOT auto-created rows); the ledger row INHERITS it at load. One discriminator, on the customer, inherited by the line — cleaner than a per-line flag.
- **Derived boolean:** `sale_class` drives `counts_as_unit_sale` = TRUE for {b2b, giveaway, staff, event}, FALSE for {eshop, taproom, customs_artifact, transfer, sample}. ASP / giveaway-volume / staff-volume / full-vs-concession mix are all first-class derivations off `inv_sales_ledger` + `sales_weekly_normalised`.
- **THREE behaviours (not two):** (1) **exclude-because-double-counted** — eshop (Shopify owns it), taproom (Lightspeed owns it); (2) **exclude-because-not-a-sale** — customs_artifact, transfer; (3) **track-with-price-tag** — b2b, giveaway, staff, event.

**The 6 internal La Nébuleuse BC accounts — classified row-by-row WITH Kouros (verified against the ledger):**
| BC acct | sale_class | counts_as_unit_sale | Why | Evidence |
|---|---|---|---|---|
| 1080 ESHOP (privé) | `eshop` | ✗ | Shopify owns it; double-count | 78,071 u 2021→2026, monthly-lump postings (matches the 1080-SKIP rule already in `ingest_bc_sales.py`) |
| 3822 TAP/SHOP | `taproom` | ✗ | Lightspeed owns it | 10,294 u, BC only back to 2026-01 (matches the 3822→taproom rule in `ingest_bc_sales.py`) |
| 3701 STE La Nébuleuse SA | `customs_artifact` | ✗ | NOT a sale — pro-forma to pay FR accises when selling to Gatesolutions SA, then scrapped; real sale is on the Gatesolutions account | net **0** units, in=out |
| 1807 Département Marketing | `giveaway` | ✓ (price≈0) | giveaways — Kouros wants "how much we offer" + ASP drag | 126 rows, 105 @ CHF 0, Σ~10 CHF |
| 1858 Staff/Investisseur | `staff` | ✓ (discounted) | staff/shareholder price | 91 rows, mostly low non-zero CHF |
| 3823 La Nébuleuse Event | `event` | ✓ (full price) | real direct event sales we make ourselves — NOT gratuity | 7 rows all Facture vente, **11,418 CHF / 72 u full price** |
| (all other 1,167) | `b2b` | ✓ | — | — |
**100% of 1,173 ledger customers resolve exactly on `bc_customer_no` (zero unmatched)** — confirms the CRM-roster-complete call (3,046 ref_customers, §2026-06-07 LATE).

**SKU-MASTER-IS-BROADER-THAN-FG finding (DECIDED principle; SKU classification itself PENDING).** The BC item ledger carries the WHOLE sellable-item master, not just retail FG. 134/348 ledger codes resolve to `ref_skus`/`ref_sku_aliases`; **214 unmatched** = 43 likely-FG-needing-alias (~18k u) + 171 unknown (~653k u, much clearly NON-FG: bulk tank `28-IPA/TRIP/BLO`, `CO2-FO`, glassware `VER25`, food `PIZZA`, keg-wash service `PREST-LAVF`, generic `ART-000xxx`, `CB5xx`/`CBBZ101`). Kouros annotating a review CSV (FG→alias / NON_FG→exclude). **Architecture rule:** the weekly FG-velocity surface filters to **resolved-FG only**; non-FG loads to the raw ledger but is flagged OUT of the FG velocity surface. (Mirrors the surface-don't-guess SKU-resolution tail at §SCHEMA FACTS — but here the unresolved tail is partly legitimately-non-FG, not just unmapped.)

**PM divergence-watch (NEW):** `sale_class` on `ref_customers` is now a SECOND classification axis alongside `trade_channel`/`is_private`/`off_trade` (§2026-06-07 LATE item 4). They are orthogonal (sale_class = how-a-sale-counts; trade_channel = who-the-buyer-is) but BOTH live on `ref_customers` — keep them single-homed, do not let one leak into the other. And `counts_as_unit_sale` must be DERIVED from `sale_class` (a generated column or read-time CASE), never a second stored boolean that can drift.

## ✅ 2026-06-10 — BC SALES ITEM-LEDGER HISTORICAL LOAD APPLIED TO PROD (`inv_sales_ledger` 44,164 rows, PM-verified live)
The full Business-Central item-ledger arc's historical load is **DONE on prod**. This is the broad item-movement ledger (NOT the monthly sales-only `inv_sales_bc`).
- **Loader:** `scripts/python/ingest_bc_sales_ledger.py` (maltytask, mirrors `ingest_bc_sales.py`; header-NAME column mapping for the 24-col "Écritures comptables article" export; `doc_type` ENUM map; **signed qty kept verbatim**; customer resolve on `bc_customer_no`; SKU resolve `ref_skus.sku_code` + `ref_sku_aliases`; `hl_resolved = qty_signed × hl_per_unit`; `INSERT…ON DUPLICATE KEY UPDATE` on `dedup_key=source_file|bc_line_seq`; `--dry-run` default; `--file` required). **⚠️ SUPERSEDED 2026-06-12 — this maltytask xlsx loader is now HISTORICAL-BACKFILL-ONLY (committed `489ab70`); the SOLE going-forward writer is the maltyweb OData connector of the SAME name. See §2026-06-12 below.**
- **Table `inv_sales_ledger` (mig 300; schema PM-verified 2026-06-10):** `id` BIGINT PK; `posting_date` DATE; **`doc_type` ENUM('shipment','invoice','credit','return_receipt')**; `doc_type_raw`; `bc_document_no`/`bc_line_seq`/`bc_source_no`; `customer_id_fk`→ref_customers; `sku_code_raw` (NOT NULL); `sku_id_fk`→ref_skus (NULL-able); **`qty_signed` DECIMAL(14,4)**; `qty_invoiced`; `sales_amount_chf`; `hl_resolved`; `source_file`; `dedup_key` UNIQUE.
- **Load result (prod-verified):** 44,164 rows; customer resolved 44,163 / 1 NULL (blank N° origine); **SKU resolved 33,204 / NULL 10,960**; doc_type shipment 31257 / credit 6580 / invoice 6309 / return_receipt 18; span **2021-01-05→2026-06-09**; ΣCHF 24,066,493; distinct dedup_key=44,164=total (idempotent).
- **🔴 SIGN CONVENTION (load-bearing, PM-verified 2026-06-10 — counter-intuitive):** in the BC item ledger, **outbound = NEGATIVE `qty_signed`**. shipment/invoice rows are NEGATIVE qty (item leaves stock); **credit/return_receipt are POSITIVE** (item returns). A "units sold" figure = `−SUM(qty_signed)` on shipment(+invoice). `sales_amount_chf` is oriented revenue-positive for shipments (Σ shipment CHF = +28.2M) and negative for credits — so qty-sign and CHF-sign are roughly INVERSE. ANY consumer must negate qty to show positive sales; never sum qty_signed raw expecting positive.
- **sale_class inherited via customer:** ledger LEFT JOIN ref_customers → b2b 39,962 / eshop 3,656 / taproom 307 / giveaway 126 / staff 91 / customs_artifact 14 / event 7 / (null cust) 1. (sale_class lives on ref_customers, §2026-06-09 taxonomy; eshop/taproom here are BC-acct postings = double-count surfaces, exclude for an outbound-sales view.)
- **Migration head context:** migs 300/301 were swept onto prod by a PARALLEL chat's `migrate.php` deploy; **head now 303** (302 cuv lock, 303 side-stock — other arcs). `ref_customers` has **NO merged_into/tombstone column** (369 inactive customers still resolve on bc_customer_no — intentional for historic ledger).
- **PENDING (Kouros):** `data/bc-ledger-sku-review.csv` — **214 unresolved SKU codes** (deposits CAUF/CAUAL, retired beers, non-FG) to classify FG_ALIAS / NON_FG_EXCLUDE; loader re-run self-heals. LogTrack weekly cross-check still pending (post-load reconciliation, basis TBD w/ Kouros).

## ✅ 2026-06-12 — BC SALES LEDGER WENT LIVE/AUTOMATED — OData CONNECTOR SUPERSEDES THE MANUAL XLSX LOADER (maltyweb `c017bc9`, mig 341, cron ENABLED; SHIPPED)
The going-forward writer for `inv_sales_ledger` is now a **live BC Online OData pull**, not a manual xlsx export. This RESOLVES the "BC May NOT ingested / manual export" gap and the §2026-06-10 "loader UNCOMMITTED" item.
- **New connector:** `scripts/python/ingest_bc_sales_ledger.py` (**maltyweb repo**, deployed `/var/www/maltytask`, runs as user **maltytask**). Replaces the manual "Écritures comptables article" xlsx export with a S2S OAuth2 client-credentials OData pull. BC creds `config/bc.env` (640 maltytask:www-data, gitignored; `config/bc.env.example` committed). DB via `config/db.env` (localhost, no tunnel). Commit `c017bc9` origin/main.
- **🔴 INCREMENTAL = ANTI-JOIN, NOT a MAX(Entry_No) watermark (load-bearing architecture decision — corrects the original plan's flaw).** BC does NOT assign Entry_No chronologically — entries can post AFTER a snapshot yet carry an Entry_No BELOW the current MAX ("holes below the high-water mark"; verified live: 27 real Sale entries 61128–61497 dated 10–12 Jun sat below the 61500 watermark, an `Entry_No gt MAX` pull would have lost them forever). Connector: cutoff = `MIN(posting_date)` in `inv_sales_ledger` (2021-01-05); pull ALL in-scope Sale Entry_Nos via `$select=Entry_No` projection (Posting_Date ge cutoff, **NO `$top`** — `$top` suppresses `@odata.nextLink` and silently truncates → rely on nextLink paging); **diff against `inv_sales_ledger.bc_line_seq`**; fetch + upsert only the missing rows via OR-chained `$filter` (**50 ids/batch — BC OData rejects the `in` operator with HTTP 501**). Self-heals holes + backdated entries; excludes pre-2021 (api_min == DB min == 17244). **Entry_Type='Sale' guard on every batch.** Refuse-don't-NULL preserved (unresolved customer/SKU → SKU-review CSV, never guessed). Secret/token never logged.
- **mig `341` (APPLIED on VPS):** `UNIQUE(bc_line_seq)` (= BC Entry_No) → upsert idempotent on Entry_No regardless of source_file; existing `dedup_key` UNIQUE preserved. **MIG HEAD now 341 (re-`--status` at next build).**
- **Cron `db/cron/maltytask-bc-sales.cron` ENABLED + installed on VPS** (every 4h: 0,4,8,12,16,20 UTC, user maltytask; cron service active). The sales feed is now LIVE/self-refreshing — aligns with maltyweb-prefers-live.
- **First `--apply` (operator-approved):** 166 rows inserted (27 holes + 139 above watermark); `inv_sales_ledger` 44,164 → **44,330**; max posting_date now **2026-06-15**. Histo 147 shipment / 7 invoice / 12 credit, 0 unmapped doc_types, customers 35/35, 6 unresolved SKUs (CAUF/CAUAL/CO2-FO/V25/V50/ART-000158 = deposits/CO2/cuv/non-FG) → review CSV.
- **ONE WRITER going forward.** The **maltytask** repo's original xlsx loader (commit `489ab70`) is now the **historical-backfill record only** (restored after an agent accidentally overwrote it); the **maltyweb** OData connector is the SOLE going-forward writer. ⚠️ Same connector NAME in both repos — the live one is the maltyweb deploy.
- **DERIVATION-TREE position:** this connector is the upstream sale-side fact feed for `inv_sales_ledger`, consumed downstream by the fulfilment burn engine (semaines_stock / Couverture, mig 325), the `v_sales_ledger_*` views (migs 326/329) + Expéditions Historique tab, and the COGS sale lane. It only INSERTs sale-side facts (new Entry_No); NEVER updates the 44,164 historical rows; **no COGS valuation coupling.**
- **STILL OPEN (non-blocking):** (1) BC ledger doc-type basis = ALLNET for quantity, invoice lines for CHF — a COGS-cutover coupling flag for the COGS chat. (2) 214/222 unresolved-SKU review CSV is Kouros's FG_ALIAS/NON_FG_EXCLUDE pass (self-heals on re-run).

## CANONICAL LANDING TABLES (live counts PM-verified 2026-05-27)
Three `source`-class tables, all `schema_meta.corrections_policy = blocked_with_redirect` (fix the source feed / re-ingest, never hand-edit):

| Table | Rows | Coverage | What it is | Writer |
|---|---|---|---|---|
| `inv_sales_orders` | 2,472 | Dec 2025 → Apr 2026 | Shopify ESHOP order headers (one per order) | `ingest-shopify-orders.ts` |
| `inv_sales_order_lines` | 4,156 | (children of above) | Shopify order LINES (one per SKU per order) | `ingest-shopify-orders.ts` |
| `inv_sales_bc` | 2,496 | Dec 2025 → Apr 2026 | Business Central monthly sales (B2B 2,328 + taproom 168) | `ingest_bc_sales.py` |
| `ref_clients` | 16 | — | `reference`, `corrections_policy=allowed`, writer manual/web — the Le Cockpit client fiche table (a contract recipe FK-links to its client) | manual/web |

**Two-source sales model by design** (mirrors COGS architecture): **Shopify = the eshop / DTC channel** (order-grain, with customer identity, fulfillment status, discounts/tax/shipping all CHF-normalized); **Business Central = the invoiced B2B + taproom channel** (the fiscal-grade authoritative source, monthly xlsx export). They are NOT redundant — different channels, different grain. (`build-sales-cogs.js` historically treated BC `Sales` as fiscal-grade authoritative with LogAnalytics fallback — see BSF-EXIT LEG below.)

## SCHEMA FACTS A STAKEHOLDER DOC CAN STATE
- **Source-enum forward-compat:** `inv_sales_orders.source` = `enum('shopify','lightspeed')` and `.channel` = `enum('eshop','taproom')` — the schema already anticipates a **Lightspeed POS** feed (taproom till) though **no Lightspeed ingest script exists yet** (`ingest-lightspeed-orders.ts` is named in `schema_meta.writer_script` as a placeholder). `inv_sales_bc.channel` = `enum('b2b','taproom')`.
- **Everything CHF-normalized at ingest:** order/line money columns are `decimal(…,4/6)` `*_chf` (subtotal/total/discount/tax/shipping; line unit_price/amount/discount). Currency normalization happens in the ingest layer, not downstream.
- **SKU resolution (the normalization keystone):** both line tables carry `sku_id_fk INT UNSIGNED` resolving the raw `sku_code` string to canonical `ref_skus.id`, plus `hl_resolved DECIMAL(14,6)` (volume per the SKU). Live resolution rates: **Shopify lines 3,893/4,156 = 94 %**; **BC 2,098/2,496 = 84 %**. The unresolved tail is the operator-facing cleanup frontier (raw SKU strings not yet mapped to a canonical SKU — surface, don't guess).
- **Idempotent re-ingest:** Shopify = `INSERT IGNORE` on `UNIQUE(source, external_order_id)` + `row_hash`; BC = `INSERT IGNORE` on `UNIQUE row_hash`; eshop-derive = `row_hash UNIQUE` + `INSERT IGNORE`. Re-running any feed is safe (same discipline as `inv_deliveries.dedup_key`).
- **Audit-logged writes:** the TS ingest + derive scripts emit `logAudit()` events (the forward event stream); Python BC ingest reads creds from `~/.config/maltytask/db.env` via the SSH tunnel (localhost:13306), no embedded creds, `--dry-run` default-safe.
- **Provenance preserved:** `external_order_id`/`external_line_id`/`order_name`/`source_name`/`tags` (Shopify) and `source_file`/`customer_no` (BC) — full lineage back to the source system retained.

## WHAT WAS NORMALIZED — the pipeline (Shopify side)
1. `ingest-shopify-orders.ts` — pulls the Shopify Admin API (`/admin/api/2024-10`, creds in `secrets/shopify.env`, never committed; default window Dec 2025 → May 2026) → upserts headers into `inv_sales_orders` + lines into `inv_sales_order_lines`. Normalizes money to CHF, captures customer + fulfillment/financial status, resolves each `sku_code` → `sku_id_fk` + `hl_resolved`.
2. `ingest_bc_sales.py` — parses the monthly BC sales xlsx exports → `inv_sales_bc` (B2B + taproom channels), same SKU resolution + CHF columns.
3. `derive-eshop-consumption.ts` — reads `inv_sales_orders`/`inv_sales_order_lines` → derives the **OUTER fulfilment packaging** consumed per eshop order → INSERTs `inv_consumption` rows with `source_event='sales_derived'` (3,633 rows live; 6 packaging MIs — cartons eshop, renforts, white box). This is the ONLY sales→consumption write-path that exists today.

## 🟢 CANONICAL BC-SALES INGEST PATH (operational — verified live 2026-06-08)
**The sales-in path is NO LONGER a BSF!Sales tab paste.** Canonical = `scripts/python/ingest_bc_sales.py` (maltytask) → `inv_sales_bc` (MySQL), run from local over the SSH tunnel (localhost:13306; creds `~/.config/maltytask/db.env`). Idempotent (`INSERT IGNORE` on `UNIQUE row_hash` = period|customer_no|sku|qty|sales_chf). Default mode = APPLY (`--dry-run` to preview). Per-month run: `python3 scripts/python/ingest_bc_sales.py --file "<path/to/export.xlsx>" --only-period 2026-05`.
- **BC export format expected** (sheet name MUST be `Data`; 0-based col indices): period col 1 (`COL_PERIOD=0`, parsed from "Period: 01.05.26..31.05.26" → '2026-05'); customer_no col 8; customer_name col 9; **sku_code col 31**; sku_desc col 32; qty col 33; sales_chf col 34; discount_chf col 35; profit_chf col 36; UOM col 38; liter_raw col 39. SKU keying = raw `sku_code` string → `ref_skus.sku_code` (active only) → `sku_id_fk`+`hl_resolved`; unresolved tail surfaced, never guessed. Customer routing: customer_no **1080** (ESHOP privé) = SKIPPED entirely (Shopify owns eshop); **3822** (TAP/SHOP) = channel=taproom; all else = b2b.
- **⚠️ HARDCODED DESKTOP PATHS:** `DEFAULT_FILES` points at `/mnt/c/Users/Kouros/Desktop/vente<month>.xlsx`. The BC export FILENAME format already drifted (Dec-Mar = `vente<mois>.xlsx`; April = `Customer_Item Sales Extended.xlsx`) — so ALWAYS pass `--file` explicitly for a new month; do not rely on the default list. Filename is cosmetic (period is parsed from col 1), but the file must exist + have a `Data` sheet.
- **NO web upload module for sales yet** (unlike GL: `charges-bc.php` is the bookkeeper CSV-upload pattern). Sales BC ingest is still operator-CLI. The durable target (roadmap item, NOT built) = a `ventes-bc.php` upload module mirroring `charges-bc.php` (drag-xlsx → dry-run → confirm-commit → `inv_sales_bc`), retiring the Desktop-path coupling. Direct BC API wiring is NOT on the near-term roadmap (BSF-exit + commissioning take priority; monthly export is the pragmatic answer).

## CONTRACT-SKU CREATION RECIPE (proven path; first applied 2026-06-08 JASPF/BAMF Chien Bleu)
**Trigger:** a BC sales line lands with `sku_id_fk NULL` because the SKU doesn't exist in `ref_skus` (e.g. a new contract keg). Canonical fix = create the `ref_skus` row(s); the sale resolves on next `build-sales-cogs.ts`.

**(1) Creation path = DIRECT MySQL INSERT into `ref_skus`.** There is NO operator UI for SKU CRUD yet (`ref_skus` `last_modified_by` is 'ingest'|'web'; the "SKU builder UI" is unbuilt — `schema_meta` says so). Salle des Machines / Capacités commission *equipment & formats*, not catalog SKUs. So contract SKUs are seeded by SQL, FK-derived from canonical (recipe + format), never guessed.

**(2) Field values (keg exemplar — copy from any active F SKU, e.g. ALTF id=3 / DIVF id=17):**
- `sku_code` = e.g. 'JASPF'/'BAMF' (matches the raw BC `sku_code` string — that's the read-time JOIN key).
- `recipe_id` = the `ref_recipes.id` (Jasper=21, Bamse=20). **NB column is `recipe_id`, not `recipe_id_fk` on ref_skus.**
- `beer_raw` = recipe short name ('Jasper'/'Bamse'); `format` = 'Keg'; `format_id` = **15** (canonical "Fût 20L keg", run_type='keg', hl_per_unit 0.200000); `unit_label` = '1 keg (20L)'; `hl_per_unit` = **0.20000**; `units_per_pack` = 1.0000.
- **20 L volume was DATA-CONFIRMED, not assumed:** BC `liter_equiv_raw` = qty×20 exactly (BAMF 90 kegs→1800 L, JASPF 76→1520 L). Always read `liter_equiv_raw / qty` before setting hl_per_unit — Chien Bleu kegs happen to be 20 L, but verify per contract.
- `is_active`=1; `is_packaging_line`=1; `is_direct_sales`=**0** (contract B2B keg, NOT eshop DTC — eshop-only SKUs set 1); `last_modified_by`='web'.
- `bom_template_id`: keg La-Neb template = **23** (`ref_packaging_bom_templates` id=23, format_id=15, supply='we_supply', "F — fût (La Neb)"). **SET ONLY IF we supply the keg material** (see §3). If client supplies own → leave NULL (2 active F SKUs already run template NULL).
- `row_hash` convention for ref_skus = sha256 of the natural key; manual-insert pattern follows `reference_ref_mi_manual_insert` discipline (compute a stable hash, set `last_modified_by`). For ref_skus the practical NK is `sku_code` — `row_hash=SHA2(sku_code,256)` is consistent with the alias/ingest pattern (verify against an existing ingest-written row's hash formula before bulk-seeding more).
- `imported_at`/`last_seen_at` = NOW().

**(3) CONTRACT COGS TREATMENT — material-responsibility is MODELED-BUT-NOT-BUILT (do NOT auto-create a BOM).**
- `client_id`=8 → `ref_clients` id=8 = "Chien Bleu". **`ref_clients` has NO material-responsibility column today** (cols: id,name,notes,created_at only). The per-client `materials_supplied_by` = nebuleuse|client flag from `[[contract_packaging_cogs_per_client]]` (Kouros 2026-05-24) is a PLANNED schema addition, NOT live.
- ⟹ **Until that flag exists + the packaging-consumption builder branches on it, a contract keg SKU carries NO packaging-material BOM on our side** unless Kouros explicitly confirms Chien Bleu is a procure+recharge (nebuleuse-supplied) client. **ASK — do not guess** (guessing the BOM = guessing a COGS-impacting mapping). If client-supplies-own: the SKU is a pure billing line, `bom_template_id`=NULL, no ref_sku_bom, no inv_consumption. If nebuleuse-supplies: set `bom_template_id`=23 AND build the material BOM keyed on run_type+format (independent of the client's liquid recipe), tagged `contract` so it depletes our inventory but stays out of Nébuleuse *product* COGS.
- **For SALES-COGS specifically it doesn't matter:** contract beers are excluded from the sell-side product-COGS rollup; the SKU's job here is just to resolve the sale line + route beer tax. A missing BOM does NOT break sales-COGS for a contract line.

**(4) BACKFILL after creating the SKUs = NOTHING (the stored NULL is irrelevant).** `loadBCSales()` in `lib/repos/mysql-sales.ts` SELECTs raw `sku_code` and does NOT read `sku_id_fk` — `build-sales-cogs.ts` **re-resolves `sku_code → ref_skus.sku_code` (active only) at read time** (its own JOIN). So once active JASPF/BAMF ref_skus rows exist, the next build run resolves them automatically. The `inv_sales_bc.sku_id_fk` column is a *staged convenience* for other consumers, NOT the sales-COGS path. OPTIONAL hygiene: `UPDATE inv_sales_bc SET sku_id_fk=(SELECT id FROM ref_skus WHERE sku_code=inv_sales_bc.sku_code AND is_active=1) WHERE sku_code IN ('JASPF','BAMF')` to clear the NULL for dashboards — but it is NOT required for COGS. Re-running `ingest_bc_sales.py` will NOT backfill (INSERT IGNORE on row_hash skips existing rows).

**(5) Beer-tax routing — `ref_beer_types` already SUFFICES.** Rows 20 (Chien Bleu - Bamse, type=Contract, tax_category=2, median_og 10.9) + 21 (Chien Bleu - Jasper, Contract, tax_cat=2, og 11.6) exist + active. Tax routes off `ref_beer_types`, not the SKU. **No extra wiring needed.** (Also present: Moût Chaud/Froid/Pomelo rows 22-24, contract intermediates.) Watch: `build-sales-cogs` joins beer-tax via recipe/beer-name — the ref_beer_types name "Chien Bleu - Bamse" must reconcile to recipe 20's identity; verify the join resolves (no unknownSKU/untaxed leak) on the post-create build run.

**Chien Bleu contract footprint (verified live 2026-06-08):** customer BC 1486 / `ref_customers` id=385 / `ref_clients` id=8. Recipes: 20 Bamse (Lager), 21 Jasper (Belgian Ale), + Moût Chaud/Froid/Pomelo intermediates. ZERO Chien Bleu ref_skus existed before this — JASPF/BAMF are the first.
