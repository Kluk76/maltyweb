# Sales-commercial — One-line state, expéditions arc pointer, May closure snapshot, what's built/designed/planned, BSF-exit leg, topbar dedup, concurrency lesson, roadmap, archived register

## ONE-LINE STATE
Sales **INGEST + normalization is BUILT and live** (Shopify orders + BC monthly sales landing in canonical `inv_sales_*` tables, SKU-resolved, lot-faithful, idempotent). **+ NEW 2026-06-10: full BC item-ledger HISTORICAL load applied to prod — `inv_sales_ledger` 44,164 rows 2021→2026, signed-qty (outbound NEGATIVE), customer-resolved, 75% SKU-resolved (see §2026-06-10).** Sales **CONSUMPTION / COGS-wiring is PARTIAL** (eshop OUTER packaging derived; full cage/loose/residual matrix = Stage-2, planned). The **Le Cockpit UI family is NO LONGER placeholder — «Expéditions» christened it 2026-06-07** (B2B order saisie + Commandes dashboard + Stock PF, live but page-hidden pending operator smoke) → [fulfilment-expeditions-arc/README.md](fulfilment-expeditions-arc/README.md).

## ✅ 2026-06-07 — EXPÉDITIONS ARC PM-BRIEFED + APPROVED (full v1 built; detail → [fulfilment-expeditions-arc/README.md](fulfilment-expeditions-arc/README.md))
Mig 279 (`ref_transporters`/`ref_customers`[intentionally DISTINCT from `ref_clients`]/`ord_orders` cache + `ord_order_status_events` truth/`ord_order_lines` SKU-units) + module `expeditions.php` + Commandes dashboard (week+date-range; eshop/taproom = read-only auto rows from `inv_sales_*`, never double-counted) + `app/fg-stock.php` `fg_stock_compute()` (anchor + production[÷units_per_pack] − ord shipped − eshop `inv_sales_orders` − taproom `inv_sales_bc`). **DIVERGENCE-WATCH carried forward (open, → arc file §PM divergence-watch):** customer identity single-homing across `ref_customers`/`ref_clients`/Shopify/BC (bootstrap gated on CRM xlsx, CRM=roster truth); at Stage-2 design, verify ord_* depletion + the tracker stay complementary, not parallel; -X cage vs -B box attribution in fg-stock after the 06-08 cage anchor.

## 📅 MAY 2026 CLOSURE — input state snapshot (PM-verified live 2026-06-08)
Where May stands the day Kouros corrected the Pack Découverte stocktake typo:
- ✅ **RM stocktake 2026-05** IN (`inv_rm_stocktake` 130 rows; Pack Découverte counted_qty corrected to 4360).
- ❌ **FG stocktake May NOT in** — `inv_fg_stocktake` latest `month_closed='2026-04'` (41 rows, bsf-stocktake). May FG count (Formularantworten 9 → sync) + the cage `-X` anchor (RESUME #0a) still owed.
- ⚠️ **Consumption May thin** — `inv_consumption` May = 287 rows (brewing 114 / packaging 162 / fermenting 11) vs April 1022. Likely incomplete May production data-entry upstream (saisie), not a pipeline break. Verify May brewing/packaging saisie is fully entered before COP.
- ⚠️ **Deliveries** — May `inv_deliveries` 29 rows, latest `date_received=2026-05-21`, 0 Pending. Late-May (22-31) invoices/DNs likely not yet ingested. The ingestion-validation gate (RESUME #12) is now the path; operator must validate any staged May invoices.
- ❌ **Sales (BC + Shopify) May NOT ingested** — both `inv_sales_bc` and `inv_sales_orders` stop at 2026-04. Owed: BC May xlsx export → `ingest_bc_sales.py --only-period 2026-05`; Shopify May → `ingest-shopify-orders.ts`.
- ⚠️ **WAC** — recompute against May-inclusive Active deliveries before valuing.
**NOT blocking May close: the F2 liquid BOM arc** (RESUME #0). F2 corrects per-SKU liquid composition for go-forward COGS accuracy; it is dry-run-only, ref_sku_bom untouched. May can close on current BOM; F2 apply improves subsequent months. (If operator wants May valued on corrected liquid BOM, apply F2 first — operator's call, not a hard gate.)

## WHAT IS BUILT vs DESIGNED vs PLANNED
- **BUILT + live:** Shopify order ingest; BC sales ingest; the three `inv_sales_*` canonical landing tables (schema_meta-classified); SKU resolution (94 %/84 %); CHF normalization; idempotent re-ingest; eshop OUTER-packaging consumption derivation (`sales_derived`, 3,633 rows); `ref_clients` reference table.
- **DESIGNED, not built (Stage-2 — the precise eshop consumption matrix):** which physical stock each sale draws from — cage-draw (`-X` 1027-bottle crate) vs unboxed loose (6×4 / 24), **half-consumed cage residuals**, **by sales channel**; a VISUAL surface dynamically updated on each BC/Shopify pull tracking RM consumption + FG accuracy/depletion + half-consumed-SKU availability; month-close snapshot → exact RM consumption + COP/COGS. Needs a theoretical mathematician + UI expert. → rm-retro-prereq-chain.md §PREREQ-1, derivation-tree-and-schema.md §Stage-2.
- **PLANNED data-feed prereq (HARD dependency of Stage-2):** the Shopify feed must carry a **pickup-vs-shipped flag** — the unbox-vs-cage discriminator. Without it the channel routing can't be computed. Account=1080 (privé) identification already present. → rm-retro-prereq-chain.md §CHANNEL-FLAG.
- **PLANNED schema-anticipated, no code:** Lightspeed POS (taproom) ingest (`ingest-lightspeed-orders.ts` — enum + writer_script placeholder only).
- **PLACEHOLDER:** the Le Cockpit operator UI family (Vente clients/contracts, Marketing, Finances/Admin). `ref_clients` exists (16 rows) but there is no built sales dashboard / commercial cockpit page yet.

## ✅ BSF-EXIT LEG — CLOSED 2026-05-28 (Phase 2 Track B)
`build-sales-cogs.js` → `build-sales-cogs.ts` (commits `04abd0a` + `914d325` on worktree-agent-a8ab8b7342513a984). `lib/sales.js` DELETED. New loader: `lib/repos/mysql-sales.ts` — UNION of `inv_sales_bc` (B2B/taproom) + `inv_sales_orders`/`inv_sales_order_lines` (Shopify eshop). Sales_COGS BSF tab write PERMANENTLY REMOVED. Output is JSON-only (`interfaces/sales-cogs-data.json`). Export-customer filter reads from MySQL `ref_export_customers`. Verified live: 2026-04 → 1624 rows (629 BC + 995 Shopify), salesCOGS_CHF=72014.4, BeerTax=15614.39. EDGE CASE: Shopify sends `PACKDEC` SKU code but BOM is registered under `PD8` (alias per migs 150/151) — PACKDEC appears in unknownSKUs/RQ until SKU alias resolution is added to the build-sales-cogs consumer. This is a pre-existing gap (BC always sent PD8 directly). NOT introduced by Track B. `run-month-close.js` updated: `build-sales-cogs.ts` added to TSX_SCRIPTS, invocation flipped to npx tsx.

## ✅ 2026-06-10 — À-VALIDER TOPBAR DEDUP — SHIPPED (mig 312, `1a81a3f`, pushed)
RULING #3 executed: `is_active=0` on ref_pages **id=99** invoice-validate (txn + log_revision). is_active=0 is the hidden-but-accessible pattern (topbar filters is_active=1; `_page_registry()`/`user_can_access()` do NOT filter is_active → page still reachable by href + access-gated). Queue stays at `triage.php?tab=valider`. À-valider now carried only by Triage. **Migration head = 312, next free = 313** (re-verify at build-start — parallel sessions live).

## ⚠️ 2026-06-10 — CONCURRENCY INCIDENT (process lesson, not a bug)
A PARALLEL Claude session worked in the SAME git clone (`/home/kluk/projects/maltyweb`) building an FG-stock KPI tranche (`kpi_fg_*` in `kpi-handlers.php` + **mig 305** + Phase-2b). When the Financier commit landed, the SHARED git index swept the parallel session's uncommitted FG suite + mig 305 INTO commit `6a013ad` (Financier author staged only 2 fns via a filtered patch, but the parallel session staged its files into the shared index between stage and commit). All code correct + `php -l` clean + both migs (304+305) applied — **nothing broken**, but `6a013ad`'s boundary is MIXED (Financier + FG). **No reconciliation needed — leave as-is** (both migs live, code clean). **LESSON: two sessions committing in one working copy race on the shared index → use separate clones/worktrees OR serialize commits.** mig 305 = `305_kpi_fg_stock_data_ready.sql` (flips FG-stock KPI trackers data_ready=1), applied + tracked, no PM action.

## ROADMAP — what CAN be developed commercially
1. ~~**Wire `build-sales-cogs.js` off BSF onto `inv_sales_bc`**~~ ✅ DONE+SUPERSEDED 2026-06-10 — loadSales now reads `inv_sales_ledger` (single source, §SINGLE-SOURCE REPOINT above), broader than `inv_sales_bc`.
2. **Stage-2 sales/eshop SKU-consumption matrix** (precise cage/loose/residual depletion, the visual dynamic-on-pull surface) — the single highest-leverage commercial+inventory build; also closes RM-retro prereq-1 and the live PACKDEC/PBD/PACKDECX8 zero-BOM leak. Gated on securing the Shopify pickup-vs-shipped flag + commissioning complete.
3. **Raise the SKU-resolution rate** (94 %/84 % → ~100 %) by mapping the unresolved raw `sku_code` tail to canonical SKUs — operator cleanup, surface-don't-guess.
4. **Lightspeed POS (taproom) ingest** — the schema already anticipates it (enum + placeholder writer).
5. **Le Cockpit operator UI** — sales dashboards, client/contract fiches (build on `ref_clients`), marketing surfaces. PARTLY REALIZED: «Expéditions» (logistics) 2026-06-07, «Tap&Shop» 2026-06-09, **«Financier» finance hub (COP/COGS/Marge) 2026-06-10 (§FINANCIER above, general/manager+)**. Remaining: client/contract fiches, marketing surfaces.
6. **Contract-packaging per-client COGS** (material-responsibility flag per client — some we procure+recharge, some supply own) — a commercial-model rule already on record (→ [[contract_packaging_cogs_per_client]]).

## 🗂️ ARCHIVED INDEX SHIPPED-ARCS-REGISTER ENTRY (compacted from index 2026-06-15)
_Verbatim SHIPPED-ARCS-REGISTER entry (SINGLE-SOURCE COGS REPOINT + FINANCIER PAGE v1 + the 4 Kouros-feedback rulings incl. BOARD COGS/COP P&L GRID + sku-costs retirement) relocated from the index during memory compaction. The §-sections above are canonical; this preserves exact index wording (incl. the  sku-costs retirement detail)._

- **SINGLE-SOURCE COGS REPOINT + FINANCIER PAGE v1** (maltytask `489ab70` + maltyweb `6a013ad`, migs 300/301/304; SHIPPED+LIVE 2026-06-10) — (a) `loadSales()` reads ONLY `inv_sales_ledger` (44,164 rows 2021→2026, single source), retiring the bc+orders union; artifact now 66 months; ledger writer `ingest_bc_sales_ledger.py` committed; sign convention EMPIRICALLY VERIFIED (outbound NEG qty/hl; CHF revenue-signed plain-SUM). **SEAM: ledger-only FOR NOW — eshop+taproom still IN the sale_class filter, peel off to Shopify/Lightspeed feeds LATER (isolated behind the filter).** (b) `financier.php` read-only finance hub (ref_pages id=239, manager/general/sort=125) consuming `cogs-report-data.json` (COP) + `sales-cogs-data.json` (COGS) via lazy `api/financier-data.php` per-month slice — NEVER recomputes. **OPEN: /financier tour card NOT dispatched (dirty shared tree — tour-gap-check flags Financier + the FG-stock page CRITICAL; deferred until tree clean).** **🆕 2026-06-10 KOUROS FINANCIER FEEDBACK — PM RULINGS (pre-build; verified live):** (1) **✅ RESOLVED 2026-06-10 — render bug ROOT-CAUSED + FIXED (maltyweb `f1ff3a1`, pushed + live-confirmed).** "All 4 sections don't work" was NOT data, NOT a JS error: `public/css/financier.css` had `.fin-panel{display:flex}` which OVERRODE the HTML `[hidden]` attribute (a `.class{display}` rule beats `[hidden]`'s UA `display:none` on SPECIFICITY) AND `.fin-panel--active` was empty → all four panels (COP/COGS/Marge/SKU) rendered STACKED simultaneously, so clicking tabs did nothing visible. Content + data were ALWAYS correct (manager-auth Playwright smoke verified: COP 77,169 CHF, COP/HL 88.76, Marge 85.8%). FIX = `.fin-panel{display:none}` + `.fin-panel--active{display:flex}` (tab JS already toggles `--active`; HTML sets it on COP). **DURABLE LESSON (reinforces the visual-acceptance gate; added to the `ui` skill rendering-bug catalog as the `[hidden]`-overridden-by-`.class-display` trap, sibling of the `<dialog> display` trap): `php -l` + data-layer checks PASS a stacked/broken page — only a real manager-auth FULL-PAGE SCREENSHOT caught it. The manager-render screenshot is the LOAD-BEARING UI gate on a role-scoped page, not optional.** *(Original static-analysis dead-end recorded for the record: node --check, hoist, tab↔panel data-tab↔fin-panel-* contract, render-target IDs, php-fpm/nginx all 200/clean — all missed it because the fault was CSS specificity, not JS/PHP.)* (2) **Retire standalone SKU cost page** — Kouros assumed BSF-linked; WRONG (verified: `sku-costs.php` reads `ref_skus`/MySQL, only `google` strings = Google Fonts). RULING = **Option (b), NOT full-absorb (a) now**: KEEP `sku-cost-detail.php` (drill-down, 2 callers sku-costs.php L370 + bom-review.php, no ref_pages row — NOT the scrap target); the Financier "Coût par SKU" tab (`fin-panel-sku`, financier.php L432-450) currently LINKS OUT to `sku-costs.php` → repoint/inline the per-SKU LIST into the panel (reuse the SAME `ref_sku_bom` read — NO re-derivation = divergence flag), THEN `is_active=0` ref_pages id=12 sku-costs + log the .php to scrapping-backlog (gate: panel covers list + 0 inbound routes). Full-module absorb = optional later increment. **✅ #2 SHIPPED 2026-06-10 (maltyweb `e72e64e`, pushed) — went FURTHER than the inline-list plan: the Financier "Coût par SKU" tab (`fin-panel-sku`) now renders the FULL per-SKU cost table SERVER-SIDE reusing sku-costs.php's EXACT `ref_sku_bom` query VERBATIM (no re-derivation) — 3 KPIs (active SKUs / recipes / médian CHF·HL) + per-recipe-grouped rows (brewing/packaging/total CHF + CHF·HL) + anomaly flags (keg-inverted/outlier/extreme-bom-line) + BOM freshness stamp + SKU→sku-cost-detail.php links + client-side recipe/format/classification filters (no reload); query try/catch-wrapped (DB hiccup can't fatal the COP/COGS modules). Retirement = **mig 318** (NOT the earlier-noted 313 — parallel sessions took 313–317; head now ≥318) sets `ref_pages.sku-costs is_active=0` AND `public/modules/sku-costs.php` DELETED (operator-authorized, git-recoverable — went past the planned is_active=0-only); `sku-cost-detail.php` KEPT (back-links repointed to /modules/financier.php); 0 dangling hrefs. Live-verified Opus-independent (mig 318 applied, is_active=0, 154 active SKUs carry BOM cost, php -l clean, HTTP 302). → scrapping-backlog **#32 RETIRED**. ⚠️ **Visual-acceptance OPERATOR-PENDING** (build session's local→site browser flaky; manager-render screenshot is the load-bearing UI gate per #1's lesson, not yet eyeballed — same gap pattern).** (3) **✅ À-valider topbar dedup SHIPPED 2026-06-10 (mig 312, commit `1a81a3f`, pushed) — RULING EXECUTED.** `is_active=0` on ref_pages **id=99** invoice-validate (txn + log_revision). NO orphan + NO new flag needed: topbar filters is_active=1 (topbar.php L18) but `_page_registry()`/`user_can_access()` query ref_pages with NO is_active filter (auth.php L370) → is_active=0 IS the hidden-but-accessible pattern (page still reachable by href + access-gated). Queue already lives at `triage.php?tab=valider` (id=4 topbar; shared partial valider-queue.php; invoice-validate.php is a thin wrapper since the 2026-06-08 merge `484cc6a`) — so hiding id=99 leaves exactly Triage carrying À-valider. NB ref_pages has NO `nav_hidden` col; min_role bump is the WRONG lever (restricts reach). **SEQUENCING: #3 + #1 parallel (independent); #2 GATED behind #1 going green (don't absorb into an undiagnosed broken panel). EQUIP: #3 sql+coder+webapp-testing; #1 ui+coder+sql+webapp-testing (manager smoke load-bearing); #2 ui+sql+coder+webapp-testing.** ⚠️ CONCURRENCY INCIDENT: `6a013ad` mixed-boundary — a parallel session's FG-stock KPI suite + mig 305 swept in via the SHARED git index; nothing broken, leave as-is; LESSON = separate clones/worktrees or serialize commits. → [sales-commercial-surface/README.md](maltyweb-pm-memory/sales-commercial-surface/README.md) §SINGLE-SOURCE REPOINT + §FINANCIER + §CONCURRENCY INCIDENT. (4) **✅ BOARD COGS/COP P&L GRID — ALL 4 STAGES DONE, ARC CLOSED (SHIPPED+LIVE+PUSHED; maltyweb `d0c7e60` ACTUALS → `b477565` BUDGET, both pushed; remote == deployed).** Built into Financier id=239 per the §BOARD ruling — host EXTENDED, NO new Node script/JSON. `financier-data.php` new modules `cop-grid`+`cogs-grid`: ACTUALS = `inv_charges_bc` net SUM(debit−credit) grouped by `gl_account_no` (is_summary=0), GL→line map mirrors the sheet exactly; `fin_gl_booked_months()`/`fin_ytd_months()` intersect window with booked months so cost numerator + HL denominator cover the SAME months (honest under-read). Denominators: COGS÷HL sold (`inv_sales_bc.hl_resolved`), COP÷HL brewed (FIN_COP `hlBrewed`). Grid card at top of fin-panel-cop/cogs, `renderCopGrid`/`renderCogsGrid` tree tables, chip "mois complets uniquement". **VERIFIED 2026-03 vs independent SQL: Brewing 49.57 / Packaging 49.50 (4206 Labels −5.34 credit-net working) / Indirect 4.69 / Utilities 1.52 (waste only) / R&D 0.21 / TOTAL COGS VARIABLE 105.48 CHF/HL; manager full-page screenshot rendered.** PLACEHOLDERS render "—" (refuse-don't-guess held): BUDGET, N-1/2025 cost, variance%, semi-var labor split, COGS grand total, COP per-type (Keg/Bott/Can/Bulk), Scotch; zero-booked GL = "0.00" (≠ "—"). **COVERAGE GATE: `inv_charges_bc` has only 2026-03 full (411 rows) + 04/05 THIN (20/22) → grid DEFAULTS to 2026-03; thin months can't be auto-distinguished from complete (chip caveat only) — bookkeeper must upload full monthly GL via Le Cockpit.** Commit-hygiene: first commit swept 3 parallel-session files via shared index → reset --soft + recommitted clean 4-file `d0c7e60`, restored the 3 to staged (same shared-index race lesson). **✅ STAGE 4 (BUDGET) SHIPPED+LIVE+PUSHED 2026-06-11 (maltyweb `b477565`, pushed; mig `327`).** `ref_budget_cogs` = 21 GL-line rows, fiscal_year 2026, `budget_chf_per_hl`, schema_meta classified; SEEDED from the operator's board-sheet COGS&YIELDS BUDGET column (total **75.76 CHF/HL**) — seed-from-explicit-statement, every number traces to a cell (not guessing). cogs-grid module loads budget per line → **BUDGET (month+ytd, FLAT ANNUAL RATE) + Vs BUDGET = (actual−budget)/budget %** columns now LIVE on the COGS grid (April proof: Malts −37.3%, Hops −20.6%, **TOTAL COGS VARIABLE −8.0%**). **COP grid UNCHANGED — no budget on its sheet.** **v1 APPROX (documented): Transport + QA/QC budgets are monthly-profiled in the source sheet but seeded as the annual rate.** Manager-auth screenshot verified. ⚠️ **GIT HYGIENE: `b477565` swept a parallel expeditions session's staged DELETION of `public/css/expeditions-sku-history.css` and was pushed before catch — deletion SAFE (file unreferenced; expeditions shipped in `e7962ce`; removal intended) so left, not history-rewritten. 2nd bare-commit sweep in one session → pathspec-commit is now a HARD RULE (CHECKLIST).** **ARC BACKLOG (non-blocking, NOT gating): N-1 cost (2025 GL CSV ingest → N-1/Vs-N-1 cols still "—"); COGS-YTD-starts-March coverage chip; May utilities booking; the dead `fin_ytd_months(rolling6m)` path the COP stage flagged.** **ARC STATUS: COP/COGS board grids fully re-sourced to operational SoT, per-line, budget-comparable, Vue-comptable+CHECK reconciliation — all 4 stages done.** Full as-built → [sales-commercial-surface/README.md](maltyweb-pm-memory/sales-commercial-surface/README.md) §BOARD COGS/COP P&L GRID §STAGE 4 BUDGET.
