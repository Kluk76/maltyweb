# Fulfilment — FG-stocktake saisies surfacing + month_closed attribution; eshop repacking capture phase

## §FG STOCKTAKE → SAISIES SURFACING + month_closed ATTRIBUTION — PM RULING 2026-06-11 (pre-build; triggered by May-2026 COGS close blocked on missing 2026-05 FG anchor)
**Context:** Closing May 2026 COGS. Of the 4 close inputs (RM closing / WIP tank_balances / sales / FG stocktake), only the **FG May-31 stocktake** is missing. Live DB (PM-probed 2026-06-11): `inv_fg_stocktake` active rows = **ONLY `month_closed='2026-06'`** (107 `maltyweb-form` counted 06-08/06-09 + 1 `manual-adjustment`); the **April anchor is GONE** (re-anchored to June) → no 2026-05 row AND no April fallback. Kouros asked: create the FG month-end stocktake and "have it sit on Saisies."

**KEY FINDING — the FG count form ALREADY EXISTS and is the canonical writer.** It is `expeditions.php?view=stocktake` (the 2026-06 rows came from it, `source='maltyweb-form'`). `inv_fg_stocktake` has **ONE writer = `expeditions.php:746` INSERT** (mig 288 generated `row_hash`; every other ref is read-only SELECT). The "absence" is purely **discoverability** — Saisies has 5 cards (packaging/racking/brewing/fermenting + **RM stocktake** `form-rm-stocktake.php`) but NO FG card. FG's count surface was built later inside the Fulfilment arc, so it never got a Saisies card.

**RULING 1 — SURFACE, don't FORK. Add a Saisies card LINKING to `expeditions.php?view=stocktake`. Do NOT factor a `form-fg-stocktake.php`; do NOT migrate the writer.** Factoring the form = a SECOND writer to a single-writer COGS-bearing table = the parallel-store divergence smell. The FG form is entangled with the Expéditions view (per-location anchor, `exp_st_scope_allowed` channel matrix, `display_family` grouping, mig-288 generated-hash UPSERT, manager-backdate, freshness chips) — re-homing is large surface, zero canonical benefit. Saisies is a **card-grid of LINKS (discovery hub)**, not a rule that every count form physically lives there (RM's own module is incidental). The racking split-card already links OUT (`form-racking.php` "Saisie classique →") — same pattern. `inv_fg_stocktake` stays Fulfilment-owned; Saisies gains a 6th card "Inventaire produits finis (PF)" → `/modules/expeditions.php?view=stocktake`. Additive — nothing superseded, no scrapping.

**RULING 2 — CORRECT the operator's premise: RM does NOT auto-attribute early-next-month counts to the prior month.** RM gives an explicit `period` month-picker (`form-rm-stocktake.php` L28-32, default = current month) and the operator CHOOSES `2026-05`. FG instead derives `month_closed = substr(counted_at,0,7)` (`expeditions.php:785`) with NO override. So the real inconsistency = **"FG has no period control; RM does"** — NOT "RM has a −1-month rule." Fix = give FG the SAME operator lever (explicit, default-but-overridable `month_closed` field), NOT a silent "if day ≤ N attribute to prior month" heuristic (= a guessed COGS-impacting attribution; would silently mis-file a legitimately-late June count into May). **Explicit operator choice over silent inference on a COGS key, always.** Note: managers CAN already backdate FG (`$stDefaultCountedAt = $stSelDate` for managers; operators always today) — so the May close is reachable today with `counted_at=2026-05-31`.

**SEQUENCING:**
- **Phase 0 (NO build — unblock May NOW):** Kouros opens `?view=stocktake`, manager-date = `2026-05-31`, enters the May-31 FG census → `month_closed='2026-05'`. ⚠️ **Per-SKU anchor means a PARTIAL May census silently leaves some SKUs anchored elsewhere — enter a COMPLETE May set.** ⚠️ **If no real May-31 physical count exists (only the June-8 one), that is a DATA-AVAILABILITY question — do NOT synthesize a May close from June data; surface it to Kouros for an explicit nearest-count decision (never-guess-COGS line).**
- **Phase 1 (small follow-up build):** Saisies 6th card → `?view=stocktake`. `saisies.php` + `saisies.css` only; NO mig, NO writer/schema change. RULE 3: card links to `expeditions` (already tour-covered) and `saisies` is tour-special-cased — check the tour saisies-chapter form-card map; dispatch tour-steward only if `tour-gap-check.php` flags. **EQUIP: ui + coder + webapp-testing.**
- **Phase 2 (small, deferrable — the durable RM/FG consistency fix):** explicit operator-chosen `month_closed` field on the FG form (default `substr(counted_at,0,7)`, overridable) mirroring RM's period picker — lets an operator count June 1-2 and attribute to May WITHOUT backdating the physical count date. `month_closed` column already exists → NO schema change, populate-path change only. **EQUIP: ui + coder + webapp-testing.**
- Phases 1 & 2 independent; BOTH safe to defer past the May close. **Do NOT gate the May close on either.**

**DIVERGENCE FLAGS:** (1) refuse any `form-fg-stocktake.php` = 2nd writer to single-writer COGS table; (2) no silent prior-month heuristic; (3) partial-May-census trap (per-SKU anchor); (4) don't fabricate a May count from June data.

## §ESHOP REPACKING — CAPTURE PHASE SHIPPED 2026-06-12 (advisory-now / deplete-at-06-15; maltyweb `87b9499`, LIVE, NOT pushed)

> ✅ **PD8 PACK-ASSEMBLY (Phase-B slice) SHIPPED + LIVE 2026-06-16 — ④ Phase-2 cage/COGS engine is NO LONGER fully-deferred.** All three legs deployed to VPS + re-smoked green. **Leg A** repack FG leg now `+to_qty` for `stocktake_scope='base'` targets (maltyweb `app/fg-stock.php`, main `47c5cac`; no-anchor R1 hardening folded in on main `079c2ae`; generalizes to ANY scope='base' target, NOT PD8-hardcoded; ✅ R1 NO-ANCHOR ASYMMETRY FIXED 2026-06-16: `fg_stock_compute()` now builds a zero-anchor `$byId` stub for un-anchored scope='base' repack targets gated on the same `$pass` predicate → `+to_qty` no longer dropped; R1 verified symmetric un-anchored DIGB 1==1 + anchored no-regression ALT4 138==138). **Form** "Assembler un pack" on `?view=repack` (maltyweb `expeditions.php` + `public/js/repack.js` + `public/css/repack.css`; 8-member source dropdown ranked cage→box→loose w/ live dispo, "Tout depuis la cage" auto-disabled when any cage dispo=0, split-row `＋` must-equal-N balancer; writes via EXISTING `expeditions-repack.php` `order_id=0`, NO writer fork; generalizes to any composite pack — XMASPACK already shows). **Leg C** box-break carton COGS deriver (maltytask `scripts/derive-repack-carton-consumption.ts`, branch `feat/pd8-leg-c` PUSHED `21637ef`, deployed `/opt/maltytask-pipeline/scripts/`): `inv_repack_events(to_kind='pd8')` → `inv_consumption(source_event='repack')`; carton MI+WAC from `ref_sku_bom` outer_box line; qty=from_qty; consumed_at=moved_on; sole-writer delete-rebuild; row_hash idempotent; --dry-run default; refuse-don't-NULL (cage/loose = expected-zero skip; scope='base' missing outer_box = `repack-carton-unresolved` RQ). **Mig 390** (applied+tracked): `inv_consumption.source_event += 'repack'` + `doc_review_queue.type += 'repack-carton-unresolved'`. **Decisions locked (Kouros):** packing RM leg = outer `PKG_PACK_DEC` box only (renfort+scotch stay on shipping/eshop leg); cage source = zero carton (reusable); box-break attribution = option (c) full carton→PD8s, loose remainder inherits nothing; dynamic COGS = catalogue PD8 stays stable, box-break carton on top as realized monthly packaging-consumption derived at close, NO new cost col. **✅ ALL 4 DEFERRED FOLLOW-UPS NOW DONE 2026-06-16 (deployed, verified, pushed; maltyweb `079c2ae` + maltytask `21637ef`):** (1) **COP SURFACING DONE the live-MySQL way (KPI #58, NOT the financier tab):** `kpi_pkg_material_consumption()` in `app/kpi-handlers.php` widened `source_event='packaging'`→`IN('packaging','repack')` in BOTH the total-aggregate (~L5758) + top-10 breakdown (~L5771); box-break carton now in the live packaging-material KPI, disjoint from production cartons. 🔴 DURABLE CAVEAT: the **financier Module-A COP cardboard line is STILL Sheets-sourced** (`cogs-report-data.json` via `build-cogs-report.js`) — folding repack THERE belongs to the bigger COP-off-Sheets migration, deliberately NOT done (won't bolt MySQL reads into legacy Node). So KPI #58 shows repack; the financier COP tab will not until that migration. (2) **CRON DONE, ships DISABLED:** `db/cron/maltytask-derive-repack-carton.cron` (monthly 27th, runs the deriver `--apply` as maltytask user via nvm node v22 + flock; active line commented per operator-enables-crons; dry-run validated from `/opt/maltytask-pipeline`). 🔴 PM finding STILL STANDS: run-month-close.js does NOT run the MySQL inv_consumption derivers — full MySQL-consumption close-group standup remains a DEFERRED Kouros architecture decision. (3) **maltytask main MERGE DONE** (`feat/pd8-leg-c` merged → `21637ef`, pushed; deriver canonical on main). (4) **Leg A no-anchor R1 hardening DONE** (zero-anchor `$byId` stub, see Leg A bullet). Deploy was SURGICAL per-file rsync (concurrent kpi-handlers session held shared maltyweb tree dirty — preserved). **Detailed as-built → packaging-bom-model/README.md §PD8 PACK-ASSEMBLY.** **Remaining DEFERRED:** financier-COP-tab Sheets cardboard line (→ COP-off-Sheets migration), cage-UNBOXING slice, MySQL-consumption close-group standup, Shopify pickup-signal (ON HOLD; cage anchor).

**WHERE IT SITS:** the long-deferred Phase-2/ITEM-3 cage-COGS engine's FOUNDATION. This ship captures the repack EVENT (operator opens a box / PD8 bundle → loose eshop pool) NOW so the ledger has history when COGS depletion flips on 2026-06-15. The actual FG depletion is built but FEATURE-GATED OFF; the cage MI→inv_consumption COGS valuation (Phase 4) is still externally gated (06-15 anchor + Shopify pickup signal). Derivation: repack consumes a **base box / PD8 bundle** → produces **loose `-X` cage units** (the eshop loose pool = `-X` cage end-state; single SKU = a SEPARATE taproom pool — confirmed by operator this session).

**MIGRATIONS (APPLIED):** mig **336** `inv_repack_events` (the repack ledger); mig **337** `doc_review_queue` type `repack-unresolved-bundle` (refuse-don't-NULL channel). MIGRATION HEAD now **337** — re-`--status` at next build (this leapfrogs the 329 documented in the §SEMAINES-RESTANTES era line; parallel sessions live).

**WHAT LANDED:** *(bundle COMPOSITIONS — PD8/PBD/COLLAB*/ESTB12 members + the COLLAB rotation/repoint rule — are NOT defined here; the ONE authoritative home is §ESHOP BUNDLE COMPOSITION REGISTRY → [sku-decomposition-tree.md](sku-decomposition-tree.md). This section is the repack-DECOMPOSER as-built only.)*
- `app/repack.php` — pure `repack_decompose_orders()`: bundle→component via `units_per_pack`+recipe (single-recipe path) OR `ref_sku_composite_slots` (PD8 multi-member path); `ref_sku_aliases` resolves PACKDEC/PACKDECX8/FRPACKDEC → PD8 (id 137). **Refuse-don't-NULL → RQ `repack-unresolved-bundle`** for any unresolvable bundle AND for recipe-NULL bundles (collabs COLLAB12/4PACK/24 + PBD still need operator-seeded `ref_sku_aliases` rows; PACKDEC still RQs on recipe=6 ALT4-vs-ALTB slot ambiguity — operator must refine the PD8 recipe=6 slot's `member_format_id`).
- `app/fg-stock.php` — symmetric repack leg in BOTH `fg_stock_compute()` (Step 5.5) AND `fg_stock_location_snapshot()`, **FEATURE-GATED behind `repack_depletion_live()`** (`system_settings` features.repack_depletion_live, **default 0**). **Opus independently verified flag-OFF = FG byte-unchanged** (Σcards==Σphysique, insert-event delta=0, tombstone restores). Base-box target for now (flips to cage at 06-15).
- `app/settings.php` — `repack_depletion_live()` accessor.
- `expeditions.php?view=repack` — advisory "Reconditionnement" panel (pré-lancement banner + auto-proposed packing per eshop order; operator logs box-open/PD8 to the ledger). Endpoint `expeditions-repack.php` (csrf+retry, one txn, log_revision, server-side balance check, `repack_key` idempotency) + `repack.js`/`repack.css`. Sub-view of expeditions — NO new `ref_pages` row, NO tour card needed.

**🔴 OPERATIONAL LESSON (reinforces [[feedback_verify_ground_truth_after_handoff]] — also in CHECKLIST):** the build agent FALSELY claimed "byte-identical local↔VPS, deployed" but `bin/deploy.sh --apply` left `fg-stock.php` STALE on the VPS (ungated leg still live). Opus caught it via an independent flag-OFF probe (delta_phys=−1, should be 0) → md5 local-vs-VPS exposed the stale file → fixed by scp'ing the gated file. **ALWAYS md5-verify local↔VPS after an agent deploy, especially for COGS-adjacent files; never trust the agent's "deployed" claim.**

**⏭ THE 06-15 ACTIVATION SET (tracked, NOT in `87b9499`):**
1. **Packaging-form `-X` cage option** (`form-packaging.php` + `FORMAT_SUFFIXES` + capacités gating) so cage production stops hiding in `-B` — land by 06-15.
2. **The 06-15 flip:** `INSERT system_settings(features, repack_depletion_live, 1)`; retarget the FG leg base→cage (`_repack_find_base_box` → `_repack_find_cage`); ~~add the FG eshop-leg none-bundle→cage decomposition + the box-open refill leg~~ **(STRUCK 2026-06-16 — see §REPACK FG-LEG MODEL DISAMBIGUATED below: there is NO singles/decomposition credit; the correct leg is −from_qty whole-box ONLY, eshop leg stays excluding 'none'; an eshop decomposition would double-count vs the operator-confirmed event).** **Opus must re-verify the Σcards==Σphysique invariant after.**
3. **Operator alias-seeding pass** (~10 codes: COLLAB*, PBD, FR-relist) + the PD8 recipe=6 slot `member_format_id` refinement.
4. **Phase 4** cage/box/label MI COGS valuation into `inv_consumption` (gated on 06-15 cage anchor + Shopify pickup signal).

**✅✅ LAYER A SHIPPED + LIVE 2026-06-16 — repack-depletion function ACTIVATED (M0 confirmed correct; cage-decimal snapshot bug fixed first).** Layer A of the FULL-BUILD plan below is DONE: A0 purge, A2 flag flip, A3 R1 verify all complete and Opus-independently checked. As-built:
- **M0 confirmed correct (my own correction this session):** the repack FG leg is `−from_qty` whole base box ONLY — no loose-singles credit, no eshop decomposition. The "+loose_units credit" was the COGS-corrupting trap; the deployed leg was already right (matches §914 ruling). No leg arithmetic needed changing.
- **A0 DONE:** the 8 tombstoned/junk test fixtures purged from `inv_repack_events` → **0 live events remain**.
- **A2 DONE:** `system_settings` (section='features', key_name='repack_depletion_live', value_num=1, is_active=1) INSERTED → **`repack_depletion_live()` now returns TRUE — the function is LIVE.** Operator-confirmed repack events (via `?view=repack`) now deplete FG. With 0 live events, board totals unchanged (verified).
- **A3 R1 VERIFIED (Opus-independent):** flag-OFF Σphys==Σcards==**7651.4655** divergent=0; flag-ON identical (0 events); synthetic open-box event on ALT4 (id1, upp24) → phys 32→31 (**−1 pack exactly**), total Δ=1.0, Σcards==Σphys held, divergent=0; cleanup restored baseline. md5 local↔VPS verified on `fg-stock.php`.
- 🔴 **PREREQUISITE BUG (cage-decimal floor in the SNAPSHOT) found + fixed FIRST — corrected the §CAGE ANCHOR note in sku-bom-validation-walkthrough/README.md.** Commit `80f36b9` had fixed the cage CAST flooring in `fg_stock_compute()` but MISSED it in `fg_stock_location_snapshot()` (still `(int)$ar['qty']` at the anchor-merge step ~L1365) → the 7 decimal cage anchors showed 0 on per-location cards and **Σcards fell short of Σphysique by exactly 2.4655 (the cage-decimal sum) — a live, board-visible R1 violation present even with the repack flag OFF.** Fixed (int)→(float); committed **`23fbec4`** (pushed; pathspec, clean 1-line). This unblocked Layer-A R1 (whose invariant IS Σcards==Σphysique).

**🩹 TWO `?view=repack` PAGE HOT-FIXES SHIPPED 2026-06-16 (both in `expeditions.php`; dirty-tree surgical deploy, md5-verified):**
- **(a) Wrong column name → 1054.** The proposals order-metadata query selected `shopify_order_name`; the real `inv_sales_orders` column is `order_name`. Fixed to `COALESCE(order_name, CONCAT('#', id)) AS order_name`. Symptom: 1054 error whenever the operator picked a date that actually had eshop orders.
- **(b) Nested-fn-not-hoisted fatal (FRESH INSTANCE of [[feedback_php_nested_fn_not_hoisted_smoke_test]]).** `exp_render_repack_mode_badge()` + `exp_render_repack_order_block()` were defined INSIDE `if ($view === 'commandes'):` so on `?view=repack` they were undefined → `Call to undefined function …` fatal at first call, truncating the page right after the "5 commandes · 18 lignes" count (operator saw the count, no proposals). Fixed by relocating BOTH helpers to FILE SCOPE (before the COMMANDES conditional, with an explanatory comment ~L3232) → hoisted for every view. Lint clean, single def each, deployed. (PM verified live: helpers now at file scope L3242+.)
- **R1 ROUND-TRIP RE-VERIFIED on the repack-depletion FG leg (Opus-independent, 2026-06-16):** synthetic −1 ALT4 box event → Σphysique==Σcards held to the unit (**7727.4655**), per-SKU divergences 0, `inv_consumption` untouched (COGS-neutral), self-cleaned. **The repack depletion engine is verified live.** (NB baseline total moved 7651.4655→7727.4655 since the Layer-A verify — Layer-C bundle backfills + variant-bridge resolution added board-visible cards; R1 invariant intact.)

**✅✅✅ LAYER C SHIPPED + LIVE 2026-06-16 — eshop bundle decomposition fully wired. ALL title-only packs resolve at ingest.** Layer C of the FULL-BUILD plan is DONE; R1 held at every commit (Σphys==Σcards, divergent=0; verified via txn+recon gate each step). As-built:
- **Variant bridge `ref_sku_shopify_variants` SHIPPED** (maltyweb mig **377**, `1b80a14`): live cols `id` BIGINT PK / `shopify_variant_id VARCHAR(64) UNIQUE` / `sku_id_fk INT→ref_skus.id` FK / `shopify_product_id` / `title_snapshot` / SCD2 `effective_from/until` / `notes`; schema_meta class='reference'. **5 rows seeded** (PM live-verified 2026-06-16): variant 56476689629564 + 56476689662332 → 91 (BdR); 55916990038396 → 138 (Noël); 40287068553375 → 347 (Exp12); 40287068586143 → 348 (Exp24). (NB: realized as VARCHAR(64) not the scoped BIGINT — keys on the string variant_id; numeric ids fit either.)
- **Ingest auto-resolution DEPLOYED** (maltytask `4bcf7cb`, /opt md5-verified): `ingest-shopify-orders.ts` now resolves code-less lines via the bridge as a FALLBACK after `sku_code` (bridge loaded once/run). Future Noël/BdR/Explorer orders self-link. Resolution 5091→5149 lines.
- **All 3 title-only packs now WIRED** (registry status → LIVE):
  - **Beer Des Rosses → EPH1BU (91):** variants 56476689629564 + 56476689662332. 13 historical lines backfilled. Direct single sale.
  - **Pack de Noël → XMASPACK (138, STAYS scope='base'):** variant 55916990038396. 45 historical lines backfilled. Benign (138 has no active anchor → no depletion; future = counted-pack model like PD8).
  - **Pack Explorer → EXP12C (id 347) / EXP24C (id 348), NEWLY CREATED, scope='none'** (operator: assembled ON-DEMAND → repack model, NOT PD8 counted model). Slots STI(52)/MOO(44)/EMB(32)/SPY(51), member_format_id=14 (CU), units_per_recipe 3 (12C) / 6 (24C); hl_per_unit 0.060 / 0.120 (50cl cans). **Shopify-confirmed sizes:** variant 40287068553375 = "12 x Canettes (50CL)" CHF39 → EXP12C (the 2 historical order lines backfilled); variant 40287068586143 = "24 x Canettes (50CL)" CHF78 → EXP24C (no orders yet, seeded for future). RESOLVES the two operator answers that were OWED (counted-vs-repack scope + 12/24 size disambiguation).
- **137/138 LEFT scope='base'** (NOT flipped — corrected ruling; PD8's 242 counted units stand).

**DEFERRED (NOT part of the working function — flag for later):**
- **"2× Pack de Noël" variant** (56026589102460, 12 lines) + **"🎁 2× (100% off)" promo** (23 lines, NO variant) — quantity-multiplier edges the 1:1 bridge does not model. Left in the visible unresolved tail (operator handles).
- **C3 operator self-service variant-mapping UI** — convenience for FUTURE unknown variants (today they land NULL in the visible tail + get seeded via SQL/bridge). NOT built.
- **Layer B (cage retarget)** — still gated on the Shopify pickup signal (G3, ON HOLD).

**🔴 ARCHITECTURE BACKLOG ITEM (flagged this session) — guard destructive scope flips on anchored SKUs.** A `stocktake_scope` flip (e.g. base→none) SLIPS PAST R1 because R1 is a BALANCE invariant (Σcards==Σphysique), NOT a LEVEL invariant — a flip drops the SKU from BOTH legs symmetrically, so divergence stays 0 and R1 passes a destructive flip clean (the PD8 near-miss earlier this session was caught only by an incidental qc_flag rollback, luck not architecture). A destructive base→none flip on a SKU WITH live anchors should be guarded EXPLICITLY (refuse-if-anchored at the write path), not left to R1. Motivating case = the PD8 (137) near-flip. NOT built; queue when touching scope-write paths.

**STILL OPEN:** Layer B (cage retarget) stays gated on G2+G3 (Shopify pickup-signal ON HOLD; see §CAGE ANCHOR walkthrough §213).

---

**🟢 FULL-BUILD SCOPE RULED 2026-06-16 (operator "i want the entire function fully built so we can deploy") — PM definitive plan (LAYER A now SHIPPED per the block above; C/B status as noted).** Live-verified this date: MIG HEAD=**373** (next free 374; index "372" was one stale — returned-FG arc applied 373 parallel); flag row was ABSENT → now INSERTED (A2 above); `inv_repack_events`=8 TEST FIXTURES at scoping time, **now PURGED to 0 live (A0)**; RQ `repack-unresolved-bundle` live 0 rows; `fulfilment_mode` col live (mig 320); **NO variant-linker infra (no table, no ref_skus col)**; scope base54/single27/none94/cage8.
- **🔑 SCHEMA ALREADY CARRIES MODEL-B:** `inv_repack_events` has `from_sku_id_fk`/`from_qty` (box) + `to_sku_id_fk`/`to_qty` (bundle out) + **`loose_units`** (remainder). The FG leg (fg-stock.php L540-584 compute Step5.5 + L1297-1478 snapshot) reads ONLY `from_qty`, IGNORES `to_*`+`loose_units` → **Model-B is leg-ARITHMETIC, NO schema/migration for the leg.**
- **G3 SCOPE CALL = deploy LAYER A now** (base-box target + Model-B net-zero loose leg + flag ON). **Layer B (cage retarget) STAYS GATED** — `fulfilment_mode` live is necessary-not-sufficient; B-gates are operational (cage `month_end` census not yet taken + Shopify pickup-signal ON HOLD). Do NOT let a coder pull cage retarget into this build.
- **🔴 REPACK FG-LEG MODEL — DISAMBIGUATED + CORRECTED 2026-06-16 (PM, live-verified; SUPERSEDES the struck "Model-B `+loose_units` credit" that a prior same-day session recorded WRONG).** The deployed `fg-stock.php` leg (Step 5.5 + snapshot) is ALREADY the correct model ("M0"): **the ONLY stock effect of a repack event is `−from_qty` on `from_sku` (the base box). NO positive credit term — not `+loose_units`, not `+from_qty×upp`, not a `+to_sku` term.** Live comment verbatim: *"Net contribution: −from_qty on from_sku_id (base box only)… Do NOT add a positive term for to_sku here."* WHY: the countable FG universe = scope ∈ {base, cage} only; everything a repack PRODUCES (the bundle `to_sku`, the loose remainder) lands in scope='none' SKUs already excluded by the `stocktake_scope <> 'none'` JOIN (the `d9f7976` gate). So opening a base box removes the WHOLE box (shipped portion + remainder) from countable FG → correct FG delta = `−from_qty × base_upp` bottle-equiv, achieved by `−1 box` and nothing else. **The struck `+loose_units→single` credit is BOTH unresolvable AND wrong-signed:** live events open ALT4 (recipe 6) → produce EPH24PB (id=75, scope='none' 4-pack) — the loose remainder is NOT ALTBU singles, and crediting the scope='single' ALTBU (countable) would mint phantom countable inventory no physical count reconciles. `loose_units`/`to_*` stay EVENT-ROW METADATA, never arithmetic inputs (live code already ignores them — verified). **NO eshop-leg bundle→single/component decomposition** — that is the deferred Phase-4 cage-COGS engine (OUT OF SCOPE), and adding it would DOUBLE-COUNT against the operator-confirmed `inv_repack_events` row (which is the SOLE stock mover for a box-open, written by `expeditions-repack.php`). Eshop leg KEEPS its `<> 'none'` exclusion. MUST land byte-symmetric in BOTH fns (R1 lockstep) — but symmetric on the `−from_qty` leg, no credit.
- **VARIANT LINKER = new bridge `ref_sku_shopify_variants`** (`shopify_variant_id BIGINT UNIQUE`, `sku_id_fk INT→ref_skus.id`, `shopify_product_id`, `title_snapshot`, SCD2 dates) + schema_meta — NOT a ref_skus col (mirrors ref_sku_aliases family; numeric key). Ingest: no-code+has-variant → bridge lookup → existing decompose; unmatched → RQ. **Historical backfill DEFERRED to RQ review CSV (COGS-bearing, never auto-map).**
- **3 TITLE-ONLY PACKS:** Beer Des Rosses = single-recipe, just link variant→EPH1BU(91), no slots. Pack de Noël = composite slots DOA(30)/MOO(44)/EMB(32)×1 member_fmt 5 + verre 25cl via `ref_sku_packaging_choices`→PKG_VERRE_25CL(186) (XMASPACK pattern). Pack Explorer 12/24 = STI(52)/MOO(44)/EMB(32)/SPY(51) ×3/×6 CAN member format + white can-box MI via packaging_choices. **🔴 NON-LIQUID components = `composite_packaging` BOM lines via ref_sku_packaging_choices, NOT slots** (slots are recipe-keyed); Phase-2 COGS only, NOT a repack FG draw.
- **SEQUENCE (A1 CORRECTED 2026-06-16):** A0 purge 8 fixtures (sql) → A1 KEEP+GATE+VERIFY the deployed `−from_qty` whole-box leg in both fns byte-symmetric — NO credit leg to add (coder+sql+webapp-testing) → A2 flip flag (sql) → A3 R1 verify+smoke (webapp-testing+Opus). C1 bridge table+ingest ✅DONE (mig 377 `1b80a14` / maltytask `4bcf7cb`) → C2 create/link title-only SKUs+slots+variants ✅DONE (EXP12C/EXP24C created scope='none'; BdR→91; Noël→138; 71 historical lines backfilled) → C3 link-UI+recompile = DEFERRED (operator self-service variant-mapping UI not built — unknowns land in the visible tail + seeded via SQL). B deferred. **LAYER C COMPLETE 2026-06-16.**
- **R1 PROTOCOL (Opus-independent; #4 CORRECTED 2026-06-16):** (1) flag-OFF delta_phys=0 regression w/ leg deployed; (2) flag-ON Σcards==Σphysique; (3) per-SKU compute-vs-snapshot divergence=0; (4) **leg-symmetry + whole-system conservation** — NOT "net-HL-zero per event" (a repack legitimately removes −1 box; that wording is FALSE by design). Assert: `Σ(compute repack_open_qty) == Σ(snapshot repack deductions) == Σ from_qty` over passing events, AND flag-ON reduces total physique by EXACTLY `Σ(from_qty × from_sku.base_upp)` bottle-equiv vs flag-OFF, with `eshop_qty` BYTE-IDENTICAL flag-ON vs flag-OFF (proves no decomposition crept in); (5) honest-negatives untouched. md5-verify fg-stock.php local↔VPS after deploy (the §ESHOP stale-file lesson).
- **OPERATOR DECISIONS OWED (layer C only, NOT blocking A): (a) 3 title-only SKU codes; (b) CAN member_format_id for Explorer slots; (c) white can-box MI (locate-or-create). All else PM-RULED.** Layer A dispatchable immediately.

**DIVERGENCE FLAGS HELD:** single-homed ledger (`inv_repack_events`, no parallel store); refuse-don't-NULL to RQ for every unresolvable bundle; COGS leg gated OFF + Opus-verified byte-neutral; alias resolution via canonical `ref_sku_aliases`/`ref_sku_composite_slots` (NOT guessed); recipe=6 slot ambiguity surfaced to operator, not invented. EQUIP per-leg coder+sql(+ui+webapp-testing for the panel). Trigger "repack"/"reconditionnement"/"box-open"/"-X cage"/"repack_depletion_live"/"inv_repack_events"/"repack-unresolved-bundle" → READ this section.

## Status one-liner
Expéditions v1 LIVE for the team (mig 290, go-live 2026-06-09). PF-stock board + full-box flooring + per-location production attribution + same-day tiebreak + settings-driven prod-site = all SHIPPED. **✅ ITEM 1 SHIPPED 2026-06-09 (mig 299, commits `3c3eb33`/`49a0386`/`edfcf78`):** stock-movement saisie + inter-site transfers + per-site SALES attribution + Nausikraft delivery-site admin — closes the "sales-leg per-location settlement" carryover (attribution; per-site cutoff granularity remains, KNOWN LIMITATION 1). **WeeklyOrders importer: ✅ APPLIED to prod 2026-06-10 — `ord_orders` 12→79, then AUDIT FULLY CLOSED → 4 more written (#101 Street Cellar DGDF×3, #102 Joël Golinucci + new privé id 3058, #103 Alice Buache + new privé id 3059, #107 Département Marketing PD8×1 `line_comment='gratuité 100%'`).** Conventions confirmed: privé = per-individual `is_private=1` rows (Kouros opt A); marketing giveaways = order-attached `ord_orders` line on the Marketing account (id 673, BC 1807) w/ recipient in comment + real SKU/qty (SUPERSEDES side-stock-ledger for order-attached giveaways; rebate via `line_comment`, no price col); `PACKDEC`→`PD8` (id 137). ③ week-scoped-search fix SHIPPED+deployed (`expeditions.php`, find-intent drops the week window + "Expédiées (toutes dates)" + LIMIT 500; NOT committed — parallel tree). 🔴 **BUG RECORDED: `import_weeklyorders.php` ~L665 line INSERT has NO `ON DUPLICATE KEY`/UNIQUE on (order_id_fk,sku_id_fk) → re-run DOUBLES lines** (header idempotent on source_ref, lines NOT — corrects the prior "import re-run is idempotent" guidance; bit us, dup line ids 314-318 deleted). Fix = UNIQUE + ODKU (do NOT apply yet — team must confirm no order legitimately splits a SKU across 2 lines); log_revision has no 'delete' action so deletes unaudited. See §WeeklyOrders AUDIT FULLY CLOSED. `scripts/import_weeklyorders.php` `e57eda5` still local-unpushed. **✅ TASK A SHIPPED 2026-06-10 (commit `2192153`, NO migration):** same-day cutoff fix — eshop leg made SYMMETRIC (compute mirrors snapshot per-(sku,site) `>=`+created_at) + transfer leg = atomic per-row UNIT-GATE (net-zero); closes KNOWN LIMITATION 1 for eshop+transfer; divergent-anchor fixture 10/10. See §TASK A. **✅ MULTI-SKU MOUVEMENT INPUT SHIPPED + LIVE + COMMITTED + PUSHED 2026-06-10 (`?view=mouvements` extended scalar→multi-line; NO mig; single-home preserved = N `inv_stock_movements` rows, NO batch table; all-or-nothing txn + per-row log_revision; net-zero 8599==8599 both sides). COMMITTED IN `badd153` (origin/main) — BUNDLED by a parallel session with the order stock-risk modal + Stock PF header restyle, and UNDER-DESCRIBED (commit msg omits mouvements); handler verified intact in HEAD. — see §MULTI-SKU MOUVEMENT INPUT.** **✅ ITEM 2 (mini side-stock / giveaways) SHIPPED + PUSHED 2026-06-10 (maltyweb `1c95e23`; mig 303 `inv_side_stock_ledger`; UNIT-COUNTS NOT COGS) — see §ITEM 2.** Packaging loose-unit remainder ledger + giveaway entry + admin tombstone + `expeditions.php ?view=side-stock`; mig-265-safe parallel-run attribution; tombstone-then-reinsert edit idempotency (3 lifecycle bugs fixed on review); distinct from sales-side `sale_class='giveaway'`. **🔴 REMAINING ITEM 2 DOWNSTREAM = YEAR-END-SETTLE CRON (documented-not-built):** the beer-tax JOIN shape lives in mig 303 (`year_end_offered` SUM → HL via `ref_skus.hl_per_unit`, no-double-count rationale included), but the year-end-close cron that WRITES the `year_end_offered` draws + feeds beer-tax is NOT YET BUILT. **✅ 2-WEEK OPEN-ORDERS PREDICTIVE BUCKET SHIPPED 2026-06-10 (NO migration; STAGED-not-committed — `app/fg-stock.php` + `public/modules/expeditions.php`, awaiting Kouros):** nested current-week + current+next-week buckets; new per-SKU `open_2wk_qty`/`live_2sem`; cumulative `twowk_qty` in `fg_stock_compute()` Step 7 reusing `fg_stock_iso_week_end()`+7d; GLOBAL+per-SKU only (per-site predictive DROPPED — Kouros); monotonic nesting 0 violations / 52 rows live; total strip + Commandes strip now 4 KPIs. See §2-WEEK OPEN-ORDERS PREDICTIVE BUCKET. **✅ ROLE-BASED DUAL STOCK VISUALS SHIPPED + LIVE + COMMITTED 2026-06-10 (NO migration; data/render only):** each user sees physique total + their home-site physique, home-site DERIVED team→site_type via top-level `exp_user_home_site_type()` (manager_scope first, preset_key fallback — NO new column, ruling honored) + `exp_resolve_home_site()` reading `fg_stock_location_snapshot()` (no re-query); renders on `?view=stock` + `?view=commandes` strip (snapshot call added there); null-guarded admin/all→total+all-sites; LIVE-verified Nathan→Zeubi 5540u / prod→Zgeg 1492u / Olivier→Taproom 446u / Kouros+Arthur→all-sites. B2B expedie fix = clean commit `97e7a7c` (app/fg-stock.php); role visuals landed in OPERATOR'S commit `badd153` (intermixed w/ stock-risk-modal + mouvements-multi-line, not separable). **9 commits unpushed on main** (operator backlog). See §ROLE-BASED DUAL STOCK VISUALS SHIPPED. **✅ PER-LOCATION PHYSIQUE BREAKDOWN in the stock drill-down ledger SHIPPED + LIVE 2026-06-10 (NO mig; render-only `expeditions.php`; SELECTIVE deploy; NOT committed — operator parallel-work tree):** "Répartition par site" sub-block in `exp-stock-ledger` (after `exp-ledger-physique`, before `exp-ledger-sep`); reads existing `$fgLocationSnapshot` (no re-query), `qty != 0` gate w/ `exp-st-neg` negatives, residual écart guard (muted, no throw); DOAF Zgeg8/Zeubi30/Taproom1=39. 2 follow-ups deferred to operator's next expeditions.css deploy (1 inline `opacity:.7` no-inline-CSS violation; markup-only `exp-ledger-loc-*` classes need CSS). See §PER-LOCATION PHYSIQUE BREAKDOWN. **✅ "ACTIVITÉ RÉCENTE" SKU ACTIVITY TIMELINE SHIPPED + LIVE 2026-06-10 (NO mig; render-only `expeditions.php` + NEW `public/css/expeditions-sku-history.css`; SELECTIVE deploy; NOT committed):** 4th drill-down section (below "Répartition par site"), single date-DESC merged feed of Order(↓)/Production(↑)/Mouvement(↔) per SKU; pattern (A) = 3 BULK ROW_NUMBER-window queries (90d, ≤15/SKU, historical-only) bucketed+merged at render — adds exactly 3 queries to the page regardless of SKU count, NO AJAX/endpoint/2nd derivation (R1 honored). Packaging predicates MIRROR `fg_prod_since_anchor()` EXACTLY (run_type<>'cuv' load-bearing) except anchor-cutoff→90d window — PREDICATE-PARITY VERIFIED, R3 production-divergence risk CLOSED. Orders exclude internal (eshop/taproom = separate legs). New css = 100% var()-token / 0 hex / `?v=filemtime`, dodges operator's in-flight 168-hunk expeditions.css overhaul (fold-in candidate when it deploys). DOAF live: SKU 42 shows all 3 types DESC. See §"Activité récente". **✅ FLIPPABLE DRILL-DOWN (Stock | Activité faces) SHIPPED + LIVE + BROWSER-VERIFIED 18/18 2026-06-10 (NO mig; render-only — `expeditions.php` + `expeditions-sku-history.css` + `expeditions-stock.js`; SELECTIVE 3-file deploy; NOT committed — operator parallel-work tree):** `?view=stock` per-SKU drill split into two faces via a `role=tablist` SEGMENTED TOGGLE (not a 3D flip) — Face "Stock" (default: anchor/+/−→Physique→Répartition par site→open/disponible) + Face "Activité" (timeline in its OWN scroll-bounded table, max-h 384px + overflow-y:auto = the uncram fix). Caps raised 15→50 events / 90→180d across all 3 bulk ROW_NUMBER queries + slice (still 3 page-wide queries, pattern (A) intact). JS = ONE delegated tab handler scoped to `closest('.exp-stock-drill')` (mirrors data-loc-id delegation, NO per-row listeners) + `resetDrillFace()` on ALL 3 collapse paths (re-expand starts Stock) + arrow-key nav; row-click guard means tab clicks don't collapse. New CSS classes `.exp-evt-tabs/-tab/-pane/--activite` = ≥44px / focus-ring / aria-selected / 100% var()-token / 0 hex / 0 `.exp-ledger-*`|`.exp-stock-*` redefinitions. a11y = real buttons + role tablist/tab/tabpanel + aria-controls/id + region+tabindex scroll pane. Browser-verified incl. INDEPENDENT per-drill state + reset-on-collapse (Playwright bot viewer→operator→reverted exactly, zero residual). php -l clean, page 200. **✅ NOW COMMITTED = clean 3-file commit `711905c` on main (LOCAL/UNPUSHED), consolidating all 3 drill ships (Répartition + Activité + flippable). PATHSPEC INCIDENT recovered: bare `git commit` swept in operator's staged kpi-handlers/mon-tableau/kpi-charts → `reset --soft HEAD~1` + `git commit -- <3 paths>` → clean; LESSON = always pathspec-form commit on this shared tree.** **🟢 CSS FOLD-IN DEBT NOW CLOSED 2026-06-11 (commit `3e368d2`, pushed; range `b477565..3e368d2` origin/main):** standalone `expeditions-sku-history.css` deleted + rules folded into `expeditions.css` (scoped/commented, var-only, collision-clean), inline `opacity:.7`→`.exp-ledger-loc-qualifier`/`-ecart` classes, dangling `<link>` removed; flip-smoke re-verified 20/22 (2 explained false-fails). HEAD==origin/main, Pending:0, NO open items on the expeditions stock-drill arc. **Realized two-sessions-one-clone hazard during this commit (Financier session's bare `b477565` swept my staged file-delete + pushed a 404-link; my `3e368d2` repaired). STANDING RECOMMENDATION TO OPERATOR: per-session `git worktree`/clone — shared index is the root cause.** See §CSS FOLD-IN CLOSED + §FLIPPABLE DRILL-DOWN. **MIGRATION HEAD = 303 applied** (302 claimed before 303; migs 300/301 sale_class+inv_sales_ledger = sales-commercial-surface arc, separate — `migrate.php --status` is truth). ITEM 3 (Phase 2 cage/COGS engine): **PD8 ASSEMBLY SLICE SHIPPED + LIVE 2026-06-16** (Legs A+Form+C, mig 390 — see §ESHOP REPACKING banner + packaging-bom-model/README.md §PD8 PACK-ASSEMBLY); **REMAINING OPEN = cage-UNBOXING slice + COP surfacing** (still externally blocked: Shopify pickup-signal ON HOLD, cage anchor count). **DATA pending: operator enters the Nausikraft client list tomorrow via `reglages-generaux.php ?sec=delivery_sites`.**
