# Fulfilment — Shopify⇄maltyweb fulfilment integration (RE2 roadmap + full build)

## ✅ SHOPIFY FULL-AUTO TRACKING-STATUS slice (schema+flag LIVE; CODE COMMITTED b4e1749; ⚠️ NEVER DEPLOYED TO /opt) — AS-BUILT 2026-06-22, CORRECTED 2026-06-29
> Trigger "shopify tracking"/"tracking_number"/"shopify_fulfilled_at"/"fulfillments"/"auto-advance shipping"/"shopify_auto_advance_shipping"/"reconcileShopifyFulfilment"/"force-auto-advance"/"R-0"/"R-1"/"lazy-create fulfilment"/"mig 434"/"mig 435" → this block.

🔴 **CORRECTION 2026-06-29 — the "R-0 LIVE" claim below was an OVER-CLAIM. PM-VERIFIED ground truth:** migs 434 (4 tracking cols) + 435 (OFF flag) ARE applied on the VPS DB, and the TS was LATER committed as **`b4e1749`** "full-auto fulfilment tracking-status…" (clean single-file, 200+/51-). **BUT the TS was NEVER DEPLOYED to `/opt/maltytask-pipeline`** — the live `*/1` cron (`/etc/cron.d/maltytask-shopify-ingest`, runs `/opt/.../scripts/ingest-shopify-orders.ts`) still runs the **Jun 19 / 1245-line PRE-tracking copy** (last `--apply-pipeline` deploy was the ee06469 era). So R-0 data-CAPTURE has NEVER actually run in production — only the schema + flag went live. Classic migration-shipped-code-didn't half-deploy; the /opt copy drifted 2 commits behind git HEAD. R-1 auto-advance being DISARMED (flag `shopify_auto_advance_shipping='0'`, LIVE-confirmed) was deliberate + correct; the capture-not-live was an incomplete deploy, NOT a deliberate hold-back. **R-0 capture goes live only when the current HEAD ingest is deployed to /opt** (pending operator decision 2026-06-29, bundled with a mig-447 customer_note/note_attributes capture change — note + tracking cols all EXIST on the DB, flag OFF, so the deploy is architecturally SAFE; deploy SURGICAL per-file, NOT `bin/deploy.sh --apply-pipeline` which whole-tree-rsyncs scripts/+lib/ and would sweep dirty `lib/enums.ts`+untracked scripts; witness the first cron tick). DURABLE: after any migration-bearing ingest session, `md5sum /opt/.../ingest-shopify-orders.ts` vs local — schema can land while code doesn't.

**Two slices built this window. R-0 = data capture (schema migrated + code committed b4e1749, but NOT deployed → NOT live, see correction above). R-1 = auto-advance (LIVE in code, DISARMED by an OFF-default flag — fail-safe). Cron unaffected (no-op until armed).**

### R-0 — tracking capture (LIVE)
- **mig 434** `434_shopify_fulfilment_tracking_cols.sql` (maltyweb, applied; head now 435): 4 nullable cols on `inv_sales_orders` → `shopify_fulfilled_at DATETIME`, `tracking_number VARCHAR(128)`, `tracking_url VARCHAR(512)`, `tracking_company VARCHAR(128)`, placed `AFTER fulfilment_mode_source`. 🔴 **PIE­GE recorded: `inv_sales_orders` has NO `updated_at` column** (don't reference one in ALTER/UPSERT).
- `scripts/ingest-shopify-orders.ts` (maltytask): added `'fulfillments'` to the Shopify field-set; derives tracking from the most-recent `status==='success'` fulfillment (created_at desc); writes all 4 cols on BOTH the INSERT and UPDATE paths (+ drift-detect in `mutableChanged` + audit before/after). Tracking is CAPTURED but NOT yet RENDERED on the board chip (= R-2 below).

### R-1 — auto-advance shipping (ARMED-IN-CODE, DISARMED-BY-FLAG, delivery-only)
- **mig 435** `435_shopify_auto_advance_flag.sql` (applied): `system_settings` section=`fulfilment` key=`shopify_auto_advance_shipping` **value_text='0' (OFF default)** is_active=1 — EXACT mirror of Nathan's BC flag (mig 433 `bc_auto_advance_shipping`). Two independent auto-advance kill-switches now exist, one per source.
- `reconcileShopifyFulfilment()` was **armed + hardened (NOT created)** — guardrails AS-BUILT:
  - **Gated / fail-safe OFF:** skips entirely unless `settingBool('fulfilment','shopify_auto_advance_shipping')` OR CLI `--force-auto-advance`. The */15 `--apply` cron (no `--force`) stays a NO-OP until the flag flips. Prints "Shopify auto-advance DISABLED … skipped" when OFF.
  - **Delivery-only (KEY garde-fou):** query filters `iso.fulfilment_mode='delivery'`. Pickup + review are NEVER touched (stay manual — Kouros decision).
  - **Lazy-create:** `INNER JOIN inv_sales_fulfilment` → `inv_sales_orders LEFT JOIN inv_sales_fulfilment`; a delivery order fulfilled in Shopify with NO local line → INSERT `inv_sales_fulfilment` (status='fulfilled', shopify_sync_state='pushed', prepared_by_user_id/site NULL) + event (source='shopify_reconcile', occurred_at = shopify_fulfilled_at else NOW()). Fast-forward straight to 'fulfilled' (Shopify collapses all steps at packing-slip print).
  - **Advance-only:** existing non-terminal lines advance; NEVER revert from a terminal state.
  - `ReconcileResult` extended with a `created` counter.

### Arming protocol (transmit when Kouros wants it ON)
1. SAFE preview WITHOUT touching the prod flag or arming the cron: `npx tsx scripts/ingest-shopify-orders.ts --dry-run --force-auto-advance --since <date>` → lists would-create/would-advance. VERIFIED: `--since 2026-06-01` → **229 delivery creations, 0 pickup/review.**
2. When ready: flip `shopify_auto_advance_shipping`→'1' → the */15 cron arms auto-advance. ⚠️ memory rule [[feedback_testing_a_flag_a_live_cron_reads]]: flipping the flag = the NEXT cron tick does REAL writes (creates fulfilled lines over the rolling 2-day window). No "test by flipping" — it IS production.

### Verification (this window)
- typecheck: 0 new errors (3 pre-existing unchanged).
- dry-run flag OFF → "DISABLED … skipped", 0 writes.
- dry-run `--force-auto-advance` → 229 created delivery-only, 0 pickup.

### R-2 — NOT done (optional)
- Render tracking number / fulfilled-at on the eshop board chip (R-0 data is captured, not yet rendered; the "Expédié" status TAG already shows via the Phase-2A cache). Cron still local-on-SSH-tunnel (migrate to VPS one day).

## ✅ ACCENT-FOLD NORMALIZER (shipping titles) — DONE/LIVE 2026-06-19
> Trigger "accent"/"normaliseShippingTitle"/"NFD"/"refold"/"Expédition gratuite accent"/"mig 402"/"title_norm" → this block.

`normaliseShippingTitle()` (ingest-shopify-orders.ts, maltytask) now NFD-decomposes + strips combining marks → "Expédition gratuite" folds to "expedition gratuite", matching the id=18 mapping. Paired maltyweb mig **402_refold-shipping-title-norm.sql** re-folds the 2 pre-existing accented `ref_shipping_methods` rows (id3 'la nébuleuse sa'→'la nebuleuse sa', id15 chronoshop '…après…'→'…apres…'); collision-checked, no UNIQUE conflicts. Deployed to /opt + applied/recorded; re-ingest reclassified the 6 stuck accented orders → review bucket **47→41**. Commits maltytask **82e1af9** / maltyweb **702d835**. (Mig 401_planning_working_days is ANOTHER session's, local/undeployed — only 402 was deployed.) Resolves the "Expédition gratuite WITH accent ×6" item from the §DRAFT-ORDER block's open list.

## ✅ SILENT-DROP AUDIT of ingest-shopify-orders.ts — ALL FIXES SHIPPED + LIVE 2026-06-19
> Trigger "silent drop"/"4th hole"/"external_line_id"/"line idempotency"/"financial_status stale"/"refunded forever"/"row_hash order"/"uk_lines_order_line"/"stable line dedup"/"mig 406"/"upsertLine"/"partial refund re-pull" → this block.

The 4th-hole hunt (8 findings) is CLOSED. All actioned findings deployed to /opt + (F's migration) applied on the VPS. The recurring root — dupe path INSERT-IGNORE no-op silently keeping a stale row — is now eliminated on BOTH the order header (G) and the line (F) paths: dupe paths UPDATE content in place. Same feed-completeness discipline as the draft-order/`--until` holes.

- **✅ Finding G (financial_status refresh) — SHIPPED, maltytask `ce99d01`, no schema.** Dupe-path order UPDATE now SELECTs/compares/UPDATEs `financial_status` (row_hash deliberately STILL excludes it — hash=identity, ride the mutable-UPDATE path, per the established connector pattern) + report line "financial_status fixed: N". The wide live re-ingest was the backfill: **10 already-rotted orders CORRECTED** (stale 'paid' that should've been refunded/other). No separate backfill SQL.
- **✅ Finding F (stable line dedup) — SHIPPED, maltytask `ee06469` + maltyweb mig `406_inv_sales_order_lines_stable_dedup.sql` `a597ce1`, applied.** DROPPED both positional `uk_lines_order_line` AND `uk_lines_row_hash`; ADDED `UNIQUE uk_lines_order_ext (order_id_fk, external_line_id)` as the SOLE dedup key (INPLACE/LOCK=NONE). `external_line_id` verified **100% populated, 0 pre-dups** → no COALESCE fallback needed (pre-flight flag (2) cleared empty). KEPT `line_index` (repack.php ORDER BY) + `row_hash` (change-detect) as columns. Connector: both INSERT-IGNORE line-writes collapsed to ONE `upsertLine()` ON DUPLICATE KEY UPDATE refreshing all mutable fields → content changes now UPDATE in place. VERIFIED idempotent: 2nd identical run = 0 content updates, lines stable at **6574**, 0 dups on either key; first run = 1907 one-time content-settle updates (row_hash/hl_resolved from the old INSERT-IGNORE era).
  - 🔑 **DURABLE counter-nuance (record for any future ODKU report-counting):** the pool runs `CLIENT_FOUND_ROWS`, so `affectedRows=1` for UNCHANGED ODKU rows too → insert vs no-op are INDISTINGUISHABLE. Honest reporting = "Lines written (new/unchanged): N" + "Lines updated (content changed): M"; **M is the only reliable signal.** Don't trust affectedRows to mean "inserted" on an ODKU under CLIENT_FOUND_ROWS.
- **✅ Hardening A/B/C/H (bundled w/ G) — SHIPPED `ce99d01`:** **A** `--since` now REQUIRED (loud error + format-validated — no silent hardcoded-2025-12-01 clip; cron unaffected, passes --since explicitly, verified sole invoker); **C** `--limit` applied POST-collection only (early pagination break removed) + refuses live use without `--force-limit`; **H** `toPeriod()` derives YYYY-MM from the UTC instant (matches created_at; pre-existing mismatch checked = 0 rows, purely preventive); **B** documented the created_at_min UTC-midnight contract (comment only).

🔑 **MIG-NUMBERING REALITY (record):** a parallel session deployed 401/403/404/405 (water-QA + planning) while this work ran; **406 was the next free** and applied cleanly. So as of this window applied head is **≥406** — re-`migrate.php --status` + `ls db/migrations` at every build-start; parallel sessions keep leading the number.

## ✅ DRAFT-ORDER SKIP-FILTER BUG — DIAGNOSED + FIXED + LIVE 2026-06-19 (the "missing John Penman order #13662")
> Trigger "missing order"/"John Penman"/"#13662"/"draft order"/"shopify_draft_order"/"source_name"/"SKIP_SOURCE_NAMES"/"admin-created sale"/"phone/event/counter sale"/"Expedition gratuite" → read THIS block first.

**ROOT CAUSE (architectural — a silent feed-completeness hole):** `scripts/ingest-shopify-orders.ts` (maltytask) hard-skipped every order with `source_name='shopify_draft_order'` (old `const SKIP_SOURCE_NAMES = new Set(['shopify_draft_order'])`, comment "not real eshop transactions"). **That assumption was wrong.** Shopify's `/orders.json` only returns *completed* draft orders (OPEN drafts live in `/draft_orders.json`, which the ingest never queries) — so a draft-sourced order appearing in the feed is a REAL completed sale created via the Shopify admin (phone / event / counter / comp). The filter silently dropped ~2 admin-created sales/day from the board, FG depletion, and fulfilment. This is the same class as the §APR-30 `--until` gap and the §INGEST LINE-IDEMPOTENCY bug: a feed-completeness hole that makes the canonical `inv_sales_orders` store under-count real sales.

**FIX (deployed + LIVE):** `SKIP_SOURCE_NAMES` emptied to `new Set<string>([])` (documenting comment). Draft-sourced orders now flow through the SAME `shipping_lines→fulfilment_mode` classifier as web orders (`ref_shipping_methods` → unmapped/null = `'review'`). No special-casing — no other `source_name` branch exists in classification, so single-homed-fact discipline preserved. Operator-chosen behavior.

**DEPLOY/BACKFILL:** file scp'd to `/opt/maltytask-pipeline/scripts/ingest-shopify-orders.ts` (md5-matched local↔VPS); live `--since 2026-06-01` backfill = 33 inserts (the `*/15` VPS cron had already auto-grabbed #13662 in its trailing 2-day window). VERIFIED in DB: #13662 John Penman present, paid CHF 168, `fulfilment_mode='pickup'` (auto).

**✅ ALL THREE OPEN ITEMS CLOSED 2026-06-19:**
1. **COMMITTED** — fix is on maltytask main as **`b125e90`** ("fix(ingest-shopify): stop dropping completed draft-sourced orders"), committed by pathspec. Git HEAD and the deployed `/opt` copy now AGREE — `/opt`-vs-git divergence resolved, no longer a revert footgun.
2. **HISTORICAL BACKFILL DONE.** Seal-state checked FIRST (correct discipline): `cogs_fiche_sealed` EMPTY (0 rows — nothing sealed), only April has a frozen `cogs_fiche_seed` (12 rows, protected by seed>live resolution) → **no sealed month restated.** Ran live `--since 2025-12-01`: **314 inserted, 612 lines, 3270 idempotent dupes.** `inv_sales_orders` now holds ALL **351** draft-sourced orders (2025-12→2026-06 = 165/23/25/26/40/38/34); **~CHF 20.3k recovered Dec–May sales** (~191 are CHF-0 comps that STILL deplete FG). Confirms the historical-completeness concern was real and is now closed.
3. **ONE shipping mapping ADDED + order reclassified.** `ref_shipping_methods` id=18 → `title_norm='expedition gratuite'`, `fulfilment_mode='delivery'`, `is_active=1` (operator CONFIRMED free-shipping = delivery — not guessed). Affected order **#13456** auto-reclassified review→delivery.

**🔴 STILL OPEN (carried to roadmap — operator/future; see index #0a11):**
- **(a) `review` bucket 47→41 triage + unmapped admin-typed shipping titles — OPERATOR, NEVER GUESS.** Awaiting operator/board triage: `Personnalisé` ×17, `Livraison` ×2, typos `Expedition gratuitwq` / `Livaison Offerte` / `Expedition (0,75.-/km)`, plus pickup-ish `RETRAIT AU SHOP` / `Sur place` / `Déjà récupéré` / `Retrait Crissier Staff`. NONE mapped (some genuinely ambiguous: km-rate could be delivery, "Déjà récupéré" reads pickup). (The accented "Expédition gratuite" ×6 already resolved by the §ACCENT-FOLD NORMALIZER block.)
- **✅ (b) `is_non_stock` watch — CHECKED, CLEAR.** Confirmed **0 non-stock SKUs among the 351 draft orders' lines** → no tap-machine (T7) pours wrongly depleting FG. Item closed.
- **(c) orphan-line pruning — DOCUMENTED, NOT auto-deleted; needs a reconcile-step decision.** Lines that vanish from a re-pull (removed/refunded lines) are NOT pruned by the new upsert path — the stable `(order_id_fk, external_line_id)` key updates present lines but leaves a since-removed line in place. Documented as a code comment; deciding whether to add a per-order reconcile-prune step is OPEN (same orphan-prune class as the derived-table NOT-EXISTS prune rule — see [[feedback_derived_table_must_prune_orphans]]; a removed Shopify line could leave a phantom FG-depleting row).
- **(d) eshop SKU-bridge gap (pre-existing, affects ALL eshop orders) — ~56% line resolution.** `PACKDEC` / `PACKDECX8` / `PBD` etc. eshop SKU codes don't resolve to `ref_skus` → ~44% of eshop lines unresolved. Predates this audit; not caused by it. Needs the eshop→ref_skus bridge/variant-mapping (relates to the C3 self-service variant-mapping UI in the §DEFERRED BACKLOG).
- **Ingest hardcoded-filter audit (standing, now largely DONE):** this audit WAS the one-time sweep — it hardened the remaining skip-sets/date-bounds (`--since` now required + validated; `--limit` post-collection + `--force-limit` gate; UTC period). The recurring 3-hole class (`--until` Apr-30 → line-idempotency → `SKIP_SOURCE_NAMES`) is closed; keep the DURABLE LESSON as the standing check.

**DURABLE LESSON:** a hardcoded skip-list on an ingest is a silent feed-completeness hazard — every `SKIP_*`/`--until`/`--since`/source_name filter on a canonical-store writer is a place real facts can be dropped without an error. When auditing "a sale is missing from the board," check the ingest's exclusion filters FIRST (skip-set, date bound, source_name branch), then classification (`review` bucket), then XOR membership. The `/orders.json` vs `/draft_orders.json` distinction is the key Shopify fact: completed drafts surface in `/orders.json` and ARE real sales.



## ✅ SHOPIFY ⇄ maltyweb FULFILMENT INTEGRATION — ROADMAP APPROVED 2026-06-10 (Kouros; plan `~/.claude/plans/sleepy-snuggling-brooks.md`). RESOLVES the long-PARKED ④ Phase-2 / pickup-signal ON-HOLD gate (the §① "FORWARD HAZARD" at lines ~310 + MEMORY active-arc).
> Trigger "Shopify"/"pickup"/"click&collect"/"retrait"/"shipping_lines"/"fulfilment_mode"/"eshop picking"/"push-shopify"/"write-back"/"ref_shipping_methods"/"inv_sales_fulfilment" → read THIS section first.

### ✅ SHOPIFY-INGEST VPS REHOME — EXECUTED + LIVE + COMMITTED 2026-06-12 (OPTION 2; PHP-rewrite REJECTED). The brief's blocking premise ("VPS has NO Node/tsx toolchain") was FALSE — it's just not on the login PATH.
> **DONE:** Option 2 executed exactly as ruled. `/opt/maltytask-pipeline` synced to current HEAD (`bin/deploy.sh --apply-pipeline`; stale 05-22 copy gone, ingest md5-matches local + has shipping_lines/fulfilment_mode; npm ci ran); `secrets/shopify.env` on VPS (640, maltytask:www-data); cron `db/cron/maltytask-shopify-ingest.cron` → `/etc/cron.d/maltytask-shopify-ingest` (`*/15`, maltytask user, nvm idiom, DB 127.0.0.1:3306 via db.env, NO tunnel); **commit `5ade521`.** VPS dry-run + a real freshening run (board now seconds-fresh, 3164 eshop orders); **autonomous fire CONFIRMED** (11:00 UTC tick wrote the log, idempotent no-op). Local-WSL `*/15` tunnel crontab RETIRED — VPS cron is sole runner. The /opt-sync GATE is CLEARED (the sync IS what fixed the stale-script hazard the ruling flagged). Ruling rationale retained below for provenance.
> Trigger "shopify rehome"/"rehome ingest"/"shopify cron VPS"/"php rewrite shopify"/"node on VPS" → read THIS block first.
**LIVE-VERIFIED VPS GROUND TRUTH (2026-06-12, ssh maltyweb):**
- **Node IS on the VPS** under nvm: `/var/www/maltytask/.nvm/nvm.sh` → **node v22.22.2**; **tsx binary present** at `/opt/maltytask-pipeline/node_modules/.bin/tsx`. `which node` empty ONLY because nvm isn't on the login PATH — the `document-ingest`/`ingest-recap` crons ALREADY invoke it via `NVM_DIR=/var/www/maltytask/.nvm bash -c '. nvm.sh && node_modules/.bin/tsx …'`. So the "PHP-only box" framing is wrong: it's a DUAL PHP+Node(nvm)+Python3 box.
- **The maltytask PIPELINE repo is ALREADY deployed to the VPS at `/opt/maltytask-pipeline`** (lib + scripts + node_modules + secrets + package.json) — SEPARATE from the PHP app at `/var/www/maltytask`. **`scripts/ingest-shopify-orders.ts` is ALREADY PRESENT there** (`/opt/maltytask-pipeline/scripts/ingest-shopify-orders.ts`, 23516 bytes, dated 2026-05-22). The script doesn't need porting — it's on the box.
- **The TS ingest is ALREADY VPS-runnable verbatim:** it connects via `lib/repos/mysql-pool.ts` which reads `DB_HOST`/`DB_PORT` from env; the pool's own doc comment says *"On the VPS itself DB_PORT=3306 (direct, no tunnel)."* On the VPS, MySQL is local → NO tunnel, NO 13306. Two env vars (`SHOPIFY_STORE`/`SHOPIFY_ADMIN_TOKEN`) + a VPS `secrets/shopify.env` (mode 640, www-data group, NOT committed) are all that's missing. line-idempotency already fixed (`bac5501`).
- **The RE-1 / BC external-API-pull pattern on the VPS is PYTHON, not PHP** — `maltytask-bc-sales` cron runs `scripts/python/ingest_bc_sales_ledger.py` (OAuth2 client-credentials against `login.microsoftonline.com`, `requests` + `pymysql` localhost:3306, watermark anti-join, ON DUPLICATE KEY idempotency, creds `config/bc.env` keys BC_TENANT_ID/CLIENT_ID/CLIENT_SECRET/ENVIRONMENT, mode 640). It is currently DISABLED-pending-smoke. So there is NO "PHP-on-VPS external-API-pull" pattern to converge with — the established external-pull connector pattern is a stand-alone Python/`requests` cron. Converging Shopify onto a *PHP* skeleton would mean inventing a 3rd pattern, not reusing one.
**RULING:** **Option 2 — rehome the proven TS ingest as `/etc/cron.d/maltytask-shopify-ingest`** running from `/opt/maltytask-pipeline` (mirror the `document-ingest` cron's nvm-bash idiom + a flock), DB_HOST=127.0.0.1 DB_PORT=3306, with `secrets/shopify.env` landed on the VPS. **Option 1 (PHP rewrite) REJECTED:** it would reimplement a mature, COGS-adjacent, fulfilment-fact-bearing classifier (`shipping_lines→fulfilment_mode`, the canonical pickup-signal list per [[feedback_canonical_list_call_not_copy]]) in PHP/curl with real regression risk on a proven path — and gain NOTHING, since Node is already on the box and the script is already deployed. **Splitting the classifier across two languages is the exact divergence smell to avoid:** `fulfilment_mode` is the load-bearing pickup/delivery fact; it must stay SINGLE-HOMED in the TS ingest (one writer, one classification, `ref_shipping_methods` as the only mapping store). A PHP twin = two implementations of one fact = silent drift. **Architecture verdict:** this IS the "consume the host" pattern correctly applied — the host being consumed is the ALREADY-PRESENT `/opt/maltytask-pipeline` Node runtime + VPS cron infra, NOT a PHP rewrite. Strangler doesn't mandate PHP; it mandates the compute homing on the VPS via the standing cron mechanism, which Option 2 satisfies.
**BUILD (small, low-risk):** (1) land `secrets/shopify.env` on VPS (operator, not committed); (2) author `db/cron/maltytask-shopify-ingest.cron` (source-of-truth in maltyweb repo, deployed via `bin/deploy.sh --apply` → `/etc/cron.d/`) mirroring the `document-ingest` nvm idiom: `*/15` or `*/30`, user=`maltytask`, `flock -n /tmp/maltytask-shopify-ingest.lock`, `cd /opt/maltytask-pipeline && HOME=… NVM_DIR=/var/www/maltytask/.nvm bash -c '. $NVM_DIR/nvm.sh && DB_HOST=127.0.0.1 DB_PORT=3306 node_modules/.bin/tsx scripts/ingest-shopify-orders.ts --since "$(date -d "2 days ago" +%F)"' >> /var/log/maltytask/shopify-ingest.log 2>&1`; (3) ensure `/opt/maltytask-pipeline` carries the `bac5501` version of the script (it's dated 05-22 — PRE-`bac5501`; the line-idempotency/shipping_lines/`--until`-default fixes must be synced to /opt before arming — VERIFY); (4) pre-create the log (`maltytask:www-data`); (5) dry-run on VPS first (the ingest `--dry-run` ROLLBACKs); (6) keep the board freshness-chip (already shipped) — it's the staleness sentinel that justified this whole rehome and stays valuable. **OPEN op-decisions:** OK to put `shopify.env` on VPS + install the cron; pick cadence. **GATE:** sync /opt pipeline to post-`bac5501` HEAD before arming (else the rehomed cron runs the OLD pre-fix script). **SIZE:** ~S (one cron file + secret + a /opt sync + dry-run smoke), NOT the M-to-L PHP rewrite. **EQUIP: coder + sql + webapp-testing** (cron/CLI/host = coder; the freshness-chip + any board verification = webapp-testing smoke; sql only if the dry-run distribution check touches the DB). NB cross-repo: cron source lands in **maltyweb** `db/cron/`; the ingest script lives in **maltytask**/`/opt`.

### 🔑 KEY FINDING — the pickup/click&collect signal lives in `shipping_lines[].title` (LIVE Shopify-API-probed during planning), NOT a tag and NOT `fulfillment_status`.
- `fulfillment_status` is a COMPLETION-state field (a false friend for pickup-vs-ship — do NOT use it to classify mode). The ingest does **NOT currently fetch `shipping_lines`** at all.
- **Live pickup titles:** "La Tap & Shop (mercredi - samedi ; 14h-22h)", "RETRAIT A CRISSIER"/"à CRISSIER", "La Nébuleuse SA".
- **Live delivery titles:** "La Poste - Vinologue (fragile)", "Vinologue Suisse - Fragile", "Standard Suisse", "Livraison Offerte/offerte/gratuite".
- Messy free-text (team is mid-standardization) → **curated map + `review` bucket, NEVER guess** (never-guess-a-mapping canon). The plan threads this directly into the §① pickup double-deplete forward-hazard: once pickup orders carry a mode and sync into `inv_sales_orders`, they deplete via the eshop leg and the picking view READS them — they are NEVER re-created as `ord_orders` (XOR holds).

### Approved phased roadmap
- **Phase 0 — FEED FOUNDATION (buildable NOW, do FIRST; zero depletion risk):** (1) fix the ingest **line-idempotency bug** (below) FIRST; (2) capture `shipping_lines` → raw `shipping_method_title` on `inv_sales_orders`; (3) curated classification — new **`ref_shipping_methods`** (title_norm UNIQUE, `fulfilment_mode` enum pickup/delivery, seeded from the verified live titles) → ingest sets new **`inv_sales_orders.fulfilment_mode ENUM('pickup','delivery','review')`** (unknown→`review`, operator-actionable); (4) fix the hardcoded `--until 2026-05-01` (cause of the **Apr-30 data gap**) → default "now" + one backfill run + **cron the ingest on a trailing window with a flock**. Mig `320_inv_shipping_method_map.sql` (table + the 2 `inv_sales_orders` cols). NO gate.
- **Phase 1 — PER-ORDER ESHOP PICKING SURFACE (buildable now, READ-ONLY re depletion):** replace the date-aggregate eshop auto-row on the Commandes board with **per-order eshop rows** (customer, SKU pills, **pickup/delivery badge** 🏬 Retrait / 🚚 Livraison / ⚠ à classer, per-day pickup-vs-ship counters). Pure read from `inv_sales_orders`+lines (XOR trivial). Rows carry `data-eshop-order-id` (NOT `data-order-id`) so existing status JS never touches them; no actions yet. Include eshop in the cross-date search. Gate: Phase-0 `fulfilment_mode`.
- **Phase 2 — FULFILMENT TRACKING + BIDIRECTIONAL SHOPIFY WRITE-BACK (the big piece; build after 0+1):** eshop workflow state in a **dedicated 1:1 side table** `inv_sales_fulfilment` (keyed on `inv_sales_orders.id`) + `inv_sales_fulfilment_events` (append-only) — MIRRORS the `ord_orders` + `ord_order_status_events` pattern; lazy-created on first action (absent row ⇒ implicit `new`/`idle`); mode-aware lifecycle (delivery new→picking→picked→fulfilled; pickup …→ready_for_pickup→picked_up); cols incl. `shopify_sync_state` enum idle/pending/pushed/failed + `shopify_fulfillment_id` + push_error/attempts. Mig `321_inv_sales_fulfilment.sql`. UI: `exp_render_eshop_chips()` + sync badge + `public/js/eshop-fulfilment.js`. API: `public/api/eshop-fulfilment-status.php` (clone of `expeditions-status.php`; one txn = event + cache upsert + audit; push-triggering status sets `shopify_sync_state='pending'`, does NOT call Shopify inline). Worker: **`scripts/push-shopify-fulfilment.ts`** (cron+flock) drains `pending` → Shopify Admin **Fulfillment Orders API** (delivery=create fulfillment→fulfilled; pickup=ready_for_pickup→picked_up); idempotent (check open fulfillment-order + stored id; "already fulfilled"=convergence); capped retry + operator re-arm. **Reverse reconcile** via the now-idempotent ingest (`source='shopify_reconcile'`). **CONFLICT POLICY: maltyweb-wins-forward — reconcile only ever ADVANCES local to match Shopify, never reverts an operator action.** GATE: confirm the Shopify Admin token has fulfillment **write** scopes (read-only blocks write-back).
- **Phase 3 — PICKUP HARDENING + TAP MACHINES (gated on the team standardizing shipping-method names):** prune/extend `ref_shipping_methods` once names settle (drive `review`→0; admin to map unknowns without deploy; split board into Retrait/Livraison lanes). **Tap machines (T7) = NON-STOCK:** new `ref_skus.is_non_stock` flag (mig `322_*`, orthogonal-flag doctrine per mig 210); fulfillable + on the picking view but excluded from FG sum via `AND rs.is_non_stock = 0` added **SYMMETRICALLY** to the eshop leg in BOTH `fg_stock_compute()` AND `fg_stock_location_snapshot()` — verify `Σcards==Σphysique` after. (Resolves the §② "T7/Cuves = equipment, must NOT deplete FG" ruling at the SKU-config level.)
- **Phase 4 — CAGE/BOX COGS ENGINE = OUT OF SCOPE (named only).** The Stage-2 / ④ Phase-2 cage/box draw-down engine (downstream CONSUMER of this fulfilment data, gated on the 2026-06-15 cage anchor) is a SEPARATE arc, not designed here.

### Hard constraints carried (non-negotiable)
- **XOR depletion:** order ∈ `ord_orders` XOR `inv_sales_orders`, never both. Eshop fulfilment workflow creates **NO** `ord_orders` rows. The discriminator is membership in `inv_sales_orders`, not a flag we invent (§①c canon).
- **Single-homed FG derivation:** `fg_stock_compute()` is the only FG depletion. ANY filter added to its eshop leg (`app/fg-stock.php` ~L462-515) MUST be mirrored in `fg_stock_location_snapshot()` (~L814-850) or `Σcards ≠ Σphysique`. **P0/P1 add NO fg-stock filter → zero depletion risk. The only symmetric-filter change is P3 (`is_non_stock`).**
- **Raw vs computed:** `inv_sales_orders`/`inv_sales_order_lines` are `class='source'`, `corrections_policy='blocked_with_redirect'` (LIVE-VERIFIED 2026-06-10) — operator workflow state goes in the `inv_sales_fulfilment` SIDE TABLE, NEVER on the raw table. An ingest-derived `fulfilment_mode` column IS OK on `inv_sales_orders` because it's a PURE FUNCTION of the Shopify pull (same class as the existing `fulfillment_status`/`financial_status`/`period` derived cols), not operator-entered state.

### 🔴 INGEST LINE-IDEMPOTENCY BUG (Shopify side — must fix FIRST, Phase-0 item 1)
`scripts/ingest-shopify-orders.ts` `insertOrderBatch()` L378-384: `const wasDupe = orderRes.affectedRows === 0; if (wasDupe) { ordersDupe++; continue; }` — when an order PRE-EXISTS (INSERT IGNORE no-op), it `continue`s at ORDER level → **its lines are never re-evaluated and mutable header fields never updated.** So a re-pull of a CHANGED order silently no-ops; you can NEVER enrich existing orders with `shipping_lines`/`fulfilment_mode`, advance `fulfillment_status`, or do reverse-reconcile. **Fix: on pre-existing order, still (a) `INSERT IGNORE` the lines (already row_hash-idempotent), (b) `UPDATE` the mutable header fields (`fulfillment_status`, `shipping_method_title`, `fulfilment_mode`).** This is the SHOPIFY-side parallel of the already-recorded `import_weeklyorders.php` `ord_order_lines` UNIQUE/ODKU bug (§WeeklyOrders status one-liner) — same "header idempotent, lines/mutables not" class, different table.

### Phase-0 build facts (PM-verified live 2026-06-10)
- **MIGRATION HEAD = 319 applied (`migrate.php --status`); next free = `320`.** (Parallel sessions live — re-`--status` + `ls db/migrations` at build-start per checklist; 312 already collided once 312_drop_invoice_validate_topbar vs 312_kpi_sales — so a fresh check is mandatory.)
- **`schema_meta` for the NEW `ref_shipping_methods` table:** `table_class='reference'`, `corrections_policy='allowed'`, `upstream_hint` ≈ "Shopify shipping-method title → pickup/delivery classification map. Seed from verified live titles; add/edit via Expéditions admin or direct MySQL; unknown titles land orders in fulfilment_mode='review'." (Mirrors `ref_transporters`/`ref_customers` which are exactly `reference`/`allowed`.) ONE `schema_meta` row required per the per-new-table rule.
- **The TWO new `inv_sales_orders` columns** (`shipping_method_title` VARCHAR, `fulfilment_mode` ENUM('pickup','delivery','review')): **NO schema_meta change needed** — the table is ALREADY `class='source'`/`blocked_with_redirect`; ALTER-adding ingest-derived columns does NOT change its classification (schema_meta is per-TABLE, not per-column). Just add the columns; do not touch its schema_meta row.
- **INGEST HOST / CRON = the maltytask LOCAL host, NOT the VPS.** `ingest-shopify-orders.ts` runs `npx tsx scripts/ingest-shopify-orders.ts` on the maltytask box: it reads `secrets/shopify.env`, connects to MySQL **over the SSH tunnel (13306)** via `withConnection`/`withTransaction`, emits `logAudit` to `data/audit-log.jsonl`. It is run MANUALLY today (no cron). **There is NO existing maltytask user crontab** (`crontab -l` empty). The flock pattern to MIRROR = **`scripts/send-ingest-recap.ts`** (the only flock-guarded script; "designed to be run by a flock-guarded cron", writes preview to `/tmp`, exits 0 silently when nothing to do). Follow its lock idiom for the P0 ingest cron (and the P2 worker cron). NB: the SSH tunnel needs `-o ServerAliveInterval=30 -o ExitOnForwardFailure=yes` for an unattended cron run (existing keepalive convention).
- **APR-30 GAP cause = hardcoded `--until 2026-05-01`** in the ingest's default window (`'fulfillment_status','total_price',…` field set is fine; the date bound is the gap). Default it to "now" + run one backfill to today, THEN cron a trailing window. Dry-run-verify the backfill BEFORE going live (double-run = 0 orders/lines inserted but a changed `fulfillment_status` updates; FG totals UNCHANGED since no eshop-leg filter is added in P0).

### EQUIP — Phase-0 Sonnet build: **coder + sql** (+ webapp-testing only if P0 surfaces any rendered page — it does NOT, P0 is ingest+migration only, so coder+sql suffices). The TS edit (`ingest-shopify-orders.ts`, maltytask) = coder; the maltyweb migration `320` = sql. NB cross-repo: the migration lands in **maltyweb** (`db/migrations/`, applied via `migrate.php` on the VPS), the ingest edit lands in **maltytask** (`scripts/`, run locally over the tunnel) — two repos, one phase.

### Phase-0 gotchas (durable)
- **TS ingest edit:** preserve the `logAudit({...})` discipline — the `wasDupe` branch currently SKIPS audit; the new "update existing order" path must emit a `logAudit` event (e.g. `action:'order-updated'`) so the change shows in `data/audit-log.jsonl`. Keep `--dry-run` working (the `DryRunRollback` throw + ROLLBACK pattern at L425-434) for both the line re-eval and the header UPDATE. The order **INSERT IGNORE → upsert** change: do NOT switch the INSERT to `ON DUPLICATE KEY UPDATE` blindly — `row_hash` is part of the UNIQUE; a content change yields a NEW row_hash, so the upsert key must be the STABLE key (`UNIQUE(source, external_order_id)`), and the UPDATE must target only the mutable header cols (not re-stamp `created_at`/`period`). row_hash stays a change-detector, not the upsert key.
- **Migration:** MySQL 8 (no `IF NOT EXISTS` on `ADD COLUMN`; no bare SELECT in the .sql — `migrate.php` exec() leaves result sets open). FK type on `ref_shipping_methods` PK and any FK = match exactly. The `inv_sales_orders` ALTER adds 2 columns (INSTANT DDL on MySQL 8 — cheap). ENUM `fulfilment_mode` default should be `'review'` (so an un-classified order is operator-visible, never silently mis-moded). schema_meta INSERT for `ref_shipping_methods` in the same migration.

### Sequencing verdict (CONFIRMED)
- **P0 is SAFE to build + land NOW with ZERO change to `fg_stock_compute()` → zero depletion risk.** P0 adds an ingest-derived column + a classification map + cron + a bug-fix; it touches NO fg-stock leg. The first fg-stock change in the whole roadmap is P3's symmetric `is_non_stock` filter. P0+P1 ship first (immediate value, no depletion risk), then P2 (bidirectional sync, gated on the Shopify write-token scope), then P3 (gated on team name-standardization + the cage anchor).
- **Cron/backfill MUST be dry-run-verified before going live** (the ingest's `--dry-run` already ROLLBACKs; verify the `fulfilment_mode` distribution + a small `review` bucket + a double-run = 0 new rows but a changed `fulfillment_status` updates + FG totals unchanged, BEFORE arming the cron).

### ✅ PHASE 0+1 LANDED + COMMITTED 2026-06-11
- **maltyweb `7e1c001`** = mig **321** `inv_shipping_method_map` (`ref_shipping_methods` classification table + the 2 `inv_sales_orders` cols `shipping_method_title`/`fulfilment_mode`). **APPLIED on VPS** (PM-verified `migrate.php --status`: 321 ✓, Pending 0). NB roadmap predicted mig **320** for this; it actually landed as **321** (320 was the perf index `idx_bd_packaging_v2_reuse_ts` from the parallel mon-tableau session). Don't trust the roadmap's "320" — `--status` is truth.
- **maltytask `bac5501`** = the Shopify ingest edit (line-idempotency bug fix + `shipping_lines` capture + `fulfilment_mode` set + `--until` default-now). **maltytask `e7962ce`** = Phase-1 per-order eshop rows on the Commandes board + cross-date eshop search (`data-eshop-order-id` namespaced).
- **FG delta from the May/June backfill = −5 units (corrective, small).** Acceptable, recorded.

### ✅ PHASE 2 SPLIT 2A/2B — PM CONFIRMED 2026-06-11 (build 2A; 2B disarmed)
**Split is sound and approved.** 2A = maltyweb-internal fulfilment tracking (side tables + status chips + API) → **goes LIVE** (team marks préparé/prêt/remis in maltyweb with NO Shopify dependency). 2B = `push-shopify-fulfilment.ts` write-back worker → BUILT + dry-run-verified but **DISARMED (no cron)**.
- **🔑 2B ACTIVATION PREREQUISITE (RECORDED, for Kouros's review):** the Shopify Admin token has **NO write/fulfillment scopes** (verified via `access_scopes.json` → none). 2B is **DOUBLE-GATED**: (a) PM/operator standing boundary = no live customer-facing Shopify writes unsupervised, AND (b) the token literally cannot write until the team grants **`write_merchant_managed_fulfillment_orders`** + **`write_fulfillments`** and **reissues** the token. Clean external gate. Do NOT arm the 2B cron until both scopes are granted + token reissued + Kouros reviews.
- **2A SCHEMA — CONFIRMED as designed** (`inv_sales_fulfilment` 1:1 UNIQUE `order_id_fk` + `inv_sales_fulfilment_events` append-only; lazy-create-on-first-action ⇒ absent row = implicit `new`/`idle`; mode-aware lifecycle delivery new→picking→picked→fulfilled / pickup …→ready_for_pickup→picked_up; `shopify_sync_state` ENUM idle/pending/pushed/failed default `'idle'`). **Mig = `322_inv_sales_fulfilment.sql` (322 PM-VERIFIED FREE 2026-06-11: --status 321 applied, Pending 0; re-check at build, parallel sessions live).** Do NOT denormalize `fulfilment_mode` onto the side table — key the advance/revert map on live `inv_sales_orders.fulfilment_mode` at action time (mode is re-classifiable; snapshotting = second store of one fact).
- **FK TYPES — verify live before the migration** (don't trust the brief's recollection): `order_id_fk`→`inv_sales_orders.id` (confirm BIGINT UNSIGNED via SHOW CREATE), `fulfilment_site_id_fk`→`ref_sites.id` (INT UNSIGNED), `*_user_id`→`users.id` (INT UNSIGNED). FK-type mismatch = our #1 migration-failure class.
- **schema_meta — MIRROR THE `ord_*` SIBLINGS, do NOT default to `'allowed'`:** `inv_sales_fulfilment` is the eshop twin of `ord_orders` → copy `ord_orders`'s schema_meta `corrections_policy` exactly; `inv_sales_fulfilment_events` is the twin of `ord_order_status_events` (append-only) → copy that row, NEVER `'allowed'` on an immutable event log. Both `table_class='operational'`; one schema_meta INSERT per table in mig 322; writer hint = "eshop-fulfilment-status.php API + future push-shopify-fulfilment.ts worker".
- **API `eshop-fulfilment-status.php` — CONFIRMED clone of `expeditions-status.php`. ROLE GATE = login-only, NO role check** (PM-verified live: `expeditions-status.php` gates purely on `current_user() !== null` → 403 if not logged in; access control is at the PAGE level via the Expéditions `ref_pages` gate, NOT in the handler). Clone that: CSRF first-gate w/ fresh-token retry hint, ONE txn (event insert + `inv_sales_fulfilment` upsert + `log_revision`), PRG/JSON. **Push-deferral CONFIRMED + load-bearing:** reaching `fulfilled`/`ready_for_pickup`/`picked_up` sets `shopify_sync_state='pending'` and returns — does NOT call Shopify inline. This is why 2A is LIVE-safe pre-scopes (nothing in 2A touches the Shopify API).
- **2A GOTCHAS (flag all to the agent):** (1) lazy-create = `INSERT … ODKU` on UNIQUE `order_id_fk` inside the txn (not SELECT-then-branch race); render treats "no row" as `new`/`idle`, never NULL-crash. (2) **`fulfilment_mode='review'` order has NO lifecycle → handler must REFUSE advance** ("à classer d'abord"), not pick a default map (never-guess applies to lifecycle selection). (3) `data-eshop-order-id` namespacing (Phase-1 convention) — `eshop-fulfilment.js` binds ONLY on it; verify no collision with `expeditions-status.js`'s `data-order-id` delegation. (4) **`log_revision` ENUM has no `'delete'`** — any cancel/abandon tombstones via `action='update'` + an event row, never a delete (and the roadmap lifecycle has no cancel terminal — decide cancel semantics explicitly, don't add a delete path). (5) **DIRTY-SHARED-TREE HAZARD ACUTE** — tree holds foreign session work (financier-data.php, in-flight 168-hunk expeditions.css overhaul, kpi-handlers churn); deploy SURGICALLY (file-scoped scp + diff deploy-delta, `coder` skill "Dirty shared tree"), NOT `bin/deploy.sh --apply`; commit by PATHSPEC; put new chip CSS in a NEW file `eshop-fulfilment.css` to dodge the expeditions.css collision (same dodge as expeditions-sku-history.css).
- **6. CONFIRMED — 2A touches NO `fg_stock_compute()`, creates NO `ord_orders` rows.** XOR preserved; side table invisible to depletion. Agent verification step must grep the diff for `fg_stock_compute`/`fg-stock.php`/`ord_orders` INSERT → ZERO hits. Do NOT read `inv_sales_fulfilment.status` into fg-stock; depletion stays on `inv_sales_orders` membership.
- **EQUIP 2A: `sql + coder + ui + webapp-testing`** (CONFIRMED per matrix; webapp-testing smoke READ-ONLY on prod). New tour card? `inv_sales_fulfilment` adds NO `ref_pages` row (same Expéditions surface) → no tour-steward dispatch needed.

## ✅ FULL SHOPIFY⇄MALTYWEB FULFILMENT BUILD COMPLETE — autonomous run 2026-06-11 (Kouros away; reviews on return)
All phases SHIPPED+LIVE+COMMITTED on respective `main` branches + deployed. Recorded as-built below.

### Phase 0 — shipping-method classification + ingest (maltyweb mig **321**; maltytask ingest `bac5501`)
- `ref_shipping_methods` (16 rows) + `inv_sales_orders.shipping_method_title` / `fulfilment_mode`. Ingest now captures `shipping_lines`, classifies pickup/delivery/review, **line-idempotency bug FIXED** (the §INGEST LINE-IDEMPOTENCY BUG above — pre-existing order now re-evals lines + UPDATEs mutable header), `--until=now` default.
- **LIVE BACKFILL: May+June ingested → 3127 orders total; FG delta −5 units (corrective, recorded).**
- **Distribution: delivery 2587 / pickup 488 / review 66** (review = mostly null-shipping-title orders).
- **15-min ingest cron ARMED — on the LOCAL WSL box over the SSH tunnel (13306), NOT the VPS.** ⚠️ NOT robust (depends on box being on + tunnel up). **FOLLOW-UP RECOMMENDED: VPS-host the ingest.**
- ⚠️ maltytask `bac5501` cosmetically bundled the ingest with 3 unrelated logtrack files from a parallel session (couldn't cleanly split on a concurrently-committed branch; harmless, local).

### Phase 1 — per-order eshop rows (maltytask `e7962ce`)
Per-order eshop rows on Commandes board (customer, SKU pills, 🏬/🚚/⚠ badge), read-only, **NO `ord_orders` twins, FG untouched** (XOR preserved). Same commit also carries the earlier-session cross-date search + wider/larger layout.

### Phase 2A — internal fulfilment tracking, LIVE (maltyweb `5508be1`, mig **322**)
`inv_sales_fulfilment` + `inv_sales_fulfilment_events` side tables; `eshop-fulfilment-status.php` API (login+csrf, mode-aware lifecycle delivery→fulfilled / pickup→picked_up, **review REFUSES advance**, push-trigger sets `shopify_sync_state=pending`, **NO inline Shopify call**); action chips + sync badge on eshop rows; new `eshop-fulfilment.css`/`.js` (dodged the expeditions.css collision per plan). **XOR preserved, fg-stock untouched, fixtures self-cleaned.** Team can mark préparé/prêt/remis NOW with zero Shopify dependency.

### Phase 2B — write-back worker, BUILT + DISARMED (maltytask `a08a718`)
`push-shopify-fulfilment.ts` write-back worker — **DISARMED** (dry-run default, no `--apply` scheduled, no cron) + **DOUBLE-GATED**: token has NO write scopes (GET 403 confirmed). 3 idempotency gates, retry cap, audit. Reverse-reconcile in ingest = INNER JOIN on `inv_sales_fulfilment` (only already-tracked orders, advance-only, never bulk-backfills) → structural no-op until team uses the workflow.
- **🔑 2B ACTIVATION (gated, for Kouros):** team grants `write_merchant_managed_fulfillment_orders` + `write_fulfillments`, reissues the Shopify token, verifies one order with `--apply`, THEN arms a cron. The internal 2A tracking works NOW without this.

### Phase 3c — `is_non_stock` flag, NO-OP infra (maltyweb `f0c57de`, mig **323**)
`ref_skus.is_non_stock` flag + symmetric `JOIN ref_skus rs … AND rs.is_non_stock=0` in BOTH `fg_stock_compute()` AND `fg_stock_location_snapshot()` eshop legs. **Verified NO-OP** (FG total 8003=8003, per-SKU byte-identical; 0 SKUs flagged) — infra ready for T7 tap machines (the §② equipment-must-not-deplete ruling, now at SKU-config level).

### DEFERRED (gated, documented for Kouros)
- **Phase 2B activation** — see 2B gate above.
- **Phase 3a/3b** (review-title self-service admin + pickup/delivery lane split) — gated on team standardizing Shopify shipping-method names (in progress).
- **Phase 4** (cage/box COGS engine) — out of scope, gated on 2026-06-15 cage anchor (separate arc).

## 🆕 SHOPIFY FULL-AUTO TRACKING-STATUS slice (RE2, read-only Shopify→maltyweb) — PM-BRIEFED 2026-06-22, NOT built
> Trigger "full-auto tracking"/"shopify auto-advance"/"fulfillment_status reconcile"/"packing slip"/"shopify_reconcile"/"fulfillments[]"/"tracking_number"/"reverse reconcile"/"auto-avancement shopify" → read THIS block.
Kouros wants the eshop tracker AUTO-advanced from Shopify (operators print Swiss Post packing slips ON Shopify → order flips `fulfillment_status='fulfilled'`). **The symmetric INVERSE of B2B** (B2B = operator-piloted, auto-advance OFF mig 433; Shopify = full-auto). **NOT blocked by write-scopes** — pure read Shopify→maltyweb (the read-only token suffices); opposite of 2B which awaits `write_fulfillments`.
**VERIFIED GROUND TRUTH (live, 2026-06-22):**
- Ingest field-set (L1085-90) ALREADY fetches the SCALAR `fulfillment_status` → written + UPDATE-refreshed on `inv_sales_orders.fulfillment_status` VARCHAR(32). **Distribution: fulfilled 3488 / (null) 170 / restocked 5 / partial 3** — the packing-slip→fulfilled signal is already half-captured in the canonical store, just not wired to the tracker.
- The `fulfillments[]` ARRAY (tracking_number/url/company, fulfilled_at, shipment_status) is NOT fetched (absent from field-set + `ShopifyOrder` type). → R-0 extension.
- Tracker statut workflow lives in side-table `inv_sales_fulfilment.status` + `_events` (mig 322); **0 rows today** (manual workflow unused → clean greenfield for auto-advance). Lifecycle: delivery new→picking→picked→**fulfilled**(Expédié); pickup …→ready_for_pickup→**picked_up**(Remis); review REFUSED; cancelled terminal. Single-writer today = PHP `eshop-fulfilment-status.php`. `_events.source` ENUM ALREADY has `'shopify_reconcile'`. The existing TS reverse-reconcile branch (L808-818) is scoped "only orders that ALREADY have a fulfilment row" → no-op (0 rows).
- `inv_sales_orders.fulfilment_mode_source` ENUM('auto','manual') EXISTS — ingest already honours a manual mode override; replicate this discipline for status.
**SLICING (PM rec):** R-0 capture (maltytask field-set + `fulfilled_at`/`tracking_number` cols on inv_sales_orders, mig ≥434; coder+sql) → R-1 auto-advance (**Option B = single PHP writer reusing eshop-fulfilment-status.php's advance-map**, NOT a 2nd TS writer of the workflow fact; behind `system_settings shopify_auto_advance_fulfillment` flag like mig 433; fulfilled→delivery terminal `fulfilled`; coder+sql+webapp-testing) → R-2 optional pickup-mapping + tracking-number display (gated on Kouros decision; ui+coder+sql).
**OPEN DECISION for Kouros (don't guess):** does fulfilled-Shopify on a PICKUP order advance to `ready_for_pickup` or do nothing (retrait = physical operator act)? Only mapping question open.
**INVARIANTS:** advance-only + idempotent (re-run = no-op); review-mode REFUSES advance (42 orders); honour operator override (Shopify advances forward, NEVER reverts an operator act); XOR (no ord_orders rows); NO fg-stock filter added (zero depletion risk); flag fail-safe (`_setting_bool` pattern). Mig head live = 433, next free ≥434 — re-`--status` at build.

### 🔴 NEWLY-SURFACED OPEN DATA-INTEGRITY ISSUE (NOT caused by this work — separate from the Shopify arc)
**Σcards ≠ Σphysique is ALREADY BROKEN by ~22 units on the FG stock page:** `fg_stock_location_snapshot()` sums to **7981** vs `fg_stock_compute()` **8003** (delta 22). git-stash CONFIRMED the gap PREDATES the Phase-3c edit (existed on old VPS code too). PM's standing invariant "Σcards==Σphysique holds" currently does NOT hold. Likely introduced by a recent parallel-session change to one of the two functions (or a same-day-tiebreak drift). **OPEN — needs investigation; recorded for Kouros as a standalone item.** (The §C fg_stock_location_snapshot sales-attribution carryover ~L371 is the prime suspect surface — re-derive both legs and diff.)

### Operational caveats (for the review)
- Phase-0 ingest cron on LOCAL box over tunnel = not robust → recommend VPS-hosting (above).
- maltytask `bac5501` bundled 3 foreign logtrack files (harmless, local).
- **Parallel sessions actively committing to BOTH repos' main branches.** The build did git history surgery once (a reset that briefly undid a parallel commit; restored it). **LESSON (reinforces CHECKLIST): only `git add <my-files> && commit` on these shared branches, NEVER reset.**

### 📋 KOUROS REVIEW SUMMARY — PM flags to raise
1. **OPEN data-integrity bug (top priority):** Σcards 7981 ≠ Σphysique 8003 (Δ22), predates this work — assign investigation; PM's invariant is currently violated.
2. **2B activation needs operator action** (Shopify write-scopes + token reissue) before any live customer-facing write — correctly disarmed until then.
3. **Phase-0 ingest cron is on the LOCAL box** — single point of failure; VPS-host as follow-up (consistent with strangler "consume the host" + maltyweb-prefers-live).
4. **Phase 3a/3b gated on Shopify shipping-name standardization** (team in progress) — 66 orders sit in `review` until then.
5. **Shared-tree commit hygiene held** (file-scoped adds; one reset incident caught + restored) — separate-worktrees-per-session recommendation stands.
