# Fulfilment — State-machine automation, eshop review-mode classification, izyrent fulfilment-date, 502 fix

## 🆕 « COMMANDE NON CHARGÉE / REMISE EN STOCK » — PM RULED 2026-06-30 (pre-build; B2B slice 1 zero-mig; eshop slice 2 deferred)
Operator ask: on Expéditions (then Boutique en ligne) surface orders stuck because the client never picked up / loaded; past a threshold (≈7 jours ouvrés) the operator clicks → cancel → release stock back into PF.
**THE LOAD-BEARING ASYMMETRY (live-verified `app/fg-stock.php`):**
- **B2B (`ord_orders`/Expéditions) deplete on the `shipped` EVENT (Step 4, `status='shipped'`, dispatch ts). An un-loaded order (entered/confirmed/picked/bl_printed) is RESERVED only — Step 7 `WHERE o.status NOT IN ('shipped','cancelled')` → `open_total_qty` → `live_futur=physique−open_total` (free-to-promise/ATP). It is NOT depleted.** ⇒ "release stock" = release the RESERVATION, which is AUTOMATIC the moment `status='cancelled'` (Step 7 excludes cancelled). NO restock leg, NO credit-back, NO `ord_returns` row. physique is UNCHANGED by the cancel (it was never debited); free-to-promise rises by exactly the cancelled qty. The existing `cancel` action in `public/api/expeditions-status.php` (from any non-shipped state, terminal, writes event+cache+log_revision) ALREADY does the release.
- **Eshop (`inv_sales_orders`/Boutique) deplete on PLACEMENT — `app/fg-stock.php` Step 5 (L696) + snapshot Leg 2 (L1670): `WHERE channel='eshop' AND DATE(created_at) >= anchor`, NO status filter.** ⇒ a paid/placed eshop order has ALREADY debited physique; cancelling needs either (a) add a cancelled-exclusion to BOTH eshop legs (symmetric, no credit leg, but COGS-path touch — MUST preserve Σcards==Σphysique) or (b) a credit-back leg. Plus eshop is PAID at placement → cancel = refund → entangles Shopify writeback (blocked on write scopes) + possibly the returns ledger. ⇒ HARDER; this is exactly why operator said Expéditions FIRST.
**RULINGS:** (1) **Aging basis = working-days since `ord_orders.requested_date`** (the promised mise-à-disposition date), scoped to `status IN (confirmed,picked,bl_printed)` (exclude `entered`=draft, `shipped/cancelled`); show the `bl_printed/picked` event `occurred_at` as "préparée depuis" context. 🔴 FLAG: `requested_date ← BC Posting_Date` for BC-sourced orders (state-machine §#4) = an ACCOUNTING date, may not be the pickup promise — CONFIRM with operator; fallback basis = latest ready-state event `occurred_at`. (2) **Working-days = REUSE `system_setting('workday_<mon..sun>','production_targets', default mon-fri)`** (the canonical source `app/planning-predict.php` Step 6 reads) via a NEW shared helper `fulfilment_working_days_between()` — call-not-copy. NO Vaud/Swiss holiday calendar exists → v1 weekday-only; holidays = documented v2 gap. (3) **Threshold = `system_settings` (section 'fulfilment', `non_loaded_threshold_working_days` default 7)** — data row, NO mig; expose in Données générales later. (4) **Cancel reason = reuse the event `comment` marker** (`comment='annulation:non-chargée'`, like `auto:bc-shipped`/`email:confirmation`) — NO mig; a first-class `ord_orders.cancel_reason` col only if a KPI needs it later. (5) **Surface = new `?view=retards` (Retards / Non chargées) on `expeditions.php`** — pure DERIVED read (owns no table): list non-shipped committed orders sorted by jours-ouvrés-late desc, days-late + last-ready-event + open lines, gated «Annuler — non chargée» button (reuse the existing cancel action + reason marker) showing "X unités relibérées en stock PF"; + a late-count badge on the Commandes board. (6) **BC writeback = NONE v1** (maltyweb-internal; BC sales order stays open in BC — reconciliation follow-up; the `maltytask-bc-orders` auto-advance cron is strict-monotonic and RESPECTS cancelled, won't re-advance).
**DIVERGENCE FLAGS:** do NOT route B2B non-chargée through `ord_returns`/disposition queue (that's POST-shipment physical returns valued vs the ledger — wrong fact). Do NOT add a B2B FG credit/restock leg (would DOUBLE-release: reservation already released by the cancel). For the eventual eshop slice, the eshop-leg cancelled-exclusion MUST be applied IDENTICALLY to Step 5 + snapshot Leg 2 (Σ-invariant) and is a COGS-sensitive change. SoT-chain: NO (ord_orders status = operational cache, not COGS/Zeppelin). Only canonical fact written = the cancellation event (+reason). **FIRST SLICE = B2B, ZERO migration:** seed threshold setting → working-days helper → `?view=retards` list → cancel+reason graft → smoke (cancel test order ⇒ open_total_qty drops, free-to-promise rises by exact qty, physique UNCHANGED). **EQUIP: coder + sql + ui + webapp-testing.** Trigger "non chargée"/"commande non chargée"/"remise en stock"/"retards expédition"/"client ne charge pas"/"pas récupéré"/"annuler libérer stock"/"jours ouvrés seuil"/"view=retards"/"non_loaded_threshold" → this section.

## 🐞 NATHAN BUG « clic Confirmé → saute jusqu'à Livré » — CADRAGE 2026-06-22 (cause NON départagée; fix NON lancé)
Symptôme rapporté : sur Expéditions, cliquer « Confirmé » avancerait jusqu'à « Livré » au lieu de s'arrêter. **CONSTAT (lecture exhaustive du déployé VPS): AUCUN chemin d'avance ne fait de cascade — tous strictement mono-cran, serveur ET client.** Vérifié sur les 4 surfaces :
- `ord_orders` handler `public/api/expeditions-status.php`: `advance` = `$advanceMap[$currentStatus]` (UN cran, L141-152/267), refuse > shipped, **ignore le `data-status` des chips**. Zéro boucle (grep while/for/do = vide). Graft `entered→confirmed` L287 = mail uniquement, ne touche pas au statut.
- Render `expeditions.php` `exp_render_chips()` L3516-3548: seule la puce `$isNext` est cliquable, les autres `disabled`. Bande de progression COMPLÈTE affichée (Confirmée·Préparée·BL·Livrée).
- JS `expeditions.js`: `postStatus` (1 fetch/clic), `applyStatusToRow` L67-110 recalcule `isDone=newRank>=stRank` depuis le statut mono-cran renvoyé — pas de cascade. `STATUS_RANK` L22 mirror exact du PHP `EXP_STATUS_RANK`.
- Eshop `eshop-fulfilment-status.php` advance L239 = `$advMap[$curStatus]` mono-cran aussi (labels Nouveau→…→Expédié/Remis, masculins ≠ ord_orders).
- ENUM canonique ord_orders (cache; vérité = `ord_order_status_events` append-only): entered(Saisie)→confirmed(**Confirmée**)→picked(Préparée)→bl_printed(BL imprimé)→shipped(**Livrée**)+cancelled. Constantes `expeditions.php` L54-81.
**→ Le code d'avance est SAIN, NE PAS le « corriger ».** Hypothèses de cause réelle, à départager AVANT tout fix (demander à Nathan: quelle VUE + n° commande, puis lire `ord_orders.status` + ses `ord_order_status_events` en RO):
1. (PROBABLE) **Méprise visuelle** — la bande affiche toutes les étapes futures; contraste done/next/futur trop faible → lue comme « tout franchi ». Si status=confirmed SANS events au-delà = pur affichage → fix UI `expeditions.css`/`exp_render_chips` (EQUIP ui+webapp-testing; piège visuel invisible en revue statique).
2. **Auto-avance BC** (#4 `e82689c`): cron `maltytask-bc-orders` (*/min, ACTIF) avance auto entered/confirmed/picked→**bl_printed** sur BC Completely_Shipped=True (s'arrête à bl_printed, PAS shipped); event `comment='auto:bc-shipped'`. Si présent → pas un bug, manque de lisibilité auto-vs-clic (reco existante: badge dérivé du comment).
3. Drift déployé/surface eshop — exclure en confirmant vue+type d'ordre.
DIVERGENCE-FLAG pour tout fix: ne jamais écrire un statut sans event `ord_order_status_events` (cache≠vérité), ne pas court-circuiter log_revision/txn unique, ne JAMAIS pousser à `shipped` par mégarde (arme la dépletion FG, `fg-stock.php` Step 4). SOT-chain: NO (status=cache opérationnel). Trigger "Nathan bug confirmé"/"saute jusqu'à livré"/"avancement cascade"/"clic Confirmé"/"step-by-step expédition" → cette section.


## ✅ EXPÉDITIONS 502 BAD GATEWAY (needs_review warning-flood → oversized FastCGI header) — FIXED 2026-06-19 (`c73a362` maltyweb main, NOT pushed — awaiting Kouros's push decision)
The Expéditions page was returning **502 Bad Gateway**. RESOLVED.
- **ROOT CAUSE:** the customers typeahead `SELECT` (`public/modules/expeditions.php` ~L1744) OMITTED `ref_customers.needs_review`, but the `array_map` that builds the typeahead list (~L2793–2804) READ that key TWICE per row → thousands of `Undefined array key "needs_review"` PHP-8 warnings per request → the warning text overran nginx's default 4 KB `fastcgi_buffer_size` → upstream sent a too-big response header → **502**. The same omission also silently broke the "needs_review rows sort last" typeahead ranking (the SOT fact was dropped).
- **FIX (3 parts, all PM-approved):** (1) added `needs_review` to the SELECT (~L1745) — restores the dropped SOT column + the broken ranking; (2) `(bool)($c['needs_review'] ?? 0)` guards on BOTH array_map reads (~L2802–2803) — permanent flood-proofing; (3) nginx `app.maltytask.ch` PHP location block bumped `fastcgi_buffer_size 32k; fastcgi_buffers 16 16k; fastcgi_busy_buffers_size 64k;` (in-place on VPS, **NOT repo-tracked**). `nginx -t` passed + reloaded.
- **VERIFICATION:** `php8.1 -l` clean (local + VPS); `nginx -t` OK; HTTP now 302→login (no 502); **discriminator confirmed** — the `needs_review` warnings were present in the error log PRE-deploy and ABSENT post-reload (proves the SELECT fix landed, not merely the buffer net masking it); reviewer confirmed the file's two OTHER `needs_review` reads (~L6678 `$reviewRows`, ~L6908 `$clientsRows`) already SELECT the column — no other gaps.
- **DURABLE LESSON:** a `SELECT` that omits a column the PHP later reads → PHP-8 `Undefined array key` warning FLOOD → oversized FastCGI response header → **502** (not an obvious "missing data" bug; presents as a gateway error). Keep php-fpm warnings VISIBLE — do NOT let the larger FastCGI buffers silently absorb a future warning flood; the buffer bump is a safety net, the SELECT/guard fix is the cure. (See OPEN-ITEM #5 of the seasonal-burn build above — this is the resolution of the note that was mis-attributed to a parallel session.)
- **STATE:** committed `c73a362` on maltyweb `main`, **NOT pushed** (awaiting Kouros). nginx change is on the VPS only (not repo-tracked).


## 🆕 SHOPIFY FULFILMENT-DATE (izyrent rentals) — PM RULED 2026-06-17 (build pending Sonnet)
Operator-urgent: the eshop "Boutique en ligne" board groups by `DATE(iso.created_at)` (= order-placement), useless for "what to prep this week". Kouros's raw-data investigation (verified, no guess): the ONLY orders with a real future fulfilment date are **izyrent portable-tap rentals** (10 in his 250-order window; PM live-probed 19 since 2026-05-01, all `tags LIKE '%izyrent%'`, all currently `fulfilment_mode='review'` except #12968=pickup). Date lives in a Shopify **line-item property `"Date"`** = FR date-range string `"{D} {Mois} {YYYY} au {D} {Mois} {YYYY}"` (Juin/Juillet/Août/Septembre/Décembre seen). Regular retrait-boutique pickups + plain deliveries carry NO date (genuinely no schedule). **PM VERDICT:**
- **(1) SCHEMA — APPROVED order-level.** `fulfilment_date DATE NULL` + `fulfilment_date_end DATE NULL` on `inv_sales_orders` (one rental period per order; board groups orders; lines carry no per-line date). NOT on `inv_sales_order_lines`. `start`=day-operator-must-have-it-ready (the board key), `end`=display range. NULL for every non-dated order.
- **(2) INGEST — APPROVED, parse best-effort.** Add `properties` to `ShopifyLineItem`; parse the `"Date"` prop via FR-month map Janvier..Décembre → start+end. Parse-fail OR no-Date-prop → leave NULL, NEVER crash, NEVER guess a date. **created_at + period STAY AS-IS** (fiscal/COGS/sales period = placement month — agreed; fulfilment_date is OPERATIONAL-display only).
- **(3) BOARD — group-by-fulfilment-date with COALESCE fallback.** Dated (izyrent) orders surface on `COALESCE(fulfilment_date, DATE(created_at))` so they land in the right prep-week; non-dated orders unchanged (fall through to created_at). Add a prominent date-range badge ("Tireuse 18→19 juin") on dated cards. This fits the existing `DATE()`-group structure with one COALESCE swap in the eshop query's group/sort key (expeditions.php ~L1931/2015/2024) — cleaner than a parallel badge-and-sort path.
- **(4) BACKFILL — re-run ingest, BUT one-line guard needed.** The header-upsert is idempotent on `(source,external_order_id)` BUT the existing-row UPDATE path (ingest L486-525) only re-syncs `fulfillment_status/shipping_method_title/fulfilment_mode` — a naive re-run will NOT fill `fulfilment_date` on the 19 existing rows. **MUST add `fulfilment_date`/`_end` to the `existing` SELECT + `mutableChanged` test + UPDATE SET**, then re-run over history fills them. DIVERGENCE FLAG: if that's skipped, only NEW orders get dates → silent split (old izyrent dateless, new dated). No separate backfill script.
- **(5) SCOPE — date-ONLY here; classification stays in task-3.** Do NOT touch `fulfilment_mode` in this slice (leave the 18 izyrent at 'review'). Auto-classifying izyrent→pickup belongs to the queued "review orders" task. Keep this slice tight: schema + parse + board key + backfill.
- **SOT-CHAIN: NO.** `inv_sales_orders` is `schema_meta table_class='source'`, `corrections_policy='blocked_with_redirect'` (raw API pull, "fix in source eshop, re-pull"). `fulfilment_date` is a DERIVED-at-ingest parse of raw API data (same status as created_at/period/fulfilment_mode) — purely an OPERATIONAL DISPLAY attribute, NOT part of the Le Zeppelin commissioning→BOM→COGS chain. The downstream eshop-consumption deriver keys on SKU LINES, not order dates → fulfilment_date never touches COP/COGS/WAC/beer-tax. Because the table is blocked_with_redirect, the column must ONLY ever be re-derived by the ingest, NEVER hand-edited.
- **MIG-NUMBER CAVEAT: parallel sessions live — `migrate.php --status` + `ls db/migrations` IMMEDIATELY before assigning** (index banner cites HEAD 391/392; re-probe, don't trust). MySQL 8: `ALTER TABLE inv_sales_orders ADD COLUMN fulfilment_date DATE NULL, ADD COLUMN fulfilment_date_end DATE NULL;` (no IF NOT EXISTS). No new table → no schema_meta row; bump schema_meta.updated_at note on inv_sales_orders if desired. RULE 3 tour: N/A (no ref_pages change). RULE 1 scrap: nothing superseded.
- **EQUIP: coder + sql + ui (+ webapp-testing smoke of the eshop board).** Ingest TS parse = coder; migration = sql; board COALESCE+badge = ui.

## ✅ ESHOP REVIEW-MODE CLASSIFICATION — SHIPPED + LIVE 2026-06-17 (the "task-3" below, built per PM ruling; NOT yet pushed — orchestrator sequences)
Operator classification of "à classer" (review-mode) eshop orders on the `Expéditions ?view=shopify` board is LIVE. Built exactly to the PM verdict (next section). What landed:
1. **mig 395 APPLIED** (maltyweb `4d57761`): `inv_sales_orders.fulfilment_mode_source ENUM('auto','manual') NOT NULL DEFAULT 'auto'` AFTER fulfilment_mode — provenance flag protecting human/auto classifications from the Shopify ingest re-sync. (No new table → no schema_meta row, per verdict.)
2. **izyrent backfill APPLIED** (one-off UPDATE, NOT a re-run): 63 izyrent review orders → `fulfilment_mode='pickup'`, `source` STAYS 'auto' (deterministically re-derivable). Eshop dist went 2637 del / 506 pick / 72 review → **2638 del / 569 pick / 9 review**.
3. **Ingest classifier + skip-guard** (maltytask `2fb0f55`, deployed to `/opt/maltytask-pipeline`): izyrent→pickup auto-classify in `scripts/ingest-shopify-orders.ts`; existing-row UPDATE leg now EXCLUDES `fulfilment_mode` from change-detection (`mutableChanged`) AND from the SET when `fulfilment_mode_source='manual'` (via `modeIsManual`/`modeToWrite`); `fulfilment_date` + other mutable fields keep syncing. Ingest NEVER writes `fulfilment_mode_source` (always defaults 'auto'); only the classify action sets 'manual'. (Matches verdict-3 exactly.)
4. **Classify action** (maltyweb `b6b1c88`): `public/api/eshop-fulfilment-status.php` `action='classify'` — whitelist `mode∈{pickup,delivery}`, REVIEW-ONLY, ONE txn `UPDATE fulfilment_mode + fulfilment_mode_source='manual'` + `log_revision` audit. **Note: `inv_sales_orders` has NO `updated_by` column (confirmed) → omitted.** (Extended the EXISTING api file per verdict-4, not a new file / not expeditions.php.)
5. **UI buttons + JS** (maltyweb `ad60efe`): `exp_render_eshop_chips` review branch renders 🏬 Retrait / 🚚 Livraison `.ef-chip--classify` buttons in a `.ef-chips` container; `public/js/eshop-fulfilment.js` `postClassify()` POSTs `{action:classify,mode}` with CSRF-retry-once, reloads on success → card re-renders with full pickup/delivery workflow chips. CSS tokens-only in `public/css/eshop-fulfilment.css`. (Review-only buttons per verdict-5; general re-classify still DEFERRED.)
**Verification:** skip-guard proven DETERMINISTICALLY (verbatim-shipped-logic harness, self-cleaned): manual+no-other-change → mutableChanged=false (no revert); manual+other-field-changed → UPDATE fires but `modeToWrite` preserves human mode; auto control → reverts (guard off). Live API dry-run couldn't exercise it (cron keeps DB in sync → pull returns no header changes for in-window orders). curl: `?view=shopify`→302, classify endpoint→403 auth-first (no fatal); no PHP Fatal in nginx error log. Browser smoke blocked by WSL→app TLS gotcha (CONNECT proxy denied by classifier) — DB-layer equivalent done per brief fallback.
**Commits NOT pushed** (orchestrator sequences pushes): maltyweb `4d57761`/`b6b1c88`/`ad60efe`, maltytask `2fb0f55`. Parallel session's `app/fg-stock.php` + financier `50d30ed` kept OUT of all pathspec commits.
**DEFERRED (unchanged):** general "corriger le mode" (pickup↔delivery on already-workflowed orders) — gate on `status='new'`/no-fulfilment-row, its own slice.

## 🆕 ESHOP REVIEW-MODE CLASSIFICATION — PM RULED 2026-06-17 (✅ BUILT — see SHIPPED section above; this is the original verdict, kept for traceability)
Classify the 72 `inv_sales_orders.fulfilment_mode='review'` eshop orders (63 are izyrent — empty shipping_lines → fell to review). Live counts PM-verified 2026-06-17: delivery 2637 / pickup 506 / review 72 (review izyrent=63). Trap: ingest UPDATE leg (`ingest-shopify-orders.ts` L604-618) UNCONDITIONALLY re-syncs `fulfilment_mode` from the classifier on every `mutableChanged` run → a human-set mode on an empty-shipping-lines order gets re-classified to 'review' and CLOBBERED. **PM VERDICT:**
- **(1) GUARD SCHEMA = `_source` ENUM, not `_locked` bool.** `ALTER TABLE inv_sales_orders ADD COLUMN fulfilment_mode_source ENUM('auto','manual') NOT NULL DEFAULT 'auto' AFTER fulfilment_mode;`. Encodes PROVENANCE (who decided the fact), house convention (mirrors count_type/line_type provenance enums + supplier-fiche auto/vérifié), debuggable. Skip-condition reads `… AND fulfilment_mode_source='auto'`. Additive DEFAULT 'auto' backfills all 3215 rows correctly (all were classifier-derived). No new table → NO schema_meta row. MySQL 8 no IF NOT EXISTS. Re-probe `migrate.php --status` + `ls db/migrations` IMMEDIATELY (parallel sessions; HEAD≈392, number leads).
- **(2) izyrent→pickup IN THE CLASSIFIER** (`buildOrderRow` ~L399-402): tags contain izyrent (TOKEN-aware, not bare substring) ⇒ force `fulfilmentMode='pickup'` regardless of shipping_lines. **source stays 'auto'** (re-derivable — must NOT be marked manual). **Backfill = ONE-OFF UPDATE, NOT a re-run** (re-run only touches orders in the sync window → unpredictable subset, mixed state). `UPDATE inv_sales_orders SET fulfilment_mode='pickup', fulfilment_mode_source='auto' WHERE fulfilment_mode='review' AND tags LIKE '%izyrent%'` (dry-count expect 63, then apply, AFTER the column exists).
- **(3) SKIP-GUARD in ingest UPDATE leg.** Add `fulfilment_mode_source` to the existing-row SELECT (L575). Compute `effectiveMode = existing.source==='manual' ? existing.fulfilment_mode : order.fulfilment_mode`, bind that; EXCLUDE `fulfilment_mode` from `mutableChanged` when source=manual (else no-op UPDATE churn + misleading audit row every run). Ingest NEVER writes `fulfilment_mode_source` (only the button does). Other mutable fields (status/dates) still re-sync on manual rows.
- **(4) MANUAL CLASSIFY HANDLER = extend EXISTING `public/api/eshop-fulfilment-status.php` with `action:'classify'`** — NOT a new api/ file, NOT expeditions.php POST handler (that's ord_orders/Saisie, wrong table + contention hotspot). The api endpoint already is the canonical eshop-order JSON mutator (CSRF→auth→txn→log_revision spine proven) AND already REFUSES review mode ("classer le mode d'abord") — classify is the door OUT of review, same file, same fulfilment_mode. Payload `{csrf, eshop_order_id, action:'classify', mode}`; server WHITELIST `mode∈{pickup,delivery}` (REFUSE 'review' from button) + eshop/shopify-source; `UPDATE … SET fulfilment_mode=?, fulfilment_mode_source='manual'`; `log_revision` on inv_sales_orders (before/after mode+source) in ONE txn (audit_row_revisions written BY log_revision — no hand-rolled 2nd path).
- **(5) UI = review-only buttons.** Two buttons "Retrait"/"Livraison" render ONLY on `fulfilment_mode='review'` cards (`expeditions.php` eshop card + `eshop-fulfilment.js` POST). General re-classify (fix a wrong pickup→delivery) DEFERRED — hazard: re-classifying an already-workflowed order desyncs the mode-specific advance-maps; review cards are pre-workflow (review refused entry) so classify-from-review is always safe. Future general "corriger le mode" must gate on status='new'/no-fulfilment-row — its own slice, logged as follow-up.
- **DERIVATION-TREE: SAFE.** `fulfilment_mode` feeds the board pickup/delivery split (presentational) + the eshop lifecycle (review refused entry). FG-depletion arms on `status='shipped'`/lifecycle events, NOT on `fulfilment_mode` directly (mode only selects WHICH lifecycle). NO COGS/WAC/beer-tax surface reads fulfilment_mode_source. A human-classified mode is STRICTLY BETTER data than stuck-in-review (which refuses workflow). The lock protects a human truth from being overwritten by an absence-of-signal. SOT-chain: NO (same status as fulfilment_date — operational display, blocked_with_redirect table; but unlike date, mode IS now intentionally human-writable via the audited button — that's the design, NOT a hand-edit of raw data).
- **BUILD ORDER (gating):** migration (add column) → izyrent one-off UPDATE → classifier change + skip-guard SHIP TOGETHER in the same ingest deploy (never leave the clobbering UPDATE leg live before the guard) → classify action + UI. RULE 2 review over combined diff. RULE 3 N/A (no ref_pages). RULE 1: nothing superseded. **Load-bearing smoke (the proof): classify a review order → re-run ingest dry-run → confirm NOT reverted.**
- **EQUIP: sql + coder + ui + webapp-testing.** Migration+UPDATE+SELECT/UPDATE-guard = sql; classifier TS + classify action PHP = coder; buttons = ui; survives-re-ingest smoke = webapp-testing. Files: `db/migrations/NNN_inv_sales_orders_fulfilment_mode_source.sql` (new), `maltytask/scripts/ingest-shopify-orders.ts` (classifier L399 + SELECT L575 + UPDATE leg L597-618), `public/api/eshop-fulfilment-status.php` (new classify action), `public/modules/expeditions.php` (review-card buttons), `public/js/eshop-fulfilment.js`.
- Trigger "classer"/"review orders"/"à classer"/"fulfilment_mode review"/"izyrent classify"/"fulfilment_mode_source"/"Retrait/Livraison buttons"/"classify eshop" → this section.

## ✅ THREE EXPÉDITIONS-CARD UPGRADES SHIPPED 2026-06-19 (`971471c` maltyweb main — committed; doc-externe + per-order comments + single BL-imprimé tag)
Three operator/PM asks built (Sonnet agents), verified, deployed SURGICALLY (per-file rsync; parallel KPI-recap session's dirty tree untouched), committed by pathspec as `971471c`. 5 code files all md5-match local↔VPS; both PHP files `php8.1 -l` clean on VPS; php-fpm reloaded clean. No browser smoke (Tailscale+auth-gated; operator eyeballs in-session).

### #1 — Doc externe display (PM ask #1) — LIVE
- **Mig 400** `db/migrations/400_ord_external_document_no.sql` = `ALTER TABLE ord_orders ADD COLUMN external_document_no VARCHAR(50) NULL`. 🔴 **Applied SURGICALLY** (manual ALTER + manual `INSERT INTO schema_migrations`) because `migrate.php` applies ALL pending and **397 is STILL pending** (parallel-session WIP) — 397 was NOT applied and remains pending. MIG HEAD = 400 applied; 397 pending; next free = 401.
- **Connector:** `External_Document_No` added to the OData `$select`, the order dict, and persisted via `write_bc_mirror_fields` (EVERY-pull mirror → stays in sync post-close). NOT in `push_bc_sales_orders.py` (push-reservation honoured; read-only back from BC).
- **Pull LIVE:** `maltytask-bc-orders` cron (every minute); deployed connector ran 07:41:03 and populated `external_document_no` for **13 of 61** BC orders with real values (e.g. "ROMANEL / 69514", "BAR DU STAFF", "STREET STAGE", "WARM UP"). Clean run, no tracebacks.
- **Render:** "Doc externe : <value>" pill on the card, reusing `exp-transporter-tag` class (NO new CSS), shown only when non-empty.

### #2 — Per-order comments (PM ask #2) — LIVE
- New **`set_comment`** action on `public/api/expeditions-status.php`: ONE txn = UPDATE `ord_orders.comment` (empty→NULL) + INSERT `ord_order_status_events` (status UNCHANGED, comment="Commentaire modifié", cols order_id_fk/status/occurred_at/user_id_fk/comment) + `log_revision` (8-arg for the ord_orders update, 7-arg insert for the event). REUSED `ord_orders.comment` — NO new table, NO migration.
- **UI:** inline ✎ edit button + inline textarea editor on every non-cancelled card (gated `can_write_expeditions()`); CSRF auto-retry reuses the existing `postStatus` machinery; in-place DOM update on success.
- 🔴 **SECURITY HOLE CLOSED:** `expeditions-status.php` previously gated on AUTHENTICATION ONLY. Added `can_write_expeditions($me)` → 403 covering ALL branches (advance/cancel/revert AND set_comment) — the pre-existing advance/cancel/revert branches are now write-gated too, not just the new action.

### #3 — Single "BL imprimé" tag (operator's 3rd ask) — LIVE
- Removed the left `exp-bc-shipped-badge` block (`expeditions.php`) + its dead `.exp-bc-shipped-badge` CSS. Divergence badge `.exp-bc-divergence-badge` UNTOUCHED; `bc_completely_shipped` column + connector auto-advance (#4) UNTOUCHED. The operational `bl_printed` chip is now the SINGLE, BC-automated tag.
- DID NOT close the one-pull insert-lag (PM Q2 recommendation followed — deferred; zero orders stuck).

**Deferred follow-ups:** (a) connector one-pull insert-lag close (two-pass insert) if ever wanted; (b) browser smoke of the comment editor on a real session.
**Trigger** "doc externe"/"external_document_no"/"External_Document_No"/"per-order comment"/"set_comment"/"commentaire"/"BL imprimé tag"/"exp-bc-shipped-badge"/"can_write_expeditions gate" → this section.

## 🆕 STATE-MACHINE AUTOMATION — #4 DELIVERED; #2 RE-PIVOT #2 FINAL → MAIL-ON-CONFIRM-CLICK, From=commandes@lanebuleuse.ch via Google relay (design RATIFIED 2026-06-17, build disarmed/not-launched)
### §CHANTIER #2 CONFIRMATION MAIL FINAL — what RE-PIVOT #2 FINAL changed vs the prior record below
🔴 The mail-on-confirm-click skeleton (graft on `advance`, off-txn best-effort, idempotence via event comment, `confirmation_email_mode` flag, recap JOINs) STANDS. RE-PIVOT #2 FINAL (Kouros 2026-06-17) overrides ONLY the **From address + routing** (line 54 below is now SUPERSEDED):
- **From = `commandes@lanebuleuse.ch`, display name «La Nébuleuse», Reply-To = `commandes@lanebuleuse.ch`.** The client must NEVER see maltytask (internal). This SUPERSEDES the prior "From STAYS noreply@maltytask.ch" line — that rationale assumed the VPS couldn't send as @lanebuleuse.ch.
- **ROUTING = Google Workspace SMTP relay + Postfix sender-dependent map.** Kouros activates the Workspace SMTP relay service (`smtp-relay.gmail.com:587`, IP-auth on VPS 83.228.215.243). On the VPS, Postfix sender-dependent relay: `@lanebuleuse.ch → smtp-relay.gmail.com`; the existing IONOS relay stays for `@maltytask.ch` (account mails unchanged). lanebuleuse.ch deliverability READY: Workspace DKIM selector `google` published, SPF includes `_spf.google.com`, DMARC `p=quarantine`. The app-layer just emits From=commandes@lanebuleuse.ch — Postfix does the routing; no per-mail SMTP config in PHP.
- **MAILER signature (FINAL):** add `?string $from=null, ?string $fromName=null, ?string $replyTo=null` to `send_mail()` (all default MAIL_FROM/_NAME → zero regression on the 3 account-mail callers). Confirmation passes the three lanebuleuse.ch overrides.
- **TRACE comment (FINAL):** the `confirmed` `ord_order_status_events` row carries `comment='email:confirmation'` (the idempotency sentinel — query for an existing `status='confirmed' AND comment='email:confirmation'` before sending; present ⇒ re-confirm, advance silently NO resend). Send outcome (recipient list + ok/fail) → `log_revision` on ord_orders. The board badge can still derive mail-state from the event comment. NO `sent_at` column, NO migration.
- **Setting seed:** PM reco = seed `confirmation_email_mode='off'` via idempotent INSERT system_settings (`section='fulfilment'`), expose the off→test→live toggle in Données générales for Kouros to walk it.
- Everything else (graft point, sequence, recap data, EQUIP, RULE 1/2/3) = as recorded below.

### (prior record) #4 DELIVERED; #2 RE-PIVOTED → MAIL-ON-CONFIRM-CLICK
Kouros wants 2 of the 5 `ord_orders` transitions automated, the other 3 stay manual. Target: création→`entered` (already auto); **`entered→confirmed` AUTO** (now via a BC `Status` signal, NOT a mail — pivot below); `picked`=manual; **BC `Completely_Shipped` → `bl_printed` AUTO** (#4, DONE); `shipped`=manual.

### ✅ #4 — AUTO `bl_printed` ON BC `Completely_Shipped` — DELIVERED + DEPLOYED + PUSHED 2026-06-17 (`e82689c` on origin/main; NO migration)
- **`requested_date ← Posting_Date`** (cascade Posting_Date → Document_Date → Shipment_Date); `order_created_date ← Order_Date` UNCHANGED; column renamed `document_date`→`posting_date`. Deployed; backfill dry-run 0-needed (Posting==Document everywhere today).
- **Auto-advance `entered/confirmed/picked → bl_printed`** when BC `Completely_Shipped=True`. Helpers `should_advance_to_bl_printed()` + `advance_status()` in `ingest_bc_sales_orders.py`. Synthetic intermediate events at the same NOW(), `comment='auto:bc-shipped'`, `user_id_fk=NULL`. Strict monotonic (never recedes from shipped/cancelled; ranks entered0/confirmed1/picked2/bl_printed3/shipped4/cancelled5). Idempotent. `bl_printed` is PRE-depletion → **0 impact COGS/FG** (depletion arms on `status='shipped'` EXCLUSIVELY, `fg-stock.php` Step 4 L27/L418 — CARDINAL invariant preserved). 8/8 truth-table verified; SQL exercised live on ORD210077 (legacy entered→bl_printed, outside open-pull set, 3 events created). NO migration.

### 🟢 #2 — `entered→confirmed` — RE-PIVOTED #2 (Kouros re-tranché 2026-06-17): MAIL-ON-CONFIRM-CLICK, design RATIFIED, build NOT YET launched (disarmed)
**RE-PIVOT (supersedes the BC-signal design above):** BC-signal detection is ABANDONED — the BC mail trace is NOT exposed by the API (probe findings below = the durable justification; OData SalesOrder 110 fields + 121 web services expose NO Sent-Email / Interaction-Log table). So the confirmation mail is **built + sent FROM maltytask**, BUT the trigger is the existing manual **« Confirmer » click** — NO separate endpoint, NO separate "send mail" button, NO BC signal. **Mail = side-effect grafted on the `advance` action of `public/api/expeditions-status.php` when the transition is exactly `entered → confirmed`.**

**RATIFIED DESIGN (this is the build brief — disarmed until Kouros validates the rendered mail):**
- **GRAFT POINT:** `public/api/expeditions-status.php`, `advance` branch. After the existing ONE txn commits (`$pdo->commit()`), when `$currentStatus==='entered' && $newStatus==='confirmed' && $action==='advance'`, fire the mail BEST-EFFORT, OUTSIDE the txn. **Sequence RATIFIED: advance the status FIRST (the click is an explicit operator action that must not fail silently), THEN send mail best-effort.** On send failure the status stays `confirmed`; return `email_sent:false` + `email_reason` in the JSON so the board surfaces "mail non parti → renvoi manuel". Do NOT make the status advance conditional on send success (would silently swallow the operator's click on a mailer hiccup).
- **DESARM FLAG:** `system_setting('confirmation_email_mode','fulfilment','off')` ∈ {off|test|live}, DEFAULT off. off = no send (Confirmer just advances, today's behavior); test = send to `system_setting('confirmation_email_test_to','fulfilment', 'kouros@lanebuleuse.ch')`; live = send to the customer email(s). Flip via MySQL at first (`system_settings` row); a Données-générales toggle is a nice-to-have follow-up, NOT a blocker. Read with `system_setting()` (app/settings.php:46, section-cached).
- **DATA (record-mail JOIN, by order_id):** n° = `COALESCE(NULLIF(bc_no,''), NULLIF(source_ref,''), CONCAT('MT-', id))`; customer name + email from `ref_customers` (`email` VARCHAR(255), may hold multiple `;`-separated — split, trim, FILTER_VALIDATE_EMAIL each, send to all valid, skip+flag if none valid); delivery date = `ord_orders.requested_date` (format via `date_display_format()` d/m/Y); lines = `ord_order_lines.qty` + `ref_skus.sku_code` + `ref_skus.unit_label` (désignation), ORDER BY line id.
- **IDEMPOTENCE / re-send:** send ONLY on the genuine `entered→confirmed` transition (1st confirmation). Re-confirm after a revert, or a re-click, does NOT re-send. **Trace = the `ord_order_status_events` row's `comment`** (already the origin-marker channel; NO new column — PM holds the no-`confirmation_email_sent_at`-column line). On the entered→confirmed advance, set `comment` to encode the mail outcome, e.g. `'Avancement … · mail:sent→a@x;b@y'` / `'… · mail:fail:<reason>'` / `'… · mail:off'`. Before sending, guard: if ANY prior `ord_order_status_events` row for this order has `status='confirmed'` AND `comment LIKE '%mail:sent%'`, SKIP the send (already mailed once) — prevents double-send across revert/re-confirm. This also feeds the board badge (which already derives from the last event comment) so the operator sees mail state with no schema change.
- **MAILER:** extend `send_mail()` (app/services/mailer.php) to accept an optional `?string $replyTo` (today Reply-To=From hardcoded) and an optional From display-name override. New `mail_order_confirmation_template($order, $customer, $lines, $bi)` reusing the kraft/aged-oak charte of `mail_account_template()`. 🔴 **SUPERSEDED by §CHANTIER #2 CONFIRMATION MAIL FINAL above:** the prior "From STAYS noreply@maltytask.ch" decision is REVERSED — FINAL is **From=`commandes@lanebuleuse.ch`** (client never sees maltytask) via the **Google Workspace SMTP relay + Postfix sender-dependent map** (`@lanebuleuse.ch → smtp-relay.gmail.com`), which resolves the deliverability concern below (Workspace DKIM `google` + SPF `_spf.google.com` cover the relayed mail). The old SPF/quarantine rationale (lanebuleuse.ch SPF lacked the VPS) no longer applies once mail is relayed THROUGH Google, not sent direct from the VPS IP.

**BC PROBE FINDINGS (durable — the justification for ABANDONING the BC-signal route):**
- SalesOrder OData = 110 fields, NO mail-sent/confirmation-sent field. 121 published web services expose NO email trace (no Interaction Log Entry t.5065, no Sent Email t.8888, no Document Sending log). Only order-progression signal = `Status` (Open→Released), which is NOT a confirmation-mail proof. → BC-signal detection dropped.

### Shared rulings still in force (both #2 and #4)
- Manual handler `public/api/expeditions-status.php` carries the full machinery (statusRank — NEVER ENUM ordinal; advanceMap; event+cache in ONE txn + log_revision; CSRF-retry). REUSE for any hand-built advance.
- `ord_order_status_events`: `user_id_fk` NULL-able; **NO `source` col** — origin distinguished via `comment` (imports use `'import:weeklyorders'`; auto-BC uses `'auto:bc-shipped'`). NO migration for the origin marker.
- **Board live-refresh piège:** an order self-advancing (to bl_printed at `*/15`, and once #2 lands, to confirmed) shows as advanced with NO operator click → recommend a board badge derived from the last event's `comment` (like the lead-time badge) so the operator distinguishes auto-advance from a colleague's click. Connector SELECT-status FOR UPDATE inside txn to avoid manual-click × cron race.
- **`ord_orders` ONLY** — NOT the eshop machine `inv_sales_fulfilment`/`_events` (mig 321/322, own API). No interaction.
- **EQUIP #2 (RE-PIVOT, mail-on-confirm-click):** `coder`+`sql` (graft on expeditions-status.php advance branch, JOIN query, mailer `$replyTo` param) + `ui` (HTML email-safe `mail_order_confirmation_template()`, kraft charte, Outlook/Gmail-safe table layout) + `webapp-testing` (smoke the Confirmer click in test-mode + the email_sent:false board surface). NO migration (trace via existing `comment` col, flag via existing `system_settings`).
- **EQUIP #4 (BC `Status`→status mapping, if ever resumed separately):** coder+sql; NOT the mail path above.
- Trigger "ruling-4"/"auto bl_printed"/"completely_shipped auto"/"mail confirmation"/"mail confirmer"/"mail sur clic Confirmer"/"confirmation_email_mode"/"entered→confirmed"/"Status Released"/"automate transition"/"state machine automation"/"Reply-To"/"commandes@lanebuleuse" → this section.

## 🆕 « RETARDS / NON CHARGÉES » B2B RELEASE SLICE (slice 1) — SHIPPED + DEPLOYED + LIVE-VERIFIED 2026-06-30 (branch `feat/expeditions-non-chargee` `481dca4`, NOT merged to origin/main; NO migration)
The « Commande non chargée → remise en stock » B2B slice planned earlier 2026-06-30. Surfaces overdue-uncharged B2B orders and lets the operator cancel one to RELEASE its reserved stock back to free-to-promise — no restock leg, no disposition queue, no migration.

- **New `?view=retards` (« Retards / Non chargées ») tab on `public/modules/expeditions.php`** — lists open B2B orders `status IN (confirmed, picked, bl_printed)` aged past a working-days threshold, most-late-first. Columns: N° commande / Client / Statut / Articles / Jours de retard / Préparée depuis / Action. Live smoke: **38 overdue orders**, CMD-12 (Ganesh Store Sàrl) leads at **16 j.o.** (req 2026-06-08).
- **Ageing basis** = `COALESCE(o.requested_date, latest picked/bl_printed event occurred_at, latest confirmed event occurred_at)` — operator chose `requested_date`; the event-date fallback handles NULL / BC-Posting-Date-absent cases.
- **`app/settings-helpers.php`: NEW `fulfilment_working_days_between(PDO,$from,$to):int`** — counts enabled weekdays (day-after-`$from` … `$to` inclusive), reads `workday_<dow>` from production_targets settings (CALL-NOT-COPY of planning-predict Step 6, not a re-implementation). Weekday-only; **Swiss/Vaud holidays = documented v2 gap**.
- **Threshold** = `system_setting('non_loaded_threshold_working_days','fulfilment',7)` — works default-only (no seed needed).
- **`public/api/expeditions-status.php` cancel branch:** whitelisted `reason='non-chargee'` → event comment marker `[annulation:non-chargee]` (mirrors the `[email:confirmation:sent]` sentinel pattern); returns `released_qty` (Σ `ord_order_lines.qty`). Single txn untouched.
- **RELEASE MECHANISM (PROVEN read-only on live DB):** cancelling order 12 → `open_total_qty −6` (each SKU −1), `physique` STRUCTURALLY UNTOUCHED — `fg-stock.php` Step 7 gate `status NOT IN ('shipped','cancelled')` auto-releases the reservation when status flips to `cancelled`. **No restock leg, no disposition queue, no migration.** (Free-to-promise = physique − open orders; releasing the order frees the units without moving physical stock — consistent with the gross-vs-free model.)
- **CSS/JS:** `public/css/expeditions.css` + `public/js/expeditions.js` (cancel handler: confirm dialog + CSRF auto-retry + under-threshold toggle).

🔴 **OPEN LOOSE ENDS (record-and-watch):**
1. **`reglages-generaux.php` settings-UI field for the threshold is BUILT (committed on the branch) but NOT DEPLOYED** — a parallel session holds reglages-generaux.php dirty; deploying mine would collide. Feature works at default 7 via the `system_setting` fallback. FOLLOW-UP: re-apply the one-field addition + deploy reglages ALONE once the parallel session's reglages work merges to main.
2. **DEPLOYED-BUT-UNMERGED divergence (stale-clone-reverts-prod HAZARD):** the feature is LIVE on the VPS but origin/main does NOT have it. A whole-tree `bin/deploy.sh --apply` from a main-based clone would REVERT it on prod. NEEDS merge of `feat/expeditions-non-chargee` → main SOON. (Same class as the recurring worktree/stale-clone reverts — until merged, deploy per-file only.)
3. **Eshop (Boutique en ligne) = slice 2, DEFERRED** — eshop depletes on PLACEMENT, so release = credit-back + Shopify refund (BLOCKED on Shopify write scopes). The fg-stock auto-release gate only frees B2B reserved stock; eshop release is a different (refund) path.
4. **BUILD-PROCESS LANDMINE:** the first agent pass SILENTLY REVERTED the per-site stock-scope feature (`d3cf218` noconsign branch) as collateral on the large `expeditions.php` edit; caught in review and retards re-applied ADDITIVELY (final diff 243+/1−, stock-scope intact). FLAG for future: large-file delegated edits on `expeditions.php` need a **noconsign-preservation guard** named in the brief (grep the stock-scope markers before+after).
5. **OPERATIONAL SIGNAL surfaced:** 38 orders ≥7 working days overdue in bl_printed/picked = the known "**bl_printed-past-date not marked Expédié**" lag is LARGE. The retards tab now makes it visible + actionable (operator can cancel-to-release the truly-dead ones).

🔴 Architecture verdict: SOUND. Release-by-cancel rides the existing `fg-stock.php` Step 7 reservation gate — one fact (open-order reservation), one mechanism (status≠shipped/cancelled), NO parallel store, NO new leg. Container/format chain untouched. Trigger "retards"/"non chargée"/"non-chargee"/"commande non chargée"/"remise en stock B2B"/"release stock"/"annulation non-chargee"/"non_loaded_threshold_working_days"/"fulfilment_working_days_between"/"jours de retard"/"feat/expeditions-non-chargee"/"reglages threshold field" → this section.
