# Fulfilment — Archived former-index SHIPPED-ARCS pointer (verbatim, compacted from index 2026-06-15)

## 🗂️ ARCHIVED INDEX SHIPPED-ARCS-REGISTER POINTER (compacted from index 2026-06-15)
_Verbatim fat Fulfilment/Expéditions pointer (eshop-repacking + forward-seasonal-burn + Historique + historical-Commandes lane + full Shopify build) relocated from the index during memory compaction. The §-sections in this file are canonical; this preserves the exact former index pointer wording._

- [Fulfilment / Expéditions arc (Le Cockpit christening, LIVE for the team 2026-06-09)](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) — **CURRENT MIGRATION HEAD = 337 applied (Shopify fulfilment 321/322/323; seasonal-burn 325; historical-Commandes views 326; weekly-client view 329; eshop-repack 336 `inv_repack_events`/337 RQ-type `repack-unresolved-bundle`) — always verify via `migrate.php --status`; parallel sessions live.** **✅ ESHOP REPACKING — CAPTURE PHASE SHIPPED 2026-06-12 (advisory-now / deplete-at-06-15; maltyweb `87b9499`, LIVE, NOT pushed). Foundation of the deferred Phase-2/ITEM-3 cage-COGS engine: captures repack EVENTS now (box/PD8-bundle open → loose `-X` cage eshop pool; single SKU = separate taproom pool, operator-confirmed) so the ledger has history before the 06-15 COGS-depletion flip. `app/repack.php` pure `repack_decompose_orders()` (units_per_pack+recipe single OR `ref_sku_composite_slots` PD8; `ref_sku_aliases` PACKDEC/PACKDECX8/FRPACKDEC→PD8 id137; refuse-don't-NULL→RQ for unresolvable + recipe-NULL collabs/PBD + PD8 recipe=6 slot ambiguity). `app/fg-stock.php` symmetric repack leg in BOTH fns FEATURE-GATED behind `repack_depletion_live()` (default 0) — Opus-verified flag-OFF = FG byte-unchanged. `app/settings.php` accessor. `expeditions.php?view=repack` advisory panel + `expeditions-repack.php` (csrf/retry/1-txn/log_revision/balance-check/`repack_key` idempotency) — sub-view, no ref_pages/tour. 🔴 LESSON: agent FALSELY claimed deployed — `bin/deploy.sh --apply` left fg-stock.php STALE on VPS (ungated leg live); Opus caught via flag-OFF probe (delta=−1) + md5 local↔VPS, fixed by scp. ALWAYS md5-verify local↔VPS after agent deploy, esp. COGS-adjacent. ⏭ 06-15 ACTIVATION SET: (1) packaging-form `-X` cage option; (2) flip flag→1 + retarget FG leg base→cage + add eshop none-bundle→cage + box-open refill leg (Opus re-verify Σcards==Σphysique after); (3) operator alias-seed ~10 codes + PD8 recipe=6 slot refine; (4) Phase 4 cage/box/label MI→inv_consumption COGS (gated 06-15 anchor + Shopify pickup). Trigger "repack"/"reconditionnement"/"box-open"/"-X cage"/"repack_depletion_live"/"inv_repack_events"/"repack-unresolved-bundle" → READ §ESHOP REPACKING in this file.** **✅ FORWARD SEASONAL BURN ENGINE (semaines-restantes) SHIPPED + LIVE on `?view=stock` 2026-06-11 (mig 325 `kpi_sku_seasonal_index` APPLIED — table + composite `idx_sl_sku_posting` + 11 `system_settings` section='stock' burn_* params; 795 cache rows / 15 families). Single burn path: `app/seasonal-burn.php` (8-fn lib) writes S via `scripts/seasonal-index-cli.php` (`--apply` ran once, cache 11:33); `fg-stock.php` Step 8/9 computes L+forward-sim at render; `expeditions.php`/`.css` triad. Burn=−SUM(qty_signed) ALL doc_types floored ≥0/wk, deny `customs_artifact` ONLY, family=`ref_skus.recipe_id` FK JOIN, NULL-sku excluded+counted, divergence from `v_sales_ledger_order_lines` documented in code. Opus-verified: global peak wk26, ZEPF L=297 vs naive 349, sim 40% shorter into June peak, 53 rows/110ms/0 errors. 🔴 OPEN: cron NOT enabled (in `db/cron/maltytask-seasonal-index.cron`, not in /etc/cron.d — PM recs enable now); authed visual smoke pending; ALL UNCOMMITTED (net-new+fg-stock.php mine via PATHSPEC; expeditions.php/.css entangled w/ parallel session, leave to owner). Pre-existing parallel `Undefined array key "needs_review"` at expeditions.php:2318 = Carnet/CRM not this build. FAST-FOLLOW: Croston's (ADI>1.32), lead-time bands (needs per-family L_repl), overstock ceiling, cron freshness chip. Trigger "semaines restantes"/"burn"/"rythme de base"/"seasonal index"/"kpi_sku_seasonal_index"/"seasonal-burn"/"rythme"/"provisoire chip"/"forecast stock"/"prévision stock" → READ §SEMAINES-RESTANTES / FORWARD SEASONAL BURN ENGINE in this file.** **✅ HISTORIQUE TAB (per-week-per-client historical sales) SHIPPED + LIVE 2026-06-11 (mig 329 `3646e4f` NOT pushed; surgical 5-file scp deploy, authed smoke PASSED KW23=364.80 HL/44 clients/AlloBoissons drill 8 SKUs). 4th read-only VIEW `v_sales_ledger_weekly_client` (6,292 client-week rows, grain (YEARWEEK mode-3, customer), SAME WHERE as the 3 data-lane views) + 8th Expéditions UI view `?view=historique` (week-blocks→client rows HL-desc→per-SKU drill off `v_sales_ledger_order_lines`; read-only, NO chips, NO CHF; new `expeditions-historique.js` drill-collapse). 🔑 GOTCHA: `YEARWEEK(d,3)` vs STR_TO_DATE-derived week_start DISAGREE at some boundaries → filter/join on the INTEGER `iso_yearweek` bucket, NEVER `week_start`; any future consumer of these views MUST key on iso_yearweek. COMPLETES the historical-sales endpoint (only the per-week-per-client AGGREGATE was requested+built; the per-DELIVERY cards remain optional/unbuilt). → §HISTORIQUE TAB.** **✅ HISTORICAL-COMMANDES DATA LANE SHIPPED + LIVE 2026-06-11 (mig 326 `78c231e` NOT pushed; 3 read-only VIEWs over `inv_sales_ledger` — `v_sales_ledger_orders` 7,497 / `_order_lines` 23,529 / `_unresolved` 222·549,387u; read-contract `docs/historical-commandes-read-contract.md`; ZERO `expeditions.php` edits = no UI-overhaul collision). GRAIN: order CARDS = `doc_type='shipment'` DISPLAY grain; `allnet` stays the AUDIT-total basis — display ≠ audit. B2B-scope (`sale_class NOT IN eshop/taproom/customs_artifact/transfer/sample`, eshop/taproom keep their auto-rows, no double-count); cutover 2026-06-08 (forward ≥ / historical <). Year profile 2021 858/2022 1294/2023 1123/2024 1435/2025 1835/2026p 952. OPEN: (1) 214/222 unresolved-SKU review CSV (Kouros, self-heals); (2) COGS-cutover coupling `489ab70` = allnet for qty / invoice lines for CHF; (3) per-DELIVERY card browse still optional/unbuilt (per-week-per-client now wired via Historique). → §HISTORICAL-COMMANDES DATA LANE.** **✅ SHOPIFY⇄maltyweb FULFILMENT INTEGRATION — FULL BUILD COMPLETE 2026-06-11 (Phase 0+1+2A LIVE; 2B DISARMED+double-gated on Shopify write-scopes; 3c is_non_stock NO-OP infra).** (Pickup signal = `shipping_lines[].title`; ingest cron on LOCAL box NOT VPS = SPOF; plan `~/.claude/plans/sleepy-snuggling-brooks.md`.) **✅ Σcards==Σphysique INVARIANT NOW HELD — closed by TWO 2026-06-12 fixes: (1) scope-gate `d9f7976` (3 depletion legs gated `stocktake_scope!='none'`, 13 scope='none' phantoms excluded); (2) ANCHOR TIEBREAK FIX SHIPPED+VERIFIED on prod — `fg_stock_compute()`+`fg_stock_location_snapshot()` anchor = `MAX(counted_at)` tiebreak `MAX(id)` (was bare `MAX(id)` → stale older-dated count inflated anchor). SINGLE-FILE targeted rsync `/tmp/fgstock-hotfix.php`→`app/fg-stock.php` (NOT bin/deploy.sh — dirty cage/repack tree); DOAF 83→45, ZEPF 2502→1256, 26/26 Production SKUs corrected, Σ=8892==8892. ⚠️ LOCAL≠DEPLOYED: local fg-stock.php has the repack `if(repack_depletion_live())` gate TANGLED in (deployed runs repack UNGATED) — do NOT revert the local anchor edit; gated version ships idempotently when repack owner deploys (needs untracked `app/repack.php`). Rollback `/tmp/deployed-fgstock.php`.** 🔴 OPEN (gated, BEFORE any `cogs-monthly-compile --apply --month 2026-06`): WEEKLY-vs-MONTH-END DISCRIMINATOR — mig 338 `inv_fg_stocktake.count_type ENUM('operational','month_end')` + month-end toggle on saisie census form + backfill (May 34→month_end, June weeklies→operational) + COGS query `WHERE month_closed=? AND count_type='month_end'` latest-per-(sku,loc)-then-sum; GATE = May-fiche FG=47 701.56 reproduces TO THE CENT. NOT started. Trigger "Shopify"/"pickup"/"click&collect"/"retrait"/"shipping_lines"/"fulfilment_mode"/"eshop picking"/"push-shopify"/"write-back"/"ref_shipping_methods"/"inv_sales_fulfilment"/"ingest-shopify"/"Σcards" → READ §FULL SHOPIFY⇄MALTYWEB FULFILMENT BUILD COMPLETE + §SHOPIFY ⇄ maltyweb FULFILMENT INTEGRATION in this file first. read when touching `ord_orders`/`ord_order_lines`/`ord_order_status_events`, **`inv_stock_movements`** (inter-site transfers, mig 299), **`inv_side_stock_ledger`** (packaging loose-unit remainder / giveaways, mig 303 — UNIT-COUNTS NOT COGS, distinct from sales-side `sale_class='giveaway'` migs 300/301), `ref_customers`/`ref_transporters`, **`app/fulfilment-site.php` (`resolve_fulfilment_site()` = THE single ship-from resolver)**, `expeditions.php` (`?view=stock|mouvements|commandes|side-stock`)/`api/expeditions-status.php`, `form-packaging.php` (side-stock accrual writer + complete-a-box draw), `app/fg-stock.php` (`fg_stock_compute()` + `fg_stock_location_snapshot()` per-site sales attribution), `reglages-generaux.php ?sec=delivery_sites` (Nausikraft client list), the WeeklyOrders importer, customer bootstrap, or commandes@ email ingestion. Holds mig-279/299/303 schema, the resolver precedence + landmines, the depletion-gap-by-design, go-live record, ITEM 1 SHIPPED + per-site-cutoff remainder, **ITEM 2 (mini side-stock/giveaways) SHIPPED 2026-06-10 (`1c95e23`) — OPEN downstream = year-end-settle cron (documented-not-built; beer-tax JOIN shape in mig 303)**, ITEM 3 parked (Phase-2 cage/COGS, externally blocked), and the R1-R7 risk flags. **Triggers: "side-stock"/"side stock"/"mini side-stock"/"giveaway"/"giveaways"/"remainder"/"loose units"/"year-end offered"/"beer offered" → READ this file.**
