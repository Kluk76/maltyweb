# Fulfilment — Returned-FG tracking (BC-derived): disposition taxonomy, Part A/B/C, reversal-pollution fix, oversell/ATP, deferred backlog

## §POST-CENSUS RESTOCK NOT COUNTED — bump origin_posting_date, don't dup/falsify-census (PM ruling 2026-06-30, Short Circuit ord_returns #16 / NC214850)
**Case (distinct from the #208 §MANUAL case below — there the NC did NOT exist; HERE the NC IS booked & the canonical restock row already exists).** `ord_returns #16` exists: origin_bc_document_no=NC214850, origin_posting_date=2026-06-26, returned_on=06-26, lines ZEPF+18/MOOF+8/DIVB+3 all disposition='restock', created via the canonical Retours queue. Kegs came back ~06-27/29 and were NOT in the 06-29 stocktake. Restock leg gates `r.origin_posting_date > $anchorDate` STRICT (fg-stock.php Step 6.7 L878 + Leg 4 L1885; $anchorDate = GLOBAL MAX(counted_at) = 06-29). 06-26 ≤ 06-29 ⇒ #16 contributes 0 → FG understated 18/8/3.
**VERDICT = Option A: UPDATE #16 `origin_posting_date` 2026-06-26→2026-06-30 (keep origin_bc_document_no=NC214850, keep returned_on=06-26), via one-shot PHP (txn + log_revision, run as www-data). NOT a 2nd row, NOT a census edit.**
- **Why A:** origin_posting_date has only TWO readers — fg-stock (the restock CUTOFF; bumping is the whole point) and returns-synthese by_client/by_beer/mix (a trailing-**90d** window, L44/L69; 06-26 and 06-30 both in-window ⇒ ZERO synthèse effect). The 'rate'/'overship' blocks read `v_physical_returns` (ledger posting_date), unaffected. So origin_posting_date OPERATIVELY = **stock-recognition / physical-re-entry cutoff**, not a load-bearing BC fact; the true BC posting date (06-26) stays canonical in `inv_sales_ledger` keyed by NC214850 (recoverable) → divergence acceptable WITH an audit comment. Bonus: 06-30 is actually MORE accurate as the physical-re-entry date (kegs re-entered ~06-29, after the count) than the 06-26 NC date.
- **Why NOT B (2nd row dated 06-30):** returns-synthese by_client/by_beer/mix SUM `ord_return_lines.qty` over the 90d window keyed on origin_posting_date → BOTH #16 (06-26) AND the new row (06-30) are in-window → KPI #283 reports **36/16/6 = DOUBLE** the real 18/8/3. Corrupts the returns analytics even though FG physique nets correct. REJECT.
- **Why NOT C (census edit at site 2 06-29):** the count was NOT short — the kegs genuinely were not present on 06-29 and arrived after. C falsifies raw append-only census (asserts a physical count that didn't happen) + touches corrections-policy data. The kegs are a **return-in DELTA after the count** = exactly what the restock leg models ("census = absolute full recount; restock = delta"). C is the wrong model.
- **Double-count safety through next censuses:** next site-2 census (end-July) is a complete recount that WILL physically include the kegs → census supersedes; origin_posting_date 06-30 < next anchor ⇒ #16 drops from Step 6.7 automatically → no double. 'never carry forward' holds.
- **🔴 GUARD the operator was missing — global-anchor re-understatement window:** the restock leg is NOT site-resolved (uses GLOBAL MAX anchor; Leg 4 hardcodes site 2). If ANY OTHER site is censused (advancing global anchor past 06-30) BEFORE site 2 (warehouse) is recounted, #16 drops out of Step 6.7 and FG RE-understates 18/8/3 until site-2's own next census. Not a double-count, a re-understatement; pre-existing non-site-resolved-restock limitation (the deferred C residual), not introduced by A. Operator must ensure site-2's next census includes the kegs.
- **Σcards==Σphysique invariant:** HOLDS by construction — Step 6.7 (total) and Leg 4 (site-2 card) use the SAME predicate, so both gain +18/8/3 together (P2 proved diff 0.0000). Probe to confirm post-write anyway.
- **Site attribution:** Leg 4 puts the restock 100% at site 2 (warehouse, `ASSUMPTION-restock-site-warehouse`). If the kegs physically returned elsewhere, the TOTAL physique is still correct but the per-site card is off. Confirm kegs are at the warehouse.
- **No re-queue hazard:** origin_bc_document_no stays NC214850 → the queue NOT-EXISTS still suppresses it; bumping the date doesn't reopen it.
- **No handler exists** for a post-census stock correction (the only FG-additive surface is the BC-credit-driven disposition queue). A one-shot PHP (txn + `log_revision`, www-data, dry-run→commit, delete after) is the right tool — same conclusion as the §MANUAL #208 case.
- **Exact write:** `UPDATE ord_returns SET origin_posting_date='2026-06-30', comment=<append audit note> WHERE id=16` wrapped in `beginTransaction` + `log_revision($pdo,$me,'ord_returns',16,$before,$after,'manual-stock-recognition-correction',<note>)` (signature `app/db-write-helpers.php:236`; pass $before pre-image → action auto='update'; $me = real admin user id). Audit note records 06-26→06-30 bump + that NC214850's BC date stays 06-26 in the ledger. ord_returns has `comment` (per expeditions.php L1631 insert), no unique key (free to UPDATE).
- **🆕 DURABLE FOLLOW-UP (if this recurs):** the contract-vs-purpose tension (BC posting date vs stock-recognition cutoff) is best fixed by a dedicated `stock_effective_date`/`restock_effective_on` column decoupled from origin_posting_date, so we never bump the BC fact. Migration + fg-stock cutoff repoint + (ideally) site-resolve the restock leg. Not 'this week'; queue if manual post-census restock corrections become routine. EQUIP sql+coder.

## §DURABLE RESTOCK-CUTOFF FIX — SETTLED BUILD PLAN 2026-06-30 (PM-designed, awaiting operator approval → Sonnet build). Supersedes the "not this week" deferral above; operator explicitly asked for the durable fix after 2 hand-patches.
**Ground truth verified live 2026-06-30:** ord_returns has NO unique key, NO CHECK; `origin_posting_date` DATE NULL, `returned_on` DATE NOT NULL, `comment` TEXT. Restock leg = **Step 6.7** (fg-stock.php query ~L872, predicate `AND r.origin_posting_date > ?` L878, `$anchorDate`=GLOBAL MAX(counted_at) L882) + **Leg 4** (~L1879/L1885, attributes 100% to warehouse via `resolve_fulfilment_site($pdo,['channel'=>'eshop'])`=site2). Both bind `$anchorDate`; IDENTICAL predicate = the Σcards==Σphysique guarantee. Disposition POST handler L1509+ (derives posting_date from ledger L1553, idempotency on bc_doc L1567, insert header L1630 / lines L1646, all log_revision); queue render L2743+ reads RAW ledger (NOT v_physical_returns) with NOT-EXISTS on bc_doc+sku L2758. Mig HEAD=449, 0 pending → next-free 450 (re-verify at build). The 3 bumped rows = ids **13(NC214843)/14(NC214847)/16(NC214850)**, origin_posting_date=06-30, returned_on=06-29/06-29/06-26; comments carry the bump note.

**A. SCHEMA — mig 450 (`450_ord_returns_stock_effective_date.sql`):** `ALTER TABLE ord_returns ADD COLUMN stock_effective_date DATE NULL COMMENT '...physical re-entry / FG-restock cutoff, decoupled from origin_posting_date (BC NC posting date); NULL → leg falls back to origin_posting_date so existing rows unchanged...' AFTER origin_posting_date;` NO FK (it's a date), NO CHECK (keep it a free correction surface; refuse-don't-NULL applies to BOM not date corrections; a `>= returned_on` CHECK would bite legit corrections). NO new schema_meta row (schema_meta is per-table, ord_returns row already present). Backfill: leave NULL for ALL existing rows EXCEPT 13/14/16 — for those, **RESTORE true origin_posting_date from inv_sales_ledger.posting_date keyed by bc_document_no AND set stock_effective_date='2026-06-30'** (one-shot PHP, txn+log_revision, www-data, dry-run→commit, rewrite comment to note restoration; NOT in migrate.php — no-SELECT rule + needs app-level audit).

**B. fg-stock.php — TWO changes, BOTH legs in lockstep (or the invariant breaks):** (1) repoint predicate `r.origin_posting_date > ?` → `COALESCE(r.stock_effective_date, r.origin_posting_date) > ?` at L878 AND L1885. (2) Change the cutoff DATE from global `$anchorDate` to the WAREHOUSE site's OWN census date (the site Leg 4 already attributes 100% to) — closes point-3 re-understatement cheaply WITHOUT a site column, because the 100%-warehouse assumption already holds. Compute the warehouse census = MAX(counted_at) for the eshop-resolved site, COALESCE→$anchorDate when warehouse never censused; bind the SAME value in both legs. Σcards==Σphysique holds because both legs use identical predicate+date (warehouse census ≤ global anchor → only ADDS the wrongly-dropped rows, in both legs together). **SITE COLUMN on ord_returns = DEFERRED v2, likely never needed** — only matters if returns re-enter at a non-warehouse site (operator must confirm; if returns always land at the warehouse, point-3 is fully closed by the warehouse-census cutoff). EQUIP coder+sql+webapp-testing; brief MUST name both file:line + the Σ-invariant probe (diff 0.0000).

**C. OPERATOR ACTION — fold into ?view=retours, no new page:** **C1 (v1) = "Date de remise en stock" optional date control** revealed when a line is dispositioned "Remettre en stock" (restock); defaults to today; POST writes `stock_effective_date` (validated date; NULL→unchanged behavior). This RETIRES the origin_posting_date hack permanently — operator sets the recognition date explicitly instead of falsifying the BC date. No-DB-nomenclature label ("Date de remise en stock" not stock_effective_date/restock); CSS exp-ret-* in expeditions.css; CSRF+can_write_expeditions+log_revision+PRG. Double-count guard unchanged (queue NOT-EXISTS on bc_doc+sku; census-supersede on next count). **C2 (fast-follow) = "Saisir un retour sans avoir" form** for the #208 manual-no-NC shape (writes bc_doc=NULL + origin_order_id_fk + stock_effective_date + visible "en attente de rapprochement avoir" badge; reconcile later via UPDATE origin_bc_document_no, NEVER disposition the NC fresh — see §MANUAL #3). EQUIP coder+sql+ui+webapp-testing.

**D. v_physical_returns refinement — mig 451 (independent, parallelizable, CREATE OR REPLACE VIEW safe inline in migrate.php — view-only, no DML):** replace the blunt same-day strip (`EXISTS(shipment/invoice same cust+sku+posting_date) IS false`) with a QTY-MATCH test that keeps PARTIAL same-day returns, strips only FULL reversals: `AND l.qty_signed <> (SELECT COALESCE(-SUM(s.qty_signed),-1) FROM inv_sales_ledger s WHERE s.customer_id_fk=l.customer_id_fk AND s.sku_id_fk=l.sku_id_fk AND s.doc_type IN('shipment','invoice') AND s.posting_date=l.posting_date)`. -SUM = positive shipped units; COALESCE -1 → no-same-day-ship keeps (>0<>-1); full reversal (credit==shipped) strips; partial (credit<shipped, e.g. NC214850 ZEPF+18 vs 60 shipped) keeps. **Does NOT reopen any double-count:** the view feeds returns-synthese rate/overship ONLY; the disposition queue + restock leg read RAW ledger, untouched → purely sharpens the analytics lens. **KNOWN RESIDUAL:** the qty-match heuristic mis-keeps when there are MULTIPLE same-day shipments+reversals for one cust+sku (no reliable pairing key — bc_source_no doesn't work, line 186); durable fix = RE-1 ingest-time `is_reversal` flag (line 172/196), deferred to BC-API arc. EQUIP sql+webapp-testing.

**E. SEQUENCING:** v1 (week-ish): [1] mig 450 (sql) → [2] fg-stock both-legs repoint+warehouse-census cutoff (coder+sql+webapp-testing, dep 1) → [3] one-shot backfill 13/14/16 (sql+coder, dep 2 — deploy 2 FIRST or they drop out mid-window) ; [4] C1 control (coder+sql+ui+webapp-testing, dep 1, parallel with 2) ; [5] mig 451 + view refinement (sql+webapp-testing, FULLY INDEPENDENT). Deferred: C2 manual-no-NC form; returns site column+per-site gating (only if non-warehouse re-entry confirmed); route disposition queue through v_physical_returns (= the existing deferred batch line 174, also closes the L1532 reversal-restock hazard); RE-1 is_reversal ingest flag.

**F. INVARIANT/DERIVATION-TREE IMPACT:** Σcards==Σphysique holds ONLY if both legs change in lockstep (predicate+date) — #1 build risk = agent edits one leg. "Never carry forward/census=complete recount" PRESERVED: stock_effective_date<next census → restock auto-drops when census supersedes (it's a delta-recognition date, census-model-compatible). ZEPPELIN: returns/restock is NOT in the commissioning→COGS chain — restock adds FG physical units only, NO cost ledger entry (BC owns revenue); stock_effective_date touches ONLY the FG-physique restock cutoff + returns-synthese window → ZERO COGS/COP/beer-tax/WAC impact. Decoupling STOPS falsifying the BC fact (durable-modeling win: don't overload one column).

**G. OUT OF SCOPE (scope discipline):** broader case-(b) no-avoir reconciliation engine (C2 covers only the #208 manual shape); Shopify/eshop keg returns (no signal pipeline, different dynamics); RE-1 is_reversal flag (BC-API arc); disposition-queue→view repoint + rebate copy-paste collapse (existing deferred batch); ord_returns site column (only if non-warehouse re-entry); contract-brewing (Chien Bleu) return handling (separate modeling decision, line 103); CHF/contra-revenue on returns (BC owns revenue, never on the FG surface).



## §MANUAL / NON-BC PHYSICAL RETURN — the "restock before the NC is booked" pattern (PM ruling 2026-06-29, Short Circuit ord #208 / BC ORD210130 / EXPED-012735)
**Case:** an event order ships full qty + depletes FG; unused full kegs/bottles come back physically; an internal logistics email (NOT synced) lists the returns; the *avoir/NC* does NOT exist in BC yet. Operator wants FG corrected now. The canonical writer (`expeditions.php?view=retours` POST handler) is **BC-credit-driven only** — it re-queries `inv_sales_ledger` for qty + posting_date and CANNOT create a row for a return with no ledger line. **So there is NO handler for a manual/non-BC physical return — genuine gap.**
**PM verdict (recorded so nobody re-litigates):**
1. **Writer:** NOT bare SQL (skips audit — there are NO DB audit triggers; `audit_row_revisions` is application-level via `log_revision()`). Use a one-shot PHP snippet via `maltytask_pdo()` that MIRRORS the POST handler insert (lines ~1630-1665): txn + `log_revision` on `ord_returns` (insert) + each `ord_return_lines` (insert). action ENUM insert/update only — insert is fine. Both tables `corrections_policy='allowed'`, `table_class='source'`, `writer_script='retours.php (future)'`.
2. **`origin_bc_document_no=NULL` is schema-OK** (nullable, no NOT NULL, NO unique key anywhere on the tables — verified SHOW CREATE 2026-06-29). Link instead via **`origin_order_id_fk`** (documentary "optional enrichment" per schema_meta). 🔴 BUT NULL bc_doc means the future NC is NOT yet deduped (see #3).
3. **🔴 FG-vs-FG DOUBLE-COUNT HAZARD (NEW class — the arc's line-45 guard only covered FG-vs-revenue + reversal-vs-restock).** Mechanisms that each add the kegs to FG: (1) this manual `restock` row, (2) a census recount that physically includes them, (3) the future BC NC dispositioned `restock`. **Exactly ONE must own the add.** Dedup between (1) and (3) is ONLY via the disposition-queue `NOT EXISTS` predicate (`expeditions.php` ~L2758: `r.origin_bc_document_no = l.bc_document_no AND rl.sku_id_fk = l.sku_id_fk`) — and the POST Step-4 idempotency (~L1565) ALSO keys on `origin_bc_document_no`. There is **NO DB unique key**; **`v_physical_returns` same-day stripping does NOT apply** (NC books days/weeks after the 06-26 ship, not same-day — won't be stripped; also the queue reads raw ledger, not the view). So **NULL bc_doc → the future NC lands in the queue → an operator restocking it = +qty TWICE.** **CORRECT no-guess design: write the manual row now (bc_doc=NULL), and when the bookkeeper books the NC, RECONCILE by `UPDATE ord_returns SET origin_bc_document_no=<real NC#>` on THIS row — NEVER disposition the NC queue line fresh.** Then the queue NOT EXISTS auto-suppresses it. Option (a) "guess the future NC# now" = REJECTED (violates never-guess; BC assigns the number at booking; wrong guess → orphan + failed dedup). **This reconciliation is a remembered operator action with NO automated guard → flag it visibly (comment on the row + a pending note).**
4. **🔴 origin_posting_date / anchor — restock leg gates `origin_posting_date > $anchorDate` STRICT, and `$anchorDate = GLOBAL MAX(counted_at)` across ALL sites (fg-stock.php L408, Step 6.7 L872-882 + Leg 4 L1879-1889; Leg 4 attributes 100% to warehouse site id=2 Zeubi).** Live global anchor 2026-06-29 = **TODAY** (sites 1/2/4 censused 06-29, site 3 06-23). ⇒ **NO origin_posting_date ≤ today produces any restock effect** (06-27 < 06-29 = silent no-op); the only value that counts is **2026-06-30** (>06-29). **GATE: only write the restock IF the kegs were NOT already counted in the 06-29 Zeubi(site2=warehouse, where restock lands) census.** If they were on the warehouse floor during the 06-29 count → FG is ALREADY correct → a restock dated 06-30 DOUBLE-COUNTS until the next census → write NOTHING (disposition the future NC non-restock). **SURFACE to operator — DB can't tell if kegs were physically present at the count.** Next-census behaviour is SAFE/no-drift: census = complete recount supersedes; origin_posting_date 06-30 < next census date → restock drops out automatically (the 'never carry forward' rule holds). Census is absolute (full count), restock is a delta (+18/+8/+3) — restock fits when the operator has the delta not the absolute count.
5. **Deposit/gas lines (Cautions fûts, Aligal) correctly EXCLUDED** — no `ref_skus` row → can't be `ord_return_lines` (sku_id_fk NOT NULL FK→ref_skus); accounting NC only, no FG effect.
**Verified facts (live 2026-06-29):** ord #208 status='shipped' source='bc' source_ref='bc:ORD210130' customer 2328; lines ZEPF(60)×40, MOOF(45)×15, DIVB(16)×5 — returns +18/+8/+3 all ≤ shipped ✓; SKUs all is_non_stock=0 scope=base recipe_id NOT NULL (beer) ✓; restock site = warehouse id=2; exactly 1 ord_returns row ever; no unique key on either table. EQUIP sql+coder(+ui if a manual-return surface is built). FOLLOW-UP CANDIDATE: a proper "saisie retour manuel (sans NC)" action on ?view=retours that writes this shape + carries the pending-NC-reconciliation flag, closing the gap so it isn't a one-shot each time.

## §RETURNED FG TRACKING (BC-DERIVED) — ARC AS-BUILT 2026-06-16 (P0+P1+P2+P3-SYNTHÈSE SHIPPED+LIVE; P3-RATE+P2b OPEN; 4 LOCAL COMMITS NOT PUSHED + 2 FRONTEND FILES DEFERRED-FROM-COMMIT)

**Scope (Kouros-APPROVED — BC-DERIVED PIVOT, supersedes the earlier order-XOR scoped design that was "to be written once Kouros approves").** Returns are **BC-derived**: the bookkeeper books an *avoir de vente* (credit memo) in Business Central → it imports to `inv_sales_ledger` as `doc_type='credit'` (also `return_receipt` for some). **BC OWNS REVENUE — the avoir reverses it; the app writes NO revenue/COGS-ledger entry.** The app owns DISPOSITION + physical FG only. **Default disposition = `quarantine`** (no FG effect until operator confirms restock).

**DISPOSITION TAXONOMY (per line, the cardinal decision) = enum extended to `restock / scrap / quarantine / rebate`:**
- `restock` = re-enters FG (resaleable) — the ONE FG/COGS-bearing leg.
- `scrap` = write-off (hygiene/best-before) — NO FG effect.
- `quarantine` = held pending decision (DEFAULT) — NOT yet in stock.
- `rebate` = a beer-credit that is a price/defect rebate, NOT a physical return — NO FG effect (added in mig 376).

**PM's earlier rulings CONFIRMED CORRECT against live data (recorded so nobody re-litigates):**
- `fg_stock_compute()` reads NO ledger `doc_type`s — physique = anchor + prod − shipped − eshop − taproom. So BC credits did NOT auto-replenish physique (they only affect the burn-velocity engine, which nets all doc_types). Therefore the restock leg is genuinely **ADDITIVE** (a new returns-in flow), NOT a suppression/avoid-double-count. The Q1 correction was right.
- **Beer-vs-deposit split = `ref_skus.recipe_id IS NOT NULL`** (CAUF/CAUAL/CO2 have NO recipe → deposit lines). Beer-credit lines = the returned FG; deposit lines ≈ all kegs back. The queue lists only beer-SKU lines.

**P0 — SCHEMA (APPLIED LIVE):**
- mig **373** `373_ord_returns.sql` — `ord_returns` (header) + `ord_return_lines` (`sku_id_fk` RESTRICT, `qty>0` CHECK, per-line disposition enum). Originally carried an order-XOR origin CHECK `chk_ord_returns_exactly_one_origin`. schema_meta rows added.
- mig **376** `376_returns_bc_derived_origin.sql` — the BC-derived pivot: **DROPPED the order-XOR CHECK**; **ADDED `origin_bc_document_no` + `origin_posting_date`** as the canonical origin (the avoir is the origin, not an order); **EXTENDED disposition enum to add `rebate`**; **ADDED `origin_ledger_sku_code`**. Both applied live. MIG HEAD now 376, next free = 377 (re-`migrate.php --status` at build start — parallel sessions live).

**P1 — DISPOSITION QUEUE (LIVE):** `?view=retours` in `public/modules/expeditions.php`. Lists beer-SKU `credit`/`return_receipt` ledger lines **awaiting disposition** (NOT EXISTS join on `bc_document_no` + `sku`), grouped by avoir; operator tags disposition per line (default **Quarantaine**). POST handler **re-queries the ledger for server-authoritative qty + posting_date** (operator CANNOT alter qty), **idempotent** (partial disposition supported — only un-dispositioned lines insert), txn + `log_revision`. CSS `exp-ret-*` in `expeditions.css`.

**P2 — FG RESTOCK LEG (LIVE + OPUS-VERIFIED):** `app/fg-stock.php`:
- **Step 6.7** (in `fg_stock_compute()`) + **Leg 4** (in `fg_stock_location_snapshot()`, 100% to the eshop-warehouse site, flagged `ASSUMPTION-restock-site-warehouse-2026-06-16`).
- Condition: `disposition='restock' AND origin_posting_date > anchor` (STRICT `>`) → adds to physique.
- **Opus-verified on LIVE DB** (rolled-back txn, anchor 2026-06-15, ZEPF): restock +5 → physique +5 exactly; **Σcards == Σphysique held (diff 0.0000) at every step**; `scrap` → 0 movement; self-cleaned. VPS backup `fg-stock.php.bak-pre-p2-*`.
- DORMANT until a real `restock` disposition exists (0 today).

**P3-RATE — RETURN-RATE KPI — DEFERRED (needs Kouros design decision; SURFACE don't guess).** Denominator design is undecided: returned ÷ ordered — **which denominator** (BC shipments vs `ord_orders`), and **how to match a return to its origin sale** given the avoir does NOT always link to the original invoice in our ledger. Do NOT guess the matching/denominator — it is sales/COGS-adjacent. **DISTINCT from P3-SYNTHÈSE below — do not conflate.**

**P3-SYNTHÈSE — RETURNS 90-DAY SUMMARY — DONE + LIVE 2026-06-16 (volume + disposition-mix, NOT a rate).** Kouros chose a **volume + disposition-mix synthèse, NOT a ratio** (no denominator), surfaced on **BOTH** the Retours tab AND Mon-tableau. Shared-compute `app/returns-synthese.php::returns_synthese(PDO $pdo, int $periodDays=90): array`:
- `by_client` — grouped **per `bc_document_no`** (customer resolved from `inv_sales_ledger`). 🔴 v1 KNOWN LIMIT: a client with 2 avoirs shows TWICE (grouping is per credit-note, not per customer) — accepted; aggregate-by-customer later if wanted.
- `by_beer` — `ref_recipes.name` (join ref_skus.recipe_id→ref_recipes.id).
- disposition mix % — over PHYSICAL dispositions; **`rebate` EXCLUDED from ALL volume** (and from the mix).
- `pending_count` — 180-day window matching the P1 queue predicate (beer-SKU credit/return_receipt ledger lines awaiting disposition; beer = `ref_skus.recipe_id IS NOT NULL`).

SURFACES (both smoke-passed 200, no fatal):
- Retours-tab panel **"Synthèse — 90 j"**.
- Mon-tableau KPI tracker **#283** (slug `returns_synthese`, viz `'bar'`, mig **380**, `data_ready=1`, Opus-verified payload; handler `kpi_logi_returns_synthese` in `kpi-handlers.php`).

Live pending backlog = **414 beer-credit lines** (includes rebates to dismiss + real returns to triage). MIG HEAD now **380**. NO tour-steward (panel inside already-toured expeditions; widget isn't a page).

**P2b — BURN-MODEL LATENT BUG (OPEN, low urgency).** The seasonal-burn engine's blanket `−SUM(all doc_types)` over-nets beer-credit `rebate`s into velocity (a price-rebate credit is not a physical return-in, so it shouldn't relieve burn). Low urgency; flag when touching `app/seasonal-burn.php`.

**RECONCILIATION GUARD (unchanged, still stands):** the burn engine already nets BC `credit`/`return_receipt` rows into velocity. The `ord_returns` table is the OPERATIONAL/physical-disposition surface; it must NOT double-count against the BC ledger rows — which is exactly why the restock leg is FG-only and the app writes NO revenue/COGS-ledger entry (BC owns that).

**GIT — 4 LOCAL COMMITS NOT PUSHED + 2 FRONTEND FILES DEFERRED-FROM-COMMIT (held for Kouros review + two-session merge):**
- `e7e0fca` — mig 373 + 376 (schema).
- `23c791b` — queue UI (P1). (NB: shared with the concurrent finance_viewer build — parallel-session sharding.)
- `0867a61` — P2 restock leg.
- `11d506b` — P3 synthèse **BACKEND** (`app/returns-synthese.php`, mig 380, kpi-handlers handler/seed).
- All 4 LOCAL on the VPS clone, NOT on origin. Concurrent-session churn on the shared clone (orphaned `222cbb9`; a shared-index race, since cleaned).
- 🔴 **`public/modules/expeditions.php` + `public/css/expeditions.css` are DEFERRED FROM COMMIT** — they carry THIS session's P1 queue + P3 synthèse-panel changes INTERMINGLED with the CONCURRENT session's repack-helper-hoist + 11-tab-wrap refactor; committing would STEAL parallel work. They are **DEPLOYED + smoke-passed on the VPS (md5 parity)** — **the VPS copy is the frontend source of truth until the two-session merge reconciles them.**
- **PM RECOMMENDATION (STRONG, recorded): do the two-session merge from a SEPARATE WORKTREE.** The SHARED CLONE is the root cause of the churn (orphaned `222cbb9`, the shared-index race, and now this expeditions.php/.css intermingling). Reinforces the standing separate-worktrees + commit-by-pathspec rules.

**OPEN ITEMS — STATUS as of 2026-06-16 (PART A/B/C ALL THREE BACKENDS SHIPPED+PUSHED+Opus-verified; see §PART A/B/C AS-BUILT below):**
- (a) **P3-RATE — SHIPPED `e76e21a`** (returns RATE block added to `returns_synthese()`). No longer deferred.
- (b) **P2b — burn-rebate over-net fix SHIPPED `5cd9788`** (rebate-disposition NOT-EXISTS guard, 3 netting sites). No longer open. LATENT (0 rebates dispositioned today) but byte-verified.
- **Newly OPEN (deferred UI/KPI wiring only — see §DEFERRED WIRING below):** (i) register an OVERSELL KPI tracker (mirror #283); (ii) surface returns-RATE on the Retours synthèse panel + oversell short-list on the Couverture drilldown. Both BLOCKED on the concurrent topbar-categories session holding `kpi-handlers.php`/`expeditions.css`/`mon-tableau.css`/`kpi-charts.js` dirty. Mechanical wiring atop existing computes; wire once that session commits.

**EQUIP for any follow-up:** sql + coder + ui + webapp-testing.

## ✅ PART A/B/C — AS-BUILT 2026-06-16 (ALL THREE BACKENDS SHIPPED + PUSHED + Opus-verified; only UI/KPI WIRING deferred)

**All three backends SHIPPED + PUSHED + Opus-verified 2026-06-16. The SPEC below (retained as the architecture record) was followed; ONE spec deviation noted on Part B.**

- **PART A — burn-rebate over-net fix: `5cd9788`.** `app/seasonal-burn.php` — `NOT EXISTS (disposition='rebate')` guard joined on `bc_document_no + sku`, applied at all **3** netting sites (per spec). LATENT today (0 rebates dispositioned). Byte-identical verified.
- **PART B — returns RATE: `e76e21a`.** Added a `'rate'` key to `returns_synthese()` (EXTENDED the fn per spec, no new fn). Trailing-365d. **Numerator = returned units (rebate EXCLUDED)** ÷ **denominator = gross BC sales `doc_type IN ('shipment','invoice')` beer SKUs.** Per-beer + per-channel (`trade_channel` → on_trade / off_trade / non classé). Carries `basis_count` for honesty (so a fake-low rate from un-triaged backlog reads as low-N). Verified sold=222792; 2 units→on_trade. **🔴 SPEC DEVIATION (correct, recorded): `inv_sales_ledger` has NO `sale_class` column** — so the spec's `sale_class='customs_artifact'` exclusion was DROPPED. No such doc_type exists anyway; `inv_sales_ledger.doc_type` values are `shipment / credit / invoice / return_receipt` (NB: NOT `customs_artifact`). The customs-artifact channel-hygiene line in the spec/Part B denominator is **moot — disregard it.** (`sale_class` lives on `ref_customers`, not the ledger; the burn-engine "customs_artifact exclusion" precedent does not transfer to ledger-grain sales queries.)
- **PART C — OVERSELL / ATP-breach: `e80648d` (+ mig 384).** Built per spec Q1-Q4 AS A COMPLETE UNIT — including Q4's snapshot table (spec had flagged Q4 as a separate Kouros-gated follow-on; it was built now, deployed DISABLED, so no scope creep — the cron is inert until the operator enables it).
  - **mig 384 `ops_oversell_snapshot`** — UNIQUE(date, sku) + `schema_meta` (source/allowed classification). MIG HEAD now 384.
  - **`app/oversell.php::oversell_current()`** — READS `fg_stock_compute()` `flag_survendu` (NO parallel calc — the divergence guard honored exactly); attributes shortfall to channel **proportional to open-commitment share**, using fg-stock Step 7's exact open-order gates (`status NOT IN('shipped','cancelled') AND line_status='to_fulfil'`).
  - **`scripts/snapshot-oversell.php`** — idempotent, flock'd.
  - **`db/cron/maltytask-oversell-snapshot.cron`** — DEPLOYED DISABLED.
  - **Opus-verified:** 5 breaches today (111 units short, 98.9% on-trade); oversold set == `flag_survendu` set; channel attribution sums exact; snapshot idempotent.

**CUSTOMER backfill (2026-06-16):** 20 active customers → `trade_channel='on_trade'`. **🔴 DELIBERATE NULL: Brasserie du Chien Bleu (id 385) left `trade_channel=NULL` — it is a CONTRACT-BREWING PARTNER, not a sales channel.** ROADMAP FLAG (PM): contract-brewing returns may warrant EXCLUSION from return-rate AND oversell entirely — different dynamics (contract logistics, not event over-booking). Revisit as a future modeling decision; do NOT let contract-brewing volume pollute the on/off-trade return-rate or ATP buckets. (Consistent with the existing `ref_clients` vs `ref_customers` separation — Chien Bleu is a `ref_clients` contract company that also has a `ref_customers` row.)

### 🔴 DEFERRED WIRING (mechanical, blocked on a concurrent session — wire once it commits)
The concurrent **topbar-categories** session holds dirty: `kpi-handlers.php`, `expeditions.css`, `mon-tableau.css`, `kpi-charts.js`. Both deferred items touch those files, so they wait (avoid the shared-tree intermingling that bit the synthèse frontend commit):
- **(a)** register an OVERSELL KPI tracker — mirror #283; logistics-domain handler `kpi_logi_*` in `kpi-handlers.php`; viz bar; reads `oversell_current()` (already built, just needs the handler + seed + viz wiring).
- **(b)** surface the returns-RATE on the Retours synthèse panel + an oversell short-list on the Couverture drilldown — both atop existing computes (`returns_synthese()['rate']` + `oversell_current()`).
Both are pure wiring atop computes that already ship. PM will wire when the topbar session commits + the shared tree is clean (or do it from a SEPARATE WORKTREE — standing rec).

### 🟡 OPERATOR ACTIONS STILL PENDING
- (i) **Enable the oversell cron** (`maltytask-oversell-snapshot.cron`) after review — it ships DISABLED.
- (ii) **Triage the ~413 return backlog** (beer-credit lines: rebates to dismiss + real returns to disposition). Until triaged, the return-RATE reads ~0 with low `basis_count` (honest, not a bug). NB: P2b burn-rebate guard + the return-RATE numerator both stay latent until rebates start getting dispositioned.

## 🆕 PART A/B/C BUILD SPEC — burn-rebate fix · returns RATE · OVERSELL/ATP tracker (PM architecture read 2026-06-16, Kouros-approved 3-part build; live-probed) — SPEC RETAINED; see AS-BUILT above for what shipped + the Part B sale_class deviation

**LEDGER FACTS (live-verified 2026-06-16, the SoT for all 3 parts):**
- `inv_sales_ledger.doc_type` ∈ {`shipment` (n=31543, qty_signed mostly NEG = depletion), `invoice` (n=6329, mostly NEG), `credit` (n=6619, mostly POS = reversal), `return_receipt` (n=18, mostly POS)}. **shipment & invoice are DISJOINT document populations (0 bc_document_no overlap at sku grain) — burn summing both does NOT double-count.**
- **SALES (depletion) doc_types = `shipment` + `invoice`** (negative qty_signed). **CREDIT/RETURN doc_types = `credit` + `return_receipt`** (positive). `qty_signed` IS the canonical unit basis (signed, SKU units).
- **BC gross-sold units (Part B denominator) = `GREATEST(0, -SUM(qty_signed)) WHERE doc_type IN ('shipment','invoice')`** for the same SKU population. (The burn engine's blanket all-doc_type sum is NET, not gross — for a RATE denominator you want GROSS sales, NOT net-of-returns.)
- **Returns numerator population (canonical, from returns-synthese.php pending_count, lines 100-114):** `doc_type IN ('credit','return_receipt') AND qty_signed>0 AND sku_id_fk IS NOT NULL AND ref_skus.recipe_id IS NOT NULL` (beer-only — same `recipe_id NOT NULL` beer filter the arc already uses). Matches the dispositioned-units population exactly.

**LEDGER↔RETURN_LINE LINKAGE (canonical, no cleaner key exists):** `ord_returns.origin_bc_document_no = inv_sales_ledger.bc_document_no` AND `ord_return_lines.sku_id_fk = inv_sales_ledger.sku_id_fk`. There is NO direct ledger-row FK on ord_return_lines (it carries `origin_ledger_sku_code` VARCHAR for display only, NOT an id FK). `ord_returns` DOES carry `origin_order_id_fk`/`origin_sales_order_id_fk` but those point at ord_orders/inv_sales_orders, NOT the ledger. **So bc_document_no + sku_id_fk IS the right and only join key** — it is exactly what returns-synthese.php pending_count already uses (precedent set). Confirm dispatch agent reuses that idiom, doesn't invent a new join.

**CUSTOMER-TYPE CLASSIFICATION (the B3/C2 "don't guess" answer — DECISIVE):**
- `ref_customers.trade_channel` ENUM('on_trade','off_trade') — **THIS is the canonical event/on-premise vs retail field. on_trade = bars/restos/clubs/theatres/events (HoReCa); off_trade = retail/supermarkets (Manor/Coop/Aligro).** Verified by customer-name sampling.
- **BUT only ~23% populated overall (622 on_trade + 64 off_trade + 2375 NULL of 3061).** On the OPEN-ORDER book it's better: 53 on_trade + 6 off_trade + 17 NULL + 1 giveaway of 77.
- `ref_customers.sale_class` ENUM has an `'event'` value BUT it is **NOT meaningfully populated — 3055 of 3061 customers are 'b2b'**, 1 each of the rest. **sale_class does NOT distinguish event vs retail. DO NOT use it for the customer-type cut.** (It IS valid for the existing `customs_artifact` exclusion only.)
- **RULING: the customer-type cut = `ref_customers.trade_channel` (on_trade vs off_trade), with a 3rd explicit `NULL/inconnu` bucket — NEVER fold NULL into either side, NEVER derive channel from sale_class or guess from name.** eshop is mixed INTO on_trade (e.g. "La Nébuleuse - ESHOP (privé)") so trade_channel ≠ a clean eshop discriminator; eshop/taproom are their own depletion legs (inv_sales_orders / inv_sales_bc), not B2B orders. **KOUROS-DECISION NEEDED: is the on_trade≈event proxy good enough, or should we (i) backfill trade_channel as part of the recurring BC→ref_customers sync, or (ii) add a finer segment column? SURFACE — do not let Sonnet guess.** Until then, present the cut as "on-premise (on-trade) / retail (off-trade) / non classé" with the NULL count visible so the operator sees coverage.

**PART A — burn over-net rebate fix (`app/seasonal-burn.php`):**
- **3 netting sites, not 2:** `sb_family_weekly_series` (~L108-120), `sb_all_sku_levels` window query (~L434-450), AND its **lifetime-stats query (~L455-465)** also nets all doc_types (first_sale/last_sale/lived_full_summer). The lifetime query doesn't sum qty so a rebate row only affects first/last-sale dates marginally — but apply the SAME exclusion to all 3 for consistency.
- **FIX SHAPE (confirmed):** add to each `WHERE` a `NOT EXISTS` correlated subquery excluding ledger rows that are credit/return AND dispositioned 'rebate': `AND NOT EXISTS (SELECT 1 FROM ord_returns r JOIN ord_return_lines rl ON rl.return_id_fk=r.id WHERE r.origin_bc_document_no = l.bc_document_no AND rl.sku_id_fk = l.sku_id_fk AND rl.disposition='rebate')`. (bc_document_no+sku join — same key as everywhere.) Only credit/return rows can match a return_line so no need to also gate doc_type inside the subquery, but adding `AND l.doc_type IN ('credit','return_receipt')` to the NOT-EXISTS guard makes intent explicit and keeps the planner honest.
- **RISK TO `6228e8a` / cron: ZERO.** The seasonal-index cron (`/etc/cron.d/maltytask-seasonal-index`, currently DISABLED) only writes `kpi_sku_seasonal_index` (the seasonal SHAPE per recipe×iso_week). It reads the same series; excluding rebate lines only sharpens the depletion signal. No schema change, no cron change. The forward sim consumes the level — a slightly higher (less-netted-down) burn = slightly more conservative runway = the intended correction.
- **MAGNITUDE / URGENCY (live): currently ZERO rebate lines dispositioned, and only 1 return line total (the Albaladejo restock=2). The leak is LATENT — it bites once the operator starts dispositioning the 3405-line beer-credit backlog as 'rebate'.** So Part A is correct to fix but is not yet distorting anything. Low urgency; ship with B/C.
- **EQUIP: sql + coder + webapp-testing** (read-only ?view=stock smoke; verify burn numbers unchanged today since 0 rebates, and that a synthetic rebate row drops out of the series).

**PART B — returns RATE (board-spec: returned ÷ BC gross-sold, same SKU pop, trailing-12mo, per-beer + per-customer-type, disposition MIX still leads, CHF in finance view only):**
- **DECISION: EXTEND `returns_synthese()` with a `rate` block, do NOT create a separate fn.** Single-source the returns aggregates (the file's stated purpose, lines 4-6). But the rate block runs on a DIFFERENT window (12mo) than the mix (90d) — so the fn signature grows a 2nd param OR computes both windows internally. **Cleanest: keep `$periodDays` for mix/by_client/by_beer (90d default) AND add the rate block on a hardcoded trailing-365d window internally** (mirrors how pending_count already pins its own 180d window regardless of $periodDays — same precedent, lines 97-99). So: mix=90d, rate=12mo, both in one fn, each documenting its window. **Do NOT unify the windows** — they answer different questions (operational recency vs annualized rate) and the board asked for 12mo rate explicitly.
- **Numerator (12mo):** dispositioned returned units `restock+scrap+quarantine` (rebate EXCLUDED — it's non-physical) over trailing 365d, per beer (`ref_recipes.name` via `ref_skus.recipe_id`) and per customer-type (`trade_channel`). **CAVEAT: only 1 line dispositioned today → the rate is ~0 until the backlog is triaged. The rate is only meaningful once dispositioning catches up.** Surface that honestly (e.g. "basé sur N retours traités" + the pending backlog count) so the operator doesn't read a fake-low rate.
- **Denominator (12mo, MUST match numerator population):** `GREATEST(0,-SUM(qty_signed)) WHERE doc_type IN ('shipment','invoice') AND sku_id_fk IS NOT NULL AND ref_skus.recipe_id IS NOT NULL`, same trailing-365d, grouped per beer and per trade_channel. GROSS sales (not net). Exclude `sale_class='customs_artifact'` to match the burn-engine channel hygiene.
- **per-customer-type:** group both num and denom by `ref_customers.trade_channel` resolved via `inv_sales_ledger.customer_id_fk` (returns side: via the bc_document_no→ledger→customer hop already in by_client). 3 buckets: on_trade / off_trade / NULL(non classé). Show coverage.
- **CHF / contra-revenue: NOT in returns_synthese() — finance view only.** Do not blend CHF into the volume/rate panel. (BC owns revenue; the avoir already reverses it. Any CHF view reads BC, not this fn.)
- **KOUROS-DECISION: the 90d-mix vs 12mo-rate dual window on KPI #283 / the Retours panel** — confirm he wants BOTH shown (mix headline 90d + rate 12mo sub-block) vs unifying. PM rec = keep both (matches his memo: "disposition MIX still leading" + "rate trailing-12mo").
- **EQUIP: sql + coder + ui + webapp-testing.**

**PART C — OVERSELL / ATP-breach tracking (the big one):**
- **Q1 — `flag_survendu` ALREADY == an ATP breach.** Exact def `app/fg-stock.php` L955: `'flag_survendu' => $liveFutur < 0` where `live_futur = physique − open_total_qty` (L905/947). `physique` = anchor + prod(historical, since-anchor) − expedie − eshop − taproom + repack + restock (on-hand FG). `open_total_qty` = `SUM(ord_order_lines.qty) JOIN ord_orders WHERE status NOT IN('shipped','cancelled') AND line_status='to_fulfil'` (Step 7, L702-712). **So flag_survendu = "committed open orders exceed on-hand FG" = ATP breach WITHOUT a scheduled-production term.** The board's ATP = on-hand + scheduled-production − committed; the current flag is the STRICTER on-hand−committed (no future-prod credit). There is NO scheduled/future-production input in fg_stock_compute today (prod is historical-since-anchor only, already in physique). **So we do NOT build a parallel oversell calc — flag_survendu IS it.** Magnitude = `-live_futur` (units short) when negative; attribution comes from the open_book per-SKU (Step 7 `$openBookBySku`, has per-order date/qty/status — extend to carry customer trade_channel).
- **Q2 — attribute oversell to EVENTS:** open orders ARE classifiable — `ord_orders.customer_id_fk → ref_customers.trade_channel`. Same answer as B3: `trade_channel` on_trade≈event/on-premise, off_trade=retail, NULL=non classé. On the open book it's 53/6/17/1 of 77 — usable but with a NULL bucket. **Same KOUROS-DECISION as B3 (is on_trade≈event good enough / backfill / finer column). Do NOT guess.** `ord_orders.internal_channel` (taproom/eshop/cage/shop) is only for `order_type='internal'` — all 77 open orders are `order_type='customer'` so internal_channel is NULL throughout; not the discriminator.
- **Q3 — WHERE oversell lives (cleanest, reusable):** **(a) a new KPI tracker reading the EXISTING `fg_stock_compute()` output (like #283) — YES, primary.** The engine already emits per-SKU `flag_survendu`, `live_futur`, `open_total_qty`, `couverture.open_book`. A new handler `kpi_logi_oversell` (or similar) calls `fg_stock_compute()`, counts `flag_survendu` SKUs, sums `-live_futur` (units short), and buckets the open_book by trade_channel. NO new compute path. **(b) a panel on the existing Couverture drilldown — secondary, cheap (the data is already in the couverture payload).** **Build (a) the KPI tracker as the headline; optionally surface the per-SKU short list on the Couverture face.** Do NOT add a 2nd derivation — read fg_stock_compute, period.
- **Q4 — HISTORICAL capture: NONE EXISTS. The engine is a pure current/forward snapshot.** `kpi_sku_seasonal_index` stores only the seasonal multiplier (recipe×iso_week×index), NOT any survendu/ATP outcome over time. There is NO table recording past oversell incidents. **To track "how often we oversold in high season" you MUST start snapshotting — that is a NEW, SEPARATE concern (a daily/weekly `ops_oversell_snapshot`-type table: date, sku_id_fk, live_futur, units_short, on_trade_short, off_trade_short).** A flock'd daily PHP cron calling fg_stock_compute() and persisting the negative-ATP rows (clone the credential-expiry / seasonal-index cron pattern, deployed DISABLED). **THIS IS A KOUROS SCOPE DECISION: the live tracker (Q3a) ships now off existing output; the time-series (snapshotting for "frequency in high season") is a distinct follow-on build — confirm he wants it in this arc or as a separate one.** PM rec: ship the live tracker now; propose the snapshot table as a fast-follow once he confirms he wants the historical frequency metric.
- **DIVERGENCE GUARD: there is exactly ONE FG/ATP derivation (`fg_stock_compute`). Any oversell tracker/panel/snapshot MUST read it — never recompute open-orders-vs-stock independently (that's the parallel-store anti-pattern).** The Step-7 open-order filter (`status NOT IN('shipped','cancelled') AND line_status='to_fulfil'`) is the canonical "committed" definition — reuse it, don't redefine.
- **EQUIP: sql + coder + ui + webapp-testing** (+ a separate sql+coder dispatch for the snapshot table/cron IF Kouros greenlights Q4).


---

## 📋 DEFERRED BACKLOG — operator-confirmed-WANTED, not abandoned (confirmed 2026-06-16; pick up later)
> These are NOT active tasks. The operator explicitly confirmed they are wanted (so they survive a chat clear), but they are low-priority / gated. Prioritized; each carries its gate + architecture verdict. Do NOT build unprompted — surface when the gate clears or when Kouros re-prioritizes.

**[BL-FULF-1] eshop bundle decomposition — "2×" quantity-multiplier edge + the 100%-off promo line.** The variant-bridge (`ref_sku_shopify_variants`) models **1 Shopify variant → 1 SKU**; it does NOT model (a) a quantity multiplier (e.g. "2× Pack de Noël" = one line, qty-mult 2) nor (b) a 100%-off promo/gift line that carries NO variant. Both surface in the **visible unresolved tail** (refuse-don't-NULL holds — nothing silently mis-decomposes), so this is a COMPLETENESS gap, not a correctness hazard. **LOW volume.** Architecture verdict: extends the existing variant-bridge + the bundle→(recipe,container) decomposition already shipped in this arc — no new table, no parallel store; a qty-mult column on the bridge resolution + a promo/zero-price line classifier. Belongs squarely in the Shopify-fulfilment decomposition lane. Gate: none hard — pick up when the unresolved tail accumulates enough of these to matter. EQUIP sql+coder(+ui if the tail surface changes).

**[BL-FULF-2] C3 — operator self-service variant-mapping UI.** Today an unknown Shopify variant lands in the **visible tail** and gets seeded into `ref_sku_shopify_variants` **manually** (direct SQL). A small admin UI (list unmapped variants → pick a `ref_skus` row → write the bridge row) closes the loop without an SQL round-trip. **Convenience, not blocking** — the manual seed path works and the tail keeps it honest. Architecture verdict: pure CRUD over `ref_sku_shopify_variants` with a `ref_skus` whitelist (refuse-don't-NULL — no free-text SKU); add a `ref_pages` row + preset grant + RULE-3 tour card on ship. Gate: none; ship when variant volume makes manual seeding annoying. EQUIP sql+coder+ui+webapp-testing.

## ✅ §RETURNS REVERSAL POLLUTION FIX — SHIPPED 2026-06-16 (`5c911b7`, pushed origin/main)

**AS-BUILT (landed 2026-06-16, commit `5c911b7`, pushed; canaries Opus-verified on VPS, not agent-reported):**
- **Mig 387 `387_v_physical_returns.sql`** (applied; mig HEAD now 388 — 388 = parallel `388_smoketest_manager_bot` session, ignore). VIEW body = `SELECT l.* FROM inv_sales_ledger l WHERE doc_type IN ('credit','return_receipt') AND qty_signed>0 AND sku_id_fk IS NOT NULL`, with TWO `NOT EXISTS` exclusions — (a) same-day booking reversals: a shipment/invoice line for same `customer_id_fk` + `sku_id_fk` on the same `posting_date`; (b) rebate-tagged lines via `ord_return_lines.disposition='rebate'`. Supporting composite index **`ix_isl_revcheck (customer_id_fk, sku_id_fk, posting_date, doc_type)`**. schema_meta row added. **VERIFIED LIVE: view = 1 505 physical-return lines; schema_meta=1; index present.**
- **`app/returns-synthese.php`**: `'rate'` numerator AND `'overship'` watchlist numerator repointed to `v_physical_returns`. **`mix` / `by_client` / `by_beer` / `pending_count` LEFT UNCHANGED** — `pending_count` intentionally NOT view-backed yet, kept consistent with the disposition queue (still deferred behind the parallel "set-site" expeditions.php session). NB: this means the view did NOT yet subsume the rebate copy-paste at by_client/by_beer/mix (RULE-1 scrap deferred with the disposition-queue batch below).
- **CANARIES (Opus-verified VPS):** return rate corrected **6.60% → 1.818%** (returned_units 14 712 → 4 050); **FWT Management SA dropped off the over-ship watchlist** (was rank ~6 @ 82.3%); overship watchlist now **25 customers over the 12-unit floor**, top over-shippers are festivals/events — Assoc Trace-Ecart 92.9%, Saubrazil 77.8%, Paléo 69.3%, Blues en scène (1112 shipped/542 ret) 48.7%, Forget Yesterday (1139/520) 45.7% — predominantly `trade_channel='non classé'`, confirming refinement #1 (bulk-tag ~40 event customers `on_trade`) is the next data-cleanup.

**🔑 DURABLE KEY FINDING (re-stated as confirmed):** same-day customer+SKU booking reversals are an **ONGOING BC workflow** (BC posts shipment → credits it → re-invoices), **~72% of raw beer "returns"**, sourced from the recurring "Écritures comptables article.xlsx" export. `doc_type_raw` cannot distinguish them. **RE-1 follow-on:** add `is_reversal`/`reversed_doc_no` flag at BC-API ingest (deterministic pairing), then flip the view body `NOT EXISTS`-on-same-day → `is_reversal=0`, ZERO consumer change. (This follow-on also lives in the RE-1 BC-API arc.)

**🟡 STILL DEFERRED (behind parallel set-site `expeditions.php` session — repoint as ONE batch so count↔queue stay consistent):** (a) disposition queue render (~L2599); (b) processed list (~L2649); (c) POST handler reversal-guard (~L1532, the critical hazard — an operator restocking a reversal line would feed Leg 4 phantom FG); (d) synthèse `pending_count` → `v_physical_returns`. Plus UI wiring (oversell KPI tracker, returns-rate panel, oversell short-list on Couverture). When this batch lands, also collapse the by_client/by_beer/mix rebate copy-paste into the view (RULE-1 scrap, currently deferred).

---

## 🔴 §RETURNS REVERSAL POLLUTION FIX — ORIGINAL PM SPEC 2026-06-16 (retained for the rationale; build LANDED above)

**FINDING (verified live, 365d, beer SKUs `rs.recipe_id IS NOT NULL`).** The returns RATE block (`returns-synthese.php` `e76e21a`), the over-ship watchlist, the synthèse `pending_count`, AND the P1 disposition queue in `expeditions.php` count GROSS `credit`/`return_receipt` ledger lines as physical returns. **72.5% are SAME-DAY BOOKING REVERSALS** — a credit posted same day as a shipment/invoice to the same customer for the same SKU (BC posts shipment(−N) → credits it(+N) → re-invoices).
- GROSS returns 365d = **14 712 units / 800 lines**; SAME-DAY reversals = **10 662 units / 468 lines** (72.5%).
- FWT Management: 926 gross "returns" = **926 same-day reversals**, ~9 real.
- CORRECTED overall rate = **1.82%** (real 4 050 / sold 222 792) vs polluted 6.6%.
- **Reversals are an ONGOING BC workflow** — same-day-reversal lines spread continuously across EVERY posting month 2025-06→2026-06 (10–80/mo). The `imported_at` clustering on 2026-06-10 is just the bulk historical-load date; posting_date distribution is what matters and it's continuous. ⇒ a read-time filter is durable AND every future BC pull stays clean *as long as consumers go through the canonical accessor*.

**🔴 `bc_source_no` does NOT work as the reversal key** (corrects the finding's premise). On these rows `bc_source_no` holds a CUSTOMER/source number (3660, 2161, 1293…), NOT the reversed sales-doc number; 0 credits join to a shipment `bc_document_no` via it. The reliable signal is the same-day cust+sku+opposing-sign match. Do NOT build the filter on `bc_source_no` — it would match nothing and exclude zero reversals.

**PM RULINGS (the 5 questions):**
1. **Canonical home = SQL VIEW `v_physical_returns`**, NOT a PHP-helper-returning-a-predicate-string. The predicate is needed in raw SQL across 6+ sites with different aliases (`l.`/`cr.`/`l2.`); a view is the callable canonical accessor for SQL consumers ([call-the-accessor-not-copy] rule). `maltytask_app` HAS `CREATE VIEW`/`SHOW VIEW` (granted 2026-05-26). The view bakes in BOTH the reversal-exclusion AND the existing rebate `disposition='rebate'` NOT-EXISTS, subsuming the current copy-paste of the rebate predicate across ~5 synthese sites + 2 expeditions sites = RULE-1 scrap win. +`schema_meta` row (derived_view, read-only). Optionally a sibling `v_sold_beer_units` for the denominator (symmetry; low cost). Reversal predicate (verified 4050 kept / 10662 excluded):
   ```sql
   AND NOT EXISTS (SELECT 1 FROM inv_sales_ledger sh
     WHERE sh.doc_type IN ('shipment','invoice')
       AND sh.posting_date=l.posting_date AND sh.customer_id_fk=l.customer_id_fk
       AND sh.sku_id_fk=l.sku_id_fk AND sh.qty_signed<0)
   ```
2. **Read-time VIEW now (durable) + ingest-flag in RE-1 (follow-on).** Now: the view IS the durable fix — fast (composite idx `(posting_date,customer_id_fk,sku_id_fk,doc_type)`, EXPLAIN-verify), safe, zero-backfill, self-sustaining. RE-1 (BC API auto-pull): add `inv_sales_ledger.is_reversal TINYINT` + `reversed_doc_no VARCHAR`, stamp at load (ingest has both rows of a doc in hand → deterministic pairing, better than the same-day heuristic). At that point the view body flips `NOT EXISTS`→`WHERE is_reversal=0` and NO consumer changes — that's the payoff of the view. Record as RE-1 follow-on; don't block today's fix.
3. **BURN/`flag_survendu` SAFE — leave alone, do NOT route through the view.** `seasonal-burn.php` L108–126 `GREATEST(0,-SUM(qty_signed))` nets a same-day −N/+N pair to 0 (self-correcting). Burn wants NET physical depletion (legitimately includes real returns adding stock back); the view is a GROSS physical-return lens — different question, must not be unified (same rationale as the code's existing divergence-from-`v_sales_ledger_order_lines` comment). `fg_stock_compute` reads NO return doc_types → safe alone. **BUT the P1 disposition POST resolver (`expeditions.php` ~L1532) is the REAL hazard**: a reversal sits in the queue today; if an operator dispositions it `restock`, Leg 4 adds phantom kegs to physique that were never returned → real FG corruption. ⇒ the canonical filter MUST reach L1532 (the POST that feeds Leg 4), not only the L2599 render. This upgrades the queue fix from cosmetic to load-bearing.
4. **DUP sale-doc — NOT applicable to the beer rate; drop from this build.** The "436 groups / 8 060 units" is almost entirely NULL-`sku_id_fk` / non-beer rows, already excluded by `rs.recipe_id IS NOT NULL`. Beer-only excess = **20 groups / ~43 units**, and those are NOT clones (distinct `bc_line_seq` + distinct `sales_amount_chf`, e.g. a chf=0 pick line + a chf=97.80 priced line under one EXPED doc) = legitimate BC multi-line structure. A read `DISTINCT` would FALSE-REJECT real lines (the `inv_deliveries` coarse-UNIQUE lesson). Do NOT dedup. 43u on a 222 792 base = rounding error.
5. **No pre-existing returns view to extend.** `returns_synthese()` is the only aggregation home; the rebate NOT-EXISTS is copy-pasted across ~5 synthese sites + 2 expeditions queue sites — the view subsumes that duplication.

**BUILD SPEC.** Migration (next free; re-`migrate.php --status`): (1) `CREATE VIEW v_physical_returns` (beer credit/return_receipt, qty_signed>0, recipe_id NOT NULL, sku_id_fk NOT NULL, minus same-day-reversal NOT-EXISTS, minus rebate-disposition NOT-EXISTS; expose id/posting_date/bc_document_no/customer_id_fk/sku_id_fk/qty_signed/recipe_id); (2) composite idx above (INPLACE; no IF NOT EXISTS on the ALTER); (3) schema_meta row(s). Code: `returns-synthese.php` repoint rate numerator + per-beer-num + per-channel-num + overship returned_units + the `pending_count` ledger-side query (L103) at the view (remove now-redundant inline rebate NOT-EXISTS; denominators stay base-table shipment/invoice). `expeditions.php` route ALL three ledger-return sites through the view — P1 queue render (~L2599), **P1 disposition POST resolver (~L1532, critical)**, processed list (~L2649). `seasonal-burn.php`/`fg_stock_compute` NO CHANGE (+1-line comment noting reversals intentionally retained in net-depletion, excluded only from the gross-return lens).

**VERIFY (Opus independently — COGS/FG-adjacent, don't trust agent gate-PASS):** view unit-sum == 4 050 real / overall rate 1.82% (4050/222792); 10 662 fewer than gross; FWT through view ~9 (not 926); P1 queue count ~413→~110–130; burn `sb_forward_sim` byte-identical before/after on a sample recipe; POST resolver REFUSES a synthetic reversal line (un-restockable); idempotent re-run; `php -l` clean; md5 local↔VPS each deployed file; `?view=retours` 200 + synthèse panel renders corrected rate.

**KOUROS-DECISIONS:** (a) view the denominator too (rec: yes, symmetry) or inline; (b) approve RE-1 ingest-flag intent now, build at BC-API time (rec); (c) confirm the ~413→~130 queue shrink is expected (reversals were never physical returns → correct to vanish).

**EQUIP: sql + coder + ui + webapp-testing.**
