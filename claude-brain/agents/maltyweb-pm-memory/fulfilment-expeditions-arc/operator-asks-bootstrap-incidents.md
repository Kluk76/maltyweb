# Fulfilment — Operator asks, known gaps/divergence-watch, CRM bootstrap, WeeklyOrders ingestion, data/name corrections, go-live, incidents

## ✅ TWO OPERATOR ASKS — BOTH SHIPPED + LIVE 2026-06-12 (was pre-build; now done, committed, Playwright-smoke-verified)

> **STATUS: DONE.** Both asks shipped, committed, live this session. ASK 1 = render-only eshop line fix (`title` + "matériel" badge) + "Dernier import Shopify" freshness chip, in the expeditions.php change set this session (render-only, NO schema, NO stock effect, T7 stays out of `ref_skus`). ASK 2 = Shopify ingest REHOMED to the VPS per the Option-2 ruling (§ below): new cron `5ade521`, VPS cron is now the SOLE runner, local-WSL `*/15` crontab RETIRED. CONFIRMED firing autonomously (11:00 UTC tick wrote the log, idempotent no-op). The two lessons reinforced: (a) PM caught the "VPS has no Node" login-PATH false-negative; (b) an agent's "byte-identical deployed" claim was false (fg-stock.php stale) — both → [[feedback_verify_ground_truth_after_handoff]]. STILL-OPEN follow-ups (unchanged, NOT part of these asks): 06-15 repack-depletion flip set (cage option in form-packaging.php, flag flip, base→cage retarget), operator alias-seeding (~10 codes incl PACKDEC/PD8 recipe-6 slot), Phase-2 cage/box COGS valuation. Pre-build probe detail retained below for provenance.

### ASK 1 — Tap-machine (T7) PICKUP orders must surface on the Commandes board ✅ SHIPPED
**AS BUILT (render-only, this session's expeditions.php change set):** eshop line render now reads `inv_sales_order_lines.title` (pill fallback `sku_code ?? title ?? '?'`) with a "matériel" badge when `sku_id_fk IS NULL` — 65× "Tireuse Portable" + 65× "Caution" now show readably instead of `?×1`. NO schema, NO ingest change, NO stock effect; T7 stays OUT of `ref_skus`. Playwright-smoke-verified. Pre-build probe below for provenance.
**LIVE-VERIFIED render behaviour (expeditions.php Commandes / `exp_render_eshop_row`):**
- Eshop order HEADER comes from `inv_sales_orders` (Query 3a, L1715) with **NO SKU-existence filter** → an order whose lines don't resolve to a SKU **IS shown** (header, customer, 🏬/🚚/⚠ badge, Shopify tag). The order is NOT dropped.
- Lines (Query 3b, L1733) = `inv_sales_order_lines isol LEFT JOIN ref_skus s` → NULL `sku_id_fk` keeps the row; render pill = **`isol.sku_code` (raw), falling back to `'?'`** when `sku_code` itself is NULL; family colour = `exp_format_family('')` (NULL `s.format`). **The line is shown but uninformative.**
- 🔑 **ROOT GAP (the load-bearing finding):** the ingest (`scripts/ingest-shopify-orders.ts` L317) maps **Shopify's SKU field → `sku_code`** and the **line TITLE → a SEPARATE `title` column** (L327). Tap-machine lines have a BLANK Shopify SKU → `sku_code=NULL` → render as bare **`?×1`**, EVEN THOUGH the descriptive `title` ("Tireuse Portable", "Caution") IS persisted in `inv_sales_order_lines.title` — **the render NEVER reads `title`.** Live: 65× "Tireuse Portable" + 65× "Caution" + Gobelets/Pack-de-Noël/Beer-des-Rosses/Carte-cadeau all sit in `title` with `sku_code=NULL` (263 NULL-sku lines / 2646 qty total). **NO order carries a "Tireuse"/"TAP"/"T7" sku_code** — they are ALL title-only.
- **DERIVATION-CORRECT FIX (no phantom SKU, no stock touch):** render pill fallback chain = `sku_code ?? title ?? '?'`; when `sku_id_fk IS NULL` add a **"non-stock / matériel" badge** on the pill (greyed, distinct from beer families). T7 stays OUT of `ref_skus` (correct — it's non-stock equipment), so it never enters `fg_stock_compute()` and never depletes. This is RENDER-ONLY (expeditions.php + .css), NO mig, NO ingest change (title already captured). The Phase-3c `ref_skus.is_non_stock` flag (mig 323) is INERT here — it only matters for SKUs that EXIST in ref_skus; T7 isn't one, so the discriminator is simply "`sku_id_fk IS NULL` → non-stock line". Reuse that predicate, don't invent a parallel flag.
- **Re "③ tap machine soon integrated as Shopify sales":** that note is the SALES/COGS lane (does a tap-machine sale post revenue / become a sellable line), NOT FG-stock. It does NOT change the fulfilment surface: even if T7 becomes a revenue line later, it stays NON-STOCK equipment (no liquid/packaging BOM, no FG depletion). Fulfilment-only display is the right scope now; a sellable-T7 path is a separate later question (would touch `inv_sales_ledger` sale_class + possibly a non-FG `ref_*` equipment catalog, NOT `ref_skus` inventory). Surface for fulfilment now; do NOT pre-build a sellable path.
- **VERDICT: BUILD (no Kouros decision needed) — render-only, SOT-safe, no phantom SKU, no depletion.** Only judgement call = the badge label ("matériel" vs "non-stock" vs "retrait matériel") — a copy choice, fine to pick + show Kouros. **EQUIP: ui + coder + webapp-testing** (sql NOT needed — zero schema/query change; the `title` column is already selected if we add it to Query 3b, which is a one-column add, still no schema). NB: add `isol.title` to the Query-3b SELECT (it's not currently selected) — that's the only backend touch.

### ASK 2 — "Several times a day" Expéditions refresh from Shopify ✅ SHIPPED (VPS-REHOMED)
**AS BUILT (Option-2 ruling executed — see §SHOPIFY-INGEST VPS REHOME below):** `/opt/maltytask-pipeline` synced to current HEAD via `bin/deploy.sh --apply-pipeline` (the 05-22 stale copy GONE — ingest now md5-matches local, has shipping_lines/fulfilment_mode capture; npm ci ran); `secrets/shopify.env` on VPS at `/opt/maltytask-pipeline/secrets/shopify.env` (mode 640, maltytask:www-data); new cron `db/cron/maltytask-shopify-ingest.cron` (maltyweb repo = source-of-truth) → `/etc/cron.d/maltytask-shopify-ingest`, maltytask user, nvm idiom (mirrors document-ingest), DB via db.env (127.0.0.1:3306, NO tunnel). **Commit `5ade521`.** Validated: VPS dry-run + a real run freshened the 2-day-stale board (latest import seconds-old, 3164 eshop orders); **cron CONFIRMED firing autonomously** (11:00 UTC tick wrote the log, idempotent no-op, no new orders in window). **The fragile local-WSL tunnel crontab is RETIRED — VPS cron is the sole runner, independent of the operator's machine.** Freshness chip ("Dernier import Shopify") shipped on the board EITHER-WAY (the staleness sentinel that justified the rehome). Pre-build probe below for provenance.

> ✅ **SHOPIFY INGEST CADENCE BUMP `*/15`→`*/1` + flock — SHIPPED+VERIFIED LIVE 2026-06-17 (PM-approved earlier same day; mirrors the BC-orders `*/1` ruling at line ~1061).** `db/cron/maltytask-shopify-ingest.cron`: cadence `*/15`→`*/1`, command WRAPPED in `flock -n /tmp/maltytask-shopify-ingest.lock bash -c '…'` (flock execs bash DIRECTLY — no nested `-c` quote collision; `HOME`/`NVM_DIR` exported explicitly INSIDE the `bash -c` since the cron env is bare; comment block updated mirroring the bc-orders cron). 🔴 **MANDATORY flock single-flight reasoning (same as BC-orders `*/1`): at 1-min cadence a slow run (token refresh + 2-day trailing window) can exceed 60s → two ingests race the same line-`row_hash` UPSERT; `-n` skips if a run is still going, next minute catches up.** Idempotency already proven (line `row_hash` UNIQUE + `INSERT IGNORE`, header upsert keyed `(source, external_order_id)`) so flock is belt-and-braces, not the sole guard. **Committed maltyweb `cdb19a5` (SINGLE file by pathspec — the dirty parallel-session files expeditions.php/kpi-handlers.php/mig392 were NOT swept in; pathspec-commit discipline honored). NOT pushed yet.** **Deployed SURGICALLY** (scp + `sudo install -m 0644 -o root -g root` → `/etc/cron.d/maltytask-shopify-ingest`) because the shared tree was dirty under a parallel session — did NOT run `bin/deploy.sh --apply` (would have pushed the foreign dirty tree). **VERIFIED LIVE:** cron RELOAD logged 08:38 UTC, fires every minute (08:38/08:39/08:40) each with the new flock command; `/var/log/maltytask/shopify-ingest.log` updates each cycle; exact-command-shape dry-run succeeded first. **🔴 STILL-OPEN follow-up (PM-flagged, NOT done): the "Dernier import Shopify" freshness chip threshold is HARDCODED 90 min** (`expeditions.php` ~L4527) — too loose at 1-min cadence (90 min stale would mean ~90 missed runs before the chip even ambers). **PM rec = tighten to ~10–15 min AND move it into `system_settings 'fulfilment'`** (do NOT re-hardcode a new literal — same never-hardcode-a-threshold discipline as the lead-time `order_lead_time_*` keys already in that section). Left for a SEPARATE edit once 1-min cadence is proven stable. EQUIP for the chip follow-up: ui + coder + sql (settings key) + webapp-testing.

> ✅✅ **SHOPIFY IMPORT DATA-LOSS BUG FIXED + LIVE 2026-06-17 (maltytask `665b335`, PUSHED).** Operator's "l'import shopify ne marche pas" was a REAL data-loss bug, not just cron cadence. ROOT CAUSE `scripts/ingest-shopify-orders.ts` (deployed `/opt/maltytask-pipeline`): Shopify Orders API `created_at_max=${UNTIL}T00:00:00Z` where UNTIL defaulted to TODAY → upper bound = today **00:00 UTC** → **every order created during the current day silently excluded** (Mon/Tue fine because yesterday's orders land next day; the board showed only Mon+Tue). Confirmed 8 orders today on Shopify, 0 in `inv_sales_orders` pre-fix. FIX: default upper bound now = current INSTANT (full ISO timestamp, L60/L67); explicit `--until DATE` now includes the WHOLE named day (`T23:59:59Z`). Deployed surgically to `/opt/maltytask-pipeline/scripts/` (md5 parity, owner maltytask:www-data 644); live-ran to backfill today's 8 (#13638–#13645). `*/1` cron keeps today current near-real-time. maltyweb hotfix `6995146` (eshop hoist) + cron `cdb19a5` CONFIRMED on origin/main (auto-sync pushed). **🔴 DURABLE BUG CLASS — "same-day exclusion via midnight-truncated upper bound": any date-window ingest whose `*_max`/`until` defaults to TODAY's date and gets coerced to `T00:00:00` silently drops the whole current day. Default upper bounds to the current INSTANT; explicit single-day windows must span `T23:59:59`. Check this FIRST when an importer 'misses recent rows'.**

> 🟢🟢 **FRESHNESS-CHIP HEARTBEAT — PM RULED 2026-06-17 (endorse heartbeat, REJECT naive threshold-tighten). PENDING BUILD.** The chip's `$isStale` keys on `MAX(imported_at)` (channel='eshop'), and `imported_at` is `DEFAULT CURRENT_TIMESTAMP` → advances ONLY on INSERT → it measures "time since last NEW order", NOT "time since last successful poll". Naively tightening 90→10-15 min would make the chip cry wolf every quiet eshop night (no new orders for hours → amber while the `*/1` poller is healthy) — strictly WORSE; trains the operator to ignore it. **RULING: make the chip a POLLER HEARTBEAT, decoupled from order recency.** (1) `ingest-shopify-orders.ts` UPSERTs a heartbeat on EVERY successful LIVE run (even 0-insert) at the END of `main()`, inside the `!DRY_RUN` guard (a mid-run throw leaves it stale → correct amber; dry-run must NOT write). (2) Heartbeat home = `system_settings` (NOT a new `ops_*` table — single last-writer-wins scalar, settings-row weight is right): **`section='fulfilment'`, `key_name='shopify_ingest_last_run_at'`, written to `value_text`** (datetime string), `is_active=1`, `description_fr` noting "écrit automatiquement — ne pas modifier" (NOT operator-editable, NOT surfaced on Données générales). 🔴 LIVE SCHEMA NOTE: `system_settings` keys on `(section, key_name)`, NO `scope` column; value in `value_num`/`value_text`. (3) STALE THRESHOLD is the operator-tunable knob, ALSO in system_settings: **`section='fulfilment'`, `key_name='shopify_ingest_stale_minutes'`, `value_num=10`, `unit_fr='min'`** — editable on Données générales (real operational threshold, same class as `order_lead_time_*`). Read with `?? default THEN validate`, clamp to a sane floor (a 0 must not pin the chip amber). (4) Chip: `$isStale = (NOW − heartbeat) > stale_minutes`; **null heartbeat (pre-first-run) → NEUTRAL, not amber**; keep `MAX(imported_at)` as INFORMATIONAL "dernière commande HH:MM" text only — no longer drives amber. THRESHOLD = **10 min** (cron `*/1`, tolerates ~9 consecutive misses before alarming — separates hiccup from dead poller; 5 too twitchy). Existing chip markup/CSS already present → no `ui` build, logic+settings+smoke only. SEQ: mig (seed 2 settings rows; MIG HEAD 392→next free 393, re-`--status` at build-start, parallel sessions live) → ingest heartbeat UPSERT (deploy surgically to `/opt/maltytask-pipeline/scripts/`, md5 parity, owner maltytask:www-data 644; `php`/tsx not `php8.1 -l`) → expeditions.php chip read both keys (`php8.1 -l` the DEPLOYED file before php-fpm reload) → webapp-testing prod-readonly smoke 3 states: fresh→green / aged-heartbeat→amber / quiet-night (old imported_at + fresh heartbeat)→GREEN. **EQUIP: coder + sql + webapp-testing** (NO ui — chip already rendered).

**LIVE-VERIFIED cadence/host (pre-build probe — now superseded by the VPS rehome above):** ingest is a **`*/15` USER-CRONTAB job on the LOCAL WSL box** (`crontab -l`: `flock -n /tmp/shopify-ingest.lock npx tsx scripts/ingest-shopify-orders.ts --since $(date -d "2 days ago")`), writing to MySQL **over a manually-established SSH tunnel on 13306** (`ssh -f -N -L 13306:... maltyweb`, ServerAliveInterval=30). BOTH cron AND tunnel are local-box-resident = the exact fragility recorded (memory was right). It already runs every 15 min, so "several times a day" is ALREADY met IF the box+tunnel are up.
- **The real thing to fix is NOT frequency — it's the host/transport fragility.** Bumping frequency on a local-box/tunnel cron buys nothing; the failure mode is "WSL asleep / tunnel dropped → board silently stale," not "not frequent enough." **DERIVATION-CORRECT TARGET: move the ingest to the VPS** (where MySQL is local, no tunnel) as a real `/etc/cron.d/maltytask-shopify-ingest` entry — same strangler discipline as the recipe-profile + seasonal-index crons (compute homes on the VPS). Cadence on the VPS: every 15–30 min is fine ("several times a day" satisfied with margin). Secrets: `secrets/shopify.env` must land on the VPS (not committed). Runtime: `npx tsx` exists where the maltytask repo is — confirm the repo (or just the script + lib) is deployable to the VPS, or wrap as a maltyweb PHP CLI that shells the existing TS (prefer keeping the TS, just rehome the cron).
- **Idempotency: SAFE to run more often.** Line-level `row_hash = sha256("shopify|order|line|sku|qty")` UNIQUE + `INSERT IGNORE` (L321/L503-553); header upsert keyed on `(source, external_order_id)` with a real UPDATE branch (L447-472). The earlier line-idempotency bug (recorded in §0a11 Phase 0) is FIXED. Re-running over an overlapping `--since` window = no-op on unchanged rows, UPDATE on changed status. No double-ingest concern.
- **VERDICT: small Kouros decision, then BUILD.** The build (rehome cron to VPS) is sound + low-risk, but it needs (a) Kouros OK to install a VPS cron + put `shopify.env` on the VPS, and (b) confirming the maltytask repo/script is present/deployable on the VPS (today the ingest only runs from the local clone). If Kouros would rather NOT put the Node toolchain on the VPS, fallback = harden the local path (autossh + a cron-health/freshness chip on the board surfacing "dernier import Shopify: {time}" so staleness is VISIBLE — same maltyweb-prefers-live discipline as the COP/seasonal freshness-chip debt). **PM rec: VPS-rehome is the clean target; the freshness-chip is worth doing EITHER WAY** (so a stalled ingest can't masquerade as "no new orders"). **EQUIP: coder + sql** (cron/CLI + host migration) **+ ui + webapp-testing** if the freshness-chip is included.

## KNOWN GAPS / GATES (as of 2026-06-07)
1. **Depletion gap (by design):** `ord_orders` empty; May–June B2B shipments live only in the sheet → physique OVERSTATES (e.g. ZEPB computed 806.58 vs sheet 396) until the **2026-06-08 full inventory re-anchors**. NO backfill planned — new anchor trues it up.
2. **Customer bootstrap GATED on the CRM client-list xlsx** (new mid-build input from Kouros). Bootstrap reworked: **CRM = roster truth; BC = fiscal links; sheet = enrichment**. Dry-run v1 done: 81 parens-ID links, 41 exact, 425 sheet-only, 29 parens-IDs absent from BC, 155 BC-only. Report `data/bootstrap-ref-customers-report.md`; script `scripts/bootstrap_ref_customers.php` (one-shot guard).
3. **Remaining to go-live:** authenticated `webapp-testing` smoke → flip `ref_pages.is_active=1` → logistics access-preset wiring + team onboarding. Plus Transport Express → `ref_transporters`.
4. **Email ingestion (commandes@)** = LATER arc; schema already carries the readiness columns.
5. ⚠️ **Mig-number coordination:** mig 280 was taken by the parallel visite-guidée session; next Expéditions mig must re-run `--status` + `ls db/migrations` first (index checklist rule). All Expéditions deploys were selective-rsync to avoid shipping the other session's uncommitted work.

## PM divergence-watch / risks (flagged 2026-06-07, OPEN)
- **R1 — FG-depletion is now a DERIVED READ, keep it single-homed.** `fg_stock_compute()` is the only FG-stock derivation; the Stage-2 direct-sales tracker (BOM arc ④) will WRITE `inv_consumption` (packaging pulls) — different fact (packaging consumption vs FG units), but at Stage-2 design time verify no second FG-depletion path appears and that fg-stock vs tracker agree on which source feeds what (eshop=`inv_sales_orders`, taproom=`inv_sales_bc`, B2B=`ord_*`).
- **R2 — -X cage vs -B box stock states.** RULE #3 (walkthrough): cage XOR box. Once the 06-08 cage inventory creates -X SKUs + anchors, fg-stock's per-SKU ledger must not double-count or mis-attribute -X vs -B units (eshop pulls are cage-first w/ -B fallback). Re-examine `fg_stock_compute()` after the cage anchor lands.
- **R3 — production term scope.** Verify `fg_stock_compute()` production excludes `is_tombstoned=1` and contract/white-label runs (`ref_recipes.classification`) that never enter Neb FG stock, and that parallel-run (-4/-B) rows attribute to the right SKU.
- **R4 — anchor semantics on 06-08.** Full inventory + cage anchor both write `inv_fg_stocktake`; ensure ONE coherent month_closed anchor set (anchor = latest month_closed; partial writes could split the anchor month).
- **R5 — taproom staleness.** `inv_sales_bc` is monthly → intra-month taproom depletion invisible; physique drifts up until BC ingest. Acceptable, but document on the dashboard if operators ask.
- **R6 — customer identity single-homing.** Four customer-ish surfaces now exist: `ref_customers` (sold-to), `ref_clients` (contract brewers), Shopify customers (`inv_sales_*`), BC sold-to. Bootstrap must define the overlap rule (a contract brewer who also buys beer = ref_customers row LINKED, not duplicated identity-fact).
- **R7 — visite-guidée convention.** Standing rule (parallel session, same day): every new `ref_pages` entry gets a bespoke tour content row. Add one for `expeditions` at is_active flip time.

## 2026-06-07 (LATE) — CRM-primary bootstrap + Carnet clients SHIPPED (all deployed+committed, PM live-verified: mig head=281, census 3046/68/368, Transport Express in ref_transporters)
1. **Mig 281 `281_ref_customers_contact_cols.sql` APPLIED** — `ref_customers` contact cols (email, address_line1/2, postal_code, city, canton, country_code); **`name` UNIQUE dropped → plain KEY** (`bc_customer_no` is the identity; CRM has legit duplicate names — supersedes the mig-279 UNIQUE in §Schema above); Transport Express seeded into `ref_transporters` (gate #3 transporter item CLOSED). NB parallel-arc numbering: 278+280 were taken by other sessions; **next free mig = 282**.
2. **CRM-primary bootstrap APPLIED (commit `73879bc`)** — input: full BC client export `Clients.xlsx` (2,678 rows, N° 1001–3943) from Kouros. Result: **3,046 `ref_customers`** = 2,678 CRM (68 «NE PLUS UTILISER» → `is_active=0`) + 368 sheet-only `needs_review=1`. Validation clean: all 270 `inv_sales_bc` customer_nos present in CRM; all 29 formerly-unmatched parens-IDs resolved. **Precedence model (canonical): CRM = roster truth; BC = fiscal validation; sheet = trade_channel/alias enrichment; NO fuzzy links anywhere.** Gate #2 (bootstrap gated on CRM xlsx) CLOSED.
3. **Carnet clients tab (commit `ff70261`)** — review queue + **MERGE tool**: one transaction — reassign `ord_orders` FKs → target, copy missing enrichment, `'alias:'` note on target, tombstone dup `is_active=0` + `'merged_into:'`, **NEVER delete**. Paginated directory w/ inline edits (whitelisted fields, `log_revision`). Saisie typeahead now ranks bc-linked rows first + «⚠ à valider» last (prevents picking unmerged dups).
4. **Off-trade business rule (Kouros, captured + applied):** `off_trade` = Cobra-channel retailers — COOP, Manor, Denner, Aldi, Lidl, Cobra Traders accts (incl. WITTICH), Migros EXCL. Migros Partenaire/MP, Aligro, Otto's. Applied to 50 CRM rows by **EXPLICIT ID list — substring matching is UNSAFE** (Baldinger≠Aldi, Grotto≠Otto's, PricewaterhouseCoopers≠COOP). 3 ambiguous left NULL pending Kouros: C.D.D. SA - Denner Partenaire (3230), Giro Migros (2817), Migros Golf Golfparc (2887). **Future-arc hook:** Kouros says channel classification "shall soon be automatic" — derive from Cobra-account linkage in BC rather than manual flags.
5. **R6 (ref_clients↔ref_customers link rule) NOT yet settled** — no contract brewer surfaced in the needs_review set demanding it; carry the watch into the operator merge pass.

## 2026-06-08 (OVERNIGHT, third update — all deployed+committed+PUSHED, PM-verified main==origin @ `6dd5521`)
1. **Merge-decision pipeline (`9803849` + safety fix `6dd5521`)** — suggestion engine bucketed the 368 needs_review rows: **138 strong fuzzy candidates / 58 weak / 89 privés / 83 puzzles** → review xlsx with DÉCISION columns on Kouros's desktop. Apply chain: `scripts/convert_merge_xlsx.py` (local) → JSON → `scripts/apply_merge_decisions.php` (VPS; dry-run default, `--apply` = single transaction; mirrors client_merge semantics exactly; `log_revision` actor `'batch-merge-cli'`; idempotent re-runs no-op). **Safety ruling baked in (`6dd5521`): sheet-A blank decision = NON (leave in queue), NEVER merge — a deleted cell must not act.** Fuzzy = suggestions ONLY; exemplar false match preserved in the tables: «Arches»→«Marchés» 0.92 (accent-strip artifact) — reaffirms the no-fuzzy-links canon (§2026-06-07 LATE item 2).
2. **Live stock hints in saisie (`134bbdf`)** — `fg_stock_compute()` → `window.EXP_STOCK_MAP` `{sku_id: {live_futur, physique}}`; advisory amber when qty > live_futur, **NEVER blocks (overselling is legal — production may be planned)**; edit mode adds back the order's own original qty; try/catch silent degrade. NB: hints inherit the depletion gap (gate #1) until the 06-08 re-anchor — amber thresholds only true up after it.
3. **Tour content (`a59d94d`)** — expeditions entry in `$PAGE_DESCRIPTIONS`+`$PAGE_ICONS` of visite-guidee.php. **R7 CLOSED** — flip-safe.
4. **Logistics preset — NOTHING TO BUILD:** auth arc had already wired expeditions (as `fulfilment`; id-based, rename carried) into manager + logistics_operator presets; activates automatically at flip. Gate #3 preset item CLOSED.

## 2026-06-10 — Customer name-correction (Blind Tigers) + NO customer-alias-table finding + `$editableFields` whitelist backlog
1. **Name correction APPLIED (data-only, no mig, no commit):** `ref_customers` id=1572 (`bc_customer_no`='2789') renamed «Blind Tigers Sàrl - Le Vestibule» → «Blind Tigers Sàrl». Operator chose to keep the legal form «Sàrl» (not bare «Blind Tigers»). Method = direct audited MySQL txn UPDATE on the VPS, run as www-data via `app/db.php`+`app/db-write-helpers.php`, mirroring app governance: captured `before`, `log_revision(...,'ref_customers',1572,before,{'name':...},'normal','Correction nom client: retrait du suffixe Le Vestibule')`, attributed `user_id=1` (Kouros). **Audit row id 13799 confirmed.** `bc_customer_no` stays '2789'; dead row id=2789 (Liechti) untouched; **0 orders attached → FK propagation moot.** SOT-clean, COGS-neutral.
2. **🔑 FINDING — there is NO customer-alias table.** Alias tables exist only for `ref_mi`/recipe/sku/supplier/yeast — NOT customers. Entity resolution keys on `bc_customer_no`, not a name alias, so the alias-preservation concern that applies to MI/supplier name edits is **MOOT for customers**: a customer name is free to correct without orphaning a resolution path. (The Carnet merge tool's `'alias:'` note is a free-text breadcrumb on `ref_customers` itself, not a separate alias store.)
3. **🟡 LATENT BACKLOG (no action requested):** `expeditions.php` inline client editor `$editableFields` whitelist **EXCLUDES `name`** (item-3 §2026-06-07 LATE / line ~49), so customer name corrections currently require raw SQL each time. **If name-correction recurs, add `name` to `$editableFields` with validation** (UNIQUE was dropped to plain KEY per mig 281, so dup names are legal — validate for non-empty/trim, not uniqueness) rather than hand-editing. EQUIP coder+sql+ui if built.
5. **Auth smoke DEFERRED to Kouros:** temp smoke-user creation DENIED by the permission classifier (account provisioning needs explicit operator authorization — correct call; NB the permanent smoketest acct id=16 exists, creds `secrets/maltyweb-smoketest.env` — candidate for next time instead of a temp user). Unauthenticated smoke all green (302 login redirect, lint clean, fg compute clean). Authenticated click-through = Kouros's first morning item.

**Morning runbook (Kouros, 2026-06-08):** xlsx → `convert_merge_xlsx.py` → dry-run → `--apply`; admin click-through; flip `is_active=1`; full inventory re-anchor (**R2/R3 re-exam of `fg_stock_compute()` trigger THEN**); rule on the 3 ambiguous off-trade IDs.

## 2026-06-08 — ref_sites canonical-name fix + 06-08 stocktake misfile correction (DATA + NAME only, no code/schema; PM-verified live)
Two live fixes done in ONE transaction as kouros (user id 1), `qc_flag='normal'`, comment marks Claude-assisted; full `log_revision`. **PM-verified against live DB 2026-06-08.**
1. **`ref_sites` renamed (name col only; `site_type` untouched, ids stable):** id1 → **«La Nébuleuse - Zgeg (Production)»** (still `site_type=production`); id2 → **«La Nébuleuse - Zeubi (Logistique)»** (still `site_type=warehouse`). «Zgeg»=Renens/Production, «Zeubi»=Crissier/Logistique — Kouros's colloquial nicknames, now the **canonical display names**. id3=Taproom(pos), id4=Nausikraft(consignment) unchanged. → use these exact display strings in any UI/report that names a site; never re-derive from site_type.
2. **06-08 stocktake misfile corrected:** Kouros entered Crissier/Logistique stock under the **Production** location (loc1) on 2026-06-08. **Moved 28 `inv_fg_stocktake` rows loc1→loc2** (`WHERE month_closed='2026-06' AND DATE(counted_at)='2026-06-08'`), row-count-guarded at exactly 28. **Final state (LIVE-VERIFIED): loc1 = only the 41 April (`month_closed='2026-04'`) anchor rows; loc2 = the 28 June (`2026-06-08`) rows; loc3/loc4 still EMPTY** (Kouros still inputting Taproom + Nausikraft).
   - **Anchor-semantics note (R4):** the 06-08 inventory now lives under `month_closed='2026-06'` at the **warehouse** location, distinct from the April **production** anchor — so `fg_stock_compute()`'s "latest month_closed" anchor moves to 2026-06 for loc2's SKUs while loc1 stays on 2026-04. When the re-anchor / cage-anchor lands, confirm `fg_stock_compute()` resolves the anchor **per location** (not globally) or the location split will mis-pick. Cage (-X) + Taproom + Nausikraft anchor rows still pending.

## 2026-06-08 — INCIDENT + DURABLE FIX: `inv_fg_stocktake.row_hash` divergence (hash-keyed row mutated without rehash)
**INCIDENT (root cause understood, data ALREADY repaired+verified by operator):** the 06-08 misfile *correction itself* corrupted 28 rows. The stocktake dedup/upsert key is `row_hash = sha256("fgct|{sku_id_fk}|{location_id_fk}|{counted_at}")`, app-written, `UNIQUE uniq_row_hash`, INSERT … ON DUPLICATE KEY UPDATE (qty only, NOT location). The ad-hoc SQL `UPDATE location_id_fk 1→2` did **NOT recompute row_hash** → 28 rows sat at loc2 still hashed as loc1; the operator's later loc1 (Production) submit hash-COLLIDED with them and overwrote qty. **Repaired** in one txn from `audit_row_revisions` (before=Crissier/batch1, after=Production/batch3): restored 23 Crissier + recreated 23 Production + fixed 5 stale hashes; final loc1=27/550u, loc2=28/5976u, loc4=26/554u; **row_hash integrity 0 mismatch across all 81 June rows.** Data clean.
**DURABLE FIX — PM RULING 2026-06-08 (APPROVED, with one mandatory amendment): convert `row_hash` to a STORED GENERATED column** `CHAR(64) AS (SHA2(CONCAT_WS('|','fgct',sku_id_fk,location_id_fk,counted_at),256)) STORED`, UNIQUE preserved → location_id_fk and hash can NEVER diverge again (fix-by-construction; the "remember to recompute hash on location change" lesson is thereby MOOT). Generated col, NOT a trigger (a trigger is bypassable by direct UPDATE exactly as this incident was; generated col is structural). STORED not VIRTUAL (UNIQUE index stability). **PM-VERIFIED LIVE before ruling:**
  - **SOLE writer = `expeditions.php:746` INSERT.** Every other ref to `inv_fg_stocktake` (warehouse.php, warehouse-export.php, app/fg-stock.php, maltytask lib/inventory*.{js,ts}, mysql-inventory.ts) is **read-only SELECT**. `sync-stocktake.js` writes BSF Sheets, never MySQL. **`source='bsf-stocktake'` has ZERO code writers** (appears only in mig 066's ENUM def). → converting is safe from rejected-INSERT; the form INSERT drops `row_hash` from its column list, ON DUPLICATE KEY fires off the generated UNIQUE.
  - **🔴 MANDATORY AMENDMENT — the premise "data is 100% hash-consistent" is FALSE for 41 rows.** `WHERE row_hash <> SHA2(CONCAT_WS('|','fgct',sku_id_fk,location_id_fk,counted_at),256)` = **41 rows**, ALL the `source='bsf-stocktake'` **April anchor** (`month_closed='2026-04'`, `counted_at IS NULL`), hashed by the OLDER legacy-import formula. June (81) is consistent; April (41) is NOT. On DROP+ADD, those 41 silently re-mint to `fgct|sid|loc` (CONCAT_WS SKIPS NULL counted_at — note PHP `sha256` would emit a trailing `|`, so PHP-formula ≠ generated-formula in the NULL case; generated col is the more-correct canonical). **Tolerable ONLY because nothing re-derives those hashes** (no bsf-stocktake writer). **Collision check PASS: all 122 rows → 122 distinct generated hashes (0 collisions); 2nd UK `uniq_sku_month_location_response` also 122/122 distinct.** Build must do this KNOWINGLY + document it in the migration comment; verify post-convert: 0 rows where `row_hash <> SHA2(...)`, 122 distinct, April-2026 active anchor still 41 rows.
  - **BUILD: own migration (confirm next free # via `migrate.php --status` + `ls db/migrations` — parallel sessions take numbers, the 278 collision is precedent; local index showed head 287). DDL = DROP INDEX uniq_row_hash → DROP COLUMN row_hash → ADD generated STORED (collation `utf8mb4_unicode_ci` to match, or mixed-collation throw) → ADD UNIQUE uniq_row_hash. No new schema_meta row (existing table). `expeditions.php` edit: drop row_hash from INSERT col-list + binding (~L748/749/763/782); KEEP the `$rowHash = hash('sha256',…)` compute — still used by the before-snapshot `SELECT … WHERE row_hash=?` at L767; ON DUPLICATE KEY UPDATE stays (counted_at no-op since match is BY the hash). EQUIP: sql + coder (NO ui — pure backend). Proof: dry-run 0 mismatch; idempotent double-submit → 1 row; UPDATE location_id_fk auto-shifts hash; self-clean test rows.**
  - **🔗 FORWARD COUPLING for task #12 stock-config admin + task #10 Stock PF:** once row_hash is generated, any admin UI editing `location_id_fk` on a stocktake row AUTO-REHASHES — desired, but it can now collide into an existing (sku,loc,date) row → the UNIQUE rejects/the ON DUPLICATE merges. The admin edit path MUST handle the UNIQUE-violation/target-exists case (the exact scenario that caused this incident). Note in the #12 brief.

**✅ SHIPPED + LIVE (mig 288, 2026-06-08 — agent conn dropped mid-run; PM live-verified + reconciled).** Generated col IS live and working: `row_hash CHAR(64) GENERATED ALWAYS AS SHA2(CONCAT_WS('|','fgct',sku_id_fk,location_id_fk,counted_at),256) STORED NOT NULL`, `UNIQUE uniq_row_hash`. 122 rows / 122 distinct hashes / 0 collisions; June repair intact (loc1=27/550u, loc2=28/5976u, loc4=26/554u); April anchor 41. Form deployed (row_hash dropped from INSERT). 0 orphan + 0 NULL sku_id_fk.
  - **FK RULING (PM, decisive): `fk_fg_sku` re-added as ON DELETE RESTRICT.** To add the generated col referencing sku_id_fk the agent had to DROP `fk_fg_sku` (was sku_id_fk→ref_skus.id ON DELETE **SET NULL** — MySQL 8 forbids SET NULL/CASCADE on a generated-col input) and did NOT re-add it ("app-level integrity"). PM ruled: RESTORE the FK as **ON DELETE RESTRICT** (MySQL 8 ALLOWS RESTRICT/NO ACTION on a generated-col input; 0 orphans/0 NULLs → clean; strictly stronger than the moot SET NULL; a NULL sku_id_fk on a count row is a corrupt fact, not a tolerable orphan — aligns w/ FK-discipline + minimize-NULL). `fk_fg_stocktake_location` (location_id_fk→ref_sites ON DELETE RESTRICT) was KEPT throughout (RESTRICT always allowed on gen-col inputs). **Both FK columns are now BOTH FK-enforced (RESTRICT) AND row_hash inputs** — fully-wired canonical state; any future mig touching either column's type/nullability must account for both the FK and the generated expression.
  - **RECONCILIATION (schema_migrations drift — DDL live but unrecorded; agent applied DDL without recording).** Ruled: (1) EDIT the 288 file so it fully describes live = gen col + uniq_row_hash + `DROP FOREIGN KEY fk_fg_sku` + `ADD CONSTRAINT fk_fg_sku … ON DELETE RESTRICT` (fold the FK re-add into 288, NOT a separate 289 — the FK drop+re-add is a consequence of the gen-col DDL, one atomic migration matching live exactly). (2) Run ONLY the missing ALTER on live (gen col + UNIQUE + DROP already live; only the RESTRICT re-add outstanding): `ALTER TABLE inv_fg_stocktake ADD CONSTRAINT fk_fg_sku FOREIGN KEY (sku_id_fk) REFERENCES ref_skus(id) ON DELETE RESTRICT;`. (3) `INSERT INTO schema_migrations (filename, applied_at) VALUES ('288_inv_fg_stocktake_row_hash_generated.sql', NOW());` (the known drift pattern; confirm exact on-disk filename first). (4) `migrate.php --status` must show 288 no longer pending + nothing to apply — migrate.php must NOT re-run 288 (would error re-DROPping/re-ADDing). schema_meta needs no new row (existing table); verify the existing row is present (don't duplicate). EQUIP sql+coder.

## 2026-06-09 GO-LIVE — Expéditions live for the whole team + first logistics-team user onboarded
- **Expéditions / Fulfilment v1 IS LIVE FOR THE TEAM.** mig 290 `290_marketing_access_preset.sql` (marketing preset id 8 + expeditions go-live, flipped `ref_pages.expeditions.is_active` 0→1) and the editable-username work are now COMMITTED + PUSHED to origin/main (commit `16f725f` mig 290; `2169a81` editable username). Page-hidden go-live gate is CLEARED.
- **Olivier onboarded + logged in 2026-06-09** — user id 19, username `OlivierBarral`, role=operator, preset=marketing (id 8). He tested the FG stocktake live: entering 5 then 3 for STIB at Taproom (loc3) correctly **UPSERTed** — last-write-wins on the key `(sku_id_fk, location_id_fk, counted_at)`, fully audited 5→3. Confirms (a) the stocktake UPSERT/dedup behaves on a real operator's input, and (b) the editable-username rename propagated correctly into the audit actor (`OlivierBarral`). Taproom (loc3) now has its first count.
- **🔒 SECURITY DRIVER for the user-auth hardening backlog:** Olivier is on his OWN personal, unmanaged work computer (tailnet node `olivier` = 100.115.87.61, Windows) — a device we do NOT control now has full ERP access via Tailscale. This is the exact surface the planned **2FA (TOTP)** hardening should address (the narrow admin-Tailscale-gate is moot — the whole app is already tailnet-gated). See user-auth-admin-arc.md §PENDING. Onboarding-path note: `app.maltytask.ch` is tailnet-only (public IP 83.228.215.243 → 403; resolves to tailnet 100.125.142.25 via tailnet-wide split-DNS); a Windows client needs `tailscale set --accept-dns=true` — and `nslookup` is MISLEADING (it bypasses NRPT) → verify via browser or `curl`.

## 2026-06-09 — WeeklyOrders SHEET INGESTION arc: PM PRE-BUILD RULINGS (dry-run→match-report→Kouros reviews unmatched→apply)
Goal: ingest the live WeeklyOrders sheet (Production/Logistic Masterfile, Google Sheet `1WA-GfwrD-lgMiWJ2ejg-92zq1Ps0CZaK6Sn11isbXyI`, tab WeeklyOrders, row-6 SKU header, per-week blocks) into `ord_orders` so operators continue on the web "where they left off." Revenue/fulfilment-bearing → never-speculate rule applies HARD; dry-run-first, flag-over-decide.

**LIVE STATE VERIFIED (PM-probed 2026-06-09):** `ord_orders`=12 / `ord_order_lines`=56 / `ord_order_status_events`=39. **The "12 test orders" ARE NOT throwaway — they are real KW24 customer orders, `source='web'`, requested_date 2026-06-08/09, all BC-matched (ids 3-14; e.g. 6=AlloBoissons BC1393, 7=Bevanar BC1579, 8=Marché Flag Aubonne shipped, 9=Département Marketing BC1807, 14=Sophie Degrauwe privé no-BC).** They have full event cascades (entered+confirmed+picked stamped at one timestamp). **NO `source_ref`/dedup column on ord_orders; NO UNIQUE business key** — only PK + non-unique indexes (requested_date, status, customer, source+review_status). This is the crux of Q6.

**THE 7 RULINGS (decisive):**
1. **SCOPE = AGREE.** Import KW24 full current week (incl. already-delivered Mon/Tue) + ALL future booked (next-Monday + coming-weeks, through last dated future row before the Feb archive). **EXCLUDE the KW6/Feb archive** (delivered months ago = not operational handoff; "past" is satisfied by KW24's delivered rows). No bounded recent-past window — the handoff is "continue where you left off," not "rebuild history." If Kouros wants a longer past tail for the Commandes range-view, that's a SEPARATE future-history-backfill arc, not this ingestion.
2. **STATUS = rightmost-checked-wins AGREE; emit EVERY reached stage as an event (events=truth), NOT just final.** Same pattern the 12 web rows already use (entered+confirmed+picked all stamped). occurred_at = requested_date (00:00) for ALL reached stages — we have no per-stage timestamps from Excel; stamping reached stages at the order date is the honest reconstruction (collapsing to final-only would LOSE the audit fact that the order passed through them). Mark imported events `comment='import:weeklyorders'` so they're distinguishable from operator-driven transitions. Cache `status`=rightmost stage. (`user_id_fk` NULL on import events — matches the existing 12.)
3. **INTERNAL CHANNELS = SKIP Taproom/Eshop/samples/Marketing-as-internal/privé-marketing notes — DO NOT import as ord_orders.** This is the R1 one-fact-one-depletion rule. Eshop depletes via `inv_sales_orders`, Taproom via `inv_sales_bc` (the `fg_stock_compute()` legs) — creating ord_orders for them = DOUBLE-DEPLETION on the board. WeeklyOrders Taproom/Eshop rows are operational notes, not the depletion truth. **CAVEAT (verify, don't assume): "(Marketing)"/"Département Marketing" is AMBIGUOUS — order id 9 is already a `customer` order to "Département Marketing (BC 1807)" (a real BC sold-to that depletes via ord_*), NOT an internal channel.** So the skip-list is: rows whose CLIENT cell is literally Taproom / Eshop / "échantillons TS"/samples — the internal-consumption channels that have their OWN depletion source. A BC-numbered Marketing sold-to is a customer order (keep). When in doubt → match report, never auto-skip a BC-numbered row.
4. **CUSTOMER MATCHING = AGREE, with the canon already established (no-fuzzy-links).** Precedence: parens BC# → `ref_customers.bc_customer_no` (exact); else name → exact, then `'alias:'`-noted rows from the merge pipeline, then **STOP** — **NO fuzzy auto-link** (the §2026-06-07 LATE item-2 + §OVERNIGHT canon: «Arches»→«Marchés» 0.92 false match proves it). Fuzzy ≥ threshold = SUGGESTION in the match report ONLY, never an auto-link. Unmatched/below-threshold/privé-not-validated → SURFACE in match report, NEVER auto-create or guess (revenue-bearing). Point the agent at the EXISTING infra: the merge-suggestion engine (`scripts/` merge pipeline, `9803849`) + the Carnet-clients typeahead ranking (bc-linked first, ⚠-à-valider last) — REUSE that resolver, don't write a new one. privé individuals → match to validated private customers (the 89 privés bucket from §OVERNIGHT) else flag.
5. **CITÉ MULTI-LOCATION FESTIVAL = AGREE, N separate ord_orders.** One customer (BC 1823), ~13 distinct delivery rows = 13 ord_orders, each its own `requested_date` + line(s) + sub-location (Courtine/Grand Canyon/Pierre Viret/Stock F&B…) in `comment`. The order grain is the delivery event, not the customer — distinct dates/locations are distinct fulfilment obligations. (No festival-grouping entity needed in v1; comment carries the sub-location string verbatim.)
6. **DEDUP/IDEMPOTENCY = the real architectural decision. RULING: ADD a `source_ref VARCHAR` column to `ord_orders` capturing the sheet-row identity, with a UNIQUE/idempotency guard — do NOT rely on a (customer,date,line-hash) heuristic.** Rationale: (a) the 12 existing rows are `source='web'` real orders that OVERLAP KW24 — a content-hash dedup would either falsely-merge a legit second same-day order to the same client or fail to recognise the operator-entered twin; (b) line-hash is brittle (qty edits on the sheet, parallel -4/-B). **`source_ref` = stable sheet coordinate** e.g. `weeklyorders:{tab_gid}:{week_label}:{row_index}` (or a sheet-row content fingerprint if row indices shift). New mig: `ALTER TABLE ord_orders ADD COLUMN source_ref VARCHAR(190) NULL` + `UNIQUE uniq_ord_source_ref (source_ref)` (NULL allowed → web/email orders unaffected; only import rows carry it) → re-run = INSERT…ON DUPLICATE KEY skip/update, structurally idempotent. **The 12 web rows: do NOT reconcile/replace/import-over them.** Match report flags any KW24 sheet row that collides with an existing web order (same customer_id_fk + same requested_date) as "⚠ possible duplicate of existing web order #N — operator decides keep-both / skip-import." NEVER auto-delete an operator's web order; NEVER auto-merge. The 12 are the operator's ground truth for the rows they already entered; the importer fills the REST of KW24 + the future book around them.
7. **OFF-TRADE = AGREE, customer attribute not order attribute.** The col2 OFF flag is `ref_customers.trade_channel` (already bootstrapped by explicit-ID list, §2026-06-07 LATE item-4 — Cobra-channel, substring-UNSAFE). Ignore it on the order; trust ref_customers. If a sheet row's OFF flag DISAGREES with the customer's stored trade_channel, that's a data-quality signal → surface in the match report (don't silently override either way).

**DIVERGENCE FLAGS for this build:** (i) double-depletion if Taproom/Eshop become ord_orders (ruling #3 prevents); (ii) fuzzy auto-link corrupting a revenue-bearing customer FK (ruling #4 — flag-only); (iii) the 12-web-row overlap silently double-creating KW24 (ruling #6 — source_ref UNIQUE + collision-flag); (iv) a BC-numbered Marketing/privé sold-to wrongly skipped as "internal" (ruling #3 caveat). **SEQUENCING:** mig (source_ref col + UNIQUE) → parser/dry-run producing match report (matched / fuzzy-suggested / unmatched / internal-skipped / web-overlap-collision buckets) → Kouros reviews unmatched+collisions → `--apply` (single txn per order; ord_orders + lines + cascaded events; log_revision; idempotent on source_ref). **EQUIP: sql + coder + xlsx-or-sheets-read** (the source is a live Google Sheet — service-account read; the existing bootstrap used a downloaded xlsx, mirror that or read via the sheets client; NO ui unless a match-report viewer page is wanted — recommend a plain markdown/CSV report first, like the bootstrap report). Mig number: re-run `migrate.php --status` + `ls db/migrations` first (parallel sessions; head was 290, next free likely 291 — VERIFY).

### ✅ APPLIED to prod 2026-06-10 (morning) — WeeklyOrders import LIVE, `ord_orders` 12→79
**State-of-record 2026-06-10. `--apply` RAN (Kouros present). VERIFIED PROD: `ord_orders` TOTAL = 79** (web=12 byte-stable ids 3-14 source_ref NULL + import=67; apply run imported=67 / skipped=0). **`ord_order_lines` = 252; `ord_order_status_events` = 160.** Predictions held exactly:
- **Festival de la Cité = 13 separate orders ✓** (ruling #5 — sub-location in comment).
- **Internal channels leaked = 0** (Taproom/Eshop/échantillons TS skipped, ruling #3 proven on live write).
- **Web-overlap-collision bucket (12 rows r7-r17,r38) correctly HELD OUT** — 1:1 to existing web #3-#14 (same customer+date); no twins created (ruling #6 collision-flag proven).
- **4 empty-order rows NOT imported** (Street Cellar/Galac, Jardins de Louis/Cuves, Gotham×2/T7) — still pending Kouros phone calls; idempotent re-apply folds them in (also Cité "5 bars" r231 + ZEPV-litres caveat) once the operator data-Qs below resolve.
- **3 internal rows skipped by design** (échantillons TS, Taproom, Eshop).

**STILL DEFERRED (separate, NOT this shipped entry):** migs **300/301** (`ref_customers.sale_class`, `inv_sales_ledger` — the BC item-ledger / sales-commercial-surface arc) NOT applied / NOT on VPS; importer commit **e57eda5** still LOCAL-ONLY unpushed (Kouros hasn't requested push).

---
*(historical — pre-apply state-of-record 2026-06-09 EOD, kept for provenance:)*

**Migration (DONE):** mig **294 `294_ord_orders_source_ref.sql`** = `source_ref VARCHAR(190) NULL` + `UNIQUE uniq_ord_source_ref` (exactly ruling #6) **APPLIED to prod** — got swept into a parallel-session commit `d3fb954` via a broad `git add` (NOT this arc's commit, but it IS committed; cross-references the §SAME-DAY TIEBREAK note that `d3fb954` was the tiebreak commit — the broad add caught the 294 file too). MIGRATION HEAD = 295 (per index; 294 source_ref + 295 submitted_by_user_fk both applied). The 294 ruling-vs-fulfilment-site distinction (§ITEM 1 code-fact 1) HOLDS: 294 is the WeeklyOrders idempotency key, independent of the future `fulfilment_site_id_fk`.

**Code (committed locally, UNPUSHED):** importer `scripts/import_weeklyorders.php` = commit **e57eda5** (local only, not pushed). Extractor = `/tmp/wo-extract.js` (cutover tool, UNCOMMITTED — a /tmp helper, NOT in the repo) → writes `/tmp/wo-parsed.json`. SKU col→code map read from sheet **row 6 at runtime** (112 cols; NonAlc placeholder cols skipped) — NOT hardcoded. Two date formats handled. Cité = 13 separate orders with sub-location in `comment` (ruling #5 honored). Internal channels (Taproom/Eshop/échantillons TS) skipped (ruling #3). Off-trade ignored on order per ruling #7.

**Dry-run buckets (86 in-scope sheet rows):** **67 matched / 12 web-overlap-collision / 0 unmatched / 3 internal-skip / 4 empty.** Scope = effective date ≥ 2026-06-08 (KW24 Monday) through coming-weeks, hard-stop BEFORE the `26/KW2` Jan–Feb archive (ruling #1). The 12 web-overlap-collisions = exactly the 12 existing `source='web'` KW24 rows (ruling #6 collision-flag working — flagged, NOT auto-merged).

**2 new accounts created (Kouros-authorized, NOT speculative):** `ref_customers` #3054 **Club Sportif Le Mouret** (bc 3952) + #3056 **Stoked Mountain Experience AG** (bc 3954), each with an alias stub (#3055 "Le Mouret", #3057 "Stocked Mountain" — typo preserved) carrying `notes='merged_into: <canonical>'`. (So the typeahead/resolver can find the sheet's spelling and route to canonical.)

**MORNING APPLY runbook (Kouros, 2026-06-10):** `node /tmp/wo-extract.js` (if the sheet changed) → `scp /tmp/wo-parsed.json maltyweb:/tmp/` → `ssh maltyweb 'sudo -u www-data php /var/www/maltytask/scripts/import_weeklyorders.php --apply'` → verify `ord_orders=79` (12 web byte-unchanged + 67 new) → spot-check Cité / future / KW24 rows. **HELD for Kouros's ruling (4 items, do NOT guess — revenue-bearing):** (a) **4 empty-orders** (Galac fûts / Cuves / T7 — SKUs not in the row-6 grid → 4 empty bucket; need a SKU mapping or a skip decision); (b) **Cité "5 bars" r231 ZEPV×3500** (separate order vs aggregate?); (c) **ZEPV-in-litres CUV caveat** (cuv qty expressed in litres on the sheet vs SKU-unit grain — unit-discipline check before it inflates).

### 2026-06-10 — WeeklyOrders AUDIT-RESIDUALS RULINGS (privé double-deplete · empty-order/event-material · week-scoped search). PM-ruled, LIVE-DB-VERIFIED, pre-build.
**Three audit residuals after the import. All verified against live prod 2026-06-10.**

**① PRIVÉ / SHOPIFY-PICKUP ORDERS — ruled SAFE TO WRITE AS CUSTOMER ORDERS; NO double-deplete on these two rows.** The two sheet rows (r285 Joël Golinucci ZEPF×2 EMBF×2; r298 Alice Buache ZEPF×2 MOOF×1) are **NOT in `inv_sales_orders`** (LIVE-VERIFIED: surname search Golinucci/Buache = 0 hits; only same-first-name strangers) → they are **manual sheet click&collect entries, NOT Shopify-synced** → writing them as `ord_orders` (order_type='customer') has **no eshop-leg twin → no double-deplete**. So 1c sub-option **(ii) is the correct one for these rows.**
  - **a. NO canonical "Privé" fold-account exists** — Kouros's "fold to one account" premise is NOT current reality. Live: **83 `is_private=1` rows, one per individual, all `trade_channel='on_trade'`, no `bc_customer_no`** (the 89-privé needs_review bucket largely became these). **RULING: do NOT invent a single shared Privé account in this build** — it would CONTRADICT the existing per-individual model + the no-fuzzy-link/never-guess canon, and there's no operator-confirmed merge map. Per-individual privé rows ARE the convention; create the 2 missing ones as their OWN `is_private=1` rows (name as on sheet, `is_active=1`, `trade_channel='on_trade'`, `bc_customer_no=NULL`) — exactly like the 83 that exist. If Kouros truly wants ONE fold-account, that's a SEPARATE merge-decision arc (reuse the Carnet merge tool → tombstone individuals `merged_into:`), not a side-effect of this import. FLAG the contradiction to Kouros; do not resolve it by guessing.
  - **b. Individual name preservation:** the per-individual `ref_customers.name` already IS the name (no fold). If Kouros insists on folding, the name lives in the **`ord_orders.comment`** free-text (there is **NO `contact_name` column** on `ord_orders` — LIVE-VERIFIED schema) — do NOT add one for 2 rows.
  - **c. 🔴 THE DOUBLE-DEPLETE MECHANISM (durable, generalized):** `fg_stock_compute()` eshop leg depletes **ALL `inv_sales_orders WHERE channel='eshop' AND DATE(created_at) >= anchor`** — it does **NOT filter `fulfillment_status`** (LIVE-VERIFIED query in `app/fg-stock.php` ~L482). So pickup-vs-ship is INVISIBLE to depletion: any eshop order in `inv_sales_orders` already depletes. **THE GATING RULE (canonical): an order belongs in `ord_orders` (B2B/expedie leg) XOR `inv_sales_orders` (eshop leg) — NEVER both. The discriminator is membership in `inv_sales_orders`, not a flag we invent.** For THESE two rows: absent from `inv_sales_orders` → ord_orders write is safe. **FORWARD HAZARD (the ④ Phase-2 gate, still ON HOLD):** once the Shopify pickup-signal rework lands and pickup orders DO sync into `inv_sales_orders`, writing the SAME pickup order to `ord_orders` for the picking workflow WOULD double-deplete. The clean solution at that time = pickup orders deplete via the eshop leg (already do), and the Expéditions picking view READS them from `inv_sales_orders` (like the existing muted read-only eshop/taproom rows on the Commandes board, expeditions.php ~L1591) — it does NOT re-create them as depleting `ord_orders`. Do NOT solve this now; it's gated on the Shopify rework. (Note: `internal_channel` ENUM already carries `'cage','shop'` beyond taproom/eshop — but internal_channel is for `order_type='internal'`, a different fact; a privé pickup is a real customer sale, not internal.)
  - **d. Write path:** these 2 rows resolve to NO customer today → the importer bucketed them UNMATCHED (it has no "fold to Privé" path — confirmed by the privé-not-validated→flag ruling #4). Convention-correct write = **create the 2 `is_private=1 ref_customers` rows first (Kouros-authorized, name-as-stated = NOT speculative), THEN re-run `import_weeklyorders.php --apply`** (idempotent on `source_ref`; resolver now matches the new rows; lines DGDF/ZEPF/EMBF/MOOF all resolve). Do NOT hand-write ord_orders bypassing the importer/resolver/event-cascade path.

**② EMPTY-ORDER ALLOCATION — split: ONE is a real FG order, the rest are NON-PRODUCT event/marketing material that must NOT become FG order lines.**
  - **a. `DGDF` EXISTS** (LIVE-VERIFIED `ref_skus` id=9, recipe 31 DrunkBeard "Galactic Drift", format 15 = keg/Fass, is_active=1). So **r80 Street Cellar "3 Galac fûts" → DGDF×3 IS a valid FG order line** (Galac = Galactic Drift = the keg). Kouros's "Galac fûts = DGDF" is CONFIRMED correct. This one row becomes a normal customer order.
  - **b. `Cuves`, `T7`, `Galac` are NOT sku_codes** (LIVE-VERIFIED: T7/CUVES/GALAC exact + substring = 0 hits in `ref_skus`). "Cuves" (r102 Jardins de Louis) and "T7" (r199 Gotham/Marketing, r223 Gotham + "1 pack dec gratuité") do NOT resolve to any SKU.
  - **c. 🔴 RULING — event/serving/marketing material does NOT live in `ord_orders` and must NOT deplete FG.** "T7" (a serving-tank / tap-tower style equipment loan), "Cuves" (serving cuves = equipment, NOT the -V cuv SALE SKU), and marketing giveaways are **non-product / equipment / promo**, not Neb FG beer. Forcing them into `ord_orders` lines = wrongly depleting beer SKUs (the never-guess-a-mapping + one-fact-one-table rule). **DO NOT allocate T7/Cuves to beer SKUs.** Options to put to Kouros (do not guess which): (i) these are equipment loans → out of scope for `ord_orders` entirely (skip; they have no FG depletion fact); (ii) the "1 pack dec gratuité" on r223 IS a real product giveaway → route to the **`inv_side_stock_ledger` `movement_type='giveaway'`** path (mig 303, the unit-count giveaway ledger §ITEM 2) keyed to the actual SKU, NOT an ord_orders line — that's the canonical home for "offered/gratuité" units. The "3 Galac fûts" (DGDF) is the ONLY one of the 4 empty-orders that is a real product order. **Keep the other 3 in the held-out bucket pending Kouros: equipment-loan-skip vs giveaway-ledger; never auto-allocate as beer SKUs.**

**③ WEEK-SCOPED SEARCH (Blind Tigers) — CONFIRMED root cause; fix = drop the date window when a client search term is present. In scope as a quick follow-up.**
  - **Root cause LIVE-VERIFIED** (`expeditions.php`): Commandes defaults `mode=week` / current ISO week (L1205); the Commandes query ALWAYS seeds `$where=['o.requested_date BETWEEN ? AND ?']` (L1475) and ANDs `c.name LIKE` onto it (L1479-1481). Blind Tigers #84/#85 are req_date 2026-06-29 (~3 wk out) → current-week window returns nothing despite a name match. (NB the Carnet-clients directory search L1884-1886 is already date-unscoped — only the Commandes ORDER list is week-trapped.)
  - **HOUSE-PATTERN RULING: when a client search term is present, the client-name filter spans ALL dates — suppress the `requested_date BETWEEN` clause (or widen to an all-time bound) for that query.** Rationale: a name search is an intent to FIND a specific client's orders, not to browse a week; the date window is a browse affordance, not a search constraint. This matches Kouros's expectation and the already-date-unscoped Carnet search. Keep the week/range nav for the no-search browse case. Preserve the SKU/statut/canal filters as-is. Small, surgical, low-risk; **YES in scope as a quick follow-up.** Do NOT build a separate global-search surface — reuse the existing Commandes `client` filter, just un-gate the date window when it's non-empty. **EQUIP: coder + sql + ui + webapp-testing** (read-only prod smoke: search "Blind Tigers" with no kw → #84/#85 appear).

**SEQUENCING:** ① + ② are data + a re-run of the existing idempotent importer (no new build needed beyond creating the 2 privé rows + DGDF allocation + putting the T7/Cuves/giveaway question to Kouros). ③ is the only code change — a surgical edit to one query's WHERE assembly in `expeditions.php`. None are gated on each other. ① forward-hazard (pickup double-deplete) stays PARKED behind the Shopify rework (④ Phase 2).

### ✅✅ WeeklyOrders AUDIT FULLY CLOSED 2026-06-10 (all outcomes VERIFIED LIVE ON PROD)
**Audited the WeeklyOrders sheet (correct tab `gid=1955899648` — the gid in Kouros's URL doesn't exist as a tab) vs `ord_orders`.** Of the apparent misses: **r107 Ailec = NOT a gap** (operator entered the wrong date; same row as web #83 — dropped). **4 orders written** via the importer path (`source='import'` + `source_ref` + events + site + audit, idempotent on `source_ref`); the rest correctly held.

**The 4 writes (verified live):**
- **#101 Street Cellar** (id 833) ← r80, **DGDF×3** (resolves ②a — Galac fûts = Galactic Drift keg, the real-FG empty-order).
- **#102 Joël Golinucci** ← r285, **ZEPF×2 EMBF×2** — NEW `is_private=1` customer **id 3058** (resolves ①d for this row).
- **#103 Alice Buache** ← r298, **ZEPF×2 MOOF×1** — NEW `is_private=1` customer **id 3059** (resolves ①d).
- **#107 Département Marketing** (id 673, BC 1807) ← r223, **PD8×1** `line_comment='gratuité 100%'`, order comment names the Gotham recipient (resolves ②c for the giveaway — see NEW CONVENTION below).

**CONVENTION CONFIRMED — privé = individual `is_private=1` rows** (Kouros option A), NOT a single fold-account. This ratifies ruling ①a: the per-individual model stands; #3058/#3059 created exactly like the existing 83. (Kouros separately wants a Shopify-integration plan-mode later — distinct future arc.)

**🆕 NEW CONVENTION — marketing giveaways (order-attached):** a 100%-rebate giveaway is booked as an `ord_orders` line to the central **Département Marketing account (id 673, BC 1807)**, with the actual recipient named in the **order comment**, the **real SKU at real qty**, so that **Σ(that account's lines × SKU value) = the annual marketing-giveaway total.** This **SUPERSEDES** the earlier ②c suggestion to route order-attached giveaways to `inv_side_stock_ledger movement_type='giveaway'` — that ledger remains the home for loose-unit/non-order giveaways, but an order-attached giveaway is booked on the Marketing customer order. **`ord_order_lines` has NO price/rebate column** — the 100% rebate is expressed via `line_comment='gratuité 100%'`; the value is DERIVABLE from SKU cost, not stored.

**SKU NOTE — `PACKDEC` → `PD8`:** `PACKDEC` is an INACTIVE Shopify alias of the canonical active SKU **PD8 (id 137)** "Pack Découverte 8×33cl" — use PD8.

**③ SEARCH FIX SHIPPED + DEPLOYED** (NOT committed — parallel-session tree; `expeditions.php` Commandes): when a find-intent is active (client-name search, OR `status='ouvertes'`/`'expediees'`), the `requested_date BETWEEN <week>` clause is DROPPED so results span ALL dates; added a new **"Expédiées (toutes dates)"** status option; a **"Résultats — toutes dates · N"** banner + greyed-out week-nav when find-intent active; `LIMIT 500` surfaced. Browse-mode (no filter) unchanged. Fixes the "can't find Blind Tigers" symptom (#84/#85 dated 2026-06-29 were week-trapped). Matches the ③ house-pattern ruling above (un-gate the date window on search-intent; reuse the existing Commandes filter, no separate global-search surface).

**🔴 IMPORTANT BUG RECORDED — `import_weeklyorders.php` line-level idempotency is BROKEN (latent data-integrity risk).** `scripts/import_weeklyorders.php` (~L665) does a plain `INSERT INTO ord_order_lines (order_id_fk, sku_id_fk, qty)` with **NO `ON DUPLICATE KEY UPDATE` and NO UNIQUE on (order_id_fk, sku_id_fk)**. The HEADER is idempotent on `source_ref`, but the LINES are NOT → **re-running the importer DOUBLES every order's lines.** This actually bit us: an idempotency re-run doubled lines on #101/#102/#103 (DGDF×3 became 2 rows, etc.); detected on verification and **deleted the dup line ids 314–318 — final state now correct.** **⚠️ This corrects the PM's prior "re-running import `--apply` is safe/idempotent" guidance — that is WRONG at the LINE level** (only the header is idempotent). **RECOMMENDED FIX (do NOT apply yet — needs team confirm):** add `UNIQUE KEY (order_id_fk, sku_id_fk)` + `ON DUPLICATE KEY UPDATE qty=VALUES(qty)`. **CAVEAT:** only safe if NO legitimate order ever carries the same SKU on two lines (different lot/tier/split) — confirm with the team first. **Also note:** `log_revision` has no `'delete'` action (ENUM is insert/update only — see migration-discipline canon), so the dup-line deletes were NOT audited.

### 🔒 DURABLE RESOLVER RULE — customer resolution MUST follow merge-tombstones to the canonical active record (REUSABLE — all customer-resolving ingests inherit it)
**Generalizes the supplier-alias resolution rule to `ref_customers` + merge-tombstones. Record prominently — this is a recurring-craft rule, candidate for the `sql`/`coder` skill if it bites a third surface.**

**RULE:** ANY customer resolution — the WeeklyOrders importer, the future **commandes@ email-order ingest**, AND the **BC sales-ledger ingest** (the one ruled on in §ITEM 1 / sales-commercial-surface) — MUST:
1. **Follow `ref_customers.notes` `merged_into: N`** to the canonical record (depth-capped to avoid cycles), and
2. **REFUSE tombstoned (`is_active=0`) matches** — never point an order/sale at a dead stub.

**WHY (the bug this prevents, observed in the original importer):** the first importer matched **tombstoned alias stubs** directly. Because a stub's id ≠ its canonical's id, the `(customer_id_fk + date)` collision check **silently missed 7 of the 12 KW24 web-overlap duplicates** (stub-id never equals the web order's canonical customer_id_fk → no collision detected) AND would have written orders pointing at dead records. The merge-tombstone follow-through is what makes the collision check correct AND keeps FKs on live customers. **The BC sales-ledger ingest you ruled on (§ITEM 1) MUST inherit this exact rule when resolving N° origine → ref_customers** — same tombstone-follow, same refuse-dead-stub discipline.
