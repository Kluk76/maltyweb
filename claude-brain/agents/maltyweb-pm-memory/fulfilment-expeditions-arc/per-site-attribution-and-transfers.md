# Fulfilment — Per-site sales attribution + inter-site transfers (ITEM 1, mig 299)

## 2026-06-09 — ITEM 1: PER-SITE SALES ATTRIBUTION + INTER-SITE TRANSFERS — FINAL SPEC (dispatch-blocking; Kouros's forks resolved)
**Scope = FULL** (transfers + per-order ship-from attribution); SKU total unchanged. Resolved against live code 2026-06-09.

**CODE-FACTS RESOLVED:**
1. **mig 294 `294_ord_orders_source_ref.sql` = `source_ref VARCHAR(190) NULL` + `UNIQUE uniq_ord_source_ref` — the WeeklyOrders idempotency key (ruling #6), NOT a site/fulfilment column. ZERO collision with `fulfilment_site_id_fk`.** Both can ALTER ord_orders in separate migrations; additive ADD COLUMN, no overlap. Parallel session owns 294/295(submitted_by_user_fk on bd_*)/296(bbt_vide)/297(pertes threshold)/**298(journal_saisies, ALREADY taken locally)**. → THIS build's mig must be **≥299** (verify at dispatch).
2. **Serving-tank-clients page = `public/modules/reglages-generaux.php` section `?sec=pkg_clients`** ("Clients packaging · Livraison cuve de service · venues · festivals"), table `ref_packaging_clients` (mig 237; venues/events for cuv keg delivery; full CRUD: add ~L280, edit ~L315, toggle ~L337, list ~L1510+). **BUT: `ref_packaging_clients` = venues/events (cuv destinations), a DIFFERENT identity from `ref_customers` (B2B sold-to). The Nausikraft-delivered clients are `ref_customers` rows, NOT packaging-clients.** So the Nausikraft list is a SIBLING admin section on the same page (`?sec=` new tab) that flags `ref_customers` rows — it mirrors the pkg_clients CRUD *pattern* but operates on `ref_customers`. (NB `ref_serving_tanks` mig 142 = in-house TANK capacity registry, irrelevant.)
3. **Order creation = `public/modules/expeditions.php`** saisie POST: INSERT `ord_orders` L521-551, UPDATE (edit) L467-490, lines DELETE+INSERT L497-551, status event L567. `transporter_id_fk` already follows the "customer default → per-order override" pattern — **mirror it exactly for site.** `scripts/import_weeklyorders.php` is the bulk-import twin (must set fulfilment_site_id_fk via the resolver too).

**A. SCHEMA (types verified: `ref_customers.id` + `ref_sites.id` both `INT UNSIGNED`; `ord_orders.id` BIGINT UNSIGNED):**
- **`inv_stock_movements`** (NEW table) — inter-site transfers: `id` BIGINT UNSIGNED PK; `sku_id_fk` INT UNSIGNED NOT NULL FK→ref_skus(id) RESTRICT; `from_site_id_fk` + `to_site_id_fk` INT UNSIGNED NOT NULL FK→ref_sites(id) RESTRICT (CHECK from≠to); `qty` DECIMAL(10,2) NOT NULL CHECK>0 (SKU units, mirror ord_order_lines); `moved_on` DATE NOT NULL; `comment` TEXT NULL; `is_tombstoned` TINYINT(1) NOT NULL DEFAULT 0 (admin tombstones, NEVER delete — mirrors event-table discipline); `created_by_user_id` INT UNSIGNED NULL FK→users(id) SET NULL; created_at/updated_at. + schema_meta row (table_class='transactional', corrections_policy: tombstone-only). Both sites must `holds_fg_stock=1` (validate in handler, not CHECK).
- **`ord_orders.fulfilment_site_id_fk`** INT UNSIGNED NULL FK→ref_sites(id) ON DELETE RESTRICT (mirror `transporter_id_fk` nullability/FK-action). NULL = historical/pre-feature → resolver falls back to channel default at read. Separate migration from 294 — no contention.
- **`ref_customers.default_delivery_site_id_fk`** INT UNSIGNED NULL FK→ref_sites(id) ON DELETE SET NULL (mirror `default_transporter_id_fk` exactly). This IS the "Nausikraft list" mechanism: Nausikraft-delivered clients = `ref_customers` rows with this set to the Nausikraft site (id4). NO separate flag/table — a nullable default-site FK is the minimal, single-homed mechanism (mirrors the transporter default already proven). NULL = no client default → resolver uses channel default.

**B. RESOLVER (single source — ONE helper both order-create-default AND snapshot-sales-attribution call):** `app/fulfilment-site.php` `resolve_fulfilment_site(PDO, $order_row): int` returning a ref_sites.id. **Precedence CONFIRMED (Kouros's inferred order is correct):**
  1. **per-order override** — `ord_orders.fulfilment_site_id_fk` if NOT NULL (operator's explicit pick wins).
  2. **client default** — `ref_customers.default_delivery_site_id_fk` (the Nausikraft list) if order_type='customer' and set.
  3. **channel default** — wholesale/B2B (order_type='customer', no client default) → Crissier (warehouse, id2); internal eshop → Crissier (id2); internal taproom → Taproom (id3). (Kouros: eshop default=Crissier, taproom base=Taproom.)
  Channel-default site ids come from `ref_sites` (DB-derived by site_type/role, NOT hardcoded — eshop/wholesale→warehouse site, taproom→pos site; if multiple, the canonical default is config — recommend a `commissioning_settings` row or the lowest-sort warehouse). The resolver is PURE (no writes); used at order-create time to seed the selector default AND at fg-stock snapshot time to attribute sales.

**C. fg_stock_location_snapshot() sales attribution:** per-site sales = Σ(sales legs) WHERE `resolve_fulfilment_site(order)` = this site. MUST reuse the ONE resolver (no inline channel→site map). Keep the Σ(cards)==physique invariant: the prod leg already credits the production site; sales legs now SUBTRACT from the resolved ship-from site (today they subtract globally per §PRODUCTION-LEG FIX OPEN/DEFERRED — THIS build closes that "sales-leg per-location settlement" carryover). Historical orders with NULL fulfilment_site → resolver returns channel default (step 3) — never errors, always attributes. eshop=`inv_sales_orders`, taproom=`inv_sales_bc`, B2B=`ord_*` (R1 source map unchanged).

**D. DISPATCH SEQUENCE (Kouros's order CONFIRMED, one reorder note):** (1) migrations [inv_stock_movements + ord_orders.fulfilment_site_id_fk + ref_customers.default_delivery_site_id_fk — 3 ALTERs/CREATE, can be ONE migration file or three; recommend ONE]; (2) **resolver helper `app/fulfilment-site.php`** (build BEFORE both consumers — it's the single-source spine); (3) order-creation ship-from selector in expeditions.php (defaulted by resolver, overridable) — **do this BEFORE snapshot attribution** so test orders carry real sites; (4) snapshot sales-attribution rewire (consumes resolver); (5) Nausikraft-client admin list on reglages-generaux.php (sets `ref_customers.default_delivery_site_id_fk`); (6) Mouvements transfer tab on expeditions.php (operators+ record; admin tombstone). **EQUIP per sub-build:** (1) sql+coder; (2) coder+sql; (3) coder+sql+ui+webapp-testing; (4) sql+coder+webapp-testing; (5) coder+sql+ui; (6) coder+sql+ui+webapp-testing.

**E. MIGRATION NUMBERS:** local head includes 298 (journal_saisies, parallel/untracked). NEXT FREE = **299** for this build (re-verify `migrate.php --status` + `ls db/migrations` AT DISPATCH — parallel sessions lead; 294-298 all parallel/untracked). If three separate migs: 299/300/301; if one: 299. Recommend ONE migration `299_fulfilment_site_attribution.sql` (CREATE inv_stock_movements + 2 ADD COLUMN + 2 FK + schema_meta) — atomic, fewer numbers to contend.

**F. LANDMINES:** (i) **ord_orders contention** — do NOT touch 294's source_ref; your ADD COLUMN is independent; never re-ALTER in a way that re-declares their column. (ii) **single-resolver** — order-create default and sales-attribution MUST call the same `resolve_fulfilment_site()`; an inline duplicate map = the divergent-store anti-pattern → cards/total drift. (iii) **beer-tax neutrality** — site attribution is a LOCATION fact, not a quantity fact; total packaged-HL and beer-tax base unchanged (Σ across sites = same total). (iv) **no parallel FG-depletion path** (R1) — `inv_stock_movements` is a TRANSFER (moves stock between sites, net-zero to global physique), NOT a sale; it must NOT enter the global depletion legs, only shift the per-site split. fg_stock_compute() global total ignores transfers; ONLY fg_stock_location_snapshot() applies them (+to_site, −from_site). (v) **Nausikraft identity** — Nausikraft is BOTH a `ref_sites` row (id4 consignment, customer_id_fk=1) AND its delivered clients are `ref_customers` — don't conflate; the client list sets `default_delivery_site_id_fk=4`. (vi) **import_weeklyorders.php** must also resolve+set fulfilment_site_id_fk (else bulk-imported orders all NULL→channel-default, acceptable but flag). (vii) **transfers vs cuv** — cuv (serving-tank) is filled-at-client = already excluded from FG; a transfer of FG between sites is distinct, don't let cuv leak into inv_stock_movements.

## ✅ ITEM 1 SHIPPED — stock-movement saisie + per-site sales attribution (2026-06-09, mig 299, autonomous run)
Built EXACTLY to the §A-F design above. 3 phases, 3 commits pushed to origin/main, all deployed live + render-smoked + invariant-verified (Opus independent). Kouros offline; he picks up the one DATA item tomorrow.

**Phase 1 — mig 299 + the single resolver (commit `3c3eb33`):**
- **mig `299_fulfilment_site_attribution.sql` APPLIED** (one atomic file, per §E recommendation). New table **`inv_stock_movements`** (id, sku_id_fk, from_site_id_fk, to_site_id_fk, qty DECIMAL(10,2), moved_on DATE, comment, is_tombstoned, created_by_user_id, created_at/updated_at; **CHECK from<>to AND qty>0**; FKs RESTRICT→ref_skus/ref_sites, user SET NULL; schema_meta source/tombstone-only). ALTER `ord_orders` ADD `fulfilment_site_id_fk` INT UNSIGNED NULL FK ref_sites RESTRICT. ALTER `ref_customers` ADD `default_delivery_site_id_fk` INT UNSIGNED NULL FK ref_sites SET NULL (mirrors `default_transporter_id_fk` — the Nausikraft mechanism).
- **`app/fulfilment-site.php`** = THE single resolver (the §B/landmine-(ii) spine): `resolve_fulfilment_site(PDO,$ctx):int` + `fulfilment_default_sites(PDO):array`. Precedence per §B: per-order `fulfilment_site_id_fk` override > customer `default_delivery_site_id_fk` > channel default (taproom→pos, eshop/wholesale→warehouse). Sites DB-derived from `ref_sites` by role (warehouse=Crissier id2 / pos=Taproom id3 / production=Renens id1), **never hardcoded**. Has a `_customer_default_site_id` prefetch short-circuit (added Phase 2) to avoid N+1.

**Phase 2 — sales attribution + ship-from selector + Nausikraft admin (commit `49a0386`):**
- **`fg_stock_location_snapshot()` now attributes SALES per site** — replicates `fg_stock_compute()` steps 4/5/6 cutoffs EXACTLY, grouped by `resolve_fulfilment_site` (NEVER an inline map → landmine-(ii) honored) + applies TRANSFERS from `inv_stock_movements` (+to/−from, is_tombstoned=0, moved_on>anchorDate). `fg_stock_compute()` TOTAL unchanged (landmine-(iv): transfers shift the split only, net-zero to global). Cards CAN go negative (honest signal, not clamped). **Invariant Σ(cards)==Σ(physique) holds** — Opus-verified 8642==8642, 0 negatives today (in-window sales=0). This CLOSES the long-deferred "sales-leg per-location settlement" carryover (attribution done; see KNOWN LIMITATION 1 for the cutoff-granularity remainder).
- **`expeditions.php` order saisie + `scripts/import_weeklyorders.php`:** "Site d'expédition" selector — "Automatique" (NULL→resolver) or explicit holds_fg_stock site; mirrors transporter_id_fk; persisted INSERT/UPDATE + log_revision; import resolves+sets per row (landmine-(vi) honored).
- **`reglages-generaux.php` NEW `?sec=delivery_sites`:** admin assigns/clears `ref_customers.default_delivery_site_id_fk` (the Nausikraft-delivered-client list). 2688 customers via datalist; 4 holds_fg_stock sites. **Operator enters the actual Nausikraft client list TOMORROW via this UI** (= the one DATA item pending; architecture + UI ready).

**Phase 3 — Mouvements transfer tab (commit `edfcf78`):**
- **`expeditions.php` new `?view=mouvements`** (operators+ record; admin tombstone; immediately live): record form (sku/from/to/qty/date/comment) → `add_movement` (CSRF, validate from<>to + strict int qty>0 + whitelist sites, INSERT, log_revision, PRG); recent-movements list; `tombstone_movement` (admin-only, is_admin defense-in-depth, `is_tombstoned=1` NEVER DELETE — tombstone-only per schema_meta). CSS in `/public/css/expeditions.css` scoped `body.expeditions`. schema_meta.writer_script set to this tab.
- **End-to-end verified:** DIV4 transfer Renens→Crissier qty5 → Zgeg 449→444, Zeubi 39→44, Σ unchanged; tombstone reverts; CHECK constraints reject from==to & qty<=0 (landmine-(iv)/(vii) hold — transfer is net-zero, distinct from cuv/sales).

**VERIFICATION (Opus independent):** all 5 surfaces render-smoke OK via forged-admin-session CLI harness (form/stock/mouvements/commandes/delivery_sites, no fatals); invariant Σphysique==Σcards==8642; resolver 5 precedence cases pass; **only intended files touched each commit** — the concurrent session's form-packaging/packaging-stats/migs 293-298 NOT committed by this run.

### ✅ MULTI-SKU MOUVEMENT INPUT SHIPPED + LIVE + COMMITTED + PUSHED 2026-06-10 (`badd153` on origin/main — bundled, UNDER-DESCRIBED) — `?view=mouvements` extended scalar→multi-line; NO migration (HEAD stays 303); built to PM recommendation, zero divergence; EQUIP ui+sql+coder +webapp-testing
Extends Phase-3's single-SKU `add_movement` to a **shared-header + repeatable SKU+Qté line grid** (operator confirmed "same route, pick once" — Site origine / Site destination / Date / Commentaire chosen ONCE; per-line destination override deferred). +551/−95 lines across 3 files; git status clean of stray files.
- **Handler `add_movement` (expeditions.php L936+):** scalar → parallel arrays `mov_sku_id[]` / `mov_qty[]`. Header validated once; each line validated independently (active SKU, strict-integer qty>0, blank rows skipped); **ALL errors collected BEFORE `beginTransaction()` → all-or-nothing** (any bad line → ZERO writes, flash names "Ligne N"); one transaction wraps N inserts; **per-row `log_revision` PRESERVED inside the loop**; flash "N mouvements enregistrés."; PRG.
- **SINGLE-HOME PRESERVED (the key architectural verdict):** writes the SAME N `inv_stock_movements` rows — **NO batch/header table, NO new migration.** A multi-SKU transfer is N transfer facts sharing a route, not a new entity. One fact, one canonical table.
- **NET-ZERO INVARIANT VERIFIED:** `fg_stock_location_snapshot` total == `fg_stock_compute` physique = **8599** before AND after a 3-row test submit (test rows tombstoned + self-cleaned). Transfer leg stays net-zero/compute-exempt per ITEM 1 landmine-(iv) — multi-line changes nothing here (still N net-zero rows).
- **UI:** 3 rows pre-shown + "+ Ajouter une ligne" + × per-row remove; advisory per-line amber over-transfer hint from `window.EXP_MOV_STOCK_MAP` (injected via `fg_stock_compute()`, ADVISORY-ONLY, never blocks). **Key fix:** `expeditions-form.js` `<script>` tag was previously loaded ONLY on `view=form` → now also on `view=mouvements` (new `initMovLines()`). Files: `public/modules/expeditions.php`, `public/js/expeditions-form.js`, `public/css/expeditions.css` (`.exp-mov-line*`, 44px touch targets, 700px/480px breakpoints — floor-tablet discipline).
- **Smoke (webapp-testing, authenticated):** 3 rows render + add/remove work; 3-line submit → 3 rows + correct flash; qty=0 on line 2 → ZERO rows + "Ligne 2" flash (all-or-nothing proven); net-zero held. Opus spot-checked handler diff: all-or-nothing + per-row audit + single-home confirmed.
- **Visite guidée:** NO new `ref_pages` row — same page, extended form; no tour card touched (correct — tour anchors on pages/views, not form-internal layout).
- **✅ COMMITTED + PUSHED (NOT as planned):** landed in **`badd153`** `feat(expeditions): Stock PF predictive-column readability + order stock-warning detail modal` — already on origin/main. A PARALLEL session (mon-tableau Phase 2b) committed FIRST and BUNDLED three in-flight features into one commit: (1) our multi-SKU mouvements (`movValidLines` loop + per-row log_revision at expeditions.php:1013-1054, `mov_sku_id[]` parallel array, `initMovLines` in expeditions-form.js); (2) order stock-risk detail modal (`exp-sdm`, `EXP_CMD_STOCK_DETAIL`); (3) Stock PF grouped-header restyle (`exp-st-th-group`, colspan 8→11). ⚠️ **The commit message names only features 2+3 — multi-SKU mouvements is committed but UNMENTIONED (under-described).** Handler VERIFIED intact in HEAD (loop + per-row audit present). Our planned per-feature commit split was PRE-EMPTED. **🚨 TWO-CONCURRENT-SESSIONS HAZARD (today, this repo):** at the time of writing HEAD=`e32d472` (mon-tableau sales Phase 2b, unpushed, ahead 1) with a DIRTY tree (kpi-handlers, kpi-charts, packaging-form, mon-tableau.css, migs 310/313) — all the OTHER session's work, not ours. With deploy-pushes-the-working-tree, an `--apply` from either session leaks the other's uncommitted changes live (see checklist hazard). Confirm `git status` before any deploy.

**KNOWN v1 LIMITATIONS (refine with operator):**
1. ~~Snapshot transfer + sales cutoffs use the GLOBAL `anchorDate`, NOT per-site.~~ **✅ CLOSED for ESHOP + TRANSFER by Task A 2026-06-10 (commit `2192153`); see §TASK A below.** Eshop snapshot+compute legs now share the IDENTICAL per-(sku,site) `>=`+created_at tiebreak (symmetric → Σcards==Σphysique under same-day); transfer leg = atomic per-row UNIT-GATE (net-zero, compute-exempt). **REMAINING per-site cutoff refinement: B2B leg = strict-global symmetric — 🔴 RECLASSIFIED 2026-06-10 as a REAL BUG (global anchorDate pollution drops same-day ships), NOT "intentional"; ruled in §B2B EXPEDIE LEG = REAL COMPUTATION BUG; taproom leg = month-grained (deferred); per-site same-day TRANSFER precision (the unit-gate trade-off — see Task A).**
2. Nausikraft client list = DATA pending (operator enters tomorrow via `?sec=delivery_sites`). Architecture + UI ready.
