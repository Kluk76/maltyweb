# Packaging BOM — ZEP6C liquid-only + compiler gate, contracted-out packaging recurrence, contract event sku_id_fk NULL, format→SKU residual-gap triage, BLA4 white-label, log_scotch_eshop, recompute service, on-save trigger, deferred observed-liquid recompute

## ✅ ZEP6C = liquid-only (externally packaged) + COMPILER GATE-HONOR LANDED (commit `de90755` + migration 191, PM-verified live 2026-05-28)
**ZEP6C** (6C format, **`ref_packaging_formats` id10, catalog_id NULL**) was **brewed by La Nébuleuse but PACKAGED BY AN EXTERNAL BREWERY** (single 2024 contractor-packed run; **discontinued, no current plan to recur**). It carries a **LIQUID-ONLY BOM (no packaging consumables).** Could one day become a full internal SKU **IF the 6C boxing format is activated in capacités** — see RECURRENCE MODEL below for the right path when that happens.
- **STATUS = CLOSED 2026-05-28** on both legs: (a) the compiler buildability gate now INNER-JOINs the commissioning chain (`ref_filler_containers × ref_process_machines × dbc_packaging_format_templates × dbc_container_types`), matching the UI gate `sdc_gated_format_ids()`; (b) ZEP6C (id=57) deactivated via migration 191 as defense-in-depth (`is_active=0`).
- **As-built — `app/sku-bom-compile.php` (commit `de90755`, +147/-9 lines, 7 edits):**
  1. **Buildability gate hardened across 3 query branches** (normal at ~159, COLLAB UNION normalIds at ~194, COLLAB UNION collabIds at ~221): all three now INNER-JOIN the dbc commissioning chain. **New helper `_compiler_gated_format_ids()` at EOF — same SQL signature as `sdc_gated_format_ids()` in `public/modules/salle-de-controle.php`.** Both gates resolve IDENTICALLY now — no longer a divergent-store smell. **Comment marks the helpers MUST stay in sync** (durable convention: PM enforces parity on any future gate-rule change).
  2. **Composite DELETE predicate widened** (~line 1169 live + ~1141 dry-run COUNT mirror): `bom_source IS NULL` → `(bom_source IS NULL OR bom_source IN ('composite_liquid','composite_packaging'))`. **Closed an active doubling bug** — any `--apply --sku 137,138,278,295` recompile would have doubled rows (existing 211 + new 211 = 422). Verified pre/post-patch dry-runs.
  3. **Non-composite DELETE predicate flipped** (~line 781): `source = 'Packaging'` → `(source <> 'Brewing' OR source IS NULL)`. Drift-proof: any future legacy tag (mi_match, Packaging, NULL) gets purged on regenerate; only Brewing rows survive. Matching `_bom_count_packaging` helper at ~line 1395 updated for honest dry-run reporting. **Supersedes the prior "compiler DELETE-predicate gap" — now closed for ALL legacy non-liquid tags, not just `'Packaging'`.**
  4. **UoT (refuse-don't-NULL) assertion at function entry** (~line 499, AFTER `$summary = []` + `$totalErrors = 0;` inits): for each requested SKU, if format is NOT in `_compiler_gated_format_ids()` AND no composite slots → SKIP with specific error `"format_id={n} not commissioned (not in dbc gate) and not composite"`. Surfaces future un-gated activations VISIBLY (CLI STATUS=ERROR row). **NEW PRECEDENT for refuse-don't-NULL: assertion runs BEFORE the main processing loop, on requested-SKU level.**
  5. **Foreach skip-guard** (~line 561): prevents the pre-existing missingIds path from clobbering the UoT-specific error message or double-incrementing `$totalErrors` when UoT already marked the SKU.
- **Migration 191 (applied 2026-05-28):** ZEP6C (id=57) → `is_active=0`. Single 2024 contractor-packed run, 1 bd_packaging_v2 event id=1213 (42912 cans ≈ 214.56 hl), 0 sales rows in 2025-12+ window (2024 sales predate MySQL sales tracking). Defense-in-depth alongside the gate fix.
- **Verification artifacts:** pre-patch dry-runs (composite DEL=0 INS=211 = would double; ZEP6C INS=3 = phantom-fill); post-patch dry-runs (composite DEL=211 INS=211 = no-op; ZEP6C errors=1 status=ERROR with UoT-specific message; auto-detect 0/0/0 unchanged); post-rsync production CLI runs match `/tmp` test wrapper byte-for-byte; `php -l` clean.

## 🟢 CONTRACTED-OUT PACKAGING RECURRENCE MODEL (PM-documented 2026-05-28 — the right path when 6C-style ever recurs)
Operator confirmed ZEP6C was the **ONLY** contracted-out SKU ever (discontinued, no plan but not impossible to recur). The 2024 contractor invoice hit account **4208 "frais de packaging externe"** via direct supplier invoice — already in 2024 COGS at period level, no per-SKU allocation existed at the time. **When it recurs, the path is — design then, not now:**
1. **New SKU code** — operator picks (internal-only marker if any; the SKU code is the customer-facing handle).
2. **`ref_packaging_bom_templates` row** for that (format_id, recipe_id) with **`supply='client_supply'`** (the **dormant ENUM lever**) → the compiler's INNER-JOIN on `supply='we_supply'` naturally REJECTS packaging emission. Liquid-only BOM via the existing `source='Brewing'` path. **NO new format code, NO new SKU flag, NO schema change.**
3. **Synthetic per-unit MI** e.g. `PKG_EXT_PACKAGING_<CONTRACTOR>_<FORMAT>` mapped to account **4208** → contractor invoice's per-unit fee from standard WAC pipeline → BOM line emitted at `client_supply` template emission time (small compiler extension at that point — design then, not now).
4. **Result:** per-SKU contractor cost in COGS, allocated honestly to sold units. **The thing we couldn't do for ZEP6C in 2024.**

Compiler buildability gate naturally protects the rest — anyone activating a `client_supply` format without commissioning gets the **UoT assertion error** (refuse-don't-NULL). **This is the canonical recurrence pattern; record any deviation as a PM ruling before building.**

## 🟢 CONTRACT PACKAGING EVENT → `sku_id_fk` is CORRECTLY NULL by design (PM ruling 2026-05-27, the converse of ZEP6C)
**The semantic for `bd_packaging_v2.sku_id_fk` on contract-packaging events (WE pack another brewery's beer — Chien Bleu, BLZ, MeltingPote, Brasserie du Château, BadFish, Abbaye de St-Maurice, …):** there is **NO La Nébuleuse SKU** for another brewery's beer, and **`ref_skus` carries NO contract-SKU concept** (it is the Nébuleuse offer catalog; no client/contract column). Therefore `sku_id_fk = NULL` is the **CORRECT** state for these events — NOT a resolution gap. This is *refuse-to-invent*: do NOT mint a contract-SKU to fill the NULL (that would be a parallel store of a fact the CLIENT axis already owns).
- **They resolve on the CLIENT axis, not the SKU axis:** the identity of a contract event is `client_fk` → `ref_clients` + the liquid recipe (`recipe_id_fk`, the client's beer) + `run_type`/`nebuleuse_format_suffix`. NOT a `ref_skus` row.
- **PM-verified live 2026-05-27 — the 248 bd_packaging_v2 sku_id_fk NULLs split:** **244 = contract events** (contract_beer set, neb_beer NULL) → **correctly NULL** (reclassify triage flag `sku_unresolved → contract_run`); **4 = Nébuleuse-product rows** with a neb_beer that genuinely failed SKU resolution → **the only "needs fixing" slice**; 0 orphan.
- **✅ RESOLVED 2026-05-28 (migrations 187/188/189, commits 849426d + cab522f, PM-verified live):** the 8 Neb-product NULLs are FIXED and the 240 true-contract rows are flagged `contract_run`. End-state: `sku_id_fk IS NULL` 248→**240** (all 240 = true contract, all carry `contract_run`); 8 resolved rows correct (EPH 29/32/35/38 + Docks/DGD CollabIn DOCB/DOCF/DGDB/DGDF = 21/22/8/9); 0 stale unresolved flags. **The "4 Neb-product" slice in practice resolved as 8 rows: 4 EPH (BC→C fold, see §EPH-BC→C below) + 4 Docks/DGD CollabIn rows rerouted to the Neb branch** (`classification='Neb'` recipes incorrectly sent to the contract branch by the old normalizer; neb_beer + suffix B/F set, flag `collabin_rerouted_to_neb`). **The CHECK `chk_sku_or_flagged` was BROADENED (DROP+recreate) to add a `contract_run` branch** — now `sku_id_fk NOT NULL OR sku_unresolved OR contract_run OR row_origin='parallel'`; integrity preserved (every row still carries a valid disposition). The 240 contract rows remain correctly NULL on `sku_id_fk` — they resolve on the CLIENT axis (the two prereqs below are still OPEN, gated on commissioning).
- **This is the MIRROR-IMAGE of ZEP6C (above):** in ZEP6C **we are the CLIENT** of a contract packager who supplied materials (→ no consumption for us); in these 244 **we are the PACKAGER** of someone else's beer (→ no Nébuleuse *product* SKU/COGS, but may consume OUR packaging stock per the per-client material-responsibility rule). Both resolve to the same primitive: **we don't book what we didn't commission as our own product.**
- **TWO open prereqs this exposes (recorded — both gated, NOT now-tasks):**
  1. **`client_fk` is UNRESOLVED on all 244** (only the `contract_beer` "Client - BeerName" string is set; `client_fk=0`/NULL). For the material-responsibility branch to EVER apply, the contract events need client resolution into `ref_clients` (parse `contract_beer` → client). Until then there's no client to read the flag from.
  2. **`ref_clients` has NO materials-responsibility flag** (cols: id/name/notes/created_at only — PM-verified live 2026-05-27). The recorded per-client COGS rule ([[contract-packaging-cogs-per-client]]) prescribes `materials_supplied_by = nebuleuse|client` (or a bool); that column **does not yet exist** and must be added before any contract packaging-consumption can branch correctly.
- **Consumption branch (when built — prereq-2 / production-SKU depletion):** ONLY `nebuleuse`-responsible contract runs emit packaging-material `inv_consumption` (material-BOM keyed on run_type+format, independent of the liquid recipe since the beer is the client's), tagged `contract` so excluded from Nébuleuse *product* COGS but inventory stays accurate. Client-supplies-own → NO consumption. Cross-link: rm-retro-prereq-chain.md PREREQ 2.

## §FORMAT→SKU RESIDUAL-GAP TRIAGE (#44) — ESSENTIALLY CLOSED (batch landed `7f86499`, migs 185+186 applied live 2026-05-27, PM-verified)
`#44` (orchestrator ordinal) was NOT a UI build — the format→SKU activation surface is already live in `salle-de-controle.php`. #44 = closing the residual gap list. **OUTCOME: 185+186 landed; 187 SKIPPED; 6C-hardening + PAC + PBD-Dec-cost-basis are the residuals.** Commit `7f86499` on main; both migrations recorded in `schema_migrations` (185_deactivate_pbd_discontinued_sku.sql, 186_collab4pack_link_format_4pb.sql, applied_at 2026-05-27 21:15). PM-verified live: PBD(294) is_active=0 recipe/format NULL; COLLAB4PACK(302) format_id=4 is_active=1.
- **✅ PBD (ref_skus id=294): DEACTIVATED via mig 185.** `is_active=0`, NO alias — Kouros ruling = **distinct DISCONTINUED product** (NOT a PD8 synonym). Was a zero-cost active silent COGS leak (0 BOM/0 slots/0 alias, was is_active=1). **⚠️ SAFETY-CHECK FINDING (RETRO FLAG): PBD has 210 `inv_sales_order_lines` spanning Dec 2025 — it really sold.** Deactivation is the going-forward fix (preserves those as read-only history), but the 210 Dec lines will resolve to ZERO COGS in the Dec retro unless PBD's real Dec composition is established → folded into the RM-retro arc as a Dec-prerequisite (see rm-retro-prereq-chain.md). schema_meta ref_skus=allowed.
- **✅ COLLAB4PACK (ref_skus id=302, 4×33cl): LINKED to format 4 (4PB) via mig 186 — bottle-level-only.** Empirically validated: format 4 `display_name`="4-pack loose bottles (4×33cl)", run_type=bot, hl_per_unit=0.0132, catalog_id=5; **12 sibling `{BEER}4PB` SKUs (ZEP4PB/DIV4PB/EMB4PB/MOO4PB/STI4PB/SPY4PB/DOA4PB/DIB4PB/EPH24PB/EPH44PB/EPH4PB) all carry identical label + 0.0132 hl** → COLLAB4PACK is the collab member of that loose-bottle family. **The holder slot (ref_packaging_items id=21) is INERT (default_mi_id_fk=NULL, we_supply_only) → no phantom holder billing.** No outer_box slot on format 4 ⟹ 4PB NEVER phantom-fills an outer carton. The earlier "loose vs 4-pack holder" carve-out is RESOLVED by the empirical family: all 12+1 SKUs on 4PB are loose, no holder. Mixed-box ASSEMBLY (4 collab bottles absorbed into a parent mixed box) stays a Stage-2 consumption-matrix concern, DEFER.
- **✅ OPERATOR CONVENTION CONFIRMED (durable): `-4P` = WITH holder; `-4PB` = WITHOUT holder (= 4 loose bottles).** This is the suffix discriminator on 4-pack SKUs. Empirically all 12+1 SKUs on format 4 (`4PB`) are loose, no holder. (Distinct from the F=keg/4=carton/B=box/BU/CU=single conventions in reference_sku_naming_convention.)
- **⏭ 33C (format 13) + V (format 18) `ref_packaging_format_mis` edges (was mig 187): SKIPPED — confirmed nothing to fix on COST.** `ref_packaging_format_mis` is read by NOTHING live (sole consumer = `public/_design/salle-recettes.html` static mockup); the live BOM compiler reads `ref_packaging_items`+`ref_packaging_bom_templates`. 33C/V already cost CORRECTLY from ref_packaging_items. The 33C container edge was ALSO blocked on an unpriced can-body MI (**PKG_CAN_ALU_33 id=617 = NULL-price stub**). **Recorded as a DORMANT metadata item — revisit ONLY if `ref_packaging_format_mis` ever gets a live reader.** Not a COGS fix; no migration spent.
- **✅ 6C / ZEP6C (ref_packaging_formats id=10) compiler-gate-honor fix → CLOSED 2026-05-28** (commit `de90755` + migration 191). Both legs shipped: (a) compiler buildability gate now INNER-JOINs the dbc commissioning chain across all 3 query branches via new helper `_compiler_gated_format_ids()` (matches UI `sdc_gated_format_ids()` IDENTICALLY — divergent-store smell closed); (b) ZEP6C deactivated. Composite DELETE doubling bug ALSO closed in the same commit (widened to `bom_source IN ('composite_liquid','composite_packaging')`); non-composite DELETE drift-proofed (`source <> 'Brewing' OR source IS NULL`). UoT refuse-don't-NULL assertion at function entry surfaces any future un-gated activation visibly. Full as-built → §ZEP6C above.
- **🟡 PAC (composite, id 295): unmapped format_mis — OPEN architectural Q, DEFER.** Whether composites need `ref_packaging_format_mis` edges AT ALL is unresolved: PD8/PAL/XMASPACK HAVE them, PAC does NOT — is slots+choices sufficient for a composite, or does a composite format still need format_mis edges? Composites derive packaging from slots/choices (not format_mis), AND format_mis feeds nothing live → leaning "slots+choices sufficient", but unconfirmed. Do NOT add PAC edges. Open item for later.
- **Migration numbering (PM-verified live 2026-05-27): latest applied = 186.** 185+186 BOTH recorded in `schema_migrations`. `migrate.php --status` = 0 pending. Next free = **187** (NOT the 33C/V edges — those were skipped). ALWAYS re-run `--status` before assigning (VPS sequence can lead local git — the untracked 166/167/168 lesson).

## ✅ BLA4 WHITE-LABEL = first live white-label model (migration 148, committed `18ebbb9`, pushed to origin/main; RECORDED in schema_migrations; PM-verified live 2026-05-26)
**BLA = "Blanche des Romands"** — a DISTINCT white-label brand, NOT BLO ("Blonde des Romands", recipe 10). The earlier BLA/BLO conflation flag (sku-decomposition-tree.md step ⑤) is RESOLVED: BLA got its own MIs.
- **Migration 148 created 3 BLA-branded MIs** (mirror the BLO attrs, price NULL): **PKG_LABEL_BLA (id 646), PKG_4PACK_BTL_BLA (id 647), PKG_6X4_BTL_BLA (id 648).** PM-verified live.
- **BLA4 (sku id 4, format 3 = 6×4-bottle) linked to recipe MOO (r44)** — liquid comes from MOO; packaging is BLA-branded.
- **3 `ref_sku_packaging_choices` Tier-1 overrides** force `label`→646, `outer_tray`→647, `outer_box`→648 (beating the compiler's `{beer}`-from-recipe pattern, which would have picked MOO-branded packaging). PM-verified: all 3 choices live, is_checked=1.
- **Recompiled → 5-line BOM (PM-verified live):** 24× PKG_BOT_PIVO (vol 0.0792 on the bottle/container line) + 24× PKG_BOT_CROWN_CAPS + 24× PKG_LABEL_BLA + 6× PKG_4PACK_BTL_BLA + 1× PKG_6X4_BTL_BLA. `is_active=0` (discontinued).
- **🟢 THE WHITE-LABEL PATTERN (template for ALL future white-labels), CONFIRMED LIVE:** *liquid-from-recipe-X + packaging-brand-overridden-to-Y via `ref_sku_packaging_choices` (highest-precedence resolver input)*. This is exactly sku-decomposition-tree.md step ⑤ working live — the design is proven. Any future white-label: create brand-Y `PKG_*` MIs, link the SKU to recipe-X, insert per-slot `ref_sku_packaging_choices` overrides → recompile.

## LOG_SCOTCH_ESHOP — ⚠️ PARTIALLY SUPERSEDED 2026-06-07 (now IN production BOM for B12/4PB/Pack-Découverte)
`LOG_SCOTCH_ESHOP` (ref_mi id201). **Kouros ruling 2026-06-07 (THREE BUCKETS above): eshop PACKS (`-B12`, `-4PB`, Pack-Découverte composites) DO carry eshop scotch as a real production-BOM line** (bucket 2) — model via template default on fmt2/fmt4 + the existing composite choices. So for THOSE formats it IS in the production BOM. The Stage-2-only treatment REMAINS for the NON-pack eshop wrapping (PD8-renforts loose, 12-box wrap, certain eshop 24-boxes' outer fulfilment) alongside LOG_* (LOG_CERCLAGE_RAJAPACK/LOG_FILM_ETIRABLE/LOG_FEUILLARD via `sales_derived`/`derive-eshop-consumption.ts`). **The discriminator: a PACK's own eshop scotch = production BOM; fulfilment/shipping wrap = Stage-2.** Detail → [sku-bom-validation-walkthrough/README.md](sku-bom-validation-walkthrough/README.md) §ESHOP-SCOTCH ALTITUDE.

## RECOMPUTE SERVICE — `compile_sku_bom_packaging` (§8.8 — SHIPPED + RUN + PM-verified live 2026-05-25)
Backend service `app/sku-bom-compile.php` + thin CLI `scripts/sku-bom-compile-cli.php`. Committed `ba817ed`. Ports the resolver INTENT of maltytask `scripts/build-sku-bom.js` but MySQL-native, template-driven, FK-resolved (no Sheets, no name-string matching).
- **⚠ FUNCTION NAME = `compile_sku_bom_packaging`** (NOT `compile_sku_bom`). `dryRun` defaults TRUE (callers MUST pass false to write); `packagingOnly=true` is REQUIRED or it throws (liquid side out of scope until F2).
- **OPERATOR DECISION: first cut = PACKAGING-ONLY.** Rebuilds ONLY the packaging side of each affected SKU; MUST NOT touch resolved liquid lines.
- **SAFE DELETE PREDICATE (PM-validated against live DB): discriminator = `ref_sku_bom.source`, NOT bom_source, NOT category.** `source='Brewing'`=1338 rows (liquid), `source='Packaging'`=387 rows (incl. all NULLs), `source='mi_match'`=24 rows. First-run DELETE: `WHERE sku_id IN (:affected) AND source='Packaging';`. Going forward the service sets `bom_source='packaging'` so later runs can also key on bom_source.
- **🔴 COMPILER-DELETE-PREDICATE GAP (OPEN compiler-hardening item, exposed by the `-X` dedup 2026-05-26):** the earlier "`mi_match` rows have zero overlap with the affected set" assumption was WRONG for the `-X` SKUs — they carried `source='mi_match'` dup rows that the `source='Packaging'`-only DELETE MISSED, leaving doubled BOM lines. They were manually DELETEd (0 `mi_match` rows remain DB-wide). **The DELETE predicate must clear ALL legacy non-liquid source tags for an affected SKU (e.g. `source IN ('Packaging','mi_match')` or `source <> 'Brewing'`), not just `'Packaging'`** — otherwise any SKU still tagged with a legacy non-Packaging source silently re-accumulates duplicates on recompile.
- **HARD SAFETY GATE (coded):** snapshot liquid row-count + liquid cost-sum per affected SKU before; abort+rollback if ANY changes. Liquid byte-identical pre/post.
- **Resolver precedence per slot:** fixed → `default_mi_id_fk`; beer-specific → SKU override (`ref_sku_packaging_choices`, empty today) → recipe binding (`ref_recipe_packaging_bindings` by role) → template default. `{beer}` → recipe `sku_prefix`, then `ref_mi.mi_id LIKE`. Resolve template by sku.format_id → active we_supply `ref_packaging_bom_templates` (bom_template_id NULL on all skus today; 16 templates, 1:1 with formats).
- **BUILDABILITY GATE (use this to scope a recompile `--sku` list — verified 2026-05-26):** the service's SKU-load query is a 4-way INNER JOIN: `ref_skus ⋈ ref_packaging_formats ⋈ ref_recipes (⟹ recipe_id MUST be NON-NULL) ⋈ ref_packaging_bom_templates (supply='we_supply' AND is_active=1)`. A SKU failing ANY of these is reported "not found (no format_id or no we_supply template)" and silently no-ops. ⚠️ **Do NOT scope a backfill by `dbc_packaging_format_templates`** (that's the *volume-derivation catalog*, a different table) — it admits SKUs the compiler rejects (hit 2026-05-26: 11 active SKUs with `recipe_id IS NULL` slipped in). Always dry-run to `errors=0` before `--apply`. **UPDATE 2026-05-26 — partially resolved:** the 4 Diversion SKUs were given recipe links (PM-verified live: **DIBB12 id62→r26, DIBBU id84→r26, DIGBU id87→r27, DIPBU id88→r28**) + recompiled + volume-backfilled; BLA4 now → r44 (white-label, above). Still recipe_id NULL: COLLAB12/24 (covered by `ref_sku_collab_temporal`→r31 once the compiler reads it, step 4), PAC/PACKDEC (composite slots, step 2). The FR* SKUs were aliased+deactivated (no longer a buildability concern).
- **Refuse-don't-NULL:** every unresolved slot → self-sufficient `doc_review_queue` row (type `sku-bom-unresolved`, added by migration 143), NEVER a mi_id=NULL INSERT.
- **RUN RESULT (the 25 affected SKUs):** 64 legacy packaging rows → 69 resolved; **NULL mi_id 26 → 0**; liquid untouched (1338 Brewing rows unchanged). 6 `sku-bom-unresolved` RQ rows emitted (all status=open): EPH1-4BC sticker (config-B box sticker required, no `PKG_STICKER_EPH*` MI), ZEP4C outer_tray, ZEP4PC holder (no `PKG_4PACK_CAN_ZEP` MI). These are REAL gaps, not errors. **Migration 139 THEN APPLIED** (orchestrator-gated on NULL=0): `chk_rsb_mi_id_not_null` CHECK = `(mi_id IS NOT NULL) OR (bom_source='liquid')` live. Refuse-don't-NULL hard floor in place.
- **BINDING BACKFILL (§8.4 step 4, operator-approved):** `ref_recipe_packaging_bindings` = 30 rows (label 15, can 11, sticker 4, scotch 0 — deliberately unseeded, operator-set via Formats UI). All mi_id_fk NOT NULL, 0 orphans. The 11 can bindings closed the 26 NULL-can lines.
- **AFFECTED SKU SET (25 distinct):** sku_ids 6,14,29,32,35,38,48,56,94,95,96,97,98,99,100,101,102,103,104,105,106,107,108,109,136 = BLO4C/DIV33C/EPH1-4BC/SPY4C/ZEP4C/EMBCU/MOOCU/SPYCU/STICU/ZEPCU/EPH1-4CU/BLOCU/ZEP12C/EMB12C/MOO12C/STI12C/SPY12C/SPY12C50/ZEP4PC. 0 composites. BLO4C(6) and BLOCU(103) have liq_rows=0 (Blonde des Romands no observed liquid) — packaging-only recompute must not choke; parity trivially 0→0.

## ON-SAVE RECOMPUTE TRIGGER (committed `539bad3` 2026-05-25)
`activate_format`/`deactivate_format`/`set_binding` in `salle-de-controle.php` call `compile_sku_bom_packaging()` for the affected recipe AFTER commit — via `sdc_recompile_recipe_packaging()` + `sdc_flash_bom_result()`. BOM recompiles LIVE on every binding/activation change. Recompute exception caught LOCALLY, never loses the committed binding. The nightly cron (CLI exists, UNSCHEDULED) is the backstop. **NB (2026-06-06): `sdc_recompile_recipe_packaging()` + `sdc_flash_bom_result()` now LIVE in `app/sku-bom-compile.php` (relocated out of this page, `function_exists`-guarded), required by both salle-de-controle.php and bom-review.php — see the SHIPPED BROWSE block at top of this file. salle-de-controle.php still CALLS them, it just no longer DEFINES them.**

## DEFERRED — full observed-liquid recompute (§8.8 F2)
Needs the canonical observed-liquid per-HL source decision BEFORE the service recomputes the liquid side: `ref_recipe_profile_*` (malt+hops only, precomputed per-HL) vs `bd_brewing_ingredients_parsed_v2` aggregate (full categories) vs `ref_recipe_ingredients` gap-fill. **Observed wins; ref_recipe gap-fill only** (`feedback_observed_data_wins_over_ref_recipe`). Liquid line = per-HL × `ref_skus.hl_per_unit`. Today the service leaves legacy liquid (source='Brewing') untouched.
