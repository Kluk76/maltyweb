# RM retro (Jan/Feb/Mar 2026) — the sequenced prerequisite chain + gate state

> Load when: reasoning about the RM retro (the back-computation of Jan/Feb/Mar 2026 RM AND the Dec-2025 reconciliation), the packaging-consumption gap, the eshop/Stage-2 consumption matrix, production-SKU depletion, "why is month-closure April meaningless", or where the retro sits in the roadmap. This is the CANONICAL statement of why the retro is blocked and what unblocks it. Supersedes the bare "RM retro paused 2026-05-22" note. Operator-stated dependency chain recorded 2026-05-27; CORRECTED + EXTENDED 2026-05-27 (Stage-2 is prereq-1, not sales_derived; Dec-reconciliation goal added; cross-reference map added). Live DB state PM-verified 2026-05-27. Companion: computation-layer-architecture.md (landmine (b) = the SAME gap), derivation-tree-and-schema.md §COST MODEL + §Stage-2.

## ⚠️ CORRECTION (2026-05-27) — prereq-1 is Stage-2 (PLANNED), NOT `sales_derived` (built)
An earlier framing said "prereq-1 (eshop packaging consumption matrix) is ALREADY BUILT — 3,633 `sales_derived` rows." **The operator clarifies this is NOT sufficient.** `sales_derived` (`derive-eshop-consumption.ts`) is only the **beer-agnostic OUTER packaging** (cartons / renforts / scotch). The PRECISE eshop consumption the retro needs — **which physical stock each sale draws from** (cage-draw vs unbox loose 6×4/24, half-consumed cage residuals, by sales channel) — is the **Stage-2 sales/eshop SKU-consumption matrix** (derivation-tree-and-schema.md line 36), which is **PLANNED, NOT built.** So the operator's "we can't retro-compute without knowing eshop consumption as precisely as possible" = **Stage-2 is a genuine prerequisite, not a done item.** The 3,633 `sales_derived` rows are a **PARTIAL input** (outer packaging only), not the whole. The two are reconciled in the prereq-1 section below.

## THE DECISION (operator, 2026-05-27)
Jan/Feb/Mar 2026 RM must be **RETRO-COMPUTED, done TOGETHER (operator + build), and is GATED ON FULL COMMISSIONING BEING COMPLETE.** Not to be started before commissioning closes. This is a deliberate sequencing call, not a backlog slip.

## WHY THE RETRO IS NEEDED (the TWO goals — operator, 2026-05-27)
`inv_rm_stocktake` holds ONLY two periods (PM-verified live 2026-05-27): **2025-12** (83 MIs, operator-declared anchor) + **2026-04** (239 MIs, validated physical count). **No Jan/Feb/Mar.**

**GOAL 1 — fill Jan/Feb/Mar so closings have a real opening (opening ≠ 0):**
- April month-closure shows **opening RM = 0** (no March close to carry forward) → anchor-month COGS is meaningless.
- The deliveries-driven inventory model only starts **May 2026** (opening = April close + deltas) — see [[deliveries-as-inventory-source]]. So Jan→Apr is the gap that must be filled by back-computation to make those closures interpretable.
- Operator caveat already on record: the earlier rolled-back Dec–Mar figures (250k/265k/299k/288k) are **probably under-valued** precisely because packaging consumption isn't tracked — fixing that is the point of the retro.

**GOAL 2 — RETRO-RECONCILE DECEMBER 2025 against the physical stocktake (primary motivation, recorded 2026-05-27):** the operator **SUSPECTS the December 2025 physical inventory was badly taken** (or there is an error somewhere). So the retro must ALSO **retro-compute December 2025 INDEPENDENTLY** (from the Dec-2025 anchor forward, or back-derive from a later validated count), **compare it to the operator's December physical stocktake, and surface the discrepancy as an analytical audit.** This is not a side-effect — it is a PRIMARY reason the retro is wanted. (This is also why the retro must extend to Dec, not just Jan→Mar — see CRITICAL PATH.) Refuse-don't-correct: the discrepancy is surfaced for the operator to adjudicate, never silently reconciled by overwriting either the computed or the counted value.

## 🚩 DEC-RETRO KNOWN-GAP — PBD (210 Dec-2025 sales lines, no BOM, distinct-from-PD8) needs a cost basis (recorded 2026-05-27, from #44 batch)
> **PBD's COMPOSITION (and exactly how it differs from PD8 — by Estafette↔Alternative) lives in the canonical §ESHOP BUNDLE COMPOSITION REGISTRY → [sku-decomposition-tree.md](sku-decomposition-tree.md). This section is the retro COST-BASIS gap ONLY; do NOT restate the composition here.**
Surfaced by the #44 PBD-deactivation safety check (mig 185 deactivated PBD `ref_skus` id=294 — distinct DISCONTINUED product per operator, NOT a PD8 alias). **PBD has 210 `inv_sales_order_lines` spanning Dec 2025 — it really sold — but has ZERO BOM.** So in the December retro, those 210 PBD lines **resolve to ZERO COGS unless PBD's actual Dec composition is established.** This is a **Dec-retro prerequisite / known-gap**, on-mission (not a regression): "PBD — 210 Dec-2025 sales lines, no BOM, distinct-from-PD8 per operator → needs a cost basis (its real composition) before the Dec reconciliation can attribute their COGS." This is exactly the kind of zero-cost-leak the Dec reconciliation (GOAL 2) is meant to surface. Deactivation was the correct going-forward fix (preserves the 210 lines as read-only history); the Dec-cost-basis is the retro-side follow-up. Cross-link: packaging-bom-model.md §FORMAT→SKU RESIDUAL-GAP TRIAGE (#44) → PBD bullet.

## THE DEPENDENCY CHAIN (operator's intent, CORRECTED 2026-05-27 against live state)
```
RM retro (Dec reconcile + Jan/Feb/Mar fill)  ← do together, GATED ON commissioning complete
  └── needs packaging consumption fully flowing into inv_consumption
        ├── prereq 1: Stage-2 sales/eshop SKU-consumption matrix   🔴 PLANNED, NOT built
        │     (which physical stock each sale draws from: cage-draw vs loose, residuals, by channel)
        │     └── PARTIAL input already exists: 3,633 sales_derived rows = OUTER packaging only
        │     └── depends on: Shopify feed carrying a pickup-vs-shipped flag (see below)
        └── prereq 2: production-SKU depletion                     🔴 THE OTHER GAP (bd_packaging → inv_consumption = 0 rows)
              └── both gated on: commissioning complete (Capacités/SKU-creation chain finalized)
```
**BOTH prereqs are open.** Prereq-1 is NOT the already-built `sales_derived` — it is the larger Stage-2 matrix; `sales_derived` is only a partial (outer-packaging) input to it.

## PREREQ 1 — Stage-2 sales/eshop SKU-consumption matrix — 🔴 PLANNED, NOT built
The PRECISE eshop consumption the retro needs: **which physical stock each sale draws from** — cage-draw vs unbox loose (6×4 / 24), **half-consumed cage residuals**, **by sales channel**. This is **Stage-2** (derivation-tree-and-schema.md line 36), still in design (needs a theoretical mathematician + UI expert). **NOT built.**
- **OPERATOR-STATED SHAPE / UX of Stage-2** (recorded 2026-05-27): a **VISUAL, dynamically updated at each BC/Shopify pull**, tracking simultaneously: **RM materials consumption + FG accuracy + FG depletion + half-consumed SKU availability to the eshop.** The "half-consumed-availability" = the **cage residuals** (a partially-drawn `-X` cage is still available to fill further eshop orders); the "visual / dynamically-updated-on-pull" is an explicit **UX requirement**, not just a batch job. Month-close snapshots the matrix → exact RM consumption + COP/COGS; eshop basis-stock KPI ≈ ~1 month of Shopify sales of the parent SKU. (Mirror of, and supersedes, the composite-pooling approach.)
- **PARTIAL INPUT THAT EXISTS (do NOT mistake for the whole):** `inv_consumption` has **3,633 `sales_derived` rows, Dec 2025 → Apr 2026** (PM-verified live 2026-05-27) — beer-agnostic OUTER fulfilment packaging only (cartons eshop, renforts, white box), 6 MIs: PKG_CARTON12_ESHOP (705 rows/1671 u), PKG_RENFORTS_ESHOP (705/6684), PKG_RENFORTS_PD_DBL (1072/1895), PKG_RENFORTS_PD_SGL (850/850), PKG_BOX_24_BTL_BLANC (200/366), PKG_BOX_12_CAN_BLANC (101/265). Writer = `derive-eshop-consumption.ts` (maltytask, commit `b9804ce`), reading Shopify `inv_sales_orders`/`inv_sales_order_lines`. This covers the box-around-it, NOT the cage-draw / loose-unit / residual routing the retro depends on.

## 🔌 CHANNEL-FLAG DATA-FEED PREREQUISITE (Stage-2 hard dependency, recorded 2026-05-27)
Stage-2 (and thus the precise retro) **DEPENDS ON the Shopify sales feed carrying a pickup-vs-shipped flag** — that flag is the **unbox-vs-cage discriminator**. Without it the channel routing (which sales draw loose units vs draw down a `-X` cage) **cannot be computed.** Plus **account = 1080 (privé)** identification — already available. So: pickup-vs-shipped = a MISSING data-feed prereq to be secured before Stage-2 can route correctly; privé/1080 = already present. (Full routing rule lives in packaging-bom-model.md "MULTIPACK STOCK-PULL" — see cross-reference map.)

## PREREQ 2 — "production-SKU depletion" — ✅ ENGINE FIXED+APPLIED+COMMITTED (24× scaling defect, fixed 2026-06-06)
### ✅ AS-BUILT (maltytask commit `942431e`, applied+verified live 2026-06-06)
The 24× scaling defect in `scripts/derive-packaging-consumption.ts` is **FIXED + APPLIED + verified + committed**. Per the coder report + the independent VP-Brewing audit (`/home/kluk/projects/maltyweb/docs/packaging-consumption-audit.md`): the writer now re-bases the denominator on **`ref_skus.units_per_pack`** (option b — 100% populated, 0 NULL, 0 ≤0, BOM-self-consistent: per-container BOM qty_per_unit == units_per_pack so `sellable×qpu = (units/upp)×upp = units`, algebraically exact). Added a **FAIL-LOUD guard** (missing units_per_pack → LOUD console.error + `skippedNoUnitsPerPack`, replaces the silent default-to-1 that caused the bug). Full rebuild: DELETE 6504 / INSERT 8565 / 0 dupes. Verified live: PKG_BOT_CROWN_CAPS 138M→5.83M (maxrow 650,208→27,092); caps≈bottles≈labels (<0.5%), 4pack=bottles/4 exact. NO migration. RM retro NOT reopened (left operator-gated per scope guard). **The propagation gap was a red herring — it was already closed; the real defect was the magnitude. Engine now TRUSTWORTHY FOR COGS (audit verdict, arithmetic hand-traced across bot/can/keg/single/4pack/12pack/24box).** Root-cause-for-the-record: old `loadCartonSize()` keyed the denominator on `category_raw='Container'` which ZERO live BOM rows carry (live taxonomy Packaging/Closure/Label/Logistics) → cpc defaulted to 1, per-box BOM lines (qpu=24) × raw individual-bottle prod_total_units = 24×.

### 📋 AUDIT FINDINGS — all in REFERENCE DATA / coverage, NOT the engine (PM-verified live 2026-06-06)
The engine is sound; these are master-data gaps that understate COGS. PM verdicts + sequencing recorded here.
1. **⚠️ 43 unpriced packaging MIs → 681,099 computed units valuate to CHF 0 (BIGGEST live COGS leak).** PM-VERIFIED via `v_mi_cost.cost_basis`: of the 43, **39 = `no_basis`** (no WAC, no catalog — `inv_deliveries.ingredient_fk` has ZERO priced rows; deliveries-as-inventory only began May 2026, so most packaging was never delivered-with-price into the DB), **3 = `wac`** (priced delivery exists — a `compute-weighted-prices` re-run prices them: PKG_INTERCAL/PALETTE/TOP_FRAME_NOMOQ), **1 = `catalog`**. ⟹ **NOT a WAC re-run task — it is predominantly a CATALOG DATA-ENTRY task** (operator/build enters canonical `ref_mi.price` for can bodies / stickers / labels from supplier catalog or last invoice — NEVER guess a COGS-impacting price; seed from the operator's invoice/catalog statement). Caveat: PKG_TEA_BOT_CH has 11 priced deliveries yet resolves `no_basis` — a separate v_mi_cost coverage bug (TEA eco-tax-split line, GL 4201), flag but NOT a packaging-leak. Price layer = `ref_mi.price` / `v_mi_cost` (resolver wac>catalog>no_basis, `price IS NULL`≠`price=0`). → phase-d-cost-source-swap.md.
2. **⚠️ Contract/white-label gap — 244 sku_id_fk-NULL runs skipped (~975k bottles+cans + 9,078 kegs, ~CHF 195k material).** This is the KNOWN Phase-2 contract-packaging-per-client model (PREREQ-2 contract lane, this file §CONTRACT-EVENT SUB-CASE + packaging-bom-model.md). NOT a now-fix; gated on the two prereqs (parse `contract_beer`→`ref_clients`; add `materials_supplied_by` flag). The 0-white-label flag on all 244 suggests contract not WL — confirm with operator before assuming zero-cost-to-Nébuleuse.
3. **❌→🟡 DIV33C BOM: RESOLVED ROW FIXED, SOURCE BINDING STILL WRONG (verified 2026-06-06 — recompile-unsafe half-fix).** DIV33C (`ref_skus` id 14, format Can33, recipe 25) resolved `ref_sku_bom` row NOW = mi 617 (PKG_CAN_ALU_33) ✅ (in-flight agent fixed it). BUT the SOURCE `ref_recipe_packaging_bindings` **id=30 (recipe_id 25, role=can) STILL = mi_id_fk 616 (PKG_CAN_ALU_50)** — its note literally says "DIV33C → PKG_CAN_ALU_33 needs separate binding". **A recompile of recipe 25 re-introduces the error.** The `can` role binding is the SOLE container-MI source; EPH(62/76/71/75)+BLO(10) correctly bind 616 (they ARE 50cl), only DIV(25) is 33cl and mis-bound. **The DURABLE fix is at binding id=30 (repoint to 617) OR a per-SKU `ref_sku_packaging_choices` can-slot override on DIV33C, NOT the resolved `ref_sku_bom` row.** Both can bodies are unpriced+is_inventoried=0 → COGS-neutral TODAY, correctness-critical when a price lands. Snapshot-before-write + log_revision; then `sdc_recompile_recipe_packaging(25)`. This is THE canonical proof the new BOM Review surface must edit at binding altitude. → packaging-bom-model.md §ACTIVE BUILD.
4. **❌ BLO4 BOM incomplete (caps+bottles only, missing label/4pack-carrier/box; 171,468 bottles).** SKU is `is_active=0` (inactive, historical, ~CHF 6k). DEFER — back-period-only; batch with the Dec-retro cost-basis work (this file §DEC-RETRO KNOWN-GAP sibling).
5. **⚠️ ~530 pre-fill loss units silently dropped (unmatched LOSS_MAP cells) + latent uk_dedup-includes-qty collision (unreachable today, no upp=1 bot run).** DEFER both — small COGS; the dropped-loss should become a ReviewQueue row (data-quality signal) not a silent counter; dedup hardening = prefer row_hash as sole dedup key. Low priority hardening, batch later.

### PM SEQUENCING RULING (2026-06-06)
**Fix #3 (DIV33C, unambiguous) NOW + price the 4 WAC-able MIs via a compute-weighted-prices re-run NOW; the 39 no_basis price entries are a SEPARATE operator catalog-data-entry pass (front of it before trusting absolute packaging COGS).** #2 stays Phase-2. #4/#5 defer/batch. **Do the clear ❌ + kick off the price-entry pass BEFORE the Phase-2 KPI dashboard goes live** — any COGS/packaging-cost tracker on that dashboard would otherwise display CHF-0-leaking, understated numbers (the dashboard must not ship showing trackers it silently understates). KPI trackers that touch packaging COGS should gate on `data_ready` (no `no_basis` MI in their input) — wire that gate, don't display a known-wrong number. The KPI build itself can PROCEED in parallel on non-COGS trackers; only the packaging-COGS trackers are gated on the price backfill.

**⚠️ STALE-CORRECTED 2026-05-31 (PM-verified live):** the "packaging = 0 rows" framing below is OBSOLETE. `inv_consumption` source_event='packaging' now holds **6,504 rows (2021→2026-05-22; 697 in 2026; 103 in 2026-05)**. The gap WAS closed by a dedicated MySQL-native writer built after the 2026-05-27 snapshot. Current breakdown: packaging 6504, brewing 4897, sales_derived 3633, fermenting 277, racking 0.
- **WRITER = `scripts/derive-packaging-consumption.ts`** (maltytask; NOT `build-packaging-consumption.js`, which it replaces; NOT a maltyweb cron, NOT the form submit). Sole writer of source_event='packaging'. `parse-all-consumption.ts` + `derive-bd-consumption.ts` own ONLY brewing+fermenting and have a FATAL assertion against writing packaging — single-writer discipline intact. **schema_meta.writer_script still says generic `build-*-consumption.js / .ts` (slightly stale label, correct intent).**
- **MODEL:** `units` (parallel→special_qty_units else prod_total_units) ÷ container_per_carton (the SKU's Container-cat BOM qty 1/4/12/24) = sellable_units; per packaging-BOM row consumption = sellable_units × qty_per_unit; + per-item LOSS columns mapped to matching BOM material. `consumed_at = DATE(submitted_at)` (NOT event_date — May events have event_date NULL but DATE(submitted_at) lands them in May; that is why `WHERE event_date>=…` returns 0 while conso shows May).
- **IDEMPOTENCY:** `--apply` does a FULL REBUILD — `DELETE FROM inv_consumption WHERE source_event='packaging'` then re-INSERT all. `--dry-run` is DEFAULT. So **re-running it picks up ANY new/backlogged bd_packaging_v2 rows automatically** — no incremental gap. **THIS IS THE KEY OPERATOR ANSWER: backlogged packaging does NOT auto-deplete RM on form-submit; you must re-run `npx tsx scripts/derive-packaging-consumption.ts --apply`.**
- **COVERAGE (PM-verified live 2026-05-31):** 91 distinct packaging MIs in SKU-BOM; only **15 never consumed** — all explainable, NOT a wiring gap: eshop-OUTER (PKG_BOX_*_BLANC, PKG_CARTON12_ESHOP, LOG_SCOTCH_ESHOP → the `sales_derived` lane, not this one) + seasonal/whitelabel/blanche SKUs simply never produced in the bd_packaging window (PKG_PACK_DEC, PKG_XMAS_PAC, PKG_*_BLA, PKG_*_EPH2/EPH4, PKG_*_ZEP variants) + PKG_VERRE_25CL (glassware, not an event material). PKG_LABEL_*/PKG_BOT_*/PKG_CAN_*/PKG_4PACK_*/caps/collars/lids ARE all consumed. **The memory's named-suspect categories are RESOLVED.**
- **RUN-TYPE COVERAGE (live):** bot 760/916, keg 755/866, can 159/192, can33 2/22, **cuv 0/244**. The **244 cuv events == the 244 sku_id_fk-NULL contract events** — cuv (serving-tank, fmt V) fills draw NO packaging materials and contract beers have no Nébuleuse SKU, so 0 conso is CORRECT, not a gap. (bot/keg/can shortfalls are the contract subset within those run_types + zero-unit/no-date skips.)
- (HISTORICAL framing, retained: events EXIST `bd_packaging` writer `tab_packaging.py`; MAPPING in `ref_sku_bom` PKG_ lines; the *intended* writer was `build-packaging-consumption.js` with `corrections_policy=blocked_with_redirect`. SAME gap as computation-layer-architecture.md landmine (b); [[packaging-consumption-pipeline-gap]]; the 2026-05-22 retro pause — all now CLOSED for the Nébuleuse lane.)
- ARCHITECTURE RULE: fix at the PRODUCE-SIDE (the packaging-event→consumption write path), **never paper over in the compute/retro layer.** The `build-rm-retro.ts` anchor-fallback-when-rollback<0 masks the symptom; it is not the fix.
- DIVERGENCE WATCH: when this write path goes live, `inv_consumption` is `derived` — ensure ONE writer (the build-*-consumption pipeline), recorded in `schema_meta.writer_script`. Do NOT add a second maltyweb-native writer for the same fact while the Node/TS one exists (= two-writers-for-one-fact, the cardinal sin).
- **CONTRACT-EVENT SUB-CASE (PM-verified live 2026-05-27):** of the 2236 bd_packaging_v2 rows, 244 are contract-packaging events (WE pack another brewery's beer) with `sku_id_fk` **correctly NULL** (no Nébuleuse SKU exists for a client's beer; ref_skus has no contract-SKU concept — full ruling: packaging-bom-model.md §"CONTRACT PACKAGING EVENT → sku_id_fk is CORRECTLY NULL"). These do NOT deplete a Nébuleuse product SKU-BOM; their packaging consumption (when material-responsibility = `nebuleuse`) flows via a material-BOM keyed on run_type+format. TWO prereqs OPEN before that branch can compute: (1) `client_fk` is unresolved on all 244 (only `contract_beer` string set) → parse `contract_beer` → `ref_clients`; (2) `ref_clients` has NO `materials_supplied_by` flag yet (cols id/name/notes/created_at) → add it. So production-SKU depletion has TWO lanes: Nébuleuse-product (ref_sku_bom expansion) AND contract (material-BOM, gated on the two prereqs above).

## THE GATE — "commissioning complete" — concrete state (PM-verified live 2026-05-27)
The retro is gated on the Capacités → SKU-creation chain being finalized, because production-SKU depletion resolves each packaging event through the format/BOM that commissioning defines. The DATA LAYER is largely instantiated:
- `dbc_container_types`=9, `dbc_packaging_format_templates`=16, `dbc_equipment_types`=7, `dbc_vessel_types`=10 (DBCommissioning catalog SEEDED).
- `ref_process_machines`=7, `ref_filler_containers`=5 (the M:N filler↔container GATE exists), `ref_packaging_formats`=22, `ref_container_mi`=3, `ref_packaging_bom_templates`=16, `ref_packaging_items`=65, `ref_packaging_format_mis`=34.
- `ref_sku_bom` clean: NULL mi_id=0 DB-wide; packaging recompile service LIVE; composite fold LIVE; volume_hl on 105 SKUs.
- So the COMMISSIONING **schema + seed + BOM-template layer is built.** What "commissioning complete" still requires before the retro is the **operator-facing Capacités/SKU-creation UI arc** (task #44 simple format→SKU section; #45 multipack builder) plus the produce-side packaging form actually WRITING consumption — i.e. the chain "DBCommissioning → Capacités instantiate → Recipe format-activation → SKU → packaging form depletes BOM" must be closed end-to-end on the UI so every produced unit deterministically resolves to a format/BOM and emits its inv_consumption packaging rows. (The Stage-1 packaging-form picker `539bad3` is the UI feeding the produce-side write; the write path itself is the open leg.)

## CRITICAL PATH (RESTATED 2026-05-27 — operator's refreshed sequence)
1. **Commissioning UI arc** — Capacités **format→SKU** (#44 simple section / #45 multipack builder) **+ the composite compiler-fold fix** + recipe format-activation coverage, so format/SKU resolution is deterministic for every packaging event. — the GATE.
2. **Produce-side packaging-consumption write path:** `bd_packaging` × `ref_sku_bom` → `inv_consumption` packaging rows (single writer = the build-packaging-consumption pipeline). — closes prereq-2 (the production-SKU depletion gap).
3. **Stage-2 sales/eshop consumption matrix** — the precise eshop/cage-draw depletion (cage-draw vs loose, residuals, by channel), **incl. securing the Shopify pickup-vs-shipped flag.** — closes prereq-1 (NOT the already-built `sales_derived`, which is only the outer-packaging partial input).
4. **THEN re-run `build-rm-retro.ts` for Dec + Jan/Feb/Mar**, together with the operator:
   - **Dec 2025:** retro-compute independently → **reconcile vs the operator's Dec physical stocktake** → surface the discrepancy as an analytical audit (GOAL 2 — refuse-don't-correct).
   - **Jan/Feb/Mar:** meaningful drift vs the Dec anchor + Apr count → interpretable Jan–Apr closings (GOAL 1).
5. THEN downstream: FG retro; meaningful April anchor-month COGS; deliveries-driven model from May onward already in place.

## 🔗 CROSS-REFERENCE MAP (operator's explicit "save the path and referencing" ask, 2026-05-27)
Where each rule-set the precise retro depends on actually lives — so it's findable from this chain:
- **Stage-2 sales/eshop SKU-consumption matrix** (the dynamic cage/box/keg-draw depletion, BC/Shopify-pull-updated, residual-tracking, month-close snapshot → RM/COP/COGS; eshop basis-stock KPI; operator-stated VISUAL/dynamic-update UX) → **derivation-tree-and-schema.md line 36** (§COST MODEL Stage-2). Shape/UX also captured in the PREREQ 1 section above.
- **Multipack `-X` cage stock-pull BY SALES CHANNEL** (cage-draw vs loose-unit; account 1080 / Shopify pickup-vs-shipped discriminator; cost-lives-once-in-`-X`; needs the Shopify pickup-vs-shipped flag) → **packaging-bom-model.md §"🔴 CRUCIAL — MULTIPACK → `-X` CAGE STOCK-PULL, BY SALES CHANNEL".**
- **`-X` cage** (1027-bottle reusable crate, WIP, never sold; contents-only BOM) + **PKG_CAGE future activable-SKU build** → **packaging-bom-model.md §"THE `-X` FORMAT = a reusable CAGE"** + §"FUTURE BUILD — `PKG_CAGE`".
- **eshop OUTER packaging** (LOG_SCOTCH_ESHOP, cartons eshop, renforts via `sales_derived`) → **packaging-bom-model.md §8.3 (LOG_SCOTCH_ESHOP) + `derive-eshop-consumption.ts` (maltytask).**
- **Composite / sampler decomposition (axis 3)** = a Stage-2 INPUT (theoretical per-recipe HL/ingredient map; Stage-2 routes which physical stock it draws from) → **sku-decomposition-tree.md.**

## ROADMAP SLOT
Sits in the **same arc as the computation-layer landmine (b)** and is the highest-leverage upstream fix once commissioning closes — one gap blocks dynamic RM + COP + COGS + the retro simultaneously. **TWO prereqs are open** (production-SKU depletion AND the Stage-2 matrix — NOT one-done-one-open as previously framed). Independent of the maltytask Phase-D BSF-exit legs (those are cost-source swaps, not consumption); can proceed in parallel with them. The retro now has TWO goals: GOAL 1 fill Jan/Feb/Mar (opening ≠ 0); GOAL 2 retro-reconcile Dec 2025 vs the suspected-bad physical stocktake. **NOT a now task — explicitly gated on commissioning complete; do not propose building it before commissioning is complete.** Operator runs the retro itself, together.
