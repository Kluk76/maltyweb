# Index header-banner detail — relocated verbatim during 2026-06-21 compaction

> Full inline active-arc recaps that lived in the LEAN INDEX header `>` block before the 2026-06-21 compaction. They were compacted to one-line pointers in the index; the verbatim detail is preserved here so nothing was lost. Each block also has a dedicated thematic topic file (cited in the block) which is canonical for that arc — read the topic file first; this archive is the safety net for the exact former index wording. Parent index: [../maltyweb-pm-memory.md](../maltyweb-pm-memory.md).

> Last updated: **2026-06-21.** Freshest current-session state (older shipped items routed to topic files). **MIG HEAD now 424 applied** (`424_recap_per_cadence_config.sql` — recap independent per-cadence config; APPLIED via `sudo mysql … < file` + MANUAL schema_migrations INSERT, NOT migrate.php, to avoid sweeping a parallel pending mig). 4 builds shipped 2026-06-21 (all live+pushed): **`d4a8861`** packaging daily-recap UNITS section (display-only, NO mig → [daily-recap-refinements.md]), **`e83c4d1`** read-only lookup modules packaging+brewing (NO mig → [read-only-lookup-modules.md]), **`4dfe7b8`** recap per-cadence config (mig 424 → kpi-tracker-catalog/shipped-tranches.md). 🔴 **424 RE-OPENS+SUPERSEDES the one-sub-per-user recap model: recap config now = `user_kpi_recap_subs` (one row per user×cadence, UNIQUE(user_id_fk,cadence)+send_dow) + NEW `user_recap_tracker_selections` (per-(user,cadence) widget set); the dashboard `user_kpi_selections` NO LONGER drives recap content.** All 4 deployed surgical per-file rsync (parallel email-orders session held a dirty tree). Re-`--status` at build-start, parallel sessions lead the number. 423 = `423_doc_email_bc_matched_reconciled.sql` (`doc_email_messages.bc_matched_order_id BIGINT UNSIGNED NULL FK→ord_orders(id) ON DELETE SET NULL` — 🔴 BIGINT UNSIGNED matching ord_orders.id, NOT the INT predicted; record: ord_orders.id is BIGINT UNSIGNED — + `parse_status` ENUM gains terminal `'reconciled'`; ALTER → NO schema_meta row). 422 = `recipe_cr_email_settings` (P4 recipe-CR tiny mig).** 415=bd_packaging_v2 loss_liquid_alloc col / 416 backfill / 417 vendable-view loss-alloc / 419 re-materialize loss KPI HL / 418=`ref_internal_order_accounts` (mine, manual-applied earlier) / 420=`comm_send_outbound` (parallel comm-threads session) / 421=email-orders original_sender / 422=recipe-CR email-settings / 423=email-orders Slice 3a bc_matched+reconciled. **✅ EMAIL-ORDERS SLICE 2 (via-Commandes original-sender recovery) LANDED + DEPLOYED + VERIFIED + COMMITTED 2026-06-21 (`2eb6744`):** mig 421 + connector `_extract_original_sender` (X-Original-Sender→X-Original-From→Reply-To, bare lowercase, guards @lanebuleuse.ch + commandes@/production@/info@, persists on INSERT + dup-key UPDATE) + `generic_vocab._resolve_customer_hint` cascade gains original_sender (inline-block still wins) + `email-orders.php`/`.css` render "Expéditeur réel : <email>" line (`.eo-original-sender`). **VERIFIED (corrects design-time predictions): 10/11 commandes@ rows recovered real customers (docks/ecublens/nausikraft/petitecave/amstein/stardrinks×2/blavignac/boissons-gds/edu-vd; id 109 no header). `_internal_rep` dropped 21→20 ONLY (NOT ~10 predicted — masked rows were mostly no_match so never flagged; only Docks id 24 was → a 1-row mis-flag). Zero rows w/ both original_sender set + _internal_rep true. bc_order_match --report: 38 parsed → 3 matched / 35 unmatched.** Slice's REAL value = 10 anonymous senders identified, NOT flag reduction. **🔴 NEW BACKLOG (L1 feed): 8 commandes@ rows now sender-identified but still `no_match` (Nausikraft/Amstein/Stardrinks/Petite Cave/Blavignac/Boissons GDS/edu-vd) = L1 per-sender template candidates, gated on Kouros `.eml` forwards.** Slice 2's 421-bundle files (bc_order_match.py + ingest_email_orders.py + generic_vocab.py + 421 mig) committed in `2eb6744` (explicit pathspec). **✅ EMAIL-ORDERS SLICE 3a (review-card BC dedup GATE, "moment 2") LANDED + DEPLOYED + COMMITTED 2026-06-21 (`d592c7e` maltyweb main; 4 files: mig 423 + bc_order_match.py + email-orders.php + email-orders.css). 🔴 NOT "read-only" (CORRECTED 2026-06-21, ground-truth verified): 3a is a DEDUP GATE in front of the PRE-EXISTING `email_order_promote` producer (writes `source='email'` ord_orders + lines; wired at email-orders.php L143 in the PARENT of the 3a commit — NOT introduced by 3a). 3a's OWN writes touch only doc_email_messages (reconciled/bc_matched); but the unmatched + "Créer quand même" paths fall through to that producer which DOES write ord_orders. Producer has NEVER fired in prod (0 source='email' rows) — 3a gates a real producer that simply hasn't run.** `bc_order_match.py --match-one --email-id N --customer-id N` → single match-JSON (customer_id_override = operator-resolved FK wins; single matcher, NO PHP re-impl — D1 held). email-orders.php submit shells out ABSOLUTE-venv → `matched`=server-side interstitial "Correspondance BC détectée" (Archiver→`reconciled`+bc_matched_order_id, FOR UPDATE/refuse-if-terminal/log_revision | Créer quand même→`order_created` handled-flip ONLY, 3b swaps it) / `unmatched`=falls through to promote / error/timeout=**FAIL-CLOSED** "vérification BC indisponible — décision manuelle". 🔴 **HARDENING (Opus-added, in commit): subprocess timeout was a deadline loop over BLOCKING fread → hung php-fpm worker; fixed = stream_set_blocking(false)+usleep poll+proc_get_status+proc_terminate(9), exitcode→-1 fail-closed. DURABLE LESSON: a PHP subprocess timeout over a blocking pipe is not a timeout — non-blocking+poll+proc_terminate is the only real fail-closed.** Opus-VERIFIED: SHOW COLUMNS bigint-unsigned FK + 'reconciled'; match email15/cust464→bc206 conf0.95, email99999→exit1 fail-closed, archive idempotent (2nd refused terminal)+log_revision+test row restored, php-l clean, md5 local==VPS, git clean (only 4 intended files; a THIRD parallel session kpi-recap/mon-tableau/mig424 dirty — left untouched). **➡️ NEXT = Slice 3b = validation-accept→BC SalesOrder CREATE — DESIGN RE-RULED 2026-06-21 (the original (B′) premise was FALSE; ground-truth verified live).** 🔴 **CORRECTION (re-ruling supersedes the prior 3b line):** `email_order_promote()` (`app/email-order-promote.php` L250) writes **`source='email'`** (+lines+source_email_id_fk+review_status='accepted'+source_ref='email:<msgid>'), NOT 'maltytask'. Push spine publishes ONLY `source='maltytask' AND bc_no IS NULL` (L207/384) → NEVER picks up source='email'. The §PRODUCER→D1 (b) hand-off is **deferred/NOT built** (arc L89); (B′) wrongly assumed it was wired. 0 source='email'/0 source_email_id_fk/0 order_created in prod = clean slate. **RE-RULING = adopt (A)(i): retag `email_order_promote()` to write `source='maltytask'` + `source_ref='mt:<id>'` so the EXISTING push→echo machinery carries it** (push creates in BC echoing `mt:<id>` → reader echo-leg `ingest_bc_sales_orders.py` L840-926 REKEYS THE SAME ROW to `bc:<No>`, NO 2nd row). RETAG IS SAFE — full consumer audit: **`ord_orders.source` drives NOTHING in FG depletion** (`app/fg-stock.php` keys Leg-1 on `o.status='shipped'`+`l.line_status='to_fulfil'` L481-514, demand-sim on `status NOT IN('shipped','cancelled')` L854; source NEVER in predicate); twin/Shopify-XOR reads `inv_sales_orders.source='shopify'` (DIFFERENT table, L188) — orthogonal; the ONLY `ord_orders.source IN('web','email','import')` consumer = `load_existing_non_bc_active_orders`→`detect_collisions` (BC reader L509/754) and it does NOT branch on the source VALUE (uses it only as report field `existing_source`; keys on customer+SKU+date). After retag, promoted email orders correctly LEAVE the collision pool → handled by the echo-leg PK path (right). **KEEP `source_email_id_fk` + `review_status='accepted'` on the retagged row — email-provenance is ORTHOGONAL to the source tag, back-link survives the bc:<No> rekey.** NO new ENUM (flip parse_status='order_created' + reuse mig-423 bc_matched_order_id FK=new ord_orders.id). 3-layer no-dup-create: (1) 3a `--match-one` at create-click on RESOLVED FK+lines (refuse→archive) (2) GET-before-create on `mt:<id>` (spine already does) (3) uniq_ord_source_ref+FOR-UPDATE — layers 1&2 complementary. sku_code IS the BC item No, ref_customers.bc_customer_no IS the customer (push L339/348/305/317) — NO new map; NEW guard: refuse create if bc_customer_no NULL. **ARMING = (ii) disarm via system_settings `email_order_bc_push_mode off|armed` (mirror recipe_cr_email_mode). 🔴 DISARMED-MODE SAFETY (the part Kouros most needs right): in OFF mode the source='maltytask' row IS written locally — SAFE for FG because depletion fires on status='shipped', and a freshly-promoted order is status='entered' → depletes NOTHING until shipped (regardless of source/BC state). OFF = write local maltytask row (status='entered') + run match-gate + BUILD+LOG payload, but DO NOT invoke push --i-have-kouros-go and DO NOT flip parse_status='order_created' (render "créée localement, en attente d'envoi BC"). The local row is a normal pending order, not a phantom; enters the BC lifecycle only when armed.** MIG likely NONE for core (only the disarm-setting seed; mig head 424 → next-free ≥425, re-`--status`). EQUIP coder+sql+ui+webapp-testing. Opus verifies before arming (promotion now writes source='maltytask' NOT 'email'; source_email_id_fk preserved; fail-closed match-gate; dry-run payload+empty GET; OFF-mode local row status='entered' depletes nothing; ONE-row-post-roundtrip, FG depleted ONCE only after ship). + L1 per-sender parsers — CORPUS RULED: 58/69 no_match are internal reply-threads (skip), only ~9 external. Cobra Traders BODY parser FIRST (broker for COOP Pratteln/Aclens; customer_hint STRING, operator resolves FK, cross-ref mig 389 bill-to=Cobra-X), then 7 PDF parsers BATCHED (Nausikraft/Petite Cave/Blavignac/Amstein/Alloboissons/Boissons-GDS[2 pdfs=2 orders→parsed_json orders[] schema touch]/edu-vd; PDF bytes already fetched since c31b540; pdftotext -layout, no new dep). EQUIP parser-coder+sql. GATE: PDF batch on Kouros `.eml` forwards; Cobra buildable now.** → [email-order-ingestion-arc.md](maltyweb-pm-memory/email-order-ingestion-arc.md) §SLICE 3b DESIGN RULED + §L1 CORPUS REALITY + §SLICE 3a AS-BUILT. 🔑 **comm-threads parallel work uses a DISTINCT table family (`comm_threads`/`comm_messages`/`comm_message_docs`/`comm_address_pins`) — NO collision with `doc_email_messages`; do NOT generalize the order lane into raw_headers JSON for their benefit (their `comm_address_pins` is their home).** Earlier-window context: **418**=`ref_internal_order_accounts` rep→customer mapping, applied MANUALLY — NOT migrate.php — because parallel **411** WAS then still pending; manual ALTER + manual schema_migrations INSERT + schema_meta. Earlier in the window: parallel sessions deployed 401 planner-caps + 403/404/405 water-QA/planning + 406 `inv_sales_order_lines_stable_dedup`; the serving-tank planner work applied **407** `ref_customers.is_serving_tank_client`+`serving_tank_cadence_days` + **408** `pl_plan_items.customer_id_fk` FK→ref_customers; 409/410 taken by parallel sessions; **411**=`ref_customers_serving_tank_fiche` = serving-tank fiche cols count/size_hl/budget_hl (budget = PER MONTH); 412/413 parallel sessions; **414**=`serving_tank_turnaround_setting` = system_settings `serving_tank_turnaround_days` (section production_targets, default 14, planner Round-8 occupancy decay); 397 STILL PENDING-parallel — do NOT touch; 402 accent-refold also applied). **ALWAYS re-`migrate.php --status` + `ls db/migrations` at build-start — parallel sessions lead the number; next-free is whatever --status shows, NOT a memorized value.**
> - ✅ **STALE doc_invoice_lines-ON-RE-ALIAS FIXED + DEPLOYED 2026-06-19 + OBI inv 221 (`55000003173`) CORRECTED:** `ta_update_invoice_line()` (`app/services/triage_actions.php`) `mi_id_fk = COALESCE(...)` → unconditional `mi_id_fk = ?` (qty/unit_price stay COALESCE); per-file rsync, live md5 `17e4539`, php-l clean. Explicit operator alias now overwrites auto-suggested MI → "doc_invoice_lines drift on re-alias" systemic gap RESOLVED (human-triage path). OBI 221 lines re-pointed to MI 597 TAPROOM_HARDWARE; inv_deliveries corrected GROSS 38.75→HT 32.25 (16.60/3.75/8.28/3.62, 10% remise + VAT stripped), header HT 32.25/VAT 2.60/TTC 34.85. 🔴 **LOCAL `triage_actions.php` modified-UNCOMMITTED — ADD to the deploy-but-uncommitted list with the Circea parser bundle; commit by PATHSPEC.**
> - ✅ **SYSTEMIC FINDING — RESOLVED-WITH-SHIPPED-FIXES 2026-06-19 (DEPLOYED, uncommitted).** Both systemic gap #2 (doc_invoice_lines drift on re-alias, see line above) AND the gross-booking finding now CLOSED. Audit of the "8 hand-entry rows": **OBI (`55000003173`) was the ONLY materialized gross-booking** (corrected →HT 32.25 earlier); rest CLEAN — FA26-04850 reconciles; FAC260354 (Entranord drêches 500 + 5% fuel 25 = 525) deliveries correct HT, its doc_invoice_lines 1.00 placeholders cosmetic; RE-2026-00205 carton correct + its 100.00 "Retour consigne fût" line a legit deposit-return not booked. **NO further COGS corrections / NO backfill / NO CFO restatement.** **FIX 2a (line_index on hand-entry) DEPLOYED** — `ta_materialize_delivery()` now sets `line_index` in the inv_deliveries INSERT ($lineIndexRaw preserves NULL when absent; 22 cols/19 binds; both callers manual-lines.php + alias.php already pass it) → row-order-pairing fragility CURED going forward; 🔴 **existing NULL rows REMAIN — pairing those legacy rows still needs row-order.** **FIX 2b (HT capture, option A) DEPLOYED** — unit-price inputs labeled "HT" + hint "net de TVA et de remise" on BOTH alias form (triage.php) AND manual-lines (triage.php + triage-manual-lines.js); qty×unit_price math UNCHANGED; **option B (prefer line_total when authoritative) stays GATED** (no engine re-arch). Surgical per-file rsync, lint-clean, no foreign drift (parallel session holds salle-de-controle.php/mig 401 dirty — untouched). Trigger "gross vs HT"/"line_total"/"total_chf"/"remise"/"discount"/"manual-lines"/"triage-alias"/"ta_materialize_delivery"/"line_index NULL"/"COGS overstated"/"HT capture" → READ [ingestion-validation-gate-arc.md](maltyweb-pm-memory/ingestion-validation-gate-arc.md) §SYSTEMIC FINDING CLOSE-OUT. EQUIP coder+sql(+webapp-testing).
> - 🔴 **DEPLOY-BUT-UNCOMMITTED pile now 7 files (operator hasn't green-lit; commit by PATHSPEC):** circea.js / nomoq.js / index.js / parser-supplier-map.ts (maltytask parsers) + triage_actions.php / triage.php / triage-manual-lines.js (maltyweb). triage_actions.php carries BOTH the alias-overwrite fix AND FIX 2a line_index.
> - ✅ **THREE EXPÉDITIONS-CARD UPGRADES SHIPPED (`971471c` maltyweb main — COMMITTED by pathspec; surgical per-file rsync, parallel KPI-recap dirty tree untouched):** (1) **Doc externe pill** — mig **400** `ord_orders.external_document_no VARCHAR(50)` applied SURGICALLY (manual ALTER + manual `INSERT INTO schema_migrations` — `migrate.php` would have swept pending 397); connector adds `External_Document_No` to OData `$select`/order-dict/`write_bc_mirror_fields` (every-pull mirror, NOT push — read-only back); LIVE pull populated 13/61 BC orders ("ROMANEL / 69514" etc.); render reuses `exp-transporter-tag` (no new CSS), shown only when non-empty. (2) **Per-order comments** — new `set_comment` action on `public/api/expeditions-status.php` (one txn: UPDATE ord_orders.comment empty→NULL + status-UNCHANGED event "Commentaire modifié" + log_revision; NO mig, reused ord_orders.comment); inline ✎ editor gated `can_write_expeditions()`, CSRF auto-retry. 🔴 **SECURITY HOLE CLOSED:** added `can_write_expeditions($me)` 403 covering ALL branches (advance/cancel/revert too — was AUTH-ONLY before). (3) **Single "BL imprimé" tag** — removed left `exp-bc-shipped-badge` block + dead CSS; divergence badge `.exp-bc-divergence-badge` + `bc_completely_shipped` + auto-advance #4 UNTOUCHED. 5 files md5-match local↔VPS, php8.1 -l clean, fpm reloaded; no browser smoke (Tailscale-gated). Deferred: one-pull insert-lag close; comment-editor browser smoke. → state-machine-and-eshop-classification.md §THREE EXPÉDITIONS-CARD UPGRADES.
> - ✅ **CIRCEA SA bioCO₂ SUPPLIER — ONBOARDED END-TO-END 2026-06-19; CO₂-ROUTING REVISED SAME DAY** (supplier id 408 SUP_CIRCEA, parser `lib/invoice-parsers/circea.js`, RQ 2093 closed, invoice 220 RE-STAGED → gate=write_active 2 lines 833.58 CHF=HT, header backfilled; awaiting operator À-valider). 🔑 Kouros's CO₂ routing ruling: Q1=SAME pool (bio blends w/ conventional) / Q2=SPLIT by use → **vrac (499 kg) → shared MI `197 PROC_ALIGAL2_LIQ`** (NOT renamed — mi_id-rename avoidance, mild misnomer; alias `CIRCEA_CO2_VRAC→197`) / **bonbonne (150 kg) → EXISTING MI `75 SALE_CO2_BOUT` "CO2 Bouteilles"** (alias `CIRCEA_CO2_BONBONNE→75`, **RE-POINTED from the now-DELETED 666**). 🔴 **REVISION (Kouros): MI 666 PROC_CO2_BONBONNE_TAP DELETED** — NO taproom-specific CO₂ MI; bonbonne folds into the existing CO₂-bottle product stream ("it is a client with bonbonne, like our clients"). Q2 split-by-use still holds CONCEPTUALLY (vrac vs bonbonne ARE separate MIs) — the bonbonne side just reuses 75 instead of a new MI. ⚠️ **OPERATOR-ACCEPTED MODEL — DO NOT RE-FLAG next Circea/CO₂-bottle invoice: 75 is a Sales-category MI (cat 6, GL 4401 revenue, unit L, is_inventoried=0); folding a kg-denominated PURCHASE there books cost to a revenue GL + stores kg on an L-unit MI; Kouros confirmed "fold into 75 as-is".** (Deleting 666 needed a stale `doc_invoice_lines` row synced off 666 first — `fk_doc_invoice_lines_mi` is ON DELETE RESTRICT.) DURABLE CO₂ convention + supplier-onboarding shape → derivation-tree-and-schema.md §CO₂ MI ROUTING CONVENTION + §SUPPLIER ONBOARDING SHAPE; parser quirks → orchestrator reference_parser_completions_roster.md. 🔴 **DEPLOYED-BUT-UNCOMMITTED (commit SOON to remove footgun): `circea.js` + `nomoq.js` + `lib/invoice-parsers/index.js` + `parser-supplier-map.ts` (maltytask repo, per-file rsync to VPS).** 🔴 INCIDENT (reinforces [[feedback_agent_may_revert_uncommitted_edits]]): the Circea subagent used `nomoq.js` as a read-template, edited it, then `git checkout nomoq.js` to "clean up" → REVERTED our deployed-but-uncommitted NoMOQ hardening in LOCAL (local 8f560362 ≠ deployed f382be74); caught via md5, restored local from VPS deployed copy. LESSON: parser subagents must NOT `git checkout` ANY shared file; name the prior uncommitted change in the brief + md5 BOTH markers before/after.
> - ✅ **v1→v2 MIGRATION-BRIDGE SCRAP LANDED (record-only; LOCAL-unpushed maltytask `10c36c6` + maltyweb `3bb9453`):** scrapped `data/RawDB-normalized.xlsx`+`.bak-mig236` (local AND VPS — live-source hazard CLOSED) + `scripts/normalize-rawdb.py`; KEPT-as-historical the four `ingest_bd_*_v2.py`; mig `399_schema_meta_bd_v2_live_writer.sql` corrected 7 schema_meta `writer_script` rows off the dead ingest → web saisie forms (fermenting=`form-fermenting.php`). 🔴 BRIDGE scrap — DISTINCT from the v1-TABLES DROP (Stage 4, still gated). → scrapping-backlog.md #34/#35.
> - ✅ **2× DIBB FG-stock DATA CORRECTIONS w/ audit rows (06-19):** (a) `ord_order_lines` #358 order #115 DIBB×54 → `line_status='rupture'` (BC dropped for stock-out → phantom depletion removed); (b) `bd_packaging_v2` #6799 `prod_total_units` 3024→3048 (+1 box, operator-confirmed).
> - 🟢 **GREENLIT BACKLOG (Kouros-approved, NOT built):** systemic **ORD↔BC invoice reconciliation** (operator-in-the-loop) — reconcile each ORD vs emitted BC invoice, flag divergences → operator allocates reason → system updates.
> - 🔴🔴 **PROD-LEG SAME-DAY BUG — fix PENDING (bundle w/ transfer commit), full ratified spec → fulfilment-expeditions-arc/fg-stock-compute-bugs.md §PROD-LEG SAME-DAY BUG.** `fg_prod_since_anchor()` same-day tiebreak DISABLED → same-day-as-census packaging events excluded (ZEPF Renens −268 vs true 17). REJECT ≥0 clamp; align onto `fg_site_sku_anchor_map`; NO mig; commit w/ transfer fix in ONE pathspec `app/fg-stock.php`.
> - ✅ **SHIPPED this window (detail in topic files):** recap per-operator send-hour (mig 398, `6553880`) + send-hour-ignored BUGFIX 06-19 (`197d97e`, NO mig — head stays 399): NEW canonical helper `app/kpi-recap-schedule.php::kpi_recap_next_due($sendHour,$cadence,$from?)` called by BOTH save-path + cron; killed `mon-tableau.php`'s `next_due_at=NOW()` literal that fired the next tick at the wrong hour → kpi-tracker-catalog/shipped-tranches; DIBX cage SKU + CAGE-ZERO-HL fix + form-packaging cage NULL-FK GUARD (`db422d3` → packaging-bom-model/cage-x-format-and-guard); transfer-leg global-anchorDate prefilter `$anchorFloor` fix (NOT committed, COMMIT BY PATHSPEC `app/fg-stock.php` ONLY → fg-stock-compute-bugs) — its ESHOP-leg symmetric follow-up still latent; cage re-denomination `-X`=1 bottle (mig 392, `406f343`/`074a879` PUSHED → packaging-bom-model); FG-stock carry-over fix (`5b9858e`, per-location census anchor → stock-pf-board-anchor-flow); planning multi-process UX P0+P1 (`f0361d4`, NOT pushed → planning-page-arc). STANDING DEPLOY RULE: `php8.1 -l` the DEPLOYED file before every php-fpm reload (cage parse-500 incident → conventions-and-helpers).
>
> **🟡 RECIPE CHANGE-REQUEST FLOW (managers request, admins approve+replay through `sdc_apply_*`) — P3a/b/c/d ALL LANDED+COMMITTED (HEAD `e106e26`) 2026-06-19, COGS preview Opus-verified; NOW P4 (notifications + badge + 3 folded refinements) PLAN FINALIZED, NOT built.** P3a `388ca86` (`app/sdc-apply.php` 11 callables; hop/yeast/qc/field carry `$ownTxn`). P3b `707a639` (`?sec=demandes` panel). P3c `bbdc0ec` (hop + SCD2 stale-guard). P3d (format/BOM dispatch salle-de-controle.php L1844-1875, $ownTxn=false, gate-at-approve, ONE post-commit recompile; **approve JSON ALREADY returns `bom_err` L1888**). **P4 = SHIP DARK: email via `app/services/mailer.php::send_mail($to,$subj,$html,$text?,$from?,$fromName?,$replyTo?,$inline?)` (noreply default, NEW `mail_recipe_change_template` mirroring `mail_account_template`) gated by PARALLEL disarm `recipe_cr_email_mode` off/test/live + `recipe_cr_email_test_to` (seed via TINY mig — `system_setting()` is READ-ONLY, set by SQL; mirror fulfilment expeditions-status.php L289-360); recipients admins=`role='admin' AND is_active=1 AND email<>''` (1 today=Kouros), requester via `requested_by_fk→users.email`; best-effort email NEVER fails committed action. LIVE-POLL the L3054 nav badge via NEW `?action=cr_pending_count` JSON on the file's OWN jsonActions dispatch (NO new endpoint, NO topbar v1). Refinements: (a) preview BEFORE via dry-run `compile_sku_bom_packaging(...,true,true)` `pkg_cost` not stored ref_sku_bom (ZEPV staleness proven 0.00716→0.01122); (b) deactivate preview "→ retiré" (compile doesn't filter is_active); (c) JS toast reads existing `bom_err`. TOUR=NO (demandes is a recettes sub-section, NOT a ref_pages row — verified). EQUIP ui+coder+sql+webapp-testing.** Mig 412 ENUMs carry all 9 change_kinds + ref_packaging_formats/ref_sku_bom. 🔴🔴 **HARD RULE FROM P3c SECURITY INCIDENT: `$managerAllowedActions` (salle-de-controle.php L136) must stay EXACTLY `['submit_change_request','withdraw_change_request']`. The per-action inner `is_admin||is_manager` gates are DEAD-CODE-SAFE only while the outer wall keeps managers out — NEVER add any direct-write action (format/binding/hop/qc/yeast/field) to the allowlist; doing so un-deadens the inner gate and grants managers direct fiscal writes, bypassing approval. A sub-agent did exactly this in P3c (classifier caught it). `cr_pending_count` is a READ — confirm it dispatches BEFORE the POST-wall (so it needs no allowlist entry) + gate it `is_admin||is_manager` inner.** Trigger "recipe change request"/"demandes recette"/"sdc_apply"/"change_kind"/"format_activate"/"bom_binding"/"managerAllowedActions"/"P4"/"cr_pending_count"/"recipe_cr_email_mode"/"mail_recipe_change_template"/"approve_change_request"/"submit_change_request" → READ [recipe-ingredients-drift-surface.md](maltyweb-pm-memory/recipe-ingredients-drift-surface.md) §P3a–P3d LANDED + §P4 EXECUTION PLAN + §SECURITY INCIDENT.
>
> **🔴🔴 PLANNING — ACTIVE REWORK PENDING (operator-flagged 2026-06-19, "il va falloir qu'on retravaille l'occupation des cuves … on s'y attèle plus tard"): SERVING-TANK (CUVE DE SERVICE) OCCUPANCY MODEL.** This is the ONE net-new design open on the planner arc (rounds 1-8 are shipped+pushed). Round-8 shipped an ESTIMATE — `predict_load_free_serving_tank_count($pdo,$asOf)` in `app/planning-predict.php` = `max(0, in_house_active(8) − occupied)`, occupied = COUNT `bd_packaging_v2 run_type='cuv'` (not repack/tombstoned) in trailing N days, N=`system_settings serving_tank_turnaround_days` (default 14, mig 414), used as a secondary cap. DEFECTS: (1) N "très aléatoire", default 14 ZEROES free cuves this week (occupied 9 > 8 fleet → OVER-SUPPRESSES proposals); (2) decay DOUBLE-COUNTS a tank re-filled in-window; (3) ROOT CAUSE = there is NO cuve return/pickup event in the data — real fix = CAPTURE one ("retour cuve" form → event table → true filled→at-client→returned occupancy, likely its own arc); (4) occupancy lives PRODUCER-SIDE not in `TankSimulator::run()` (promote to a `serving` block if any 2nd consumer needs it); (5) 3 unjoined client ID spaces (bd_packaging_v2.client_fk→ref_packaging_clients vs ref_customers.is_serving_tank_client vs legacy ref_clients). Cold-resume-ready top-of-file flag in the topic file. EQUIP coder+sql. CARDINAL: planning = INTENT only, feeds nothing into COGS/COP/WAC/BOM/beer-tax/inventory. Trigger "rework cuves"/"occupation des cuves"/"serving tank occupancy"/"retour cuve"/"cuve return"/"serving_tank_turnaround_days"/"free cuve count" → READ [planning-page-arc.md](maltyweb-pm-memory/planning-page-arc.md) §NEXT/ACTIVE REWORK first.
> 
> **🟢 PLANNING PAGE — FULLER PRODUCTION PLANNER — ROUNDS 1-8 ALL SHIPPED + DEPLOYED + COMMITTED + PUSHED 2026-06-19 (maltyweb main; migs 401/407/408/411/414 APPLIED on VPS).** Producer factors tank space, packaging-output capacity, anticipated racks + dry-hops, serving-tank fills (DEMAND→client-recurrence trigger, never hardcodes the 5 clients), with per-client budget/size/count pace-check (Round-7, MONTHLY budget) + occupancy-aware free-cuve count (Round-8). **CARDINAL: Planning is INTENT, never feeds COGS/COP/WAC/BOM/beer-tax/inventory** (enforced schema_meta + code). **Round-8 (latest, `4dc27c0`+`c104e28`):** (A) manual add_packaging form now has a serving-tank client `<select>` (JS-toggled on pkg_type=serving_tank) → manual cuve rows no longer customer-NULL [closes a follow-up]; (B) `predict_load_free_serving_tank_count()` now subtracts N-day cuv-fill occupancy (mig 414 `serving_tank_turnaround_days`, default 14) — ⚠️ **ARCHITECTURE DEVIATION (PM-ratified): built PRODUCER-SIDE, not in the `TankSimulator::run()` `serving` block the ruling named** — substance met (count-based decay, pure-read), location differs; PROMOTE to the sim if a 2nd consumer ever needs serving occupancy. 🔴 **default N=14 currently ZEROES free cuves this week (occupied=9 > 8-tank fleet) — Kouros must calibrate N; real fix = a cuve return/pickup event (deferred).** Full pushed chain since r3: `521ccbf`→`6eb25a7`→`2e02d1d`→`652bbae`→`982f298`→`43abfce`→`bce4e6b`→`50e4eb3`→`4dc27c0`→`c104e28`. 🔴 **OPEN:** editable UI for serving_tank_turnaround_days (deferred — salle-de-controle parallel-edit); `serving_tank_cadence_days` UNUSED (Kouros's choice, not a defect); LATENT DIVERGENCE bd_packaging_v2.client_fk→ref_packaging_clients vs is_serving_tank_client on ref_customers vs legacy ref_clients = THREE client ID spaces no join (doesn't block count); `bd_brewing_gravity_v2` COOLING Stirling batch=172 2026-06-12 NULL recipe_id_fk (backfill at source). 🔴 **MASTER GATE STILL OPEN (8 rounds): authenticated MANAGER browser UAT** of whole planner + serving-tank cards + manual picker + salle-de-controle objectifs toggles + Round-6 fiche + Round-7 pace-check — Opus verifies structurally/rollback only, can't click as manager. Trigger "fuller planner"/"production planner"/"serving tank"/"cuve de service"/"is_serving_tank_client"/"client recurrence"/"cadence"/"turnaround"/"serving_tank_turnaround_days"/"free cuve count"/"dry-hop propose"/"tank space"/"packaging capacity"/"max_brews_per_day"/"max_packaging_runs_per_day"/"workday"/"objectifs"/"Transferts"/"core-only"/"mutex packaging"/"client picker" → READ [planning-page-arc.md](maltyweb-pm-memory/planning-page-arc.md) §ROUND-8 AS-BUILT + §FULLER PRODUCTION PLANNER. EQUIP coder+sql+webapp-testing(UAT). MIG HEAD now ≥414.
> 
> **🟢 PLANNING PAGE — MULTI-PROCESS-PER-DAY UX HARDENING (P0+P1) SHIPPED 2026-06-16, `f0361d4` on main (NOT pushed).** Overlap-shift need (one day stacks racking+brewing+dry-hopping in wort AND bottling+kegging in packaging). 🔑 **PM VERDICT VINDICATED: NO schema + NO engine change — `pl_plan_items` already N-rows-per-(plan_date,section), the 3 add-handlers already append by `seq=MAX+1`, render already lists cards w/ add-form-after, the eligibility engine already replays multi-item deterministically (ORDER BY plan_date,section,seq,id; per-day compute-elig→all-wort→all-packaging). The fix was RENDER-LAYER ONLY.** Files: `public/modules/planning.php` + `public/css/planning.css` + `public/js/planning.js` ONLY. Deployed (targeted rsync+chown www-data; php -l clean; page 302 auth-redirect no-fatal; assets 200). **P0:** add-forms collapsed behind a `＋ Ajouter` trigger (reopen same day/section after PRG via sessionStorage); full-width grid (reset `.home .main` centering); touch targets ≥44px (×delete/add/accept-reject); no-JS field-visibility bug fixed (`.pl-nonbrewing-fields` server-rendered hidden to match default brewing selection). **P1:** hors-process ember left-border; empty eligibility dropdown explains "garde / état cuve"; proposed cards 2px border + ⟳ glyph; per-form day-label; em-dash empty-state for read-only sections; delete-confirm out of inline onsubmit into delegated JS; `.pl-section` dropped `flex:1` (size to content). **P2 polish DEFERRED** pending Kouros manager-login UAT (beer-name/process hierarchy, `<label>`s, role=grid→group, flash auto-dismiss). 🔴 **STILL-OPEN: authenticated MANAGER browser UAT** — JS interactions (trigger toggle, sessionStorage reopen, eligibility dropdown, accept/reject) were reasoned not clicked (no manager session); must verify live before P2/push. Trigger "multi-process"/"overlap shift"/"plusieurs process"/"several processes per day"/"simultaneous"/"add another"/"planning UX" → READ [planning-page-arc.md](maltyweb-pm-memory/planning-page-arc.md) §MULTI-PROCESS-PER-DAY. EQUIP ui+coder(+webapp-testing UAT).
> 
> **✅ PLANNING PAGE — ALL THREE PHASES SHIPPED + LIVE 2026-06-15** (built, deployed, verified, committed). `public/modules/planning.php`: P1 manual week calendar + P2 dynamic process-eligibility (mig 364 `pl_plan_days`/`pl_plan_items`, INTENT-layer + schema_meta source class) + P3 predictive suggestions (mig 365 `suggest_reason` + system_settings stock/`plan_suggest_target_weeks`=3.0w). **`ref_pages.planning` now `is_active=1`** (domain='general'); tables clean slate (0 rows, fixtures cleaned). Commits `da202a9` (P1+P2 superset post history-reorg) / `d102a0d` (P3) / `beec798` (mig 367 + tour card). MIG HEAD now **367**. 🔴 **DURABLE (ALL future new pages): a new ref_pages row is INVISIBLE to onboarded users until added to their access PRESET — `user_can_access()` only role-floors preset-LESS users (admins); adding the page to `ref_access_preset_pages` via migration is a REQUIRED ship step. Bit us: operators/managers 403'd after is_active=1 until mig 367 granted `planning` to manager/production_operator/logistics_operator presets (idempotent uniq_preset_page + INSERT IGNORE).** Access policy (Kouros): manager→write (section-gated canWort/canLog; manager_can hierarchy makes prod⊇logistics); prod/logistics operators→read-only; sales_manager + marketing presets NOT granted → Louis Maechler, Thierry Stierli, Olivier Barral currently NO access (revisit at onboarding). 🔑 Engine `app/planning-eligibility.php` PURE-READ (CARDINAL: zero writes; forward-replay over TankSimulator snapshot + in-plan items, racking unlocks packaging, server-side re-check on POST) + Phase-3 producer SEPARATE surface `app/planning-predict.php` (engine stays projection; review caught 2 criticals — shared $freeCct, best→worst-coverage filter); both VERIFIED LIVE. **CARDINAL preserved: Planning is INTENT, never feeds COGS/COP/WAC/BOM/beer-tax/inventory** (enforced in schema_meta + code comments). Tour card SHIPPED (RULE 3 satisfied; steward also drafted a `financier` card held for PM ratification — that belongs to the COGS-fiche arc, NOT here). OPEN follow-ups: (a) 3 excluded users pending onboarding decision; (b) optional predictive cron (disabled-default, like fulfilment ships-cron); (c) authenticated browser UAT of page JS (needs manager login — recommend to Kouros). 🟡 Heads-up: `app/tank-simulator.php` has an uncommitted PARALLEL-session refactor (CCT-dest racking + BBT-overwrite fix) in the shared tree — preserves run() contract so engine unaffected, but flag if sequencing on the sim. Trigger "planning"/"planning page"/"eligibility engine"/"pl_plan_days"/"pl_plan_items"/"week calendar"/"forward-replay"/"planifier"/"Phase 3"/"predictive"/"suggestions"/"proposed"/"suggest_reason"/"semaines de stock" → READ [planning-page-arc.md](maltyweb-pm-memory/planning-page-arc.md).
>
> **✅ ENERGY/UTILITIES COP WEB-PATH — SHIPPED+LIVE+Opus-verified 2026-06-17 (mig 396; +follow-up A/B `f7dd965`/`12f89e1`/`00f4238` COMMITTED+PUSHED; mig396 itself NOT committed).** Energy COP moved OFF the stale Node `cogs-report-data.json` into IN-APP PHP compute. mig 396 `ref_utility_tariffs`+`ops_utility_closures`+`saisie-energie` ref_pages (3 schema_meta). saisie page `saisie-energie.{php,css,js}` (4 cumulative meter indexes → COALESCE-upsert `inv_energydata`, REFUSE if source='invoice'); estimator `app/utilities-estimate.php` ports `lib/utilities.js` 1:1; PARITY 0.0000 CHF × 5 months vs Node. ONE shared accessor `utilities_cop_ht_for_month(PDO,$month)` (GL 4700 gas+water / 4702 elec) called by ALL consumers + auditable detail panel. 🔴 **STANDING SCOPING RULE: the financier has TWO operational boards — COP (÷hlBrewed) AND COGS (÷hlSold); both share GL utilities lines; ANY future COP/COGS-feed work MUST check BOTH.** 🔴 DATA-GAP (pre-existing): 2026-01/02/03 inv_energydata rows LACK peak_kw → rolling-mean used → elec off ~1%; fix = backfill from seeded closures (awaiting Kouros). OPEN: commit mig396+saisie-energie+utilities-estimate by PATHSPEC; tour card held until commit; RULE 1 scrap-log utility-tariffs.json/utility-closures.json once Node COP retired. EQUIP sql+coder+ui+webapp-testing. Trigger "energy"/"utilities COP"/"saisie-energie"/"Index énergie"/"meter reading"/"gaz"/"électricité"/"eau"/"ref_utility_tariffs"/"ops_utility_closures"/"utilities-estimate"/"peak kW"/"compteur"/"énergie dans COGS" → READ [energy-cop-webpath.md](maltyweb-pm-memory/energy-cop-webpath.md) §AS-BUILT + §FOLLOW-UP ARC.
>
> **🔴🔴 ACTIVE ARC (LIVE 2026-06-16) — DYNAMIC-FINANCIER REBUILD + `cogs_fiche_sealed` (Kouros-APPROVED).** Arc RE-OPENED. Live financier path moves from the local-via-tunnel TS engine into IN-APP PHP compute (`app/cogs-fiche-compute.php`::`cogs_fiche_compute_month()` + `fin_closeable_months()`) so Fiche + both CSV endpoints render LIVE; published-month immutability becomes STRUCTURAL via append-only `cogs_fiche_sealed` (REJECTED pure-live-+-CSV-only — a signed month must not silently re-value when a back-period source moves). **Render = SEALED→frozen+"Clôturé (signé) — figé le {date}" / UNSEALED-closeable→live-compute+"Calculé (live)" / April→`cogs_fiche_seed` anchor.** "Sceller"(CFO/admin)=freeze live→sealed; "Restater"=dated re-seal→new row `supersedes_id`, old retained (visible delta). **STEP 1 IN PROGRESS** (Sonnet: byte-for-byte port of `cogs-monthly-compile.ts`; parity harness vs TS oracle `~/maltytask-run/run-fiche.sh`; GATE parity=0; Opus verifies canaries RM Hops **29 668.66** / WIP **23 946.93** / variation **−39 035.88** / basis **+1 808.82** BEFORE any UI wiring). 🔴 NUMERIC DRIFT to reconcile: those canaries differ from BOTH prior published-May records (351 884.58 / 353 867.69) — almost certainly the tank-sim WIP-restatement flowing into a fresh live compute; Opus establishes oracle-current vs sealed delta, surfaces it as the deliberate restatement (live proof of WHY the seal table). Remaining (gated parity=0): mig fingerprint-cache on repurposed `cogs_fiche_monthly` → wire lazy cache → repoint financier.php + both CSV endpoints → **mig `cogs_fiche_sealed` + seal/restate + sealed-vs-live render** → deprecate TS as LIVE path (KEEP as oracle). 🔴 RM = naive `final_qty×cost_chf` (do NOT switch to `loadRM`); port must NOT "fix" it — Hops 29 668.66 = regression tripwire. **MIG HEAD=373, next free=374** (re-`--status` at build start). EQUIP sql+coder+ui+webapp-testing. Trigger "dynamic financier"/"cogs_fiche_sealed"/"sceller"/"restater"/"figé"/"Clôturé (signé)"/"Calculé (live)"/"cogs-fiche-compute"/"fin_closeable_months"/"parity harness"/"seal/restate" → READ [financier-cogs-fiche.md](maltyweb-pm-memory/financier-cogs-fiche.md) §ACTIVE ARC first.
>
> **✅ (BASELINE — published May still holds) FINANCIER-FICHE COGS — keystone close.** May-2026 close shipped+ratified+verified+pushed (`423a660` maltytask); engine `scripts/cogs-monthly-compile.ts` (now being ported to PHP per the rebuild above; KEPT as parity oracle). May Σtotal 353 867.69 / opening ties April 394 367.74 / variation −40 500.05 / basis-adj +1 808.82 (memo, already included). 🔴 RM = naive `final_qty×cost_chf` (do NOT switch to `loadRM` — 1000× under-values g-priced items). mig 339 = `inv_fg_stocktake.count_type ENUM('operational','month_end')`; CLOSING census=month_end, intra-month weeklies=operational. C1 (month-defaults→last-closed via `app/finance-period.php::fin_last_closed_month`) + C2 (CFO comprehensive CSV `986cbb0`, PURE-PHP no xlsx — operator standing ruling) shipped. Financier tour card SHIPPED `4aa6fa7`. Next close = JUNE (enter June-30 FG census month_end → `--apply 2026-06`). → [financier-cogs-fiche.md](maltyweb-pm-memory/financier-cogs-fiche.md).
>
> **✅ RECAP-EMAIL GRANULARITY OVERHAUL — ARC CLOSED 2026-06-12** (`a0a9965`+`36d0d70`). `app/kpi-email-render.php` = pure-projection email-safe mirror of the full on-screen viz vocab (ZERO DB calls); new viz_type → add ONE case. 🔴 STANDING GUARD: stays a PURE projection — never query/recompute; missing number → add to the HANDLER's `meta`. → kpi-tracker-catalog/README.md §RECAP-EMAIL GRANULARITY OVERHAUL.
>
> **✅ RECETTE DESIGN-TARGETS — SHIPPED+PUSHED `2363c9d`** (mig 335): `ref_recipes.{og,fg,ph,abv}_target` + Cibles QC form. 🟢 KPI #7 (OG attainment) fast-follow queued = observed-OG-at-cooling (bd_brewing_gravity_v2 Cooling) ÷ og_target; render '—' on NULL target, NEVER fall back to ref_recipe_profile; gated on operator entering targets. → kpi-tracker-catalog/README.md §RECETTE DESIGN-TARGETS.
>
> ✅ **COBRA DISTRIBUTOR IDENTITY — `ref_customer_identity` BILLING-ALIAS FIX SHIPPED 2026-06-16 (mig 389; applied to prod; NOT git-committed/pushed — left for Kouros review).** DURABLE ARCHITECTURAL FACT: La Nébuleuse's big-retail customers are BILLED via distributor **Cobra Traders** — BC books orders under `Cobra Traders - X` sub-accounts (bill-to); WeeklyOrders/operators entered the SAME shipment under the geographic store account (ship-to) → two `ref_customers` rows for one order → BC connector collision-skip (keyed on customer_id_fk) MISSED them → DUPLICATE orders. Proven by ledger bill-to test (100% of `inv_sales_ledger` sales post under Cobra-X; geographic accounts ZERO direct billing = pure ship-to). FIX: mig 389 `ref_customer_identity` NON-destructive link (member_customer_id_fk UNIQUE→canonical_customer_id_fk, relation ENUM 'billing_alias', both INT UNSIGNED→ref_customers.id, schema_meta reference/allowed; merge REJECTED — would tombstone→perpetual drift); 40 links seeded (756 ALIGRO/755 COOP/993 MIGROS/760 MANOR/934 WITTICH); EXCLUSIONS Migros Partenaire ×10 (direct, Kouros carve-out)/Migros Golf 1667/Cooper SA 323/Le Bled 2305/Volg 944,949; connector `ingest_bc_sales_orders.py` + `canon(cid)` collapses both collision sides; dup BC echoes #142/#143 cancelled (FG-neutral). VERIFIED #52/#53 ACTIVE, 40/40 FK OK. 🔴 **REUSABLE RULE: confirm Cobra-distribution via ledger bill-to test + operator, NEVER guess membership by name prefix** (Migros Golf/Cooper SA/Le Bled = name-collision counterexamples). 🔴 NOT pushed — shared-clone dirty (parallel expeditions.php +54/expeditions.css +92/pm-memory + per-order departure-site work): commit ONLY `db/migrations/389_ref_customer_identity.sql` + `scripts/python/ingest_bc_sales_orders.py`, pathspec never `-A`. Trigger "Cobra"/"Cobra Traders"/"ref_customer_identity"/"billing_alias"/"bill-to"/"ship-to"/"Aligro"/"distributor identity"/"duplicate order" → READ §COBRA DISTRIBUTOR IDENTITY in fulfilment-expeditions-arc/README.md.
>
> **MIG HEAD now ≥406 applied, 397 STILL PENDING (parallel session).** Re-`migrate.php --status` + `ls db/migrations` at EVERY build-start (parallel sessions live; the number leads — do NOT memorize "next free"). 397_confirmation_email_settings is applied-pending for ANOTHER session — DO NOT touch/re-number/apply it. 398=recap send-hour, 399=schema_meta bd_v2 writer-fix, 400=`ord_orders.external_document_no` (applied SURGICALLY — manual ALTER + manual schema_migrations INSERT, to avoid migrate.php sweeping pending 397), 401=`planning_working_days_and_brew_cap`, 402=accent-refold shipping-title norm, 403/404/405=parallel session (water-QA + planning), 406=`inv_sales_order_lines_stable_dedup` (Shopify line dedup → external_line_id, applied this session). **🔴 CITE TRACKERS BY SLUG** (`ref_kpi_trackers` carries both `id` PK + `tracker_no`, off by 1, resolve to same row). 📌 **WATCH:** a future-dated `inv_sales_ledger.posting_date` can inflate latest-month snapshots — suspect first if 'latest closed month' logic looks wrong. Per-migration narration (335→398) lives in [build-history/changelog-2026-06.md](maltyweb-pm-memory/build-history/changelog-2026-06.md) and each arc's topic README (390 PD8→packaging-bom-model; 391 per-format objectives + 382 KPI#87 + 356 ops-health→kpi-tracker-catalog; 369/370/371/372 fulfilment+email→fulfilment-expeditions-arc + email-order-ingestion-arc; 383 topbar + 385/386/388 smoke-bots + 387 v_physical_returns + 384 oversell→user-auth-admin-arc + fulfilment-expeditions-arc; 396 energy→energy-cop-webpath). 🔴 DURABLE (from those migs): a PRESET CANNOT bypass the role floor — ONLY per-user `user_page_access` grants do (presets consulted AFTER the floor); `kpi_validate_params()` scope whitelist (~L104) silently REJECTS unlisted scopes pre-dispatch — any new production_targets scope must touch BOTH whitelist AND dispatch.
>
> **📦 ALL completed-arc narration is COMPILED OUT — this index only points.** The full per-session build-changelog lives in [build-history/](maltyweb-pm-memory/build-history/README.md); the verbatim pre-2026-06-05 index narration is archived in [build-history/former-index-archive.md](maltyweb-pm-memory/build-history/former-index-archive.md) (consolidated 2026-06-19 from the former last-updated + resume-point snapshots). The §SHIPPED-ARCS REGISTER below is the one-line pointer table for completed work; the RESUME POINT carries only what is still open/next.


> 🆕 **STATE-MACHINE AUTOMATION — #4 DELIVERED (auto bl_printed); #2 = RE-PIVOT FINAL build-brief (2026-06-17).** **✅ #4 (auto `bl_printed` on BC `Completely_Shipped` + `requested_date←Posting_Date` cascade) DELIVERED/PUSHED `e82689c`, NO mig** — synthetic events `comment='auto:bc-shipped'`/user NULL, monotonic, PRE-COGS (depletion arms on `shipped` ONLY). Préparée(#2½)/Livré manual; BL imprimé auto.
>
> 🟢 **CHANTIER #2 — CONFIRMATION MAIL ON «CONFIRMER» CLICK — RE-PIVOT #2 FINAL DESIGN RULED 2026-06-17 (build-brief READY+code-PROBED, DISARMED/not-launched).** Mail BUILT+SENT FROM maltytask, triggered by the operator's «Confirmer» click (BC mail trace NOT API-exposed → abandoned). GRAFT on the existing `advance` action of `public/api/expeditions-status.php` for `entered→confirmed` (NO new endpoint/button): advance status FIRST then best-effort off-txn send, JSON gains `email_sent`. From=`commandes@lanebuleuse.ch` «La Nébuleuse» via Postfix sender-dependent relay → Google Workspace SMTP (`smtp-relay.gmail.com`); client never sees maltytask. Disarm `system_setting('confirmation_email_mode','fulfilment','off')` ∈ off/test/live. Idempotence + trace via `ord_order_status_events.comment='email:confirmation'` sentinel (NO mig, NO sent_at col). Mailer gains `?from/?fromName/?replyTo` params (default = zero regression). EQUIP ui+coder+sql+webapp-testing; 3 ordered steps; commit by PATHSPEC. Trigger "confirmation email"/"mail client"/"Confirmer"/"email:confirmation"/"chantier 2"/"confirmation_email_mode"/"commandes@lanebuleuse"/"Google relay"/"smtp-relay"/"mail_order_confirmation_template" → READ [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) §state-machine-and-eshop-classification (full PROBED design + recap JOIN + BC-probe justification).
>
> ✅ **BC↔maltytask OPERATIONAL LAYER (A/B/C) + PHASE 2 WRITE-BACK (D) — COMPLETE 2026-06-15/17.** A/B/B-ext/C all DONE (migs 354/355/357; `2f0087e`/`59bd1f7`; per-line `line_status enum(to_fulfil,non_livre,rupture)` + writer `expeditions-line-status.php` + backorder badge; all 5 fg-stock depletion legs gated, Opus no-double-deplete verified). 🏁 **PHASE 2 D = D1+D2 COMPLETE** (D1 `7db6861`, D2 `de7b610` on origin; mig 366; writers `push_bc_sales_orders.py`/`push_bc_credit_memos.py`; CMs via BC API v2.0 salesCreditMemos; echo `Your_Reference='mt:<id>'`/`externalDocumentNumber='mt:cm:<id>'`; GET-before-create idempotent; live round-trips PASSED; `D365 BUS FULL ACCESS` suffices). 🔴 STANDING: D = proven plumbing, **no maltytask-native order/correction PRODUCER in v1** (OPEN-SCOPING flag). ✅ READER DATE-SEMANTICS FIX (`9e2adfb`, mig 368; revised 06-17, NOT committed): `requested_date` ← cascade `Posting_Date→Document_Date→Shipment_Date` + lead-time badge SHIPPED (`3b69225`, mig 369; `system_settings 'fulfilment'` thresholds). 🟢 EMAIL-ORDER INGESTION ("commandes readers") — **P0 LIVE + `--apply` NOW LIVE (2026-06-19): Gmail DWD ACTIVE (impersonates production@, filters to:commandes@); doc_email_messages WRITTEN, orders → email-orders.php review queue, no_match → "Non parsé"; NO cron yet (operator enables, manual `--force` re-parse).** P1+P3 DONE (`0a1f267`/`172fd95`/`6ef9df0`, migs 371/372; module `email-orders.php`, ref_pages id=480, `app/email-order-promote.php`, L2 generic_vocab difflib≥0.85). **🟢🟢 ATTACHMENT-ORDER SLICE LIVE+COMMITTED 2026-06-19 (`c31b540` maltyweb main):** `attachment_pdf.py` (BevanarPdfParser, `pdftotext -layout`, reconcile ≤0.10 CHF) + `attachment_xlsx.py` (MigrosFroidevilleXlsxParser, openpyxl, header/grid detect, reconcile) + connector attachment BYTE-FETCH (RFC822+Gmail) + registered before GenericVocabParser; ZERO new pip deps; preceded by `8f1b397` text-core generic_vocab hardening (committed). **LIVE `--apply --force` 30-day window: 34 parsed / 66 no_match of 100** (generic_vocab 31, bevanar_pdf 2 [No 32503+32529], migros_xlsx 1 [13 lines exact total]; 66 no_match = non-orders + reply-threads + parser-less senders = L1 candidates). **Vocab seeds LIVE: Huguette→EPH2 (recipe 76), découverte/pack découverte→PD8 (sku 137)** — done as master-data aliases NOT parser hardcodes (411 terms; "fûts Huguette" now parses via PDF parser). 🔴🔴 **DURABLE LESSON: Bevanar order PDFs arrive `application/octet-stream` + .pdf filename + NO `Content-Disposition: attachment` → disposition-ONLY attachment walk silently DROPPED them (Migros XLSX masked it — it HAD disposition). Fix = `_is_attachment_part()` = filename-truthy (non-text/multipart) OR disposition=attachment. VERIFY THE REAL EMAIL'S MIME TREE THROUGH THE CONNECTOR, not the parser vs a clean attachment file** (captured real Bevanar .eml fixtures to lock). 🟢 **REP→INTERNAL-ACCOUNT SLICE LIVE+COMMITTED 2026-06-19 (`5fecd85` maltyweb main, mig 418).** `ref_internal_order_accounts`(sender_email UNIQUE→customer_id_fk FK→ref_customers, is_active, notes) — applied MANUALLY (parallel mig 411 pending → migrate.php would sweep it) + schema_migrations row + schema_meta reference/admin-managed. **6 seeded rows (verified live, each → the rep's OWN ACTIVE acct, never an échantillon dup):** dorian@→3050(Fairhall/BC3947), john@→2377(Penman/BC3625), louis.cardis@→895(Cardis/BC2062), nicolas.berger@→1164(Berger/BC2358), nicolas.cerruela@→2233(Cerruela/BC3478), tania@→2379(Schott/BC3629). **Daniel Hersberger NOT a rep → DROPPED; "3478" was a BC number not an id (Cerruela id=2233).** Parser injects `_internal_rep`+`_rep_email` via `extra_hints` ONLY when customer_hint='' AND bare sender ∈ @lanebuleuse.ch; **BUG FIXED in verif: `.endswith` tested the RAW `"Name <email>"` header → flagged=0; extract bare email FIRST → 21 flagged, 0 coexist w/ resolved customer.** Resolver (`email-orders.php`+`.css`): parameterized SELECT JOIN ref_customers PRE-FILLS customer typeahead (operator-overridable, htmlspecialchars). 🔴🔴 **NEW DISCOVERY → TOP of next-slice backlog: "via Commandes" Google-Groups From-rewrite.** External customers mailing the `commandes@` LIST have `From` rewritten to `commandes@lanebuleuse.ch` (display name kept, "'Ana Rocha' via Commandes") → (1) real customer LOST (customer_hint='', real sender in `Reply-To`/`X-Original-From`/`X-Original-Sender` which parser ignores); (2) MIS-FLAGGED `_internal_rep` w/ `_rep_email=commandes@` (harmless to pre-fill — commandes@ unmapped — but inflates flag count + buries real customers). FIX (next slice): resolve original sender from those headers BEFORE the customer cascade → recovers Ana Rocha/Nausikraft-via-Commandes/Tip Top Drinks etc + removes mis-flag; + guard commandes@/production@ system addrs from triggering `_internal_rep`. 🔴🔴 **CARDINAL VISION CLARIFIED 2026-06-19 (Kouros, SUPERSEDES the earlier "reconciliation-only / promote paused-forever" note): THE MODULE EVOLVES INTO AUTOMATIC ORDER-FILLING INTO BC.** ONE pipeline, split by a dedup gate: read email order → CHECK if already in BC (`ord_orders source='bc'`) → EXISTS: archive (suppress/reconcile case) / NOT: create a VALIDATION ASK (the `email-orders.php` review card) → operator validates → maltytask CREATES the order in BC (D1 push spine). **Auto-fill-into-BC IS the confirmed goal** (replaces manual keying over time; dedup gate prevents clashes with orders the team still keys during transition). **Promote is UN-PAUSED — the gated BACK-HALF** (endpoint = a BC SalesOrder create via `push_bc_sales_orders.py`, NOT merely a `source='email'` ord_orders row). **rep-account pre-fill (mig 418) ACTIVE again** — pre-fills the validation ask for not-in-BC rep orders + doubles as a dedup-side identity aid. No-double-deplete invariant STILL hard (an order already in BC must never create a 2nd ord_orders row → double Leg-1 depletion+COGS; dedup gate is the safety). 🔑 **OPEN DESIGN Q (flag at build, lean (b)):** direct-create-ord_orders+push vs push-to-BC-and-echo-back via the `*/15` reader — echo-back is cleaner, avoids a parallel store. **BUILD SEQUENCE (confirmed): (1) DEDUP-VS-BC MATCH ENGINE [building now, DRY-RUN reconciliation report FIRST — no writes; primary `external_document_no` ("No 32529"), fuzzy fallback customer+date±Nd+SKU-overlap, low-conf→unmatched/never-auto-link; first deliverable = of ~34 parsed email orders which already exist in BC vs which don't] → (2) via-Commandes original-sender extraction (Reply-To/X-Original-From/X-Original-Sender → name the real customer on asks) → (3) validation-accept → BC PUSH [D1 spine, DISARMED, per-push Kouros-go, Opus independent verify of the no-duplicate-create invariant].** EQUIP coder+sql (+ui validation surface) + Opus independent verify (gates inventory/COGS both by SUPPRESSING dups at the gate AND CREATING the right BC order downstream). L1 sender roster observed: Nausikraft, Tip Top Drinks, FC Renens, Cobra Traders, Villvert-via-Louis, + gmail/hotmail direct. 🔴 SHARED CLONE MULTI-SESSION-DIRTY: parallel UNRELATED `scripts/python/ingest_email_comm.py` (comm_threads/comm_messages info@ inbox = entity-discussion-tracker arc; left untouched) + mig 411 serving-tank pending → commit by PATHSPEC, deploy surgically. EQUIP parser-coder+sql (+ui resolver pre-fill).** Trigger "block manual B2B"/"Pepitium"/"correction compta"/"avoir"/"credit memo"/"non livré"/"rupture"/"write-back"/"Phase 2 BC"/"line_status"/"D1"/"D2"/"salesCreditMemos"/"jour d'enlèvement"/"lead time"/"requested_date semantics" → READ [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) §bc-orders-integration + §D LAUNCH-READY SCOPE. Trigger "email order"/"commandes@"/"per-sender parser"/"Gmail"/"email_order_promote" → READ [email-order-ingestion-arc.md](maltyweb-pm-memory/email-order-ingestion-arc.md).

> 🟢 **ENTITY DISCUSSION TRACKER — SUPPLIER-SIDE TWO-WAY HUB COMPLETE + DEPLOYED 2026-06-19 (P0/P1a/P1b/P2/P2.5/P3-SEND all shipped). NOT git-committed (commit by PATHSPEC pending Kouros go).** Unified chronological mail+doc timeline on the supplier fiche (tabbed Fiche/Évaluation/Discussions, two-pane inbox) + **outbound reply-by-email FROM maltyweb, per-operator own-identity send, with drag-drop attach.** ✅ **gmail.send GRANTED + verified — real self-send proven** (kouros@→kouros@, returned Message-ID + gmail id, delivered; requesting `[readonly,send]` TOGETHER mints the per-user impersonation token). **P3-SEND AS-BUILT:** mig **420** `comm_send_outbound` (NOT 414 — parallel sessions took 414-419: `comm_messages.source` += `'sent'`, + `send_status ENUM('sent','failed')` + `send_error` + `sent_by_user_id`→users.id); Python sender `scripts/python/send_email_comm.py` (impersonates operator's OWN @lanebuleuse.ch addr, multipart/mixed, own deterministic Message-ID, In-Reply-To/References from parent, **DROPS cross-mailbox threadId** per Ruling A); `sf-comm-thread.php` gained `send_reply` (admin|manager SERVER-enforced, recipient=most-recent inbound, confirm-then-send, **row ONLY on Gmail 2xx**, no-row-on-failure→error to UI) + supplier timeline returns `sent_by`→"Envoyé par X"; composer UI in salle-fournisseurs.js/.css (blue `--bbt` send box DISTINCT from green `--hop` note box, gated inbound∧admin|manager∧@lanebuleuse.ch email via `window.SF_USER_EMAIL`). 🔴🔴 **DURABLE — ATTACH-PATH FIX: email attachments must write STRAIGHT to doc_files via a synchronous `action=attach_upload` (writes bytes to `data/email-attachments/YYYY-MM/`, sha256 dedup, INSERT doc_files source_folder='email-comm-attach', returns the BIGINT immediately), NOT through the `upload-document.php`/doc_uploads INVOICE-INGESTION pipeline** (the first build did the latter + polled `?supplier_docs=` for a UUID that can never resolve for a generic attachment — broken). The supplier-docs PICKER path (GET `?supplier_docs=<id>` UNION certs+thread-docs+invoices/DNs, returns real BIGINTs) was always fine. (This SUPERSEDES the earlier "reuse upload-document.php for desktop drag-drop" plan.) 🔴 **GIT: parallel session `git add -A`-SWEPT mig 413 into its commit `bce4e6b "style(expeditions)…"` (wrong message); remaining 8 files (420, ingest_email_comm.py, send_email_comm.py, gmail-comm.env.example, sf-comm-thread.php, salle-fournisseurs.js/.css/.php) STILL UNCOMMITTED + at risk — commit by explicit PATHSPEC, NEVER `-A`** (live proof of the standing rule — a foreign `-A` ate our migration). **NON-FISCAL observation layer (never COGS/COP/WAC/BOM/beer-tax).** 🔴 **KEY FINDING (durable): ref_suppliers has NO email col → `comm_address_pins` is the SOLE email→entity map; until pins exist EVERY thread → review bucket; triage-assign builds the pin set.** OPEN: Kouros's full authed click-through (real in-session drag-drop + a real send to a SUPPLIER — backend self-send proven); commit by pathspec; **bounces invisible under write-on-2xx (v1 gap)**; 2 blank-email users can't send (gated by design); cron disabled (operator enables); customer side = P3-cust deferred (schema forward-compat — customer_id_fk + XOR in place, needs a NEW customer fiche surface). Trigger "discussion tracker"/"mail tracker"/"timeline fournisseur"/"comm_threads"/"comm_messages"/"comm_address_pins"/"sf-comm-thread"/"ingest_email_comm"/"send_email_comm"/"attach_upload"/"send_reply"/"Répondre par e-mail"/"gmail.send"/"email thread on card"/"client card"/"supplier card discussion" → READ [entity-discussion-tracker-arc.md](maltyweb-pm-memory/entity-discussion-tracker-arc.md) §P3-SEND AS-BUILT + §AS-BUILT. EQUIP sql+coder+ui+webapp-testing (+parser-coder light envelope).
>
> ✅ **SUPPLIER EVALUATION / EF-01 — FULL BUILD SHIPPED + LIVE 2026-06-19 (all 5 waves, smoke 8/8 PASS).** Migs **409** (6 tables `supplier_evaluations`/`_criteria`/`_grids`/`_grid_criteria`/`supplier_cert_documents`[FK doc_file_id_fk→doc_files.id BIGINT]/`supplier_nc`[capa_ref soft text, MA-01 doc-only] + `ref_suppliers.criticality ENUM` additive + EF-01 grid seed A22/B13 thresholds 75/50 + 6 schema_meta) + **410** (KO flags). **As-built: `inv_deliveries.id` is BIGINT → `supplier_nc.delivery_id_fk` is BIGINT UNSIGNED.** KO criteria ratified = **A2(COA)+A5(traçabilité)** (KO forces non_agree). Criticality category set ratified = {Malt,Hops,Yeast,Brewing Adjunct,Brewing Mineral,Packaging(ALL),Cleaning Chemical(CIP)}; Process Chemical+Taproom non_critique → seeded **24 critique / 35 non_critique / 77 NULL** (no-delivery→manual override). Server-side scoring engine `app/supplier-eval-helpers.php::supplier_eval_compute()` (applicable-max %, KO-forces-non_agree); handlers sf-evaluation-save(supersession + valid_until by cadence)/sf-nc-save/sf-cert-link/sf-criticality-override + read sf-supplier-evaluation(autofeed). Évaluation section (6 subsections) in `#sf-fiche` (salle-fournisseurs.js +667/.css +495, PHP unchanged). Cron `scripts/send-supplier-review-reminders.php` (clone credential-expiry watchdog; **deployed DISABLED**; recipient=system_settings.ops.supplier_review_alert_recipient→admin; lead=ops.supplier_review_lead_days default 60) + export `public/api/supplier-evaluation-export.php` (pure-PHP CSV 11 cols, critique-first). NON-FISCAL observation layer (never COGS/COP/WAC/BOM/beer-tax). RULE-3 tour does NOT trigger (salle-fournisseurs not in ref_pages). **Commits maltyweb LOCAL-only (NOT pushed — push deferred to Kouros; parallel session commits interleaved in shared clone):** `d0a11dc`(mig 409/410)/`561ba4f`(handlers)/`7b10538`(UI)/`b522ec8`(cron+export). 🔴 **CARRY-FORWARD:** push deferred; operator enables cron + sets system_settings recipient/lead; 24 critique flagged "à évaluer" (first real campaign = operator/quality-team); **A6 (NC handling) score stays MANUAL** (autofeed shows NC count as evidence, no auto-score); 77 NULL-criticality get manual override as used; capa_ref soft text (wire FK if a CAPA register ever built). Companion doc EF-01 (Procedure-Evaluation-Fournisseurs-2026.pdf). Trigger "évaluation fournisseur"/"supplier evaluation"/"EF-01"/"B-Corp"/"agréé"/"approvisionnement PRP"/"fournisseur critique"/"supplier scoring"/"supplier_evaluations"/"criticité"/"review reminder cron"/"auditor export"/"supplier_eval_compute" → READ [supplier-evaluation-arc.md](maltyweb-pm-memory/supplier-evaluation-arc.md) §BUILD STATUS.
>
> 🆕 **PENDING BUILD (PM design RULED 2026-06-16) — CREDENTIAL-EXPIRY WATCHDOG (`ops_credential_expiry`, mig 381).** Daily flock'd PHP cron emails T-30/14/7/1 + post-expiry reminders before the BC Entra `client_secret` (config/bc.env, len 40, expiry recorded NOWHERE) silently kills the every-4h poll + future webhook. PM verdict = GENERALIZE NOW (N-credential table, schema_meta **system/app_only**, **NO FK**, CARDINAL never feeds COGS), BC=row 1. New table cols: label/cred_key(doc-only, secret never touched)/expires_on DATE/lead_days JSON `[30,14,7,1]`/last_reminded_stage INT(idempotency cursor, reset on expires_on change)/recipient NULL/is_active. Cron = EXACT clone of send-kpi-recap (user=**maltytask**, flock, deployed DISABLED + pre-flight header); checker `scripts/send-credential-expiry-reminders.php` (--dry-run default) reuses `send_mail()`+`mail_account_template()`. Recipient precedence: row.recipient → system_setting('ops','credential_alert_recipient') → `users role='admin' AND is_active=1` (no is_admin col; admins=role enum). **ONLY external input from Kouros = the actual secret expiry date** (Azure → App registrations → maltytask → Certificates & secrets). Mig head VERIFIED LIVE 381 free (re-check at build-start). EQUIP coder+sql (webapp-testing only if a UI is added — v1 seed-by-SQL, no page). Trigger "credential expiry"/"secret expir"/"client_secret"/"rotation reminder"/"ops_credential_expiry"/"watchdog" → READ [[project-re1-bc-sales-ledger-automation]] §CREDENTIAL-EXPIRY WATCHDOG.
>
> ✅ **RETURNS REVERSAL POLLUTION FIX — `v_physical_returns` VIEW SHIPPED+PUSHED 2026-06-16 (`5c911b7`, mig 387).** Canonical SQL view excludes ~72% same-day BC booking-reversals (shipment→credit→re-invoice, an ONGOING BC workflow not a load artifact) + rebate-disposition lines from the gross-return lens. Body = beer credit/return_receipt qty_signed>0 sku NOT NULL, minus same-day same-cust same-sku NOT-EXISTS + `ord_return_lines.disposition='rebate'` NOT-EXISTS; idx `ix_isl_revcheck (customer_id_fk,sku_id_fk,posting_date,doc_type)`; schema_meta row. **VERIFIED LIVE (Opus, not agent): view=1 505 lines; return rate 6.60%→1.818% (14 712→4 050 units); FWT Mgmt SA dropped off overship watchlist (was ~rank6 @82.3%); overship now 25 cust >12-floor, top=festivals (Trace-Ecart 92.9/Saubrazil 77.8/Paléo 69.3/Blues 48.7/Forget-Yesterday 45.7) mostly non-classé ⇒ next data-cleanup = bulk-tag ~40 event customers on_trade.** WIRED so far: `returns-synthese.php` rate-num + overship-num ONLY; mix/by_client/by_beer/pending_count UNCHANGED. 🟡 STILL DEFERRED (ONE batch, behind parallel set-site expeditions.php session): expeditions P1 queue render ~L2599 + processed ~L2649 + **POST reversal-guard ~L1532 (critical — phantom restock feeds Leg 4)** + synthèse pending_count → view; + collapse by_client/by_beer/mix rebate copy-paste into view (RULE-1 scrap). 🔑 RE-1 follow-on: `is_reversal`/`reversed_doc_no` ingest-flag at BC-API load → flip view body NOT-EXISTS→`is_reversal=0`, ZERO consumer change (`bc_source_no` does NOT link credit→reversed-doc — do NOT build on it). MIG HEAD now 388 (388=parallel smoketest session). Trigger "reversal"/"return rate"/"taux de retour"/"v_physical_returns"/"over-ship"/"disposition queue pollution" → READ §RETURNS REVERSAL POLLUTION FIX (AS-BUILT) in fulfilment-expeditions-arc/README.md. (Ingest-flag follow-on ALSO in RE-1 BC-API arc.)
>
> ✅ **RETURNED-FG ARC (BC-DERIVED) — P0+P1+P2+P3-SYNTHÈSE + PART A/B/C BACKENDS ALL SHIPPED+PUSHED+Opus-verified 2026-06-16.** Returns = bookkeeper books *avoir de vente* in BC → `inv_sales_ledger` doc_type credit/return_receipt; BC owns revenue, app owns DISPOSITION + physical FG only. Disposition enum **restock/scrap/quarantine/rebate** (default quarantine; rebate EXCLUDED from volume). P0 migs 373/376; P1 `?view=retours` queue; P2 `fg-stock.php` Leg 4 restock gated `disposition='restock' AND origin_posting_date>anchor` (Opus-verified additive, Σcards==Σphysique 0.0000); P3-synthèse `app/returns-synthese.php` (by_client/by_beer/mix/rate) + KPI #283 (mig 380). A=burn-rebate over-net fix `5cd9788`; B=returns RATE `e76e21a` (🔴 SPEC DEVIATION: ledger has NO `sale_class` col → customs_artifact filter DROPPED, sale_class lives on ref_customers); C=oversell/ATP `e80648d` + mig 384 `ops_oversell_snapshot` (cron DEPLOYED DISABLED, reads `flag_survendu` no parallel calc). 4 backend commits PUSHED (`2a96f83`). 🔴 Chien Bleu (385) trade_channel DELIBERATELY NULL (contract-brewing partner, not a sales channel). 🟡 OPEN: oversell-KPI tracker + returns-RATE-panel + oversell-on-Couverture WIRING deferred (was blocked on concurrent session — wire from a worktree); enable oversell cron; triage ~413 return backlog. Trigger "return"/"retour"/"avoir de vente"/"ord_returns"/"disposition"/"restock"/"quarantine"/"rebate"/"return rate"/"taux de retour"/"synthèse retours"/"oversell"/"ATP" → READ [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md) §returned-fg-tracking.

> ✅ **CFO/THIERRY READ-ONLY ACCESS PRESET (`finance_viewer`, id=10; mig 378) + FINANCE-WRITE CAPABILITY (`manager_scope='finance'`) — SHIPPED+LIVE 2026-06-16** (`15ac64d`+`23c791b`). CFO end-state: user 22 Thierry Stierli, role=manager, manager_scope='finance', preset 10 (20 ref_pages + per-user charges-bc override). Both HARD FINDINGS closed: (A) expeditions write-leak → new `can_write_expeditions($u)` in auth.php + 16 expeditions.php gates + line-status 403; (B) charges-bc `require_admin`→`require_page_access`. 🟢 scope='finance'→`manager_can('finance')`=TRUE so financier "Sceller"/"Restater" will pass when seal/restate ships; CFO stays read-only on prod/logistics (finance≠all/logistics). 🔴 INCIDENT (durable): ran in shared clone concurrent with seal/restate arc → sharded commits briefly broke HEAD → reinforces worktrees + pathspec + surgical-deploy. 🟡 OPEN: commits LOCAL (push pending); Thierry browser smoke pending. Trigger "CFO"/"Thierry"/"financier access"/"finance_viewer"/"read-only preset"/"charges-bc access"/"expeditions write leak"/"can_write_expeditions"/"manager_scope finance"/"finance-write" → READ [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md) §CFO FINANCE_VIEWER PRESET.

> 🆕 **PENDING BUILD (PM design RULED 2026-06-15) — RECURRING BC → `ref_customers` SYNC (Kouros "synch les clients").** SHIPS+RUNS FIRST, BEFORE resuming BC-orders Phase-1 `--apply` (clean customers → clean order resolution). Daily VPS cron full-scan upsert off BC `customers` standard OData (reuse `ingest_bc_sales_ledger.py` `_load_bc_env`/`_get_token`/nextLink-no-`$top`); strict BC-owned (address/email/phone/blocked→is_active/bc_no-reconcile-only) vs maltytask-curated (name/trade_channel/sale_class/notes/needs_review NEVER touched — name = operator wins, Salle-des-Machines); merge-tombstone-follow + refuse-dead-stub (inherit DURABLE RESOLVER RULE). **DRIFT not dedup** (bc_customer_no already UNIQUE, 0 dup groups live; stale numbers are the failure mode) — phantom-number-absence is the SOLE auto-reconcile license, all else → review. Stoked Mountain (id=3056 bc 3954-phantom w/ order id=49 / id=3060 bc 3951-real needs_review) = ONE-TIME Carnet merge cleanup first (null 3060's bc, set 3056=3951, tombstone 3060). Mig `bc_last_synced_at`(+`phone`) on ref_customers; NO new table; maybe `doc_review_queue` type `bc-customer-identity-drift`. EQUIP sql+coder+ui+webapp-testing. Trigger "synch les clients"/"customer sync"/"BC customer"/"Stoked Mountain"/"drifted bc_customer_no"/"client sync" → READ [sales-commercial-surface/README.md](maltyweb-pm-memory/sales-commercial-surface/README.md) §RECURRING BC → ref_customers SYNC first.

> 🆕 **PENDING BUILD (PM design RULED 2026-06-15) — BC SALES DISCOUNT → canonical `inv_sales_invoice_lines` + revive #100 (id=99 `discount_rebate_rate`).** Discount has NO canonical home (ledger=item-ledger grain, no discount col, live-confirmed); BC standard `salesInvoices?$expand=salesInvoiceLines` exposes LINE-level discount w/ same `config/bc.env` creds. Legit NEW canonical fact (not parallel store) — HELPS bc-xlsx retirement. Per-line table (NOT rollup); connector pulls salesInvoices+salesCreditMemos on postingDate basis, anti-join idempotent; "discount granted"=discountAmount+invoiceDiscountAllocation; rate tile=`line` trend, all-sales num+denom SAME universe, gross from ledger. Mig 2-step (create→Opus-verify→flip), CRON folded into ledger connector invocation. EQUIP coder+sql+ui+webapp-testing. Trigger "discount"/"remise"/"escompte"/"rabais"/"#100"/"discount_rebate_rate"/"salesInvoices" → READ [sales-commercial-surface/README.md](maltyweb-pm-memory/sales-commercial-surface/README.md) §BC SALES DISCOUNT first.

> ✅ **SHIPPED 2026-06-15 — "Objectifs de production" section on Salle de contrôle (`?sec=objectifs`)** (mig 346; Kouros: "le visuel est top"). `system_settings` section='production_targets' (wort 16'700 hl / 528 brassins; divisors weeks=48/months=12; pkg Fûts 7'920 / Bouteilles 4'620 / Canettes 1'008); W/M/Y trackers Produit/Objectif/Reste + %bar; admin-gated form. 🔑 CANONICAL ACTUALS (v2, NON-FISCAL read-only overlay): wort = `bd_brewing_gravity_v2` Cooling SUM(final_volume); brews = Cooling-row COUNT; pkg = `bd_packaging_v2` SUM(vendable_hl) GROUP BY run_type filtered `is_tombstoned=0 AND reuses_packaging_id_fk IS NULL`. Trigger "objectifs de production"/"16700 hl"/"528 brassins" → READ [kpi-tracker-catalog/README.md](maltyweb-pm-memory/kpi-tracker-catalog/README.md) §OBJECTIFS DE PRODUCTION.

> ✅ **SHIPPED 2026-06-15 (migs 351+352) — Objectifs-de-production WIDGETS on Mon tableau (#281 Moût, #282 Packaging), operator-activable** via the existing KPI activation system (NO parallel widget store; shared `app/production-targets.php::production_targets_compute()`). 🔴 TWO grouped_bar lessons (now in kpi-tracker-catalog/README.md conventions): (1) grouped_bar consumes `result.breakdown[]` NOT series/groups; (2) grouped_bar shares ONE max-scale across rows → feed a magnitude-INDEPENDENT measure (% d'atteinte), not absolute hl. Files uncommitted (Kouros commits on request); both widgets data_ready=1, screenshot-verified. Trigger "mon tableau widget"/"objectifs widget"/"grouped_bar"/"breakdown vs series" → READ [kpi-tracker-catalog/README.md](maltyweb-pm-memory/kpi-tracker-catalog/README.md) §OBJECTIFS-WIDGET ON MON TABLEAU.

> ✅ **TOPBAR CONSOLIDATION — SHIPPED + LIVE + COMMITTED 2026-06-16 (Option B), `2f61e52` (maltyweb main).** 6 drill-down categories + 2 standalone buttons. 6 files: `app/page-categories.php` (NEW canonical accessor `page_categories()`), `app/partials/topbar.php` (rewrite — N-category drill-down render), `public/js/topbar.js` (N-panel toggle), `public/css/app.css` (`.tb__cat`/`.tb__standalone` + global header `min-width:0` overflow hardening), `public/modules/reglages-generaux.php` (category_key/category_sort editor fields), `db/migrations/383_topbar_category_key.sql`. **Mig 383 APPLIED** (additive: `ref_pages.category_key VARCHAR(32) NULL` + `category_sort SMALLINT DEFAULT 0`; **`domain` UNTOUCHED** per the Option B ruling). **LIVE-VERIFIED MAPPING:** standalones (`category_key=NULL`) = `zeppelin` (✦ button — family-router hub, NOT a Production peer), `saisies` (✎ button), `mon-tableau` (brand/home). production={wort,fermentation,packaging,planning}; logistique={approvisionnement,email-orders,expeditions,tap-shop,warehouse,rm-comparison,triage}; qualite={qa}; finance={financier,charges-bc,bom-review}; pilotage={sb-board,sb-guerre,journal-saisies}; systeme={settings,ingest,db-browser}. **CANONICAL-ACCESSOR DISCIPLINE HONORED:** `page_categories()` defined ONCE in `app/page-categories.php`, CALLED by BOTH topbar.php (render) AND reglages-generaux.php (validation via array_keys + form selects) — no duplicated literal. **VERIFIED LIVE:** admin/operator/viewer webapp-testing smokes passed; category hides when user has 0 permitted children; deep links resolve w/ active-state; scrollWidth==clientWidth at 1280/1440/1920 (overflow bug fixed at root). **Access-gating UNCHANGED — domain was NOT load-bearing in auth** (Opus recon: `user_can_access()` gates purely on page_key override→min_role floor→preset; `ref_pages.domain` load-bearing in EXACTLY TWO places — tour filter `!='admin'` + reglages editor/badge-CSS; Option B re-homed nothing so exposure moot). **RULE 3: NO tour-steward dispatch** — Option B left domain untouched, no page changed tour scope. ── 🔧 Two related fixes from a PARALLEL session, now LIVE + committed under `241647d`: (1) `?view=repack` `order_name` column fix; (2) nested-fn-not-hoisted relocation of `exp_render_repack_order_block`/`exp_render_repack_mode_badge` to FILE SCOPE (the recurring "nested fn isn't hoisted → 500" class). Repack-depletion FG leg R1 round-trip RE-VERIFIED live (Σphysique==Σcards held; COGS-neutral). Trigger "topbar"/"barre"/"category_key"/"page_categories"/"dropdown nav"/"consolidation"/"Le Zeppelin button"/"6 categories"/"standalone button" → details in this banner.
>


---

## RESUME-POINT items relocated verbatim (2026-06-21 compaction)

_These STILL-OPEN/NEXT RESUME-POINT items carried multi-KB inline recaps; compacted to one-liners in the index. Verbatim detail preserved here; the cited thematic topic file is canonical for each._

BFD. **✅ BREWING-FORM INGREDIENT-LINE RUNAWAY — FIXED + DATA-CORRECTED 2026-06-19 (`5ad8929` maltyweb).** `form-brewing.php` §5-§7 now in an `$ownTx`-guarded txn; §7a pre-image → §7b hard-DELETE all parsed lines under all non-tombstoned headers for (beer,batch) → §7c plain-INSERT submitted set (ON DUP KEY removed) → stored lines == EXACTLY submitted (positional-line_idx append runaway structurally dead); FIRST audit coverage of the parsed-lines write (one log_revision, action 'update'). Invariant 6→6→6→5 PASS; LIVE re-save of Speakeasy 67 (`?edit=1513` brewday hdr; ingredient hdr=1137) stays 6, no new header; md5 `34e66df3` local↔VPS. NUANCE: header id 1137 was STABLE across corriger cycles (NK=beer,batch) — accumulation was pure line-append under one header, NOT new-header-per-cycle (commit msg over-speculated). Brief B: header 1137 collapsed 65→6 in one audited txn (2 audit_row_revisions, qc_flag elevated; mi_id_fk 3,20,6,51,82,80; raw_blob_text JSON_LENGTH=6; /tmp pre-image snapshots kept, no repo scripts). COP clean-window: 0 derived inv_consumption rows existed (June derive not run) → next month-close picks up the clean 6, no recompute. ✅ **DERIVER ORPHAN-PRUNE — LANDED (maltytask `3b21ba8`, pushed 2026-06-19), `--apply` GATED with Kouros:** `derive-bd-consumption.ts` now PRUNES `inv_consumption` rows whose `source_row_id` no longer maps to a live parsed line (step 3 in the `applyWrites` txn, same liveness predicate as the derive funcs, whole-table-per-source_event, convergence assertion + `orphan_pruned` audit meta + default-dry-run prune-count). Closes this LATENT BACKLOG. Dry-run = **9 brewing / 0 fermenting** orphans (genuine stale: Double Oat 2025-02-06 superseded by a live 02-27 brew = double-count; inv_consumption ids 10751-10759). 🔴 `--apply` NOT yet run — full COP re-derivation touching Feb-2025 historical; phantoms persist until run. **Feb 2025 is BELOW the fiche signed horizon → prune is SILENT, NOT a CFO/Thierry restatement** (seal model begins at the April-2026 `cogs_fiche_seed` anchor; Feb 2025 has no seed/monthly/sealable row — only fed the legacy Node/BSF pipeline). Anti-pattern now in `coder` skill + maltytask `feedback_derived_table_must_prune_orphans.md`. → [consumption-cogs-derivation-wiring.md](maltyweb-pm-memory/consumption-cogs-derivation-wiring.md) §DERIVER ORPHAN-PRUNE — AS-BUILT.

0a. **✅ CAGE ANCHOR LANDED + UNIT-MODEL DECIDED + SCOPE-POLLUTION FIXED — SHIPPED 2026-06-15** (migs 362+363, commits `e205522`+`80f36b9`; Opus-verified; committed by pathspec, NOT pushed — shared clone has other sessions' WIP). 8 cages renamed `-X`→`X` (ALTX..ZEPX, NO alias); CAST-truncation removed (cage qty DECIMAL end-to-end); cage-detection repointed off the hyphen-suffix onto canonical `stocktake_scope='cage'` (all 3 sites + JS); salle-de-controle generator now mints `<prefix>X` (root closed). **🔑 DURABLE OPERATOR MODEL: cages tracked in CAGE-UNITS directly — 1027 is used NOWHERE (nominal `units_per_pack` label only, NEVER a divisor/discriminator; cage bottle-count varies, STIX=1035 today). RETRACTS the earlier bottles÷1027 assumption.** Scope counts now base=54/single=27/none=93/cage=8 (cage == EXACTLY the 8). REMAINING: NOT pushed (Kouros confirms); DOCB awaits Nausikraft count; DOAX uncounted; June close needs a `month_end` cage census to feed COGS. **This is the cage-anchor gate ④-Phase 2 was waiting on — now landed+clean; ④-Phase 2 still DEFERRED on the OTHER gate (Shopify pickup-signal ON HOLD).** → [sku-bom-validation-walkthrough/README.md](maltyweb-pm-memory/sku-bom-validation-walkthrough/README.md) §CAGE ANCHOR LANDED.

0a-cage-scope. **✅ FIXED 2026-06-15 (mig 362, `e205522`) — `stocktake_scope='cage'` pollution cleared.** Root cause = a 2026-06-09 claude-cli edit ("Taproom: hors inventaire POS", audit ids 12077-80/12175-76) wrongly set scope='cage' on 34 non-cage SKUs. Per Kouros: DOCB/DOCF (Neb/CollabIn)→**base** (DOCB countable in consignment/Nausikraft); 32 class=Contract→**none** (Néb does NOT stock-count client product). Guard `sku_code NOT LIKE '%-X'`; 34 rows audited. Result: cage == exactly the 8 -X cages. Folded into §0a above.

0a11. **✅ SHOPIFY⇄MALTYWEB FULFILMENT INTEGRATION — FULL BUILD COMPLETE** (migs 321/322/323; Phase 0+1+2A LIVE; 2B DISARMED on Shopify write-scopes; 3c is_non_stock NO-OP). ✅ Σcards==Σphysique invariant RESTORED (`d9f7976` symmetric scope-gate + anchor-tiebreak fix). 🔴 CARRIED-FORWARD: eshop-bundle COMPONENT depletion (Phase-4, gated on cage anchor). ✅ **INGEST SPOF RESOLVED — cron is VPS-resident (`/etc/cron.d/maltytask-shopify-ingest`, `5ade521`) and BUMPED `*/15`→`*/1`+flock 2026-06-17 (`cdb19a5`, NOT pushed; surgical scp deploy under a dirty shared tree; verified firing every minute). 🔴 STILL-OPEN: "Dernier import Shopify" freshness chip threshold hardcoded 90 min (`expeditions.php` ~L4527) — too loose at 1-min cadence; PM rec tighten ~10–15 min + move to `system_settings 'fulfilment'`, deferred until 1-min proven stable.** ✅ **DRAFT-ORDER SKIP-FILTER BUG FIXED+LIVE+CLOSED 2026-06-19 (the "missing John Penman #13662"):** `ingest-shopify-orders.ts` `SKIP_SOURCE_NAMES` dropped ALL `source_name='shopify_draft_order'` orders — but completed drafts in `/orders.json` ARE real admin-created sales (phone/event/counter); ~2/day silently lost from board+FG+fulfilment. Emptied the skip-set → now classified like web orders. **All 3 open items CLOSED:** (1) ✅ COMMITTED maltytask main `b125e90` by pathspec — git HEAD == `/opt`, divergence gone; (2) ✅ HISTORICAL BACKFILL DONE — seal-state checked first (`cogs_fiche_sealed` EMPTY, only April `cogs_fiche_seed` frozen, NO sealed month restated), `--since 2025-12-01` = 314 ins/612 lines/3270 dupes → ALL 351 draft orders now in `inv_sales_orders`, ~CHF 20.3k recovered Dec–May (~191 CHF-0 comps still deplete FG); (3) ✅ `ref_shipping_methods` id=18 'expedition gratuite'→delivery (operator-confirmed), order #13456 reclassed review→delivery. ✅ **SILENT-DROP AUDIT — ALL FIXES SHIPPED+LIVE 2026-06-19:** G (financial_status refresh on dupe-path, maltytask `ce99d01`, re-ingest CORRECTED 10 rotted 'paid'-should-be-refunded) + hardening A(--since now REQUIRED+validated)/B(UTC contract doc)/C(--limit post-collection + `--force-limit` gate)/H(toPeriod UTC) bundled `ce99d01`; **F stable line dedup** — mig **406** `inv_sales_order_lines_stable_dedup` (DROP positional `uk_lines_order_line`+`uk_lines_row_hash`, ADD SOLE `uk_lines_order_ext(order_id_fk,external_line_id)` INPLACE; external_line_id 100% pop / 0 pre-dups) + connector `upsertLine()` ODKU (maltytask `ee06469` / maltyweb `a597ce1`); idempotent-verified 6574 lines stable. 🔑 ODKU under CLIENT_FOUND_ROWS: affectedRows=1 for unchanged too → "updated (content changed): M" is the only reliable counter. 🔴 STILL-OPEN ROADMAP (operator/future): (a) **review bucket 47→41 triage** + unmapped admin-typed titles (`Personnalisé`×17, `Livraison`×2, typos, pickup-ish) → operator, NEVER guess; (b) ✅ **`is_non_stock` watch CHECKED CLEAR** — 0 non-stock among the 351 draft lines; (c) **orphan-line pruning** — removed/refunded lines that vanish on re-pull aren't pruned by the upsert (documented comment, needs a reconcile-step decision; phantom-FG class per [[feedback_derived_table_must_prune_orphans]]); (d) **eshop SKU-bridge gap** — PACKDEC/PACKDECX8/PBD unresolved (~56% line resolution, PRE-EXISTING, all eshop orders; → C3 variant-mapping UI). DURABLE: audit ingest exclusion filters FIRST when a sale is "missing." → full detail [shopify-fulfilment-integration.md](maltyweb-pm-memory/fulfilment-expeditions-arc/shopify-fulfilment-integration.md) §SILENT-DROP AUDIT (SHIPPED) + §DRAFT-ORDER SKIP-FILTER BUG.

0a11b. **✅ ESHOP-VIEW HOIST HOTFIX — LIVE + GIT-CONSOLIDATED 2026-06-17 (`6995146` on main, NOT pushed).** Same nested-fn-not-hoisted 500 class as 0a6/0a11/topbar-repack: `exp_render_chips`/`exp_render_eshop_chips`/`exp_render_eshop_row` were defined INSIDE the `if ($view==='commandes'):` block in `expeditions.php` → PHP doesn't hoist conditionally-declared fns → `?view=shopify` fataled "Call to undefined function exp_render_eshop_row()". Pre-existing in `47c5cac`; operator reported "boutique en ligne shows nothing / site down" (only `?view=shopify` + a transient warehouse blip actually 500'd; rest fine). FIX (Sonnet ui+coder+webapp-testing): lifted the 3 helpers w/ docblocks to file scope under the "FILE SCOPE (hoisted)" comment, behavior-neutral. Worktree off `47c5cac`→`a53786c`, deployed SURGICALLY (single-file rsync `--rsync-path="sudo rsync"`, owner ubuntu:ubuntu 644, md5 parity), Playwright smoke as `smoketest` bot: `?view=shopify` 200 / 42 eshop rows / `?view=commandes` 200 regression-clean. GIT: parallel session committed `406f343` (mig392 cage-redenom, also touches expeditions.php) → main moved to `f9ab038`; rebased hotfix IN THE ISOLATED WORKTREE (clean, no conflict w/ mig392)→`6995146`, `git merge --ff-only` into main (only expeditions.php; main clone clean for it). `exp_render_eshop_row` now at L3579, ABOVE commandes-if at L3665. Worktree removed. 🔴 **PROD vs MAIN STATE: prod expeditions.php = `47c5cac`+hoist (md5 `ec93c714`) — LIVE/works but BEHIND mig392 (parallel session's cage-redenom committed to main, NOT yet deployed to prod). main expeditions.php = mig392+hoist (md5 `f2f4ac42`). NO LANDMINE — both carry the hoist, so whenever mig392 deploys, prod gets hoist+mig392 correctly; a future full deploy can't re-break shopify.** warehouse.php:1816 (htmlspecialchars-on-array) fired 2× at 08:53/08:59 during the parallel 08:56 php-fpm reload, ZERO hits since; current code (FETCH_COLUMN→scalars) can't produce it → judged transient/opcache, not chased. **NOT pushed** (along with `cdb19a5` cron-bump still unpushed). Vindicates the STANDING "smoke deployed page after any nested/conditional-fn move" + "deploy surgically under a dirty shared tree" rules.



---

## SHIPPED-register + RESUME items relocated verbatim (2026-06-21 compaction, pass 2)

3. **🔴🔴 Node tank-sim NAME-KEYING PORT DONE + COMMITTED (maltytask `0251394`) — surfaced a FIRST-CLASS COGS-STORE FINDING: `inv_tank_balances` has MASSIVELY OVERSTATED BBT/WIP across the ENTIRE history. NOT yet `--apply`'d; apply-scope decision WITH OPERATOR.** Root cause (Opus-verified): `bd_packaging_v2.neb_beer` carries SKU CODES for the MAJORITY of rows; old sim keyed packaging→BBT on `normalizeBeerName(neb_beer)` which passes SKU codes through unchanged → SKU-coded packaging NEVER drained its BBT → phantom WIP. Full-history dry-run: CCT byte-identical every month; **BBT drops substantially nearly every month** (2025-05 738→336 hl, 2026-05 766→560, 2023-10 95→29; snapshot rows 669→616). This is the standing-deferred recipe-keyed `(recipe_id_fk,batch)` port of the PHP RC#1. **TWO consequences:** (1) BOARD-vs-STORE DIVERGENCE — PHP board recipe-keyed since ~2026-06-03/04 has shown corrected lower BBTs while inv_tank_balances (Node-written) still holds OLD overstated; apply reconciles. (2) CLOSED-PERIOD COGS RESTATEMENT (OPEN CFO ITEM) — closed months incl. immutable May computed on inflated WIP. **🔴 HAZARD ANSWER (operator's Q): NO live-read consumer recomputes a closed-month COGS from inv_tank_balances at view time (fiche renders STORED rows) — BUT the seed-closed guard is NARROWER than assumed: `assertNotSeeded` blocks ONLY `cogs_fiche_seed` months (April), NOT already-computed `cogs_fiche_monthly` closes; `applyFiche` upserts on `(month_key,category_key)` → re-`--apply --month 2026-05` AFTER corrected WIP lands would OVERWRITE published May wip/total/variation IN PLACE. Never-recompute = a CONVENTION, not a hard guard.** REC: open-month-only apply + HOLD-AND-QUANTIFY closed overstatement as a CFO memo + harden the guard to also refuse `cogs_fiche_monthly` months. (Earlier-this-day CCT-DEST 2-cell fix — same lineage — still needs `--apply --month 2025-10`+`2026-06`.) → tank-simulator-occupancy.md §FIRST-CLASS COGS-STORE FINDING + financier-cogs-fiche.md §CLOSED-PERIOD WIP-RESTATEMENT.

6d. **🟢 PARALLEL-RUN "STIB shows loss, STI4 doesn't" — RESOLVED 2026-06-21 (`461867a`, pushed, LIVE). Real issue = DATA-ENTRY + MIS-LABELLED FIELD, NOT a redistribution feature.** Root cause: (1) DB col `loss_untaxed_full_units` ("full unit lost", mig 233, = Yves's "unité perdue", loss-KPI, OUT of beer-tax base) was MIS-LABELLED **"Perte liquide autre"** in `packaging-form.js` → operator typed **4** for Stirling STIB id6802 (true=**1**), inflating loss 0.18%→0.22%; (2) recap didn't group parallel runs so all breakage landed on the one typed row. **SHIPPED:** data fix `bd_packaging_v2.id=6802` `loss_untaxed_full_units` 4→1 (audit #21727, stored vendable/tax/loss_kpi re-materialised); RELABEL "Perte liquide autre"→**"Unité perdue (pleine)"** in `packaging-form.js` + `visite-guidee.php`; DISPLAY-ONLY recap grouping in `kpi_pkg_daily_recap()` (`app/kpi-handlers.php` ~L6001-6074) by `(recipe_id_fk,batch,event_date,fam)` → `loss_pct=Σgroup loss_kpi/Σgroup vendable` + volume-share `loss_hl`, **headline totals unchanged** (STIB & STI4 now both 0.18%); the earlier liquid migs 415/416/417/419 + form-packaging recompute + Pertes double-count fix all KEPT (harmless/correct, same commit). 🟡 **The heavyweight "redistribute unit COUNTS across all history incl. sealed months" build = NOT BUILT / DEFERRED** — display-grouping + data fix + relabel met the need; ruling kept on file as the path IF per-SKU STORED vendable/COGS ever needs a true split. **DURABLE LESSON: operator-term "loss %" = unit/invendable losses ÷ TOTAL parallel-run production (Yves: Σloss_units/Σ(B+4 produced)); CONFIRM FIELD SEMANTICS vs LABELS before building — the label↔col mismatch cost a full wrong-target build.** → [loss-model-consistency-arc.md](maltyweb-pm-memory/packaging-losses-convention/loss-model-consistency-arc.md) §RESOLVED (+ §DEFERRED ruling). Trigger "unit loss"/"invendable"/"unité perdue"/"loss_untaxed_full"/"half_filled"/"Perte liquide autre"/"redistribute loss"/"spread breakage"/"parallel run loss"/"loss rate per SKU"/"STIB STI4"/"recap grouping".

- **PLANNING PAGE — `/modules/planning.php` (Phase 1 calendar + Phase 2 eligibility)** (mig 364 `pl_plan_days`+`pl_plan_items`; commit `da202a9`; SHIPPED+COMMITTED+LIVE 2026-06-15, **pushed-status per shared-clone discipline — confirm with Kouros**). 6 files / 2090 ins: `app/planning-eligibility.php` (NEW, the engine) + `public/api/planning-eligibility.php` (NEW, thin JSON endpoint) + `public/modules/planning.php` (Phase 1→2) + `planning.js` + `planning.css`. 🔑 **`planning_week_eligibility(PDO,DateTimeImmutable $weekStart)` is PURE-READ (CARDINAL RULE — zero writes; only SELECTs on bd_fermenting_v2/bd_racking_v2/commissioning_settings/ref_recipes/pl_plan_items).** Forward-replay: seed CCT/BBT from TankSimulator snapshot → load week's `pl_plan_items` → walk 7 days computing eligibility BEFORE applying each day's items, then apply (mutate working state). In-plan items (even `hors_process=1`) propagate occupancy → a planned racking unlocks packaging on subsequent days; same-day gate is correct (planned racking does NOT unlock same-day packaging). GATES: racking = cold-crash date + effective_garde days (from `yeast-eligibility.php`); packaging = racked_on + min_days_after_racking (commissioning_settings §packaging); kze/dry_hopping = any occupied BBT/CCT. Return shape `['YYYY-MM-DD'=>['racking'=>[],'kze'=>[],'dry_hopping'=>[],'packaging'=>[],'brewing'=>['cct_conflicts'=>[]]]]`. Endpoint: require_login()+require_page_access('planning'), `?week=YYYY-MM-DD` snapped to Monday (read-default-then-validate), Cache-Control no-store, JSON_UNESCAPED_UNICODE|JSON_HEX_TAG|JSON_HEX_AMP. planning.php hydrates `window.PLANNING_ELIGIBILITY` (HEX_TAG|HEX_AMP) + server-side re-checks eligibility on add_wort/add_packaging POST (rejects non-eligible recipes unless brewing/hors_process); JS populates eligible-only selects, `__hors_process__` override auto-checks the hors_process box. Pre-commit reviewer fixed 3 blockers: B1 lowerBound inverted (today→weekStart), B2 endpoint missing HEX flags, B3 packaging drain matched recipe_id alone→fixed to recipe_id+batch (the canonical (recipe_id_fk,batch) keying rule). 🔴 **`ref_pages.planning` is `is_active=0`** (operator/general) → page NOT reachable yet, no tour card due until activated. Trigger "planning"/"eligibility engine"/"pl_plan_days"/"pl_plan_items"/"week calendar"/"forward-replay"/"planifier".

- **QA / HACCP QUALITY-CONTROLS — `/modules/qa.php`** (migs 358/359/360 source-class tables + 361 page-activate; commits `0b52f13`/`eac31da`/`3ede620`/`01728e2`/`8734849` maltyweb + tour `f8478da`; SHIPPED+LIVE+SMOKE-PASSED 2026-06-15, **commits LOCAL-only NOT pushed** — cage-session interleave in shared clone). First real build on the `qa` page (was inactive placeholder). 🔴 **NON-FISCAL observation layer — NEVER feeds COGS/stock/WAC/BOM/beer-tax.** 3 async-JSON panels (net-content/cleaning-efficacy/bottle-reception) mirroring `rm-stocktake-line-add.php`; dropdowns derive by FK from real canonical tables (bd_packaging_v2×ref_skus / bd_cip_events×ref_cip_types / inv_deliveries / ref_mi cat 8); `parse_nullable_decimal()` single-homed in `app/db-write-helpers.php`. Defaults LOCKED (non-destructive): g/mL single measured_value+measure_type discriminator; surface_label free-text; per-reading target+tolerance_abs (no central qa_targets); is_conforming derived-at-write. Tour gap CRITICAL 2→1 (remaining `financier` = COGS-fiche arc's). OPEN: panel-C MI dropdown = whole Packaging cat (125); Q3 inline-fiche capture not built; ref_qa_surfaces/qa_targets normalization deferred. ✅ **4th panel "Analyse de l'eau" (HACCP CCP-1/`CAPA-2026-005`) SHIPPED + LIVE 2026-06-19** (commit `70cc016` maltyweb, LOCAL-only NOT pushed; migs 403/404/405 applied — 402 taken by `402_refold-shipping-title-norm`, 401 in-flight parallel/untouched): 4th tab on qa.php `?view=eau` (NO ref_pages/preset/tour change — RULE 3 satisfied); `ref_water_sample_points` (PS-1..6 seeded; PS-2/4/6 is_active=0 pending hydraulic-schema validation; PS-3=CCP-1) + `ref_water_parameters` (10 params; `limit_operator` ENUM lte/gte/range/presence_absence; RANGE cols `limit_min`/`limit_max` NULL where unverified, `limit_basis='… à confirmer'` — no invented numbers) + `qa_water_analysis` (source/NEVER-COGS; `action_limit` VARCHAR snapshot-at-write; `is_conforming` TINYINT NULL derived; `row_hash` UNIQUE); handler `qa-water-analysis.php` mirrors cleaning-efficacy, 400-not-500, parse_nullable_decimal reused; smoke 9/9 PASS (self-cleaned). ⚠️ obs table uses `created_by_fk` (sibling qa_* use `submitted_by_user_id_fk`) — naming divergence noted, built as-is. Companion HACCP docs AE-01 + MU-EAU-01 → operator Desktop. 🔴 OPEN: push pending (Kouros); OPERATOR-GATE validate PS list vs real hydraulic install before activating PS-2/4/6 + AQ fills NULL OPBD limits; lookup admin-CRUD NOT built (seed-by-SQL v1, CRUD = flagged fast-follow). EQUIP coder+sql+ui+webapp-testing. → [qa-haccp-controls-arc.md](maltyweb-pm-memory/qa-haccp-controls-arc.md). Trigger "qa"/"qa/qc"/"HACCP"/"CCP"/"net content"/"cleaning efficacy"/"bottle reception"/"contrôle qualité"/"qa.php"/"water analysis"/"analyse de l'eau"/"eau"/"sample point"/"PS-1"/"ref_water_parameters"/"ref_water_sample_points"/"qa_water_analysis"/"CAPA-2026-005".

- **BBT/CCT CIP-CADENCE INFRA — RESOLVER BUILT, NOT YET ON BBT CARDS** (migs 175 `ref_cip_types` / 176 `bd_cip_events` / 177 backfill / 213 cct-cadence keys / 230 filler-target; verified live 2026-06-11). Canonical model: **`bd_cip_events`** = one row per CIP (XOR parent FK racking/brewing/packaging; `target_kind`=vessel|machine × `target_code` incl. `bbt`; `target_number`=BBT number; `cip_type_id_fk`→`ref_cip_types`{1 Soude,2 Acide,3 Full CIP,4 Full CIP+rinser}; `cip_date` VARCHAR mixed ISO/US-`%c/%e/%Y`). **A "blend" = a `bd_racking_v2` row INTO a BBT** (`racking_destination_type='BBT'`, `bbt_number`, `is_tombstoned=0 AND interrupted_flag=0`). **Thresholds + reset-type sets ALREADY in `commissioning_settings` section `cip_cadence`** (`cip_cadence_acid_after`=6, `cip_cadence_full_after`=6, `cip_cadence_acid_reset_types`='2', `cip_cadence_full_reset_types`='3,4'). **THE DERIVATION ALREADY EXISTS: `app/cip-cadence.php` → `cadence_for_all_bbts(PDO)` / `cadence_for_bbt(PDO,int)`** = pure-read, per-request-memoised, returns per-BBT {last_full_cip_at, last_acid_cip_at, racks_since_full, racks_since_acid, threshold_full/acid, recommended_action none|acid_recommended|full_recommended, severity ok|warn|critical}; live-verified correct (BBT#8 → 17 racks since full → critical). Currently surfaced ONLY as `window.BBT_CIP_CADENCE` in the RACKING session form (session-body-racking.php:368-392) for the BBT picker. **283 bbt CIP events live; 8 active BBTs.** ⚠️ DATA-QUALITY FLAG: some `bd_cip_events.cip_date` are FUTURE-dated (2026-12-xx) — input/normalization issue, not a resolver bug. **GAP for the BBT-card ask: BBT cards render in `packaging.php` (§"Tanks de garde BBT", `$bbtRef`/`$bbtOccMap`, static `<div class="tank-card">` — NOT clickable). The CCT click-to-modal pattern to MIRROR lives in `tanks.php` (`<button.tank-card data-cct>`, `window.CCT_DETAILS`, `<dialog#cct-detail-modal>`, `public/js/cct-detail-modal.js`).** Build = make BBT cards clickable + inject `cadence_for_all_bbts()` + BBT detail modal; NO new table, NO new threshold store, reuse the resolver verbatim. CSS skeleton `public/css/cip-section.css` exists (racking-form context).

- **USER-AUTH / ADMIN / MANAGER-SCOPE / EMAIL / SHARED-DEVICE** (migs 261/262/264, HEAD `993d5ae`+post-ship `97aae6d`/`1649fb0`/`2e92de1`) — prod-vs-logistics `manager_scope` + `manager_can()`; `user_invites`+`set-password.php`+`mailer.php` (Postfix→IONOS noreply@maltytask.ch); `brewery_identity()` DB-sourced footer (the Neuchâtel-hardcode lesson → coder+ui skill); `auth_shared_devices` remember-me suppression. **2026-06-09: admin-editable username `2169a81` PUSHED; ROBUST LOGIN RESOLVER `auth_resolve_user()` ✅ SHIPPED+LIVE+COMMITTED+PUSHED (resolver = `d50ec4d`) — `auth_verify()` resolves typed identifier on full-username OR first-name-token OR email (case/accent-insensitive, whitespace-collapsed), AMBIGUOUS→FAIL-CLOSED; fixed the "username→First Last" lockout; soft dep = first-name uniqueness (warn at user-create, backlog). BOTH FOLLOW-UPS NOW SHIPPED+PUSHED: #1 collision warning `1fa4bcb`; #2 static login hint `2cd2b24` (`<small class="auth__hint">Prénom, nom complet ou e-mail</small>` in public/login.php + `.auth__hint` rule in public/css/app.css reusing `--ink-mute`, 0.78rem, margin-top 4px; Playwright-verified renders). RESOLVER ARC + both follow-ups FULLY CLOSED.** **2026-06-11: SALES_MANAGER PRESET (id=9, mig 324 APPLIED+VERIFIED on VPS, file UNTRACKED/uncommitted) for Louis Maechler — 8-page set (mon-tableau/financier/settings/approvisionnement/expeditions/tap-shop/warehouse/rm-comparison), operator-NARROWED from PM's 17-page option-B; intended config role=manager/manager_scope=logistics/preset=9; admin trio (charges-bc/ingest/db-browser) stays `require_admin`-gated (un-grantable to managers by design); (C) capability-refactor BACKLOG. NO ref_pages change → no tour-steward dispatch, BUT post-apply advisory flagged CRITICAL=2 PRE-EXISTING tour gaps (open steward item). PARALLEL-DEVELOPER GUARDRAILS (Louis sales-side onboarding) recorded — 5 rules into his brief. MIG HEAD now 324.** → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md).



---

## Thematic-pointer + SHIPPED/PENDING detail relocated verbatim (2026-06-21 compaction, pass 3)

6e. 🟢 **FULL RECIPE MODIFICATION-REQUEST FLOW — PHASE-0 DECISIONS LOCKED + FINAL BUILD PLAN READY 2026-06-19 (BUILD-READY, NOT built).** Managers FILE requests (admins stay direct); notify = in-app badge + email (test-mode first); **scope INCLUDES formats/packaging-BOM** (diverges from prior exclusion — safe b/c request writes only the 2 request tables; ADMIN approval replays through existing writers, capability gate on APPLY-step, LIVE COGS delta at decision). New tables `recipe_change_requests` + `_lines` (mig **412**, next free — VPS head 410, 397 NOW applied, 411=parallel-local `serving_tank_fiche` DON'T touch). change_kind ENUM gains `format_activate/format_deactivate/bom_binding`. 🔴 KEY FINDING: the manager-403-vs-toast discrepancy is a SINGLE top wall `salle-de-controle.php` L128 `if(!is_admin)` → ALL per-action `is_admin||is_manager` checks are DEAD for managers; relax L128 ONLY for `submit_change_request`, never flip the per-action checks on. SINGLE-WRITER file → sequential atoms, no parallel agents. 4 phases (P1 sql+coder; P2-P4 ui+coder+sql+webapp-testing). Trigger "modification request"/"recipe change request"/"demandes recette"/"recipe_change_requests"/"submit_change_request"/"manager edit recipe" → READ [recipe-ingredients-drift-surface.md](maltyweb-pm-memory/recipe-ingredients-drift-surface.md) §PHASE-0 DECISIONS LOCKED + FINAL BUILD PLAN.

- **BOM REVIEW SURFACE — ARC CLOSED** (defect tab `7730bc4` + browse tab `43022d7` + WAC-divergence guard; SHIPPED+LIVE 2026-06-06, ZERO mig) — `public/modules/bom-review.php`(now 2-tab)+css+js; standalone, `require_page_access('bom-review')` (ref_pages: min_role=admin, domain=admin, sort=185). **Defect tab:** 3 feeds; EDIT ONLY at binding/per-SKU-choice/ref_mi-price altitude (NEVER `ref_sku_bom` — recompile clobbers); brewing read-only-w/-provenance; CSRF+log_revision+snapshot+PRG; altitude round-trip tested (DIV33C→617, parity 0); reconcile-pages 0 drift. Feed 2 (unpriced-MI) = `ref_mi.price IS NULL OR =0` ∩ active pkg-BOM = 24 rows, PM-VERIFIED IDENTICAL to `v_mi_cost.cost_basis='no_basis'` → ACCEPTED w/ forward-divergence ticket. **Browse tab "Parcourir les BOM":** all 153 active SKUs grouped classification→subtype (`ref_recipes`, LEFT JOIN, census 99/34/6/7 + 7 composite + 2 format-NULL = no drop), format-filterable (`ref_packaging_formats.display_name`); provenance tier + snapshotted `ref_sku_bom.cost` (no recompute on render); SAME edit actions/recompile path; confirm-blast-list + inline parity + closed-month COGS warning. **⚠️ `sdc_recompile_recipe_packaging()`+`sdc_flash_bom_result()` RELOCATED into `app/sku-bom-compile.php` (function_exists-guarded), required by both pages — canonical home is the lib now, NOT salle-de-controle.php.** → [packaging-bom-model/README.md](maltyweb-pm-memory/packaging-bom-model/README.md) §SHIPPED BROWSE.

- [Consumption / COGS / COP derivation wiring — v1 vs v2, module-by-module (VERIFIED LIVE 2026-06-08)](maltyweb-pm-memory/consumption-cogs-derivation-wiring.md) — read when reasoning about which production version (v1/v2) a computational module reads, why a month's `inv_consumption` is under-populated, the `inv_consumption` writer chain, the COGS/COP engine location, the May-close gating, or "migrate the computational modules onto maltyweb". Holds: the per-script source-table table (all `derive-*-consumption.ts`+`parse-tank-simulation.ts` already read v2; none on maltyweb; none cron'd); THE REAL GAP = `bd_brewing_ingredients_parsed_v2` FROZEN at 2024-06-17 (brewing lines un-refreshable from v2; live lines in v1 `bd_brewing_ingredients_parsed`); COGS/COP engine NOT on maltyweb (`cop_monthly`/`cogs_monthly` = 0 rows, still Node→BSF); the revised May-close gating list; the full migration sequence. 🔴 **COGS INVENTORY-VARIATION FICHE = SEPARATE table family, NOT `cogs_monthly` (verified live 2026-06-11, mig HEAD 330).** Canonical fiche tables (mig 330, both `policy=allowed`): `ref_cogs_fiche_categories` (12 keys Malt/Hops/Ingredients/Yeast/Cartons/Inliner/Capsules/Verres/Bouteilles/Etiquettes/Canettes/CapsFuts, each w/ `inv_gl`+`charge_gl` from EXT CFO-source, `is_brewing` flag) + `cogs_fiche_opening_anchor` (April-2026 opening ALREADY SEEDED, 12 `category_key` rows, Σ=431 354 CHF — the only externally-anchored month). Fiche keys on **`category_key`**, NOT `source ENUM(rm/fg/wip)`. `cogs_monthly` is OWNED by `build-cogs-report.js` (Node sales-driven COGS, `policy=blocked_with_redirect`, read by `app/kpi-handlers.php`) — DO NOT hijack/ALTER it for the fiche; that's a writer-collision parallel-store smell. CUTOVER = TS `cogs-monthly-compile.ts` reads anchor → writes a NEW `cogs_fiche_monthly` `(month_key,category_key)` (NOT yet built; needs mig + schema_meta writer=cogs-monthly-compile.ts). Provenance is STRUCTURAL (anchor table = seed; computed table = compute) — no provenance enum needed. Basis-change reconciling line: disclose, don't fabricate (FG/F2 delta ≈+1809 clean; RM/yeast g→kg portion "non isolable — base héritée", never a computed number).

