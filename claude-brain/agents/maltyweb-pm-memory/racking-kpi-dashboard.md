# Racking / Transferts KPI section — Tank Board (Fermentation) dashboard

> Load when: building/touching racking KPIs, the Transferts dashboard section, or any aggregate read over `bd_racking_v2`. Scoped 2026-05-27 (PM consult).

## ✅ PPB C8 HARDENING — DEPLOYED 2026-05-28 (uncommitted, RULE-2 gate pending)
- **What landed in `app/loss-metrics.php` + `public/modules/tanks.php`:** (a) NEW `LOSS_WORT_SALES` const mirrors `TankSimulator::WORT_SALES` (`'Chien Bleu - Moût Chaud'`/`'…Froid'`) with TODO marker to switch to `process_type='wort_contract'` when that lever exists; (b) NEW `_loss_racking_data_floor(PDO)` memoised resolver + public `loss_racking_data_floor(PDO)` accessor returning `DATE(MIN(submitted_at))` over `bd_racking_v2 WHERE is_tombstoned=0` (live floor = **2023-10-25**); (c) `_loss_load_cast_out()` now HAVINGs `brew_date >= floor` AND skips beers in `LOSS_WORT_SALES`; (d) tanks.php line 1453 column header **"Soutirage" → "Transferts"** (canonical post-`fa6221c`); line 1389 threshold caption "soutirage" → "transferts"; caption appends "· couverture transferts depuis {floor_date}".
- **Live verify:** pre-fix 803 brewed → 163 complete, **640 stuck "en cours"** (the cast-out census was pulling brews back to 2018 with no racking coverage); post-fix 803 brewed → 377 in PPB (426 excluded as pre-coverage), 113 complete, **264 incomplete** = 30 real WIP <90d + 226 DRAWN_SHORT + 8 PKG_NULL; floor violations 0.
- **DATA-COVERAGE CUTOFF RULE (durable, recorded here for future KPIs):** derive the floor LIVE from `bd_racking_v2` MIN(DATE(submitted_at)) — do NOT move it to `commissioning_settings`. The racking table is the source-of-truth for the racking-coverage horizon; storing it would be a stored-derived-duplicate (the same anti-pattern as A1). Any other KPI needing the same gate replicates the derivation, not a config copy.
- **🟡 NEW OPEN FINDING — `bd_packaging_v2.vendable_hl` 100% NULL (2236/2236 live).** The residual 226 DRAWN_SHORT batches confirm the PM's hypothesis: recent batches are under-credited on packaged volume because the column carrying that fact is empty across the entire table. **NOT a PPB fix** — belongs to the packaging-pre-framework-pass / future packaging pilot under the session framework. Cross-ref: packaging-bom-model/README.md, packaging-brewing-pre-framework-pass.md. Same family as the (resolved) Cuv same-day dedup + the RM-retro prereq-2 packaging-consumption gap — all three are "packaged-volume keying is not yet faithful".
- **Did NOT touch:** `LOSS_BBT_EMPTY_THRESHOLD_HL` (2.5 HL stands); `_loss_load_packaging_map()` dedup (already Cuv-aware); `TankSimulator::loadPackagingEvents()` (only the cast-out census filter changed — sim path untouched, no parity action needed).
- **Vocab drift outside PPB scope (backlog, separate session):** "Soutirage" still in `public/modules/form-packaging.php:850,893,894,911,912` + `public/modules/saisies.php:7,43,67`. Sweep in the packaging-form pre-framework pass.
- **STATE:** deployed to VPS, NOT yet committed. RULE-2 commit-stage code review next, then operator visual check on the live PPB section.

## ✅ AS-BUILT — LANDED + DEPLOYED + COMMITTED 2026-05-27 (`c38cea1`)
- `c38cea1` feat(fermentation): racking/transferts KPI section on `public/modules/tanks.php` (+ `public/css/app.css`). Built EXACTLY per the plan below.
- **What shipped:** (1) **operator strip** — racks + HL last-30d, racks this ISO week; (2) **per-beer historical table** with `?rack_year=` filter — volume racked, # racks, avg post-rack CO₂/O₂, CSS volume bars; Nébuleuse vs contract kept SEPARATE (never summed). Reads `bd_racking_v2` directly (`is_tombstoned=0`), beer resolved via recipe FK join.
- **As advised: `avg_speed` + destination-split EXCLUDED.** No JS, no chart-lib, no migration — server-rendered like ferm-stats (the cheapest path in the plan, taken).
- **LIVE-VERIFIED numbers:** last-30d 14 racks / 1295.8 HL; sanity total 399 racks / 33,233.6 HL (matches the 399-row live data-state below).
- **Autonomy note:** operator lost internet mid-session having granted "use all agents/skills necessary" → the agent proceeded with the 5 recommended KPI defaults (below) rather than blocking. The KPI choice is read-only + trivially iterable — operator can redirect the headline-KPI set on return WITHOUT a rebuild.
- **DATA ANOMALY surfaced by the table — CO₂ RESOLVED 2026-05-27, O₂ still open:** the table originally showed Diversion avg CO₂ **113.97 g/L** and avg O₂ **13,076 ppb**. The **CO₂ side was ONE typo** (id 344, 666→6.66), now CORRECTED live (see §CO₂-CORRECTION); post-fix Diversion CO₂ avg ~4.07, max 6.66 — clean. **O₂ outliers remain a live data-quality item** (refuse-don't-correct; re-check for a single-typo story). NB the KPI table aggregates `bd_racking_v2` is_tombstoned=0 directly, so it now reflects the corrected CO₂ automatically. (Distinct from — but adjacent to — the 3 NULL-dest Diversion rows 60/66/278.)
- **Still open (carried):** logged-in browser walkthrough of the new KPI section (+ the forms); the 3 NULL-destination Diversion data-quality rows; strain classification keeping the transfer gate Zepp-only. Migration sequence at **182** (KZE=180, safety_cip_done=181, **QC-thresholds backend=182 `13aae07`** — Phase 1 of 2; QC-Phase-2 = form-wiring `window.QC_THRESHOLDS`→FormFramework + Salle-de-Contrôle CO₂ editor, NOT YET BUILT). **TRANSFERT INTERROMPU + beer-lost ruling = saisie-transferts-and-yeast-family.md §TRANSFERT INTERROMPU** (mig ~183; sim volume-aware; gate drops Q2 EXISTS; loss record-only; QC-P2 + interruption = separate commits).

## ROUND-3 transferts deltas — LANDED (recorded 2026-05-27; PM-confirmed via operator + first live rack)
- **`rack_type` is now DERIVED server-side from CIP equipment** — the manual "Type de rack" field was REMOVED. rack_type is no longer operator free-text; it's inferred from which CIP machines are in the event set. Divergence-clean (one fact, one derivation).
- **KZE Target PU / Average PU** — migration **180** (`bd_racking_v2.kze_target_pu`, `kze_avg_pu` DECIMAL(6,2)); shown when KZE CIP selected (see KZE section below).
- **`safety_cip_done` VARCHAR(8)** — migration **181**, added on `bd_racking_v2` AFTER `centri_rinsed`. (Capture flag, varchar to match the flat-col date/flag convention.)
- **Dest-tank-number dropdown bug FIXED** — conditional UI must re-sync AFTER `FormFramework.loadDraft()` (draft restore was leaving the dest-number select desynced from the restored destType). Pattern: re-run the conditional-UI sync as a post-loadDraft hook.
- **FIRST LIVE RACK SUBMITTED** on the new Saisie Transferts form (operator). QC warning popup (FormFramework threshold system) confirmed working — but the volume band is a false positive (see QC-THRESHOLDS RULING below).

## QC-THRESHOLDS RULING — per-beer FormFramework bands (PM, 2026-05-27; NOT YET BUILT, design locked)
> Read when building per-recipe QC thresholds for ANY operator form (racking now; brewing/packaging convergence next). Triggered by the operator hitting a false-positive "121 HL généralement entre 10 et 100 HL" warning on the first live rack. The hardcoded `racked_vol_hl: warn[10,100]` in `public/js/racking-form.js` FormFramework.init is the bug (same hardcoded-constants pattern in `form-brewing.js`).
- **121 HL is REAL, operator is RIGHT (verified live):** Zepp racked_vol_hl n=91, avg **125.8**, max **160.0**, σ 25.5. Embuscade avg 103, Moonshine 89. 20+ racks ≥100 HL, all Zepp. BBT caps go 90/120/240, CCT to 150 — big-BBT blends of multiple 32-33HL brews. The generic warn[10,100] false-positives the highest-volume, most-frequent beer.
- **METRIC GOVERNANCE SPLIT (the core ruling — do NOT treat the 4 metrics uniformly):**
  - `racked_vol_hl` = EMERGENT fact → default = **history mean±2σ (warn) / ±3σ (outlier) per recipe** over `bd_racking_v2` (is_tombstoned=0), clamped [single-brew floor ≈25, max dest-tank cap 240]; operator-overridable. NOT operator-hand-set (data knows better). History is ample for the beers that matter (Zepp 91 / Moonshine 62 / Embuscade 57 / Diversion 47); thin tail (collab/EPH n=1-7) gets a permissive/global fallback. = the COALESCE(override, computed-default) yeast-garde pattern.
  - `bbt_co2` = RECIPE SPEC, but **SEEDED from clean per-recipe history** (operator DECISION 2026-05-27, supersedes the earlier "starts NULL → global" sub-ruling): default = **operator-set target ± tolerance on `ref_recipes`, PRE-SEEDED from the clean per-recipe historical mean** so it works out-of-the-box; operator tunes on the Salle de Contrôle Recettes panel. **CO₂ history is CLEAN, NOT poison** — the earlier "Diversion avg 18.14 / max 666 = gravity-scale garbage" premise was WRONG: it was ONE decimal-misplacement typo (id 344, 666→6.66, corrected live 2026-05-27 — see §CO₂-CORRECTION below). With id 344 removed/fixed, every one of 399 CO₂ rows sits 2.39–6.66 g/L (textbook); Diversion clean = avg ~4.07, max 6.66, σ tight. So a per-recipe mean IS a usable seed (n is ample for the beers that matter: Zepp/Moonshine/Embuscade/Diversion all 40+). Clean beers ARE recipe-stable (Zepp 4.60, Embuscade 4.75, Moonshine ~3.9, σ≈0.5) = real per-style separation → carbonation is a deliberate spec, just bootstrapped from history rather than typed cold. **Seeding from clean canonical history is NOT guessing** (refuse-don't-guess applies to inventing from a price/pattern, not to deriving a default from the recipe's own measured history).
  - `bbt_o2` + `bbt_pressure` = UNIVERSAL PHYSICS → keep GLOBAL (one band all beers), but MOVE the constant into `commissioning_settings` (the flat key→value global-threshold store, same home as `packaging.min_days_after_racking`). Per-recipe override allowed FUTURE, not now. (Diversion O₂ 1983 vs others 9-185 = bad rows, confirms O₂ is universal + outliers are data errors.)
- **HOMES (no parallel store):** recipe specs → NEW NULLABLE columns on `ref_recipes` mirroring mig-168 yeast pattern (`co2_target`/`co2_tolerance` + `racked_vol_warn_lo/hi`/`racked_vol_outlier_lo/hi` overrides); globals → `commissioning_settings`; volume DEFAULT → DERIVED in a view (e.g. `v_recipe_vol_band`, mean±kσ per recipe — the `ref_recipe_profile` gravity/pH precedent), NEVER stored on the recipe row (A1 stored-derived-duplicate anti-pattern). **NO new `ref_qc_thresholds` table** — that's a parallel store of facts owned by ref_recipes + commissioning_settings + bd_racking_v2 history.
  - **CO₂ seed mechanic (operator decision 2026-05-27):** the migration that adds `co2_target`/`co2_tolerance` PRE-SEEDS each recipe's `co2_target` from its clean per-recipe historical mean over `bd_racking_v2` (is_tombstoned=0, post-id344-fix), tolerance a sensible default (e.g. ±0.5 or ±2σ). Unlike `racked_vol` (which stays a live DERIVED view band), CO₂ target is a STORED recipe spec — but seeded once from history, then operator-tunable on the Recettes panel. The seed is a one-time bootstrap, not a stored-derived-duplicate (after seeding the column is an editable spec, no longer tracking history). Thin-tail recipes (n<3) get NULL → resolver degrades to global, refuse-don't-guess.
- **DELIVERY SHAPE:** `app/qc-thresholds.php` single resolver (COALESCE recipe-override → history-default → global), n<3 history → degrade to global + soft note, refuse-don't-guess (never a tight band from 1 point). PHP resolves server-side → `window.QC_THRESHOLDS = {recipeId:{...}}` (json_encode hydration, CCT_DETAILS precedent) → FormFramework reads a DYNAMIC map (engine change: accept injected map, fall back to static block if none → non-breaking for brewing/packaging). **PRELOAD all eligible beers' bands + re-key client-side on card select** (the Transferts form picks the beer AFTER load — static-per-page-load would be stale). Re-key on the selected card's recipe_id.
- **SEQUENCING:** racking first (only form with a LIVE false-positive). Mechanism is reusable (build generic, instantiate per form) → RULE-1 backlog: converge brewing (gravity/temp/vol bands — `ref_recipe_profile` already has the history) + packaging (O₂/CO₂ at fill) onto the same `app/qc-thresholds.php` + dynamic-map. Migration ~182 (re-check `--status`, currently 181). schema_meta for any new view/table.
- **DIVERGENCE FLAGS:** no `ref_qc_thresholds` table; never store computed vol band on recipe row (volume stays a live derived view); CO₂ target IS seeded-then-stored on ref_recipes (one-time bootstrap from clean history, NOT a live-tracking duplicate — distinct from vol); refuse-don't-guess on n<3 (NULL → global); single resolver home; is_tombstoned=0 filter on all band/seed math (consider excluding the 3 NULL-dest Diversion rows 60/66/278); **the CO₂ seed query must run AFTER the id-344 fix is in the live DB (raw RawDB.xlsx now also fixed 666→6.66 as of 2026-05-28; `RawDB-normalized.xlsx` still stale but live DB is authoritative for the seed), so re-verify the live value is 6.66 before computing the seed.**

## CO₂-CORRECTION — id 344 typo fixed (build-state, 2026-05-27; PM-verified live)
- **WHAT:** `bd_racking_v2` id 344 (Diversion) `bbt_co2` was **666.000** — a single decimal-misplacement typo. Operator confirmed true value = **6.66**. Corrected live: `666.000 → 6.66`.
- **VERIFIED LIVE (PM, post-fix):** global CO₂ now n=399, **min 2.39 / max 6.66 / avg 4.363**; exactly ONE row was >6.5 (id 344) and it's now the max at 6.66. Diversion family (recipe-name JOIN) now n=54, min 2.39 / **max 6.66 / avg 4.067** — clean, tight, textbook. Operator's recipe-specific Diversion (id-344-excluded) figures: avg 4.05–4.11, max 5.00, n≈46–47.
- **PREMISE REFUTED:** the earlier §QC-THRESHOLDS ruling claim "Diversion avg 18.14 / max 666 = gravity-scale data garbage that poisons any mean" was WRONG. It was not garbage — it was ONE typo. CO₂ history is clean and usable as a per-recipe seed. (Distinct from the Diversion *gravity*-scale anomaly `reference_diversion_gravity_scale_anomaly`, which is a separate, genuine source-data issue on OG/FG — NOT on CO₂.)
- **✅ RAW SOURCE FIXED 2026-05-28 (reconvergence agent) — CO₂ divergence CLOSED on the raw file:** the reconvergence agent corrected the RAW source `RawDB.xlsx` sheet `RackingData` cell **N346** ("CO2 in BBT" column, Diversion row): **666 → 6.66**, matching the live `bd_racking_v2.bbt_co2` id=344 fix. Guard-asserts confirmed pre-write value was 666 + row was Diversion; re-read verified 6.66. Backup at `/home/kluk/projects/maltytask/data/snapshots/RawDB-backup-pre-co2fix-20260528.xlsx`. So a future re-normalize of `RawDB.xlsx` now CARRIES 6.66 — the CO₂ revert landmine is closed on the RAW file.
- **⚠️ BUT `RawDB-normalized.xlsx` IS STILL STALE (the re-ingest gate, NOT the CO₂ fix's fault):** the DERIVED `data/RawDB-normalized.xlsx` was deliberately NOT hand-edited — it still holds BOTH the old CO₂ 666 AND the racking date-swap (the normalizer is a pass-through for both). The CO₂ raw fix ALONE does not make the normalized file re-ingest-safe — the date-swap still gates it. Safe re-ingest path: (1) racking-agent corrects the date-swap in RAW `RawDB.xlsx`, (2) regenerate `RawDB-normalized.xlsx` from the corrected raw (then carries committed normalizer fixes + 6.66 CO₂ + correct dates), (3) THEN re-ingest is safe. Until all three, NO re-ingest of the historical backfill (would revert live fixes via row_hash change).
- **O₂ still open:** the Diversion O₂ outliers (the old "avg 13,076 ppb") were NOT part of this fix — O₂ stays a global-band metric and its outliers are a separate data-quality item (RESUME-POINT data-quality bullet, refuse-don't-correct). Re-check whether O₂ has its own single-typo story before treating it as garbage too.
- **✅ STILL INTACT post racking-redesign (PM-verified live 2026-05-28):** after the racking agent's C4/C5 work (`e5eb802`/`55721ad`) and a new live rack (id=405, event 2026-05-26, submitted 2026-05-27), `bd_racking_v2` is **400 rows (live=400, tomb=0)**, CO₂ id=344 = **6.660** (fix HELD), global CO₂ min 2.39 / max 6.66 / avg 4.363 / n=399 (the 400th row's CO₂ NULL — it's the new live rack). The C4/C5 redesign did NOT re-ingest from xlsx — it only appended live form rows + added test rows since deleted (id gap 399→405). **The 666 typo fix and the date-swap fix are BOTH safe.** Date-swap also INTACT — monthly day>12 histogram healthy across all 32 months (no impossible-0% month), 0 future rows. The xlsx-revert landmine remains theoretical: it only bites if someone re-ingests the historical backfill from `RawDB.xlsx`/`RawDB-normalized.xlsx`, which nothing on a near-term path does.

## KZE flash-pasteurisation form addition — LANDED + COMMITTED 2026-05-27 (`b40e73f`, migration 180)
- `b40e73f` feat(transferts): KZE section on `form-racking.php` + migration **180** (`bd_racking_v2.kze_target_pu`, `kze_avg_pu` DECIMAL(6,2)).
- **Trigger:** when KZE is in the CIP set (individual KZE done, OR Centri+KZE simultané), the form reveals **Target PU + Average PU** inputs (JS-toggled, required-on-visible, NO hidden-required deadlock; included in both the form row AND the hash basis).
- **MODEL = 1-to-1 scalar columns on `bd_racking_v2`, NOT a child table.** Correct call: PU is one-per-rack (a scalar fact of the rack event), not 1-to-many like CIP. If packaging-KZE ever needs PU, that's a DIFFERENT event's columns (on `bd_packaging_v2`), NOT a parallel store on a shared table. Architecturally clean — no divergence.
- NB: this is the rack-EVENT's pasteurisation outcome; the KZE *CIP* (the machine clean) still lives in `bd_cip_events` (cip-module.md). Two distinct facts, two distinct homes — do not conflate.

## CANONICAL HOME — there is NO separate "fermentation page"
- The operator's "fermentation page" = **`public/modules/tanks.php`** (`$active_module = "fermentation"`, crumbs `["Accueil","Fermentation","Tank Board"]`). It IS the live Fermentation dashboard. `form-fermenting.php` is an operator INPUT form, NOT a dashboard — do not confuse.
- `tanks.php` is a **flat-scroll dashboard**, NO sec/subtab router. GET params today = as-of date (`year`/`month`/`day`) + `ferm_year` (ferm-stats filter). A racking section adds its OWN filter param (e.g. `rack_year`) the same way — do NOT invent a tab router.
- Page layout precedent (top→bottom): `<section class="wort-kpis">` (KPI stat strip: occupied CCT, HL in CCT) → `<section class="tanks-section">` (CCT cards) → `<section class="tanks-section ferm-stats">` (per-beer historical table + year filter + CSS bars). **The Racking section = a NEW sibling `<section class="tanks-section">` placed AFTER ferm-stats** (downstream in the process: ferment → cold-crash → RACK).

## RENDERING CONVENTIONS (mirror exactly)
- **NO chart library.** House does bespoke SVG (cct-detail-modal.js = hand-rolled monotone-cubic Hermite spline) + CSS `--pct` bars (ferm-stats). For the historical visual: either a CSS-bar table (cheapest, mirror ferm-stats) or a small hand-rolled SVG bar/line like cct-detail-modal. Do NOT introduce Chart.js/d3.
- **Data-builder convention = the PHP module IS the builder.** No separate query-module/endpoint/repo. SQL runs INLINE in `tanks.php` (prepared statements directly against `bd_*_v2`/`ref_*`), accumulated into PHP arrays.
- **Hydration pipeline** (only if JS rendering is needed): PHP computes array → `<script>window.X = <?= json_encode($x, JSON_HEX_TAG|JSON_HEX_AMP|JSON_HEX_APOS|JSON_HEX_QUOT|JSON_UNESCAPED_UNICODE) ?>;</script>` → deferred JS file (`?v=filemtime`) reads `window.X`. Precedent: `window.CCT_DETAILS` + `cct-detail-modal.js`. For a simple KPI-strip + CSS-bar table, server-render in PHP (no JS needed) — mirror ferm-stats, cheaper.
- **CSS** in `/public/css/`, never inline. The tank-board CSS lives in app.css / page CSS; add racking styles there. Light-kraft palette, token-only (no hardcoded colours; mid-tone tokens as TEXT must pass AA on `--bg-elev` → use `--oak-deep`/`--steel-mid`/`--ink-mute`, never `--oak`/`--steel`).
- **compute-on-READ (live view)** is the right mode per the computation-layer decision rule (KPI re-slices = cheap, must-be-live, never frozen). NO materialized cache, NO migration needed. Show a `computed_at`/"as of" freshness note.

## VERIFIED LIVE DATA-STATE (`bd_racking_v2`, is_tombstoned=0, 2026-05-27)
- 399 live rows; `event_date` + `racked_vol_hl` + `start_time` + `end_time` = 100% populated; range 2023-10-25 → 2026-05-22 (~31 mo).
- `bbt_o2` 399/399, `bbt_co2` 399/399 — fully real. CO₂ range **2.39–6.66 g/L, avg 4.363** (post id-344 fix 2026-05-27; was one 666 typo). [Prior note said 398/399; live confirms 399/399 CO₂ non-NULL.]
- `blend_hl>0` on 125 rows (residual = real).
- `neb_recipe_id_fk` 362, `contract_recipe_id_fk` 36 — contract racking is real, MUST attribute separately from Nébuleuse (never sum them as one).
- Destination split = **near-degenerate: BBT 395, CCT 1, (null) 3** (the 3 NULL = the known Diversion data-quality backlog rows ids 60/66/278). YT 0.
- `hors_process_flag=1` = **0 rows** (gate just went live; all backfill = flag 0). Hors-process-rate KPI is trivially 0% today — build it (degrades gracefully), but it carries no signal yet.
- Blend/parallel double-count risk = **LOW**: only 3 groups share (event_date, neb_beer, neb_batch), all genuine separate racks. `SUM(racked_vol_hl)` is sound without dedup — but EXCLUDE `is_tombstoned=1` always.

## KPI VERDICTS (real vs not) — [BUILT: the REAL set below shipped in `c38cea1`; the NOT-real set was excluded as advised]
REAL (build these):
- **Volume racked over time** = `SUM(racked_vol_hl)` by month/beer. Sound. Filter is_tombstoned=0, NULL-safe.
- **# of racks** = COUNT. Sound.
- **Post-rack CO₂ / O₂ distribution** = `bbt_co2`/`bbt_o2` (99-100% populated). Real quality KPI — avg/min/max or band.
- **Residual/blend volume** = `blend_hl` (125 rows >0). Real.
- **Contract vs Nébuleuse split** = neb_recipe_id_fk vs contract_recipe_id_fk. Real, MUST keep separate.
- **Garde / time-in-CCT before rack** = derivable via the eligibility chain (cold-crash date → rack event_date) BUT it's a JOIN across `bd_fermenting_v2` ColdCrash → `bd_racking_v2`, same derivation the ferm-stats table already does (cooling→cold-crash). Sound but needs the join; mirror ferm-stats' prefix→recipe resolution.

NOT real today (do NOT build as headline; surface as gaps):
- **`avg_speed`** — only 58/399 (15%) populated AND does NOT equal `racked_vol_hl / duration` (id3 vol=92/320min→17.25 but speed=20). Meaning/unit ambiguous. NOT a sound KPI. The operator's "auto-derivable from CIP start+end" is aspirational — start/end ARE 100% populated, so a *derived* `racked_vol_hl / duration_hr` rate is computable, but (a) it's a NEW metric not the stored avg_speed, (b) duration has garbage (min=-692 min — start>end entry bugs; max=1003 min). REFUSE to headline a speed KPI until duration is cleaned + the metric defined with the operator.
- **Destination split** — degenerate (395 BBT / 1 CCT). Show it but it carries ~no signal; not a headline.
- **Rack duration** = `TIMESTAMPDIFF(MINUTE,start_time,end_time)` IS 100% derivable BUT has negative + outlier values (back-dating / day-month swap fragility) → guard (filter ≤0 and absurd highs), surface count of excluded.

## DIVERGENCE / DERIVATION-TREE RISKS
- `bd_racking_v2` is **DESTINATION-only, never an occupancy source** (occupancy = TankSimulator) — KPIs read it fine, but never use it to infer tank occupancy.
- Contract vs neb: NEVER sum the two recipe FKs into one beer total.
- Back-dated racks + day↔month swap (see `project_racking_date_day_month_swap`) → some event_date / duration values are wrong. Volume sums by month inherit any swapped dates; flag, don't blind-correct.
- 3 NULL-dest Diversion rows (60/66/278) — exclude from destination KPI, surface as the existing data-quality backlog (RQ candidate).
- Read DIRECTLY from `bd_racking_v2` (no canonical view exists for racking aggregates; none needed for a read-only KPI). Do NOT couple to COGS/inventory — racking isn't a COGS source. is_tombstoned=0 is the only mandatory filter.

## SEQUENCING — [DONE as planned: one PHP pass, inline SQL, server-rendered, no migration; `c38cea1`]
- No migration (read-only). Data-builder (inline SQL in tanks.php) → server-render KPI strip + CSS-bar table (mirror ferm-stats) → optional JS/SVG only if a richer historical visual is wanted. KPI panel + historical table CAN be one PHP pass (both read the same query set); parallelizing buys little — one Sonnet (ui+sql+coder) does both.
- RULE-2 reviewer before commit. RULE-1: nothing superseded (additive section).

## OPERATOR DECISIONS [RESOLVED — defaults 1-4 taken as recommended; #5 speed left out as advised. Operator may revise the headline-KPI set on return (read-only, no rebuild).]
1. Which KPIs front-and-center (recommend: volume racked, # racks, CO₂/O₂ quality, contract/neb split — DROP avg_speed + destination-split as headlines).
2. Historical window: rolling-12mo vs per-year (ferm-stats uses per-year `?ferm_year=` — recommend mirror it).
3. Per-beer vs aggregate (ferm-stats is per-beer — recommend mirror).
4. Operator-KPI (recent ops health: last-30d racks, today's pending) vs management-KPI (trends, totals). Recommend: KPI strip = operator/recent; historical table = management/trend.
5. Define the rack-speed metric IF wanted (vol/duration), AND clean the negative-duration rows first — else leave speed out.
