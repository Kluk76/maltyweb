# Email-order ingestion arc (`commandes@lanebuleuse.ch` → ord_orders → BC) — a.k.a. "COMMANDES READERS"

> Read when touching: `commandes@` mailbox ingestion, per-sender email-order parsers, `doc_email_messages`/email-order producer, the Phase-1 BC-orders link from email, the logistics mail↔BC validation surface, **"commandes readers"** / "lire les commandes" / "auto-read orders" / "prepare on BC". Companion: [fulfilment-expeditions-arc/README.md](fulfilment-expeditions-arc/README.md) (D1/D2 BC spine, ord_orders schema, fg_stock XOR), [sales-commercial-surface/README.md](sales-commercial-surface/README.md) (ref_customers, BC two-source). PM-SCOPED 2026-06-15. Kouros: "lance".
>
> **🔴🔴🔴 2026-06-19 — CARDINAL VISION CLARIFIED (Kouros, SUPERSEDES the "promote paused-forever / auto-push NOT this build" note): THIS MODULE EVOLVES INTO AUTOMATIC ORDER-FILLING INTO BC BY maltytask. ONE pipeline, split by a dedup gate.**
> ```
> read email order
>   → CHECK if it already exists in BC (ord_orders source='bc')
>       → EXISTS  : nothing to do — ARCHIVE  (the reconciliation / suppress case)
>       → NOT     : create a VALIDATION ASK  (the existing email-orders.php review card)
>           → operator validates → maltytask CREATES the order in BC  (D1 push spine)
> ```
> - **AUTO-FILL-INTO-BC IS THE CONFIRMED END VISION** (Kouros picked this explicitly 2026-06-19) — the module replaces manual keying OVER TIME. The dedup-vs-BC gate is what makes that safe during transition: it prevents clashes with orders the team still keys manually.
> - **PROMOTE IS NOT PAUSED-FOREVER — it is the BACK HALF of the pipeline**, gated BEHIND the dedup check. Its endpoint is a **BC SalesOrder create** (via the `push_bc_sales_orders.py` D1 spine), NOT merely an `ord_orders source='email'` row. Reconciliation (matched→archive) and entry (unmatched→validate→BC) are ONE pipeline, split by the dedup gate. The reader's job is BOTH: suppress what's already in BC, AND become the entry path for what isn't.
> - **🔑 OPEN DESIGN QUESTION (flag at build, don't pre-decide):** on validation-accept, does the system (a) directly create `ord_orders source='email'` + push to BC, or (b) push to BC and let the `*/15` BC reader ECHO it back as a `source='bc'` row? **Path (b) — push-to-BC-and-echo-back — is CLEANER and avoids a parallel store** (no `source='email'` row competing with the BC echo; one canonical row, BC-owned). Lean (b); confirm at build.
> - **CURRENT PROCESS (operator ground truth, the transition baseline):** every email order is MANUALLY keyed into BC today by reps/logistics; the BC `*/15` reader pulls them into `ord_orders` (source='bc')/Expéditions. So today the email is a DUPLICATE of a row BC already produced — which is exactly why the dedup gate comes FIRST. As auto-fill matures the team keys less and the unmatched→create path carries more.
> - **rep-account pre-fill (mig 418) is ACTIVE again** — it pre-fills the validation ask for not-in-BC rep orders (its original purpose), and doubles as a match/identity aid on the dedup side.
> - **EMPIRICAL PROOF (live `ord_orders`, 2026-06-19) that the overlap is real:** the two Bevanar emails the parser parsed — **No 32503 + No 32529** — ALREADY EXIST as `source='bc'` rows (id=206 `bc:ORD210128` `external_document_no=32503`, 7 lines, confirmed; id=223 `bc:ORD210147` `external_document_no=32529`, 2 lines, shipped), customer FK Bevanar/Dorga, pulled by the LIVE `*/15` BC reader. `external_document_no` carries the order/PO No on 13/61 bc rows. The existing Shopify-pickup twin-check does NOT cover this (it guards only `inv_sales_orders source='shopify' fulfilment_mode='pickup'`); there was never a twin-check vs `source='bc'` ord_orders — which is exactly the MATCH ENGINE (dedup gate) now being built.
> - **No-double-deplete invariant STILL HARD:** an email order that ALREADY exists in BC must NEVER create a second `ord_orders` row (→ DOUBLE Leg-1 fg-stock depletion + double operational COGS). The dedup gate is the COGS/inventory safety. Currently HARMLESS in prod (0 `source='email'` ord_orders exist), but the match engine must land + be Opus-verified before any commandes@ order is ever pushed or surfaced as actionable.
> Trigger "already in Expéditions"/"already in BC"/"duplicate email order"/"dedup vs BC"/"reconciliation"/"auto-fill BC"/"order-filling"/"validation ask"/"echo-back"/"exception queue"/"unmatched email order"/"external_document_no match"/"Bevanar No"/"32503"/"32529" → READ THIS BLOCK.
>
> **➡️ BUILD SEQUENCE (confirmed 2026-06-19) — the pipeline lands in three gated steps.**
> 1. **DEDUP-VS-BC MATCH ENGINE — building now, DRY-RUN REPORT FIRST (safe, no writes).** Match each parsed email order vs `ord_orders source='bc'`: **PRIMARY = `external_document_no`** (the "No 32529"); **FUZZY FALLBACK = customer + requested_date±Nd + SKU-overlap**; low-confidence → UNMATCHED, NEVER auto-link. First deliverable = a **reconciliation report**: of the ~34 parsed email orders, which already exist in BC vs which don't. Outcome per order: MATCHED → auto-archive (record the bc link, no review card); UNMATCHED → validation ask.
> 2. **via-Commandes ORIGINAL-SENDER EXTRACTION** (Reply-To / X-Original-From / X-Original-Sender) so unmatched alerts + validation asks name the REAL customer, not the `commandes@` list address.
> 3. **VALIDATION-ACCEPT → BC PUSH** [D1 spine, DISARMED, per-push Kouros-go, **Opus INDEPENDENT verify of the no-duplicate-create invariant**]. This is the un-paused back-half — the unmatched order, once validated, is created in BC.
> EQUIP: **coder + sql** (+ **ui** for the validation/exception surface) + **Opus independent verify** (it gates inventory/COGS — both by SUPPRESSING dups at the gate AND by CREATING the right BC order downstream). NEVER-GUESS hard: a fuzzy match that isn't confident → leave UNMATCHED, never auto-link a bc row; a customer that doesn't resolve → operator picks the FK, never guessed.
>
> **🟢🟢🟢 2026-06-19 — REP→INTERNAL-ACCOUNT SLICE IS LIVE + VERIFIED + COMMITTED (`5fecd85` maltyweb main).** The rep-account pre-fill shipped end-to-end. **Migration 418 `ref_internal_order_accounts`** (`sender_email VARCHAR UNIQUE → customer_id_fk` FK→ref_customers, is_active, notes) — applied **MANUALLY** (manual ALTER + manual `schema_migrations` INSERT, NOT migrate.php, because parallel **mig 411** is still pending-applied and migrate.php would sweep it); schema_meta classified reference/admin-managed. **6 SEEDED ROWS (verified live, each → the rep's OWN ACTIVE BC account, never an échantillon dup):** dorian@→**3050** (Fairhall/BC3947) · john@→**2377** (Penman/BC3625) · louis.cardis@→**895** (Cardis/BC2062) · nicolas.berger@→**1164** (Berger/BC2358) · nicolas.cerruela@→**2233** (Cerruela/BC3478) · tania@→**2379** (Schott/BC3629). **CORRECTIONS vs the held proposal:** (a) **Daniel Hersberger was NOT a rep → DROPPED** (the proposed 2400/BC3650 is gone); (b) Nicolas Cerruela added (id **2233**); (c) the note "3478" was a BC number, NOT a ref_customers id — Cerruela's ref_customers.id is **2233**.
> **Parser side (`ingest_email_orders.py`):** injects `_internal_rep` + `_rep_email` into `parsed_json` via an `extra_hints` serialisation path, **ONLY when `customer_hint=='' AND the bare sender email ends with @lanebuleuse.ch`** (forwarded-customer + external From both take precedence → no internal-account dumping). 🔴 **BUG CAUGHT + FIXED IN VERIFICATION (durable):** the first cut tested `from_address.endswith('@lanebuleuse.ch')` on the RAW header — which is `"Name <email>"` form → NEVER matched live (flagged=0). FIX = extract the BARE email FIRST, then test the suffix. After fix: **21 flagged, 0 coexisting with a resolved customer** (precedence holds — flag fires only on the empty-hint internal path).
> **Resolver side (`email-orders.php` + `email-orders.css`):** on `_internal_rep`, a PARAMETERIZED SELECT against `ref_internal_order_accounts JOIN ref_customers` PRE-FILLS the customer typeahead (operator-overridable; htmlspecialchars output). php -l gated before deploy. **customer_hint stays a string→FK-at-validation — the pre-fill is a hint, the operator confirms (never-guess held).**
>
> **🟢🟢🟢 SLICE 2 — via-Commandes ORIGINAL-SENDER recovery — LANDED + DEPLOYED + VERIFIED 2026-06-21 (NOT yet committed).** Slice 1 (dedup-vs-BC match engine, `scripts/python/bc_order_match.py`, dry-run `--report`) landed earlier: of 36 parsed orders, 3 in BC (all Bevanar via `external_document_no` exact match conf 0.95, no false matches). Slice 2 unblocks moment 2 by recovering the masked external sender. **AS-BUILT:**
> - **Mig 421** `db/migrations/421_doc_email_original_sender.sql` = `ALTER TABLE doc_email_messages ADD COLUMN original_sender VARCHAR(320) NULL AFTER from_address`. Applied CLEAN via migrate.php (0 foreign pending at apply-time, 420 was last applied). NO schema_meta row (ALTER on existing table, not a new table).
> - **Connector `scripts/python/ingest_email_orders.py`:** `_extract_original_sender(msg)` tries X-Original-Sender → X-Original-From → Reply-To, `parseaddr`-extracts the BARE email FIRST then guards @lanebuleuse.ch + system addrs (commandes@/production@/info@), stores bare lowercase. Persisted on BOTH the INSERT and the dup-key UPDATE branch (UPDATE backfilled the 11 existing rows on `--force` re-ingest). Threaded to the parser via `env.original_sender`. `_internal_rep` guard now ALSO requires `original_sender is None`.
> - **Parser `email_parsers/generic_vocab.py`:** `_resolve_customer_hint()` cascade is now a→inline client block (WINS) → b→forwarded → c→**original_sender (NEW)** → d→external From → e→''. Same bare-extract+guard at every level.
> - **UI `public/modules/email-orders.php` + `public/css/email-orders.css`:** SELECT now fetches `original_sender`; all 3 buckets (À valider / Non parsé / Traités) render a secondary line "Expéditeur réel : <email>" when original_sender present AND not contained in from_address (the masked via-Commandes case). New CSS class `.eo-original-sender` scoped `body.email-orders-page`, JetBrains Mono, `var(--ink-label)`. php -l clean.
> - **🔴 VERIFIED RESULTS — corrected the design-time predictions (record these as ground truth):**
>   - original_sender recovered for **10/11 commandes@ rows** (id 109 had NO recoverable header). Real customers recovered: docks, ecublens, nausikraft (lorin@), petitecave, amstein, stardrinks ×2, blavignac, boissons-gds, edu-vd. ⬅️ **THIS is the real value of the slice — 10 previously-anonymous external customers now identified.**
>   - `_internal_rep` dropped **21→20, NOT ~10 as predicted.** ROOT CAUSE: the masked commandes@ rows were mostly `parse_status='no_match'` (generic_vocab declined the body), so they were never FLAGGED `_internal_rep` to begin with — only Docks (id 24) was. The mis-flag was a **1-row** problem. ⬅️ DO NOT carry the "flag drops ~10" claim forward; sender recovery, not flag reduction, was the win.
>   - BUG-check CLEAN: zero rows with both `original_sender` set AND `_internal_rep` true.
>   - `bc_order_match.py --report` now: **38 parsed → 3 matched-to-BC / 35 unmatched.** Recovered-sender rows still show `no_order_ref_customer_unresolved` (CORRECT — customer_hint is a string, FK resolved later at the review card = dedup moment 2).
> - **🔴 NEW BACKLOG ITEM (L1-template feed):** 8 of the 11 commandes@ rows are `parse_status='no_match'` — generic_vocab can't parse their order BODIES even though we now know the sender. These are exactly the L1 per-sender-template candidates (Nausikraft, Amstein, Stardrinks, Petite Cave, Blavignac, Boissons GDS, edu-vd…), GATED on Kouros forwarding a sample `.eml` per top sender. The recovered original_sender is now VISIBLE in the "Non parsé" bucket so the operator knows who they're from. → folded into §L1 PER-SENDER TEMPLATES roster.
> - **🔴 GIT STATE: NOT committed (Kouros commits on ask).** Uncommitted in the shared maltyweb tree (parallel comm-threads session ALSO dirty): `bc_order_match.py` (Slice 1), `ingest_email_orders.py`, `generic_vocab.py`, `421_doc_email_original_sender.sql`, `email-orders.php`, `email-orders.css`. SHARED-TREE RACE RISK — commit MY files via explicit `git add <files>` pathspec SOON. 🔑 comm-threads parallel session = DISTINCT tables (`comm_threads`/`comm_messages`/`comm_message_docs`/`comm_address_pins`) — NO collision.
>
> **🟢🟢🟢 SLICE 3a — review-card BC dedup GATE ("moment 2") — LANDED + DEPLOYED + COMMITTED 2026-06-21 (`d592c7e` maltyweb main).** 🔴 **CORRECTED 2026-06-21 (ground-truth verified live) — 3a is NOT "read-only on business data": it is a DEDUP GATE in front of the PRE-EXISTING `email_order_promote` producer, which WRITES `source='email'` ord_orders + ord_order_lines + source_email_id_fk + review_status='accepted' + source_ref='email:<msgid>' (with FOR UPDATE + refuse-if-already-order_created idempotency).** That producer was wired at `email-orders.php` L143 in the PARENT of the 3a commit — NOT introduced by 3a. 3a's OWN writes touch only `doc_email_messages` (`parse_status='reconciled'` + `bc_matched_order_id`), but the `unmatched` + "Créer quand même" paths fall THROUGH to `email_order_promote()` which DOES INSERT ord_orders. It has NEVER fired in prod (0 source='email' rows, 0 source_email_id_fk, 0 parse_status='order_created'). **The earlier "READ-ONLY / writes only doc_email_messages" claim is WRONG and is RETRACTED — 3a = dedup gate in front of the pre-existing email_order_promote producer; 0 rows created to date.** Built per the §SLICE 3a DESIGN RULING (D1/D2/D3) above. **AS-BUILT (Opus-independently-verified, not just agent report):**
> - **Mig 423** `db/migrations/423_doc_email_bc_matched_reconciled.sql` applied CLEAN via migrate.php (was the only pending). `doc_email_messages` gains: `bc_matched_order_id BIGINT UNSIGNED NULL` FK→ord_orders(id) ON DELETE SET NULL — 🔴 **type is BIGINT UNSIGNED matching `ord_orders.id`, NOT the INT UNSIGNED the design line predicted** (record this: ord_orders.id is BIGINT UNSIGNED); and `parse_status` ENUM widened to add terminal `'reconciled'` (existing values + order preserved). NO schema_meta row (ALTER on existing table).
> - **`scripts/python/bc_order_match.py`:** new `--match-one --email-id N --customer-id N` mode → single JSON obj `{status,bc_id,method,confidence,reason,bc_order:{external_document_no,order_date,total}}`. Reuses the CANONICAL matcher via a `customer_id_override` param (the operator-resolved FK wins over the parsed_json hint — satisfies "match resolved state, not stale parsed_json"). Exit 0 on clean matched/unmatched, non-zero on error. `--report` unchanged → single matcher, NO PHP/SQL re-impl (D1 held).
> - **`public/modules/email-orders.php`:** on submit, shells out (ABSOLUTE venv python path) to `--match-one`. `status=matched` → server-side INTERSTITIAL (PRG via `$_SESSION`, redirect) with banner "Correspondance BC détectée" + two buttons: **"Archiver — déjà dans BC #NNN"** (→ `parse_status='reconciled'` + `bc_matched_order_id`, mirrors `email_order_promote`: FOR UPDATE, refuse-if-terminal, log_revision, txn) and **"Créer quand même"** (existing `order_created` flip — in 3a the handled-flip ONLY, NOT a BC create; 3b swaps it). `status=unmatched` → falls through to existing promote. **FAIL-CLOSED:** subprocess error/timeout/unparseable → interstitial "Vérification BC indisponible — décision manuelle requise", NEVER silent-create. "Traités" bucket shows `reconciled` rows with "Rapproché BC #NNN" badge.
> - **🔴 HARDENING (Opus-added post-agent, in the commit) = the REAL fail-closed guarantee:** the shell-out timeout was first a deadline loop over a BLOCKING `fread` → a silent/hung python would block past 20s and `proc_close` would HANG the php-fpm worker. Fixed: `stream_set_blocking($pipes,false)` non-blocking reads + usleep poll + `proc_get_status` + on-deadline `proc_terminate(9)`+`proc_close`, exitcode forced -1 → fail-closed branch. **DURABLE LESSON: a PHP subprocess timeout over a blocking pipe read is not a timeout — it can hang the worker; non-blocking + poll + proc_terminate is the only real fail-closed.**
> - **`public/css/email-orders.css`:** `.eo-bc-interstitial` (amber/red), `.eo-btn-archive` (green), `.eo-btn-force-create` (neutral), `.eo-done-item__status-badge` (reconciled/created), `.eo-flash--warn`. Scoped `body.email-orders-page`, NO inline.
> - **VERIFIED (independent Opus spot-check):** SHOW COLUMNS confirms `bc_matched_order_id bigint unsigned` + FK + `'reconciled'` enum value; `--match-one` email 15/customer 464 (Bevanar) → matched bc_id=206 method=order_ref_exact conf 0.95; email 11/customer 2684 → unmatched (internal_rep) exit 0; email 99999 → exit 1 (fail-closed branch triggers); archive write transitions parsed→reconciled + bc_matched=206, idempotent (2nd call refused as terminal), log_revision written, test row restored (NO DB residue); php -l clean; md5 local==VPS on all files; git clean (only the 4 intended files committed). 🔑 A THIRD parallel session (kpi-recap / mon-tableau / mig 424) was also dirty in the shared tree — left UNTOUCHED.
>
> **✅ RESOLVED (by Slice 2, LANDED 2026-06-21) — the "via Commandes" Google-Groups From-rewrite.** External customers who email the `commandes@` distribution list have their `From` rewritten by Google Groups to `commandes@lanebuleuse.ch` (display name kept, e.g. `"'Ana Rocha' via Commandes"`), losing the real sender (which lives in `Reply-To`/`X-Original-From`/`X-Original-Sender`). Slice 2 FIXED both failure modes: original-sender recovery (10/11 commandes@ rows recovered) + the system-addr `_internal_rep` guard (now also requires `original_sender is None`). HINT-only/customer-recovery — still no auto-FK; FK resolves at the review card (dedup moment 2). See the §SLICE 2 AS-BUILT block above for verified results (note: the mis-flag was a 1-row problem, not the predicted ~11; the win was sender recovery).
>
> **🟢🟢🟢 2026-06-19 — ATTACHMENT-ORDER PARSING SLICE IS LIVE + VERIFIED + COMMITTED (`c31b540` maltyweb main).** The first attachment slice shipped: XLSX (Migros) + PDF (Bevanar) order parsers, fed by connector byte-fetch in BOTH the RFC822 and Gmail paths, registered before `GenericVocabParser`. ZERO new pip deps (XLSX→`openpyxl` already installed; PDF→`pdftotext -layout` shell-out). **What landed (`c31b540`):** (1) `attachment_pdf.py` = `BevanarPdfParser` (`pdftotext -layout`, reconcile gate ≤0.10 CHF line-sum vs stated total); (2) `attachment_xlsx.py` = `MigrosFroidevilleXlsxParser` (openpyxl, deterministic header/grid detection, reconcile gate); (3) connector now FETCHES attachment bytes (closing the meta-only gap that was step 0 of this slice). Prior commit `c31b540`'s predecessor `8f1b397` = the text-core generic_vocab hardening (already recorded). **LIVE `--apply --force` re-parse of the 30-day to:commandes@ window: 34 parsed / 66 no_match out of 100.** Parser breakdown: generic_vocab 31, bevanar_pdf 2 (orders No 32503 + 32529), migros_froideville_xlsx 1 (13 lines, exact total). The 66 no_match = genuine non-orders + reply-only threads + senders without a parser yet (L1 candidates).
> **🔴🔴 KEY BUG + DURABLE LESSON (record for ALL future attachment work):** the first attachment build passed unit tests against standalone .pdf files + the Migros .eml fixture but FAILED LIVE — Bevanar order PDFs arrive as `application/octet-stream` with a `.pdf` filename and **NO `Content-Disposition: attachment` header**, so the connector's disposition-ONLY attachment walk silently dropped them (`attachments_json=[]` → no_match). Migros's XLSX masked the bug because it DID carry the disposition header. FIX = a `_is_attachment_part()` helper that treats a part as an attachment when `get_filename()` is truthy (and content-type isn't text/multipart) OR `disposition=attachment`. **LESSON: verify the REAL email's MIME tree THROUGH THE CONNECTOR, not just the parser against a clean attachment file** — a clean .pdf/.xlsx fixture hides how the part is wrapped in the actual envelope. Real Bevanar `.eml` fixtures were captured to lock this. (Same class as the earlier "verify the real email's envelope, not the parser against a clean file" forwarded-customer bug.)
> **🟢 VOCAB SEEDS NOW LIVE:** the two gaps below are SEEDED — Huguette→EPH2 (recipe 76), découverte/pack découverte→PD8 (sku 137). Vocab is now a 411-term live read.
> **🟢🟢🟢 2026-06-19 — TEXT-CORE PARSER-HARDENING SLICE IS LIVE + PM-VERIFIED. P0 was already PASSED.** The forwarded-envelope + qty-binding + Swiss-NPA-guard work (slice steps 1+2) SHIPPED to the VPS and is verified against real fixtures + live 411-term vocab. **What landed in `scripts/python/email_parsers/generic_vocab.py` (4 changes):** (1) `strip_quoted_history()` cuts the body at the earliest quote-intro marker (drops already-quoted order history → no duplicate parse); (2) `_resolve_customer_hint()` multi-signal cascade — inline client block → forwarded external sender → external From → '' — with `@lanebuleuse.ch` filtered at EVERY level (forwarder/internal never wins); (3) `_ADDRESS_LINE_RE` Swiss-NPA/street/phone/email guard that REMOVES address lines (never re-binds a postal code to a SKU); (4) `_QTY_X_RE` binds the `Nx` prefix to the product. **Live verification (fixtures mode against 9 real .eml + live VPS vocab):** Olivier Corthay order now resolves customer=`Corthay Olivier <olivier_corthay@hotmail.com>` (was the internal forwarder), 3 correct lines ZEPB/ALTB/SPYB qty 2 each, NO phantom "1936 Verbier" postal-code line, DEL-/rabais→notes; sample-02 (order fully quoted) correctly `no_match` (no dup). 4 parsed / 6 no_match across the 10 samples — the no_matches are the Migros XLSX (attachment slice), the Bevanar "3 fûts Huguette" thread, and genuine non-orders. **The four diagnostic bugs from the first live run (phantom postal-code line, dropped `Nx`-prefixed lines, forwarder-as-customer, quoted-history dup) are ALL FIXED.**
> **🟢 DEPLOY/STATUS (2026-06-19) — ATTACHMENT SLICE IS COMMITTED.** `c31b540` (attachment_pdf.py + attachment_xlsx.py + connector byte-fetch + registry) is on **maltyweb main, COMMITTED**; `8f1b397` (text-core generic_vocab hardening) likewise. **`--apply` IS NOW LIVE** — the connector writes `doc_email_messages`; parsed orders flow into the already-shipped `email-orders.php` review queue; no_match segregated into "Non parsé". Re-parse is a MANUAL `--force`; **NO cron yet** (operator enables, per house pattern). `doc_email_messages` no longer empty: 34 parsed / 66 no_match on the 30-day window.
> **🟢 VOCAB GAPS — RESOLVED (seeded as master-data aliases, as designed — NOT parser hardcodes):** **Huguette→EPH2 (recipe 76)** and **découverte / pack découverte→PD8 (sku 137)** are LIVE in the canonical vocab; `generic_vocab.load_vocab()` now picks them up (411 terms). The Bevanar "fûts Huguette" order parses via the PDF parser; découverte resolves to PD8.
> **🟢 INTERNAL SAMPLE-REP ORDERS — NOW PRE-FILLED (rep slice LIVE `5fecd85`).** A rep's own sample order (From a mapped `@lanebuleuse.ch` rep mailbox, customer_hint='') now carries `_internal_rep`+`_rep_email`, and `email-orders.php` PRE-FILLS the rep's active BC account in the customer picker (operator confirms). Still a HINT — never an auto-FK. See the §AS-BUILT for the 6 mappings.
> **🟢 NEXT-SLICE ORDER (UPDATED 2026-06-21 — Slice 1 ✅ + Slice 2 ✅ + Slice 3a ✅ + Slice 3b ✅ all LANDED + COMMITTED (3b `99c79d4`, DISARMED); Cobra L1 body-parser ✅ COMMITTED `e36b762`; the CARDINAL VISION holds — auto-fill-into-BC is the end vision; pipeline = dedup-gate → matched→archive | unmatched→validate→BC-create. The back-half (3b) is BUILT+COMMITTED; only the SUPERVISED ARM-TEST + operator flip remains to make it live-firing):**
> - ✅ **Slice 1 — DEDUP-VS-BC MATCH ENGINE** (`scripts/python/bc_order_match.py`, dry-run `--report`): 38 parsed → 3 matched-to-BC (Bevanar via `external_document_no` exact) / 35 unmatched. PRIMARY=`external_document_no`, fuzzy fallback never auto-links.
> - ✅ **Slice 2 — via-Commandes original-sender extraction** (mig 421 + connector + parser + UI, LANDED 2026-06-21): 10/11 masked senders recovered; review cards now name the real customer.
> - ✅ **Slice 3a — review-card BC dedup GATE ("moment 2")** (mig 423 + `--match-one` mode + submit-time interstitial + fail-closed shell-out, LANDED + COMMITTED `d592c7e` 2026-06-21): re-runs the matcher the instant the operator resolves the customer FK → "Correspondance BC détectée" interstitial (Archiver→`reconciled` / Créer quand même→handled-flip). 🔴 NOT read-only (corrected): 3a's own writes touch only doc_email_messages, but it is a GATE in front of the pre-existing `email_order_promote` producer (writes source='email' ord_orders + lines on unmatched/Créer-quand-même paths); 0 rows created to date. See §SLICE 3a AS-BUILT above for the correction + verified results + the non-blocking-poll fail-closed hardening.
> - 🟢🟢🟢 **SLICE 3b — validation-accept → BC SalesOrder CREATE — LANDED + DEPLOYED + COMMITTED 2026-06-21 (`99c79d4` maltyweb main; DISARMED — Opus independently verified before committing).** The (A)(i) retag ruling below was BUILT end-to-end. **AS-BUILT (4 files, committed):**
> - **Mig 426** `db/migrations/426_email_order_bc_push_mode.sql` (NEW, applied via migrate.php — was SOLE pending) — seeds `system_settings(section='logistics', key='email_order_bc_push_mode', value_text='off')`. 🔴 **DISARMED by default** — flip to `'armed'` to fire live BC POSTs (operator-set in Données générales, NO code change). **NB 425 was claimed by a parallel session (`425_brewing_lookup_page`) → 3b took 426.** No schema_meta row (seed into existing table).
> - **`app/email-order-promote.php` — THE CORE RETAG:** `source='maltytask'` (was `'email'`); `source_ref='mt:<ord_orders.id>'` set POST-INSERT via UPDATE in the SAME transaction (matches `bc_echo_format(local_id)` in `push_bc_sales_orders.py` EXACTLY, so the EXISTING push→echo machinery picks it up + rekeys the SAME row to `bc:<No>`); new explicit 8th param `bool $allowTwin=false` (force_create bypass), twin-check gated on `!$allowTwin`; idempotency check changed from `source_ref` match to `source_email_id_fk + source='maltytask'`; Step 8 (`parse_status` flip to `'order_created'` — owned by promote, fires in BOTH modes) kept intact. 🔴 `source_email_id_fk` + `review_status='accepted'` PRESERVED on every row (email provenance is ORTHOGONAL to the source tag — survives the `mt:<id>`→`bc:<No>` rekey). Producer had 0 rows → CLEAN retag, no backfill.
> - **`public/modules/email-orders.php` create path:** (a) `bc_customer_no` NULL guard BEFORE promote (refuse "Client sans compte BC — à créer dans BC d'abord"); (b) `force_create` final no-dup gate re-runs `--match-one` on resolved FK+lines before promote (reuses 3a non-blocking-poll fail-closed); (c) build + log BC payload (BOTH modes, `error_log`) + push AFTER promote — `mode='armed'` → real POST (`--apply --i-have-kouros-go`); `mode='off'` (default) → dry-run, NO POST; **the armed flags appear ONLY inside `if ($bcPushMode==='armed')` (Opus-verified L365-366; default 'off' L362)**; (d) Bucket 3 ("Traités") shows 3 states: **"Rapproché BC #N"** (3a reconciled) / **"Créé — BC #N"** (bc_no set) / **"En attente d'envoi BC"** (bc_no NULL, off-mode local row awaiting supervised flush) — BC-reached state read off `ord_orders.bc_no`. 🔴 force_create match-gate = a ~60-line COPY-PASTE of the validate gate — DEBT, refactor to a shared helper later.
> - **`public/css/email-orders.css`:** `.eo-done-item__status-badge--pending-bc` (amber) + `--sent-bc` (green) badges, scoped `body.email-orders-page`.
> - **BC state model:** represented via `ord_orders.bc_no` — NULL = pending, set = sent. `source='maltytask' AND bc_no IS NULL` is the push-spine pickup predicate. `parse_status='order_created'` = "promoted to a LOCAL order" (NOT "pushed to BC"). `push_bc_sales_orders.py` + `ingest_bc_sales_orders.py` were NOT modified (read-only reference).
> - **🔴 OPUS-VERIFIED LIVE BEFORE COMMIT (record as ground truth):** `email_order_bc_push_mode='off'` is_active=1 in DB; armed branch unreachable without the flip; 0 `source='maltytask'` rows + 0 `source_email_id_fk` rows (test residue cleaned — synthetic order 237/email 117 DELETEd); **OFF-mode E2E wrote ONE `status='entered'` row source='maltytask' source_ref='mt:237' bc_no=NULL, FG-depletion query returned 0** (status='entered'≠'shipped'); `bc_customer_no`-NULL guard refuses (customer 2699); independently confirmed FG depletion in `app/fg-stock.php` L481-514/844-880/947/1375 keys on `o.status` + `l.line_status='to_fulfil'`, NEVER `o.source` (the retag-safety proof); `php -l` clean both; md5 local==VPS.
> - **🟢🟢🟢 SUPERVISED ARM-TEST EXECUTED + PASSED 2026-06-21** (one real round-trip via `push_bc_sales_orders.py --make-test-order/--delete-bc` + guarded cleanup). The live BC-write path is **PROVEN end-to-end**; `email_order_bc_push_mode` is **STILL `'off'` (NOT armed)** — the flag-flip remains Kouros's operational decision. AS-RUN ground truth:
>   1. `--make-test-order` minted synthetic local order **id=238** (source='maltytask', customer Nausikraft SA bc_customer_no=1001; line ALT4×2 `to_fulfil` + ALTB×1 rupture).
>   2. **DRY-RUN proved:** BC OAuth2 token acquired (auth+connectivity OK); GET-before-create probe ran READ-ONLY (`Your_Reference`/mt:238 absent → WOULD CREATE); rupture line correctly EXCLUDED; exact OData payload printed.
>   3. **LIVE `--apply --i-have-kouros-go`:** real BC POST created SalesOrder **ORD210160** (line ALT4×2 posted). 🔑 **The PUSH SPINE ITSELF rekeys local 238 IN PLACE post-POST → `bc_no='ORD210160'`, `source_ref='bc:ORD210160'` — it does NOT wait for the */15 reader echo-leg.** (Refines prior model: the */15 echo-leg is the BELT, the inline rekey is the SUSPENDERS — both rekey-in-place, never a 2nd row.)
>   4. **NO-DOUBLE-DEPLETE INVARIANT CONFIRMED:** ran `ingest_bc_sales_orders.py --apply` → echo-leg saw ORD210160 already linked to local 238, rekeyed in place (Committed: 0 INSERTED, 21 updated). Exactly ONE `ord_orders` row referenced the order throughout. `source` stayed `'maltytask'` after rekey (only bc_no+source_ref updated). ⇒ the (A)(i) retag shape holds STRUCTURALLY in live fire.
>   5. **TEARDOWN clean:** `--delete-bc ORD210160` (line+header removed from BC); local rows DELETEd via guarded SQL (`comment LIKE '%SYNTHETIC TEST ORDER%'`); `ord_order_bc_lines`=0 for 238; FINAL SWEEP across ALL `bc_no`/`bc_order_no`/`document_no` cols = 0 refs to ORD210160, 0 `source='maltytask'` rows. Fully clean, no residue.
> - **🔴 STATE NOW: 3b live-write path PROVEN, `email_order_bc_push_mode='off'` (not armed).** REMAINING on arc: **(a)** Kouros flips `off→armed` in Données générales when operationally ready (tech is proven, no code change); **(b)** 7 PDF L1 parsers (gated on `.eml` forwards; Boissons GDS needs the `orders[]` multi-order schema-touch); **(c)** email-connector cron enablement; **(d)** the BC-reader `/tmp` CSV perms bug (below).
> - **🔴 BACKLOG BUG (pre-existing, NOT 3b — separate item).** `ingest_bc_sales_orders.py` CRASHES *after* committing its DB work with `PermissionError: [Errno 13] /tmp/bc-orders-unresolved-skus.csv` — a stale `/tmp` file owned by ANOTHER user that `www-data` cannot overwrite. The reader's upsert/echo work COMMITS FINE *before* the crash (so DB stays correct); only the review-CSV write at **L1462 `_write_review_csv`** dies. The **`*/15` cron likely hits this every run** (silent partial failure — surfaces nothing to the operator). **FIX:** write the CSV to a path `www-data` owns (e.g. under `/var/www/maltytask/data/` or a per-uid tmp dir), or unlink-before-write / chmod. Trigger: "bc reader crash"/"unresolved-skus.csv"/"PermissionError /tmp". EQUIP coder.
>
> **🟢🟢🟢 COBRA L1 PARSER (a.k.a. CobraBodyParser) — LANDED + COMMITTED 2026-06-21 (`e36b762` maltyweb main).** First L1 per-sender body parser. **AS-BUILT:**
> - **`scripts/python/email_parsers/cobra.py` (`CobraBodyParser`)** — parses `g.dutoit@cobratraders.com` plain-text BROKER orders (no attachment, no prices). Registered in `email_parsers/__init__.py` BEFORE `GenericVocabParser`. Registry now = `['CobraBodyParser','BevanarPdfParser','MigrosFroidevilleXlsxParser','GenericVocabParser']`.
> - **Output is HINT-ONLY (same contract as generic_vocab, verified):** `customer_hint` = the END-CUSTOMER string from the SUBJECT ("COOP Pratteln" / "COOP Aclens"), `requested_date`, `lines[]` `{qty, raw, sku_hint}`; the BROKER (Cobra) surfaced in `notes`. 🔑 honors the Q7 BROKER ruling — customer_hint is a STRING, operator resolves the FK at the card (where mig-389 ref_customer_identity COOP↔Cobra bill-to/ship-to ambiguity is settled); parser NEVER auto-resolves broker-vs-end-customer.
> - **Coverage gate (per parser protocol):** every "- La Nébuleuse" bullet must parse or the parser DECLINES (no silent partial / under-report).
> - **VERIFIED LIVE:** email 43 → 4 lines (Embuscade 108 / Stirling 108 / Diversion IPA 54 / Speakeasy 54; customer COOP Pratteln; date 2026-06-24); email 92 → 1 line (Diversion IPA 108; COOP Aclens; 2026-07-24). Both now `parse_status='parsed'` `parser_matched='cobra'`. Corpus now **67 no_match / 40 parsed**.
> - **🔴 POLISH DEBT (record):** `sku_hint` stays the RAW description (not a resolved SKU code) — SAME as generic_vocab; operator resolves at the card. Could add vocab pre-resolution later.
>
> **— Design ruling that drove the build (kept for rationale; the AS-BUILT above is the live state):**
> - 🟢🟢🟢 **SLICE 3b — validation-accept → BC SalesOrder CREATE — DESIGN RE-RULED 2026-06-21 (the original (B′) premise was FALSE; ground-truth verified live).** 🔴🔴 **GROUND-TRUTH CORRECTION (Kouros, verified this session) — the prior (B′) ruling assumed the producer ALREADY writes `source='maltytask'`. IT DOES NOT.** `email_order_promote()` (`app/email-order-promote.php` L250) writes **`source='email'`** (+ord_order_lines L262 +source_email_id_fk +review_status='accepted' +source_ref='email:<msgid>'); `push_bc_sales_orders.py` publishes ONLY `source='maltytask' AND bc_no IS NULL` (L207/384) → it will NEVER pick up a source='email' row. So an operator promoting an email order today creates a real local order BC never receives. The §PRODUCER→D1 (b) hand-off (promote source='email'→'maltytask') was **deferred/NOT built** (this file L89) — (B′) wrongly treated it as the "already-ratified hand-off". STATE: 0 source='email' ord_orders, 0 source_email_id_fk, 0 parse_status='order_created' → producer NEVER run, clean slate, no damage. **THE LOAD-BEARING FINDING still holds: the echo-leg ALREADY EXISTS and does the no-double-deplete round-trip** — `ingest_bc_sales_orders.py` L840-926 parses `mt:<id>`, finds the existing `source='maltytask'` row by PK, and REKEYS THAT SAME ROW IN PLACE (`source_ref 'mt:<id>'→'bc:<No>'`, sets bc_no), NO second row. **⇒ RE-RULING = adopt (A)(i): change `email_order_promote()` to write `source='maltytask'` + `source_ref='mt:<id>'` (the exact format push spine + echo-leg expect) so the EXISTING push→echo machinery picks it up and rekeys the SAME row to bc:<No>.** This makes the (B′) shape actually work: ONE canonical ord_orders row across the whole lifecycle → XOR/no-double-deplete holds STRUCTURALLY. **CONSUMER AUDIT — retag from 'email' to 'maltytask' is SAFE (verified every reader of `ord_orders.source`):** (1) **FG depletion does NOT key on `source` at all** — `app/fg-stock.php` Leg-1 (expédié B2B) keys on `o.status='shipped'` + `l.line_status='to_fulfil'` (L481-514); demand-sim keys on `status NOT IN('shipped','cancelled')` + `line_status='to_fulfil'` (L854); `ord_orders.source` is NEVER in any FG predicate. (2) **Twin/Shopify-XOR reads `inv_sales_orders.source='shopify'`** (a DIFFERENT table, promote L188) — orthogonal to ord_orders.source. (3) **The ONLY consumer of `ord_orders.source IN('web','email','import')` is `load_existing_non_bc_active_orders`→`detect_collisions`** (BC reader L509/754) — and it does NOT branch on the source VALUE; it uses `source` only as a report field (`existing_source`) and keys collisions on customer+SKU-overlap+date. After retag, promoted email orders correctly LEAVE the collision-candidate pool and are handled by the echo-leg PK path (exactly right — they ARE now maltytask-pushed orders, not raw non-BC orders). No other reader filters on the value. **Pure-(B)/(ii) rejected: (B) (no local row, born source='bc') loses the mt:<id> handle + needs a brand-new reader email-correlation path; (ii) (teach the spine to also handle source='email') multiplies the canonical-tag surface for no gain since email-provenance lives in source_email_id_fk anyway.** **(B — EMAIL-PROVENANCE survives the retag) KEEP `source_email_id_fk` (FK→doc_email_messages.id, ON DELETE SET NULL) AND `review_status='accepted'` on the retagged source='maltytask' row — they are ORTHOGONAL to the source tag (email-provenance, not source-routing). The back-link survives the `mt:<id>`→`bc:<No>` rekey (the echo-leg rekeys source_ref only, never touches source_email_id_fk). So "this maltytask order came from email X" is preserved in the column, no need to encode it in source_ref. source_ref carries the BC handle (mt:<id>→bc:<No>); source_email_id_fk carries the email handle — two different keys, both kept.** **(C — 3a FK / Shopify branch interaction) The retag does NOT interact with the 3a `bc_matched_order_id`/'reconciled' state: 'reconciled' is the SUPPRESS leg (already-in-BC, no create); 'order_created' is the CREATE leg (3b). The Shopify-pickup twin-check (promote L172-189) reads `inv_sales_orders source='shopify' fulfilment_mode='pickup'` — it is independent of ord_orders.source, so the XOR (email leg vs shopify leg) STILL HOLDS unchanged after retag.** **(Q1 STATUS MODEL) NO new ENUM value — flip `doc_email_messages.parse_status='order_created'` (existing terminal) + set `bc_matched_order_id`=the new ord_orders.id (reuse mig-423 FK; it's literally "which ord_orders row this email maps to"). reconciled=already-in-BC-suppressed (3a) vs order_created=we-created-it (3b), both carry bc_matched_order_id, distinguished by parse_status.** Traités render: "Créé — en attente d'écho #—" until the FK'd ord_orders row gains bc_no (reader rekeyed) → "Créé — BC #<No>"; live-poll (cheap read). **(Q2 NO-DUP-CREATE INVARIANT = THREE layers, all exist): (1) PRIMARY = re-run `bc_order_match.py --match-one` at the create click on the RESOLVED FK+lines, refuse→divert-to-archive if now matched (catches "someone keyed it into BC since the card opened" — a DIFFERENT bc order, no mt: tag); (2) GET-before-create in BC on `Your_Reference eq 'mt:<id>'` — spine ALREADY does this unconditionally (catches re-push of the SAME maltytask row); (3) `uniq_ord_source_ref` + FOR-UPDATE + refuse-if-terminal in promote. Layers 1&2 are COMPLEMENTARY (different bc order vs same mt row) — keep BOTH, don't collapse.** Fail-CLOSED (subprocess err → refuse, "vérification BC indisponible", never silent-push) reusing 3a's non-blocking-poll plumbing. **(Q3 ARMING = (ii) build fully wired, disarm via a system_settings flag** `email_order_bc_push_mode ∈ off|armed` mirroring `recipe_cr_email_mode`/expeditions confirmation-disarm: `off`(default)→handler runs the FULL match-gate + builds payload + writes the source='maltytask' row but invokes push WITHOUT `--i-have-kouros-go` (dry-run, row sits awaiting supervised flush, render "créée localement, en attente d'envoi BC" — do NOT flip to order_created until the push ACTUALLY fires); `armed`→both flags→live BC POST. Kouros flips off→armed in Données générales, no code change to arm.) **🔴🔴 DISARMED-MODE FG-SAFETY (verified, the worry resolved): in OFF mode the source='maltytask' row IS written locally — and that is SAFE, it does NOT deplete FG before reaching BC. WHY: FG depletion fires on `o.status='shipped'` (fg-stock.php L481-514), and a freshly-promoted order is `status='entered'` → it depletes ZERO FG until it is actually shipped, REGARDLESS of source or BC state. The local maltytask row in OFF mode is a normal pending order (exactly like a web/import order awaiting fulfilment), NOT a phantom — it only enters the BC create lifecycle when armed. A disarmed build that writes the local row therefore does NOT create FG-depleting orders behind Kouros's back. (The only thing OFF withholds is the BC POST + the order_created flip.)) **(Q4 LINE/CUSTOMER RESOLUTION = NO new map — VERIFIED in spine: `BC SalesOrderSalesLines.No ← ref_skus.sku_code` (push L339/348), `Quantity ← qty`, `Sell_to_Customer_No ← ref_customers.bc_customer_no` (L305/317); reader comment confirms "BC item codes ARE the canonical sku_code". sku_code IS the BC item No — no ref_skus→BC indirection to invent.** ONE new never-guess guard 3b needs that 3a didn't: refuse create if the resolved customer's `bc_customer_no` IS NULL ("client sans compte BC — à créer dans BC d'abord"), never push a headerless order. **(Q5 MIG = likely NONE for the core path** — bc_matched_order_id FK + order_created terminal already exist (mig 423/371); ONLY a tiny `system_settings` seed for `email_order_bc_push_mode` (never hardcode a disarm). MIG HEAD 424 applied this session (parallel kpi-recap) → next-free ≥425, RE-`--status` at build-start. **EQUIP: coder + sql + ui + webapp-testing** (smoke in `off` mode E2E: local source='maltytask' row created, match-gate fires, payload logged, ZERO BC write; + fail-closed degraded path; + customer-without-bc_customer_no refusal). 🔴 OPUS VERIFIES BEFORE ARMING: promotion NOW writes source='maltytask' (NOT 'email'/'bc') — this is the CORE retag, grep email_order_promote for the literal; source_email_id_fk + review_status='accepted' STILL set on the retagged row (provenance preserved); match-gate on create path fail-closed; dry-run push prints exact payload + empty GET-before-create; OFF-mode E2E writes a status='entered' local row that depletes NOTHING; behavioural before/after = ONE supervised test order → exactly ONE ord_orders row post round-trip, bc_no set, source_ref='bc:<No>', source_email_id_fk intact, FG depleted ONCE (and only AFTER ship).
> - 🟢🟢 **SLICE 3a — DESIGN RULED 2026-06-21 (PM, 3 decisions). READ-ONLY GATE: writes ONLY `doc_email_messages`; NO `ord_orders`/BC/`inv_*` write — 3b arms the create.** **(D1) PHP↔Python match reuse = SHELL OUT to `bc_order_match.py` (single matcher, NOT a PHP/SQL re-impl).** Overrode the "do it all in SQL/PHP" lean — expressing exact+fuzzy in PHP = the two-matchers DIVERGENCE the "call the canonical accessor, never copy its literal" rule forbids; the fuzzy is revenue/COGS-adjacent (never-guess). Grow `bc_order_match.py --match-one <email_id> --customer-id <id>` (or stdin JSON) returning the SAME match-JSON shape as `--report` (`{matched,bc_order_id,external_document_no,bc_date,bc_amount,confidence,reason}`); **call the venv python by ABSOLUTE path** (php-fpm empty PATH footgun); hard subprocess TIMEOUT; **FAIL-CLOSED degraded path** ("vérification BC indisponible — décision manuelle", NEVER silent no-match→safe-to-create); **match the OPERATOR-RESOLVED state (resolved FK + edited lines), NOT stale `parsed_json`**. **(D2) State model = ADD BOTH: `bc_matched_order_id INT UNSIGNED NULL FK→ord_orders(id) ON DELETE SET NULL` (which BC order) + new terminal `parse_status='reconciled'`** (NOT 'archived'; ENUM now `unparsed/parsed/no_match/error/order_created/reconciled`). Buckets: `order_created`=we created it (3b) / `reconciled`=already in BC, suppressed; both terminal, rendered distinctly ("Rapproché BC #NNN" vs "Créé/Traité"). Archive write = locked (FOR UPDATE) + idempotent (refuse if already terminal) + `log_revision`, mirroring `email_order_promote()`. NO new schema_meta row (ALTER on existing table). **(D3) UX = (ii) SUBMIT-TIME INTERSTITIAL, server-enforced gate** (two buttons "Archiver — déjà dans BC #NNN" / "Créer quand même"; in 3a "Créer quand même" = the existing `order_created` handled-flip, NOT a BC create — 3b swaps it for the real push). The "live/dynamic visibility" rule does NOT move a SAFETY gate client-side; **(i) AJAX preview banner is OPTIONAL sugar layered ON TOP (read-only `?action=bc_match_preview`), never the gate.** **MIG = claim 423 `423_doc_email_bc_matched_reconciled.sql` (422=recipe-CR email-settings, applied; pending=0 at 06-21 — re-`--status` at build-start).** EQUIP coder+sql+ui+webapp-testing (smoke BOTH archive/créer paths AND the fail-closed degraded path). **DIVERGENCE GUARDS:** don't let D1 become a PHP/SQL re-impl; match resolved-state not stale hints; fail CLOSED; stay read-only vs ord_orders/BC (the 3a "Créer quand même" is the handled-flip ONLY). **SHARED-TREE: coder edits already-dirty Slice-2 files (`bc_order_match.py`/`email-orders.php`/`email-orders.css`) AND comm-threads parallel session is dirty on same tree → surgical per-file rsync (NOT bin/deploy.sh), md5 per file, commit by explicit `git add <files>` pathspec, do NOT `git checkout` the dirty Slice-2 files (Circea-revert class). comm-threads = DISTINCT table family (no schema collision); do NOT generalize the order lane into shared raw-headers JSON for them.**
> - ⏸️ **PARALLEL — L1 per-sender templates — Cobra body-parser ✅ COMMITTED, 7 PDF parsers still GATED. CORPUS REALITY + SEQUENCING RULED 2026-06-21 (PM, from the 69 no_match bodies Kouros pulled).** Of 69 no_match: **58 are internal @lanebuleuse.ch reply-threads (NOT order targets — staff discussion), only ~9 external distributors.** Split: **(a) BODY-TEXT ✅ DONE = Cobra Traders** (g.dutoit@cobratraders.com, ids 43+92; full order in body, BROKER for COOP Pratteln/Aclens) — see §COBRA L1 PARSER AS-BUILT below. **(b) PDF-ATTACHMENT (7, order in PDF, body="ci-joint") = Nausikraft(38)/Petite Cave(47)/Blavignac(59)/Amstein(63)/Alloboissons(77)/Boissons GDS(98, TWO pdfs Romanel+Yverdon = TWO orders one email)** — need per-supplier PDF parsers. **(c) NON-ORDERS skip = edu-vd(56 pickup-confirm), Stardrinks/Heineken(91+102 German fwd query).** **SEQUENCING RULING (Q6): Cobra body parser FIRST** (clean, no PDF dep, exercises the broker pattern needed before the PDF batch) → **7 PDF parsers BATCHED** once `.eml` fixtures land. **(Q7 BROKER = customer_hint stays a STRING, operator resolves FK, NEVER auto-resolve broker-vs-end-customer.** Cobra→"COOP Pratteln" parsed to customer_hint string; do NOT resolve to a FK, esp. not Cobra's own account. 🔑 CROSS-REF mig 389 ref_customer_identity: COOP-family is BILLED via Cobra-X sub-accounts (bill-to=Cobra, ship-to=geo store) — the operator faces exactly that bill-to/ship-to ambiguity at the card; parser surfaces BOTH signals as strings (broker from From, end-customer from subject), operator picks the FK which resolves via mig 389. PARSER does NOT apply ref_customer_identity (resolution-layer, operator-confirmed). Nausikraft "1465" = THEIR internal PO code, NOT a customer code → note, never customer_hint. **MULTI-CUSTOMER SINGLE EMAIL (Boissons GDS 2 PDFs) = genuinely TWO orders — emit ONE ParsedOrder PER attachment PDF, never merge; SCHEMA-TOUCH: parsed_json gains a top-level `orders[]` array, review UI renders 2 cards from one email row. 🔴🔴 CORRECTED 2026-06-21 (PM, conflict caught by Boissons-GDS coder + Opus-verified vs live BC spine — SUPERSEDES the earlier `source_ref='email:<msgid>#<idx>'` clause, which was WRONG): each sub-order MUST promote with `source_ref='mt:<ord_orders.id>'` (NOT `email:<msgid>#<idx>`), the SAME format 3b's retag mandates. WHY: `push_bc_sales_orders.py` rekey (L373-393) keys `WHERE id=%s AND source='maltytask' AND bc_no IS NULL AND source_ref='mt:<id>'`; a per-attachment source_ref → rekey matches 0 rows → RuntimeError AFTER the BC POST (orphaned BC order, no local link) → breaks no-double-deplete. Each sub-order already has its OWN PK → its OWN unique `source_ref='mt:<id>'` → uniq_ord_source_ref satisfied AND BC echo works per sub-order naturally. The `#<idx>` discriminator is UNNEEDED for uniqueness. → §MULTI-ORDER (BOISSONS GDS) RATIFICATION below. Land this schema touch WITH the Boissons GDS PDF parser, never bolt on after. Do NOT silently drop the 2nd PDF (under-report class).)** **(Q8 PDF BYTES = AVAILABLE — the attachment slice `c31b540` 2026-06-19 already byte-fetches attachments in BOTH RFC822 + Gmail paths (`_is_attachment_part()` treats get_filename()-truthy as attachment even w/o Content-Disposition). PDF extract = `pdftotext -layout` first (digital, no OCR, /usr/bin/, ZERO new pip dep), fall back to pdftoppm+tesseract raw-subprocess only if ~empty. YES extract the 7 PDFs' text now via a one-off to design the parsers — but capture REAL `.eml` ENVELOPES through the connector (the octet-stream Bevanar lesson: a clean .pdf hides how the part is wrapped). Get Kouros to forward the 7 PDF-bearing `.eml`.)** **(Q9 EQUIP = `parser-coder + sql`** (xlsx only if any turn out XLSX — corpus is all PDF, so no). PROTOCOL UNCHANGED: 100%-coverage-or-refuse, reconcile gate ≤tol line-sum-vs-total (per Bevanar PDF ≤0.10 CHF), no fixture-tests v1 (but capture real .eml as design input), HINT-only `lines[].sku_hint` (NOT a parser-written FK; distinct from invoice `lines[].miId`), vocab stays a LIVE read — L1 parser does LAYOUT/structure, feeds tokens into the 411-term vocab resolver, NEVER hardcodes SKU strings.) GATE: PDF batch blocked on Kouros forwarding a real `.eml` per top sender; **Cobra ✅ DONE (`e36b762`, see §COBRA L1 PARSER AS-BUILT).** 🔴 Boissons GDS PDF parser carries the parsed_json `orders[]` multi-order schema-touch — land it WITH that parser.
> **✅ REP→INTERNAL-ACCOUNT MAPPING — SHIPPED 2026-06-19 (`5fecd85`, mig 418).** See the §AS-BUILT block at the top of this file for the 6 verified mappings, the corrections (Hersberger dropped, Cerruela=2233 added, 3478=BC-not-id), the manual-apply rationale (parallel mig 411 pending), and the raw-header `.endswith` bug that was caught + fixed (flagged 0→21). The earlier held 6-row proposal listing Daniel Hersberger=2400/BC3650 is SUPERSEDED — Hersberger was not a rep.
> **🔴 L1 PER-SENDER TEMPLATES — Cobra ✅ DONE (`e36b762`); 7 PDF parsers GATED** on Kouros forwarding `.eml` per top recurring sender (Nausikraft/Petite Cave/Blavignac/Amstein/Alloboissons/Boissons GDS×2). Roster observed from the live no_match bucket: ~~Cobra Traders ✅~~ **Nausikraft, Tip Top Drinks, FC Renens, Villvert-via-Louis,** plus gmail/hotmail direct customers. **🟢 ENRICHED by Slice 2 (2026-06-21) — 8 of the 11 commandes@ rows are now sender-identified but still `parse_status='no_match'` (generic_vocab can't parse their BODIES) → exact L1-template candidates, now NAMED:** Nausikraft, Amstein, Stardrinks, Petite Cave, Blavignac, Boissons GDS, edu-vd (+ docks/ecublens). The recovered original_sender is visible in the "Non parsé" bucket so the operator can identify them while awaiting the `.eml` feed.
> **🔴 DIVERGENCE GUARDS for the forwarded-envelope work:** (a) customer_hint stays a STRING HINT — extracting the original sender from a forward chain MUST NOT auto-link a `ref_customers` FK; the human picks the FK at validation (revenue-bearing, never-guess). (b) Multi-address forward chains (A fwd B fwd C): pick the DEEPEST original sender (the innermost forwarded-from), NOT an intermediate forwarder, and NOT an internal @lanebuleuse.ch address — but if ambiguous (multiple external candidates, no clear forwarded-header block), emit customer_hint='' (or the raw chain as a note) → review, NEVER guess a customer. (c) The address/postal-code guard must REMOVE phantom lines, not silently RE-BIND them to a different SKU — a 4-digit token that fails the qty-context test drops OUT of the line grammar entirely (it is an address), it does not become a different quantity. (d) HINT-only invariant: the parser NEVER writes a customer_id_fk/sku_id_fk; all resolution is the operator's at the P3 surface (already enforced — keep it).
> **🟢 SCOPE EXPANDED 2026-06-19 (two Kouros decisions, PM-RATIFIED) — ATTACHMENT-ORDER PARSING + INTERNAL-REP→ACCOUNT MAPPING now IN-SCOPE.** The in-flight text-core coder (forwarded-envelope + qty-binding + NPA guard) is UNAFFECTED — these are the NEXT slice after it lands. **RATIFIED NEXT-SLICE ORDER:** text-core (in-flight) → **attachment-order parsing** (gate: connector must FETCH bytes first) ∥ **rep→internal-account mapping** (gate: Kouros's BC account id) → **L1 per-sender templates** (gate: real .eml per sender).
> - **🔴 GROUND-TRUTH CORRECTION — the 10 .eml samples DIVERGE from the brief (PM-verified the actual files at `/home/kluk/projects/maltyweb/data/email-order-samples/`):** (1) **NO PDF attachment exists in any of the 10 samples.** (2) **sample-10 (18 MB) = MIGROS, not Bevanar** — From `dave.pilloud@gmvd.migros.ch`, subj "commande Froideville", attachment = **`Nébuleuse_Froideville.xlsx` (13.5 MB XLSX)** + an inline signature PNG (122 KB); the 18 MB is base64 bloat of those, not a PDF. (3) **Bevanar "Commande No 32503" = sample-04/05, BODY/THREAD order with NO attachment** (From `ahauray@bevanar.ch` ↔ production@). So the attachment-order surface Kouros means is **XLSX-first (Migros), and PDF is anticipated-but-unsampled.** Build the attachment dispatcher to handle BOTH content-types from day one; the FIRST real attachment parser to write is the Migros XLSX (we HAVE the sample), PDF parser waits on a real PDF sample. (4) Internal-rep samples are samples-01/03/06/07 **Dorian Fairhall**, 08 **Megan Iannone**, 02 **Kouros** — all @lanebuleuse.ch "commande échantillon vendeur" / "client à créer".
> - **🔴 OCR/ATTACHMENT-FETCH LANDSCAPE (PM-verified live on VPS):** the connector's `.venv` has **NO Python PDF/OCR lib** (only `openpyxl==3.1.5`); system binaries **`pdftotext`/`pdftoppm`/`tesseract` ARE present** at `/usr/bin/`. The maltytask Node invoice OCR (`lib/ocr-core.js`) is just `execSync` shell-outs to `pdftoppm`+`tesseract` — **NOT reusable code (Node, wrong repo), but the TECHNIQUE ports trivially.** **CONNECTOR GAP (blocker): `ingest_email_orders.py` currently captures attachment METADATA ONLY** (`{filename, content_type, size_bytes:None, content:None}` in BOTH the RFC822 and Gmail-API walk paths) — it does NOT download attachment bytes. So step 0 of the attachment slice = teach the connector to FETCH bytes (Gmail API `messages.attachments.get` / RFC822 `part.get_payload(decode=True)`). **DECISIONS:** XLSX → `openpyxl` (already installed, ZERO new dep). PDF → `pdftotext` FIRST (digital PDFs have a text layer — most B2B order PDFs are digital, cheap, no OCR); fall back to `pdftoppm`+`tesseract` ONLY if `pdftotext` yields ~empty (scanned PDF). That fallback adds NO Python dep (shell-out), but does need `pdf2image`/`pytesseract` OR raw `subprocess` — PM lean = **raw `subprocess` shell-out (no new pip dep), mirroring `ocr-core.js`'s execSync pattern.** HOOK POINT: a new `email_parsers/` module per content-type (`attachment_xlsx.py`, `attachment_pdf.py`) fed by an attachment-extraction helper in the connector; same `parse()→ParsedOrder|None` contract, registered in the registry. KEEP Model B: attachment parse → `parsed_json` HINTS only, NO ord_orders, NO FK, NO LLM. EQUIP: **parser-coder + sql** (+ `xlsx` skill for the Migros XLSX shape).
> - **🔴 INTERNAL-REP → INTERNAL/SAMPLES ACCOUNT (decision 2) — modeled as a RULE at the resolver, NOT a parser guess.** When From is `@lanebuleuse.ch` AND no external client block resolves (rep places a sample/own order), route customer_hint to a **designated internal/samples BC account** — NOT ''→review, NOT the rep-as-customer. **MAPPING HOME = a small ref table `ref_internal_order_accounts` (sender_email VARCHAR UNIQUE → customer_id_fk INT→ref_customers.id, is_active, notes)** — NOT system_settings (FK integrity + per-rep granularity), NOT a users column (reps aren't all `users`). schema_meta class=reference/allowed. **RESOLUTION LAYER, not parse layer:** parser only SIGNALS "internal-rep order" via a flag in parsed_json (`_internal_rep: true` + `rep_email`) computed from From∈@lanebuleuse.ch AND no external-sender/forwarded-customer block; the PHP validation resolver reads the flag, looks up `ref_internal_order_accounts` by rep_email, and PRE-FILLS the customer picker with the mapped account (operator can still override). customer_hint stays a STRING at parse time → FK at validation, exactly as Model B mandates. **🔴 CRITICAL DIVERGENCE GUARD — the internal account must NOT become a dumping ground:** the rule fires ONLY when From is internal AND NO external customer was named (not forwarded-on-behalf-of an external client). A `@lanebuleuse.ch` From that IS a forward carrying a real external customer (the common forwarded-order case from the text-core slice) MUST resolve to the FORWARDED customer, never the internal account — forwarded-envelope detection (step 1 of the text-core slice) takes PRECEDENCE over the internal-rep rule. **DATA HAZARD (PM-verified live):** `ref_customers` already has a SPRAWL of fuzzy per-rep échantillon rows (4 near-dup "Dorian … échantillons" accounts: ids 2997/2899/2828/3017, plus John/Louis/Nico/Tania variants) — the mapping MUST point to the ONE canonical account Kouros designates, never auto-pick a fuzzy-named échantillon row. `ref_customers.sale_class` ENUM ALREADY carries `'sample'` + `'staff'` — the designated account should be `sale_class='sample'`. **DEL- price prefix / rabais -15% on these orders = OPERATOR-SET at validation, NOT a parser/BC-create field in v1** — ord_orders has no price-tier/discount column and v1 doesn't push price to BC (D1 pushes qty/SKU; pricing is BC-side off the customer's price group). Surface the rep's stated discount as a parsed_json NOTE; operator handles the tier. **Kouros still owes: the actual BC account identifier (`bc_customer_no` / ref_customers.id) for the internal/samples account.** EQUIP: **sql + coder** (+ ui for the resolver pre-fill on email-orders.php). MIG HEAD ≥408 — re-`--status` at build start; `ref_internal_order_accounts` = next-free mig.
>
> **🟢🟢🟢 PHASE 1 L1 PDF PARSERS LANDED 2026-06-21 (`fe8266c`, deployed+verified vs real fixtures).** 4 single-order PDF parsers: **Nausikraft / Petite Cave / Amstein / Alloboissons** (id 38/47/63/77). Verified vs real fixtures (exact qty arrays; Alloboissons reconcile gate passes line-sum-vs-total). HINT-only contract held (`lines[].sku_hint` raw, operator resolves FK). **🔴 BLAVIGNAC = DOCUMENTED PERMANENT DECLINE (NOT a gap):** id 59 is a SCANNED IMAGE PDF + HANDWRITTEN quantities → pdftotext/OCR yields garbage; stays `parse_status='no_match'`, operator keys it manually. Do NOT count Blavignac as an open L1 parser; do NOT attempt an OCR parser for it (handwriting defeats tesseract). Remaining L1 = ONLY Boissons GDS (the multi-order, below).
> **🟢🟢🟢 MULTI-ORDER (BOISSONS GDS, id 98) — PHASE 2 RATIFIED 2026-06-21 (PM, after Boissons-GDS coder caught the source_ref conflict + Opus re-verified the BC spine live). DECISIONS:** Boissons GDS = ONE email, TWO PDFs (Romanel dépôt #69514, Yverdon dépôt #69516) = genuinely TWO physical orders to two depots. Must emit one ParsedOrder per PDF, promote as two distinct ord_orders rows.
> - **(Q1) RATIFIED = OPTION A — atomic-all.** The review card renders N sub-order cards (operator resolves customer FK + SKU FKs per card); ONE "Valider les N commandes" button promotes ALL sub-orders in a SINGLE transaction. Existing idempotency `source_email_id_fk + source='maltytask'` works UNCHANGED (all-or-nothing — either 0 or N rows exist for the email, never a partial). Email → `parse_status='order_created'` ONCE, after all N succeed. `source_ref='mt:<id>'` per row (each PK its own). **NO new column, NO migration, minimal new surface on the revenue path.** Option B (per-sub-order Valider + `source_sub_index` discriminator + conditional status-flip) REJECTED: it multiplies moving parts (migration + idempotency rewrite + conditional flip) on the COGS/inventory-bearing revenue path for a 2-depot same-supplier case the atomic button handles cleanly. Accepted downside: operator resolves all sub-orders before promoting any.
> - **(Q1 / source_ref) `source_ref='mt:<id>'` is MANDATORY per sub-order** — see the CORRECTED multi-order clause in §L1 ruling above. The per-attachment `email:<msgid>#<idx>` idea is INCOMPATIBLE with the live BC push→echo spine (rekey keys on `source_ref='mt:<id>'`). Each sub-order's own PK gives a naturally-unique `mt:<id>`; no `#idx` discriminator needed.
> - **(Q2) parsed_json multi shape RATIFIED:** keep the single-order shape UNCHANGED (backward-compat for the 5 existing parsers + cobra + generic_vocab — `_serialise_parsed_order` L505/L530 `_kind:'parsed_order_hints'` stays). For multi, emit `{_kind:'parsed_order_hints_multi', _schema_version:1, orders:[ {customer_hint, requested_date, notes, lines:[...]}, ... ]}`. Connector serialize + `email-orders.php` render BRANCH on `_kind`. The `orders[]` sub-objects reuse the EXACT single-order field shape (customer_hint/requested_date/notes/lines[]) so the card renderer is one shared partial driven over a 1-or-N list. Extra-hints (`_internal_rep` etc.) stay top-level on the single shape; not expected on multi (Boissons GDS is external, attachment-sourced).
> - **(Q3) multi-ParsedOrder return RATIFIED = `list[ParsedOrder]`, NO MultiParsedOrder wrapper.** `dispatch()` (`email_parsers/__init__.py` L88, currently returns `ParsedOrder | None`) detects a `list[ParsedOrder]` return and the CONNECTOR serializes it as the multi shape (new `_serialise_parsed_orders_multi` sibling, or `_serialise_parsed_order` gains a list branch). `base.py` ParsedOrder dataclass STAYS UNCHANGED (frozen/slots). The Boissons GDS parser returns `list[ParsedOrder]` (one per PDF). Rationale: a dedicated wrapper class adds a type to thread through dispatch + every caller for zero gain; a list is the minimal, type-checker-friendly signal and keeps the frozen ParsedOrder contract intact. Update dispatch's signature/docstring to `ParsedOrder | list[ParsedOrder] | None`; the decline-fall-through protocol is unchanged (None still = declined). 🔴 Connector must handle: empty list = treat as decline (no_match), 1-element list = MAY serialize as single shape OR multi (PM lean: 1-element from a list-returning parser still serializes multi for consistency, but single is acceptable — agent's call, document it).
> - **(Q4) EQUIP RATIFIED = `parser-coder + sql + ui + webapp-testing`.** parser-coder (Boissons GDS PDF parser + dispatch list-handling + connector serialize), sql (none expected — Option A needs NO migration; sql on standby only if a constraint surprise appears), ui (email-orders.php multi-card render branch + the "Valider les N commandes" button), webapp-testing (smoke the 2-card render + the atomic promote E2E). 🔴 OPUS INDEPENDENTLY VERIFIES (revenue/inventory-bearing): atomic promote writes EXACTLY 2 ord_orders rows source='maltytask' source_ref='mt:<id1>'/'mt:<id2>' both status='entered' (→ ZERO FG depletion until shipped), email flips order_created ONCE, idempotent re-submit refused (2nd call finds the rows, refuses), and — if `email_order_bc_push_mode='armed'` later — each sub-order pushes as its OWN BC SalesOrder + rekeys its OWN row (no cross-contamination). Smoke in `off` mode (no BC write).
> - **(Q5) MIGRATION = NONE for Option A.** (Option B would have needed one; rejected.) For the record, live HEAD = **429 applied** (`429_comm_entity_registry`; 0 pending as of 2026-06-21), next-free = **430** — re-`migrate.php --status` at build-start regardless (parallel sessions lead the number).
> - **DIVERGENCE GUARDS:** (a) NEVER silently drop the 2nd PDF (under-report); both PDFs parse-or-the-whole-email-declines per the coverage-or-refuse protocol (a parser that reads PDF1 but not PDF2 must NOT emit a 1-order result — it declines, → no_match, operator handles). (b) customer_hint per sub-order stays a STRING → operator resolves each depot's FK at its card, NEVER guessed (Romanel vs Yverdon are distinct ship-to). (c) Boissons GDS is a DISTRIBUTOR-with-depots, not a broker — both PDFs bill to the same Boissons GDS account but ship to different depots; the operator may resolve both to the same customer FK (depot = ship-to detail, not a separate ref_customers row) — surface the depot # in notes, do NOT invent per-depot customer rows. (d) reconcile gate per PDF (line-sum vs stated total ≤ tol) — each PDF gated independently.
>
> **🔑 BC GATE IS OPEN, not blocked.** The active-arc index line "BC API auto-pull gated on Entra admin-consent" is STALE for the WRITE path: Thierry granted Entra admin-consent (RE1, secret on VPS `config/bc.env`), and the D1 order-create + D2 credit-memo write spines are LIVE-PROVEN under `D365 BUS FULL ACCESS` (no fresh consent needed). "Prepare on BC" therefore does NOT block on Entra — it blocks on (a) the live mailbox (P0 — NOW DONE) and (b) the human-validation gate (built). See PRODUCER→D1 HAND-OFF below.

