# Email-order ingestion arc (`commandes@lanebuleuse.ch` → ord_orders → BC) — split index

a.k.a. "COMMANDES READERS". Read when touching: `commandes@` mailbox ingestion, per-sender email-order parsers, `doc_email_messages`/email-order producer, the Phase-1 BC-orders link from email, the logistics mail↔BC validation surface, "commandes readers"/"lire les commandes"/"auto-read orders"/"prepare on BC". Companions: [../fulfilment-expeditions-arc/README.md](../fulfilment-expeditions-arc/README.md) (D1/D2 BC spine, ord_orders schema, fg_stock XOR), [../sales-commercial-surface/README.md](../sales-commercial-surface/README.md). Grew past 80KB; split during the 2026-06-21 compaction. Parent index: [../maltyweb-pm-memory.md](../../maltyweb-pm-memory.md).

- [active-vision-and-slices.md](active-vision-and-slices.md) — **the live cold-resume state**: §CARDINAL VISION (auto-fill-into-BC, dedup gate), the confirmed 3-step BUILD SEQUENCE, §REP→INTERNAL-ACCOUNT SLICE (mig 418, live), §SLICE 2 via-Commandes original-sender recovery (mig 421, deployed), §SLICE 3a review-card BC dedup gate (mig 423, `d592c7e`), §SLICE 3b design ruling, the OCR/attachment-fetch landscape, the internal-rep resolver rule, the BC-gate-is-open note. **Read this first on any email-orders resume.**
- [p1-p3-as-built.md](p1-p3-as-built.md) — §P3 validation UI as-built (mig 372, `172fd95`/`6ef9df0`, ref_pages id=480) + §P1 layer-2 generic parser + Model B wiring (mig 371, `0a1f267`).
- [model-parser-plan-phasing.md](model-parser-plan-phasing.md) — ratified CANONICAL PRODUCER MODEL = Model B (validation-first, no premature FK); the two reconciliations; ground-truth facts; the THREE-LAYER deterministic parser stack (no LLM); phasing; PRODUCER→D1 hand-off; divergence flags.
