# Financier — Fiche COGS (variation de stock) build

> Read when a task touches the Financier page (`public/modules/financier.php`), the Fiche COGS / variation-de-stock tab, the `cogs_fiche_*` / `ref_cogs_fiche_categories` tables, or the monthly-compile engine `scripts/cogs-monthly-compile.ts` (maltytask repo). Verified live 2026-06-11.

## 🟢 ARC ~CLOSED (CUTOVER SHIPPED + OPUS-VERIFIED 2026-06-16) — DYNAMIC-FINANCIER REBUILD + `cogs_fiche_sealed` (Kouros-APPROVED)
**The dynamic-financier rebuild is SHIPPED, verified, LIVE on the VPS. NOT pushed (held on maltyweb main — two-session-merge / hold-for-review discipline; flag if it should push).** The live financier path now renders via in-app PHP compute through the resolver; published-month immutability is STRUCTURAL via append-only `cogs_fiche_sealed`. The keystone "May close shipped" record below holds as the published baseline; governance: **May DISPLAYS live 355 331.86, NOT cutover-sealed at old 353 867.69 — Thierry (CFO) seals on validation.**

### ✅ CUTOVER COMMIT — `646641e` (maltyweb) "feat(cogs): wire resolver + seal/restate to Fiche COGS financier surface" — 12 files
- **`app/cogs-fiche-resolve.php`** — canonical accessor (precedence sealed > seed > live > unavailable).
- **`app/cogs-fiche-compute.php`** — opening chain now SEAL-AWARE: prior-month closing resolved **sealed > live-cache > seed**.
- **`public/modules/financier.php`** — resolver wired @L421; **dead direct `cogs_fiche_monthly` Fiche JOIN REMOVED per RULE 1** (only the month-selector discovery query still references the table → minor follow-up #1 below).
- **`public/js/financier.js`** — sealed-provenance branch + seal/restate modal.
- **`public/css/financier.css`** — `.fin-fiche-provenance-chip--sealed` (ember/gold variant).
- **`public/api/cogs-fiche-csv.php` + `cogs-comprehensive-csv.php`** — both route through resolver + stamp a Provenance header.
- **`public/api/cogs-fiche-seal.php`** (NEW POST handler) — CSRF + gate + `cogs_fiche_seal_month` + `log_revision` + PRG.
- **Migs 374/375/378/379** (see below).

### ✅ MIGS — all tracked + applied on VPS
- **374** `cogs_fiche_monthly.source_fingerprint` (fingerprint cache). **375** `cogs_fiche_sealed` append-only seal/restate store (schema_meta: reference / allowed-append-only; one seal = 12 rows; `supersedes_seal_id` FK chains restatements).
- **378** `finance_viewer` preset. **379** added `'finance'` to `users.manager_scope` ENUM → **now `enum('production','logistics','all','finance')`**.
- **Migration numbering note:** coder used 378/379; **373/376/377 belong to the parallel returns+shopify-variants session** (committed by them in `e7e0fca`). No collision.

### ✅ SEAL/RESTATE GATE — LIVE (CFO cross-dependency RESOLVED)
- Gate = **`is_admin() OR manager_can('finance')`** (the `'finance'` scope is now real, via mig 379). This RESOLVES the open Step-5 cross-dependency that flagged scope=NULL CFO would fail the gate.
- **To enable Thierry to seal/restate:** `UPDATE users SET role='manager', manager_scope='finance' WHERE id=<thierry>` (or Données générales → Utilisateurs → Manager / Finance). Admin already passes via `is_admin()`.

### ✅ OPUS VERIFICATION (all green, 2026-06-16)
- **May parity post-cutover:** provenance `live`, total **355 331.86**, Hops RM naive **29 668.66** (loadRM-inversion tripwire HELD), opening **394 367.74**, variation **−39 035.88**, basis **+1 808.82**. April → seed; future → unavailable.
- `cogs_fiche_sealed` = **0 rows** (coder spot-check self-cleaned, FK-aware).
- All **8 deployed files md5-match local.** Commit clean — parallel-session files (auth.php, charges-bc, expeditions-line-status) untouched.

### 🟡 MINOR NON-BLOCKING FOLLOW-UPS (recorded, not actioned)
1. **month-selector discovers months via `cogs_fiche_monthly`** — a never-resolved closeable month won't list until first resolve (the only remaining `cogs_fiche_monthly` reference on the page).
2. **cross-month opening staleness** only for an UNSEALED prior feeding an open next month — **sealing the prior eliminates it** (this is exactly what the seal mechanism is for).

### TS COMPILER — DEPRECATED as live path
`scripts/cogs-monthly-compile.ts` (maltytask) is now the **parity oracle ONLY** — no longer the production compute path (that's `app/cogs-fiche-compute.php` in-app PHP).

---

### (historical, pre-cutover) DATA LAYER record — superseded by the shipped commit above
The pre-cutover record below is retained for the build trail; the resolver `app/cogs-fiche-resolve.php` and `app/cogs-fiche-compute.php` are now committed in `646641e`.
- **mig 374 `374_cogs_fiche_fingerprint.sql`** (APPLIED) — `cogs_fiche_monthly.source_fingerprint VARCHAR(64)` (SHA1 over RM/FG/WIP source counts + MAX(updated_at); NULL = stale → recompute).
- **mig 375 `375_cogs_fiche_sealed.sql`** (APPLIED) — append-only seal/restatement store. **One seal event = 12 rows** (one per `COGS_FICHE_CATEGORIES`). Active seal = latest event per month_key. `supersedes_seal_id` FK chains restatements (ON DELETE RESTRICT). schema_meta row written: **class=reference, corrections_policy=allowed/append-only**. (NB mig 376 `376_returns_bc_derived_origin.sql` is a PARALLEL-session ord_returns mig — NOT this arc. MIG HEAD now ≥376; re-`--status` at build start.)
- **`app/cogs-fiche-resolve.php`** (deployed, NOT yet committed) — the public surface, precedence **sealed > seed > live > unavailable**:
  - `cogs_fiche_resolve_month(PDO,$month):array` → `{provenance, sealed_at, sealed_by, categories{12}, totals}`. Live months fingerprint-cache-backed via `cogs_fiche_monthly`; **sealed months are NEVER recomputed (frozen)** — the structural immutability guard.
  - `cogs_fiche_seal_month(PDO,$month,$sealedBy,$note):int` — freezes current live → `cogs_fiche_sealed`; restatement if already sealed (appends, supersedes prior). Guards: refuses to seal the April seed anchor; refuses non-closeable months; transactional.
  - `cogs_fiche_source_fingerprint(PDO,$month):string`.
  - `_cfs_recompute_and_cache` is the live-compute+cache path (delegates to `app/cogs-fiche-compute.php`).
- **Opus-verified results (all PASS):** 2026-04 → seed **394 367.74**; **2026-05 → live 355 331.86** (the CORRECTED post-tank-fix value), Hops RM naive canary **29 668.66** (the loadRM-inversion tripwire held); fingerprint cache hit stable; seal + restate exercised; self-cleaning teardown left `cogs_fiche_sealed` empty.
- **🔴 GOVERNANCE DECISION (Kouros, locked 2026-06-16): May DISPLAYS the corrected live 355 332, it is NOT cutover-sealed at the old stored 353 868.** At cutover May is just a live closeable month; **sealing is a forward operator action** — Thierry (CFO) validates/seals later via the UI. So the three-way render is: April=seed / May=live-computed / (nothing sealed yet).
  - This SUPERSEDES the earlier in-flight canaries in this file (variation −39 035.88 / WIP 23 946.93) — the verified live May total is now **355 331.86**; the prior 351 884.58 / 353 867.69 records are the SUPERSEDED published values, retained below as historical baseline. The seal table is exactly what makes the new live value safe to display without re-valuing a signed month.
- **3 resolve.php polish fixes folded into the cutover coder brief:** (#1) hoist `PDO::prepare` above the foreach in `_cfs_recompute_and_cache` (12 prepares→1); (#2) `supersedes_seal_id` must point at the prior event's **HEAD** row id (currently stores the TAIL row id — restatement-chain bug); (#4) `mb_substr($sealedBy,0,128)` guard before INSERT.

### KOUROS DECISION (locked 2026-06-16)
- **APPROVED `cogs_fiche_sealed` (freeze signed months against drift) — REJECTED pure-live-+-CSV-only.** Rationale: a signed month must NOT silently re-value when a back-period source moves (the exact +1 464 / −669 May drift we hit; same class as the tank-sim WIP-restatement hazard, §OPEN CFO ITEM). Honor-system never-recompute is a CONVENTION, not a guard — this makes it a guard.
- **Render model (three-way):** SEALED month → frozen `cogs_fiche_sealed` values + chip **"Clôturé (signé) — figé le {date}"**; UNSEALED closeable month → **live compute** + chip **"Calculé (live)"**; April → immutable `cogs_fiche_seed` anchor (unchanged).
- **"Sceller" action (CFO/admin)** = freezes the current live compute into `cogs_fiche_sealed`. **"Restater"** = explicit dated re-seal → NEW row with `supersedes_id` → old row RETAINED (append-only audit of what was signed when + the delta). Drift becomes a DELIBERATE, VISIBLE restatement, never silent.

### BUILD STATE (2026-06-16)
- **Step 1 IN PROGRESS (Sonnet coder running):** compute port → `app/cogs-fiche-compute.php` (`cogs_fiche_compute_month()`) + `fin_closeable_months()` in `app/finance-period.php`; **byte-for-byte port of `scripts/cogs-monthly-compile.ts`** (preserve the naive RM formula — §🔴 INVERSION — and the `count_type='month_end'` + ROW_NUMBER latest-per-(sku,loc) FG reads). Parity harness vs the TS oracle on the VPS (`~/maltytask-run/run-fiche.sh`). **GATE: parity = 0.** Opus verifies CANARIES before ANY UI wiring: **RM Hops 29 668.66** (NOT 29.67 — the loadRM inversion tell), **WIP 23 946.93**, **variation −39 035.88**, **basis +1 808.82**.
  - 🔴 **NUMERIC DRIFT TO RECONCILE (PM flag for Opus):** the decision's canaries (variation −39 035.88, WIP 23 946.93) DIFFER from BOTH prior published-May records in this file — headline §STATUS (WIP 21 814.28 / variation −42 483.16 / total 351 884.58) AND the discriminator/C2 record (total 353 867.69 / variation −40 500.05, basis +1 808.82). Three different May totals now in play. This is almost certainly the tank-sim WIP-restatement (§OPEN CFO ITEM: BBT 766→560 hl for 2026-05) flowing into a freshly-recomputed live May. **Do NOT silently adopt any one number — Opus must establish which is the TS-oracle's CURRENT output (parity target) vs the SEALED/published value, and surface the delta as the deliberate restatement the seal mechanism is FOR.** This drift is the live proof of why the seal table was approved.
- **Remaining steps (gated on Step-1 parity=0):** (2) mig — fingerprint cache on the repurposed `cogs_fiche_monthly` → (3) wire lazy cache → (4) repoint `financier.php` + BOTH CSV endpoints (`cogs-fiche-csv.php` + `cogs-comprehensive-csv.php`) to `cogs_fiche_compute_month()` → (5) **mig `cogs_fiche_sealed` + seal/restate actions + sealed-vs-live render** → (6) deprecate `cogs-monthly-compile.ts` as the LIVE path (KEEP as the parity oracle). **MIG HEAD = 373, next free = 374** (verified live `migrate.php --status` 2026-06-16, 0 pending; re-check at each build start — parallel sessions live; 373 = ord_returns from a parallel session).
- **EQUIP:** sql + coder + ui + webapp-testing. Step 5 (seal/restate) is COGS-bearing identity/immutability → Opus-verify independently; sealed-render is the sensitive class.
- 🔴 **CFO SEAL-WRITE GATE — CROSS-DEPENDENCY FROM THE finance_viewer ARC (recorded 2026-06-16, MUST resolve at Step 5):** the in-flight financier rework already added `$canSealFiche = is_admin() || manager_can('finance')`. **But there is NO 'finance' scope/domain wired into `manager_can` yet**, and the CFO (Thierry, user 22) was shipped 2026-06-16 with `manager_scope=NULL` (pure read-only, preset `finance_viewer` id=10). So as written, the scope=NULL CFO will NOT pass `manager_can('finance')` → his **Sceller/Restater buttons stay hidden**. When Step 5 goes live, gate the seal/restate WRITE so the CFO passes: either (a) a preset-membership check (`access_preset_id_fk = 10`/finance_viewer can seal), or (b) wire a real `'finance'` scope/domain into `manager_can` and give the CFO that capability WITHOUT granting general write (he must stay read-only everywhere else). Pick deliberately — admin already passes via `is_admin()`; the only question is the CFO path. → cross-ref user-auth-admin-arc.md §CFO FINANCE_VIEWER PRESET.

### Divergence flags PM is watching on this rebuild
- **No parallel store:** `cogs_fiche_sealed` must be the ONE home for frozen signed months; `cogs_fiche_monthly` is being repurposed to a fingerprint CACHE (not a second canonical close store). Seed (April) ∪ sealed (signed) ∪ live-compute (open) — three roles, no overlap. The opening resolver must read the SEALED value for a signed prior month, never re-compute it.
- **Refuse, don't NULL / never guess:** the PHP port must reproduce the engine's FG-missing guard (no fabricated zero close) and the immutability behavior; restate creates a row, never mutates.
- **Naive RM formula is load-bearing** (§🔴 INVERSION) — the port must NOT "fix" it to loadRM. The Hops 29 668.66 canary is the regression tripwire.

### Adjacent fixes same session (2026-06-16, recorded for arc continuity — NOT financier-fiche scope)
- **BC collision guard hole FIXED + on origin `641de66`** (BC/fulfilment arc): `ingest_bc_sales_orders.py` now folds shipped web/email/import orders into the collision candidate set with a two-tier match (active: customer+SKU; shipped: customer+SKU+requested_date) — closes the same-day-manual-ship-then-BC-pull duplicate hole. Phantom dup order 147 (BC ORD210073 twin of shipped manual order 134, Pétanque Renanaise) cancelled; cross-lane dup scan = 0. (Belongs to fulfilment-expeditions-arc — cross-noted here only because it shipped alongside the rebuild kickoff.)
- **Shared-clone hygiene:** a coder commit briefly bundled 3 other-session files (373_ord_returns.sql, expeditions.css/php); isolated cleanly into single-file `641de66`; the 3 restored to uncommitted (content preserved) for their owning session. Live proof of the COMMIT-WITH-A-PATHSPEC checklist rule.

---

## ✅ STATUS HEADLINE (2026-06-12 — MAY CLOSE FULLY FINALIZED: SHIPPED + RATIFIED + VERIFIED + COMMITTED + PUSHED)
**The financier-fiche arc keystone is CLOSED.** May 2026 COGS-fiche close is done end-to-end. **Engine COMMITTED + PUSHED — `423a660 feat(cogs): finalize May-2026 close — RM unit-consistency diagnostic + basis-change line` is HEAD on maltytask `main`; `origin/main..HEAD` empty; working tree clean.** (Correction to a prior brief that said the engine was uncommitted at base `fcad639` — a concurrent session committed + pushed it; the deferred commit is DONE, nothing left to commit.) The `cogs_fiche_monthly` May rows (written 06:45 2026-06-12) are **RATIFIED BY KOUROS and INDEPENDENTLY VERIFIED by Opus** (gate), all checks PASS.
- **May headline (DB, published, verified):** RM **282 368.74** · WIP **21 814.28** · FG **47 701.56** · TOTAL **351 884.58** · OPENING **394 367.74** · VARIATION **−42 483.16** · BASIS_ADJ **+1 808.82**. 12 rows. (rm+wip+fg=total ✓; total−opening=variation ✓.)
- **Opus verification record (all PASS):** (1) 12 rows, rm+wip+fg=351 884.58=total ✓; (2) total−opening=−42 483.16=variation ✓; (3) **April-seed continuity EXACT — April seed SUM(total_chf)=394 367.74 == May opening ✓ (the key fiscal invariant)**; (4) Yeast RM=1 496.03, g→kg hardening held (engine diagnostics label naive=CORRECT / conv=WRONG; 27 unit-mismatch items SURFACED as diagnostics, NOT applied) ✓; (5) **engine dry-run reproduces the published DB rows TO THE CENT → HEAD engine `423a660` == what's published, no drift** ✓; (6) basis adjustment +1 808.82 = FG/F2 restatement ONLY (3 pure-RM categories correctly carry 0) ✓.
- **Noted to Kouros (NOT a blocker):** engine May valuation diverges from legacy EXT (RM ≈ −47k vs EXT) — EXPECTED; engine supersedes EXT. Correctness rests on the April-seed tie-out + internal identities, NOT on EXT match.
- **basis_adjustment is COMPUTED BY THE ENGINE, not an operator input:** `computeBasisAdjustment()` revalues April FG at CURRENT ref_sku_bom cost − seeded April fg_chf; FG/F2 portion only, RM/yeast = "non isolable — base héritée". The +1 808.82 is an OUTPUT; no CLI flag / config row for it.
- **g→kg/loadRM hardening is DONE in-engine:** RM flows through `loadRmStock` (lib/rm-stock-mysql.js); formula = `finalQty × costChf` (NO conversionFactor — finalQty already in pricing unit); SANITY GUARD throws if Yeast RM ≥ 50 000 (catches conversionFactor creep); 27 unit-mismatch items = diagnostics only.
- **🔴 INVERSION — DO NOT "harden via `loadRM`". The naive formula IS the correct one. (Operator-confirmed + Opus-verified at HEAD `423a660`, 2026-06-12.)** Two DIFFERENT functions, do not conflate: (1) `loadRmStock` (lib/rm-stock-mysql.js) = the engine's ROW LOADER — pulls `final_qty`/`cost_chf`/`conversion_factor` from `inv_rm_stocktake`×`ref_mi`; the engine uses this and is CORRECT. (2) `loadRM` (lib/inventory.js) = the legacy COGS/COP inventory loader that APPLIES `conversionFactor` — **WRONG for this data: it 1000×-UNDER-values g-input/kg-priced items** (per-category recon proof: Hops `loadRM`=29.67 vs correct **29 668.66**; same shape for yeast + process aids). **`inv_rm_stocktake.final_qty` is stored in the PRICING unit (kg), so naive `final_qty × cost_chf` is right; multiplying by conversionFactor (0.001) is the bug.** The April yeast 565k blowup was a ONE-OFF g→kg `pricing_unit` METADATA DRIFT on a CLOSED period (which we SEED, never recompute) — NOT a formula bug. The follow-up "harden RM before signed close" is **RESOLVED as "naive is correct; permanent unit-consistency diagnostic added (surfaces any conversionFactor≠1 item, does NOT halt — the 27 flagged May items are loadRM-metadata-stale, not stocktake-mismatched)"** — NOT "switch to loadRM". Engine keeps naive + prints the diagnostic; the `convValue` (cogs-monthly-compile.ts:263) is computed ONLY as a WRONG-for-diagnostics what-if, never used in totals.
- **B4/B5 financier UI + Saisies PF card = COMMITTED** maltyweb `ad9c14b`; **+ banner CSS fix COMMITTED maltyweb `70efa0e`.** Engine finalize = maltytask `423a660`.
- **Smoke 25/26 on app.maltytask.ch (full-page, post-May-publish):** May renders, "Calculé" provenance chip, the **"Ajustement de base (WAC + révision BOM F2, non récurrent)"** line renders per-category + in CSV, NULL-basis cats (Levures/Inliner/Verres) render BLANK not 0.00, CSV ties to 351 884.58. (Earlier B4/B5 smoke was 13/14 on April-seed-only.)
- **TWO CSS banner bugs found+fixed during smoke (both deployed; the 2nd is `70efa0e`):** (1) earlier — false incomplete-banner gated on provenance (now `provenance ≠ seed`); (2) `.fin-fiche-warn{display:flex}` OVERRODE `[hidden]` so the banner showed on COMPLETE months → added `.fin-fiche-warn[hidden]{display:none}`. (Standing CSS lesson: a `display:` rule beats `[hidden]` — always pair a `display:flex/grid` component with a `[hidden]{display:none}` reset.)
- **⏭ NEXT MONTHLY CLOSE = JUNE** — gated on the June-30 FG census at month-end (operator enters via Saisies → PF card → `expeditions.php?view=stocktake`). **🔴 The closing census MUST be entered with the "Inventaire de clôture mensuelle (COGS)" checkbox CHECKED (→ `count_type='month_end'`) — otherwise it lands `operational` and the engine excludes it from FG.** The June weeklies already entered stay `operational` and are now correctly excluded. Then engine `--apply 2026-06`, reconcile (opening = May close 351 884.58), auto-publishes on the Fiche tab. Closed-periods-never-recomputed remains standing policy — May is now immutable. (Discriminator detail → §WEEKLY/MONTH-END FG DISCRIMINATOR below.)

## ⚡ (SUPERSEDED — May close now FULLY FINALIZED, see headline above) STATUS HEADLINE (2026-06-11) — awaiting operator May FG census then git commit
Full pipeline is LIVE on the VPS (deployed, NOT yet git-committed — awaiting Kouros's go). Migs 330 + 332 applied. Fiche COGS tab (5th tab) renders April, ties to the cent, smoke-tested 13/14 on app.maltytask.ch. Engine built + May dry-run validated. **Only remaining work is operator-gated:** Kouros enters the May-31 FG census via the now-surfaced stocktake form → then finalize `basis_adjustment_chf`, run engine `--apply` for 2026-05, reconcile, publish. Detail below.

## Page placement (Le Zeppelin IA)
- **`financier.php` = "Pôle Financier", `ref_pages` id=239, page_key=`financier`, label "Financier", `min_role='manager'`, `domain='general'`, is_active=1.** Belongs to **Le Cockpit** (commercial/financial family). Read-only fiscal hub — NEVER recalculates fiscal numbers; consumes pre-computed JSON artifacts (`interfaces/cogs-report-data.json` COP, `interfaces/sales-cogs-data.json` COGS/Marge) + lazy `/api/financier-data.php`.

## Page architecture (verified 2026-06-11)
- **Body class:** `<body class="home financier">`. CSS scoped `body.financier …`.
- **CSS file:** `public/css/financier.css` (linked at financier.php:330 with `?v=filemtime`). app.css linked first (line 329). NO inline CSS — add Fiche styles HERE.
- **JS:** `public/js/financier.js` (+ `kpi-charts.js`). Linked at bottom with `?v=filemtime`.
- **Hydration = PHP→window.JSON→JS** (NOT server-side rendered tables; server pre-renders panel shells + a default slice, JS hydrates). `window.FIN_COP`, `FIN_COP_MONTHS`, `FIN_SALES_TREND`, `FIN_SALES_MONTHS`, `FIN_SALES_DEFAULT_SLICE`, `FIN_GL_MONTHS` etc. injected in a `<script>` block (financier.php:886+) with `$JSON_FLAGS`. Heavy per-SKU detail is lazy-fetched per month via the API endpoint. Follow this pattern for the Fiche: inject seed/monthly series as `window.FIN_FICHE_*`, render in financier.js.

## Tab pattern (verified — 4 tabs today)
- `<nav class="fin-tabs" role="tablist">` with `.fin-tab` buttons carrying `data-tab="<key>"`, `aria-controls="fin-panel-<key>"`, `id="fin-tab-<key>"`, `aria-selected`. Active = `.fin-tab--active`.
- Panels: `<section class="fin-panel" id="fin-panel-<key>" hidden role="tabpanel" aria-labelledby="fin-tab-<key>">`. First panel = `.fin-panel--active` (not hidden).
- **Existing 4 tabs:** `cop` (default/active), `cogs`, `marge`, `sku`. (`sku` = "Coût par SKU", absorbed the retired standalone sku-costs.php per scrapping #32.)
- **Tab-switch JS (financier.js ~L505):** ONE delegated loop over `.fin-tab` — on click toggles `.fin-tab--active` + aria-selected on tabs, then `p.hidden = (p.id !== 'fin-panel-'+target)` + `.fin-panel--active` on panels. Plus a `keydown` arrow-nav handler (~L527). **A 5th "Fiche" tab = add the button + panel + reuse this exact loop (no JS rewrite needed).**
- **Reusable table classes:** `.fin-table` (+ `.fin-table-scroll` wrapper), `.fin-grid-table .fin-table-compact` (+ `.fin-grid-scroll`) for the GL accounting views. Reuse these for the Fiche table — do NOT invent new table classes.

## CSV export convention (app-wide)
- **Comma separator, double-quote enclosure:** `fputcsv($out, [...], ',', '"')`. Canonical exporter = `public/modules/warehouse-export.php` (GL recap CSV, `Content-Type: text/csv; charset=utf-8`, filename `…-{period}-{today}.csv`). financier now has TWO Fiche-toolbar CSV exporters: `public/api/cogs-fiche-csv.php` (single-section quick export) + `public/api/cogs-comprehensive-csv.php` (4-section CFO export, see C2 below). Both comma + double-quote + UTF-8 BOM. NB: `charges-bc.php` only *ingests* CSV (accept=".csv"), doesn't export.
- **🔴 STANDING RULING — CFO COGS export = CSV (pure PHP, no python/openpyxl, no xlsx/composer dependency). Operator override 2026-06-12, recorded as policy.** The PM had ratified XLSX (one-sheet-per-section, via the `xlsx` skill) to avoid a ragged stacked-§-block CSV. The OPERATOR overrode this in the C2 build brief ("Pure PHP — NO xlsx, NO composer libraries") and explicitly REJECTED the earlier multi-tab XLSX-via-python approach → a single pure-PHP 4-section stacked CSV shipped (`986cbb0`). **Going forward, financier/CFO exports default to pure-PHP CSV. If a future need genuinely requires typed-number Excel tabs, RE-RAISE with the operator first — do NOT default to xlsx.**

## Tables (LIVE — migs 330 + 332 APPLIED on VPS 2026-06-11; applied=339, 0 pending)
**Correction to stale index note:** mig 332 was reported "NOT applied" when a parallel dashboard commit (`fb2778a`) swept it in — it is now APPLIED. Do NOT re-create.

### `ref_cogs_fiche_categories` (mig 330) — 12 rows, the fiche category master
`id`(PK uint), `category_key`(varchar32 UNIQUE), `label_fr`(varchar64), `inv_gl`(varchar16), `charge_gl`(varchar16), `display_order`(smallint, MUL), `is_brewing`(tinyint def 0), `is_active`(tinyint def 1), created_at, updated_at.
12 categories in display order: Malt(1200.1/4101,brewing) · Hops(1200.2/4102,brewing) · Ingredients/Ingrédients(1200.3/4104,brewing) · Yeast/Levures(1200.4/4103,brewing) · Cartons(1201.1/4207) · Inliner(1201.2/4205) · Capsules(1201.3/4200) · Verres(1203.0/6607) · Bouteilles(1204/4200) · Etiquettes(1205/4206) · Canettes(1207/4202) · CapsFuts(1221/4209). First 4 `is_brewing=1`, rest packaging.

### `cogs_fiche_seed` (mig 332) — 12 rows = the EXT-April-signed anchor (one per category for month 2026-04)
`id`, `month_key`(char7 MUL), `category_key`(varchar32 MUL), `rm_chf` `wip_chf` `fg_chf`(**DECIMAL(14,4) — chosen for cent-perfect sums**), `total_chf`, `opening_chf`, `variation_chf`, `seed_source`(varchar64 def 'EXT-April-signed'), `seeded_at`. **This is the immutable April-2026 signed anchor (opening of May = closing of April).** e.g. Malt total 54351.88 / opening 50604 / variation +3747.88. schema_meta: `cogs_fiche_seed` = **reference / allowed**.
- **April seed TIE-OUT PASSED to the cent** (cross-read against EXT): ΣJ (Total Inventaire) = 394 367.74 · ΣK = 431 354.00 · ΣL (Variation) = −36 986.26 · ΣRM = 329 852.08 · ΣWIP = 22 364.27 · ΣFG = 42 151.39. The EXT cross-read caught a −356.625 half-rounding on Levures — DECIMAL(14,4) is why it ties.

### `cogs_fiche_monthly` (mig 332) — 0 rows (EMPTY until May `--apply`)
Same shape as seed + `basis_adjustment_chf`(decimal14,4 NULL), `computed_at`(datetime), `row_hash`(char64 UNIQUE — idempotency key). Computed per-month per-category variation-de-stock rows land here. schema_meta: `cogs_fiche_monthly` = **derived / blocked_with_redirect**, writer = `cogs-monthly-compile.ts`. **Currently EMPTY — the May `--apply` is the first row-write into it; the Fiche tab reads seed (April) ∪ monthly (May onward).**

### mig 332 provenance notes (don't re-derive)
- **mig 332 was RENUMBERED from 331** — a concurrent `331_kpi_grouped_bar_viztype.sql` (the MON TABLEAU #5 grouped-bar work) had already taken 331. The file on disk is `332_cogs_fiche_seed_and_monthly.sql`.
- **mig 332 DROPPED `cogs_fiche_opening_anchor`** (a table mig 330 had created) and removed its `schema_meta` row — the opening is now resolved live by UNION(seed ∪ monthly), so a separate anchor table was a redundant parallel store. Do NOT re-create `cogs_fiche_opening_anchor`.

## Engine — `scripts/cogs-monthly-compile.ts` (maltytask repo, TS) — ✅ COMMITTED + PUSHED `423a660`, May `--apply` LANDED + VERIFIED
Computes RM/WIP/FG per category → `cogs_fiche_monthly`. Guards:
- **Opening resolver reads UNION(seed ∪ monthly)** — opening of month N = closing of N-1 from whichever home holds it (April from seed, May+ from monthly).
- **Immutability guard:** refuses to recompute ANY month present in `cogs_fiche_seed` (closed-periods-never-recomputed = standing policy).
- **FG-MISSING guard:** refuses to fabricate a zero close when the FG census for the month is absent (no fabricated zero close).
- **May dry-run validated:** RM Yeast = 1 496 (sane — **confirms the earlier g→kg yeast blowup is a CLOSED-PERIOD-ONLY artifact**, not live), WIP computes, opening resolves from the April seed.
- **✅ UNIT-INVARIANT RESOLVED — naive IS correct, do NOT switch to `loadRM` (see headline §🔴 INVERSION for the full proof):** the engine's ROW LOADER is `loadRmStock` (lib/rm-stock-mysql.js); formula = naive `finalQty × costChf` (`inv_rm_stocktake.final_qty` is stored in the PRICING unit, so NO conversionFactor — multiplying by it is the bug that 1000×-under-values g-input/kg-priced items). DISTINCT from the legacy `loadRM` (lib/inventory.js) which APPLIES conversionFactor and is WRONG here. Sanity guard throws if Yeast RM ≥ 50 000; permanent unit-consistency DIAGNOSTIC surfaces any conversionFactor≠1 item (27 May items flagged = loadRM-metadata-stale, NOT applied, does not halt). Verified May: Yeast RM=1 496.03. Resolved in `423a660`.

## Build verdict / sequencing for the Fiche tab
- The Fiche tab is render-only over `cogs_fiche_seed` + `cogs_fiche_monthly`; both are canonical single-home tables, no parallel store. SOUND.
- **GATE:** `cogs_fiche_monthly` is EMPTY. A tab built today shows only April seed. Confirm with the operator whether the monthly-compute pipeline lands first (so the tab has >1 month) or the tab ships seed-only as a v1. Don't fabricate May rows.
- Categories/GLs come from `ref_cogs_fiche_categories` — JOIN, never hardcode the 12 labels or GLs.
- EQUIP ui+sql+coder (+webapp-testing for deployed-page smoke).

## ✅ B4 + B5 SHIPPED + DEPLOYED + SMOKE-TESTED — COMMITTED maltyweb `ad9c14b` (+ banner CSS `70efa0e`); engine finalize maltytask `423a660`
**Tasks B4 (Finance fiche tab) + B5 (CSV download) DONE, on the VPS, smoke 25/26 post-May-publish (was 13/14 on April-seed-only).** Renders, ties to 394 367.74 / −36 986.26 (April) and 351 884.58 (May), 12 rows + GLs, provenance chip ("Clôture signée (référence)" vs "Calculé"), incomplete-month banner. **Bugs found + fixed during smoke (all committed):** (1) CSV 500 — duplicate `:month` named param under native prepares; (2) false incomplete-banner on seed months → gated on `provenance ≠ seed`; (3) `.fin-fiche-warn{display:flex}` overrode `[hidden]` → added `.fin-fiche-warn[hidden]{display:none}` (`70efa0e`). **COMMITTED — see headline.** 4 files:
- **`public/modules/financier.php`** (edited): L199 `fin_fmt_chf(float):string` helper (number_format, space thousands, 2 dec). L323–391 Fiche PDO data block (UNION over `cogs_fiche_seed` ∪ `cogs_fiche_monthly` × `ref_cogs_fiche_categories`) → `$ficheMonths/$ficheData/$ficheTotals/$ficheLatestKey/$ficheIsIncomplete/$ficheProvenance/$ficheBasisRows`. L448–452 5th nav tab button `data-tab="fiche"` (reuses the existing delegated tab loop — no JS rewrite, as the pattern predicted). L919+ `#fin-panel-fiche` section **PHP-rendered table PER MONTH, server-side, NO JS recompute** (`hidden` attr correct). L1095–1101 7 `window.FIN_FICHE_*` globals injected.
- **`public/js/financier.js`** (edited): L1579+ Module E IIFE — month switcher (show/hide `.fin-fiche-month-block`), provenance-chip update, incomplete-month warning, CSV href update on select change. **Switcher only — table itself is server-rendered, NOT hydrated.**
- **`public/css/financier.css`** (edited): ~120 lines before `@media` — `.fin-fiche-controls`, `.fin-fiche-provenance-chip` (seed/computed variants), `.fin-fiche-csv-btn`, `.fin-fiche-warn`, column sizing, variation coloring, total row, basis row.
- **`public/api/cogs-fiche-csv.php`** (NEW): auth-gated `require_page_access('financier')`, validates `?month=YYYY-MM` against actual DB months, **re-queries server-side (never echoes client floats)**, UTF-8 BOM for Excel, comma+double-quote CSV (mirrors warehouse-export convention as documented), totals + basis-adjustment rows.
- Both PHP files pass `php -l` (VPS PHP) AND deployed-page smoke (13/14) — done.
- **April 2026 anchor totals confirmed from `cogs_fiche_seed`:** Valeur Stock (RM) 329 852.08 / Total Inventaire 394 367.74 / Variation Stock −36 986.26 CHF.
- **Architecture verdict: SOUND.** Render-only over the two canonical single-home tables; CSV re-queries server-side (no float-laundering); categories JOINed not hardcoded; tab reuses delegated loop. As-built matches the documented pattern.

## ✅ Saisies entry-point — 6th card "Inventaire produits finis (PF)" (2026-06-11, deployed + smoke-tested)
New card on the Saisies page → `expeditions.php?view=stocktake` (operator-gated). This surfaces the FG-census stocktake form the operator needs to enter the May-31 count. Deployed + smoke-tested. (This is the form Kouros uses to unblock the May publish below.)

## Kouros ratifications (2026-06-11) — standing decisions
- (a) **Seed April from EXT + engine proves-on-May** (don't recompute April; prove the engine by recomputing May from April opening).
- (b) The one-time WAC/F2 basis change is shown as a **separate "Ajustement de base (non récurrent)"** line (= `basis_adjustment_chf`), not folded into the recurring variation.
- (c) **Closed-periods-never-recomputed is standing policy** (mirrored by the engine's immutability guard).

## ✅ DONE (was "STILL PENDING") — May close finalized 2026-06-12
May-31 FG census entered (`inv_fg_stocktake` month_closed=2026-05, 34 rows); `basis_adjustment_chf` = +1 808.82 (FG/F2; RM/yeast = "non isolable — base héritée"); engine `--apply 2026-05` landed 12 rows into `cogs_fiche_monthly`; reconciled (opening=April seed 394 367.74); published on the Fiche tab; engine committed+pushed `423a660`. **Next monthly close = JUNE (gated on June FG census at month-end) — see headline.**

### FG census gotcha (derivation note — FG stocktake → fiche FG)
- May census started **33/37 base SKUs**; operator confirmed **ALTB / EST4 / EPH1B / ESTF genuinely zero**, then re-counted **PD8 (1078 April → 225 May)**.
- **The expeditions stocktake form UPSERTS per (sku × location × counted_at) and BLANK fields SKIP (do NOT erase)** — verified in code + live. So adding ONE SKU later to a PRIOR date is SAFE (no destructive overwrite of the already-counted siblings). This is the mechanism that makes incremental census correction non-destructive — relevant to any future "patch a closed/in-progress census" ask.

## ✅ WEEKLY/MONTH-END FG DISCRIMINATOR — SHIPPED + VERIFIED 2026-06-12 (#2 follow-up to the DOAF anchor fix; regression gate PASSED to the cent)
The problem this closes: the engine's FG reads (computeFg for the close month + computeBasisAdjustment for the PRIOR month) were globbing ALL `inv_fg_stocktake` rows for a month. With operators now entering **intra-month weekly counts** (the June weeklies, 109 rows), a naive `WHERE month_closed=?` would let weekly snapshots pollute the closing FG valuation. The discriminator separates the two count cadences.
- **Mig `339_inv_fg_stocktake_count_type.sql`** (NOT 338 — `338_kpi_sales_hl_trackers.sql` already existed; a parallel session applied 337+338 on the VPS, so by apply time the VPS was 0-pending and migrate.php applied ONLY 339 cleanly via the sanctioned `sudo -u www-data php migrate.php` path). Adds `count_type ENUM('operational','month_end') NOT NULL DEFAULT 'operational' AFTER source`. **Backfill (live-verified): April 41 rows → month_end, May 34 → month_end, June 109 → operational.** MIG HEAD now **339** — re-`--status` next build.
- **Engine `scripts/cogs-monthly-compile.ts`** (maltytask) — BOTH FG reads (`computeFg` AND the basis-adjustment prior-month read) now `WHERE month_closed=? AND count_type='month_end'`, wrapped in `ROW_NUMBER() OVER (PARTITION BY sku_id_fk, location_id_fk ORDER BY counted_at DESC, id DESC)=1` (latest-per-(sku,loc) guard); NO `is_active` filter (preserved as before). **⚠️ This script runs LOCALLY via the tunnel — it is NOT deployed to `/opt/maltytask-pipeline` (the pipeline carries only `lib/`, no cogs script), so the local edit IS the production path. Left UNCOMMITTED in the maltytask working tree** (operator's local run target; commit-on-ask).
- **Writer affordance — `public/modules/expeditions.php` + `public/css/expeditions.css`** (both deployed via single-file rsync; local==deployed pre-deploy so it was purely additive). POST flag `$stCountType = isset($_POST['month_end_census']) ? 'month_end' : 'operational'`, threaded into the INSERT (bind-aligned, `$stCountType` last) + ON DUP UPDATE + `log_revision` after-array; new checkbox **"Inventaire de clôture mensuelle (COGS)"** with scoped `.exp-st-month-end` CSS (design tokens, `:has(:checked)` highlight). php -l clean; INSERT-shape smoke (txn rollback) passed; page renders (302→login, no 500).
- **🔴 CORRECTED RULE — April seed rows = `month_end`, NOT `operational` (this OVERRIDES the earlier "leave April default operational" brief instruction):** the basis-adjustment read reads the PRIOR month's FG (for May that's April). With `count_type='month_end'` now ANDed into that read, leaving April `operational` returns ZERO April rows → basis adjustment breaks → the PUBLISHED, IMMUTABLE May fiche changes. **The correct rule is: any month's CLOSING census = `month_end` (April seed + May census both qualify); ONLY intra-month weeklies (June's 109) are `operational`.** PROVEN by the regression gate — with April→month_end, the May dry-run reproduces the published fiche TO THE CENT (FG **47 701.56**, basis_adj **+1 808.82**, total **353 867.69**), diagnostics show "prior-month (2026-04) FG rows loaded: 41" (all found) and "FG rows loaded: 34" (May census only, June weeklies excluded). Had April stayed operational, basis would be 0 and the gate would FAIL. **Regression gate PASSED — the immutable May fiche is preserved to the cent.**
- **Housekeeping:** cogs-monthly-compile.ts uncommitted in maltytask (operator's local run target); maltyweb mig 339 + expeditions.php/.css deployed but UNCOMMITTED in the shared dirty tree (parallel session active on repack/kpi — name files, do NOT `git add -A`).

## 🔴 OPEN CFO ITEM (2026-06-15) — CLOSED-PERIOD WIP-RESTATEMENT QUESTION (linked from the tank-simulator finding)
The Node tank-sim packaging→BBT drain was keyed on a SKU-code-vs-canonical-name mismatch and **NEVER DRAINED SKU-coded packaging from its BBT** → `inv_tank_balances` (the WIP store this engine reads in `computeWip(month)`) has MASSIVELY OVERSTATED BBT/WIP across the ENTIRE history (2025-05 BBT 738→336 hl, 2026-05 766→560 — Opus-verified full-history dry-run; CCT unaffected). Fix committed maltytask `0251394`, NOT yet `--apply`'d. Root-cause + magnitude + hazard analysis → tank-simulator-occupancy.md §FIRST-CLASS COGS-STORE FINDING.
- **Impact on the fiche:** closed COGS months (incl. the IMMUTABLE May close) were computed against INFLATED closing-WIP. The fiche is seed-closed/never-recompute, so sealed numbers won't AUTO-change — but they were built on overstated WIP. **This is a closed-period RESTATEMENT question for the CFO, independent of the tank-sim apply.**
- **🔴 The "never-recompute" promise is NARROWER than assumed — it is a CONVENTION for monthly closes, not a hard GUARD.** `assertNotSeeded` throws ONLY for `cogs_fiche_seed` months (April only). `applyFiche` upserts on `uq_cogs_fiche_monthly_month_cat (month_key, category_key)` → re-`--apply --month 2026-05` AFTER corrected WIP lands would OVERWRITE the published May wip/total/variation IN PLACE. So do NOT re-`--apply` any published month (May) on the back of the tank-sim correction unless it is a deliberate CFO-sanctioned restatement. Recommend HARDENING the immutability guard to also refuse months already in `cogs_fiche_monthly`.
- **Clean path:** open-month-only apply (land corrected WIP for the OPEN month so the next close — June — uses correct WIP) + HOLD-AND-QUANTIFY the closed-period overstatement as a CFO memo. Apply-scope decision is WITH THE OPERATOR.

## 🟡 NEXT BUILD (PM-RULED 2026-06-12) — financier unified default + comprehensive CFO export
Two operator-ratified features; PM rulings below. EQUIP coder+sql+ui+xlsx+webapp-testing.
> **✅ C1 (= items A+B) SHIPPED 2026-06-12 (smoke all-PASS; files staged/modified, operator will commit — NOT yet committed).** Finance page month defaults unified to last-closed + pending-state panel.
> **✅ C2 (item C, comprehensive CFO export) SHIPPED + LIVE + COMMITTED + PUSHED 2026-06-12 — maltyweb commit `986cbb0` on origin/main, deployed to app.maltytask.ch. Built as CSV (pure-PHP), per operator instruction overriding the prior XLSX ratification. §1 tie-out for 2026-05 = 353 867.69 / −40 500.05 (ties live DB).**
>
> **C1 as-built (verified):**
> - NEW `app/finance-period.php` — `fin_last_closed_month(PDO $pdo): ?string` = UNION-MAX over `cogs_fiche_seed ∪ cogs_fiche_monthly` (live = **2026-05**); `strict_types`, try/catch-never-throws, mirrors `brewery_identity()` pattern (settings-helpers.php). **Call the accessor, never copy the UNION inline.**
> - `public/modules/financier.php` — required L25; `$lastClosedMonth` computed ~L37; all **4** selectors repointed: `$copLatestKey` (COP) + `$salesLatestKey` (COGS+Marge) overridden to `$lastClosedMonth` if present; `$glLatestMonth` (both P&L grids) = last-closed if in `glMonths` else most-populated fallback; `$ficheLatestKey` now also routes through the helper for consistency (already May). JS globals `FIN_COP_DEFAULT`/`FIN_SALES_DEFAULT`/`FIN_GL_DEFAULT` follow transitively. Pending panel `.fin-pending-import` added inside BOTH `#cop-grid-card` + `#cogs-grid-card`, fires when `$glPendingForLastClosed=true` (last-closed absent from GL data); shows `inv_charges_bc` row count as explanatory text.
> - `public/css/financier.css` — `.fin-pending-import` block + **mandatory `.fin-pending-import[hidden]{display:none}` reset** (the `70efa0e` scar held).
> - **Smoke (all PASS):** all 6 month selectors = 2026-05; pending panel 0 in DOM (CORRECT — May has 17 GL rows, IS in glMonths so not pending); March switch loads content; test user id=16 reverted to viewer; DB confirms last_closed=2026-05.
- **A. "Last closed month" SoT = FICHE-DERIVED via a shared helper, NOT a settings row.** Canonical signal = `MAX(month_key)` over `cogs_fiche_seed ∪ cogs_fiche_monthly` (currently **2026-05**). REJECT a `system_settings` "last_closed_month" pointer = parallel/divergent store of a fact the fiche already owns (drift = silent-corruption class); the engine `--apply` advancing the rows IS the advance, zero operator action. Build `fin_last_closed_month(PDO):?string` (the UNION-MAX) as a **shared** helper (new `app/finance-period.php`, NOT settings-helpers.php) so Expéditions/dashboards reuse it. **Call the accessor, never copy the UNION inline.** All 4 financier selectors DEFAULT to it; keep each tab's `*LatestKey` as the data-AVAILABILITY signal and branch `in_array($lastClosed,$tabMonths)`.
- **B. Pending-state UX = "charges comptables en attente d'import" panel, NEVER a stale-month fallback.** Reuse this page's freshness/provenance-chip vocab (`.fin-fiche-provenance-chip` / `.fin-fiche-warn`). **CSS scar: every new `display:flex` warn/pending component MUST ship a `[hidden]{display:none}` reset** (the `70efa0e` bug). Make it self-sufficient: show the month + the `inv_charges_bc` row-count present (Mar 323 / Apr 15 / May 17 = the WHY). Scope: COP + GL/charges P&L grid only (charges-import-gated); cogs/marge/fiche reach the closed month via sales-cogs feed + fiche tables.
- **C. ✅ SHIPPED + LIVE + COMMITTED + PUSHED 2026-06-12 (maltyweb `986cbb0`, origin/main, deployed) — built as CSV (pure-PHP), NOT XLSX. OPERATOR OVERRODE the prior XLSX ratification** (build brief: "Pure PHP — NO xlsx, NO composer libraries"). The earlier PM reasoning (XLSX one-sheet-per-section avoids ragged mixed-col CSV) was SUPERSEDED by the explicit no-libraries instruction; one stacked 4-section CSV shipped instead.
>   - **AS-BUILT:** NEW endpoint `public/api/cogs-comprehensive-csv.php` (239 lines, pure PHP 8.1, no libs). Auth `require_page_access('financier')`; `?month=YYYY-MM` regex-validated; streams ONE CSV (UTF-8 BOM, comma delim, attachment `cogs-complet-<month>.csv`); 4 sections stacked (title → blank → header → data → total → 2 blanks between).
>   - **4 sections:** §1 FICHE COGS — VARIATION DE STOCK (exact same DB query as `cogs-fiche-csv.php`; verified 353867.69 / -40500.05 for 2026-05). §2 COGS PAR SKU (`sales-cogs-data.json.months[m].bySKU`, 94 SKUs for May; 9 cols SKU/HL/Qté vendue/COGS matière/Taxe bière/COGS total/CA/Coût-unité/HL-unité). §3 COP — COÛT DE PRODUCTION (`cogs-report-data.json.months[i].cop`; handles BOTH legacy top-level numeric `total` AND current `subtotal.current.{total,hl,perHL}`, AND both `ingredients`/`otherIngredients` key names). §4 TAXE BIÈRE (`sales-cogs-data.json` beerTax byCategory+breakdown; rates derived from tax/hl = canonical beerTaxRates; `exportsExcludedHL` rendered as HL not CHF).
>   - **UI:** `financier.php` 2nd `<a>` button `#fiche-comprehensive-csv-btn` after `#fiche-csv-btn` on the Fiche toolbar (reuses `.fin-fiche-csv-btn`); `financier.js` `updateFicheMonth()` also sets the 2nd button href on month change; `financier.css` moved `margin-left:auto` off the shared `.fin-fiche-csv-btn` class onto the `#fiche-csv-btn` ID rule (prevents the 1st button creating layout issues with the 2nd).
>   - **Smoke (all PASS):** HTTP 200 + `text/csv; charset=utf-8`; 7708 bytes / 160 lines; BOM present; all 4 section headers found; §1 totals 353867.69 / -40500.05; button rendered w/ href `?month=2026-05`; test user id=16 elevated to manager then reverted to viewer.
>   - **Deployed surgically (not blanket rsync):** `public/api/cogs-comprehensive-csv.php` (new) + `public/modules/financier.php` + `public/js/financier.js` + `public/css/financier.css`. **COMMITTED + PUSHED `986cbb0` (origin/main); deployed to app.maltytask.ch.**
>   - **Auth path (mirrors `cogs-fiche-csv.php`):** `auth.php` → `require_page_access('financier')` (manager+ gate). Endpoint defaults to `fin_last_closed_month()` when `?month` omitted.
>   - **KEPT** the shipped single-section `cogs-fiche-csv.php` (Fiche quick export) — now two CSV buttons on the toolbar.
- **D. CSV/XLSX section sources (VERIFIED live 2026-06-12):** §1 Fiche = `cogs_fiche_seed ∪ cogs_fiche_monthly` × `ref_cogs_fiche_categories` (reuse existing server query, JOIN don't hardcode). §2 COGS par SKU = `sales-cogs-data.json.months[m].bySKU`. §3 COP = `cogs-report-data.json.months[i].cop` (the financier COP tab's source — NOT cop-report-data.json; don't add a 3rd COP source). **§4 Taxe bière = `sales-cogs-data.json.months[m].beerTax`** (the canonical pick) — it is a COMPUTE (`lib/beer-tax.js`), NOT raw inv_sales_bc, NOT a standalone artifact; structure `{total,reduction,byCategory:{0..3:{hl,tax}},breakdown:{fromSales,fromStock}}`. ALSO present in `cogs-report-data.json.months[i].beerTax` (board view, +rate/fullRate) — cross-check only, don't dual-source. Rates (`reduction` 0.4, fullRates/effectiveRates) live in `cogs-report-data.json.beerTaxRates` — read, NEVER hardcode; cat 0–3 labels from `ref_beer_types`.
- **SEQUENCING:** ✅ (1) helper [DONE C1] → ✅ (2) re-default 4 selectors + pending branch [DONE C1] → ✅ (3) comprehensive export endpoint + 2nd download button on Fiche toolbar [DONE C2 — built CSV not XLSX per operator] → ⏳ (4) ONE combined-diff review + operator commit (C1+C2 both staged/uncommitted).
- **Note:** close stays OFF Données générales. A future MANUAL display-freeze override ("freeze finance at month X regardless of fiche") would be a display PREFERENCE (legit `system_settings` row) — different feature, do NOT pre-build.

## 🎫 TOUR (PM tour-steward standing duty)
**✅ FINANCIER TOUR CARD SHIPPED 2026-06-16 — `4aa6fa7` `feat(tour): add Visite-guidée card for the Financier (Fiche COGS) page`.** Closes the open "tour-steward card for financier" item. `maltyweb-tour-steward` authored `$PAGE_DESCRIPTIONS['financier']` + `$PAGE_ICONS['financier']` in `public/modules/visite-guidee.php`, deployed surgically (single-file rsync, md5 local↔VPS parity), and `scripts/tour-gap-check.php` now reports **critical=0**. (financier is a sensitive COGS/COP class → the steward returned the draft `PM-RATIFY: financier`; PM ratified, then it shipped.) `journal-saisies` is special-cased by the tour into the saisies opener/form chapters (not a freestanding card gap). Committed by pathspec + pushed.
_(historical)_ `scripts/tour-gap-check.php` had flagged `financier` + `journal-saisies` as pre-existing CRITICAL gaps (predating this arc); the Fiche COGS tab lives on the financier page, so the financier card covers it.

## 🗂️ ARCHIVED INDEX HEADER BANNER (compacted from index 2026-06-15)
_Verbatim header banner relocated from the index during memory compaction. Detail above is the canonical record; this preserves the exact index wording._

> **✅ FINANCIER-FICHE COGS ARC KEYSTONE CLOSED — May 2026 close SHIPPED + RATIFIED (Kouros) + VERIFIED (Opus gate, all 6 checks PASS) + COMMITTED + PUSHED (`423a660` maltytask main; `origin/main..HEAD` empty; tree clean).** Engine `scripts/cogs-monthly-compile.ts` IS committed (a concurrent session landed it — the deferred commit is DONE, nothing left). 12 May `cogs_fiche_monthly` rows: RM 282 368.74 · WIP 21 814.28 · FG 47 701.56 · TOTAL 351 884.58 · OPENING 394 367.74 · VARIATION −42 483.16 · BASIS_ADJ +1 808.82. Key fiscal invariant: April-seed close 394 367.74 == May opening (EXACT). Engine dry-run reproduces published rows TO THE CENT (HEAD == published, no drift). Yeast RM 1 496.03 (g→kg held; 27 unit-mismatches = diagnostics, NOT applied). Smoke 25/26; banner CSS fix `70efa0e` (maltyweb). **🔴 RM-FORMULA INVERSION (standing): naive `final_qty × cost_chf` IS CORRECT (`inv_rm_stocktake.final_qty` is in the pricing unit) — do NOT "harden via `loadRM`" (lib/inventory.js applies conversionFactor → 1000×-under-values g-input/kg-priced items, Hops 29.67 vs correct 29 668.66). Engine's loader is `loadRmStock` (lib/rm-stock-mysql.js), distinct from the rejected `loadRM`; April yeast 565k = one-off CLOSED-period (seeded, never-recomputed) metadata drift, not a formula bug; permanent non-halting unit-consistency diagnostic added.** Engine May valuation diverges from legacy EXT (RM ≈ −47k) — EXPECTED, engine supersedes EXT; correctness rests on seed tie-out + identities, NOT EXT match. Kouros ratified: seed-April-from-EXT + engine-proves-on-May; WAC/F2 basis = separate "Ajustement de base (non récurrent)" line; closed-periods-never-recomputed = standing policy. **⏭ NEXT MONTHLY CLOSE = JUNE — gated on June-30 FG census at month-end, entered with the new "Inventaire de clôture mensuelle (COGS)" CHECKBOX CHECKED → `count_type='month_end'`** (operator via Saisies → PF card → `expeditions.php?view=stocktake`); then engine `--apply 2026-06` (opening auto = May 351 884.58) → reconcile → auto-publishes. May now immutable. **✅ WEEKLY/MONTH-END FG DISCRIMINATOR SHIPPED+VERIFIED 2026-06-12 (#2 follow-up to DOAF anchor fix): mig 339 adds `inv_fg_stocktake.count_type ENUM('operational','month_end') DEFAULT 'operational'` (MIG HEAD now 339 — re-`--status` next build); engine `cogs-monthly-compile.ts` (maltytask, runs LOCALLY via tunnel, IS the prod path) BOTH FG reads now `WHERE count_type='month_end'` + ROW_NUMBER latest-per-(sku,loc); expeditions.php/.css checkbox writer. 🔴 CORRECTED backfill rule (OVERRIDES the earlier 'leave April operational' brief): April seed = month_end, NOT operational — basis-adj reads PRIOR-month FG (May→April), operational-April → 0 rows → breaks the published immutable May; rule = any CLOSING census=month_end (April seed + May census), ONLY intra-month weeklies (June 109)=operational. Regression gate PASSED to the cent (May FG 47 701.56 / basis +1 808.82 / total 353 867.69 reproduced; 41 April + 34 May loaded, June excluded). UNCOMMITTED: cogs-monthly-compile.ts (maltytask, operator's local run target) + mig 339/expeditions.php/.css (maltyweb dirty shared tree — name files, NOT `git add -A`).** **✅ C1 (financier month-defaults unified to last-closed + pending-import panel) SHIPPED 2026-06-12, smoke all-PASS, files staged/modified NOT-yet-committed (operator commits) — NEW `app/finance-period.php`::`fin_last_closed_month(PDO):?string` = UNION-MAX(cogs_fiche_seed ∪ cogs_fiche_monthly)=2026-05, the SoT signal (REJECTED a system_settings pointer = parallel store); call the accessor never copy the UNION; `.fin-pending-import[hidden]{display:none}` reset held. ✅ C2 (comprehensive CFO export) SHIPPED+LIVE+COMMITTED+PUSHED 2026-06-12 — maltyweb `986cbb0` (origin/main, deployed app.maltytask.ch); built as CSV (pure-PHP `public/api/cogs-comprehensive-csv.php`, 239 lines no libs; auth.php→require_page_access('financier') manager+; defaults to `fin_last_closed_month()`; 4 stacked sections Fiche / COGS-par-SKU(94) / COP(dual legacy `{total}` + current `{subtotal.current}` shapes) / Taxe-bière(rate DERIVED, never hardcoded); UTF-8 BOM; `?month=YYYY-MM`), **per operator STANDING RULING OVERRIDING the prior XLSX ratification** ("Pure PHP — NO xlsx, NO composer libraries"; future typed-Excel need → RE-RAISE first); 2nd Fiche-toolbar button `#fiche-comprehensive-csv-btn`; smoke all-PASS (200, text/csv, BOM, 4 headers, §1 totals 353867.69 / -40500.05 ties live DB); deployed surgically (4 files: api + financier.php/.js/.css). C1 still staged/uncommitted (operator commits).** **TOUR:** `financier`+`journal-saisies` = pre-existing CRITICAL gaps; new Fiche tab may want a card (financier sensitive → PM-RATIFY). → [financier-cogs-fiche.md](maltyweb-pm-memory/financier-cogs-fiche.md).
