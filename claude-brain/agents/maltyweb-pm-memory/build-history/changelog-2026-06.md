# Build-changelog — 2026-06

> Part of the build-history changelog (rolling, append-only, newest-first). Parent index: [README.md](README.md). Newest 2026-06 entries prepend to the top of this file.

### 2026-06-11 (MON TABLEAU KPI dashboard — multiple tranches; commits `b6db62c`,`fb2778a`,`bc678af`,`cdff109` on `main`, NOT PUSHED)
- **✅ TANK-OCCUPANCY TRANCHE — 4 tiles #13/#14/#15/#17 SHIPPED+DEPLOYED+VERIFIED (mig 333; census 57→61 live).** All 4 consume canonical `TankSimulator::run()` via shared per-request helper `kpi_tanks_get_sim_state()`, READ-ONLY zero-writes (no third occupancy computation — DIVERGENCE FLAG honored). #13 `cct_bbt_occupancy` viz `kpi_number`→`bar` (80.8%, 21/26 vessels, 2-bar CCT/BBT split — build added `opts.xLabels` to `buildBarChart`/`renderKpiBar`); #14 `cct_capacity_utilization` 92.3% (1549.9÷1680 HL active CCT cap, amber>90%, div-0 guard); #15 `cct_idle_days` mean 9j/max 24j CCT6; #17 `hl_in_tank` 1846.2 HL physical NOT valued. **#15 idle-source: derives source CCT from brewday batch→CCT map (`bd_brewing_brewday_v2.cct` via `loadBatchCCT`) NOT `bd_racking_v2.cct_number`; handler mirrors (join racking→brewday, group `b.cct`, MAX(event_date)). First cut keyed (beer,batch) → review-caught → fix `cdff109` re-keyed `(COALESCE(neb_recipe_id_fk,contract_recipe_id_fk),batch)` — DO NOT simplify back to cct_number/beer-name.** CAPACITY live-confirmed: `ref_cct` 18 active Σ1680 HL (old 1530 spec was STALE), `ref_bbt` 8 active Σ870 HL (active = `status='active'` NOT `is_active`). **Node-port gate CLARIFIED: lifted for read-only occupancy DISPLAY; applies only to flagship recommendation engines #23/#64 + WIP-valuation #181.** Catalog L109/L115/L344 corrected. Detail → kpi-tracker-catalog.md §TANK-OCCUPANCY TRANCHE.
- **✅ widgets #2 `hl_brewed_ytd` + #5 `production_by_beer_yoy` — identical-chart bug FIXED (mig 331 `grouped_bar` viztype).** #2 stays sparkline (12-elem monthly-cumulative YTD + `meta.prev_series` N-1 ghost, cooling-anchored recipe_id_fk join unchanged, 6832.3 HL ▲+11.4%); #5 → NEW `viz_type='grouped_bar'` "Production par bière — YTD vs N-1", `renderKpiGroupedBar` consumes per-beer `breakdown[]` (17 beers, Zepp 2453.7 ▲+10%); `fb2778a` truncated ghost to `lastDataMi`. ⚠️ `fb2778a` inadvertently swept in pre-staged unrelated **mig 332 `cogs_fiche_seed_and_monthly`** (NOT applied on VPS) from a concurrent COGS-fiche session. Detail → kpi-tracker-catalog.md §widgets #2+#5 FIXED.
- **Other 2026-06-11 arcs (full detail in their thematic files, NOT duplicated here):** ORDER-AWARE COVERAGE fast-follow #1 (`sb_forward_sim`, per-SKU couverture face, ALTF 14.4→1.86wk) → fulfilment-expeditions-arc.md §ORDER-AWARE COVERAGE; FORWARD SEASONAL BURN ENGINE (mig 325, `app/seasonal-burn.php` 8-fn shared lib, `scripts/seasonal-index-cli.php`, cron DISABLED-pending-Kouros, global peak ISO-wk26) → fulfilment-expeditions-arc.md §SEMAINES-RESTANTES; HISTORIQUE TAB (mig 329 `v_sales_ledger_weekly_client`, key on `iso_yearweek` NEVER `week_start`, commit `3646e4f`) → fulfilment-expeditions-arc.md §HISTORIQUE; CIP CADENCE ON BBT CARDS (mig 328, `bbt-detail-modal.js`/`.css`, STR_TO_DATE err-1411 UPDATE gotcha → fix `cip_date REGEXP '^[0-9]{4}-'` pre-filter) → packaging-page-dashboard.md §CIP CADENCE ON BBT CARDS.

### 2026-06-11 (v1 `bd_*` DECOMMISSION — FINAL READER WAVE DONE; all v1 CONSUMERS now on _v2; 3 commits)
- **✅ `b17c686` (maltyweb) — sku-bom-compile.php §2b observed malt/hops → `bd_brewing_ingredients_parsed_v2`** (header_id → `bd_brewing_ingredients_v2`). This was the last COGS-critical v1 read in the compiler (its cooling read moved in `aa6fb0f`; now the ingredients read too). Verification: **0 v1-only keys lost, 75 v2-only gained, 1496 identical.** The **6 differing keys were a v1 DATA BUG** — v1 stored 1g/2g/5g for hops additions physically 1/2/5 **kg**; v2 has the correct 1000/2000/5000 g → repoint **CORRECTS (raises) observed hops COGS for 6 batches** (rid 51 b45, rid 57 b126 ×4, rid 30 b48). **sku-bom-compile.php is now FULLY off v1** (batch-HL + malt/hops + dry-hop all v2).
- **✅ `ed41319` (maltyweb) — `scripts/python/refresh_recipe_profile.py` → all 7 v1 tables to _v2** (the biggest single v1 consumer). Notable structural fixes folded in: removed the fragile `RECIPE_TO_PREFIX` + `beer_raw`-LIKE prefix parsing (the rule-D identity-fracture hazard) → now **JOIN `bd_fermenting_v2` → `ref_recipes` on `recipe_id_fk`**; gravity LONG→WIDE via `event_type`→column map; packaging `pct_loss` derived from `loss_kpi_hl/objective_hl` + O2/CO2 from `bd_packaging_readings` child. **Dry-run clean across 61 recipes; output stable.** 🔴 **ITS CRON (`/etc/cron.d/maltytask-recipe-profile`) IS DISABLED pending manual smoke — OPERATOR ACTION ITEM: smoke + re-enable** (same recipe-profile-cron convention as the seasonal-index cron).
- **✅ `face01f` (maltytask) — `scripts/parse-tank-simulation.ts` cooling read → `bd_brewing_gravity_v2` (`event_type='Cooling'`)**, aliased back to `cool_*` so `CoolingRow`/downstream are unchanged. This was a reader MISSED in the earlier wave (an earlier grep truncated); now caught — reinforces the missing-reader-hunt standing concern (always re-grep the full both-repo tree).
- **🟢 DECOMMISSION STATE NOW — ALL v1 CONSUMERS REPOINTED ✅** (verified by full both-repo grep): tanks.php, sku-bom-compile.php, packaging.php, beer-tax.js, build-efficiency-data.ts, refresh_recipe_profile.py, parse-tank-simulation.ts. **Remaining v1 references are NOT consumers:**
  - **v1 WRITER chain (Stage 3 — the thing to STOP, GATED on operator):** `scripts/python/ingest.py` (writes v1 bd_brewing_brewday/cooling/gravity/ingredients/timings/fermenting/packaging; also a dedup SELECT on bd_packaging) + `scripts/python/parse_bd_ingredients.py` (reads v1 bd_fermenting + bd_brewing_ingredients, writes v1 bd_brewing_ingredients_parsed) + `tab_*.py` builders.
  - **Deferred (intentional):** `app/db-correct.php` (admin-only; UPDATEs `bd_brewing_ingredients_parsed.mi_id_fk` — alias corrections on the v1 table; becomes moot at DROP → remove then) + `scripts/python/validate_fk_candidates.py` (dev FK-audit utility, no cron — reads bd_racking/bd_brewing_brewday).
  - `app/loss-metrics.php:42` = a comment, not a query.
- **STAGE STATUS:** Stage 1 (repoint all readers) ✅ **DONE**; Stage 2 (start_ferm backfill) **MOOT** (anchored on `event_date`); Stage 3 (stop v1 writer = ingest.py + parse_bd_ingredients.py + tab_*.py) **READY — zero consumers depend on v1 reads — GATED on operator go-ahead**; Stage 4 (snapshot → soak → DROP v1 bd_* tables + remove db-correct v1 branch + schema_meta cleanup) **GATED on Stage 3 + operator**.
- **Separate ingest-root ticket:** recent `bd_brewing_timings_v2` rows have NULL `recipe_id_fk` — fix at the writer (NULL-FK = unresolved-link smell), don't paper over downstream.

### 2026-06-11 (v1 `bd_*` DECOMMISSION — Stage 1b/1d cooling+packaging repoints DONE + CCT tooltip UI fix; 3 commits)
- **✅ `aa6fb0f` (maltyweb) — v1 cooling + packaging repoint.**
  - `app/sku-bom-compile.php` batch-HL (the COGS per-HL **denominator**) `bd_brewing_cooling` → `bd_brewing_gravity_v2 WHERE event_type='Cooling'`; date proxy `DATE(submitted_at)`; key (recipe_id_fk, batch). = Stage 1b, the COGS-critical cooling read. **NB: only the cooling read moved — the `bd_brewing_ingredients_parsed` read in the SAME file is STILL v1 (→ next-wave #1).**
  - `public/modules/packaging.php` 3 KPI queries `bd_packaging` → `bd_packaging_v2` (format→`run_type` enum, `YEAR(event_date)`, `+is_tombstoned=0`). = Stage 1d.
  - **Delta verified + operator-signed-off:** 753 batches identical, **8 corrective** (v1 dup/missing-brew per rule B), e.g. Embuscade 236 32.8 → 128.0 HL. Corrective COGS shift accepted.
- **✅ `262945a` (maltytask Node) — v1 cooling repoint:** `lib/beer-tax.js loadOGByBeer` (OG median) + `scripts/build-efficiency-data.ts` (brewed volume) → `bd_brewing_gravity_v2` Cooling. **🔴 Beer-tax Opus-verified per the COGS-bearing rule: ALL core-beer median OGs UNCHANGED (0.0°P delta), ZERO tax-category shifts** (v2 even drops a suspicious v1 NYL 23.2°P blank-batch outlier). Efficiency: only known dup/missing-brew batches move. **Closes the cooling half of beer-tax + efficiency.**
- **DECOMMISSION STATE NOW:** `bd_brewing_cooling` (v1) has **ZERO remaining COGS/tax/efficiency readers** (only refresh_recipe_profile.py); `bd_packaging` (v1) only reader left = refresh_recipe_profile.py; `bd_brewing_timings` (v1) only reader left = refresh_recipe_profile.py. **3 REMAINING v1 readers next wave (flagged for operator go-ahead):** (1) `bd_brewing_ingredients_parsed` cluster = sku-bom-compile.php ingredients read + db-correct.php (read) + parse_bd_ingredients.py (writer, reads v1 bd_brewing_ingredients + bd_fermenting) — verify ingredients_parsed_v2 canonical+equivalent first, COGS-bearing→Opus verify; (2) `refresh_recipe_profile.py` whole-file → v2 (reads 7 v1 tables; biggest single consumer + the start_ferm consumer → adopt event_date anchor). After 1+2 → zero v1 readers → Stage 3 (stop ingest.py v1 writer) → Stage 4 (snapshot+DROP, operator-gated+soak).
- **✅ `045994d` (maltyweb) — CCT chart tooltip legibility fix** (`tanks.php` modal): tooltip was black-on-black (dark-glass chip had pulled the kraft-paper DARK ink tokens). Swapped to LIGHT tokens (`--bg-elev`/`--bg`/`--ink-faint` + `#7ec8f4` for the CC row). Smoke-confirmed: tooltip legible, FG history shows (J+0 12.1, J+1 10.6). **DURABLE UI RULE recorded into conventions:** the app is a LIGHT kraft-paper theme — `:root` ink tokens are DARK (for a light bg) — so ANY element with a DARK background must use LIGHT text (cream `--bg-elev`/`--bg`, tan `--ink-faint`), never the default ink vars, else black-on-black.
- Memory updated: lean-index pending; `v1-bd-tables-decommission.md` (Stage 1b/1d ✅ + cooling-readers-done block + reader-set trimmed to the 3 remaining + Stage table); `conventions-and-helpers.md` (light-theme dark-element rule).

### 2026-06-11 PM (HISTORIQUE TAB — per-week-per-client historical sales SHIPPED + LIVE)
- **Commit maltyweb `3646e4f` — NOT pushed** (`feat(expeditions): Historique tab — per-week-per-client historical sales (BC views)`). 5 files / 879 insertions: mig `329_sales_ledger_weekly_client_view.sql`; `docs/historical-commandes-read-contract.md` (appended); `public/css/expeditions.css`; NEW `public/js/expeditions-historique.js` (drill collapse); `public/modules/expeditions.php`.
- **4th VIEW `v_sales_ledger_weekly_client` (mig 329) APPLIED on prod — 6,292 client-week rows.** Grain `(YEARWEEK mode-3, customer)`; SAME WHERE as the other 3 historical-Commandes views (shipment-doc, resolved-FG, B2B-scope, cutover < 2026-06-08).
- **8th Expéditions UI view `?view=historique`:** week-blocks → client rows (sorted HL desc) → expandable per-SKU drill (reads `v_sales_ledger_order_lines`), muted unresolved footnote. Read-only, NO status chips, NO CHF (read-contract honored). Reuses the existing ISO-week filter plumbing.
- **🔑 DURABLE GOTCHA:** MySQL `YEARWEEK(d,3)` vs `STR_TO_DATE(CONCAT(YEARWEEK,' Monday'),'%X%V %W')` DISAGREE at some week boundaries → the build filters/joins on the integer `iso_yearweek` bucket DIRECTLY (NOT on the derived `week_start` date), so the weekly view and the SKU-drill window always match. Any future consumer of these views MUST key on `iso_yearweek`, not `week_start`.
- **Deployed SURGICALLY** (scp only the 5 files — tree dirty with packaging.php/tanks.php from other sessions; no blanket deploy). **Authed smoke PASSED:** KW23 (1–7 June) = 364.80 HL / 44 client rows; AlloBoissons SA drill → 8 SKU rows (MOOF/EMBF/ZEPF…); week nav → W22; zero JS errors. Smoketest account (users.id=16) reverted to viewer. Committed by pathspec; tree clean of my files; no foreign sweep.
- **COMPLETES the historical-sales endpoint** Kouros asked for — BC-canonical history now normalised (data lane, mig 326) AND visible per-week-per-client in Expéditions. Carried-forward OPEN: (1) unresolved-SKU review CSV (self-heals); (2) COGS-cutover coupling `489ab70` (allnet-qty / invoice-CHF); (3) the per-DELIVERY historical cards (original read-contract blend) remain optional/unbuilt — only the per-week-per-client aggregate was requested + built.

### 2026-06-11 PM (v1 `bd_*` DECOMMISSION — tanks.php Stage 1f COMPLETE + plan corrected A–D; arc elevated to ASAP)
- **Operator escalation (verbatim): "we NEED to move away from any kind of v1 dependency ASAP."** Arc elevated to standing ASAP priority on `v1-bd-tables-decommission.md`. Still a PHASE OF BSF-EXIT; NO drop authorized.
- **✅ tanks.php Stage 1f DONE — now 100% v1-free** (`grep 'bd_brewing_timings\b'` → 0 hits). Two commits, maltyweb main: `f398163` (fermentation reads/CC cards re-keyed off `beer_raw` onto (recipe_id_fk,batch) on `bd_fermenting_v2` — see the per-commit entry below) **+ `e063ddf` (NEW this session)** = fermentation-start anchor repointed OFF v1 `bd_brewing_timings` ONTO `bd_brewing_timings_v2`; anchor `COALESCE(MAX(STR_TO_DATE(start_ferm,…)), MIN(event_date))` keyed (beer,batch); `start_ferm` all-NULL so falls through to `event_date` (brew date) as ferment day-0 — v2-native, no backfill. **Remove tanks.php from the v1-reader list.**
- **Plan corrections (verified live this session):**
  - **(A) start_ferm backfill LARGELY MOOT** — v2 `start_ferm` 0/2271 NULL by design (v2 form has no separate ferment-start); v1 has gaps AND zero rows for recently-brewed batches (Moonshine 125, brewed 2026-06-09) → v1→v2 backfill wouldn't cover them. ⚠️ `bd_brewing_timings_v2.recipe_id_fk` is NULL on recent rows → timings-v2 reads MUST key on **(beer, batch)**, not recipe_id_fk (separate NULL-FK ingest gap → own ticket, fix at master-data root). Stage 2 re-scoped to a single decision: can `refresh_recipe_profile.py` adopt the `event_date` anchor? If yes → DROP the backfill stage (no migration).
  - **(B) cooling repoint → `bd_brewing_gravity_v2` Cooling col `final_volume` is a CORRECTION, not lossy.** "34 v1-only orphans" = EPH seasonal recipe_id remaps across vintages (exist in v2 under a different rid; join by v2 id/beer, not v1's stale cool_beer_recipe_id). "8 volume mismatches" = v1 dupes + missing brews; v2 is dedup'd + complete; OG (final_gravity) matches v1↔v2. NO `event_date` col on gravity_v2 → date proxy `DATE(submitted_at)`. **COGS/efficiency numbers WILL MOVE (as a fix) → before/after batch-HL delta report to Kouros BEFORE landing** sku-bom-compile.php / build-efficiency-data.ts / beer-tax.js.
  - **(C) Full v1-reader set confirmed both repos:** maltyweb sku-bom-compile.php (cooling + ingredients_parsed) / db-correct.php / packaging.php (×3); maltytask `lib/beer-tax.js` (OG-at-cooling, highest tax blast) + `build-efficiency-data.ts`; Python `refresh_recipe_profile.py` (biggest, repoint FIRST) + `parse_bd_ingredients.py`. KEEP (current canonical, no _v2 suffix): bd_packaging_readings / bd_cip_events / bd_tank_readings.
  - **(D) STANDING RULE:** `beer_raw` write-encoding flipped ~2026-05-29 to bare canonical name + separate batch col → any surface still reverse-parsing 'PREFIX BATCH' silently drops post-May reads; never read v1 (v2 is canonical + cleaner), grep for v1 table names before declaring any `bd_*` read correct.
- Memory updated: lean-index V1 entry (ASAP banner, tanks.php DONE, A–D) + `v1-bd-tables-decommission.md` (Stage 1f DONE, Stage 2 re-scoped, A–D, reader-set, KEEP-list).

### 2026-06-11 (tanks.php fermentation-card RE-KEY fix — beer-identity-fracture instance closed; maltyweb `f398163`, deployed+committed; read-only, no mig)
- **BUG:** `public/modules/tanks.php` fermentation CCT detail cards were frozen/stale (blank fg/pH on live batches). ROOT = the beer-identity-fracture: all **8** fermentation read/cold-crash lookups (3 occ-map stmts, 3 modal-bundle stmts, 2 page-level CTEs `$fermStmt`/`$avgFinalsStmt`) matched identity by reverse-parsing the free-text `beer_raw` in the OLD `"PREFIX BATCH"` shape. The saisie WRITE side flipped `beer_raw` → **bare canonical names** (`"Moonshine"`) ~**2026-05-29**, with identity now in `recipe_id_fk`+`batch`. Every read since late May silently dropped.
- **FIX (commit `f398163`, maltyweb main):** all 8 sites re-keyed to `(recipe_id_fk, batch)` on `bd_fermenting_v2`, with `beer_raw` fallback only when the simulator can't resolve recipe_id (rare sparse edges). recipe_id_fk 100% populated on v2 reads + ColdCrash; simulator recipe_id === bd_fermenting_v2.recipe_id_fk across all current batches. Read-only, no writes/migration. Smoke: Moonshine 125 now shows fg=10.6/pH=4.42 (2026-06-10) where the card was blank.
- **DURABLE — beer_raw write-encoding flip ~2026-05-29:** saisie now writes BARE canonical name into `beer_raw` (was `"PREFIX BATCH"`), identity in `recipe_id_fk`+`batch`. **Any OTHER surface still reverse-parsing `beer_raw` is SUSPECT and silently dropping reads.** Closes a concrete instance of the standing read-side re-key duty (match on recipe_id_fk, never beer name — see [[feedback_match_on_recipe_id_not_beer_name]]). PM standing action: when touching any bd_fermenting/bd_* read, grep for `beer_raw` parsing and re-key to (recipe_id_fk,batch).

### 2026-06-10 (SINGLE-SOURCE COGS REPOINT + FINANCIER PAGE v1; maltytask `489ab70`, maltyweb `6a013ad`; migs 300-305 applied+live, PM-verified)
- **Migration head 299→305** (all applied+verified live 2026-06-10): 300=`ref_customers_sale_class` (COGS taxonomy); 301=`inv_sales_ledger` (44,164-row canonical BC item-ledger 2021-01→2026-06); 302=`cuv_hl_per_unit_lock` (DB trigger locking cuv hl_per_unit=0.01); 303=`inv_side_stock_ledger` (packaging loose-unit remainder + giveaway, commit `1c95e23`); 304=`financier_page` (ref_pages id=239); 305=`kpi_fg_stock_data_ready` (FG-stock KPI trackers data_ready=1).
- **ARC 1 — SINGLE-SOURCE COGS REPOINT (maltytask `489ab70`):** per Kouros "one single source, not many", `lib/repos/mysql-sales.ts loadSales()` now reads ONLY `inv_sales_ledger` (the 44k canonical ledger), retiring the `inv_sales_bc`(6mo)+`inv_sales_orders`(Shopify) union. 2026-04 b2b/taproom/eshop reconcile to ledger 0.00%. Artifact `sales-cogs-data.json` regenerated = 66 months (was 6), deployed VPS. Canonical ledger writer `scripts/python/ingest_bc_sales_ledger.py` committed (was untracked). **Sign convention VERIFIED EMPIRICALLY:** outbound = NEG `qty_signed` AND `hl_resolved` (both negated→positive); `sales_amount_chf` revenue-signed (credits negative, plain SUM nets, never negate). SKU resolved sku_id_fk→ref_sku_aliases→raw (unresolved→`unknownSKUs`, never guessed). **SEAM:** ledger-only FOR NOW — the shipped `sale_class` filter {b2b,eshop,taproom,event,giveaway,staff} INCLUDES eshop+taproom from the ledger today (vs the ruling's plan to exclude); eshop→Shopify and taproom→Lightspeed (custom API requested) peel off to dedicated feeds LATER, isolated behind the filter. COGS shifted (2026-05 salesCOGS ~69,136) = expected+more correct.
- **ARC 2 — FINANCIER PAGE v1 (maltyweb `6a013ad`, mig 304):** read-only finance hub `public/modules/financier.php` (ref_pages id=239, manager/general/sort=125, manager preset). COP←`cogs-report-data.json` via `kpi_load_cogs_json`; COGS←`sales-cogs-data.json` (single-source ledger) via NEW `kpi_load_sales_cogs_json`+`kpi_sales_cogs_month_slice`; Marge/ASP from the COGS slice; Coût-par-SKU links OUT to sku-costs.php. NEVER recomputes. 5.2MB/66-mo artifact NOT injected — `public/api/financier-data.php` = manager-gated lazy per-month-slice endpoint (~43KB HTML); CSS/JS separated+cache-busted+fr-CH fmt; per-module freshness stamp (cron deferred). VERIFIED artifact-exact (COP 2026-04 88.76 CHF/HL; COGS 2026-05 69,135.53), php -l clean, auth gates 302/401/403, viewer denied (screenshot), XSS-safe. **OPEN: manager-authenticated VISUAL smoke NOT done** (auto-mode blocked fixture elevation — Kouros eyeballs as admin / authorizes elevation); **/financier Visite-guidée tour card NOT dispatched** (steward shouldn't deploy from dirty shared tree).
- **⚠️ CONCURRENCY INCIDENT:** a PARALLEL Claude session in the SAME clone (`/home/kluk/projects/maltyweb`) was building an FG-stock KPI tranche (`kpi_fg_*` + mig 305 + Phase-2b). When Financier committed, the SHARED git index swept the parallel session's uncommitted FG suite + mig 305 INTO `6a013ad` (filtered-patch stage raced the parallel stage). All code correct, php -l clean, both migs (304+305) applied — nothing broken, but `6a013ad`'s boundary is MIXED. **No reconciliation needed — leave as-is. LESSON: two sessions committing in one working copy race on the shared index → separate clones/worktrees OR serialize commits.**

### 2026-06-09 (FULFILMENT ITEM 1 — stock-movement saisie + per-site sales attribution + Nausikraft admin; mig 299; 3 commits pushed; autonomous run, Opus-verified)
- **mig `299_fulfilment_site_attribution.sql` APPLIED (head=299):** one atomic file — CREATE `inv_stock_movements` (from<>to + qty>0 CHECKs; FK RESTRICT→ref_skus/ref_sites, user SET NULL; tombstone-only schema_meta) + ALTER `ord_orders.fulfilment_site_id_fk` INT FK ref_sites RESTRICT + ALTER `ref_customers.default_delivery_site_id_fk` INT FK ref_sites SET NULL (Nausikraft mechanism, mirrors default_transporter_id_fk).
- **Phase 1 (`3c3eb33`):** `app/fulfilment-site.php` = THE single resolver `resolve_fulfilment_site(PDO,$ctx)` (precedence per-order override > customer default > channel default; sites DB-derived by role warehouse=id2/pos=id3/production=id1, never hardcoded) + `fulfilment_default_sites()`. Used by BOTH order-create selector AND snapshot attribution → no divergent channel→site map.
- **Phase 2 (`49a0386`):** `fg_stock_location_snapshot()` attributes SALES per site (replicates `fg_stock_compute()` steps 4/5/6 cutoffs exactly, grouped by resolver) + applies `inv_stock_movements` transfers (+to/−from, is_tombstoned=0, moved_on>anchor); global TOTAL unchanged (transfers net-zero); cards may go negative (honest). **Σ(cards)==Σphysique=8642 Opus-verified.** Ship-from selector ("Automatique"→NULL→resolver, or explicit holds_fg site) on `expeditions.php` order saisie + `import_weeklyorders.php`; `reglages-generaux.php ?sec=delivery_sites` admin sets `ref_customers.default_delivery_site_id_fk` (Nausikraft client list, operator enters tomorrow).
- **Phase 3 (`edfcf78`):** `expeditions.php ?view=mouvements` — operators record transfers (CSRF, from<>to + strict int qty>0 + site whitelist, log_revision, PRG), admin tombstone (is_tombstoned=1, never DELETE). E2E: DIV4 Renens→Crissier qty5 shifts split (Zgeg 449→444, Zeubi 39→44), Σ unchanged; tombstone reverts; CHECKs reject from==to/qty<=0.
- **Closes** the long-deferred "sales-leg per-location settlement" carryover (ATTRIBUTION). **Remainder:** sales+transfer cutoffs still on GLOBAL anchorDate (prod leg has the per-site `submitted_at` tiebreak; apply the same ROW_NUMBER per-(sku,site) to sales/transfers). PARKED: ITEM 2 mini side-stock/giveaways (blocked on concurrent form-packaging + migs 293/294 + Kouros D-Q5/6/7); ITEM 3 Phase-2 cage/COGS engine (Shopify pickup-signal ON HOLD + cage anchor Mon 2026-06-15). → fulfilment-expeditions-arc.md §ITEM 1 SHIPPED.

### 2026-06-08 OVERNIGHT (EXPÉDITIONS pre-flip completion — merge pipeline + stock hints + tour content; main==origin @ `6dd5521` PM-verified)
- **Merge-decision pipeline (`9803849` + safety fix `6dd5521`):** 368 needs_review → 4 buckets (138 strong fuzzy / 58 weak / 89 privés / 83 puzzles) → review xlsx w/ DÉCISION cols on Kouros's desktop. Apply chain `scripts/convert_merge_xlsx.py` → JSON → `scripts/apply_merge_decisions.php` (dry-run default, `--apply` single transaction, mirrors client_merge semantics, log_revision actor `'batch-merge-cli'`, idempotent). **Safety ruling: blank decision in sheet A = NON, never merge — a deleted cell must not act.** Fuzzy = suggestions only — false-match exemplar «Arches»→«Marchés» 0.92 preserved in the tables.
- **Saisie live-stock hints (`134bbdf`):** `fg_stock_compute()` → `window.EXP_STOCK_MAP` `{sku_id:{live_futur,physique}}`; advisory amber when qty > live_futur, never blocks (overselling legal — production may be planned); edit mode re-adds the order's own qty; try/catch silent degrade.
- **Tour content (`a59d94d`):** expeditions in `$PAGE_DESCRIPTIONS`/`$PAGE_ICONS` of visite-guidee.php — R7 closed, flip-safe. **Logistics preset: nothing to build** — auth arc pre-wired fulfilment id into manager + logistics_operator presets (id-based, rename carried); activates at flip.
- **Auth smoke deferred:** temp smoke-user creation denied by permission classifier (correct — account provisioning needs operator authorization; permanent smoketest acct id=16 = next-time alternative). Unauthenticated smoke green (302 redirect, lint, fg compute).
- **Morning runbook (Kouros):** xlsx → convert/dry-run/apply → admin click-through → flip `is_active=1` → inventory re-anchor (R2/R3 `fg_stock_compute()` re-exam triggers) → rule on 3 off-trade ambiguous. → fulfilment-expeditions-arc.md §2026-06-08 OVERNIGHT.

### 2026-06-08 (VISITE GUIDÉE chapter expansion — SHIPPED+LIVE+PUSHED `91f8c82`)
- **Operator-directed expansion of the form steps into extensive chapters** ("understand each input, parallel runs, how pertes liquides are computed"). 17 → **25 steps** for production presets; logistics preset unchanged at 11 (no production chapters, no capstone). Chapters: Brassage 3 / Fermentation 2 / Transferts 3 / Conditionnement 3 / capstone "La chaîne des pertes" (production-gated) / Inventaire RM enriched (vide ≠ zéro). Content map stays pure PHP keyed page_key + sub-index — no DB, no migration.
- **PM consultation answers went in essentially verbatim;** the 4 verify-in-code flags resolved against deployed code: (a) cold crash = checkbox in Mesures section (copy adjusted); (b) racking loss labels "Perte cuve départ"/"Perte cuve arrivée", forced comment IS live (outlier when loss_note empty); (c) packaging disposition live labels verbatim ("Perte liquide sans capsule"/"autre"/"à moitié remplie", "Perte capuchon fût"); (d) "+ Ajouter un format parallèle" confirmed.
- **Vetoes enforced + grep-verified on rendered HTML:** zero DB nomenclature (prod_total_units/qte_unites/loss_/bd_/_fk), no inline numeric thresholds ("selon les seuils configurés"), no future promises, no purge-cadence detail.
- **UI:** chapter eyebrow + mini-progress badge ("SAISIE · BRASSAGE 1/3"); compact dot mode >20 steps (5px dots, chapter gaps, ::before tap-zone expansion), 390px no-overflow verified; welcome "3 minutes" → "5 minutes".
- **Verified live** with self-cleaned test users tourx2 (preset 2, 25 steps all visible) + tourx3 (preset 3, 11 steps, no Brassage/capstone) — both tombstoned + deleted same session (audit ids 10860/10861), one login each (fail2ban-aware).
- **QUEUED POLISH:** form-packaging.php ~L2451-2452 helper text shows raw field names `prod_total_units`/`qte_unites` to operators — no-DB-nomenclature violation on the FORM itself (the tour worked around it). Fold into next packaging-form/ui-polish dispatch.

### 2026-06-07 (UI/UX QUALITY REWORK — Phase 3 EXECUTED + LANDED; 🏁 ARC FULLY CLOSED, one carve-out)
- **Commits `e05b18c` (3A hub/SdM/admin/fiche) / `e679efa` (3B sessions/packaging-form/SDC-visual) / `aba178a` (3C dashboards/CCT-modal/sb + app.css) — all pushed, PM-verified main==origin.** 3 parallel Sonnet bundles, then ONE fresh-context adversarial review over the COMBINED diff BEFORE commit (RULE-2 pre-commit, done properly — proven shape for multi-bundle bursts).
- **Review verdict COMMIT AFTER FIXES — 2 P1s caught pre-commit:** PF-04 preventDefault pre-submit gate on the novalidate packaging form REMOVED (silent-block risk; proper a11y = post-server-validation, deferred); PF-03 invalid `role="listbox"`+aria-live on the tank grid → `role="group"` + sr-only `role="status"` summary fed by `buildTankCardsFromData`. P3: wort Filtrer submit 32→44px. 5 hazards cleared (dup main-content id exclusive, CCT-02 reduced-motion net, WO-04 static innerHTML, wort idempotent-GET double-submit, aria-pressed sync).
- **Coverage:** LZ-01/02/04v, SDM-03/04, RG-03/05, SF-03/04, SE-03..06, SH-08, PF-01/02v/03/05, WO-04/05/06, CCT-02/03/04; sdc.css:88 backdrop removed (deferred-P1 closed); sb-sync-ts backdrop removed; smh-header sticky-kept; af-zoom-btn opt-out; mon-tableau FIRST battery pass; brewery literals (classification-appareil + salle-de-controle).
- **Arc closes** with §RESIDUAL BACKLOG (4 items): bom-review polish (the explicit post-F2 carve-out), PF-04 focus-first-invalid on PRG, mon-tableau structural (coordinate w/ KPI-tranche session — kpi-handlers.php/mon-tableau.js), deuteranopia chart-pair pass. Concurrent visite-guidée WIP untouched, zero collisions — the app.css-single-owner rule worked. → ui-ux-quality-rework-arc.md.

### 2026-06-07 POST-MIDNIGHT SYNC (push closed + login.php committed; PM-verified vs repo + live DB)
- **PUSH ✅ DONE — maltyweb main == origin/main @ `6f0e773`** (`git log origin/main..main` = 0). ALL of tonight's commits are on origin. **`public/login.php` COMMITTED via `ca65c22`** (carries BOTH the tour-redirect block and the device-interstitial wiring) — the "deployed-uncommitted shared file" divergence flagged at evening close-out is CLOSED; clean-checkout deploy is safe.
- **Newly-observed commits on main (pushed, not previously changelogged):** `5186454` fix(sku-bom-compile) active-only {beer}-pattern + §8.1 box_label suppression and `9fec7d5` feat(bom-review) clear_sku_choice = the two **Dispatch-D1 commits** (RESUME #0(a0)) — present+pushed, but the D1 data-outcome debrief is PENDING; plus `b676c9b`/`dcbce1d` fg-stock fixes (Expéditions Stock PF) and `1b9694a` mig-280 header-comment fix.
- **Live `ref_sku_bom` census drifted from the recorded ②-state:** Brewing 1472/236.64 (unchanged) but **Packaging 655 rows / 2013.36 CHF** (recorded: 580/2001.45); NULL mi_id still 0; **8 active `-X` cage SKUs now exist** (were scheduled for 06-08); active SKUs = 148. Consistent with D1 purge + cage-SKU creation+recompile in parallel session(s) — ATTRIBUTION PENDING DEBRIEF; do not quote 2001.45 as current.
- **⚠️ Mig 281 TAKEN:** untracked local `db/migrations/281_ref_customers_contact_cols.sql` (parallel CRM customer-bootstrap session; with `data/crm-clients-2026-06-07.{xlsx,json}` + `scripts/convert_crm_xlsx.py`). NOT on VPS, NOT applied (live `schema_migrations` head verified = 280). **NEXT FREE = 282.** This is exactly the parallel-sessions `ls db/migrations` checklist case (rule 17).

### 2026-06-07 EVENING CLOSE-OUT (device interstitial committed + KPI picker bug + terminology relabels + fermentation mini-tranche + smoke E2; all deployed+committed maltyweb main)
- **`94d5a0d` feat(devices): device-classification interstitial COMMITTED** — as-built matches the PM spec: personal=is_shared=0 'Appareil personnel' row; shared=labeled is_shared=1 + NEW `rt_revoke_current()` in remember_token.php (cookie-hash revoke, NOT rt_lookup which rotates); remember-me DEFERRED on unclassified devices via `$_SESSION['pending_remember']` (issue-then-revoke race eliminated per PM ruling); `device_classified()`/`device_mark_personal()` in device.php; outside ref_pages (visite-guidee precedent); ZERO mig. **⚠️ login.php wiring DEPLOYED but UNCOMMITTED** (file shared with parallel tour session — whoever commits next carries both knowingly).
- **`f7fe151` fix(kpi): stale `MT_STUBBED_DOMAINS` const in mon-tableau.php hid ALL 21 new racking/packaging/tanks trackers from the picker for EVERYONE incl. admin** (smoke E2 catch). Truth moved to `kpi_stub_domains()` in kpi-handlers.php ADJACENT to the dispatch match; mon-tableau consumes at 3 gate sites. Verified viewer picker=8 / admin=51. PLUS data fix: `ref_pages.min_role` operator-floor on 12 pages (viewer keeps mon-tableau/sb-board/sb-guerre only) — smoke showed a preset-less viewer could download warehouse GL CSV + reach the live brewing form. Audit row.
- **`b838cca` fix(kpi): operator terminology ruling** — category label **'Soutirage'→'Transferts'** (soutirage = FILLING in house usage); trackers 37/38/39/40/42 mise-en-fût→transfert wording; **34/35 (O₂) recategorized fermentation→packaging** (operator: O2 is a packaging KPI); 41→fermentation 'Temps moyen en CCT (brassin→transfert)', 46→fermentation 'Temps moyen de garde en BBT (transfert→packaging)'. Audit row.
- **`6f0e773` feat(kpi): fermentation mini-tranche, 4 live → census 55.** Operator ruling verbatim: fermentation END = start of cold crash; START = FIRST cooling (first fill/pitch). #31 avg_fermentation_time 10.1j weighted/143 batches/12m (Zepp 13.5j n=37 Opus-verified independently — EXACT); #16 cct_days_per_beer (4840 j-CCT/16 recipes); #18 beers_fermenting_now (14 in CCT, 120d guard); #20 tank_turns_period (4 this month; turns_per_tank=0.22 via ref_cct 18 active CCTs in meta). bd_fermenting_v2 recipe_id_fk+batch 100% on ColdCrash (824 rows, ENUM DryHop|Reads|Purge|ColdCrash).
- **`1d8672b` recap cron ACTIVATED** — as user `maltytask` NOT www-data (www-data can't write /var/log/maltytask → would silently kill every run; **house convention: PHP crons run as maltytask**). Operator confirmed inbox rendering. **`803d3f8` viewer seed-tuning APPLIED** (8 trackers→min_role viewer, operator blessed).
- **Smoke E2** via permanent smoketest account (users id=16, viewer; creds `/home/kluk/projects/maltytask/secrets/maltyweb-smoketest.env` gitignored 600, operator blessed; WSL workaround in webapp-testing skill ref: ssh -L 8443 + Node CONNECT proxy + --proxy-server; login path /login.php). PASS: login/empty-state/recap-no-email/charges-bc gate/MT logo→mon-tableau/390px/wort regression/console clean. FAIL→FIXED: picker 4/8 (f7fe151); viewer CSV/form access (min_role floor). QUEUED: **topbar horizontal overflow at 820px tablet-landscape** (no mid-width breakpoint) → parallel session's topbar a11y arc, do NOT double-edit.
- **Operator data observations awaiting ruling:** Diversion avg fermentation 2.7j (13 batches, raw pairs consistent 1-6d) — real fast-sour or operators logging soured-wort crash? DrunkBeard b1 74d in CCT no racking record — lagering or data gap?

### 2026-06-07 (EXPÉDITIONS ARC — full v1 LIVE, Le Cockpit christened; PM briefed + recorded; detail → fulfilment-expeditions-arc.md)
- **PM pre-approval brief approved by Kouros** (saisie+actionable dashboard / «Expéditions» christens Le Cockpit / full v1 one arc). Built by the parallel session, all deployed/linted/smoke-tested/committed same day.
- **Mig 279** `bbc49fe`: ref_transporters (Galliker/Loxya) / ref_customers (sold-to master, DISTINCT from ref_clients) / ord_orders (status ENUM = CACHE; exactly-one-party CHECK; email-readiness cols) / ord_order_lines (SKU units) / ord_order_status_events (append-only TRUTH, same-tx cache). ref_pages fulfilment→expeditions, **is_active=0 = go-live gate**.
- **Module** `f9535ef` (expeditions.php 3 tabs, one-tx POST, log_revision, PRG, ?edit=, inline new-customer needs_review=1; api/expeditions-status.php rank-map not ENUM order; require_once csrf fix) + **Commandes dashboard** `af6e0ea` (ISO-week + date-range ≤92j GET contract; eshop/taproom read-only auto rows from inv_sales_*; 3 queries) + **Stock PF** `4a8f49e`/`b676c9b` (`app/fg-stock.php` `fg_stock_compute()`: anchor + production ÷units_per_pack [caught a 24× inflation, 942431e class] − ord shipped − eshop inv_sales_orders − taproom inv_sales_bc).
- **Gaps/gates:** depletion gap by design until 06-08 inventory re-anchor (ZEPB 806.58 vs sheet 396); customer bootstrap GATED on CRM xlsx (CRM=roster truth, BC=fiscal links, sheet=enrichment; dry-run v1 done); smoke→flip→logistics preset; Transport Express→ref_transporters; commandes@ ingestion later arc. PM risk flags R1-R7 recorded in the arc file. Mig 280 coordination: taken by visite-guidée session; selective-rsync deploys held.

### 2026-06-07 LATE (UI/UX gravure CALIBRATION ADDENDUM — operator feedback; commits `49981f4` + `c31163f`; arc stays closed)
- **Operator feedback:** BBT fill level unclear on packaging.php; ferment vs cold-crash CCTs indistinguishable on tanks.php.
- **Root cause = z-order PORT BUG, not subtlety:** the `9dcd4ca` PHP port emitted the fill-tint rect BEFORE the opaque paper-body polygon — SVG z-order is document order, so the tint was 100% INVISIBLE (the `_design` spec had correct order; the port inverted it). **DURABLE LESSON (folded into `ui` skill rendering-bug catalog): when porting SVG markup between implementations, verify PAINT ORDER, not just element presence — opacity tuning is meaningless under an opaque sibling.** Also: `svg_vessel_cct` used `--cold` for ferment (wrong accent) → `--hop`.
- **`49981f4`:** paint order fixed in ALL 4 vessel fns (paper→tint→hatch); tint op 0.15→0.34-0.35; surface line sw 1.4 + meniscus ticks; FERMENT = 5 static CO₂ bubbles (reduced-motion safe); COLD = ✻ frost glyph + 4 wall ticks — **states now pass GRAYSCALE distinguishability** (ux-quality §1 "color never the only signal" was being violated, now satisfied); BBT 25/50/75/100% graduation ticks+labels. Elements 59–73, budget fine.
- **`c31163f`:** "100" graduation label clipped to "10" at the viewBox 80px edge → right-anchored x=79.5 `text-anchor=end` (companion lesson in the skill entry).
- Deployed selective rsync, php -l clean, smoke 302, Opus verified visually (color + grayscale VPS-rendered screenshots). This RESOLVES the "`app/svg-tanks.php` modified-uncommitted" working-tree note from the LATE NIGHT entry below. **OPEN: operator confirms tanks.php/packaging.php next visit.** → ui-ux-quality-rework-arc.md §CALIBRATION ADDENDUM.

### 2026-06-07 LATE NIGHT (VISITE GUIDÉE onboarding tour + mig-280 collision remediation + device interstitial + recap cron + Expéditions Stock PF)
- **`ca65c22` feat(onboarding): Visite guidée — first-login guided tour (mig 280).** New `/modules/visite-guidee.php` — `require_login()` only, deliberately NOT in `ref_pages` (no topbar nav). Steps built server-side from `ref_pages` (is_active=1, domain≠admin) × `user_can_access()` — each user sees only their preset's pages. French content map keyed by `page_key` with label-fallback for future pages; deep-dive steps for Mon tableau, Saisies hub (section-opener treatment), the 4 production forms (access-gated), Inventaire RM, a "Bon à savoir" (autosave/keepalive/green-flash), final step → mon-tableau. Preset 2 = 17 steps. Design mockup kept at `public/_design/visite-guidee.html`.
- **Mig 280 `users.tour_seen_at TIMESTAMP NULL`** — marked on first tour LOAD (`UPDATE … WHERE tour_seen_at IS NULL` + audit revision 'Visite guidée : première ouverture'); guarantees exactly one auto-redirect, never a nag loop. PM-verified live: 1/11 users already toured; remaining 10 (incl. Kouros + all existing users) get it exactly once on next login — intended.
- **🔴 MIG-280 NUMBERING-COLLISION REMEDIATION EXECUTED (closes RESUME #10 anomaly):** the untracked `278_users_tour_seen_at.sql` (applied live 20:20) collided with the parallel session's `278_kpi_seed_v2_batch`; remediated = rename → `280_users_tour_seen_at.sql` + paired manual `UPDATE schema_migrations SET filename=…` + **manual `rm` of the stale 278 file on the VPS**. PM-verified live 2026-06-07 ~23:30: `schema_migrations` head = 280 (applied_at 20:20:20 preserved), no stale row. **NEXT FREE = 281.** NEW LESSON: **deploy rsync does NOT delete removed files — a renamed migration leaves its OLD filename live on the VPS where it shows as "pending"; rm it manually** (folded into the checklist mig pre-flight).
- **Login-flow changes** (login.php / set-password.php): default post-auth landing `/` → `/modules/mon-tableau.php` (operators no longer land on the DB-status dev page); `tour_seen_at IS NULL` overrides `next` → tour. set-password: fresh invitees → tour; password RESETS of existing users → mon-tableau (freshUser SELECT extended with tour_seen_at — same incident class as the earlier manager_scope omission: extend the freshUser SELECT whenever a login-flow-bearing column is added). Topbar user panel gained "Visite guidée" re-launch link above "Mes appareils".
- **UI:** `public/css/visite-guidee.css` + `public/js/visite-guidee.js`, kraft tokens only; **grid-overlay step pattern** (all steps grid-row/column 1, inactive `visibility:hidden`) chosen after an overflow-clipping incident with absolute positioning + min-height; inline-SVG vignettes (no emoji — tofu on emoji-less tablets/headless); self-clipping `overflow-x:hidden` on own body class (inactive steps carry translateX(32px) which widened mobile to 425px — caught at 390px verification).
- **Verification:** end-to-end with self-cleaned test user (tourtest2, erased with audit tombstone): first-login redirect / 17 steps / keyboard nav all steps / Ouvrir-cette-page / user-menu re-launch / Terminer→mon-tableau / second-login→mon-tableau all ✓. NOTE: heavy Playwright login testing tripped **fail2ban** — verification agent saw 403 from public IP and wrongly concluded "Tailscale-only by design"; public access confirmed fine afterward (lesson: a 403 mid-smoke may be fail2ban, check the jail before concluding architecture).
- **Sibling landings same evening (PM-verified via git log, recorded here):** `94d5a0d` feat(devices): **first-login device-classification interstitial SHIPPED** (was RESUME #3b, ZERO mig); `1d8672b` feat(cron): **KPI recap dispatcher ACTIVATED** (operator confirmed inbox rendering — closes the "recap cron flip AFTER Kouros confirms inbox" item); `4a8f49e` feat(expeditions): **Stock PF live** — derived FG stock view w/ drill-down ledger (parallel Expéditions session continues, PM still NOT briefed on the module); `f7fe151` fix(kpi): stale `MT_STUBBED_DOMAINS` hid all 21 new racking/packaging/tanks trackers from the picker.
- **⚠️ PUSH STATE at PM verification 2026-06-07 ~23:30: main ahead 2 of origin (`ca65c22` + `f7fe151`)** — the "PUSHED" claim in the dispatch was premature. Working tree: `app/sku-bom-compile.php` (F2 staged work) + `app/svg-tanks.php` modified-uncommitted.
- **Open:** tour content map needs a bespoke French row whenever a new `ref_pages` entry ships (label-fallback exists but bespoke text is better) — standing convention recorded in user-auth-admin-arc.md.

### 2026-06-07 NIGHT (UI/UX QUALITY REWORK — Phase 2 FULLY CLOSED + C2 CONVERGENCE; arc Phases 0+1+2+C2 all closed in ONE day)
- **SdM scene integration (`cd96cd4`):** `salle-des-machines.js` scene painter adopts gravure — `sceneDefs()` 3 hatch patterns (bh_px/bh_pd/bh_pj); `cylGravure()` replaces `cylShade` for SCENE vessels (cylShade KEPT for mini config icons, intentional); strapShadow after every weldSeam; outline weights 1.85/1.1; CUITE calandre interne + reduced-motion-gated steam wisps; whirlpool vortex glyph; colour switch scoped to `svg.sdm-draw` only (--oak-deep/--hairline-2 ink). brewScene ~227 / cellar ~130 elements. Selective rsync (JS+CSS), smoke 302. C1 gate passed → **gravure = the house sketch language.**
- **C2 convergence (`9ed0f6f`):** **`app/svg-tanks.php` = the ONE canonical vessel library.** All four `svg_vessel_*` reimplemented gravure, signatures + CSS hooks verbatim (.sb-vessel--pulsing, .sb-conveyor-lines…), 50–110 elements each; pre-build inventory confirmed only sb-board called kettle/packaging_line, sb-mother = require-guard only. `app/svg-vessels.php` → tombstone + require svg-tanks.php + function_exists no-op stubs → **scrapping-backlog #31** (gate: BSF-exit Phase 6 AND zero `svg-vessels` grep hits). Bundled: **sb-zone__header restored STICKY + FROSTED** (backdrop-filter legit on sticky — composites once; audit deferred-P2 CLOSED). cct-detail-modal token reads verified unaffected. Smoke: sb-board/sb-mother/tanks/packaging non-500; php -l clean local+VPS.
- **Teardown:** `/_design/sketch-vessels-v2.html` + `/_design/sketch-brewhouse-v2.html` removed local + VPS (404 confirmed); gravure spec lives in svg-tanks.php + the arc file.
- **Arc state:** Phases 0+1+2+C2 CLOSED same-day (commits `9104273`/`01c99e3`/`9dcd4ca`/`cd96cd4`/`9ed0f6f` + audit report). Remaining = Phase 3 per-page polish, gated (bom-review→F2; SDC Recettes; kpi-charts/mon-tableau→mig 277; sb-* behavior→atom 11 — **atom-11 smoke doubles as visual acceptance of the sb-* gravure look + frost header**). → ui-ux-quality-rework-arc.md.

### 2026-06-07 EVENING (MON TABLEAU v2 LANDED — mig 278_kpi_seed_v2_batch; 51 trackers live; parallel-session mig 279 expeditions)
- **Commits (maltyweb main, chronological):** `2a8f2b5` recap-guard reads users.email from DB not session (session-payload defect family #3 — auth_login payload carries NO email; admin-added emails now effective without re-login) → `8800a9e` v2 layout (dense 4-col grid w/ viz_type span map; category-grouped dashboard + collapsible grouped picker, one `$CATEGORY_ORDER`/`$CATEGORY_LABELS` PHP source; fr-CH leak fixed ×4 in kpi-charts.js; `fmt()` unit-aware) → `db2da33` 15 sub-pages gated w/ `require_page_access` inheriting parent page_key (triage partials had NO auth bootstrap; salle-fournisseurs flagged no-nav-link → ref_pages registration queued) → `b66244a` REAL MP days-of-supply (v_rm_stock_dynamic CHF ÷ trailing-90d inv_consumption rate, window whitelist {30,60,90,180}, Opus-verified 147.0 j) + maintenance_opex_trend own GL-6100 12-mo series + honesty sweep (28/43 honest, label fix 111, 13 stub-mismatches) → `e8f5336` production tranche: 21 new live handlers (racking 7 / packaging 11 / tank readings 2 / wort yoy) + **mig 278_kpi_seed_v2_batch APPLIED** (relabels 111+266; 13 stubs→data_ready=0 incl. 4 utilities — admin stub-notice clear). **Live: 51 data_ready=1** (census 51/136/81).
- **Opus V1 verification EXACT:** PKG Jun-2026 17/293.0 HL/63,809 u; RACK Jun 4/390.6 HL; Cooling YTD 6,502.8 HL/199 brews; DoS 147.0.
- **Dispositions stays-0:** 6+43 (no yield targets), 47 (no flowmeter), 55 (no target fill), 59/60 (loss per-run), 63 (tank-sim port), 64 (no schedule), 65 (COGS pipeline). **Tracker 57 misread-correction = next-tranche cheap win** (inv_consumption direct-read IS allowed; ban is on bd_packaging×SKU_BOM recompute).
- **⚠️ PARALLEL SESSION + NUMBERING COLLISION:** second session landed `9104273` (ui-ux Phase 1) + `bbc49fe` **mig 279_expeditions_orders APPLIED** (ref_transporters / ref_customers [intentionally distinct from ref_clients, documented in-mig] / ord_orders+lines+status_events; ref_pages fulfilment→expeditions) AND left pending `278_users_tour_seen_at.sql` — DUPLICATE of the applied 278 → rename→280 before applying; **NEXT FREE = 280**. Incident: a mig draft self-INSERTed into schema_migrations → collided with migrate.php bookkeeping → FAIL-after-full-apply; CONVENTION = migrate.php does bookkeeping, NEVER the file.
- **Data fix:** users id=9 'Stphane'→'Stéphane' (audit row). Invites 7/8 SENT; Stéphane unclicked; Joelson waits on mailbox. Phase-3 deployed smoke (webapp-testing) launched, results pending.
- Open: viewer seed-tuning UPDATE (mig 278 §4 commented, awaits blessing); EUR→CHF 0.945 hardcode centralization (view ×2 + DoS handler, together); GL-6100 series thin (only 2026-03 in inv_charges_bc).

### 2026-06-07 PM (UI/UX QUALITY REWORK — Phase 0 + Phase 1 SHIPPED, Phase 2 sketch preview in flight)
- **Phase 0:** 3 `--ease-*` tokens added to app.css `:root` (--ease-out / --ease-in-out / --ease-drawer); full P0–P3 audit at `reports/ui-ux-audit-2026-06-07.md` (35 findings: 1 P0 / 14 P1 / 13 P2 / 7 P3; 10 shared-layer, 25 per-page); scroll-listener ban PASS app-wide.
- **Phase 1 (commit `9104273`, 34 files, ZERO mig):** P0 cleared (skip-to-content + id=main-content × 25 `<main>`); global :focus-visible rings; backdrop-filter off 4 scrolling containers (salle-de-controle.css deferred — frozen arc); form-resilience keepalive on reglages-generaux; wort tab ARIA; SdM live regions; warm shadows; nav-rail real buttons; sessions.php recipe names (label-map rule); touch-action + :active scale w/ reduced-motion net; overscroll-contain × 4; `brewery_identity()` de-hardcoding × 4 pages (SDM footer said Lausanne — truth Renens). php -l clean, 200 × 10 pages.
- **Phase 2 IN FLIGHT:** etched line-art / oak-ink-hatching-on-kraft direction (from Desktop inspiration imgs); preview v1 live `/_design/sketch-vessels-v2.html`; Variante B "gravure dense" being added per Opus density critique; **Kouros density sign-off = integration gate** before svg_cct/svg_bbt redraw (signatures frozen) + svg-tanks↔svg-vessels convergence (RULE-1).
- **Tooling gotcha recorded:** headless Chromium on the WSL box can't reach app.maltytask.ch (Tailscale 100.125.142.25; curl OK, Chromium times out) — curl-to-/tmp + file:// screenshot workaround → skills-and-build-tooling.md webapp-testing entry.
- Full ledger → ui-ux-quality-rework-arc.md §STATUS / PHASE LEDGER.

### 2026-06-07 PM (F2 liquid compiler NUMERICALLY VERIFIED — 4 incidents; whirlpool mig 276; P3 in flight on mig 277)
- **F2 compiler verification chain (ALL dry-run only; ref_sku_bom untouched 2135/2250.003568 after each):** `888473b` dry-hop branch + stage tags → `f99fa2a` inc#2 (v1 dry-hop path joined `bip.source_id` against `bd_fermenting_v2.id` while source_id points into v1 `bd_fermenting` → CROSS-RECIPE contamination, MOO/ZEP fabricated bills + EMB 2×; v1 DH path DELETED — bd_fermenting_v2 DryHop is recipe_id_fk-native + complete for F2 scope, v1-only = 18 contract brews out of scope) → `c9ff4ef` inc#3 (per-MI window → RECIPE-LEVEL shared basisBatches; retired ingredients now age out, EMB DH 1119→~533 g/HL family; per_hl = Σqty ÷ Σ(all basis HL)) → `2d59b4e` inc#4 (skip MAD-rejection when MAD/median<0.05, keep 2×MAD otherwise; missing-DH batches = DATA GAPS fully excluded from basis for any-DH-history recipes — b232 case). Verified reference: EMB basis {225-231,233} ΣHL 944.6; EMBF DH 566.38 g/HL (Amarillo 264.66/Cascade 153.50/Simcoe 132.33 n=7, Mosaic 15.88 n=1) — Opus hand-computed = preview EXACT. DOC edge: 2 DryHop events batch='None' → excluded until re-entered. Apply-gates 11-14 added (same-MI-multi-stage slot_name uniqueness, basis/outlier transparency, DH-alternation semantic guard, review-to-apply staleness). → walkthrough §F2 COMPILER VERIFICATION RECORD.
- **🔴 Standing discipline recorded (3 sub-agent gate-PASS failures in ONE day: TEA lump-sum / DH contamination / per-MI window):** COGS-bearing sub-agent claims are verified by Opus against independently computed references BEFORE reaching the operator → index BUILD-LAUNCH CHECKLIST + conventions-and-helpers.md.
- **Whirlpool + boil-timing** (`2a803d4` + **mig 276**): `hop_addition_stage` +'whirlpool' APPENDED LAST; CONVENTION: stage ordering via explicit FIELD()/rank maps, NEVER ENUM order; whirlpool (≈100°C flameout, no cooling) ≠ hop_stand (cooled to 80°C); `hop_boil_time_min` = boil-minutes REMAINING at addition (60=start, 0=flameout), boil-only, else forced NULL. Operator actively re-attributing hop stages in SDC (EMB live: whirlpool Cascade + first_wort Herkules). → recipe-ingredients-drift-surface.md §HOP-STAGE WHIRLPOOL.
- **IN FLIGHT (single repo-writer dispatch):** SDC recette UX batch (hops ALWAYS render g — kill g→kg auto-promote for hops; process-order hop bill; qty input per BRASSIN ÷ EFFECTIVE brassin incl. per-recipe override → qty_per_hl storage unchanged) + **P3 recap emails = mig 277** `user_kpi_recap_subs` + `send-kpi-recap.php` (reuses Phase-2 compute_handler registry) + DISABLED cron + cadence UI — closes the user-account arc's last build. NEXT FREE mig = 278 after P3 (re-verify `--status`).

### 2026-06-07 (BOM validation steps ①+② EXECUTED — WAC switch + template-enrichment batch) — maltyweb commits, migs 272+273 applied
- **① WAC-costing switch** committed+deployed+recompiled (−13.5% aggregate; was staged-uncommitted in `app/sku-bom-compile.php`).
- **②-A compiler** `c7d2c3e`+`052a8cb`: composite member-container packaging emitted data-driven from member format template+bindings (PD8 112 rows/2.80; PAL 83/6.99; PAC 28/6.91; XMAS 45/3.15); COLLAB4PACK temporal-resolve; explicit-null-override in solo path; composites in cron auto-detect + `sdc_recompile_recipe_packaging` (self-caught `array_column('id')`-vs-`'sku_id'` bug). Gate A: zero diff outside 6 target SKUs, idempotent.
- **②-B data** `c060b54` = **mig 272** (sticker qty 24→1; -B12 sticker explicit-nulls; branded-scotch bindings EMB/MOO/DIV/ZEP + §8.1 auto-suppression; box_label slot fmt1 + 8 label-beer choices; Nomoq intercal suppressed *4C; PKG_TEA_BOT_CH 0.02 + TEA template line on 6 glass formats; LOG_SCOTCH_ESHOP →0.000930; COLLAB12 sticker null; PACKDECX8 deactivated) + **mig 273** (schema: `ref_sku_bom.slot_name` + 4-col unique key + idx_sku_id; `ref_sku_packaging_choices.qty_per_unit` DECIMAL(14,6)). RQ 447/448 auto-resolved.
- **🔥 TEA-WAC poisoning incident (root-fixed same hour):** 11 Univerre TEA inv_deliveries rows were qty=1 lump-sums → WAC 1311.75/unit → first recompile hit 11.88M CHF. Re-granularized to paired-bottle-line qty (invoice_ref+file_id_fk join, rate 0.0214–0.0219 ✓); WAC 0.02033; full recompile 812 del/814 ins, parity 0. Build agent had misdiagnosed rows as misassigned (they were legit TEA, wrong granularity). Lesson → billing-format-and-wac-unit-model.md §LUMP-SUM ECO-FEE ROWS.
- **Post-B defect:** `uses_branded_scotch` recipe-altitude leak onto ZEPC (can box) — fixed via Tier-1 choice id 47 (scotch→TRANSP, sticker restored). Flag → packaging-bom-model.md.
- **FIX 7 BOUNCED:** P25/P50 keg-share — formats 16/17 catalog_id=NULL fail the dbc-commissioning gate. **PM ruling: compiler arm via `ref_packaging_formats.derived_from_format_id` FK (pours inherit commissioning from the keg parent), NOT a fake dbc catalog row.** → sku-bom-validation-walkthrough.md §FIX-7 RULING.
- **Final verified state:** packaging 528 rows/2001.37 CHF (from 1751.92); composite_packaging 83/11.92; brewing 1472/236.64 byte-identical; Gate B ALL PASS; idempotent. COGS delta +249.45 CHF pending operator sign-off.

### 2026-06-06 (P2b — KPI personal dashboard) — 2 commits on `main`, SHIPPED+LIVE, ZERO migration
- `99d5d54` + `2c37c9d` feat(kpi): personal KPI dashboard `public/modules/mon-tableau.php` (min_role=viewer, registered in `ref_pages` via idempotent upsert) — reads `user_kpi_selections`, runs `kpi_dispatch()` per tracker, renders via the new shared `public/js/kpi-charts.js`. Built to all 6 P2b constraints.
- **kpi-charts.js extract:** chart primitives lifted out of `wort-kpis.js` → `public/js/kpi-charts.js` (`window.KpcCharts`, 9 viz_types, pure viz layer — no fetch/recompute). wort.php loads kpi-charts.js BEFORE wort-kpis.js (both `defer`); wort page verified unchanged (constraint #3).
- **Picker = server-filtered + server-revalidated** by domain×role×data_ready; POST rebuilds the allowed-set server-side (`mt_build_allowed_set`) and REFUSES out-of-scope selections — a forged manager-tracker POST by a viewer is correctly rejected (constraint #2 proven). CSRF + log_revision + PRG; NO new compute path (constraint #4).
- **Stub handling (constraint #1):** 4 `utilities` data_ready=1 trackers map to a STUB handler → detected, excluded from picker/render, surfaced to an admin-only notice. This is exactly the seed-vs-implemented-domain mismatch the constraint anticipated (the data_ready=1 set minus the wort/cogs/ops_health/rm_procurement implemented domains).
- **Feed-2 WAC-divergence sub-flag** added to `bom-review.php` (0 rows today, lights up when real) per the interim-guard ruling.
- **Role-filter test:** viewer=0 / manager=30 / admin=39 selectable trackers. **VIEWERS GET AN EMPTY PERSONAL DASHBOARD** — no data_ready=1 tracker has min_role='viewer'. Flagged for the next seed-touch (seed-tuning question: open a few general/ops_health trackers to viewer), NOT a bug.
- ~24 packaging MIs still unpriced in active BOMs (operator catalog-pricing backlog, surfaced in BOM Review Feed 2).
- **Migration head UNCHANGED at 270** (P2b needed none). NEXT FREE = 271, RESERVED for P3 recap mig `user_kpi_recap_subs`.
- **NEXT = P3 KPI recap emails** (PHP CLI sharing kpi-handlers + live `send_mail`, mig 271, cron-disabled-pending-smoke, cadence UI). **PM RULING: P3 WAITS ON ONBOARDING** — can't be smoke-tested end-to-end until users have emails (onboarding) + tracker selections. Agreed with operator.

### 2026-06-01 (Fermenting state-gated selector + hop-stage model) — 3 commits on `main`, committed-NOT-pushed, mig **253** applied + PM-verified live
- `c38d924` feat(recipes): hop addition stage taxonomy on `ref_recipe_ingredients` (mig 253) — ENUM stage + boil_min + 2 generated-stored keys + `uq_recipe_mi_stage` (replaces `uq_recipe_mi`) + ENFORCED CHECK `chk_rri_boil_time`; NO seeding (163 rows NULL). Loader double-count fix (`recipe-ingredients-loader.php` Step-3 dedup-and-sum by mi_id_fk) shipped in same commit.
- `8cc1c73` feat(salle-de-controle): per-hop addition-stage editor in Recettes — inline stage select + conditional boil-minutes + multi-stage add/delete, 3 admin/manager JSON handlers w/ CHECK-invariant + log_revision. JS went into existing inline `<script>` block (debt flagged).
- `e81358e` feat(fermenting): dynamic state-gated beer selector by event type — racking-mirror cards, single TankSimulator + shared sim filter, identity-trap fixed (card = raw `bfw.beer`). Gates live: Reads/ColdCrash=8, DryHop=3, Purge=15, Hors-process=23.
- Open: (1) OR-bridge retirement (DryHop gate ORs canonical stage WITH observed bd_fermenting history — self-retires per recipe as operator classifies; track to drop observed arm). (2) Operator browser smoke-test = final gate before push. NEXT FREE MIG = 255.

