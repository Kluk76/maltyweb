# Build-changelog — 2026-06

> Part of the build-history changelog (rolling, append-only, newest-first). Parent index: [README.md](README.md). Newest 2026-06 entries prepend to the top of this file.

### 2026-06-15 (PLANNING PAGE — Phase 2 dynamic eligibility SHIPPED+COMMITTED; mig 364; commit `da202a9`)
- **✅ PLANNING PAGE NOW FULLY LIVE — both phases.** Phase 1 (manual week calendar on `pl_plan_days`/`pl_plan_items`, mig 364) was already in place; Phase 2 (dynamic process-eligibility) shipped this commit. `da202a9` = 6 files / 2090 insertions.
- **DERIVATION POSITION:** Salle de contrôle / production-scheduling surface. Reads the SOT chain (yeast/garde eligibility, racking, commissioning_settings) to ADVISE the operator which processes are eligible per day; writes ONLY its own plan rows. Does NOT feed COGS/COP/stock/WAC/BOM/beer-tax. Eligibility computation is a pure projection over canonical facts.
- **NEW: `app/planning-eligibility.php` (405L)** — `planning_week_eligibility(PDO $pdo, DateTimeImmutable $weekStart): array`. 🔑 **CARDINAL RULE: pure-read — zero writes anywhere; only SELECTs on bd_fermenting_v2, bd_racking_v2, commissioning_settings, ref_recipes, pl_plan_items.** Algorithm = forward-replay: seed CCT/BBT working state from TankSimulator snapshot → load the week's `pl_plan_items` → walk 7 days, computing eligibility BEFORE applying each day's items, then apply them to mutate working state. In-plan items (even `hors_process=1`) propagate their occupancy effect — a planned racking unlocks packaging on SUBSEQUENT days (same-day gate stays correct: NOT unlocked same-day). Return shape `['YYYY-MM-DD' => ['racking'=>[],'kze'=>[],'dry_hopping'=>[],'packaging'=>[],'brewing'=>['cct_conflicts'=>[]]]]`. GATES: racking = cold-crash date + effective_garde days (resolved via `app/yeast-eligibility.php`); packaging = racked_on + min_days_after_racking (commissioning_settings section='packaging'); kze/dry_hopping = any occupied BBT/CCT.
- **NEW: `public/api/planning-eligibility.php` (34L)** — thin JSON endpoint. require_login() + require_page_access('planning'); reads `?week=YYYY-MM-DD` (read-default-then-validate), snaps to Monday; Cache-Control no-store; json_encode JSON_UNESCAPED_UNICODE | JSON_HEX_TAG | JSON_HEX_AMP.
- **`public/modules/planning.php` (Phase 1→2, 908L total):** requires planning-eligibility.php on GET; computes `$eligibility = planning_week_eligibility()` and hydrates `window.PLANNING_ELIGIBILITY` (JSON_HEX_TAG|JSON_HEX_AMP). Server-side eligibility re-checks on add_wort POST (non-brewing, !hors_process, recipe set → reject if not in eligible set) and add_packaging POST (same). Packaging form persists recipe_id_fk + bbt_number.
- **`public/js/planning.js` (190L):** `populateEligibleSelect()` from `window.PLANNING_ELIGIBILITY[date][process]`; `initWortProcessToggle()` repopulates on non-brewing process change; `__hors_process__` override auto-checks the hors_process checkbox; `initPackagingForm()` populates packaging recipe select + syncs bbt_number hidden field.
- **`public/css/planning.css` (455L):** Phase 2 styles `.pl-eligible-badge` / `.pl-ineligible-warning` / `.pl-elig-select--override` / `.pl-section--override-active`.
- **FUNCTIONAL VERIFICATION (before commit):** (1) Packaging 2026-06-16 → 8 BBTs eligible (Alternative/8 racked 06-15, min_days=1 → PASS). (2) Racking 2026-06-16 → Embuscade/235 (garde=8≥7 PASS), Embuscade/236 (1<7 correctly absent), Moonshine/124 (7=garde PASS). (3) In-plan forward propagation: inserted test racking EMB/236 planned 06-16 → appeared in packaging eligible 06-17 source=in_plan (PASS); same-day gate correct (NOT in packaging 06-16); test row deleted cleanly.
- **PRE-COMMIT REVIEWER BLOCKERS FIXED:** B1 lowerBound was inverted (used today instead of weekStart) → always weekStart. B2 API endpoint missing JSON_HEX_TAG|JSON_HEX_AMP → added. B3 packaging drain matched recipe_id alone → fixed to recipe_id+batch (honors the canonical (recipe_id_fk,batch) keying rule — sql skill #25/#27).
- 🔴 **ARCHITECTURE NOTE for next session:** `ref_pages.planning` is `is_active=0` (operator/general) — the page is NOT reachable by operators yet. Therefore NO tour-steward dispatch is due (RULE 3 predicate requires is_active=1). When the operator activates the page, dispatch the tour-steward and verify topbar visibility. Push-status of `da202a9` per shared-clone discipline — confirm with Kouros.

### 2026-06-15 (QA / HACCP QUALITY-CONTROLS — `/modules/qa.php` SHIPPED+LIVE; migs 358-361; commits LOCAL-only NOT pushed)
- **✅ FIRST REAL BUILD ON THE `qa` PAGE — SHIPPED+LIVE+SMOKE-PASSED.** `qa` was an inactive `ref_pages` placeholder (id 11) with pre-written tour copy; mig `361_activate_qa_page.sql` flipped `is_active=1, href='/modules/qa.php'`. All on PM-recommended defaults; operator granted "go for it".
- **DERIVATION POSITION:** Salle de contrôle / QA-QC family. 🔴 **NON-FISCAL observation layer — reads canonical production facts, writes only its own observation rows, NOTHING reads back into the SOT chain (NEVER feeds COGS/COP/stock/WAC/BOM/beer-tax).** Dropdowns derive BY FK from real canonical tables, never string-copy: net-content→`bd_packaging_v2` LEFT JOIN `ref_skus`; cleaning-efficacy→`bd_cip_events` JOIN `ref_cip_types`; bottle-reception→`inv_deliveries` (status not cancelled/tombstoned); packaging-MI→`ref_mi WHERE category_id=8` (Packaging, confirmed via `ref_mi_categories`).
- **MIGRATIONS (maltyweb, committed local, applied+VPS-verified):** `358_qa_net_content_readings.sql` / `359_qa_cleaning_efficacy_checks.sql` / `360_qa_bottle_reception_checks.sql` — 3 `source`-class / `corrections_policy='allowed'` tables, `schema_meta` rows written, `writer_script='/modules/qa.php'`. FK types verified live: packaging/cip/delivery = BIGINT UNSIGNED; mi/user = INT UNSIGNED. Commit `0b52f13`. + `361_activate_qa_page.sql` (`eac31da`). ⚠️ benign mig-number collision: a parallel session also created `361_ord_orders_bc_and_maltytask_source.sql` — migrate.php tracks by filename so both coexist; theirs to commit. (Live HEAD now 364 — parallel sessions also applied 357 + 362-364.)
- **HANDLERS (async JSON, mirror `rm-stocktake-line-add.php`):** `public/api/qa-net-content.php` / `qa-cleaning-efficacy.php` / `qa-bottle-reception.php`. Envelope `{ok:true,id,…}` / `{ok:false,error}` / `{ok:false,reason:'expired',csrf}` / duplicate→`{ok:true,duplicate:true}`. Each: csrf_verify + `require_page_access('qa')` + audit revision + row_hash idempotency + read-`??`-then-validate + ENUM whitelist + FK-existence checks. Shared `parse_nullable_decimal()` added to EXISTING `app/db-write-helpers.php` — single canonical def, NOT copied (honors call-the-accessor rule). Commits `3ede620` → `01728e2` (PRG→JSON refactor).
- **UI:** `public/modules/qa.php` (747L) + `public/css/qa.css` (297L) + `public/js/qa.js` (314L) — 3 stacked panels, optional `?view=net|cip|recep`, async fetch + DOM prepend, escHtml, external CSS/JS (no inline). Commit `8734849`.
- **TOUR:** card for `qa` shipped by tour-steward (`f8478da`; + bonus journal-saisies card). Tour gap CRITICAL 2→1; remaining critical = `financier` (steward correctly flagged PM-RATIFY sensitive-COGS class → belongs to COGS-fiche arc, NOT this build).
- **SMOKE (webapp-testing PASSED):** all panels render; dropdowns A:50 / B:30 / C:50,125; 3 write round-trips OK; bad-enum rejected (400+JSON, NOT 500); test rows self-cleaned COUNT=0; test acct id=16 elevated→reverted.
- **DESIGN DEFAULTS LOCKED (non-destructive, operator may revisit):** weight=g / volume=mL via single `measured_value` + `measure_type` discriminator; `surface_label` free-text (no `ref_qa_surfaces` lookup); per-reading `target_value`+`tolerance_abs` (no central `qa_targets` master); `is_conforming` derived-at-write.
- **STATUS:** maltytask+maltyweb commits LOCAL-only NOT pushed (cage-session commits interleaved in shared clone — pushing would carry their in-flight work; deferred to Kouros). Work fully LIVE on VPS.
- **OPEN/FOLLOW-UP:** (1) panel-C MI dropdown shows whole Packaging cat (125) — tighten to glass/bottle if operator asks; (2) Feature A inline capture on conditionnement fiche (Q3 phase-2) NOT built — standalone qa-page only; (3) `ref_qa_surfaces` lookup + central `qa_targets` master = future normalization; (4) `financier` tour card still open (its arc owns it). → qa-haccp-controls-arc.md.

### 2026-06-11 (ingest-root ticket CLOSED — `bd_brewing_timings_v2` NULL recipe_id_fk FIXED; commit `2037e2f` maltyweb + mig 334)
- **✅ CLOSED — the open ingest-root ticket (NULL `recipe_id_fk` on `bd_brewing_timings_v2`) is FIXED at the writer + backfilled.** This was the last data-gap forcing the v1-decommission timings reads to key on (beer,batch) instead of recipe_id_fk.
- **ROOT CAUSE:** `public/modules/form-brewing.php` (the brewing-saisie writer) built every sibling v2 row — `bd_brewing_brewday_v2`, `bd_brewing_ingredients_v2`, `bd_brewing_gravity_v2` (×4) — with `'recipe_id_fk' => $recipeId`, but OMITTED it from the `$timingsRow` for `bd_brewing_timings_v2`. So every brewing saisie since the v2 form went live wrote a NULL-FK timings row — **28 live rows, all 2026 core beers: Zepp 215/216/217, Moonshine 124/125, Embuscade 235/236.** (This is why tanks.php `e063ddf` + refresh_recipe_profile.py `ed41319` had to key timings on (beer,batch).)
- **FIX (two parts, both in `2037e2f`):** (1) **WRITER (root):** added `'recipe_id_fk' => $recipeId` to `$timingsRow` — future timings rows carry the FK. `php -l` clean, deployed. (2) **BACKFILL:** mig `334_backfill_timings_recipe_id_fk.sql` — UPDATE the 28 NULL rows from the canonical `bd_brewing_brewday_v2` sibling per (beer,batch), `MAX(recipe_id_fk)`, guarded by `recipe_id_fk IS NULL` (never overwrites, idempotent). Dry-run: all 28 resolve unambiguously (brewday rid == ref_recipes base rid; 0 ambiguous, 0 unresolved).
- **APPLIED via `migrate.php`** — ⚠️ run as `sudo -u www-data` (config/db.env is www-data-readable only; a bare `php scripts/migrate.php` as the ubuntu SSH user FATALS on db.env permissions). **PM-verified live 2026-06-11: NULL recipe_id_fk 28 → 0 of 2271. migrate.php --status shows `334_backfill_timings_recipe_id_fk.sql` ✓ / 0 pending.**
- **FOLLOW-ON (optional, NOT done):** now that `bd_brewing_timings_v2.recipe_id_fk` is 100% populated, the tanks.php + refresh_recipe_profile.py timings reads (currently keyed on (beer,batch) per `e063ddf`/`ed41319`) COULD upgrade to key on recipe_id_fk for robustness. NOT required — (beer,batch) works; leave the current keying in place. Detail → v1-bd-tables-decommission.md.

### 2026-06-11 (recipe-profile cron CORRECTION + HARDENING; commit `b9016d0` maltyweb)
- **✅ GROUND-TRUTH CORRECTION — the recipe-profile cron was ENABLED all along.** The earlier "DISABLED pending manual smoke / operator must re-enable" note (from the C2 agent, repeated in the session wrap-up + PM memory) was WRONG. `/etc/cron.d/maltytask-recipe-profile` runs nightly at 03:00 UTC; the "operator must re-enable" action item is MOOT and has been scrubbed from PM memory (index + topic file + the Stage-3 changelog entry above). VPS-verified.
- **Smoke-passed** `refresh_recipe_profile.py` as the maltytask cron user: 61 recipes, 122 profile rows, 146 malt / 133 hop rows, 0.8s, 0 errors. One `--apply` primed `ref_recipe_profile`.
- **DROPPED the cron's obsolete step 1** (`parse_bd_ingredients.py --apply`) — that was the legacy v1 free-text parser (reads v1 `bd_brewing_ingredients`/`bd_fermenting` → writes v1 `bd_brewing_ingredients_parsed`). Since `refresh_recipe_profile.py` now reads `..._parsed_v2` (`ed41319`) AND v1 is frozen (bsf-ingest disabled, `0449a95`), step 1 is obsolete and would touch frozen v1. Cron is now **refresh-only, still enabled.**
- **Fixed the stale header** — the script has NO `--dry-run` flag; omit `--apply` to preview.
- **Stage-4 consequence:** `scripts/python/parse_bd_ingredients.py` is now referenced by NO cron and NO consumer (its only output, v1 `bd_brewing_ingredients_parsed`, is read only by the deferred `db-correct.php` branch). Added to the Stage-4 v1 removal checklist alongside `ingest.py`/`tab_*.py`. Detail → v1-bd-tables-decommission.md.

### 2026-06-11 (v1 `bd_*` DECOMMISSION — STAGE 3 DONE: v1 WRITER STOPPED; commit `0449a95` maltyweb)
- **✅ STAGE 3 — STOP THE v1 WRITER — DONE (commit `0449a95`, maltyweb).** v1 `bd_*` tables now receive NO new writes and (post Stage 1) have NO readers → **FROZEN, intact.** Only Stage 4 (snapshot → soak → DROP) remains, gated on a soak + final re-grep + operator go-ahead.
- **Operator CONFIRMED (2026-06-11):** ALL brewing/fermenting/racking/packaging data entry has fully moved to the maltyweb **saisie forms** (which write `bd_*_v2` directly). The **BSF Google Forms are RETIRED** → this **CLEARS the BSF-exit Phase 6 operator hard-gate for these tables.**
- **The v1 writer** = the `maltytask-bsf-ingest` cron (`/etc/cron.d/maltytask-bsf-ingest`, source `db/cron/maltytask-bsf-ingest.cron`): every 30 min, BSF Sheets → `ingest.py --tab=all --apply` → v1 `bd_*` tables. **NOW DISABLED** — active line commented with a dated rationale, deployed, **VPS-verified (no active bsf-ingest line remains).** The daily ref-table sync lines in that cron were already disabled since 2026-05-12.
- **v1↔v2 reconciliation run BEFORE disabling — CLEAN:** v2 ≥ v1 everywhere except brewday (835 v1 / 831 v2), cooling (2322 / 2286), timings (2306 / 2271). The v1-only rows there are **ALL 2021-2022 contract/collab brews with `batch=NULL`** (BLZ, Chien Bleu, Brasserie du Château, NYL, Les Combières, MeltingPote, BadFish) — legacy un-batched aggregation artifacts that **v2 already normalized under proper batch numbers, read by NO consumer.** No real data lost.
- **STAGE STATUS NOW:** Stage 1 (repoint all readers) ✅ DONE; Stage 2 (start_ferm backfill) MOOT (event_date anchor); Stage 3 (stop v1 writer) ✅ **DONE (`0449a95`)**; Stage 4 (snapshot → soak → DROP v1 `bd_*` + remove db-correct.php v1 branch + validate_fk_candidates refs + schema_meta cleanup) GATED on a ≥1-2wk soak now ticking + a final full both-repo re-grep + operator go-ahead.
- **For Stage 4 (when the gate clears):** re-run the FULL both-repo v1-reader grep (the tank-sim miss proved this is mandatory); snapshot each v1 `bd_*` table before DROP; remove the `db-correct.php` v1 `ingredients_parsed` UPDATE branch (its only remaining v1 reference) + `validate_fk_candidates.py` dev refs; schema_meta cleanup; soak ~1-2 weeks watching for operator-visible issues before DROP.
- **~~Still open (separate, NOT gating Stage 4): the `bd_brewing_timings_v2` NULL `recipe_id_fk` ingest-root ticket (fix at the writer).~~ ✅ NOW CLOSED — `2037e2f` + mig 334 (see the top-of-file entry).** **NOTE: the previously-listed "disabled `refresh_recipe_profile` cron, operator must re-enable" item was a WRONG claim — corrected `b9016d0` (see below). The cron was ENABLED all along.** Full plan → v1-bd-tables-decommission.md.

### 2026-06-11 (OBSERVED YEAST ON BBT CARDS — render-only; 2 files UNCOMMITTED per operator; NO mig)
- **✅ SHIPPED + DEPLOYED + SMOKE-VERIFIED.** BBT occupied-card now surfaces observed yeast (strain + generation) from `bd_brewing_brewday_v2`, matching the CCT board nomenclature. Downstream of tank-sim occupancy; **independent of the paused strain-classification arc — reads OBSERVED brewday data, NOT the `ref_recipes` strain link** (observed-wins).
- **Derivation = verbatim copy of the CCT board query** (`tanks.php` L338-345, v2-canonical): `SELECT yeast AS bd_yeast, yeast_gen AS bd_yeast_gen FROM bd_brewing_brewday_v2 WHERE beer=:beer AND batch=:batch ORDER BY event_date DESC LIMIT 1`. `$bbtYeastStmt` prepared ONCE (packaging.php ~L105) before the BBT loop, executed per-iteration on `$rawBeer`+`$batch` → `$bbtOccMap[$num]` `'yeast'`/`'yeast_gen'`. Single-source nomenclature, no parallel read.
- **Render:** occupied-card `else:` block (~L483) → guarded muted chip `<span class="tank-card__yeast tanks-mute">Levure · {strain} (G{gen})</span>`, htmlspecialchars-escaped, only when strain non-empty (refuse-don't-NULL), between blend line + rack date. CSS `public/css/app.css` ~L3943 `.home .tank-card__yeast` (JetBrains Mono 10px, sibling muted-sub style; no inline CSS).
- **Verify:** `php -l` clean; `bin/deploy.sh --apply` succeeded (4 parallel-session dirty files byte-identical local↔VPS, clean no-op rsync — no foreign-dirt leak this run); Playwright smoke = all 7 occupied BBTs carry chip with real DB-matching values (Zepp #212 → W34/70 G12; US-05 G1, Verdant G2); empty BBT #1 no chip; no 500.
- **Files (2, UNSTAGED per operator):** `public/modules/packaging.php`, `public/css/app.css`. NO mig, NO writes, no ref_pages → NO tour-steward dispatch. Architecturally clean: single-source observed read, refuse-don't-NULL honored, zero COGS/tax surface.
- **⚠️ DATA-ENTRY SMELL (future cleanup, NOT this build):** one occupied BBT's observed strain = literal `"New Yeast"` (gen 7) — placeholder/free-text in `bd_brewing_brewday_v2.yeast`, not a real strain. Render faithful; fix at the brewday-form root. Detail → packaging-page-dashboard.md §OBSERVED YEAST ON BBT CARDS.

### 2026-06-11 (MON TABLEAU KPI dashboard — multiple tranches; commits `b6db62c`,`fb2778a`,`bc678af`,`cdff109` on `main`, NOT PUSHED)
- **✅ TANK-OCCUPANCY TRANCHE — 4 tiles #13/#14/#15/#17 SHIPPED+DEPLOYED+VERIFIED (mig 333; census 57→61 live).** All 4 consume canonical `TankSimulator::run()` via shared per-request helper `kpi_tanks_get_sim_state()`, READ-ONLY zero-writes (no third occupancy computation — DIVERGENCE FLAG honored). #13 `cct_bbt_occupancy` viz `kpi_number`→`bar` (80.8%, 21/26 vessels, 2-bar CCT/BBT split — build added `opts.xLabels` to `buildBarChart`/`renderKpiBar`); #14 `cct_capacity_utilization` 92.3% (1549.9÷1680 HL active CCT cap, amber>90%, div-0 guard); #15 `cct_idle_days` mean 9j/max 24j CCT6; #17 `hl_in_tank` 1846.2 HL physical NOT valued. **#15 idle-source: derives source CCT from brewday batch→CCT map (`bd_brewing_brewday_v2.cct` via `loadBatchCCT`) NOT `bd_racking_v2.cct_number`; handler mirrors (join racking→brewday, group `b.cct`, MAX(event_date)). First cut keyed (beer,batch) → review-caught → fix `cdff109` re-keyed `(COALESCE(neb_recipe_id_fk,contract_recipe_id_fk),batch)` — DO NOT simplify back to cct_number/beer-name.** CAPACITY live-confirmed: `ref_cct` 18 active Σ1680 HL (old 1530 spec was STALE), `ref_bbt` 8 active Σ870 HL (active = `status='active'` NOT `is_active`). **Node-port gate CLARIFIED: lifted for read-only occupancy DISPLAY; applies only to flagship recommendation engines #23/#64 + WIP-valuation #181.** Catalog L109/L115/L344 corrected. Detail → kpi-tracker-catalog.md §TANK-OCCUPANCY TRANCHE.
- **✅ widgets #2 `hl_brewed_ytd` + #5 `production_by_beer_yoy` — identical-chart bug FIXED (mig 331 `grouped_bar` viztype).** #2 stays sparkline (12-elem monthly-cumulative YTD + `meta.prev_series` N-1 ghost, cooling-anchored recipe_id_fk join unchanged, 6832.3 HL ▲+11.4%); #5 → NEW `viz_type='grouped_bar'` "Production par bière — YTD vs N-1", `renderKpiGroupedBar` consumes per-beer `breakdown[]` (17 beers, Zepp 2453.7 ▲+10%); `fb2778a` truncated ghost to `lastDataMi`. ⚠️ `fb2778a` inadvertently swept in pre-staged unrelated **mig 332 `cogs_fiche_seed_and_monthly`** (NOT applied on VPS) from a concurrent COGS-fiche session. Detail → kpi-tracker-catalog.md §widgets #2+#5 FIXED.
- **Other 2026-06-11 arcs (full detail in their thematic files, NOT duplicated here):** ORDER-AWARE COVERAGE fast-follow #1 (`sb_forward_sim`, per-SKU couverture face, ALTF 14.4→1.86wk) → fulfilment-expeditions-arc.md §ORDER-AWARE COVERAGE; FORWARD SEASONAL BURN ENGINE (mig 325, `app/seasonal-burn.php` 8-fn shared lib, `scripts/seasonal-index-cli.php`, cron DISABLED-pending-Kouros, global peak ISO-wk26) → fulfilment-expeditions-arc.md §SEMAINES-RESTANTES; HISTORIQUE TAB (mig 329 `v_sales_ledger_weekly_client`, key on `iso_yearweek` NEVER `week_start`, commit `3646e4f`) → fulfilment-expeditions-arc.md §HISTORIQUE; CIP CADENCE ON BBT CARDS (mig 328, `bbt-detail-modal.js`/`.css`, STR_TO_DATE err-1411 UPDATE gotcha → fix `cip_date REGEXP '^[0-9]{4}-'` pre-filter) → packaging-page-dashboard.md §CIP CADENCE ON BBT CARDS.

### 2026-06-11 (v1 `bd_*` DECOMMISSION — FINAL READER WAVE DONE; all v1 CONSUMERS now on _v2; 3 commits)
- **✅ `b17c686` (maltyweb) — sku-bom-compile.php §2b observed malt/hops → `bd_brewing_ingredients_parsed_v2`** (header_id → `bd_brewing_ingredients_v2`). This was the last COGS-critical v1 read in the compiler (its cooling read moved in `aa6fb0f`; now the ingredients read too). Verification: **0 v1-only keys lost, 75 v2-only gained, 1496 identical.** The **6 differing keys were a v1 DATA BUG** — v1 stored 1g/2g/5g for hops additions physically 1/2/5 **kg**; v2 has the correct 1000/2000/5000 g → repoint **CORRECTS (raises) observed hops COGS for 6 batches** (rid 51 b45, rid 57 b126 ×4, rid 30 b48). **sku-bom-compile.php is now FULLY off v1** (batch-HL + malt/hops + dry-hop all v2).
- **✅ `ed41319` (maltyweb) — `scripts/python/refresh_recipe_profile.py` → all 7 v1 tables to _v2** (the biggest single v1 consumer). Notable structural fixes folded in: removed the fragile `RECIPE_TO_PREFIX` + `beer_raw`-LIKE prefix parsing (the rule-D identity-fracture hazard) → now **JOIN `bd_fermenting_v2` → `ref_recipes` on `recipe_id_fk`**; gravity LONG→WIDE via `event_type`→column map; packaging `pct_loss` derived from `loss_kpi_hl/objective_hl` + O2/CO2 from `bd_packaging_readings` child. **Dry-run clean across 61 recipes; output stable.** 🔴 **ITS CRON (`/etc/cron.d/maltytask-recipe-profile`) IS DISABLED pending manual smoke — OPERATOR ACTION ITEM: smoke + re-enable** (same recipe-profile-cron convention as the seasonal-index cron).
- **✅ `face01f` (maltytask) — `scripts/parse-tank-simulation.ts` cooling read → `bd_brewing_gravity_v2` (`event_type='Cooling'`)**, aliased back to `cool_*` so `CoolingRow`/downstream are unchanged. This was a reader MISSED in the earlier wave (an earlier grep truncated); now caught — reinforces the missing-reader-hunt standing concern (always re-grep the full both-repo tree).
- **🟢 DECOMMISSION STATE NOW — ALL v1 CONSUMERS REPOINTED ✅** (verified by full both-repo grep): tanks.php, sku-bom-compile.php, packaging.php, beer-tax.js, build-efficiency-data.ts, refresh_recipe_profile.py, parse-tank-simulation.ts. **Remaining v1 references are NOT consumers:**
  - **v1 WRITER chain (Stage 3 — the thing to STOP, GATED on operator):** `scripts/python/ingest.py` (writes v1 bd_brewing_brewday/cooling/gravity/ingredients/timings/fermenting/packaging; also a dedup SELECT on bd_packaging) + `scripts/python/parse_bd_ingredients.py` (reads v1 bd_fermenting + bd_brewing_ingredients, writes v1 bd_brewing_ingredients_parsed) + `tab_*.py` builders.
  - **Deferred (intentional):** `app/db-correct.php` (admin-only; UPDATEs `bd_brewing_ingredients_parsed.mi_id_fk` — alias corrections on the v1 table; becomes moot at DROP → remove then) + `scripts/python/validate_fk_candidates.py` (dev FK-audit utility, no cron — reads bd_racking/bd_brewing_brewday).
  - `app/loss-metrics.php:42` = a comment, not a query.
- **STAGE STATUS:** Stage 1 (repoint all readers) ✅ **DONE**; Stage 2 (start_ferm backfill) **MOOT** (anchored on `event_date`); Stage 3 (stop v1 writer = ingest.py + parse_bd_ingredients.py + tab_*.py) **READY — zero consumers depend on v1 reads — GATED on operator go-ahead**; Stage 4 (snapshot → soak → DROP v1 bd_* tables + remove db-correct v1 branch + schema_meta cleanup) **GATED on Stage 3 + operator**.
- **~~Separate ingest-root ticket: recent `bd_brewing_timings_v2` rows have NULL `recipe_id_fk` — fix at the writer.~~ ✅ CLOSED 2026-06-11 — `2037e2f` + mig 334** (writer `form-brewing.php` omitted the FK from `$timingsRow`; 28 NULL rows backfilled; PM-verified 28→0 of 2271). See top-of-file entry.

### 2026-06-11 (v1 `bd_*` DECOMMISSION — Stage 1b/1d cooling+packaging repoints DONE + CCT tooltip UI fix; 3 commits)
- **✅ `aa6fb0f` (maltyweb) — v1 cooling + packaging repoint.**
  - `app/sku-bom-compile.php` batch-HL (the COGS per-HL **denominator**) `bd_brewing_cooling` → `bd_brewing_gravity_v2 WHERE event_type='Cooling'`; date proxy `DATE(submitted_at)`; key (recipe_id_fk, batch). = Stage 1b, the COGS-critical cooling read. **NB: only the cooling read moved — the `bd_brewing_ingredients_parsed` read in the SAME file is STILL v1 (→ next-wave #1).**
  - `public/modules/packaging.php` 3 KPI queries `bd_packaging` → `bd_packaging_v2` (format→`run_type` enum, `YEAR(event_date)`, `+is_tombstoned=0`). = Stage 1d.
  - **Delta verified + operator-signed-off:** 753 batches identical, **8 corrective** (v1 dup/missing-brew per rule B), e.g. Embuscade 236 32.8 → 128.0 HL. Corrective COGS shift accepted.
- **✅ `262945a` (maltytask Node) — v1 cooling repoint:** `lib/beer-tax.js loadOGByBeer` (OG median) + `scripts/build-efficiency-data.ts` (brewed volume) → `bd_brewing_gravity_v2` Cooling. **🔴 Beer-tax Opus-verified per the COGS-bearing rule: ALL core-beer median OGs UNCHANGED (0.0°P delta), ZERO tax-category shifts** (v2 even drops a suspicious v1 NYL 23.2°P blank-batch outlier). Efficiency: only known dup/missing-brew batches move. **Closes the cooling half of beer-tax + efficiency.**
- **DECOMMISSION STATE NOW:** `bd_brewing_cooling` (v1) has **ZERO remaining COGS/tax/efficiency readers** (only refresh_recipe_profile.py); `bd_packaging` (v1) only reader left = refresh_recipe_profile.py; `bd_brewing_timings` (v1) only reader left = refresh_recipe_profile.py. **3 REMAINING v1 readers next wave (flagged for operator go-ahead):** (1) `bd_brewing_ingredients_parsed` cluster = sku-bom-compile.php ingredients read + db-correct.php (read) + parse_bd_ingredients.py (writer, reads v1 bd_brewing_ingredients + bd_fermenting) — verify ingredients_parsed_v2 canonical+equivalent first, COGS-bearing→Opus verify; (2) `refresh_recipe_profile.py` whole-file → v2 (reads 7 v1 tables; biggest single consumer + the start_ferm consumer → adopt event_date anchor). After 1+2 → zero v1 readers → Stage 3 (stop ingest.py v1 writer) → Stage 4 (snapshot+DROP, operator-gated+soak).
- **✅ `045994d` (maltyweb) — CCT chart tooltip legibility fix** (`tanks.php` modal): tooltip was black-on-black (dark-glass chip had pulled the kraft-paper DARK ink tokens). Swapped to LIGHT tokens (`--bg-elev`/`--bg`/`--ink-faint` + `#7ec8f4` for the CC row). Smoke-confirmed: tooltip legible, FG history shows (J+0 12.1, J+1 10.6). **DURABLE UI RULE recorded into conventions:** the app is a LIGHT kraft-paper theme — `:root` ink tokens are DARK (for a light bg) — so ANY element with a DARK background must use LIGHT text (cream `--bg-elev`/`--bg`, tan `--ink-faint`), never the default ink vars, else black-on-black.
- Memory updated: lean-index pending; `v1-bd-tables-decommission.md` (Stage 1b/1d ✅ + cooling-readers-done block + reader-set trimmed to the 3 remaining + Stage table); `conventions-and-helpers.md` (light-theme dark-element rule).

### 2026-06-11 PM (HISTORIQUE TAB — per-week-per-client historical sales SHIPPED + LIVE)
- **Commit maltyweb `3646e4f` — NOT pushed** (`feat(expeditions): Historique tab — per-week-per-client historical sales (BC views)`). 5 files / 879 insertions: mig `329_sales_ledger_weekly_client_view.sql`; `docs/historical-commandes-read-contract.md` (appended); `public/css/expeditions.css`; NEW `public/js/expeditions-historique.js` (drill collapse); `public/modules/expeditions.php`.
- **4th VIEW `v_sales_ledger_weekly_client` (mig 329) APPLIED on prod — 6,292 client-week rows.** Grain `(YEARWEEK mode-3, customer)`; SAME WHERE as the other 3 historical-Commandes views (shipment-doc, resolved-FG, B2B-scope, cutover < 2026-06-08).
- **8th Expéditions UI view `?view=historique`:** week-blocks → client rows (sorted HL desc) → expandable per-SKU drill (reads `v_sales_ledger_order_lines`), muted unresolved footnote. Read-only, NO status chips, NO CHF (read-contract honored). Reuses the existing ISO-week filter plumbing.
- **🔑 DURABLE GOTCHA:** MySQL `YEARWEEK(d,3)` vs `STR_TO_DATE(CONCAT(YEARWEEK,' Monday'),'%X%V %W')` DISAGREE at some week boundaries → the build filters/joins on the integer `iso_yearweek` bucket DIRECTLY (NOT on the derived `week_start` date), so the weekly view and the SKU-drill window always match. Any future consumer of these views MUST key on `iso_yearweek`, not `week_start`.
- **Deployed SURGICALLY** (scp only the 5 files — tree dirty with packaging.php/tanks.php from other sessions; no blanket deploy). **Authed smoke PASSED:** KW23 (1–7 June) = 364.80 HL / 44 client rows; AlloBoissons SA drill → 8 SKU rows (MOOF/EMBF/ZEPF…); week nav → W22; zero JS errors. Smoketest account (users.id=16) reverted to viewer. Committed by pathspec; tree clean of my files; no foreign sweep.
- **COMPLETES the historical-sales endpoint** Kouros asked for — BC-canonical history now normalised (data lane, mig 326) AND visible per-week-per-client in Expéditions. Carried-forward OPEN: (1) unresolved-SKU review CSV (self-heals); (2) COGS-cutover coupling `489ab70` (allnet-qty / invoice-CHF); (3) the per-DELIVERY historical cards (original read-contract blend) remain optional/unbuilt — only the per-week-per-client aggregate was requested + built.

### 2026-06-11 PM (v1 `bd_*` DECOMMISSION — tanks.php Stage 1f COMPLETE + plan corrected A–D; arc elevated to ASAP)
- **Operator escalation (verbatim): "we NEED to move away from any kind of v1 dependency ASAP."** Arc elevated to standing ASAP priority on `v1-bd-tables-decommission.md`. Still a PHASE OF BSF-EXIT; NO drop authorized.
- **✅ tanks.php Stage 1f DONE — now 100% v1-free** (`grep 'bd_brewing_timings\b'` → 0 hits). Two commits, maltyweb main: `f398163` (fermentation reads/CC cards re-keyed off `beer_raw` onto (recipe_id_fk,batch) on `bd_fermenting_v2` — see the per-commit entry below) **+ `e063ddf` (NEW this session)** = fermentation-start anchor repointed OFF v1 `bd_brewing_timings` ONTO `bd_brewing_timings_v2`; anchor `COALESCE(MAX(STR_TO_DATE(start_ferm,…)), MIN(event_date))` keyed (beer,batch); `start_ferm` all-NULL so falls through to `event_date` (brew date) as ferment day-0 — v2-native, no backfill. **Remove tanks.php from the v1-reader list.**
- **Plan corrections (verified live this session):**
  - **(A) start_ferm backfill LARGELY MOOT** — v2 `start_ferm` 0/2271 NULL by design (v2 form has no separate ferment-start); v1 has gaps AND zero rows for recently-brewed batches (Moonshine 125, brewed 2026-06-09) → v1→v2 backfill wouldn't cover them. ⚠️ `bd_brewing_timings_v2.recipe_id_fk` was NULL on recent rows → timings-v2 reads keyed on **(beer, batch)** (separate NULL-FK ingest gap). **✅ that gap is NOW FIXED — `2037e2f` + mig 334, 2026-06-11; FK 100% populated (28→0 NULL). The (beer,batch) keying still works and stays in place; re-keying onto the FK is optional hardening.** Stage 2 re-scoped to a single decision: can `refresh_recipe_profile.py` adopt the `event_date` anchor? If yes → DROP the backfill stage (no migration).
  - **(B) cooling repoint → `bd_brewing_gravity_v2` Cooling col `final_volume` is a CORRECTION, not lossy.** "34 v1-only orphans" = EPH seasonal recipe_id remaps across vintages (exist in v2 under a different rid; join by v2 id/beer, not v1's stale cool_beer_recipe_id). "8 volume mismatches" = v1 dupes + missing brews; v2 is dedup'd + complete; OG (final_gravity) matches v1↔v2. NO `event_date` col on gravity_v2 → date proxy `DATE(submitted_at)`. **COGS/efficiency numbers WILL MOVE (as a fix) → before/after batch-HL delta report to Kouros BEFORE landing** sku-bom-compile.php / build-efficiency-data.ts / beer-tax.js.
  - **(C) Full v1-reader set confirmed both repos:** maltyweb sku-bom-compile.php (cooling + ingredients_parsed) / db-correct.php / packaging.php (×3); maltytask `lib/beer-tax.js` (OG-at-cooling, highest tax blast) + `build-efficiency-data.ts`; Python `refresh_recipe_profile.py` (biggest, repoint FIRST) + `parse_bd_ingredients.py`. KEEP (current canonical, no _v2 suffix): bd_packaging_readings / bd_cip_events / bd_tank_readings.
  - **(D) STANDING RULE:** `beer_raw` write-encoding flipped ~2026-05-29 to bare canonical name + separate batch col → any surface still reverse-parsing 'PREFIX BATCH' silently drops post-May reads; never read v1 (v2 is canonical + cleaner), grep for v1 table names before declaring any `bd_*` read correct.
- Memory updated: lean-index V1 entry (ASAP banner, tanks.php DONE, A–D) + `v1-bd-tables-decommission.md` (Stage 1f DONE, Stage 2 re-scoped, A–D, reader-set, KEEP-list).

### 2026-06-11 (tanks.php fermentation-card RE-KEY fix — beer-identity-fracture instance closed; maltyweb `f398163`, deployed+committed; read-only, no mig)
- **BUG:** `public/modules/tanks.php` fermentation CCT detail cards were frozen/stale (blank fg/pH on live batches). ROOT = the beer-identity-fracture: all **8** fermentation read/cold-crash lookups (3 occ-map stmts, 3 modal-bundle stmts, 2 page-level CTEs `$fermStmt`/`$avgFinalsStmt`) matched identity by reverse-parsing the free-text `beer_raw` in the OLD `"PREFIX BATCH"` shape. The saisie WRITE side flipped `beer_raw` → **bare canonical names** (`"Moonshine"`) ~**2026-05-29**, with identity now in `recipe_id_fk`+`batch`. Every read since late May silently dropped.
- **FIX (commit `f398163`, maltyweb main):** all 8 sites re-keyed to `(recipe_id_fk, batch)` on `bd_fermenting_v2`, with `beer_raw` fallback only when the simulator can't resolve recipe_id (rare sparse edges). recipe_id_fk 100% populated on v2 reads + ColdCrash; simulator recipe_id === bd_fermenting_v2.recipe_id_fk across all current batches. Read-only, no writes/migration. Smoke: Moonshine 125 now shows fg=10.6/pH=4.42 (2026-06-10) where the card was blank.
- **DURABLE — beer_raw write-encoding flip ~2026-05-29:** saisie now writes BARE canonical name into `beer_raw` (was `"PREFIX BATCH"`), identity in `recipe_id_fk`+`batch`. **Any OTHER surface still reverse-parsing `beer_raw` is SUSPECT and silently dropping reads.** Closes a concrete instance of the standing read-side re-key duty (match on recipe_id_fk, never beer name — see [[feedback_match_on_recipe_id_not_beer_name]]). PM standing action: when touching any bd_fermenting/bd_* read, grep for `beer_raw` parsing and re-key to (recipe_id_fk,batch).

### 2026-06-10 (SINGLE-SOURCE COGS REPOINT + FINANCIER PAGE v1; maltytask `489ab70`, maltyweb `6a013ad`; migs 300-305 applied+live, PM-verified)
- **Migration head 299→305** (all applied+verified live 2026-06-10): 300=`ref_customers_sale_class` (COGS taxonomy); 301=`inv_sales_ledger` (44,164-row canonical BC item-ledger 2021-01→2026-06); 302=`cuv_hl_per_unit_lock` (DB trigger locking cuv hl_per_unit=0.01); 303=`inv_side_stock_ledger` (packaging loose-unit remainder + giveaway, commit `1c95e23`); 304=`financier_page` (ref_pages id=239); 305=`kpi_fg_stock_data_ready` (FG-stock KPI trackers data_ready=1).
- **ARC 1 — SINGLE-SOURCE COGS REPOINT (maltytask `489ab70`):** per Kouros "one single source, not many", `lib/repos/mysql-sales.ts loadSales()` now reads ONLY `inv_sales_ledger` (the 44k canonical ledger), retiring the `inv_sales_bc`(6mo)+`inv_sales_orders`(Shopify) union. 2026-04 b2b/taproom/eshop reconcile to ledger 0.00%. Artifact `sales-cogs-data.json` regenerated = 66 months (was 6), deployed VPS. Canonical ledger writer `scripts/python/ingest_bc_sales_ledger.py` committed (was untracked). **Sign convention VERIFIED EMPIRICALLY:** outbound = NEG `qty_signed` AND `hl_resolved` (both negated→positive); `sales_amount_chf` revenue-signed (credits negative, plain SUM nets, never negate). SKU resolved sku_id_fk→ref_sku_aliases→raw (unresolved→`unknownSKUs`, never guessed). **SEAM:** ledger-only FOR NOW — the shipped `sale_class` filter {b2b,eshop,taproom,event,giveaway,staff} INCLUDES eshop+taproom from the ledger today (vs the ruling's plan to exclude); eshop→Shopify and taproom→Lightspeed (custom API requested) peel off to dedicated feeds LATER, isolated behind the filter. COGS shifted (2026-05 salesCOGS ~69,136) = expected+more correct.
- **ARC 2 — FINANCIER PAGE v1 (maltyweb `6a013ad`, mig 304):** read-only finance hub `public/modules/financier.php` (ref_pages id=239, manager/general/sort=125, manager preset). COP←`cogs-report-data.json` via `kpi_load_cogs_json`; COGS←`sales-cogs-data.json` (single-source ledger) via NEW `kpi_load_sales_cogs_json`+`kpi_sales_cogs_month_slice`; Marge/ASP from the COGS slice; Coût-par-SKU links OUT to sku-costs.php. NEVER recomputes. 5.2MB/66-mo artifact NOT injected — `public/api/financier-data.php` = manager-gated lazy per-month-slice endpoint (~43KB HTML); CSS/JS separated+cache-busted+fr-CH fmt; per-module freshness stamp (cron deferred). VERIFIED artifact-exact (COP 2026-04 88.76 CHF/HL; COGS 2026-05 69,135.53), php -l clean, auth gates 302/401/403, viewer denied (screenshot), XSS-safe. **OPEN: manager-authenticated VISUAL smoke NOT done** (auto-mode blocked fixture elevation — Kouros eyeballs as admin / authorizes elevation); **/financier Visite-guidée tour card NOT dispatched** (steward shouldn't deploy from dirty shared tree).
- **⚠️ CONCURRENCY INCIDENT:** a PARALLEL Claude session in the SAME clone (`/home/kluk/projects/maltyweb`) was building an FG-stock KPI tranche (`kpi_fg_*` + mig 305 + Phase-2b). When Financier committed, the SHARED git index swept the parallel session's uncommitted FG suite + mig 305 INTO `6a013ad` (filtered-patch stage raced the parallel stage). All code correct, php -l clean, both migs (304+305) applied — nothing broken, but `6a013ad`'s boundary is MIXED. **No reconciliation needed — leave as-is. LESSON: two sessions committing in one working copy race on the shared index → separate clones/worktrees OR serialize commits.**

### 2026-06-09 (FULFILMENT ITEM 1 — stock-movement saisie + per-site sales attribution + Nausikraft admin; mig 299; 3 commits pushed; autonomous run, Opus-verified)
- **mig `299_fulfilment_site_attribution.sql` APPLIED (head=299):** one atomic file — CREATE `inv_stock_movements` (from<>to + qty>0 CHECKs; FK RESTRICT→ref_skus/ref_sites, user SET NULL; tombstone-only schema_meta) + ALTER `ord_orders.fulfilment_site_id_fk` INT FK ref_sites RESTRICT + ALTER `ref_customers.default_delivery_site_id_fk` INT FK ref_sites SET NULL (Nausikraft mechanism, mirrors default_transporter_id_fk).
- **Phase 1 (`3c3eb33`):** `app/fulfilment-site.php` = THE single resolver `resolve_fulfilment_site(PDO,$ctx)` (precedence per-order override > customer default > channel default; sites DB-derived by role warehouse=id2/pos=id3/production=id1, never hardcoded) + `fulfilment_default_sites()`. Used by BOTH order-create selector AND snapshot attribution → no divergent channel→site map.
- **Phase 2 (`49a0386`):** `fg_stock_location_snapshot()` attributes SALES per site (replicates `fg_stock_compute()` steps 4/5/6 cutoffs exactly, grouped by resolver) + applies `inv_stock_movements` transfers (+to/−from, is_tombstoned=0, moved_on>anchor); global TOTAL unchanged (transfers net-zero); cards may go negative (honest). **Σ(cards)==Σphysique=8642 Opus-verified.** Ship-from selector ("Automatique"→NULL→resolver, or explicit holds_fg site) on `expeditions.php` order saisie + `import_weeklyorders.php`; `reglages-generaux.php ?sec=delivery_sites` admin sets `ref_customers.default_delivery_site_id_fk` (Nausikraft client list, operator enters tomorrow).
- **Phase 3 (`edfcf78`):** `expeditions.php ?view=mouvements` — operators record transfers (CSRF, from<>to + strict int qty>0 + site whitelist, log_revision, PRG), admin tombstone (is_tombstoned=1, never DELETE). E2E: DIV4 Renens→Crissier qty5 shifts split (Zgeg 449→444, Zeubi 39→44), Σ unchanged; tombstone reverts; CHECKs reject from==to/qty<=0.
- **Closes** the long-deferred "sales-leg per-location settlement" carryover (ATTRIBUTION). **Remainder:** sales+transfer cutoffs still on GLOBAL anchorDate (prod leg has the per-site `submitted_at` tiebreak; apply the same ROW_NUMBER per-(sku,site) to sales/transfers). PARKED: ITEM 2 mini side-stock/giveaways (blocked on concurrent form-packaging + migs 293/294 + Kouros D-Q5/6/7); ITEM 3 Phase-2 cage/COGS engine (Shopify pickup-signal ON HOLD + cage anchor Mon 2026-06-15). → fulfilment-expeditions-arc.md §ITEM 1 SHIPPED.

### 2026-06-08 OVERNIGHT (EXPÉDITIONS pre-flip completion — merge pipeline + stock hints + tour content; main==origin @ `6dd5521` PM-verified)
- **Merge-decision pipeline (`9803849` + safety fix `6dd5521`):** 368 needs_review → 4 buckets (138 strong fuzzy / 58 weak / 89 privés / 83 puzzles) → review xlsx w/ DÉCISION cols on Kouros's desktop. Apply chain `scripts/convert_merge_xlsx.py` → JSON → `scripts/apply_merge_decisions.php` (dry-run default, `--apply` single transaction, mirrors client_merge semantics, log_revision actor `'batch-merge-cli'`, idempotent). **Safety ruling: blank decision in sheet A = NON, never merge — a deleted cell must not act.** Fuzzy = suggestions only — false-match exemplar «Arches»→«Marchés» 0.92 preserved in the tables.
- **Saisie live-stock hints (`134bbdf`):** `fg_stock_compute()` → `window.EXP_STOCK_MAP` `{sku_id:{live_futur,physique}}`; advisory amber when qty > live_futur, never blocks (overselling legal — production may be planned); edit mode re-adds the order's own qty; try/catch silent degrade.
- **Tour content (`a59d94d`):** expeditions in `$PAGE_DESCRIPTIONS`/`$PAGE_ICONS` of visite-guidee.php — R7 closed, flip-safe. **Logistics preset: nothing to build** — auth arc pre-wired fulfilment id into manager + logistics_operator presets (id-based, rename carried); activates at flip.
- **Auth smoke deferred:** temp smoke-user creation denied by permission classifier (correct — account provisioning needs operator authorization; permanent smoketest acct id=16 = next-time alternative). Unauthenticated smoke green (302 redirect, lint, fg compute).
- **Morning runbook (Kouros):** xlsx → convert/dry-run/apply → admin click-through → flip `is_active=1` → inventory re-anchor (R2/R3 `fg_stock_compute()` re-exam triggers) → rule on 3 off-trade ambiguous. → fulfilment-expeditions-arc.md §2026-06-08 OVERNIGHT.

### 2026-06-08 (VISITE GUIDÉE chapter expansion — SHIPPED+LIVE+PUSHED `91f8c82`)
- **Operator-directed expansion of the form steps into extensive chapters** ("understand each input, parallel runs, how pertes liquides are computed"). 17 → **25 steps** for production presets; logistics preset unchanged at 11 (no production chapters, no capstone). Chapters: Brassage 3 / Fermentation 2 / Transferts 3 / Conditionnement 3 / capstone "La chaîne des pertes" (production-gated) / Inventaire RM enriched (vide ≠ zéro). Content map stays pure PHP keyed page_key + sub-index — no DB, no migration.
- **PM consultation answers went in essentially verbatim;** the 4 verify-in-code flags resolved against deployed code: (a) cold crash = checkbox in Mesures section (copy adjusted); (b) racking loss labels "Perte cuve départ"/"Perte cuve arrivée", forced comment IS live (outlier when loss_note empty); (c) packaging disposition live labels verbatim ("Perte liquide sans capsule"/"autre"/"à moitié remplie", "Perte capuchon fût"); (d) "+ Ajouter un format parallèle" confirmed.
- **Vetoes enforced + grep-verified on rendered HTML:** zero DB nomenclature (prod_total_units/qte_unites/loss_/bd_/_fk), no inline numeric thresholds ("selon les seuils configurés"), no future promises, no purge-cadence detail.
- **UI:** chapter eyebrow + mini-progress badge ("SAISIE · BRASSAGE 1/3"); compact dot mode >20 steps (5px dots, chapter gaps, ::before tap-zone expansion), 390px no-overflow verified; welcome "3 minutes" → "5 minutes".
- **Verified live** with self-cleaned test users tourx2 (preset 2, 25 steps all visible) + tourx3 (preset 3, 11 steps, no Brassage/capstone) — both tombstoned + deleted same session (audit ids 10860/10861), one login each (fail2ban-aware).
- **QUEUED POLISH:** form-packaging.php ~L2451-2452 helper text shows raw field names `prod_total_units`/`qte_unites` to operators — no-DB-nomenclature violation on the FORM itself (the tour worked around it). Fold into next packaging-form/ui-polish dispatch.

### 2026-06-07 (UI/UX QUALITY REWORK — Phase 3 EXECUTED + LANDED; 🏁 ARC FULLY CLOSED, one carve-out)
- **Commits `e05b18c` (3A hub/SdM/admin/fiche) / `e679efa` (3B sessions/packaging-form/SDC-visual) / `aba178a` (3C dashboards/CCT-modal/sb + app.css) — all pushed, PM-verified main==origin.** 3 parallel Sonnet bundles, then ONE fresh-context adversarial review over the COMBINED diff BEFORE commit (RULE-2 pre-commit, done properly — proven shape for multi-bundle bursts).
- **Review verdict COMMIT AFTER FIXES — 2 P1s caught pre-commit:** PF-04 preventDefault pre-submit gate on the novalidate packaging form REMOVED (silent-block risk; proper a11y = post-server-validation, deferred); PF-03 invalid `role="listbox"`+aria-live on the tank grid → `role="group"` + sr-only `role="status"` summary fed by `buildTankCardsFromData`. P3: wort Filtrer submit 32→44px. 5 hazards cleared (dup main-content id exclusive, CCT-02 reduced-motion net, WO-04 static innerHTML, wort idempotent-GET double-submit, aria-pressed sync).
- **Coverage:** LZ-01/02/04v, SDM-03/04, RG-03/05, SF-03/04, SE-03..06, SH-08, PF-01/02v/03/05, WO-04/05/06, CCT-02/03/04; sdc.css:88 backdrop removed (deferred-P1 closed); sb-sync-ts backdrop removed; smh-header sticky-kept; af-zoom-btn opt-out; mon-tableau FIRST battery pass; brewery literals (classification-appareil + salle-de-controle).
- **Arc closes** with §RESIDUAL BACKLOG (4 items): bom-review polish (the explicit post-F2 carve-out), PF-04 focus-first-invalid on PRG, mon-tableau structural (coordinate w/ KPI-tranche session — kpi-handlers.php/mon-tableau.js), deuteranopia chart-pair pass. Concurrent visite-guidée WIP untouched, zero collisions — the app.css-single-owner rule worked. → ui-ux-quality-rework-arc.md.

### 2026-06-07 POST-MIDNIGHT SYNC (push closed + login.php committed; PM-verified vs repo + live DB)
- **PUSH ✅ DONE — maltyweb main == origin/main @ `6f0e773`** (`git log origin/main..main` = 0). ALL of tonight's commits are on origin. **`public/login.php` COMMITTED via `ca65c22`** (carries BOTH the tour-redirect block and the device-interstitial wiring) — the "deployed-uncommitted shared file" divergence flagged at evening close-out is CLOSED; clean-checkout deploy is safe.
- **Newly-observed commits on main (pushed, not previously changelogged):** `5186454` fix(sku-bom-compile) active-only {beer}-pattern + §8.1 box_label suppression and `9fec7d5` feat(bom-review) clear_sku_choice = the two **Dispatch-D1 commits** (RESUME #0(a0)) — present+pushed, but the D1 data-outcome debrief is PENDING; plus `b676c9b`/`dcbce1d` fg-stock fixes (Expéditions Stock PF) and `1b9694a` mig-280 header-comment fix.
- **Live `ref_sku_bom` census drifted from the recorded ②-state:** Brewing 1472/236.64 (unchanged) but **Packaging 655 rows / 2013.36 CHF** (recorded: 580/2001.45); NULL mi_id still 0; **8 active `-X` cage SKUs now exist** (were scheduled for 06-08); active SKUs = 148. Consistent with D1 purge + cage-SKU creation+recompile in parallel session(s) — ATTRIBUTION PENDING DEBRIEF; do not quote 2001.45 as current.
- **⚠️ Mig 281 TAKEN:** untracked local `db/migrations/281_ref_customers_contact_cols.sql` (parallel CRM customer-bootstrap session; with `data/crm-clients-2026-06-07.{xlsx,json}` + `scripts/convert_crm_xlsx.py`). NOT on VPS, NOT applied (live `schema_migrations` head verified = 280). **NEXT FREE = 282.** This is exactly the parallel-sessions `ls db/migrations` checklist case (rule 17).

### 2026-06-07 EVENING CLOSE-OUT (device interstitial committed + KPI picker bug + terminology relabels + fermentation mini-tranche + smoke E2; all deployed+committed maltyweb main)
- **`94d5a0d` feat(devices): device-classification interstitial COMMITTED** — as-built matches the PM spec: personal=is_shared=0 'Appareil personnel' row; shared=labeled is_shared=1 + NEW `rt_revoke_current()` in remember_token.php (cookie-hash revoke, NOT rt_lookup which rotates); remember-me DEFERRED on unclassified devices via `$_SESSION['pending_remember']` (issue-then-revoke race eliminated per PM ruling); `device_classified()`/`device_mark_personal()` in device.php; outside ref_pages (visite-guidee precedent); ZERO mig. **⚠️ login.php wiring DEPLOYED but UNCOMMITTED** (file shared with parallel tour session — whoever commits next carries both knowingly).
- **`f7fe151` fix(kpi): stale `MT_STUBBED_DOMAINS` const in mon-tableau.php hid ALL 21 new racking/packaging/tanks trackers from the picker for EVERYONE incl. admin** (smoke E2 catch). Truth moved to `kpi_stub_domains()` in kpi-handlers.php ADJACENT to the dispatch match; mon-tableau consumes at 3 gate sites. Verified viewer picker=8 / admin=51. PLUS data fix: `ref_pages.min_role` operator-floor on 12 pages (viewer keeps mon-tableau/sb-board/sb-guerre only) — smoke showed a preset-less viewer could download warehouse GL CSV + reach the live brewing form. Audit row.
- **`b838cca` fix(kpi): operator terminology ruling** — category label **'Soutirage'→'Transferts'** (soutirage = FILLING in house usage); trackers 37/38/39/40/42 mise-en-fût→transfert wording; **34/35 (O₂) recategorized fermentation→packaging** (operator: O2 is a packaging KPI); 41→fermentation 'Temps moyen en CCT (brassin→transfert)', 46→fermentation 'Temps moyen de garde en BBT (transfert→packaging)'. Audit row.
- **`6f0e773` feat(kpi): fermentation mini-tranche, 4 live → census 55.** Operator ruling verbatim: fermentation END = start of cold crash; START = FIRST cooling (first fill/pitch). #31 avg_fermentation_time 10.1j weighted/143 batches/12m (Zepp 13.5j n=37 Opus-verified independently — EXACT); #16 cct_days_per_beer (4840 j-CCT/16 recipes); #18 beers_fermenting_now (14 in CCT, 120d guard); #20 tank_turns_period (4 this month; turns_per_tank=0.22 via ref_cct 18 active CCTs in meta). bd_fermenting_v2 recipe_id_fk+batch 100% on ColdCrash (824 rows, ENUM DryHop|Reads|Purge|ColdCrash).
- **`1d8672b` recap cron ACTIVATED** — as user `maltytask` NOT www-data (www-data can't write /var/log/maltytask → would silently kill every run; **house convention: PHP crons run as maltytask**). Operator confirmed inbox rendering. **`803d3f8` viewer seed-tuning APPLIED** (8 trackers→min_role viewer, operator blessed).
- **Smoke E2** via permanent smoketest account (users id=16, viewer; creds `/home/kluk/projects/maltytask/secrets/maltyweb-smoketest.env` gitignored 600, operator blessed; WSL workaround in webapp-testing skill ref: ssh -L 8443 + Node CONNECT proxy + --proxy-server; login path /login.php). PASS: login/empty-state/recap-no-email/charges-bc gate/MT logo→mon-tableau/390px/wort regression/console clean. FAIL→FIXED: picker 4/8 (f7fe151); viewer CSV/form access (min_role floor). QUEUED: **topbar horizontal overflow at 820px tablet-landscape** (no mid-width breakpoint) → parallel session's topbar a11y arc, do NOT double-edit.
- **Operator data observations awaiting ruling:** Diversion avg fermentation 2.7j (13 batches, raw pairs consistent 1-6d) — real fast-sour or operators logging soured-wort crash? DrunkBeard b1 74d in CCT no racking record — lagering or data gap?

### 2026-06-07 (EXPÉDITIONS ARC — full v1 LIVE, Le Cockpit christened; PM briefed + recorded; detail → fulfilment-expeditions-arc.md)
- **PM pre-approval brief approved by Kouros** (saisie+actionable dashboard / «Expéditions» christens Le Cockpit / full v1 one arc). Built by the parallel session, all deployed/linted/smoke-tested/committed same day.
- **Mig 279** `bbc49fe`: ref_transporters (Galliker/Loxya) / ref_customers (sold-to master, DISTINCT from ref_clients) / ord_orders (status ENUM = CACHE; exactly-one-party CHECK; email-readiness cols) / ord_order_lines (SKU units) / ord_order_status_events (append-only TRUTH, same-tx cache). ref_pages fulfilment→expeditions, **is_active=0 = go-live gate**.
- **Module** `f9535ef` (expeditions.php 3 tabs, one-tx POST, log_revision, PRG, ?edit=, inline new-customer needs_review=1; api/expeditions-status.php rank-map not ENUM order; require_once csrf fix) + **Commandes dashboard** `af6e0ea` (ISO-week + date-range ≤92j GET contract; eshop/taproom read-only auto rows from inv_sales_*; 3 queries) + **Stock PF** `4a8f49e`/`b676c9b` (`app/fg-stock.php` `fg_stock_compute()`: anchor + production ÷units_per_pack [caught a 24× inflation, 942431e class] − ord shipped − eshop inv_sales_orders − taproom inv_sales_bc).
- **Gaps/gates:** depletion gap by design until 06-08 inventory re-anchor (ZEPB 806.58 vs sheet 396); customer bootstrap GATED on CRM xlsx (CRM=roster truth, BC=fiscal links, sheet=enrichment; dry-run v1 done); smoke→flip→logistics preset; Transport Express→ref_transporters; commandes@ ingestion later arc. PM risk flags R1-R7 recorded in the arc file. Mig 280 coordination: taken by visite-guidée session; selective-rsync deploys held.

### 2026-06-07 LATE (UI/UX gravure CALIBRATION ADDENDUM — operator feedback; commits `49981f4` + `c31163f`; arc stays closed)
- **Operator feedback:** BBT fill level unclear on packaging.php; ferment vs cold-crash CCTs indistinguishable on tanks.php.
- **Root cause = z-order PORT BUG, not subtlety:** the `9dcd4ca` PHP port emitted the fill-tint rect BEFORE the opaque paper-body polygon — SVG z-order is document order, so the tint was 100% INVISIBLE (the `_design` spec had correct order; the port inverted it). **DURABLE LESSON (folded into `ui` skill rendering-bug catalog): when porting SVG markup between implementations, verify PAINT ORDER, not just element presence — opacity tuning is meaningless under an opaque sibling.** Also: `svg_vessel_cct` used `--cold` for ferment (wrong accent) → `--hop`.
- **`49981f4`:** paint order fixed in ALL 4 vessel fns (paper→tint→hatch); tint op 0.15→0.34-0.35; surface line sw 1.4 + meniscus ticks; FERMENT = 5 static CO₂ bubbles (reduced-motion safe); COLD = ✻ frost glyph + 4 wall ticks — **states now pass GRAYSCALE distinguishability** (ux-quality §1 "color never the only signal" was being violated, now satisfied); BBT 25/50/75/100% graduation ticks+labels. Elements 59–73, budget fine.
- **`c31163f`:** "100" graduation label clipped to "10" at the viewBox 80px edge → right-anchored x=79.5 `text-anchor=end` (companion lesson in the skill entry).
- Deployed selective rsync, php -l clean, smoke 302, Opus verified visually (color + grayscale VPS-rendered screenshots). This RESOLVES the "`app/svg-tanks.php` modified-uncommitted" working-tree note from the LATE NIGHT entry below. **OPEN: operator confirms tanks.php/packaging.php next visit.** → ui-ux-quality-rework-arc.md §CALIBRATION ADDENDUM.

### 2026-06-07 LATE NIGHT (VISITE GUIDÉE onboarding tour + mig-280 collision remediation + device interstitial + recap cron + Expéditions Stock PF)
- **`ca65c22` feat(onboarding): Visite guidée — first-login guided tour (mig 280).** New `/modules/visite-guidee.php` — `require_login()` only, deliberately NOT in `ref_pages` (no topbar nav). Steps built server-side from `ref_pages` (is_active=1, domain≠admin) × `user_can_access()` — each user sees only their preset's pages. French content map keyed by `page_key` with label-fallback for future pages; deep-dive steps for Mon tableau, Saisies hub (section-opener treatment), the 4 production forms (access-gated), Inventaire RM, a "Bon à savoir" (autosave/keepalive/green-flash), final step → mon-tableau. Preset 2 = 17 steps. Design mockup kept at `public/_design/visite-guidee.html`.
- **Mig 280 `users.tour_seen_at TIMESTAMP NULL`** — marked on first tour LOAD (`UPDATE … WHERE tour_seen_at IS NULL` + audit revision 'Visite guidée : première ouverture'); guarantees exactly one auto-redirect, never a nag loop. PM-verified live: 1/11 users already toured; remaining 10 (incl. Kouros + all existing users) get it exactly once on next login — intended.
- **🔴 MIG-280 NUMBERING-COLLISION REMEDIATION EXECUTED (closes RESUME #10 anomaly):** the untracked `278_users_tour_seen_at.sql` (applied live 20:20) collided with the parallel session's `278_kpi_seed_v2_batch`; remediated = rename → `280_users_tour_seen_at.sql` + paired manual `UPDATE schema_migrations SET filename=…` + **manual `rm` of the stale 278 file on the VPS**. PM-verified live 2026-06-07 ~23:30: `schema_migrations` head = 280 (applied_at 20:20:20 preserved), no stale row. **NEXT FREE = 281.** NEW LESSON: **deploy rsync does NOT delete removed files — a renamed migration leaves its OLD filename live on the VPS where it shows as "pending"; rm it manually** (folded into the checklist mig pre-flight).
- **Login-flow changes** (login.php / set-password.php): default post-auth landing `/` → `/modules/mon-tableau.php` (operators no longer land on the DB-status dev page); `tour_seen_at IS NULL` overrides `next` → tour. set-password: fresh invitees → tour; password RESETS of existing users → mon-tableau (freshUser SELECT extended with tour_seen_at — same incident class as the earlier manager_scope omission: extend the freshUser SELECT whenever a login-flow-bearing column is added). Topbar user panel gained "Visite guidée" re-launch link above "Mes appareils".
- **UI:** `public/css/visite-guidee.css` + `public/js/visite-guidee.js`, kraft tokens only; **grid-overlay step pattern** (all steps grid-row/column 1, inactive `visibility:hidden`) chosen after an overflow-clipping incident with absolute positioning + min-height; inline-SVG vignettes (no emoji — tofu on emoji-less tablets/headless); self-clipping `overflow-x:hidden` on own body class (inactive steps carry translateX(32px) which widened mobile to 425px — caught at 390px verification).
- **Verification:** end-to-end with self-cleaned test user (tourtest2, erased with audit tombstone): first-login redirect / 17 steps / keyboard nav all steps / Ouvrir-cette-page / user-menu re-launch / Terminer→mon-tableau / second-login→mon-tableau all ✓. NOTE: heavy Playwright login testing tripped **fail2ban** — verification agent saw 403 from public IP and wrongly concluded "Tailscale-only by design"; public access confirmed fine afterward (lesson: a 403 mid-smoke may be fail2ban, check the jail before concluding architecture).
- **Sibling landings same evening (PM-verified via git log, recorded here):** `94d5a0d` feat(devices): **first-login device-classification interstitial SHIPPED** (was RESUME #3b, ZERO mig); `1d8672b` feat(cron): **KPI recap dispatcher ACTIVATED** (operator confirmed inbox rendering — closes the "recap cron flip AFTER Kouros confirms inbox" item); `4a8f49e` feat(expeditions): **Stock PF live** — derived FG stock view w/ drill-down ledger (parallel Expéditions session continues, PM still NOT briefed on the module); `f7fe151` fix(kpi): stale `MT_STUBBED_DOMAINS` hid all 21 new racking/packaging/tanks trackers from the picker.
- **⚠️ PUSH STATE at PM verification 2026-06-07 ~23:30: main ahead 2 of origin (`ca65c22` + `f7fe151`)** — the "PUSHED" claim in the dispatch was premature. Working tree: `app/sku-bom-compile.php` (F2 staged work) + `app/svg-tanks.php` modified-uncommitted.
- **Open:** tour content map needs a bespoke French row whenever a new `ref_pages` entry ships (label-fallback exists but bespoke text is better) — standing convention recorded in user-auth-admin-arc.md.

### 2026-06-07 NIGHT (UI/UX QUALITY REWORK — Phase 2 FULLY CLOSED + C2 CONVERGENCE; arc Phases 0+1+2+C2 all closed in ONE day)
- **SdM scene integration (`cd96cd4`):** `salle-des-machines.js` scene painter adopts gravure — `sceneDefs()` 3 hatch patterns (bh_px/bh_pd/bh_pj); `cylGravure()` replaces `cylShade` for SCENE vessels (cylShade KEPT for mini config icons, intentional); strapShadow after every weldSeam; outline weights 1.85/1.1; CUITE calandre interne + reduced-motion-gated steam wisps; whirlpool vortex glyph; colour switch scoped to `svg.sdm-draw` only (--oak-deep/--hairline-2 ink). brewScene ~227 / cellar ~130 elements. Selective rsync (JS+CSS), smoke 302. C1 gate passed → **gravure = the house sketch language.**
- **C2 convergence (`9ed0f6f`):** **`app/svg-tanks.php` = the ONE canonical vessel library.** All four `svg_vessel_*` reimplemented gravure, signatures + CSS hooks verbatim (.sb-vessel--pulsing, .sb-conveyor-lines…), 50–110 elements each; pre-build inventory confirmed only sb-board called kettle/packaging_line, sb-mother = require-guard only. `app/svg-vessels.php` → tombstone + require svg-tanks.php + function_exists no-op stubs → **scrapping-backlog #31** (gate: BSF-exit Phase 6 AND zero `svg-vessels` grep hits). Bundled: **sb-zone__header restored STICKY + FROSTED** (backdrop-filter legit on sticky — composites once; audit deferred-P2 CLOSED). cct-detail-modal token reads verified unaffected. Smoke: sb-board/sb-mother/tanks/packaging non-500; php -l clean local+VPS.
- **Teardown:** `/_design/sketch-vessels-v2.html` + `/_design/sketch-brewhouse-v2.html` removed local + VPS (404 confirmed); gravure spec lives in svg-tanks.php + the arc file.
- **Arc state:** Phases 0+1+2+C2 CLOSED same-day (commits `9104273`/`01c99e3`/`9dcd4ca`/`cd96cd4`/`9ed0f6f` + audit report). Remaining = Phase 3 per-page polish, gated (bom-review→F2; SDC Recettes; kpi-charts/mon-tableau→mig 277; sb-* behavior→atom 11 — **atom-11 smoke doubles as visual acceptance of the sb-* gravure look + frost header**). → ui-ux-quality-rework-arc.md.

### 2026-06-07 EVENING (MON TABLEAU v2 LANDED — mig 278_kpi_seed_v2_batch; 51 trackers live; parallel-session mig 279 expeditions)
- **Commits (maltyweb main, chronological):** `2a8f2b5` recap-guard reads users.email from DB not session (session-payload defect family #3 — auth_login payload carries NO email; admin-added emails now effective without re-login) → `8800a9e` v2 layout (dense 4-col grid w/ viz_type span map; category-grouped dashboard + collapsible grouped picker, one `$CATEGORY_ORDER`/`$CATEGORY_LABELS` PHP source; fr-CH leak fixed ×4 in kpi-charts.js; `fmt()` unit-aware) → `db2da33` 15 sub-pages gated w/ `require_page_access` inheriting parent page_key (triage partials had NO auth bootstrap; salle-fournisseurs flagged no-nav-link → ref_pages registration queued) → `b66244a` REAL MP days-of-supply (v_rm_stock_dynamic CHF ÷ trailing-90d inv_consumption rate, window whitelist {30,60,90,180}, Opus-verified 147.0 j) + maintenance_opex_trend own GL-6100 12-mo series + honesty sweep (28/43 honest, label fix 111, 13 stub-mismatches) → `e8f5336` production tranche: 21 new live handlers (racking 7 / packaging 11 / tank readings 2 / wort yoy) + **mig 278_kpi_seed_v2_batch APPLIED** (relabels 111+266; 13 stubs→data_ready=0 incl. 4 utilities — admin stub-notice clear). **Live: 51 data_ready=1** (census 51/136/81).
- **Opus V1 verification EXACT:** PKG Jun-2026 17/293.0 HL/63,809 u; RACK Jun 4/390.6 HL; Cooling YTD 6,502.8 HL/199 brews; DoS 147.0.
- **Dispositions stays-0:** 6+43 (no yield targets), 47 (no flowmeter), 55 (no target fill), 59/60 (loss per-run), 63 (tank-sim port), 64 (no schedule), 65 (COGS pipeline). **Tracker 57 misread-correction = next-tranche cheap win** (inv_consumption direct-read IS allowed; ban is on bd_packaging×SKU_BOM recompute).
- **⚠️ PARALLEL SESSION + NUMBERING COLLISION:** second session landed `9104273` (ui-ux Phase 1) + `bbc49fe` **mig 279_expeditions_orders APPLIED** (ref_transporters / ref_customers [intentionally distinct from ref_clients, documented in-mig] / ord_orders+lines+status_events; ref_pages fulfilment→expeditions) AND left pending `278_users_tour_seen_at.sql` — DUPLICATE of the applied 278 → rename→280 before applying; **NEXT FREE = 280**. Incident: a mig draft self-INSERTed into schema_migrations → collided with migrate.php bookkeeping → FAIL-after-full-apply; CONVENTION = migrate.php does bookkeeping, NEVER the file.
- **Data fix:** users id=9 'Stphane'→'Stéphane' (audit row). Invites 7/8 SENT; Stéphane unclicked; Joelson waits on mailbox. Phase-3 deployed smoke (webapp-testing) launched, results pending.
- Open: viewer seed-tuning UPDATE (mig 278 §4 commented, awaits blessing); EUR→CHF 0.945 hardcode centralization (view ×2 + DoS handler, together); GL-6100 series thin (only 2026-03 in inv_charges_bc).

### 2026-06-07 PM (UI/UX QUALITY REWORK — Phase 0 + Phase 1 SHIPPED, Phase 2 sketch preview in flight)
- **Phase 0:** 3 `--ease-*` tokens added to app.css `:root` (--ease-out / --ease-in-out / --ease-drawer); full P0–P3 audit at `reports/ui-ux-audit-2026-06-07.md` (35 findings: 1 P0 / 14 P1 / 13 P2 / 7 P3; 10 shared-layer, 25 per-page); scroll-listener ban PASS app-wide.
- **Phase 1 (commit `9104273`, 34 files, ZERO mig):** P0 cleared (skip-to-content + id=main-content × 25 `<main>`); global :focus-visible rings; backdrop-filter off 4 scrolling containers (salle-de-controle.css deferred — frozen arc); form-resilience keepalive on reglages-generaux; wort tab ARIA; SdM live regions; warm shadows; nav-rail real buttons; sessions.php recipe names (label-map rule); touch-action + :active scale w/ reduced-motion net; overscroll-contain × 4; `brewery_identity()` de-hardcoding × 4 pages (SDM footer said Lausanne — truth Renens). php -l clean, 200 × 10 pages.
- **Phase 2 IN FLIGHT:** etched line-art / oak-ink-hatching-on-kraft direction (from Desktop inspiration imgs); preview v1 live `/_design/sketch-vessels-v2.html`; Variante B "gravure dense" being added per Opus density critique; **Kouros density sign-off = integration gate** before svg_cct/svg_bbt redraw (signatures frozen) + svg-tanks↔svg-vessels convergence (RULE-1).
- **Tooling gotcha recorded:** headless Chromium on the WSL box can't reach app.maltytask.ch (Tailscale 100.125.142.25; curl OK, Chromium times out) — curl-to-/tmp + file:// screenshot workaround → skills-and-build-tooling.md webapp-testing entry.
- Full ledger → ui-ux-quality-rework-arc.md §STATUS / PHASE LEDGER.

### 2026-06-07 PM (F2 liquid compiler NUMERICALLY VERIFIED — 4 incidents; whirlpool mig 276; P3 in flight on mig 277)
- **F2 compiler verification chain (ALL dry-run only; ref_sku_bom untouched 2135/2250.003568 after each):** `888473b` dry-hop branch + stage tags → `f99fa2a` inc#2 (v1 dry-hop path joined `bip.source_id` against `bd_fermenting_v2.id` while source_id points into v1 `bd_fermenting` → CROSS-RECIPE contamination, MOO/ZEP fabricated bills + EMB 2×; v1 DH path DELETED — bd_fermenting_v2 DryHop is recipe_id_fk-native + complete for F2 scope, v1-only = 18 contract brews out of scope) → `c9ff4ef` inc#3 (per-MI window → RECIPE-LEVEL shared basisBatches; retired ingredients now age out, EMB DH 1119→~533 g/HL family; per_hl = Σqty ÷ Σ(all basis HL)) → `2d59b4e` inc#4 (skip MAD-rejection when MAD/median<0.05, keep 2×MAD otherwise; missing-DH batches = DATA GAPS fully excluded from basis for any-DH-history recipes — b232 case). Verified reference: EMB basis {225-231,233} ΣHL 944.6; EMBF DH 566.38 g/HL (Amarillo 264.66/Cascade 153.50/Simcoe 132.33 n=7, Mosaic 15.88 n=1) — Opus hand-computed = preview EXACT. DOC edge: 2 DryHop events batch='None' → excluded until re-entered. Apply-gates 11-14 added (same-MI-multi-stage slot_name uniqueness, basis/outlier transparency, DH-alternation semantic guard, review-to-apply staleness). → walkthrough §F2 COMPILER VERIFICATION RECORD.
- **🔴 Standing discipline recorded (3 sub-agent gate-PASS failures in ONE day: TEA lump-sum / DH contamination / per-MI window):** COGS-bearing sub-agent claims are verified by Opus against independently computed references BEFORE reaching the operator → index BUILD-LAUNCH CHECKLIST + conventions-and-helpers.md.
- **Whirlpool + boil-timing** (`2a803d4` + **mig 276**): `hop_addition_stage` +'whirlpool' APPENDED LAST; CONVENTION: stage ordering via explicit FIELD()/rank maps, NEVER ENUM order; whirlpool (≈100°C flameout, no cooling) ≠ hop_stand (cooled to 80°C); `hop_boil_time_min` = boil-minutes REMAINING at addition (60=start, 0=flameout), boil-only, else forced NULL. Operator actively re-attributing hop stages in SDC (EMB live: whirlpool Cascade + first_wort Herkules). → recipe-ingredients-drift-surface.md §HOP-STAGE WHIRLPOOL.
- **IN FLIGHT (single repo-writer dispatch):** SDC recette UX batch (hops ALWAYS render g — kill g→kg auto-promote for hops; process-order hop bill; qty input per BRASSIN ÷ EFFECTIVE brassin incl. per-recipe override → qty_per_hl storage unchanged) + **P3 recap emails = mig 277** `user_kpi_recap_subs` + `send-kpi-recap.php` (reuses Phase-2 compute_handler registry) + DISABLED cron + cadence UI — closes the user-account arc's last build. NEXT FREE mig = 278 after P3 (re-verify `--status`).

### 2026-06-07 (BOM validation steps ①+② EXECUTED — WAC switch + template-enrichment batch) — maltyweb commits, migs 272+273 applied
- **① WAC-costing switch** committed+deployed+recompiled (−13.5% aggregate; was staged-uncommitted in `app/sku-bom-compile.php`).
- **②-A compiler** `c7d2c3e`+`052a8cb`: composite member-container packaging emitted data-driven from member format template+bindings (PD8 112 rows/2.80; PAL 83/6.99; PAC 28/6.91; XMAS 45/3.15); COLLAB4PACK temporal-resolve; explicit-null-override in solo path; composites in cron auto-detect + `sdc_recompile_recipe_packaging` (self-caught `array_column('id')`-vs-`'sku_id'` bug). Gate A: zero diff outside 6 target SKUs, idempotent.
- **②-B data** `c060b54` = **mig 272** (sticker qty 24→1; -B12 sticker explicit-nulls; branded-scotch bindings EMB/MOO/DIV/ZEP + §8.1 auto-suppression; box_label slot fmt1 + 8 label-beer choices; Nomoq intercal suppressed *4C; PKG_TEA_BOT_CH 0.02 + TEA template line on 6 glass formats; LOG_SCOTCH_ESHOP →0.000930; COLLAB12 sticker null; PACKDECX8 deactivated) + **mig 273** (schema: `ref_sku_bom.slot_name` + 4-col unique key + idx_sku_id; `ref_sku_packaging_choices.qty_per_unit` DECIMAL(14,6)). RQ 447/448 auto-resolved.
- **🔥 TEA-WAC poisoning incident (root-fixed same hour):** 11 Univerre TEA inv_deliveries rows were qty=1 lump-sums → WAC 1311.75/unit → first recompile hit 11.88M CHF. Re-granularized to paired-bottle-line qty (invoice_ref+file_id_fk join, rate 0.0214–0.0219 ✓); WAC 0.02033; full recompile 812 del/814 ins, parity 0. Build agent had misdiagnosed rows as misassigned (they were legit TEA, wrong granularity). Lesson → billing-format-and-wac-unit-model.md §LUMP-SUM ECO-FEE ROWS.
- **Post-B defect:** `uses_branded_scotch` recipe-altitude leak onto ZEPC (can box) — fixed via Tier-1 choice id 47 (scotch→TRANSP, sticker restored). Flag → packaging-bom-model.md.
- **FIX 7 BOUNCED:** P25/P50 keg-share — formats 16/17 catalog_id=NULL fail the dbc-commissioning gate. **PM ruling: compiler arm via `ref_packaging_formats.derived_from_format_id` FK (pours inherit commissioning from the keg parent), NOT a fake dbc catalog row.** → sku-bom-validation-walkthrough.md §FIX-7 RULING.
- **Final verified state:** packaging 528 rows/2001.37 CHF (from 1751.92); composite_packaging 83/11.92; brewing 1472/236.64 byte-identical; Gate B ALL PASS; idempotent. COGS delta +249.45 CHF pending operator sign-off.

### 2026-06-06 (P2b — KPI personal dashboard) — 2 commits on `main`, SHIPPED+LIVE, ZERO migration
- `99d5d54` + `2c37c9d` feat(kpi): personal KPI dashboard `public/modules/mon-tableau.php` (min_role=viewer, registered in `ref_pages` via idempotent upsert) — reads `user_kpi_selections`, runs `kpi_dispatch()` per tracker, renders via the new shared `public/js/kpi-charts.js`. Built to all 6 P2b constraints.
- **kpi-charts.js extract:** chart primitives lifted out of `wort-kpis.js` → `public/js/kpi-charts.js` (`window.KpcCharts`, 9 viz_types, pure viz layer — no fetch/recompute). wort.php loads kpi-charts.js BEFORE wort-kpis.js (both `defer`); wort page verified unchanged (constraint #3).
- **Picker = server-filtered + server-revalidated** by domain×role×data_ready; POST rebuilds the allowed-set server-side (`mt_build_allowed_set`) and REFUSES out-of-scope selections — a forged manager-tracker POST by a viewer is correctly rejected (constraint #2 proven). CSRF + log_revision + PRG; NO new compute path (constraint #4).
- **Stub handling (constraint #1):** 4 `utilities` data_ready=1 trackers map to a STUB handler → detected, excluded from picker/render, surfaced to an admin-only notice. This is exactly the seed-vs-implemented-domain mismatch the constraint anticipated (the data_ready=1 set minus the wort/cogs/ops_health/rm_procurement implemented domains).
- **Feed-2 WAC-divergence sub-flag** added to `bom-review.php` (0 rows today, lights up when real) per the interim-guard ruling.
- **Role-filter test:** viewer=0 / manager=30 / admin=39 selectable trackers. **VIEWERS GET AN EMPTY PERSONAL DASHBOARD** — no data_ready=1 tracker has min_role='viewer'. Flagged for the next seed-touch (seed-tuning question: open a few general/ops_health trackers to viewer), NOT a bug.
- ~24 packaging MIs still unpriced in active BOMs (operator catalog-pricing backlog, surfaced in BOM Review Feed 2).
- **Migration head UNCHANGED at 270** (P2b needed none). NEXT FREE = 271, RESERVED for P3 recap mig `user_kpi_recap_subs`.
- **NEXT = P3 KPI recap emails** (PHP CLI sharing kpi-handlers + live `send_mail`, mig 271, cron-disabled-pending-smoke, cadence UI). **PM RULING: P3 WAITS ON ONBOARDING** — can't be smoke-tested end-to-end until users have emails (onboarding) + tracker selections. Agreed with operator.

### 2026-06-01 (Fermenting state-gated selector + hop-stage model) — 3 commits on `main`, committed-NOT-pushed, mig **253** applied + PM-verified live
- `c38d924` feat(recipes): hop addition stage taxonomy on `ref_recipe_ingredients` (mig 253) — ENUM stage + boil_min + 2 generated-stored keys + `uq_recipe_mi_stage` (replaces `uq_recipe_mi`) + ENFORCED CHECK `chk_rri_boil_time`; NO seeding (163 rows NULL). Loader double-count fix (`recipe-ingredients-loader.php` Step-3 dedup-and-sum by mi_id_fk) shipped in same commit.
- `8cc1c73` feat(salle-de-controle): per-hop addition-stage editor in Recettes — inline stage select + conditional boil-minutes + multi-stage add/delete, 3 admin/manager JSON handlers w/ CHECK-invariant + log_revision. JS went into existing inline `<script>` block (debt flagged).
- `e81358e` feat(fermenting): dynamic state-gated beer selector by event type — racking-mirror cards, single TankSimulator + shared sim filter, identity-trap fixed (card = raw `bfw.beer`). Gates live: Reads/ColdCrash=8, DryHop=3, Purge=15, Hors-process=23.
- Open: (1) OR-bridge retirement (DryHop gate ORs canonical stage WITH observed bd_fermenting history — self-retires per recipe as operator classifies; track to drop observed arm). (2) Operator browser smoke-test = final gate before push. NEXT FREE MIG = 255.


## 🗂️ ARCHIVED INDEX MIG-HEAD HEADER BANNER (compacted from index 2026-06-15)
_Verbatim MIG HEAD header banner (migs 335→356 narration) relocated from the index during memory compaction. The per-arc records in this changelog + kpi-tracker-catalog.md §OPS-HEALTH are canonical; this preserves the exact index wording._

> **MIG HEAD: applied=356, 0 pending** (VPS `--status` verified 2026-06-15). **356 = `356_ops_health_pipeline_tiles_live.sql` — OPS-HEALTH PIPELINE-HEALTH TRANCHE SHIPPED+LIVE+PUSHED (commit `bfa541c`):** 4 tiles LIVE — `data_freshness` #223 (table; whitelisted table→ts-col map, NO free-SQL; future-date guard) / `supplier_mi_resolution_failures` #225 (open RQ sales-sku-unknown+sku-bom-unresolved = 1087 RED, real backlog) / `auto_write_vs_manual_ratio` #227 (99.3%) / `avg_triage_time` #228 (bar, 41.8h) — **CITE BY SLUG: `ref_kpi_trackers` carries BOTH `id` (PK) and `tracker_no` (catalog #), differing by 1 (`id = tracker_no − 1` consistently — e.g. data_freshness id=223/tno=224; discount_rebate id=99/tno=100), so by-id and by-tracker_no citations resolve to the SAME row. Mig 356 keyed on `tracker_no` and landed the CORRECT slugs — operator re-verified live by slug 2026-06-15: no wrong rows touched, no live tile gapped. The earlier "~+1 drift" was an id-vs-tracker_no verification confusion, NOT a build error.** + HONEST RE-TIER 6 mislabeled compute→gap: #219 parser_coverage / #220 quarantined_values / #221 validation_rule_failures / #226 llm_fallback / #229 classifier_accuracy = Node-side-only (maltytask data/quarantine.json, validate-data.js, parser-oracle, LLM cache — NOT on VPS/MySQL → blocked until BSF-exit lands Node pipeline on VPS); #230 duplicate_detection = structurally-0 (doc_files.file_hash 0-populated + 0 dn-invoice-dup RQ → dropped not shipped-misleading). NON-FISCAL admin tiles → direct data_ready=1 (no two-step verify gate). All read MySQL-only + email-deliverable free. ops_health census now **14 live / 2 compute / 7 gap** (supersedes the stale "10/23"). #77 (real slug `fg_aging_best_before`, fg_stock domain) — mig 356 SET IT TO gap/data_ready=0 (live confirms gap, operator-verified by slug); stays gap-unbuilt, belongs to deferred cage/COGS Phase-2, NOT ops_health. **📌 WATCH-ITEM:** an `inv_sales_ledger` posting_date=tomorrow (BC future-dated) — freshness tile guarded, but could subtly inflate MAX(posting_date)/latest-month snapshots; suspect first if any "latest closed month" logic looks wrong. → kpi-tracker-catalog.md §OPS-HEALTH PIPELINE-HEALTH TRANCHE. 353 = `353_backfill_brewday_recipe_id_fk`; 354/355 = `ord_order_bc_lines`(+source_ref) BC line-snapshot (the pending BC↔ord_orders Phase-2B "B" leg appears to be landing — re-check owning session). 352 = mig 352 moved tracker #282 to `category='packaging'` — was wrongly 'production'; mig 351 `351_kpi_production_targets_widgets.sql` seeded production_targets trackers #281/#282 — Objectifs-de-production Mon-tableau widgets, SHIPPED+VISUALLY-VALIDATED; prior head 350: BC SALES DISCOUNT arc landed migs 348 = `inv_sales_invoice_lines` per-line canonical store + 350 = #100/id=99 `discount_rebate_rate` go-live flip; commits `6ec3fb7`+`5ba1def` pushed; SHIPPED+LIVE+VERIFIED — but the discount connector is NOT yet folded into the bc-sales cron (manual-run only; REMAINING STEP = co-schedule `ingest_bc_sales_invoice_lines.py` in `db/cron/maltytask-bc-sales.cron` → see sales-commercial-surface.md §BC SALES DISCOUNT AS-BUILT). Re-`--status` at build-start. Prior head=345: ✅ VPS `--status` 2026-06-12: 0 pending; 344 = repoint legacy bc KPI tiles → `inv_sales_ledger` + drop #86/#92/#187 to data_ready=0 + retire #94/#100; 345 = tranche-2 go-live re-flip after Opus verification; maltyweb `3b8dead`/`fb61ba5`/`9cc52b3`, **scrapping-backlog #33 EXECUTED — sales-KPI parallel-store divergence CLOSED, whole sales KPI surface now reads ONE source `inv_sales_ledger`; bc TABLE retained for fg-stock/expeditions/tap-shop**. Prior: 342 = `viz_type` ENUM +'stacked_columns' + seed data_ready=0; 343 = go-live flip for sales-monthly trackers #277-280, `3c8c8ad`/`c60aadf`; 341 = `UNIQUE(bc_line_seq)` on `inv_sales_ledger`, BC OData connector `c017bc9`; head 335→345 spans intervening parallel-session migs, re-`--status` to enumerate). RE-VERIFY `migrate.php --status` at build-start (parallel-session collision remains a live class). Prior session arcs ALL COMMITTED+PUSHED on chain `a0a9965`(recap-email)→`2363c9d`(recette targets)→`bc678af`/`cdff109`(tank-occupancy tiles #13/#14/#15/#17, mig 333, read canonical `TankSimulator::run()` via `kpi_tanks_get_sim_state()`, zero writes)→`b6db62c`/`fb2778a`(MON TABLEAU #2 sparkline + #5 grouped_bar mig 331)→`ad9c14b`(financier fiche). REFERENCE PATTERNS: `kpi_wort_hl_brewed`(#1) / `renderKpiGroupedBar` / `renderKpiSparkline` (+meta.prev_series ghost). Earlier 2026-06 landings COMPILED OUT → [build-history/changelog-2026-06.md](maltyweb-pm-memory/build-history/changelog-2026-06.md); pre-2026-06-05 → [build-history/former-index-last-updated.md](maltyweb-pm-memory/build-history/former-index-last-updated.md).

## 🗂️ ARCHIVED INDEX RESUME-POINT + BUILD-STATE ENTRIES (compacted from index 2026-06-15)
_Verbatim RESUME-POINT items (mostly ✅ completed arcs) and CURRENT BUILD-STATE migration-history paragraphs relocated from the index during memory compaction. The per-arc thematic files cited within each entry remain canonical; this is the exact former index wording, preserved so nothing is lost. Line numbers refer to the pre-compaction index._

### --- RESUME-POINT items ---
----LINE 46----
V1. **🟢 v1 `bd_*` TABLES DECOMMISSION — STAGE 1 + STAGE 3 COMPLETE (2026-06-11); v1 tables now FROZEN (no readers, no writer); only Stage 4 (DROP) remains, gated on soak + re-grep + operator. PHASE OF BSF-EXIT.** ✅ **Stage 3 (STOP v1 writer) DONE — commit `0449a95` (maltyweb):** operator CONFIRMED all brewing/ferm/racking/packaging data entry moved to maltyweb saisie forms (→ `bd_*_v2` direct); BSF Google Forms RETIRED → **BSF-exit Phase 6 hard-gate CLEARED for these tables.** DISABLED the `maltytask-bsf-ingest` cron (`/etc/cron.d/maltytask-bsf-ingest`, drove `ingest.py --tab=all --apply` → v1 every 30 min); active line commented w/ dated rationale, deployed, VPS-verified (no active bsf-ingest line). **Pre-stop v1↔v2 reconciliation CLEAN:** v2≥v1 except brewday 835/831, cooling 2322/2286, timings 2306/2271 — v1-only rows ALL 2021-22 contract/collab batch=NULL legacy artifacts (BLZ/Chien Bleu/Bsr du Château/NYL/Combières/MeltingPote/BadFish), no consumer, no real data lost. ✅ **Stage 1 (ALL READERS REPOINTED):** tanks.php (`f398163`+`e063ddf`), sku-bom-compile.php (cooling `aa6fb0f` + ingredients `b17c686`), packaging.php (`aa6fb0f`), beer-tax.js + build-efficiency-data.ts (`262945a`), refresh_recipe_profile.py whole-file (`ed41319`), parse-tank-simulation.ts (`face01f`). COGS-bearing repoints Opus-verified (cooling 0.0°P core OG / 0 tax shifts; ingredients 0 keys lost / 6 = v1 g/kg DATA BUG v2 corrects; batch-HL 753 identical / 8 corrective e.g. Embuscade 236 32.8→128.0 HL). **✅ recipe-profile cron CONFIRMED ENABLED ALL ALONG (commit `b9016d0`, maltyweb, 2026-06-11; VPS-verified) — the earlier "DISABLED pending smoke / operator must re-enable" note was WRONG; action item SCRUBBED.** `/etc/cron.d/maltytask-recipe-profile` runs nightly 03:00 UTC; smoke-passed `refresh_recipe_profile.py` as maltytask cron user (61 recipes, 122 profile rows, 146 malt/133 hop, 0.8s, 0 errors; one --apply primed `ref_recipe_profile`). `b9016d0` DROPPED the cron's obsolete step 1 `parse_bd_ingredients.py --apply` (legacy v1 free-text parser → frozen v1; refresh now reads parsed_v2 per `ed41319`) + fixed stale header (script has NO --dry-run; omit --apply to preview); cron is now refresh-only, still enabled. **STAGE STATUS: 1 ✅ DONE; 2 MOOT (event_date anchor); 3 ✅ DONE (`0449a95`); 4 (snapshot→soak→DROP + remove db-correct v1 branch + validate_fk_candidates refs + **`parse_bd_ingredients.py`** + schema_meta cleanup) GATED on ≥1-2wk soak now ticking + final full both-repo re-grep + operator go-ahead.** Remaining v1 refs NOT consumers: deferred (db-correct.php admin UPDATE on v1 ingredients_parsed.mi_id_fk, validate_fk_candidates.py dev utility, **`parse_bd_ingredients.py` — now referenced by NO cron + NO consumer (only output = v1 bd_brewing_ingredients_parsed, read only by deferred db-correct.php branch); retire at Stage-4 DROP alongside ingest.py/tab_*.py**) + 1 comment (loss-metrics.php:42). **✅ CLOSED — ingest-root ticket: `bd_brewing_timings_v2` NULL recipe_id_fk FIXED (2026-06-11, `2037e2f` maltyweb + mig 334).** Writer `form-brewing.php` built every sibling v2 row with `recipe_id_fk` but OMITTED it from `$timingsRow` → 28 NULL-FK timings rows (2026 core: Zepp 215/216/217, Moonshine 124/125, Embuscade 235/236). Fix = (1) add `recipe_id_fk` to `$timingsRow` (deployed); (2) **mig `334_backfill_timings_recipe_id_fk.sql`** backfills the 28 from the brewday_v2 sibling per (beer,batch), guarded `recipe_id_fk IS NULL` (idempotent); applied via `migrate.php` as `sudo -u www-data` (bare `php migrate.php` as ubuntu FATALS on db.env perms). PM-verified live: NULL 28→0 of 2271. Optional future hardening: re-key the (beer,batch) timings reads onto the now-populated FK — NOT required, (beer,batch) works. ROOT RULE (D): beer_raw flipped ~2026-05-29 to bare-name+batch-col — never reverse-parse 'PREFIX BATCH'; Stage-4 re-grep before DROP is MANDATORY (parse-tank-simulation.ts was a truncated-grep miss). KEEP: bd_packaging_readings/bd_cip_events/bd_tank_readings. Full plan/audit → [v1-bd-tables-decommission.md](maltyweb-pm-memory/v1-bd-tables-decommission.md). EQUIP coder+sql(+ui+webapp-testing for deployed-page smoke); COGS/tax repoints = Opus per-recipe numeric verify.

----LINE 47----
0a. **✅ cage (-X) form SHIPPED** (`91ce8c0`, deployed 2026-06-07); 8 active -X SKUs now exist (ZEP/MOO/EMB/STI/SPY/DIV/DOA/ALT-X, ids 279-286, PM-verified live 2026-06-09). REMAINING: confirm whether -X SKUs carry `inv_fg_stocktake` anchor rows yet + R2 eshop cage-first netting once they do. Spec → [sku-bom-validation-walkthrough.md](maltyweb-pm-memory/sku-bom-validation-walkthrough.md) §CAGE-FORM BUILD.

----LINE 48----
0a10. **✅ STOCK DRILL-DOWN render-only cluster SHIPPED + LIVE + NOW COMMITTED 2026-06-10 (3 render-only ships, NO mig; clean 3-file commit `711905c` on main, LOCAL/UNPUSHED = `public/modules/expeditions.php` + `public/js/expeditions-stock.js` + `public/css/expeditions-sku-history.css`; SELECTIVE rsync deploy). ⚠️ PATHSPEC INCIDENT recovered (never left local): bare `git commit` swept in operator's staged kpi-handlers.php/mon-tableau.css/kpi-charts.js → `git reset --soft HEAD~1` + `git commit -- <3 paths>` restored operator staging + landed clean-3. LESSON now in CHECKLIST.** Inside `?view=stock` `exp-stock-ledger` per-SKU drill: (1) "Répartition par site" sub-block (reads existing `$fgLocationSnapshot`, no re-query); (2) "Activité récente" timeline (3 BULK ROW_NUMBER window queries page-wide, NO AJAX/endpoint, packaging predicates MIRROR `fg_prod_since_anchor()` except anchor-cutoff→window — R3 CLOSED); (3) **FLIPPABLE DRILL — `role=tablist` SEGMENTED TOGGLE (not 3D flip) splitting drill into Face "Stock" (default) + Face "Activité"** (timeline in OWN scroll-bounded table max-h 384px/overflow-y — the uncram fix); caps raised 15→50 events / 90→180d; JS = ONE delegated `button[data-evt-tab]` handler scoped `closest('.exp-stock-drill')` (NO per-row listeners) + `resetDrillFace()` on ALL 3 collapse paths + arrow-key nav; new `.exp-evt-tabs/-tab/-pane/--activite` CSS = ≥44px/focus-ring/aria-selected/100% var()/0 hex/0 `.exp-ledger-*`|`.exp-stock-*` redefinitions; a11y real buttons+role tablist/tab/tabpanel+region scroll pane. **BROWSER-VERIFIED 18/18 (Playwright, viewer→operator→reverted exactly, zero residual): independent per-drill state + reset-on-collapse confirmed.** php -l clean, page 200. **🟡 CSS fold-in debt:** `expeditions-sku-history.css` + `exp-ledger-loc-*` + 1 inline `opacity:.7` all gated on operator's in-flight 168-hunk expeditions.css overhaul deploying. EQUIP ui+sql+coder+webapp-testing. → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §FLIPPABLE DRILL-DOWN + §PER-LOCATION PHYSIQUE + §"Activité récente".

----LINE 49----
0a9. **✅ MULTI-SKU MOUVEMENT INPUT SHIPPED + LIVE + COMMITTED + PUSHED 2026-06-10 (extends ITEM-1 Phase-3 `?view=mouvements` scalar→multi-line; NO mig, HEAD stays 303).** Shared header (origine/destination/date/comment once) + repeatable SKU+Qté grid; `add_movement` reworked to parallel arrays `mov_sku_id[]`/`mov_qty[]`, ALL lines validated BEFORE `beginTransaction()` → **all-or-nothing** (bad line → zero writes, "Ligne N"), one txn / N inserts, **per-row log_revision preserved** (handler expeditions.php:1013-1054). **SINGLE-HOME PRESERVED = N `inv_stock_movements` rows, NO batch table** (multi-SKU = N route-sharing facts, not a new entity). Net-zero 8599==8599 both sides pre/post test. `expeditions-form.js` now also loads on `view=mouvements` (`initMovLines()`). NO new `ref_pages`/tour card (same page). **COMMITTED IN `badd153` (origin/main) — NOT the planned standalone commit: a PARALLEL session bundled it with the order stock-risk modal (`exp-sdm`) + Stock PF header restyle into ONE commit whose MESSAGE omits mouvements (UNDER-DESCRIBED); handler verified intact in HEAD.** ⚠️ **TWO-CONCURRENT-SESSIONS HAZARD today** (HEAD then `e32d472` unpushed + dirty = the OTHER session's mon-tableau work, migs 310/313) — deploy-pushes-working-tree means an `--apply` leaks the other session's uncommitted changes; `git status` before any deploy. EQUIP ui+sql+coder+webapp-testing. → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §MULTI-SKU MOUVEMENT INPUT.

----LINE 50----
0a6. **✅ TAPROOM SINGLE-UNIT-SKU GAP CLOSED + WEEKDAY-CHIP BUG FIXED 2026-06-09 (no mig; data + `expeditions.php`; commit `afe1dcd`).** (1) DGDBU CREATED `ref_skus` id=315 (recipe 31 / format_id=5 BU, hl_per_unit=0.00330 from format, scope='single', 6/6 BU-sibling-conform, 4-line packaging BOM, liquid line=0 at PHP-recompile like all BU siblings; is_direct_sales=0 flagged to Kouros). (2) Contract beers OUT of taproom — BAMB/BAMF/JASPB/JASPF scope 'base'→'cage'; DECISION: NO contract singles (BAMBU/JASPBU NOT created). (3) Weekday chip now derives via `exp_dow_fr()` (was hardcoded "lun."). ⚠️ **`afe1dcd` SHIPPED BROKEN — correcting an earlier mis-report of it as a clean ship:** `exp_dow_fr()` was placed NESTED inside the stocktake render block, so it was undefined at the EARLIER ~L2824 locations-overview fresh-chip ("✓ compté") call site → today's live taproom count tripped "Call to undefined function exp_dow_fr()" → PROD 500 on the WHOLE PF-stock page. `php -l` passed (valid syntax / conditional decl) and the deployed-page SMOKE WAS SKIPPED, so it shipped broken. **FIXED IN PROD BY KOUROS (not the build agent) = `ae297c2` "hoist exp_dow_fr to top level"** (Kluk76, 2026-06-09 15:57; helper moved to the unconditional top-level date-helper section ~L934; on origin/main, VPS-deployed, php -l clean). Functional outcome stands (weekday derives, 2026-06-09→"mar.") AND the 500 is gone. DURABLE LESSON (also captured in the `coder` skill): PHP nested fn defs are runtime-declared in CALL ORDER — a helper used before its later nested def fatals; keep page helpers TOP-LEVEL, and SMOKE the deployed page (never trust `php -l` alone for a function-existence/runtime fault). → header §EXPÉDITIONS OUTAGE FIX `ae297c2`. OPEN (asking Kouros): DOCB rescope base→cage pending confirm; DEFERRED — move 21/24 DGDB stopgap into DGDBU as 21 units via the FG-stocktake EDIT feature (NOT ad-hoc SQL), pending taproom input complete. → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §TAPROOM SINGLE-UNIT-SKU GAP ✅ SHIPPED.

----LINE 51----
0a4. **🆕 WeeklyOrders SHEET → ord_orders INGESTION — PM RULINGS RECORDED 2026-06-09 (pre-build; dry-run→match-report→Kouros reviews unmatched→apply).** Scope=KW24 full + ALL future booked, EXCLUDE Feb archive; status=every-reached-stage event (occurred_at=requested_date, comment='import:weeklyorders'); SKIP Taproom/Eshop/samples as ord_orders (R1 double-depletion — but a BC-numbered Marketing/privé sold-to IS a customer order, keep); customer match = BC#→exact→alias, NO-fuzzy-auto-link (flag only), reuse merge/Carnet resolver; Cité festival = N orders (sub-location in comment); **DEDUP = new `ord_orders.source_ref VARCHAR + UNIQUE` (mig, next free ~292 VERIFY — 291 taken by Tap&Shop), NOT a line-hash; the 12 existing `source='web'` KW24 rows are REAL operator orders — never reconcile/replace, flag web-overlap collisions for operator**; off-trade = customer attribute, ignore on order. EQUIP sql+coder+sheets/xlsx-read. Full record → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §WeeklyOrders SHEET INGESTION.

----LINE 52----
0a3. **🆕 BC↔ord_orders ORDER INTEGRATION — PHASE 1 SCOPED + PM-RULED 2026-06-15 ("on lance phase 1 pour sûr"); pre-build (dry-run→match-report→Kouros→apply).** Phase 1 = pull OPEN B2B BC sales orders → `ord_orders`/`_lines`/`_status_events`, ELIMINATES the manual WeeklyOrders importer (which is a one-shot `/tmp/wo-parsed.json` apply, NO cron — verified). READ-ONLY toward BC. **RULINGS:** (1) SOURCE = standard `salesOrders` API as the set-driver (29 live, doc-state-scoped, drops posted) + custom `SalesOrder` web service as a TARGETED by-No lookup ONLY for `ShpfyOrderNo`+`Shipping_Agent_Code` if standard header lacks them — PROBE first, skip hybrid if standard exposes ShpfyOrderNo. Reuse connector OData idioms verbatim (S2S OAuth2 `config/bc.env`, OR-chain `$filter` 50/batch [BC rejects `in`], nextLink paging NO `$top`). (2) SHOPIFY DOUBLE-COUNT EXCLUSION = `ShpfyOrderNo` non-empty → EXCLUDE (eshop already in `inv_sales_orders`); belt-braces also exclude system customers 1080(eshop)/3822(taproom). (3) CUTOVER = stop running WeeklyOrders importer; new ENUM val `ord_orders.source='bc'`, `source_ref='bc:<OrderNo>'` (col already UNIQUE varchar190 — idempotent upsert); existing web/import rows AGE OUT, never migrate; 1st dry-run match-report flags open-row collisions vs existing web/import → operator per-row; no dual-live steady state. (4) 🔴 CRUX status-preservation = pull is a HEADER/LINE refresher gated `status IN('entered','confirmed')`; NEVER writes status or status_events (operator-owned, read-only toward BC); BC `Completely_Shipped`/order-leaves-open-set = reconciliation SIGNAL surfaced on board, NOT auto-advance/cancel; freeze lines once picked+. (5) SKU = `(Item_No, UoM_Code)` → ref_skus (item alone insufficient — UoM selects format), via `ref_sku_aliases`; refuse-don't-NULL → review CSV + hold/flag order, never guess item+UoM→SKU. (6) REFRESH = VPS cron `*/15` (match shopify-ingest cadence; set tiny+hot), user maltytask, NOT local; webhook = Phase-2-era. (7) MIG = ONE: `ALTER ord_orders MODIFY source ENUM(+'bc')`; no new table → no schema_meta row; re-`--status` (head was 345). **🔴 #1 DIVERGENCE = fg-stock DOUBLE-DEPLETE: `fg_stock_compute()` has 3 legs (Step4 ord_orders shipped / Step5 eshop inv_sales_orders / taproom inv_sales_bc); a Shopify-origin BC order in ord_orders shipped → depletes Step4 AND Step5. The ShpfyOrderNo exclusion is exactly what prevents it — OPUS-VERIFY the no-double-deplete invariant before done (COGS/inventory-bearing; sub-agent PASS = claim).** ord_orders vs inv_sales_ledger stay DISJOINT facts joined only by BC No (open-operational vs posted-fiscal); burn engine keeps reading the ledger NOT ord_orders. Phase 2 (input-on-maltytask→writeback-to-BC) = LATER arc. SCOPE = tight 3-4 days (resolvers/tables/board/match-report all reused). SEQ: probe→mig→connector dry-run→match-report→Kouros→apply→cron→Opus-verify fg-stock invariant. **EQUIP coder+sql+webapp-testing.** Full record → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §BC↔ord_orders PHASE 1.

----LINE 53----
0a8. **✅ THE 4 COGS TILES (#168/#169/#171/#172) ARE LIVE — OPTION C EXECUTED + SHIPPED 2026-06-10.** Producer = **`scripts/build-cop-report.ts`** (maltytask repo, MySQL-native, replaces dead/archived BSF `build-cogs-report.js`) — reuses `lib/cop-indirect.js`/`lib/utilities.js`/`lib/inventory.js` VERBATIM (no divergent COP), adds 4 MySQL loaders (per-SKU cost split `ref_sku_bom×ref_skus×ref_packaging_formats×ref_mi` via `CAT_KEY_MAP`; packaged HL `bd_packaging_v2.vendable_hl` neb-only; brewed HL `bd_brewing_gravity_v2` Cooling final_volume; BC charges `inv_charges_bc`), `withConnection()` over tunnel 13306, **ZERO DB/Sheets writes** → emits local `interfaces/cop-report-data.json`. **Reconcile gate PASSED EXACT** (per-SKU Σ==`ref_sku_bom` Σ to 0.000000: ZEPF/EMBB/ZEPV). Emits SLIM handler-shape only (`cop.totalVariables`/`cop.brewing.{malts,hops,ingredients}.current`/`cop.{packaging,indirect,utilities,rd}.total`); yeast folded into "ingredients" (handler #171 reads malts+hops+ingredients). **Deployed** to `/var/www/maltytask/interfaces/cogs-report-data.json` (handler's hardcoded first candidate; `maltytask:www-data` 644; NO PHP/handler edit, NO mig; kpi_cache per-request). Smoke (www-data kpi_dispatch, period **2026-04** closed): #168=75773.73 / #169=87.16 → **TILES LIVE.** **✅ APR+MAY CHARGES BC INGESTED + COP RE-RUN + REDEPLOYED 2026-06-10 — April COP now COMPLETE: brewing 13928.33 / packaging 33288.24 / indirect 16404.73 / utilities 13548.07 / rd 189.46 → totalVariables 77169.37 / 88.76 CHF/HL. Tiles now #168=77169.37 / #169=88.76 / #171=13.64 / #172 full, all kpi_dispatch-verified** (was 75773.73/87.16 pre-charges). **🔧 NEW DURABLE FALLBACK = maltyweb `scripts/php/ingest-charges-bc-csv.php`** (deployed `/var/www/maltytask/scripts/php/…`, UNCOMMITTED): server-side CLI reusing `charges-bc-upload.php`'s EXACT helpers + row_hash (BYTE-IDENTICAL → future UI re-upload dedups to 0), dry-run default / `--apply` INSERT IGNORE + audit, `last_modified_by='ingest'`. **USE THIS whenever the Charges BC UI is gated** (operator off-Tailscale, blocked by admin gate). Applied Apr 20 / May 22 rows (1 dupe); PM live-verified Apr+May present, all `ingest`, GLs 4302/4500/4510/4600/6100. **📋 OBSERVATION (bookkeeper, non-blocking):** May GL4510 came SUMMARY-ONLY (entry_no=null/0.00 — no May R&D 4510 invoice or absent from export); April 4510=160.04 fine. Flagged to operator. **🔴 OPEN:** (1) **AUTOMATION/freshness-chip STILL pending** — regen MANUAL (local tsx over tunnel + sudo install), NO cron, producer NOT on VPS → as-fresh-as-last-run, against maltyweb-prefers-live (SEQUENCING below); (2) **May COP lands after May CLOSED + re-run** (charges already banked); (3) **TWO scripts UNCOMMITTED** (commit-on-ask): `build-cop-report.ts` (maltytask) + `ingest-charges-bc-csv.php` (maltyweb); (4) **admin Tailscale-gate UX recurs off-Tailscale** — open Q: Charges BC reachable off-Tailscale vs stay gated w/ CLI fallback. EQUIP coder+sql. Full as-built → [kpi-tracker-catalog.md](maltyweb-pm-memory/kpi-tracker-catalog.md) §OPTION C EXECUTED + §FOLLOW-UP 2026-06-10. **This RESOLVES the §THE 4 COGS TILES / step-118(a) / Option-C-ruling thread.**

----LINE 54----
0a5. **✅ SHIPPED 2026-06-09 — PER-FORMAT MAGNITUDE GUARD (NARROWED to cuv serving-tank FILL VOLUME only).** maltyweb commit `f019526`; files `public/js/magnitude-guard.js` (NEW) + `packaging-form.js` + `form-packaging.php`. Scope narrowed per Kouros ("volume/litre fields only") — counts (keg/bot/can) + loss/taproom litres NOT guarded (no HL-vs-L slip mode; an OOM band on counts would false-flag 6-keg Jasper / 100-bottle Chien Bleu). **Band from canonical `ref_serving_tanks` (5/10/30 HL) → 50–30 000 L; SERVER re-evaluates fresh per POST, no-ops if table absent (never blocks a submit).** Wired via existing `FormFramework.extraWarnings()` + outlier confirm-with-comment dialog; token `magnitude_outlier_confirmed=1` → `bd_packaging_v2.audit_flags`; stored value byte-identical on confirm; edit-mode suppresses re-nag (`data-mag-confirmed-fields`). Tests 28/28 client + 8/8 server-isolation (live DB); live UI round-trip NOT auto-tested (webapp-testing lacked prod role) → 4-step manual handed to Kouros. **Historical audit: 255 cuv rows, 0 out-of-band — the ZEPV 100× was purely `ref_skus.hl_per_unit` config in the sales→COGS/compiler lane, NOT operator input** (separately: 9 cuv rows prod_L=0 / neg vendable_hl, ids incl. 2160 [−10HL], 2217 [−5HL] = tank emptyings/transfers, flagged as a different category). **UPSTREAM ✅ RESOLVED 2026-06-10: -V revert (`ref_skus.hl_per_unit` 1.0→0.01 for EMBV/MOOV/ZEPV) + per-litre recompile + compiler liner-branch fix SHIPPED; ZEPV sales material ≈937 CHF, cutover ZEPV gate CLEARED on technical grounds (flip pending only a commit + the cutover-thread procedure). See RESUME #0 🟢🟢 block.** Brewing/racking magnitude guards = possible future extension via the reusable `MagnitudeGuard` helper, NOT built. → [saisie-forms-5-correction-arc.md](maltyweb-pm-memory/saisie-forms-5-correction-arc.md) §MAGNITUDE GUARD §SHIPPED RECORD.

----LINE 55----
0a11. **✅ SHOPIFY⇄MALTYWEB FULFILMENT INTEGRATION — FULL BUILD COMPLETE 2026-06-11 (autonomous; Kouros reviews on return).** Phase 0 (mig **321** maltyweb + ingest `bac5501` maltytask): `ref_shipping_methods` 16 rows + `inv_sales_orders.shipping_method_title`/`fulfilment_mode`; ingest captures `shipping_lines`, classifies pickup/delivery/review, line-idempotency bug FIXED, `--until=now`; May+June backfill → 3127 orders (delivery 2587/pickup 488/review 66), FG delta −5. **⚠️ 15-min ingest cron is on the LOCAL WSL box over tunnel 13306, NOT the VPS — single point of failure, VPS-host recommended.** Phase 1 (`e7962ce`): per-order eshop rows + 🏬/🚚/⚠ badges, read-only, NO `ord_orders` twins, FG untouched. Phase 2A (mig **322**, `5508be1`): `inv_sales_fulfilment` + `_events` side tables + `eshop-fulfilment-status.php` API (mode-aware, review-refuses-advance, push-trigger sets `shopify_sync_state=pending`, NO inline Shopify) + chips/badge — **LIVE, XOR preserved, fg-stock untouched**. Phase 2B (`a08a718`): `push-shopify-fulfilment.ts` write-back worker BUILT but **DISARMED + DOUBLE-GATED** (no cron + token has NO write scopes, 403 confirmed) — activation needs team to grant `write_merchant_managed_fulfillment_orders`+`write_fulfillments` + reissue token + `--apply` verify. Phase 3c (mig **323**, `f0c57de`): `ref_skus.is_non_stock` + symmetric exclusion in BOTH fg-stock fns, verified NO-OP (8003=8003, 0 flagged) — T7 infra ready. **DEFERRED:** 2B activation; Phase 3a/3b (gated on team standardizing Shopify shipping-names); Phase 4 cage/box COGS (gated 06-15 anchor). **✅ Σcards≠Σphysique INVARIANT RESTORED — FIXED+DEPLOYED+VERIFIED+COMMITTED 2026-06-12 (maltyweb `d9f7976` "fix(fg-stock): gate depletion legs on stocktake_scope != 'none'"; NO mig).** ROOT CAUSE was NOT §C sales-attribution — it was the 3 depletion legs (B2B Step4/Leg1, eshop Step5/Leg2, taproom Step6/Leg3 in `app/fg-stock.php`) gated ASYMMETRICALLY across `fg_stock_compute()` vs `fg_stock_location_snapshot()`. FIX = gate all 3 legs symmetrically on `stocktake_scope <> 'none'` in BOTH fns (transfer leg untouched); the dead `is_non_stock=0` eshop filter (mig 322, 0 rows) REPLACED by the scope predicate. **Opus independently verified:** gap=0 + per-SKU divergences=0 (both fns agree to the unit); all 13 scope='none' phantoms excluded; B2B compute expedie=1393 == raw scope!=none shipped=1393 (no legit depletion dropped); eshop/taproom INNER JOINs orphan-fk=0; NULL-sku_id_fk rows (722 eshop/497 bc) were already undepletable pre-gate (no-op); taproom=0 legit (latest bc period 2026-05, leg window period>2026-06); the 4 honest-negatives on REAL inventoried SKUs (ALTB/DGDF/DIB4/STI4C) untouched (scope!=none). **Level note: Σphysique now 15386 (was 8177) because a stocktake census was entered 2026-06-09 → anchor_qty=14701 — NOT a regression, explained by census.** **`is_non_stock` column now fully UNUSED/INERT** — left in place; **RULE 1 scrapping decision PENDING OPERATOR: backfill-from-scope OR scrap (non-blocking).** **🔴 CARRIED-FORWARD OPEN PHASE-4 ITEM (flagged to operator, NOT silently closed): eshop-bundle COMPONENT depletion — a B12 box / 4-pack sale consumes real component bottles, but until the cage/box decomposition engine lands (gated on 2026-06-15 cage anchor + the unit-nesting-ladder decision 24↔single↔12↔4 = which SKU is inventory-of-record) that component drawdown is NOT reflected in FG stock. ZEPV/-V cuve needs NO such follow-up (liquid accounted at tank stage per the -V liner arc).** **⚠️ Parallel sessions committing to BOTH repos' main; only `git add <my-files>` + commit, NEVER reset on shared branches (one reset incident caught + restored this run).** MIG HEAD now **323** applied — re-`--status` at next build. EQUIP per-phase coder+sql(+ui+webapp-testing for 2A). Full as-built + Kouros review summary → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §FULL SHOPIFY⇄MALTYWEB FULFILMENT BUILD COMPLETE.

----LINE 56----
0a7. **🆕 OPERATOR ATTRIBUTION STABILITY + GLOBAL EVENT-RECAP PAGE — PM-RULED 2026-06-09 (pre-build).** Trigger "opérateur"/"saisies récentes"/"attribution"/"Corriger"/"recap saisies"/"event feed"/"journal des saisies"/"who modified" → READ [operator-attribution-and-event-recap-arc.md](maltyweb-pm-memory/operator-attribution-and-event-recap-arc.md) first, recap unprompted. ROOT CAUSE: attribution = `bd_*_v2.email` STRING-COPY of `$me['username']` (NO user FK anywhere on bd_*_v2), re-stamped on EVERY write incl. edit across all 4 forms (~13 sites) → Kouros's Corriger of Gonzalo's racking (id=434, Speakeasy/66) clobbered email→"Kouros Ghavami". Audit ("who/when/what") ALREADY EXISTS + correct in `audit_row_revisions` (log_revision, action insert/update, before/after_json, user_id/username). RULINGS: ASK1(B) stopgap (no mig) = edit path preserves original `email` not re-stamp + fix id=434→Gonzalo (operator-stated, not inferred); ASK1(A) durable (mig) = additive nullable `submitted_by_user_id_fk`→users(id) set-once-never-on-edit, backfill unambiguous-only (refuse-don't-guess), display `users.display_name` (NB username=full-name/display_name=short-name INVERSION). ASK2 = read-only UNION-feed page over bd_*_v2 (NO parallel store), `general` domain sort=15 below sb-board=10 ("Lots en cours"=`sb-board.php`); row identity=(source_table,row_pk); drill-down = live row + `audit_row_revisions WHERE target_table/target_pk`. SEQUENCE: ASK1 first (foundation), then ASK2. EQUIP ASK1(B) coder+sql / ASK1(A) sql+coder+ui / ASK2 ui+sql+coder+webapp-testing. COGS-NEUTRAL/SOT-safe. mig head 292/next free 293 (VERIFY).

----LINE 57----
0a3. **✅ TASK #10 STOCK PF BOARD SHIPPED 2026-06-09 (maltyweb `24d510a`, LIVE+PM-VERIFIED; the spec request 2026-06-09 was answered "already built").** All 4 build-order items DONE: R4 per-SKU anchor fix (latest count per (sku_id_fk,location_id_fk) via MAX(id), SUM across locations, anchor_date=MAX(counted_at) — global MAX(month_closed) zeroing bug GONE), `fg_stock_location_snapshot()`, `expeditions.php ?view=stock` rebuilt (4 location cards + freshness chips + TOTAL strip + 2 derived lines + display_family-grouped per-SKU table, multipacks split), pinned Commandes stock-strip. LIVE-VERIFIED: anchor 2026-06-08, 34 SKU rows, Σcards==Σanchor 7080==7080, Taproom NULL→graceful zero. **OPEN: uncommitted working-tree change to `expeditions.php` = back-date role gate on stocktake POST (managers/admins only; operators coerced to today; explicit '0' writes a stock-out) — separate in-flight piece, needs its own RULE-2 review + commit.** **✅ TWO PF-STOCK FIXES LANDED 2026-06-09 (committed+pushed+deployed, origin/main afe1dcd..566b9d3, NO mig): (a) OUTAGE `ae297c2` — `exp_dow_fr()` was nested inside the stocktake render fn → undefined at the earlier fresh-chip call site → today's count tripped a 500 on the whole PF-stock page; hoisted to top-level (DURABLE: PHP nested fn defs are runtime-declared in call order, keep page helpers top-level). (b) PRODUCTION-LEG `566b9d3` — the PM-predicted bug: flow windows ran off a single global `$anchorDate` while the anchor SUM is per-(sku,location), so today's partial Taproom count poisoned the cutoff and dropped same-day production-site packaging (DIV4/DIVB invisible). Fix = production site DB-derived (`ref_sites site_type='production'`, set-valued no hardcode) + per-SKU prod cutoff MAX(counted_at) at prod site(s) w/ COALESCE→global fallback + `run_type='cuv'` EXCLUDED from prod (3 guards); SALES legs (expedie/eshop/taproom) STILL global — per-location settlement DEFERRED to the stock-movement-saisie arc. Live-verified DIV4 prod=412.54/DIVB=108/ZEPV absent.** **✅ sales-leg per-location settlement NOW CLOSED (ATTRIBUTION) by ITEM 1 2026-06-09 (mig 299):** `fg_stock_location_snapshot()` attributes sales per site via the single `resolve_fulfilment_site()` + applies `inv_stock_movements` transfers; Σ(cards)==Σphysique=8642. **✅ SAME-DAY CUTOFF — ESHOP + TRANSFER NOW CLOSED by TASK A 2026-06-10 (commit `2192153`, NO mig, `app/fg-stock.php`):** eshop leg made SYMMETRIC (compute Step 5 mirrors snapshot's per-(sku,warehouse-site) `>=`+created_at tiebreak via `fg_site_sku_anchor_map()`; strict-`>` fallback for uncounted SKUs) → Σcards==Σphysique under same-day; transfer leg = atomic per-row UNIT-GATE (`$rowPass=$fromPass||$toPass`, both legs or neither → net-zero, compute stays transfer-exempt). SCOPE NOTE in `fg_stock_compute` rewritten to per-leg table. Divergent-anchor fixture (eshop SKU60 + cross-site transfer SKU39 fromPass=T/toPass=F) 10/10 PASS. ACCEPTED TRADE-OFF: same-day inbound transfer to a same-day-recounted site transiently overcounts until next count (PM-ruled OK for the hard net-zero invariant). **REMAINDER (deferred): per-site same-day TRANSFER precision (unit-gate trade-off) + taproom MONTH-granularity** (B2B = strict-global symmetric, intentional). Prod-leg same-day = closed earlier via `d3fb954` (NO DATETIME mig); Task A had no mig (HEAD stays 299). Full record → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §TASK #10 SHIPPED + §TWO PF-STOCK FIXES + §ITEM 1 SHIPPED. **🔴 STALE-INVARIANT WARNING (2026-06-11): the Σcards==Σphysique equalities asserted above (7080==7080, 8642, 8003) NO LONGER HOLD on the live page — `fg_stock_location_snapshot()` now sums 7981 vs `fg_stock_compute()` 8003 (Δ22), confirmed PRE-EXISTING (predates Phase-3c). OPEN data-integrity item — see 0a11.**

----LINE 58----
0a2. **✅ SHIPPED + RECONCILED + PROVEN — `inv_fg_stocktake.row_hash` → STORED GENERATED column (mig 288, 2026-06-08; agent conn dropped mid-run → PM live-verified + reconciled; operator FINAL confirmation = arc record matches reality).** `row_hash CHAR(64) GENERATED ALWAYS AS SHA2(CONCAT_WS('|','fgct',sku_id_fk,location_id_fk,counted_at),256) STORED NOT NULL`, UNIQUE uniq_row_hash, 122 rows/122 distinct hashes/0 mismatch (tautology), form deployed. **FK RULING DONE: both `fk_fg_sku` AND `fk_fg_stocktake_location` re-added as ON DELETE RESTRICT** — live-verified `DELETE_RULE='RESTRICT'` on BOTH (fk_fg_sku was SET NULL; agent dropped it for the gen-col, restored as RESTRICT which MySQL 8 allows on a gen-col input; 0 orphans/0 NULLs; both FK cols now FK-enforced RESTRICT + hash inputs). **RECONCILE COMPLETE:** mig 288 file edited to fully describe live (FK drop + RESTRICT re-add), synced to VPS, `INSERT INTO schema_migrations` done → `migrate.php --status` = 288 applied / 0 pending (288 NOT re-run). **`expeditions.php` stocktake INSERT: `row_hash` DROPPED from col list (deployed live — gen-col is auto-computed); `$rowHash` compute KEPT for the before-snapshot SELECT.** **PROOF passed:** in a rolled-back txn, UPDATE `location_id_fk` 1→3 on a June row auto-changed row_hash (760cf472… → 4be4ec73…) — divergence now STRUCTURALLY IMPOSSIBLE. **Committed maltyweb main: `d1a0e40` (NOT pushed; prior multipack commit `f97bcb9` also local-only).** Separate same-day data-corruption repair (earlier): loc1=27/550u, loc2=28/5976u, loc4=26/554u — intact. EQUIP sql+coder. Forward-couples to task #12 admin edit-path (UNIQUE-collision handling). Full record → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §row_hash divergence §SHIPPED.

----LINE 59----
0. **🔑🏁 KEYSTONE — BOM VALIDATION: ① ✅ + ② ✅ + ③ F2 LIQUID ✅ + ④ DIRECT-SALES TRACKER MVP ✅ SHIPPED 2026-06-09 — ALL FOUR STEPS DONE (④ Phase 2 DEFERRED, gated; see #0b).** ④ shipped as **"Tap&Shop"** (operator-named; page_key='tap-shop', mig 291, commit `a13a9d0`, Opus-verified LIVE). F2 applied (147 SKUs / 1260 deleted / 1557 liquid rows / 0 parity / 0 errors); code maltyweb `9012c8d`+`0956471` (apply branch + parity gate + CLI `--apply`); 2 conventions LOCKED (liquid CHF-canonical; slot_name=stage); operator decisions ratified (DIG YEASTVIT fixed, finings gap-fill + forward-build saisie field, SPY/DIB floors, gate-15); DGD 18 kg/HL cleared. **🟢 F2 follow-ups: finings (`f639632`) DONE, warehouse $glSt cleared; -V cuv costing (`92c9c13`) BROKE sales-COGS (ADJUDICATION REVERSED 2026-06-09) → FIXED + cutover ZEPV gate ✅ CLEARED 2026-06-10 (cuv hl_per_unit revert + liner-branch per-unit generalisation; see the 🟢🟢 block below).** 7 cosmetic out-of-scope NULL rows remain. Full record → [walkthrough §F2 FOLLOW-UPS + §F2 LIQUID APPLIED](maltyweb-pm-memory/sku-bom-validation-walkthrough.md). **NEXT BOM-arc work = ④ Phase 2 (the cage/COGS engine, #0b below) — DEFERRED behind 2 operator-confirmed external deps (Shopify pickup-vs-shipped signal + cage anchor 2026-06-15); recette versioning v2 #0c SHIPPED (mig 289, `a1c68dc`).** **🟢🟢 COGS COST-SOURCE CUTOVER ZEPV GATE ✅ CLEARED 2026-06-10 (Opus live-verified VPS) — flip now GO on technical grounds.** The cuv revert + liner-branch fix shipped: `ref_skus.hl_per_unit` 1.0→**0.01** for EMBV/MOOV/ZEPV(27/46/61) (the only 3 divergent single-format SKUs; découverte composite packs PACKDEC/FRPACKDEC/FRPACKCHEF correctly LEFT ALONE = legit blended composite) + liquid recompile (`--apply --sku 27,46,61`, 0 parity) + **COMPILER DEFECT FIXED** in `app/sku-bom-compile.php` cuv `liner_amortized` branch (was HARDCODED `qty=1.0`/`cost_direct=$cuvLinerCostPerHl` per-HL → after revert, liner stayed per-HL while liquid went per-litre = unit mismatch inside one BOM → `build-sales-cogs.ts:612 materialCHF=units×SUM(cost)` would still mis-cost ZEPV ~7800; FIX = scale BOTH `qty=>$skuHlPerUnit` + `cost_direct=>round($cuvLinerCostPerHl*$skuHlPerUnit,6)`, generalising the hl_per_unit=1.0 special case; deployed + packaging recompiled, 0 parity; CHF/HL DISPLAY unchanged since cost/qty still recovers liner_cost_per_hl). **DURABLE LESSON: 92c9c13's liner-amortization was only ever exercised at hl_per_unit=1.0; it silently broke for any other value — the CHF/HL display gate NEVER caught it (display divides cost/qty), the COGS bug only surfaces in the SUMMED per-unit sales-material path. A COGS-bearing compiler line MUST be gated on the sales-material path, not display/conservation; per-unit lines MUST scale by the SKU's own hl_per_unit, never hardcode a basis.** Live-verified: `Σref_sku_bom.cost` ZEPV 0.096594/EMBV 0.224576/MOOV 0.121062, skuHL=0.01; ZEPV sales material ≈**937 CHF** (~100× phantom GONE); `--cost-source=diff` 2026-05 prints NO cuv per-SKU diff lines (sheet==mysql). Rollback snaps `_snap_refskus_cuv_hlrevert_20260610`/`_snap_skubom_cuv_hlrevert_20260610`. CONSISTENCY RULE STANDS (LOCKED): litre-denominated SKUs carry `ref_skus.hl_per_unit=format.hl_per_unit`, never diverge (JASPL=0.01 canonical). **🏁🏁 CUTOVER FLIPPED + SHIPPED + COMMITTED 2026-06-10 — maltytask `0129bdb` `feat(sales-cogs): cut over default cost-source sheet→mysql (post-F2)`.** `build-sales-cogs.ts` default `?? "sheet"→"mysql"` (~L131; `--cost-source=sheet` legacy fallback, `=diff` audit) + folded `run-month-close.js` step-3 (build-sku-bom) drop from critical-path parallel block. Deps already committed: maltyweb `6b8f6a0` (cuv liner per-unit fix) + `7fbeecb` (mig 302 DB-trigger lock cuv hl_per_unit=0.01) + the cuv revert+compiler-fix above. POST-FLIP VERIFY (regression tripwire): `build-sales-cogs --month 2026-05 --dry-run` NO flag → header `cost-source=mysql`, clean, wrote `interfaces/sales-cogs-data.json`. **May 2026 (mysql): 73 SKUs, COGS CHF 59,676.02, NonBeer 31,617.70, BeerTax 12,735.39 (sales 11,813.43 + invendables 212.03 + contract 709.93 on 46.74 HL); ZEPV 936.96.** RULE-2 GO signed off; drift = benign post-F2 pattern. TWO post-flip RESIDUALS (NOT blockers, BOTH PARKED 2026-06-10): (R-A) 3 unknown sales SKUs (`sales-sku-unknown=3`, pre-existing → triage pass post-close); (R-B) contract beers BAMB/BAMF/JASPB/JASPF/JASPL null beer-tax cat=0 CHF → PARK pending accountant sign-off (tax-liability-routing decision, NOT a code defect; do NOT guess the Cat-per-OG — COGS/tax-impacting). The PRIOR "RE-OPENED" verdict below is HISTORICAL.** Root cause: `mysql-sku-bom.ts` builds `skuHL` from `ref_packaging_formats.hl_per_unit` (=0.01) but `skuCost` from `ref_sku_bom` rows compiled at `×ref_skus.hl_per_unit (=1.0)` → TWO bases, same SKU; `build-sales-cogs` does `units(=litres 9700) × skuCost(9.8873 per-HL) = 95,907 CHF` (~100×). beer-tax stayed sane (uses `units×skuHL`=97 HL). **CANONICAL -V BASIS = PER-LITRE `hl_per_unit=0.01`** — matching format-18 (0.01, '1L liner'), sales uom='L' + hl_resolved (litres/100), and JASPL=0.01 (the contract -V sibling). **CONSISTENCY RULE LOCKED: ref_skus.hl_per_unit MUST EQUAL format.hl_per_unit for litre-denominated SKUs; NEVER diverge.** RESOLUTION ✅ DONE 2026-06-10: **liner session REVERTED hl_per_unit→0.01 for EMBV/MOOV/ZEPV + recompiled -V liquid+liner_amortized at 0.01 (their lane, `app/sku-bom-compile.php`) AND fixed the hardcoded-per-HL compiler defect that the revert exposed; the held flip is now UNBLOCKED on technical grounds (see the 🟢🟢 CLEARED block above).** Then ZEPV skuCost≈0.0989/litre → litres×cost=959.07 CHF ✓ (=97 HL × per-HL 9.8873). The `92c9c13` "phantom GONE" gate-PASS verified only CHF/HL display (9.89) + packaging conservation, NEVER the sales-COGS litre-units path = insufficient COGS gate (the standing failure mode). Packaging-conservation proof (non-cuv 2001.4473→1908.9059, −92.54 cuv liner delta) STILL HOLDS — it just doesn't certify the sales path; the liner amortization NUMBER is correct either basis, only the multiplier flips. **NEXT for the MySQL flip = (after liner-session revert+recompile) re-run `build-sales-cogs --cost-source=diff`, confirm ZEPV ~959 CHF, then flip default sheet→mysql + RULE-2 + final diff** (BLO/BLA not-sold-in-May, off critical path). The F2 finings + warehouse.php $glSt follow-ups are DONE (finings commit `f639632` = discoverability hint + observed-wins auto-retire, racking finings deferred; $glSt CLEARED not-affected). Cosmetic residual: bom-review.php shows MI-102 catalog price on the `liner_amortized` line (display-only). → walkthrough §ADJUDICATION REVERSAL + §COGS COST-SOURCE CUTOVER. EQUIP coder+sql.** *(Below is the now-HISTORICAL handoff-checkpoint narration, superseded by the apply.)* **HISTORICAL: ③ F2 was IN OPERATOR'S HANDS (handoff checkpoint 2026-06-08).** D1 ✅ SHIPPED+VERIFIED (`5186454`/`9fec7d5` — inactive-MI fix + §8.1 mechanism-exclusivity + clear_sku_choice) + D2 ✅ SHIPPED+PUSHED (`9fa60a7` — oldest-invoice/gate-15 into dry-run). **F2 dry-run REGENERATED post-`9fa60a7` (gate-15 included): 142 in scope, 79 gain/49 lose/14 no-change, +800/−588/668 changed lines, 0 errors, 0 unresolved MIs; canonical preview `/var/www/maltytask/data/sku-bom-liquid-preview.json` (REGENERATE at apply — gate-14 staleness).** Engine Opus-re-verified (EMBF DH 566.38 g/HL; DIG/DIP pilsener OI 0.6331; per-HL identical per beer). **Operator review artifact = `/home/kluk/projects/maltytask/reports/f2-liquid-review-digest-2026-06-08.md` (REBUILT off corrected preview; 18 beers; multi-brew malt/hops numbers updated + supersede note, single-brew + dry-hop unchanged).** Operator resumes the walk on corrected numbers → ONE apply dispatch. **✅ MULTI-BREW BASIS BUG FIXED + OPUS-VERIFIED 2026-06-08 (maltyweb commit `9012c8d`, NOT pushed; file `app/sku-bom-compile.php` `compile_sku_bom_liquid`, no mig):** the bug = malt/kettle numerator was ONE per-batch grain bill (= one brew) ÷ `batch_hl=SUM(cool_final_volume_hl)` over ALL N parallel brews → ÷N understatement on every multi-brew beer (DIG/DIP/single-EPH N=1 unaffected). FIX = §2a GROUP BY adds `COUNT(*) AS n_brews`→`$batchNBrews`; §2b malt+hops_kettle accumulators × n_brews per-batch BEFORE MAD/averaging (dry-hop/minerals/gap-fill/oldest-invoice/packaging UNTOUCHED; defaults n_brews=1). **Opus INDEPENDENT verify (preview 2026-06-08 13:03 UTC): apply-gate-16 (a)-(e) ALL PASS** — EMB total malt 21.03 kg/HL (Munich 7.368 + Pilsener 13.662) vs recette 22.83 = ~8% real drift ✓ (EMB b181 hand-check 347×4÷124.5 = 11.1486 ✓); STI 17.52 ✓; **DIG byte-UNCHANGED 10.27 kg/HL = regression-guard PASS**; 0 errors/0 unresolved. CHECK confirmed: saisie enters grain bill per-single-brew + one cooling row per brew; N_brews = cooling-row count reliable (0 zero-cooling batches; dist 18.5% N=1/42% N=3/30% N=4); **the correct N_brews scaling ALREADY EXISTS in `warehouse.php`+`warehouse-export.php` WIP costing — F2 was the LONE omitter** (no shared helper needed; blast-radius §5 resolved). **apply-gate-16 RESOLVED-pending-operator-rewalk** (no longer a build blocker — remaining gate = operator multi-brew re-walk on the rebuilt digest). Scope drift 142→147 / 146→151 = 5 new active SKUs (daily prod drift, NOT a fix artifact — gate-14 regen-at-apply covers it). **🟡 TWO FOLLOW-UPS (logged, NOT part of fix): (1) DGD 18 kg/HL Pilsener is NOT multi-brew (DGD single-brew n_cool=1) → SEPARATE data issue (unit/typo), confirm vs brew record at DGD re-walk; (2) `warehouse.php` `$glSt` FG-valuation query ~L564 may share the one-brew÷batch-HL mismatch for GL cost ratios → SEPARATE review after F2, EQUIP coder+sql.** Detail → walkthrough §MULTI-BREW (FIX LANDED + apply-gate-16). **APPLY PREDICATE NOT YET BUILT** (DELETE source='Brewing' AND bom_source IN(NULL,'liquid') AND mi_id NOT NULL per SKU + reinsert bom_source='liquid' + snapshot + log_revision + idempotent + gates 1-15; EQUIP coder+sql, +ui only if on-page render). **2 MUST-DECIDE-BEFORE-APPLY: (1) 🔴 DIG PROC_YEASTVIT ~22000× recipe-spec scale error — fix `ref_recipe_ingredients` first; (2) 🟠 DGD MALT_PILSENER 18.04 kg/HL implausible — NOW ISOLATED as a single-brew DATA-ENTRY issue (NOT the multi-brew basis bug; DGD n_cool=1) → confirm vs brew record at DGD re-walk.** 9 other anomalies + still-open operator decisions (SPY floor confirm, finings per-beer, gate-15 sign-off, dormant-EPH3 OI, dry-hop data-gap acceptances) → [walkthrough §F2 REVIEW HANDOFF CHECKPOINT](maltyweb-pm-memory/sku-bom-validation-walkthrough.md). **(b) ④ DIRECT-SALES TRACKER — Phase 1 MVP ✅ SHIPPED 2026-06-09 ("Tap&Shop", mig 291, commit `a13a9d0`, Opus-verified LIVE).** New read-only top-bar page `public/modules/tap-shop.php` (page_key='tap-shop', **ref_pages id=186**, domain='logistics', **min_role='operator'** [seeded 'manager', FLIPPED to 'operator' 2026-06-09 — see TOPBAR-VISIBILITY FIX below], sort=105 right after Expéditions; access via ref_access_preset_pages presets 1+3+8). Plane A = Shopify (inv_sales_orders/lines) + taproom (inv_sales_bc channel='taproom') sales per beer/SKU/period with "Non rattaché" bucket for unresolved lines (visible, never dropped), dual-channel bars via KpcCharts, per-beer/SKU table by display_family. Plane B = virtual consumption stock = `physique` from `fg_stock_compute()` — ONE call (L142, the SAME derivation the weekly stocktake uses); page WRITES NOTHING (read-only verified by grep: only comment-mentions of INSERT/UPDATE/DELETE). Forward-compat Phase-2 hooks already in: `vd_load_direct_sales($pdo,$from,$to)` normalizer; SKU set gated on `is_direct_sales`; sku_id carried throughout (no name strings); inert "Consommation directe (à venir)" placeholder panel. Opus verify: read-only ✓ / single fg_stock_compute call ✓ / mig 291 recorded ✓ / ref_pages row correct ✓ / HTTP 200 / ALT4 physique=62 matches fg_stock_compute ✓; pushed. **④ Phase 2 = the cage/COGS engine — DEFERRED, GATED on 2 operator-confirmed external deps: (1) Shopify pickup-vs-shipped signal ON HOLD (team is actively reworking Shopify's shipping/pickup setup — wait so we key on the FINAL mechanism, not a moving target); (2) cage opening anchor — physical count starts MONDAY 2026-06-15, load into `inv_fg_stocktake` then.** Phase-2 scope unchanged: cage-vs-loose routing, `inv_stock_residual` ledger, `inv_consumption` writes (sales_derived + cage draw-down), COGS-into-COP, eshop carton bin-packing. Switch-on once BOTH deps land; Tap&Shop is forward-compatible for it. **Keg-share (draft pours -P25/-P50) handled SEPARATELY (packaging-template batch), NOT in ④ — operator-confirmed.** Operator checkpoints 2026-06-08: cage inventory anchor, keg-sticker count, invoice-feed list. **✅ TOPBAR-VISIBILITY FIX LANDED 2026-06-09 (data-only, no deploy, Opus-verified live): `UPDATE ref_pages SET min_role='operator' WHERE page_key='tap-shop'` (id 186; txn-wrapped + audit_row_revisions before{min_role:manager}→after{min_role:operator}). CONFIRMED LIVE: id 186 min_role='operator', domain='logistics', is_active=1; Olivier=user 19 role='operator' preset 8 with user_page_access granted=1 on 186 (his grant was already correct); `user_can_access('tap-shop',{19,operator,preset 8})`===true via PHP CLI. He sees the link on next refresh (per-request perm cache, no re-login). ROOT CAUSE = the page was SEEDED min_role='manager' (oversight) AND Olivier's per-user grant was set BEFORE min_role was relaxed — `user_can_access` floors on min_role, so the grant alone couldn't surface it. The "step-3-before-step-4" ordering footgun (per-user grants applied while min_role still excludes the role → silently invisible despite granted=1) is NOT addressed in code — left as a separate potential future change (operator-confirmed defer).** **⚠️ 2026-06-08 CHIEN BLEU CONTRACT BUILD touched F2-adjacent state — PM-RECONCILED, NO F2-REVIEW IMPACT:** (i) canonical VPS preview `data/sku-bom-liquid-preview.json` was REGENERATED full-scope at 11:04 (now `skus`=**147** = the digest's 142 in-scope + **5 NEW CONTRACT SKUs** on recipes 20 Bamberg / 21 Jasper — ISOLATED recipes, NONE in the 18-beer digest set; liquid compile deterministic → the 142 existing per-beer numbers are byte-identical superset, NO restore needed, digest at `reports/f2-liquid-review-digest-2026-06-08.md` STILL VALID — it cites scope=142 from the 03:42 UTC run; the apply still REGENERATES per gate-14 anyway). (ii) Contract SKUs added to F2 population: JASPB(312)/BAMB(313)/JASPL(314) bottle/bulk + JASPF(308) keg, all recipe 20/21. **NEXT F2 SESSION: scope is 142 Neb-lineup SKUs; the +5 contract SKUs ride along in the prew but are reviewed on their OWN contract terms, not in the Neb walk.** (iii) **PACKAGING METRIC moved by EXPECTED POPULATION, NOT REGRESSION:** ALL-SKU bom_source='packaging' now **576 rows / 2008.9371 CHF** (was 572 / **2001.4473**); the +4 rows / +7.4898 CHF = EXACTLY JASPB+BAMB bottle(3.3249)+outer_box(0.4200) each — packaging EXCLUDING the 4 contract SKUs is **byte-identical 2001.4473**. **2001.4473 remains the byte-stable Neb-only anchor; 2008.9371 is the new ALL-SKU total incl. contract.** active-only = 1995.29 (population). Next F2/packaging gate-check must use the correct denominator — do NOT alarm on 2008.94.

----LINE 60----
0c. **✅ RECETTE TEMPORAL VERSIONING (v2 SCD2 write-side) — SHIPPED+VERIFIED+PUSHED 2026-06-09, commit `a1c68dc` + mig 289 (head 289 / NEXT FREE 290). RESUME #0c CLOSED.** The "likely NO migration" guess was WRONG — UNIQUE `uq_recipe_mi_stage` (dates not in key) → 1062 on first edit; resolved Option A (mig 289): STORED gen-col `open_key = CASE WHEN effective_until IS NULL AND is_active=1 THEN 1 ELSE NULL END`, DROP `uq_recipe_mi_stage`, ADD UNIQUE `uq_rri_open(recipe_id,mi_id_fk,stage_key,boil_time_key,open_key)` (≤1 open row/slot, unlimited closed history). Backfill 164 rows effective_from='2025-01-01'/until NULL (0 null_from / 164 open / 164 genesis). Code: `app/recipe-ingredients-loader.php` `rri_close_version(PDO,$id)` helper (read-side L174-175 UNCHANGED); `salle-de-controle.php` 3 hop write-actions → close-then-insert (`set_hop_stage` L1534 / `add_hop_addition` L1659 / `delete_hop_addition` L1882 = CLOSE not tombstone, is_active stays 1), each txn-wrapped; display query L2148 + `AND effective_until IS NULL`. Boundary: from INCLUSIVE / until EXCLUSIVE / CURDATE() server-side. **Opus live-verified: indexes/gen-col/backfill/mig-row/helper ✓; 1062-avoidance + 3-asof boundary PASS; EMB r32 11-ingredient set no-regression ✓; `ref_sku_bom` UNTOUCHED 2427 ✓.** SCOPE: only the 3 hop actions write `ref_recipe_ingredients` today (no generic qty editor) — close helper reusable so future generic editor inherits SCD2 free. STILL OUT (later increments): version-history display UI in SDC; as-of awareness for form-fermenting.php dry-hop EXISTS check; v3 full retrofit. Detail → memory/project_recipe_versioning_temporal.md.

----LINE 64----
3b. **✅ device-classification interstitial SHIPPED+COMMITTED (`94d5a0d`, ZERO mig; AS-BUILT CORRECTION 2026-06-08: `login.php`-ONLY step — set-password/invite does NOT classify the device, so invite path = set-password → auto-login → tour → mon-tableau with NO device prompt; set-password auto-login does NOT stamp last_login_at, only login.php does; remember-me deferral is login.php-scoped) + ✅ VISITE GUIDÉE tour SHIPPED (`ca65c22`, mig 280) + ✅ tour CHAPTER EXPANSION SHIPPED+LIVE+PUSHED (`91f8c82`, 2026-06-08: 25 steps production / 11 logistics, vetoes grep-verified)** — all LIVE before the 06-08 AM logins. **✅ login.php divergence CLOSED (post-midnight sync 2026-06-07): wiring COMMITTED via `ca65c22` (carries tour-redirect + interstitial wiring) + PUSHED; PM-verified tree clean of it.** Standing convention: new ref_pages entry → add bespoke tour content row, NOW ENFORCED by the **TOUR-STEWARD SYSTEM (SHIPPED 2026-06-09, RULE 3)** — `scripts/tour-gap-check.php` drift detector + `migrate.php` post-apply advisory + `maltyweb-tour-steward` agent (PM dispatches after any ref_pages build; sensitive-class pages return `PM-RATIFY:` for PM ratification). ⚠️ NOT YET COMMITTED: visite-guidee.php (tap-shop+qa cards) + tour-gap-check.php (new) + migrate.php (advisory) deployed-live-but-uncommitted. STILL-OPEN SEPARATE BUG: form-packaging.php ~L2451-2452 helper text shows raw `prod_total_units`/`qte_unites` (no-DB-nomenclature violation on the form itself). → [user-auth-admin-arc.md](maltyweb-pm-memory/user-auth-admin-arc.md) §EVENING CLOSE-OUT + §VISITE GUIDÉE (full §TOUR-STEWARD SYSTEM record there).

----LINE 68----
6d. **✅ "New Yeast" BBT/CCT board placeholder — SHIPPED + DEPLOYED + SMOKE-VERIFIED 2026-06-11 (render-only derive-at-read; 3 files UNSTAGED/uncommitted per shared-tree discipline; NO mig, NO raw mutation).** Both boards derive the observed strain via `app/yeast-eligibility.php` (new helpers L264-325): `yeast_observed_strain_expr($alias)`→SQL `IF(<alias>.yeast='New Yeast',<alias>.new_yeast,<alias>.yeast)` (alias whitelisted `[A-Za-z0-9_]`, injection-safe); `resolve_observed_yeast_strain(PDO,$yeast,$newYeast)`→coalesce then precedence (a) exact CI `ref_yeast_strains.name` (b) CI `ref_yeast_strain_aliases.alias` JOIN strain (c) raw + `resolved:false`; NEVER invents. tanks.php CCT query L335-356 uses the expr, resolves post-fetch → `detail.yeast.strain` (cct-detail-modal.js UNTOUCHED, inherits). packaging.php BBT `$bbtYeastStmt` resolves → escaped chip "Levure · {strain} (G{gen})". Live probes: Speakeasy b64→"Pomona", Diversion b46→"Pinnacle Low Alcohol"; render smoke both boards 0 console errors / 0 "New Yeast" leaks (BBT 5 Pomona G7, BBT 2 Pinnacle Low Alcohol G1). **DIVERGENCE FROM PRE-BUILD RULING (working-as-designed, NOT a bug): `apinnacle` does NOT render raw/ambiguous — alias id=34 (`apinnacle`→strain 15 Pinnacle Low Alcohol, operator-curated 2026-05-11 per mig 228) resolves at step 2b; that's a curated decision, not a guess. Lever to show it raw = delete alias id=34 (operator call). NO live in-tank new_yeast renders unresolved today.** **STILL-OPEN FOLLOW-UPS (gated): (1) retire bogus `ref_yeast_strains` id=69 'New Yeast' (no family) — separate migration, gated on apply-time re-verify of 0 live FK refs (v1 FK constraint still live mid-decommission); NOT started. (2) alias seeds for any historical messy new_yeast lacking canonical/alias match — operator-gated, explicit mappings only, NEVER guess; none in-tank, matters only for historical board views.** GIT: 3 files (app/yeast-eligibility.php, public/modules/tanks.php, public/modules/packaging.php) modified+UNSTAGED; the earlier BBT yeast-chip build (packaging.php + app.css `.tank-card__yeast`) is part of this same uncommitted set; tree also carries unrelated parallel-session work (financier.*, saisies.php, mig 330) — deploy-pushes-working-tree, `git status` before any `--apply`. → [yeast-strain-taxonomy.md](maltyweb-pm-memory/yeast-strain-taxonomy.md) §OBSERVED-STRAIN DERIVE-AT-READ.

----LINE 69----
6b. **✅ BURST-BAG / PACKAGING-LIQUID-LOSS MIS-BOOKING — SHIPPED+COMMITTED 2026-06-09 (maltyweb `3d96847`; mig 292; COGS/WIP/beer-tax-impacting; OPEN months touched).** Compute-fn FLOOR (vendable bccomp-floored at 0 in `compute_packaging_vendable_hl()` keg/cuv branch ~L298, NO-OP on 1015 real runs) + view mirror **mig 292 `292_floor_keg_cuv_vendable_view.sql`** (keg/cuv arm `GREATEST(0,…)`, tax arm derives from it; applied via direct CREATE OR REPLACE VIEW NOT apply-all, to dodge the parallel session's in-flight `291_tap_shop_page.sql`; schema_migrations row inserted manually — **MIG HEAD = 292 / NEXT FREE = 293**) + cuv/keg loss routes to typed `loss_keg_liquid_l` (form field relabelled "Perte liquide / poche éclatée", packaging-form.js dynamic on run_type) + tank-sim drain predicate broadened to `vendable_hl>0 OR loss_kpi_hl>0`, drain=vendable+loss_kpi, PHP guard skips sum≤0 (protects 1303); lost beer now DEPLETES the tank (Kouros approved). **Backfill = 6 pure-loss rows fixed** (4 ZEPV cuv 1297/1320/2160/2217 + 2 keg 1335 EMBF/1998 SPYF — the 2 keg sibling rows folded in); all vendable=0/tax=0/loss_kpi=5/3.5/10/5/0.2/0.02, stored==view confirmed; **+23.72 HL restored to packaged-HL AND beer-tax base** (2024-06 +8.5 / 2024-07 +0.2 / 2025-12 +0.02 / 2026-04 +10.0 / 2026-05 +5.0 — last two OPEN, COGS/tax live). **5 void all-zero cuv rows (1385/1386/1406/1407/1408) HARD-DELETED + tombstoned.** **✅ RESOLVED 2026-06-10 (was a 🔴 CRITICAL FINDING — "held -V cutover #0/#0a5 is URGENT"):** the read-path view `v_bd_packaging_v2_vendable` computes vendable=(prod/units_per_pack)×s.hl_per_unit, so while EMBV/MOOV/ZEPV carried hl_per_unit=1.0 the view recomputed all 245 cuv PRODUCTION rows at 100× (stored cols correct; only the VIEW was hot). The -V revert to 0.01 SHIPPED 2026-06-10 → SELF-RESOLVED. **Opus live-verified: view id 1298 ZEPV vendable_hl=5.0 == stored 5.0** (was 500.0). Cuv view volumes now match stored; burst rows were always immune (prod=0). **✅ Row 1303 (STI4C can) RESOLVED by the loss-model-consistency fix #6c (`7a8b63a`): vendable −23.4→+19.8, loss_kpi 0.69→43.9 — no longer open here.** → [packaging-losses-convention/burst-bag-liquid-loss-arc.md](maltyweb-pm-memory/packaging-losses-convention/burst-bag-liquid-loss-arc.md) §SHIPPED RECORD.

----LINE 70----
6c. **✅ LOSS-MODEL CONSISTENCY — SHIPPED + COMMITTED 2026-06-09 (maltyweb `7a8b63a`; mig 293 `293_loss_model_no_liquid_subtraction.sql`; COGS/WIP/beer-tax-impacting across history; Opus independently verified).** Canonical model LIVE: liquid losses (`loss_liquid_other_units`, `loss_keg_liquid_l`) NO LONGER subtract from vendable in ANY branch → they sum into `loss_kpi_hl`. bottle/can vendable = good filled units only; keg/cuv vendable = hlGross − taproom (**taproom KEPT — it's a subset of prod, taxed via hlGross; I REFINED the original "drop all three" ruling which would have double-counted taproom**). mig-292 GREATEST/bccomp floor SUPERSEDED — its view logic deleted in the 293 rewrite (292 file lingers on disk, dead/already-applied; do not re-run). Files: `form-packaging.php` (compute fn) + `app/packaging-stats.php` (loss-% double-add removed) + mig 293 (view rewrite). **Backfill: 733 already-computed rows → +540.8 HL restated to vendable AND beer_tax_base, +534 HL newly visible in loss_kpi (2023 +94.4 / 2024 +205.4 / 2025 +156.0 / 2026 +85.1); row 1303 STI4C-can catastrophic run vendable −23.4→+19.8, loss_kpi 0.69→43.9; 0 negative-vendable after; stored==view verified bot/can/keg.** Row 1303 RESOLVED by the model (REMOVE from burst-bag #6b STILL-OPEN). **🔴 CRITICAL SAVE — the build agent's backfill OVER-REACHED:** its recompute hit the compute fn's generic `$isContract` fallback and newly populated 57 previously-NULL CONTRACT rows (BLZ, Abbaye de St-Maurice, MeltingPote, Septentrion, Obrist…), adding +858 HL of contract beer to the tax base (true total +1399, not the dry-run's +554). Opus caught it via independent snapshot verification and **REVERTED those 57 rows to NULL.** → do-not-repeat now in conventions-and-helpers.md §CODING (backfill must not auto-populate contract rows; the magnitude/row-set gate). **FISCAL DE-RISK:** `bd_packaging_v2.beer_tax_base_hl` is read ONLY by packaging dashboards (packaging-stats.php, packaging-loss-types.php); the FILED beer tax is computed by `lib/beer-tax.js` from brewing-side OG×volume, NOT this column → the +540.8 restatement is an internal-metric correction, almost certainly NOT a filed-declaration change (confirm declaration basis w/ accountant; fiscal alarm largely moot). **cuv:** stored correct (verified @0.01); ✅ the -V `hl_per_unit` revert LANDED 2026-06-10 (#0/#0a5) → view reconciled (Opus-verified id 1298 view==stored 5.0), no longer 100×. RULE-2 MEDIUM finding (view bot/can beer_tax omits unsaleable) = FALSE POSITIVE (achieves +unsaleable by not-subtracting; 0 stored-vs-view divergence). Snapshot `bd_packaging_v2_lossfix_snapshot_20260609143950` retained on VPS for rollback. **✅ FOLLOW-UPS #1 + #2 RESOLVED 2026-06-09 (DB-ONLY, NO git, Opus live-verified) — operator directive "mint Néb SKUs at standard formats, Néb pays contract Biersteuer, compute all" then left for the day. (1) 20 Contract recipes → 28 NEW ref_skus at standard fmts (bot fmt1 0.0792/24, keg fmt15 0.20/1, can33 0.0033/1; reused Chien Bleu BAMB/BAMF/JASPB/JASPF), relinked+computed 162 bd_packaging_v2 rows (row_hash=SHA256(recipe_id|format_id|sku_code)). (2) CollabIn DGDB(r31 sku_id=8)+DOCB(r29 sku_id=21) via EXISTING real SKUs (0.0792) — DGDB vendable 10.108 HL (closes April ~10 HL gap), DOCB 3.541; CollabIn taxes via SALES not contract-packaging → NOT in the contract base. 🔴 **MAGNITUDE FLAG (accountant review): Contract bd_packaging_v2 = 162 rows / Σvendable 2958.265 HL / Σloss_kpi +28.247 (live-verified; ALL 162 feed `loadContractPackagedHL` = contract Biersteuer base). ~3.4× the +858 HL over-reach discussed — agent computed ALL 162 NULL contract rows at REAL standard formats (not just the 57 loss-bearing); rule-correct + operator-approved-in-principle, SIZE needs accountant review before any contract Biersteuer declaration consumes it. Nothing auto-files overnight (contract tax on-demand via reports).** NOT a guess — standard formats + explicit operator directive = canonical seeding (supersedes the run_type-fallback guess that left them NULL). Snapshots (live-verified): **`bd_packaging_v2_contractfix_snapshot_20260609_153201`** (162 rows; NOTE underscore before time) + `bd_packaging_v2_lossfix_snapshot_20260609143950`. DEBT (non-blocking): 6 minted codes flagged rename (PALEC/PALEF id326/327 r37, SESSC/SESSF 328/329 r38 = Le Traquenard generic; LAPTB/LAPTF 330/331 r40 truncated); ALL 42 contract recipes have `ref_recipes.sku_prefix=NULL` → SDC activate_format blocked until set. **STILL OPEN: (3) 2 May rows 2198/2200 (alex, May 4, bot, prod 1760/2362) — genuinely unidentifiable, need alex to name the beers (missing from May close); plus the -V hl_per_unit revert (#0/#0a5).** Contract fix = DB-only, no commit. Session commits: `f019526` magnitude-guard / `3d96847` burst-bag / `7a8b63a` loss-model. EQUIP coder+sql+ui. → [loss-model-consistency-arc.md §CONTRACT-PACKAGING GAP CLOSED](maltyweb-pm-memory/packaging-losses-convention/loss-model-consistency-arc.md).

----LINE 74----
10. **✅ MIG-278 COLLISION REMEDIATED (PM-verified live 2026-06-07 ~23:30: head = `280_users_tour_seen_at.sql`, applied_at preserved, no stale row; NEXT FREE = 281).** Remediation as ruled: rename→280 + paired `schema_migrations` UPDATE same step + **manual rm of the stale 278 file on the VPS (rsync deploy does NOT delete removed files — a renamed migration's old filename lingers and shows "pending")**. Concurrent-arcs sweep: (a) **✅ PUSH DONE (post-midnight sync 2026-06-07)** — main == origin @ `6f0e773`, ALL evening commits pushed (`ca65c22` tour incl. login.php wiring → RESUME #3b closed; `f7fe151`, `b838cca`, `94d5a0d`, `1d8672b`, `803d3f8`, `91ce8c0`; plus newly-observed `5186454`/`9fec7d5` = the two Dispatch-D1 commits — on main+pushed, data-outcome debrief PENDING: live `ref_sku_bom` packaging census NOW 655 rows/2013.36 CHF ≠ recorded 580/2001.45, and 8 active `-X` cage SKUs already exist — attribution pending parallel-session debrief, do NOT treat 2001.45 as current; `b676c9b`/`dcbce1d` fg-stock, `1b9694a` mig-280 header); mig 281 = CRM contact cols, APPLIED+committed (see §CURRENT BUILD-STATE) — **NEXT FREE = 282**; (b) **✅ EXPÉDITIONS arc — ALL PRE-FLIP BUILDS DONE (2026-06-08 overnight, main==origin @ `6dd5521` PM-verified)** — CRM bootstrap `73879bc` + Carnet clients `ff70261` + merge-decision pipeline `9803849`/`6dd5521` (368→4 buckets xlsx → convert → audited apply CLI; blank decision = NON never merge; «Arches»→«Marchés» 0.92 = the no-fuzzy-links proof case) + saisie live-stock hints `134bbdf` (advisory amber, never blocks — overselling legal) + tour content `a59d94d` (R7 CLOSED) + logistics preset already pre-wired by auth arc (id-based, rename carried — nothing to build); auth smoke DEFERRED (temp-user creation correctly denied by classifier; NB smoketest acct id=16 exists for next time). REMAINING = operator morning runbook ONLY: merge decisions (368) → admin click-through → flip `is_active=1` → 06-08 re-anchor (then R2/R3 `fg_stock_compute()` re-exam) → 3 off-trade ambiguous + R6 link rule → [fulfilment-expeditions-arc.md](maltyweb-pm-memory/fulfilment-expeditions-arc.md) §2026-06-08 OVERNIGHT; (c) KPI v2-tranche fold into kpi-tracker-catalog.md (partially done via Mon-tableau-v2 record).

----LINE 78----
12. **🏁 SHIPPED + LIVE + E2E-VERIFIED 2026-06-08 — Ingestion validation gate + SIE/SIL energy web-path (Phases A+B1a+B1b+B2; migs 282/283/284, head 284 / NEXT FREE 285).** NO MORE BSF/Drive (Drive sourcing RETIRED). **NOTHING auto-writes** — worker STAGES the gate's write-plan to `doc_invoices.delivery_write_plan` + energy to `energy_extract`, writes ZERO inv_deliveries; operator validates → `--commit` replays the plan via the IDENTICAL `appendDeliveryToMysqlWithConn` loop (one writer). Commits: A=maltytask `c80f6f8`/maltyweb `fd1c5ed`; B1a=maltytask `8c991a6`/maltyweb `d894b6f`; B1b=maltyweb `6973316`; B2=maltyweb `e0e580d`. SUDOERS gap closed. **SIE period convention: dated 21 May 2026 → keyed 2026-04 (consumption-period); 66 kW / 829.5 kVArh.** List filter `delivery_write_plan IS NOT NULL` = forward-only.

----LINE 79----
12b. **🏁 À-VALIDER → TRIAGE MERGE SHIPPED + COMMITTED + LIVE-PROVEN 2026-06-08 (maltyweb main `484cc6a` "feat(triage): À-valider queue as triage tab + fix delivery double-write", 8 files: shared partial valider-queue.php, triage.php tab, 3 triage-alias validated_at stamps, invoice-validate.php thin wrapper, overlay-bleach CSS fix, poll JS fix).** Built to the merge verdict: triage `?tab=valider` tab + count badge `$countValider` + body-class toggle; NEW shared partial `public/modules/partials/valider-queue.php` = THE single list renderer; `invoice-validate.php` now a thin wrapper (orphan-page RULE-1 scrap satisfied). **Lenzlinger double-write landmine CLEARED:** Fix A (list query `AND NOT EXISTS … inv_deliveries d WHERE d.file_id_fk=df.id`) + Fix C (triage-alias resolution stamps `validated_at=NOW(),validated_by=<operator id>` at the `$openLeft===0` chokepoint in alias.php/create.php/manual-lines.php — reject.php deliberately NOT stamped) + inv 208 backfilled out. **✅ WATCH-ITEM CLEARED — live Valider PROVED OUT END-TO-END:** operator validated the clean Tinguely invoice (`doc_invoices.id=209`, FAC `01-26050180`) in production → commit worker ran the full chain (recap-lines.php → `/api/invoice-validate.php` → ingest-one-local-commit.sh → `delivery_write_plan` replay) → **7 Active inv_deliveries written (source Invoice-OCR), `validated_at` + `validated_by=1` stamped, HT 641.61 reconciles.** The single commit path (POST `/api/invoice-validate.php`) is now field-proven. **BUG SURFACED + FIXED during the proof (in `484cc6a`):** tsx cold-start can exceed the UI's 2-min poll cap → a *successful* commit showed a false red "✗ Échec"; FIXED in `public/js/invoice-validate.js` = poll cap 2→4 min + a final status re-check on timeout + a neutral "Toujours en cours / Recharger" state instead of red failure. **This is the single-invoice CANARY for the bulk-validate fan-out risk — "Tout valider" fires N concurrent cold-start workers unthrottled; the throttle remains a BACKLOG ITEM.** Also fixed same commit: `.iv-card-overlay` CSS bug (unconditional `display:flex` overrode `[hidden]`, permanently bleaching every card on both the tab and the standalone page) → gated on `:not([hidden])`. **2 STANDING BACKLOG ITEMS:** (1) commit-side precheck "b" DEFERRED (maltytask `commitPlan`/ingest-one-local.ts — defense-in-depth; A+C already close the reachable path); (2) 🔴 TWO RECAP-SHAPE SOURCES — valider-queue.php kept its own BATCH query (vat_rate/gate_failures/aggregate cols) instead of `fetch_recap_for_invoice()` (recap-lines.php = the DESIGNATED canonical); ONE UI renderer preserved but NOT the canonical one → consolidate before a 3rd recap consumer; (3) bulk "Tout valider" fan-out throttle (above). → [ingestion-validation-gate-arc.md](maltyweb-pm-memory/ingestion-validation-gate-arc.md) §À-VALIDER → TRIAGE MERGE — SHIPPED+PROVEN.


### --- CURRENT BUILD-STATE migration history + Financier feedback ---
----LINE 92----
- **Migrations:** applied through **298** (live-verified VPS 2026-06-09). **298 = `298_journal_saisies.sql` — Journal-des-saisies arc: VIEW `v_saisie_events` (UNION over the 7 bd_*_v2 tables, 21,887 rows) + submitted_at indexes + ref_pages 'journal-saisies' sort=15; commit `5b918f3` (deployed-not-pushed). 297 + 296 = parallel BBT-vide arc (`296_bd_racking_v2_bbt_vide_scrapped_hl.sql` + `297_pertes_bbt_empty_threshold.sql`) — APPLIED 2026-06-09 as a SIDE-EFFECT of the journal session's deploy.sh/migrate.php run (deploy pushes the working tree; both additive; Kouros's uncommitted BBT-vide PHP/CSS/JS also leaked live in the same deploy). 295 = `295_submitted_by_user_fk.sql` — operator-attribution arc: nullable `submitted_by_user_id_fk INT UNSIGNED → users(id)` on ALL 7 bd_*_v2 tables (NOT 4 — brewing is 4 sub-tables: brewday/gravity/ingredients/timings); commit `c8e895c` (deployed-not-pushed). 294 = `294_ord_orders_source_ref.sql` (parallel: WeeklyOrders ingestion). 293 = `293_loss_model_no_liquid_subtraction.sql` — loss-model-consistency view rewrite, commit `7a8b63a`. 292 = floor view (SUPERSEDED by 293; file dead on disk). 291 = `④ Tap&Shop` direct-sales tracker MVP — ref_pages 'tap-shop' + access-preset rows; commit `a13a9d0`. 290 = `290_marketing_access_preset.sql` (parallel; expeditions is_active 0→1). 289 = `289_rri_scd2_open_uniqueness.sql` — recette SCD2 write-side; commit `a1c68dc`. **299 = `299_fulfilment_site_attribution.sql` ✅ APPLIED 2026-06-09 (ITEM 1: CREATE `inv_stock_movements` + ALTER `ord_orders.fulfilment_site_id_fk` + ALTER `ref_customers.default_delivery_site_id_fk`; commits `3c3eb33`/`49a0386`/`edfcf78` pushed). **300-305 APPLIED+verified live 2026-06-10:** 300=`ref_customers_sale_class` (single-source COGS taxonomy); 301=`inv_sales_ledger` (44,164-row canonical sales ledger 2021→2026); 302=`cuv_hl_per_unit_lock`; 303=`inv_side_stock_ledger` (loose-unit remainder + giveaway); 304=`financier_page` (ref_pages id=239 financier, manager/general/sort=125); 305=`kpi_fg_stock_data_ready` (FG-stock trackers data_ready=1 — swept into Financier commit `6a013ad` by the shared-index concurrency incident, see SHIPPED register). **306-310 APPLIED (parallel sessions, 2026-06-10):** 310=`310_kpi_daily_recap_trackers.sql` — NEW `viz_type='recap'` ENUM value (10th) + the 2 daily-recap composite trackers #270 wort / #271 packaging (NOT yet committed; see kpi-tracker-catalog.md §2026-06-10 RECAP). 306-309 = parallel-session arcs (re-verify owners at build-start). **312 = À-valider topbar dedup (`is_active=0` ref_pages id=99 invoice-validate; commit `1a81a3f`, pushed; 2026-06-10).** 311 = `kpi_utilities_batch6` (parallel mon-tableau session). **313 = `313_packaging_objective_hl.sql` ✅ APPLIED 2026-06-10 (mine — daily-recap Feature C): `bd_packaging_v2.objective_hl DECIMAL(8,3) NULL`, a planning annotation (NOT fiscal); one column per row (parallel runs = separate rows). Leakage audit PASSED — objective_hl appears ONLY in kpi-handlers.php (read), form-packaging.php (write), packaging-form.js, the mig; ZERO in compute_packaging_vendable_hl / v_bd_packaging_v2_vendable / beer_tax_base_hl / loss_kpi_hl. **⚠️ The `objective_hl` WIRING in form-packaging.php + packaging-form.js was ALREADY COMMITTED in the Réassigner-cuve commit `4af2155` (swept in incidentally — noted in that commit body); do NOT re-commit those two files' objective_hl hunks. mig 313 file itself + any mon-tableau-side objective_hl read may still be uncommitted — verify before committing.** **🏁 PHASE 2b KPI BUILD COMPLETE 2026-06-10 added migs 305,306,307,308,309,311,312,314,315,316 (mine) + 310/313/317 (parallel) → MIGRATION HEAD now ≥318. NEXT FREE ≥319** (re-verify `--status` + `ls db/migrations` at build-start — parallel sessions live; the head moved a lot today across 10 atomic batches). **318 = `ref_pages.sku-costs is_active=0` — sku-costs.php retirement (the standalone "Coût par SKU" page absorbed into the Financier tab); maltyweb `e72e64e`, pushed; 2026-06-10. NB the retiring session originally planned 313 but parallel sessions had already taken 313–317.** **317=`317_vendable_view_head_leaf_split.sql` ✅ APPLIED 2026-06-10 — Réassigner-cuve serving-tank reassignment (this was a DISTINCT arc, NOT the KPI parallel session; 316 was the parallel `316_kpi_logistics_equipment_batch10`, which is why this landed as 317): view vendable head→leaf split (vendable MOVES to leaf via `has_child` EXISTS; tax/loss STAY head via `reuses IS NULL`); NO-OP on current data (2270 rows / 0 diff, zero reuse rows pre-feature). **✅ COMMITTED+PUSHED maltyweb `4af2155`** (mig 317 + `form-packaging.php` / `packaging-form.js` / `packaging-form.css` / `app/sku-bom-compile.php` liner-amortization predicate fix). **REAL operator reassignment verified: Blues Rules 6740→Rincette → leaf 6767 (vendable 5/tax 0/reuses 6740/liner reallocated 105/new_liner_transport 0), head 6740 vendable→0/tax 5 stays, batch-211 cuv totals conserved 33.5+33.5 HL, view==stored.** `4af2155` ALSO swept in the uncommitted mig-313 `objective_hl` wiring in form-packaging.php/packaging-form.js (FLAG: daily-recap Feature-C session must NOT double-commit it). 1 Blues tank (6739) still un-reassigned. Full record + follow-ups (multi-tank 2-leg cap, undo stored/view divergence, liner inv_consumption deferral) → [loss-types-and-form-changes-arc.md §RÉASSIGNER CUVE](maltyweb-pm-memory/packaging-losses-convention/loss-types-and-form-changes-arc.md).** Phase-2b commits `6a013ad`/`09006ce`/`d264111`/`4f6a98c`/`598f788`/`a353ea2`/`e32d472`/`ea10fb7`/`7cbedf7`/`b3a1e02` = maltyweb main LOCAL (verify push). Full record → kpi-tracker-catalog.md §PHASE 2b BUILD COMPLETE. **⚠️ ACTIVE PARALLEL-EDIT HAZARD on `app/kpi-handlers.php` (2026-06-10): Phase-2b built ALL domains into this ONE file across 10 batches AND a concurrent recap session shares it → the qa_qc batch (`7cbedf7`) swept the recap session's PHP handlers (mig-310 recap + A/B/C tweaks) into git history. Recap HANDLER code is COMMITTED (matches deploy); recap frontend/form files (kpi-charts.js, mon-tableau.css, packaging-form.js, form-packaging.php) + migs 310/313 stay UNCOMMITTED-but-deployed/applied. `app/kpi-handlers.php` is now the single highest-traffic shared file — coordinate + single-owner before touching it. Prior: **284-282 = ingestion-validation-gate arc (284 = ref_pages id=99 invoice-validate + tour row [`6973316`]; 283 = doc_invoices.delivery_write_plan JSON [`d894b6f`]; 282 = doc_invoices.validated_by FK→users.id + energy_extract JSON [`fd1c5ed`]**; NB the pre-build "mig 282 carries 3 cols" plan was SPLIT 282/283 as built). 281 = ref_customers contact cols. 285-288 = expeditions (display_family 287 / inv_fg_stocktake row_hash gen-col 288). Prior (PM-verified vs live 2026-06-07 LATE NIGHT): **280 = users.tour_seen_at [visite guidée, `ca65c22`]** — the 278-collision REMEDIATED: renamed→280 + paired schema_migrations UPDATE + manual VPS rm of the stale 278 file; 279 = expeditions/orders [parallel session]; 278_kpi_seed_v2_batch = Mon-tableau-v2 seed; 277 = P3 recap; 276 = whirlpool; 275 = liquid window guard; 274 = FIX-7; 273 = slot_name; 272 = BOM step-② data; 271 ALT-b25; 266-270 access+KPI. 281 = ref_customers contact cols + name-UNIQUE→KEY + Transport Express seed (CRM bootstrap arc). (Stale interim markers in this para superseded by **NEXT FREE = 306** stated above — re-verify `--status` + `ls db/migrations` at build-start, parallel sessions live.) **LESSON (2026-06-07): rsync deploy does NOT delete removed files — a renamed migration leaves its OLD filename live on the VPS where it shows as "pending"; rm it manually as part of any rename remediation.** **INCIDENT-CONVENTION (2026-06-07): a migration SQL file must NEVER self-INSERT into `schema_migrations` — migrate.php does the bookkeeping; a self-INSERT collided with migrate.php's own INSERT → FAIL reported after a fully-applied migration.** **MON TABLEAU v2 ✅ LANDED — record → kpi-tracker-catalog.md §2026-06-07 EVENING.** `app/sku-bom-compile.php` staged WAC edits are NOW COMMITTED (① done) — file is clean to edit again. Full per-migration ledger (131→265, what each did, the renumber-drift history) → [derivation-tree-and-schema.md](maltyweb-pm-memory/derivation-tree-and-schema.md) §SQL BUILD SCHEMA + [build-history/](maltyweb-pm-memory/build-history/README.md). Migration collisions are caught + corrected at build-start every time via the `--status` pre-flight (PPB 194→200; mother-shell 204→215; the rule holds).

----LINE 104----
- **SINGLE-SOURCE COGS REPOINT + FINANCIER PAGE v1** (maltytask `489ab70` + maltyweb `6a013ad`, migs 300/301/304; SHIPPED+LIVE 2026-06-10) — (a) `loadSales()` reads ONLY `inv_sales_ledger` (44,164 rows 2021→2026, single source), retiring the bc+orders union; artifact now 66 months; ledger writer `ingest_bc_sales_ledger.py` committed; sign convention EMPIRICALLY VERIFIED (outbound NEG qty/hl; CHF revenue-signed plain-SUM). **SEAM: ledger-only FOR NOW — eshop+taproom still IN the sale_class filter, peel off to Shopify/Lightspeed feeds LATER (isolated behind the filter).** (b) `financier.php` read-only finance hub (ref_pages id=239, manager/general/sort=125) consuming `cogs-report-data.json` (COP) + `sales-cogs-data.json` (COGS) via lazy `api/financier-data.php` per-month slice — NEVER recomputes. **OPEN: /financier tour card NOT dispatched (dirty shared tree — tour-gap-check flags Financier + the FG-stock page CRITICAL; deferred until tree clean).** **🆕 2026-06-10 KOUROS FINANCIER FEEDBACK — PM RULINGS (pre-build; verified live):** (1) **✅ RESOLVED 2026-06-10 — render bug ROOT-CAUSED + FIXED (maltyweb `f1ff3a1`, pushed + live-confirmed).** "All 4 sections don't work" was NOT data, NOT a JS error: `public/css/financier.css` had `.fin-panel{display:flex}` which OVERRODE the HTML `[hidden]` attribute (a `.class{display}` rule beats `[hidden]`'s UA `display:none` on SPECIFICITY) AND `.fin-panel--active` was empty → all four panels (COP/COGS/Marge/SKU) rendered STACKED simultaneously, so clicking tabs did nothing visible. Content + data were ALWAYS correct (manager-auth Playwright smoke verified: COP 77,169 CHF, COP/HL 88.76, Marge 85.8%). FIX = `.fin-panel{display:none}` + `.fin-panel--active{display:flex}` (tab JS already toggles `--active`; HTML sets it on COP). **DURABLE LESSON (reinforces the visual-acceptance gate; added to the `ui` skill rendering-bug catalog as the `[hidden]`-overridden-by-`.class-display` trap, sibling of the `<dialog> display` trap): `php -l` + data-layer checks PASS a stacked/broken page — only a real manager-auth FULL-PAGE SCREENSHOT caught it. The manager-render screenshot is the LOAD-BEARING UI gate on a role-scoped page, not optional.** *(Original static-analysis dead-end recorded for the record: node --check, hoist, tab↔panel data-tab↔fin-panel-* contract, render-target IDs, php-fpm/nginx all 200/clean — all missed it because the fault was CSS specificity, not JS/PHP.)* (2) **Retire standalone SKU cost page** — Kouros assumed BSF-linked; WRONG (verified: `sku-costs.php` reads `ref_skus`/MySQL, only `google` strings = Google Fonts). RULING = **Option (b), NOT full-absorb (a) now**: KEEP `sku-cost-detail.php` (drill-down, 2 callers sku-costs.php L370 + bom-review.php, no ref_pages row — NOT the scrap target); the Financier "Coût par SKU" tab (`fin-panel-sku`, financier.php L432-450) currently LINKS OUT to `sku-costs.php` → repoint/inline the per-SKU LIST into the panel (reuse the SAME `ref_sku_bom` read — NO re-derivation = divergence flag), THEN `is_active=0` ref_pages id=12 sku-costs + log the .php to scrapping-backlog (gate: panel covers list + 0 inbound routes). Full-module absorb = optional later increment. **✅ #2 SHIPPED 2026-06-10 (maltyweb `e72e64e`, pushed) — went FURTHER than the inline-list plan: the Financier "Coût par SKU" tab (`fin-panel-sku`) now renders the FULL per-SKU cost table SERVER-SIDE reusing sku-costs.php's EXACT `ref_sku_bom` query VERBATIM (no re-derivation) — 3 KPIs (active SKUs / recipes / médian CHF·HL) + per-recipe-grouped rows (brewing/packaging/total CHF + CHF·HL) + anomaly flags (keg-inverted/outlier/extreme-bom-line) + BOM freshness stamp + SKU→sku-cost-detail.php links + client-side recipe/format/classification filters (no reload); query try/catch-wrapped (DB hiccup can't fatal the COP/COGS modules). Retirement = **mig 318** (NOT the earlier-noted 313 — parallel sessions took 313–317; head now ≥318) sets `ref_pages.sku-costs is_active=0` AND `public/modules/sku-costs.php` DELETED (operator-authorized, git-recoverable — went past the planned is_active=0-only); `sku-cost-detail.php` KEPT (back-links repointed to /modules/financier.php); 0 dangling hrefs. Live-verified Opus-independent (mig 318 applied, is_active=0, 154 active SKUs carry BOM cost, php -l clean, HTTP 302). → scrapping-backlog **#32 RETIRED**. ⚠️ **Visual-acceptance OPERATOR-PENDING** (build session's local→site browser flaky; manager-render screenshot is the load-bearing UI gate per #1's lesson, not yet eyeballed — same gap pattern).** (3) **✅ À-valider topbar dedup SHIPPED 2026-06-10 (mig 312, commit `1a81a3f`, pushed) — RULING EXECUTED.** `is_active=0` on ref_pages **id=99** invoice-validate (txn + log_revision). NO orphan + NO new flag needed: topbar filters is_active=1 (topbar.php L18) but `_page_registry()`/`user_can_access()` query ref_pages with NO is_active filter (auth.php L370) → is_active=0 IS the hidden-but-accessible pattern (page still reachable by href + access-gated). Queue already lives at `triage.php?tab=valider` (id=4 topbar; shared partial valider-queue.php; invoice-validate.php is a thin wrapper since the 2026-06-08 merge `484cc6a`) — so hiding id=99 leaves exactly Triage carrying À-valider. NB ref_pages has NO `nav_hidden` col; min_role bump is the WRONG lever (restricts reach). **SEQUENCING: #3 + #1 parallel (independent); #2 GATED behind #1 going green (don't absorb into an undiagnosed broken panel). EQUIP: #3 sql+coder+webapp-testing; #1 ui+coder+sql+webapp-testing (manager smoke load-bearing); #2 ui+sql+coder+webapp-testing.** ⚠️ CONCURRENCY INCIDENT: `6a013ad` mixed-boundary — a parallel session's FG-stock KPI suite + mig 305 swept in via the SHARED git index; nothing broken, leave as-is; LESSON = separate clones/worktrees or serialize commits. → [sales-commercial-surface.md](maltyweb-pm-memory/sales-commercial-surface.md) §SINGLE-SOURCE REPOINT + §FINANCIER + §CONCURRENCY INCIDENT. (4) **✅ BOARD COGS/COP P&L GRID — ALL 4 STAGES DONE, ARC CLOSED (SHIPPED+LIVE+PUSHED; maltyweb `d0c7e60` ACTUALS → `b477565` BUDGET, both pushed; remote == deployed).** Built into Financier id=239 per the §BOARD ruling — host EXTENDED, NO new Node script/JSON. `financier-data.php` new modules `cop-grid`+`cogs-grid`: ACTUALS = `inv_charges_bc` net SUM(debit−credit) grouped by `gl_account_no` (is_summary=0), GL→line map mirrors the sheet exactly; `fin_gl_booked_months()`/`fin_ytd_months()` intersect window with booked months so cost numerator + HL denominator cover the SAME months (honest under-read). Denominators: COGS÷HL sold (`inv_sales_bc.hl_resolved`), COP÷HL brewed (FIN_COP `hlBrewed`). Grid card at top of fin-panel-cop/cogs, `renderCopGrid`/`renderCogsGrid` tree tables, chip "mois complets uniquement". **VERIFIED 2026-03 vs independent SQL: Brewing 49.57 / Packaging 49.50 (4206 Labels −5.34 credit-net working) / Indirect 4.69 / Utilities 1.52 (waste only) / R&D 0.21 / TOTAL COGS VARIABLE 105.48 CHF/HL; manager full-page screenshot rendered.** PLACEHOLDERS render "—" (refuse-don't-guess held): BUDGET, N-1/2025 cost, variance%, semi-var labor split, COGS grand total, COP per-type (Keg/Bott/Can/Bulk), Scotch; zero-booked GL = "0.00" (≠ "—"). **COVERAGE GATE: `inv_charges_bc` has only 2026-03 full (411 rows) + 04/05 THIN (20/22) → grid DEFAULTS to 2026-03; thin months can't be auto-distinguished from complete (chip caveat only) — bookkeeper must upload full monthly GL via Le Cockpit.** Commit-hygiene: first commit swept 3 parallel-session files via shared index → reset --soft + recommitted clean 4-file `d0c7e60`, restored the 3 to staged (same shared-index race lesson). **✅ STAGE 4 (BUDGET) SHIPPED+LIVE+PUSHED 2026-06-11 (maltyweb `b477565`, pushed; mig `327`).** `ref_budget_cogs` = 21 GL-line rows, fiscal_year 2026, `budget_chf_per_hl`, schema_meta classified; SEEDED from the operator's board-sheet COGS&YIELDS BUDGET column (total **75.76 CHF/HL**) — seed-from-explicit-statement, every number traces to a cell (not guessing). cogs-grid module loads budget per line → **BUDGET (month+ytd, FLAT ANNUAL RATE) + Vs BUDGET = (actual−budget)/budget %** columns now LIVE on the COGS grid (April proof: Malts −37.3%, Hops −20.6%, **TOTAL COGS VARIABLE −8.0%**). **COP grid UNCHANGED — no budget on its sheet.** **v1 APPROX (documented): Transport + QA/QC budgets are monthly-profiled in the source sheet but seeded as the annual rate.** Manager-auth screenshot verified. ⚠️ **GIT HYGIENE: `b477565` swept a parallel expeditions session's staged DELETION of `public/css/expeditions-sku-history.css` and was pushed before catch — deletion SAFE (file unreferenced; expeditions shipped in `e7962ce`; removal intended) so left, not history-rewritten. 2nd bare-commit sweep in one session → pathspec-commit is now a HARD RULE (CHECKLIST).** **ARC BACKLOG (non-blocking, NOT gating): N-1 cost (2025 GL CSV ingest → N-1/Vs-N-1 cols still "—"); COGS-YTD-starts-March coverage chip; May utilities booking; the dead `fin_ytd_months(rolling6m)` path the COP stage flagged.** **ARC STATUS: COP/COGS board grids fully re-sourced to operational SoT, per-line, budget-comparable, Vue-comptable+CHECK reconciliation — all 4 stages done.** Full as-built → [sales-commercial-surface.md](maltyweb-pm-memory/sales-commercial-surface.md) §BOARD COGS/COP P&L GRID §STAGE 4 BUDGET.

