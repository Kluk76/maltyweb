# Build-changelog appendix — former RESUME POINT verbatim (former index lines 16-91)

> Part of the build-history changelog. Parent index: [README.md](README.md). This block was lifted VERBATIM out of the LEAN INDEX during the 2026-05-27 index-lean refactor — the former RESUME POINT detailed narration + landed-arc blocks. Archival; superseded by the structured period changelogs + the live RESUME POINT in the parent index.

## ⏭ RESUME POINT (2026-05-27m; Phase-D content unchanged since 27i) — **PHASE D STEP 1 LANDED.** Migration **178** `178_v_mi_cost_resolver.sql` COMMITTED (`04b4d50`) + applied live + verified: creates `ref_fx` (CHF=1.0/EUR=0.945, replaces hardcoded 0.945, operator-editable) + `v_mi_cost` (thin per-MI cost resolver: wac > catalog[price IS NOT NULL incl 0] > no_basis[price IS NULL → NULL, never 0]; live 367 MIs = wac 96/catalog 141/no_basis 130) + `v_mi_cost_gap` (diagnostic: no_basis MIs actually in an active BOM OR RM stock = **32 rows**, cost-neutral swap since none have a BSF price either). Resolver bug caught+fixed pre-commit: `COALESCE(price,0)>0` → `price IS NOT NULL` (so price=0 free-samples = catalog cost 0, not no_basis). **OPERATOR DECISION = SOFT ROLLOUT** (swap to MySQL cost-neutral now; no_basis-in-BOM values at 0 + tracked via gap view; clear gaps at operator pace THEN flip the build guard to strict-fail). The only COGS that MOVES = the 96 WAC MIs (vs old BSF MI.price) — gated behind before/after diff + operator sign-off. Full arc → **phase-d-cost-source-swap.md**.
`migrate.php` = **through 180 applied, 0 pending** (162-174 + 175/176/177 CIP + 178 Phase-D + 179 yt_number + 180 KZE-PU). **Latest migration = 180 (KZE PU, committed `b40e73f`); Phase-D latest = 178. Next free number = re-check `migrate.php --status`** (the VPS sequence can lead local-git; was the source of the earlier 166/167/168 collision). v_mi_wac unchanged (96 rows, 0 unresolved); catalog hygiene DONE for delivery-backed MIs (172/173 `6098585`); GOVERNANCE loose-end CLOSED (yeast migs `ed8e7c6`).
**✅ PHASE-D STEP-3 GATE SIGNED OFF + SCOPE LOCKED (2026-05-27, conversion-correct harness).** Operator's 3 decisions: **(1) apply April-2026 ONWARD** (forward-only, April incl, no pre-April restatement); **(2) warehouse.php conversion bug → BACKLOG** (don't touch this session); **(3) DIRECTIVE "do not touch BSF — completely out of BSF"** (no BSF writes AND cost/inventory valuation must SOURCE from MySQL). Harness: today RM (BSF price, no conv) = CHF 334,319; correct (v_mi_cost WAC × conversion_factor) = CHF 287,902 → ~46.4k overstatement from the g→kg conversion bug; genuine cost-swap impact once conversion-correct = +CHF 99 RM / <CHF 2.50/SKU = cost-NEUTRAL. **PM RE-VERIFIED read-only: with_conv reproduces 288,101 (≈287,902); valuing at v_mi_cost WAC WITHOUT conversion = 898,306 — proves the 334k→288k swing bundles TWO effects (conversion fix + cost swap) → MUST diff separately.** **SCOPE = TWO commits: Commit A = RM valuation FULLY onto MySQL (inv_rm_stocktake qty + v_mi_cost cost + ref_mi.conversion_factor + ref_mi metadata via FK-resolved category; ZERO BSF in RM path; HARD CUTOVER no env flag; 334,319→~288,101 April); Commit B = COGS/BOM cost swap via same bridge (cost-neutral, FX-careful currency='CHF' lever). A THEN B, each its OWN before/after diff + sign-off.** **CLAIM-5 RESOLVED: CASSONADE/SUGAR (cf=1.0 kg/kg) + STICKER_DIB/SPY_DK (cf=1.0) are ALREADY CORRECT in MySQL ref_mi — the 0.001/NULL anomalies are BSF-ONLY → auto-resolved by the BSF exit, NO MySQL hygiene migration needed.** NOT yet dispatched. **Rulings + BSF-exit roadmap → phase-d-cost-source-swap.md §STEP-3 GATE + §GATE RULINGS.** **(6) Phase D — Step 1 ✅ DONE; Step 2b ✅ DONE+COMMITTED (maltytask `a3bc9ed` = `lib/repos/mysql-wac.ts loadMiCost()` over v_mi_cost, 367 rows/0 errors, resolveCostChf() w/ no_basis for refuse-don't-zero, NO consumer yet); Step 2a DEFERRED. NEXT = dispatch Commit A.** ⚠️ **CORRECTION (file-verified 2026-05-27, supersedes any "warehouse reads v_mi_wac" claim): `warehouse.php`/`warehouse-export.php` read PERIOD-FROZEN `wac_snapshots` (`LEFT JOIN wac_snapshots w … AND w.period=(SELECT MAX(period)…)`, `COALESCE(w.wac_chf, m.price*IF(currency=EUR,0.945,1))`), NOT live `v_mi_wac`. TWO WAC surfaces exist BY DESIGN: `v_mi_wac` (LIVE → COGS via v_mi_cost) vs `wac_snapshots` (FROZEN month-end → warehouse display); they differ (7 MIs mismatched live, e.g. HOPS_ELDORADO snap 19.75 vs live 13.85). So Step 2a (warehouse→v_mi_cost) would SILENTLY flip snapshot-WAC→live-WAC = a behavior change, NOT a numbers-identical refactor → DEFERRED to backlog (operator product decision; warehouse works fine on snapshots; period-anchored inventory value vs real-time cost is a COHERENT split — do NOT "fix" warehouse to live without an operator decision). NOT a Phase-D blocker. → **Step 3** = the FX-careful cost swap (port `lib/mi.js loadMI`→MySQL carrying v_mi_cost cost [the bridge, JS consumers transparent] + rewrite `build-sku-bom.js`→TS via typed repos) + before/after COGS diff + sign-off. **STEP-3 BASIS CONFIRMED:** today's COGS uses LIVE WAC (compute-weighted-prices → WAC from Active deliveries → writes BSF MI.price → build-sku-bom reads it), so `v_mi_cost` (built on live `v_mi_wac`) IS the correct swap source — matches today's basis; swap stays as designed. ⚠️ **STEP-3 FX LANDMINE:** the two consumers treat currency INCONSISTENTLY — `lib/inventory.js` L96 expects NATIVE + self-converts (`EUR ? price*EUR_CHF`); `build-sku-bom.js` L273-400 uses price RAW (NO FX). So v_mi_cost.cost_chf (already CHF) CANNOT drop into MI.price blindly (double-converts in inventory + surfaces latent EUR-unconverted lines in build-sku-bom); the loader must emit cost_chf with currency normalized so inventory.js doesn't re-convert, AND build-sku-bom's raw use reconciled. THIS WILL MOVE COGS → mandatory diff + sign-off. → **Step 4** = retire BSF writeback (`compute-weighted-prices --write-master`). `lib/inventory.js` + ~6 callers STAY JS (D.2). **COALESCE catalog gate CLEAN for ALL delivery-backed MIs**; only remaining catalog consideration = NO-DELIVERY inventoried MI set (see below). → **(6b) consumption→WAC/RM wiring** (RM value + consumption valued at WAC; on-hand = deliveries − consumption; DEPENDS ON unit-normalization clean — now CLEAN through 164/165/auditor-sweep — + is_inventoried reconciled; the 2 wac_unresolved MIs now resolved) → **(7) Recette fold-in-vs-standalone decision**. (Racking/CIP track NOW LANDED + committed `8c53e98`/`36b72c5`/`b269eee`/`592641d` — the prior "stay clear of racking agent files" collision risk is CLEARED; `salle-de-controle.php` + `app/cip-events.php` + `app/partials/cip-section.php` + migs 175/176/177/179 are on `main`.) **PARALLEL operator-priority track:** operator visual review of recolour + hub + the NEW racking KPI section + all 3 forms (logged-in browser walkthrough still pending), then flip Brassage + Fermentation saisies cards LIVE (gated on Fermentation RULE-2 GO + commit). **Racking KPI section + KZE PU LANDED 2026-05-27m (`c38cea1`/`b40e73f`, mig 180) — see racking-kpi-dashboard.md.** Plus standing queue: liquid dry-hop ml/l capability; dual-nav reconciliation + index.php fate (scrapping-backlog #12/#13); **DATA-QUALITY backlog — Diversion `bd_racking_v2` CO₂/O₂ outliers (avg CO₂ 113.97 g/L, O₂ 13,076 ppb; surfaced by the racking KPI table) + the 3 NULL-dest Diversion rows 60/66/278 — RQ candidate, refuse-don't-correct without operator review** (consistent w/ the known Diversion gravity-scale anomaly); strain classification of the other ~76 recipes (keeps transfer gate Zepp-only until done — operator data entry, not a code task).
**PHASE-D CATALOG-FALLBACK RISK = NO-DELIVERY inventoried MIs (the last catalog-gate item):** catalog (`ref_mi.price`) is ONLY the fallback for MIs with NO Active deliveries — all delivery-backed MIs are WAC-valued, so the catalog-vs-WAC scan is now CLEAN within [0.67,1.5]×. The genuine risk is the no-delivery set: POMONA fk238 (0.233, no basis) PLUS many no-delivery inventoried MIs carrying NULL/blank price + NULL currency (PM-verified: id=2 MALT_PALE_ALE, id=5 MALT_VIENNA, numerous HOPS). Can't be auto-derived (refuse-don't-guess) → at Phase-D wiring time these need manual/historical values OR an explicit accept-WAC-only-with-sane-default decision. → billing-format-and-wac-unit-model.md §CATALOG-HYGIENE ROUND 2.
**✅ GOVERNANCE LOOSE-END — RESOLVED 2026-05-27:** yeast migrations 166/167/168 (Phase-1 of Saisie-Transferts + yeast-family) are now committed `ed8e7c6` ("chore(db): recover applied-but-uncommitted yeast-family migrations") — a fresh VPS-from-git no longer drifts. → billing-format-and-wac-unit-model.md §GOVERNANCE (resolved) + the yeast-family thematic file.
**FUTURE fixed-ECO ingest path (log-don't-build):** when Carbagas starts billing the eco-fee as a flat ~50 CHF/delivery, the carbagas parser must map it to fk591 (now is_inventoried=0) and store it as a fixed-fee line (qty/price shape TBD). Revisit when the first fixed-fee invoice arrives. → §ECO-FEE CONVENTION.
**PARKED (band-check method note, still valid):** plausibility-band sweeps must EXCLUDE `pricing_unit='month'` MIs (TEA PKG_TEA_BOT_CH is not a per-unit cost). Full §MIGRATION 165 / §WAC-HYGIENE BATCH AS-BUILT → billing-format-and-wac-unit-model.md.

**✅ SAISIE TRANSFERTS ROUND 2 + 3-FORM CIP CONVERGENCE — FULLY LANDED + DEPLOYED + PM-VERIFIED LIVE 2026-05-27j.** Four commits on `main`: `8c53e98` (CIP schema migs 175/176/177 + shared `app/cip-events.php` writer + `app/partials/cip-section.php` + `public/css/cip-section.css`), `36b72c5` (Salle-de-contrôle editable CIP-types list-CRUD over `ref_cip_types`), `b269eee` (racking round-2 = all 10 operator points + CIP consume + mig **179** `bd_racking_v2.yt_number`), `592641d` (converge brewing+packaging onto `bd_cip_events`). **`bd_cip_events` is the SINGLE CIP store for ALL 3 forms** — racking (per-machine centri/kze/pump + centri+KZE inline + dest-vessel CIP w/ dynamic bbt/cct/yt label + A3 start/end TIMEs + type from ref_cip_types; **A2 dest-CIP optional when residual>0 blend**), brewing (CCT+YT vessel), packaging (centri/kze/pump+inline + tank vessel; live-path writes events, draft-path audit-logs only). **All 3 forms STOPPED writing flat CIP cols + removed them from the `bd_*_v2` hash basis (no drift); flat cols KEPT for legacy Sheets-ingest only (scrapping #14 stands).** Writer is atomic (txn/savepoint, nested-safe), idempotent re-submit; **4 review-found bugs (hidden-required deadlock ×2, non-atomic tombstone, missing notes) fixed before forms consumed it.** Racking round-2 also: client DROPPED→FK-resolved `ref_recipes.client_id`→`ref_clients` (Nébuleuse when NULL); YT N° dropdown(ref_yt)+yt_number persisted; "Volume résiduel en cuve" relabel + derived resultant (DISPLAY-only, not stored — simulator derives its own); CO₂/O₂+CIP labels swap by dest type; vitesse moyenne input removed (avg_speed col kept); BBT-occupied lots added to hors-process (sim-filtered, +8); back-dating works (no min/max on event_date). **LIVE-VERIFIED:** ref_cip_types = Soude/Acide/Full CIP/Full CIP+rinser; bd_cip_events backfill racking 679 + brewing 412 + packaging 0; all times NULL (backfill-only, no live event yet); migrate.php 175/176/177/179 ✓. **🔑 REUSABLE ENTRY POINT for the next CIP-on-a-form build = `app/partials/cip-section.php` ($cipConfig) + `cip_upsert($pdo,$sourceForm,$parentId,$events)` in `app/cip-events.php` (call AFTER bd_upsert) + add the form's source_form ENUM value + typed FK; do NOT re-model.** **BACKLOG: (1) 3 racking rows ids 60/66/278 (Diversion) `cip_bbt_done=Yes` but NULL dest_type — excluded from backfill (refuse-don't-guess), data-quality/RQ candidate; (2) browser walkthrough PENDING all 3 forms (server-verified only); (3) strain classify — Zepp-only gate until operators classify ~76 recipes; (4) CIP-first convention extends to any future form; (5) machine list fixed ENUM, ref_cip_types editable.** Full as-built → cip-module.md (marked AS-BUILT/LIVE) + saisie-transferts-and-yeast-family.md. | Prior (CIP/Round-2 DESIGN, superseded by the as-built above): `bd_cip_events` polymorphic-parent (source_form ENUM + 3 typed FKs RESTRICT + XOR CHECK; target_kind/target_code/target_number + kind↔code CHECK; inline_group TINYINT group-id; A3 start/end TIME); machine list fixed ENUM; ZERO downstream CIP consumers (only 3 forms + Sheets ingest); precedent `bd_packaging_readings`. | Prior (Round 1 LANDED): **FULLY LANDED + DEPLOYED + PM-VERIFIED LIVE 2026-05-27 (`2d131de` + `fa6221c`, both on `main`; Phase-1 migs in `ed8e7c6`).** ALL 3 phases done. Phase 2b (per-recipe yeast editor) + Phase 3 (form-racking + occupancy ruling) both shipped. **Occupancy ruling APPLIED EXACTLY:** TankSimulator is the authority on BOTH candidate paths (gated: SQL brew+coldcrash+no-later-brew + garde HAVING + NOT EXISTS(bd_racking_v2 …) → sim-filter; hors-process: SQL no-HAVING/no-EXISTS → sim-filter). bd_racking_v2 = DESTINATION-only. **Live-proven: gated = 3 un-racked Zepp lots (CCTs 12/14/16, garde=14); sim-filter drops already-racked (hors-process 18→15).** **Prod data state (intentional): recipe 57 Zepp = sole classified+linked beer → sole time-gated; ~76 others unclassified → hors-process route until operator classifies via Recettes→"Levure & garde".** Backlog (gated, refuse-don't-guess): (1) classify+link the other ~76 recipes = operator data entry (optional bulk seed); (2) CCT→CCT blend occupancy = model extension, out of scope, comment in form-racking.php; (3) SCD2 v2 coupling = yeast_strain_id_fk + 3 overrides must join the version-stamped set. NOT browser-walked (server-side verified). Full → saisie-transferts-and-yeast-family.md §AS-BUILT (Phase 3) + §OCCUPANCY-MODEL RULING (now APPLIED). **(superseded scoping kept below.)**
**Phase 1 = migrations 166/167/168 APPLIED on VPS (NOT renumbered — `migrate.php --status` shows yeast=166/167/168, WAC-hygiene=169/170/171, PLUS 172/173 further catalog hygiene; CORRECTS the 27d index claim that the yeast series was renumbered to 169/170/171).** PM-verified read-only: 166 ref_yeast_strains.family ENUM(ale,lager,non_alcool,spontane,mixte) NULL (all 30 family=NULL intentional, 29 active+1 inactive); 167 ref_yeast_family_defaults seeded (ale=7/18-22, lager=14/9-13, non_alcool=7/18-22 prod=1; spontane+mixte garde=NULL prod=0; schema_meta row class=reference); 168 ref_recipes + yeast_strain_id_fk(int unsigned FK→strains.id RESTRICT) + garde_days_min_override(int unsigned)+ferm_temp_min/max_override(DECIMAL(4,1)), NO garde-max, 0 recipes linked yet (77 active recipes, 29 active strains). **Phase 2a = `salle-de-controle.php` #sec-biochem reads/edits ref_yeast_family_defaults (5 cards incl Non-Alcool, garde-max dropped, real temps); new admin-gated `update_yeast_family` POST L440-516 (two-step validate, CSRF, PRG, log_revision→audit_row_revisions); dead onBiochemBlur removed; YEAST_DEFAULTS JS const survives L1542 — still feeds the non-persisting recipe-CREATION mock (expected, out of scope).** **Phase 2b IN FLIGHT = "Levure & garde" Recettes panel: strain pick → inline strain→family classify (MUST write ref_yeast_strains.family, the canonical owner) → family defaults → per-recipe overrides → persist ref_recipes.yeast_strain_id_fk + 3 overrides; recipe CREATION stays mock. Shared resolver `app/yeast-eligibility.php` = COALESCE(recipe override → family default → UNRESOLVED); NULL garde = NOT time-gate-eligible, surfaces only under hors-process (no invented floor — SUPERSEDES the design's old fallback-floor-7).** **PM DECISION-CHECK: all 3 in-flight decisions SOUND/APPROVED/CONFIRMED — sole caveat: inline-classify must UPDATE the strain row, never copy family onto the recipe (broken-FK-derivation anti-pattern); no separate strains page needed now.** **⚠️ GOVERNANCE: confirm yeast migs 166/167/168 are COMMITTED to local git (27d index flagged them untracked on the VPS) — else fresh-VPS-from-git drift; VERIFY HEAD includes the 3 files.** **🆕 PHASE 3 (form-racking) IS BUILT (rename + eligible-beer cards consuming app/yeast-eligibility.php + hors-process toggle + mig 174 hors_process_flag/reason on bd_racking_v2 — all applied/verified) BUT has an OPEN OCCUPANCY-MODEL FIX before commit. PM RULING (2026-05-27, verified live): the gate's pre-rack CCT-occupancy is OWNED BY TankSimulator (`app/tank-simulator.php`), NOT pure SQL — same architecture as form-packaging (coarse SQL pre-filter → sim authority). The gated SQL had a real gap (freed a CCT only by next brew, never by rack-out → already-racked lots wrongly eligible in the post-rack/pre-next-brew window). FIX: keep coarse SQL, add `NOT EXISTS(bd_racking_v2 rk WHERE rk.neb_recipe_id_fk=bfw.recipe_id_fk AND rk.neb_batch=bfw.batch AND rk.is_tombstoned=0)` to the GATED query ONLY, THEN filter BOTH candidate lists through `(new TankSimulator($pdo))->run(today)` (drop null/mismatched-(beer,batch) CCT) reusing form-packaging's L495-524 helper. Hors-process: drop garde HAVING + OMIT the EXISTS, but STILL go through the sim (relaxes the TIME gate, not physical reality). CCT→CCT blend re-racks = hors-process ONLY, NO current-CCT-after-blend tracking this build (refuse-don't-guess; backlog gated on real workflow). bd_racking_v2 = DESTINATION-only, never an occupancy source. Dispatch Sonnet coder+sql, RULE-2 before commit. Full Q1-Q4 ruling + dispatch brief → saisie-transferts-and-yeast-family.md §OCCUPANCY-MODEL RULING.** Full as-built + decision verdicts → saisie-transferts-and-yeast-family.md §AS-BUILT + §DECISION-CHECK.
**(superseded scoping block, kept for the resolved-Qs + design-decision record:)** Operator's 4 open Qs RESOLVED. Extends TWO families: Salle de contrôle/Recettes+Biochimie (upstream — gains strain link, family-defaults table, per-recipe spec overrides) + the `form-racking.php` Transferts gate (downstream consumer). **Canonical names resolved:** NEW `ref_yeast_family_defaults` (family ENUM ale/lager/non_alcool/spontane/mixte PK, garde_days_min, ferm_temp_min/max, `is_produced`[spontane/mixte=0], is_active; seed ale/non_alcool=7, lager=14) is the family-defaults home (NOT commissioning_settings — family-specific; NOT the mock's `ref_biochem_defaults`); `ref_yeast_strains` ADD `family` ENUM (DON'T touch the orthogonal `type` enum — all 30 rows currently `type=unknown`, biology≠process-bucket, no guess-migrate); `ref_recipes` ADD `yeast_strain_id_fk` (INT UNSIGNED → ref_yeast_strains.id, RESTRICT) + `garde_days_min_override`/`ferm_temp_min_override`/`ferm_temp_max_override` (NULL=inherit; **NO family string col — derive by FK**; **NO garde-max**). **Override resolver = COALESCE(recipe override, family default, fallback floor).** Recettes/Biochimie UI lives in `salle-de-controle.php` (`?sec=recettes`/`?sec=biochem`) = PURE JS MOCKUP today (persists nowhere) that already shows the target model; new-recipe modal must select a STRAIN (mock selects family directly), drop the garde-max input, add a non_alcool card. **SCD2 flag:** override is a versioned recipe-spec fact but SCD2 v1 dormant → don't version-stamp now; MUST add to the version-stamped set when SCD2 v2 activates. **Sequencing:** Phase 1 (3 migrations+seed) gates all → Phase 2 (Recettes/Biochimie wiring) + Phase 3 (form rename label-only + cold-crash/CCT gate + hors-process toggle) PARALLELIZE after Phase 1 (form reads tables not UI; override NULL→family default; seed produced-beer strain links from canonical for day-one-correct gating). Dispatch 2 Sonnet briefs (A=migrations first; B=Phases 2+3, share the COALESCE resolver helper). `form-packaging.php` is the complete state-gating + hors-process precedent; the bd_fermenting_v2-ColdCrash→bd_brewing_brewday_v2.cct DERIVATION-GAP JOIN stands. Full design + verified schema facts + as-designed decisions → saisie-transferts-and-yeast-family.md.
**COMMITTED bundles** (clean tree): step-③ composite fold (`92fd102`, mig 156), density recipe-loader leg (`12d6ce1` migs 157-159 + `f334e55` mig 160), Brassage form (`564d699`), obs-path ml→mass fix (`a4b7efb`, mig 161 `v_bip_canonical`), site-wide light-kraft recolour (`3c57e5b`), Le Zeppelin hub family-router (`e65d0dd`) + saisies arrow contrast fix (`897d008`), v_mi_wac WAC view + unit-normalized deliveries (`d8c004d`, migs 162+163) + audit-report docs follow-up commit, **migration 164 PEACH_TEA+MOSAIC unit fixes (`5de9fd6`, applied live)**, **migration 165 yeast full-qty re-model + auditor report (`1d670d3`, applied+verified live)**, **WAC-hygiene batch migs 169/170/171 (A1 ECO non-inventoried + A2 view month-exclude + A3 catalog hygiene) (`930e09a`, applied+verified live)**, **yeast migs 166/167/168 recovered into git (`ed8e7c6`, governance loose-end closed)**, **catalog-hygiene round 2 migs 172/173 (BE134+SIMCOE / LOG_SCOTCH last-purchase) (`6098585`, applied+verified live)**, **Saisie Transferts Phase 2 (`2d131de`: Biochimie defaults + per-recipe yeast editor + `app/yeast-eligibility.php`)**, **Saisie Transferts Phase 3 (`fa6221c`: Transferts rename + state-gated cards + TankSimulator occupancy + mig 174; deployed+verified live)**.

**✅ v_mi_wac WAC VIEW — DONE + COMMITTED + PUSHED + VERIFIED LIVE (`d8c004d`, origin/main; migrations 162 data-fixes + 163 view; gate passed read-only).** The billing-format/WAC unit-truth build (was "READY TO DISPATCH" — now LANDED).
- **Migration 162 (data fixes):** unit-normalized the 8 touched delivery rows (MUNICH/US05/W3470 mis-scale + GALAXY/SIMCOE/WATER_DEMIN qty×price corrections) — ALL 8 now satisfy invariant `qty×price=total_original` (PM-verified). 5 yeast MIs (US05/W3470/FARMHOUSE/VERDANT/POMONA) → `pricing_unit=kg` (PM-verified all 5).
- **Migration 163 (`v_mi_wac` view):** WAC grouped by `ingredient_fk` (so multi-supplier MIs aggregate across suppliers); 98 rows, 2 flagged `wac_unresolved` = PKG_TEA_BOT_CH (pu=month) + PROC_ALIGAL2_ECO (pu=NULL) — **exactly as the audit predicted.** Template was mig 161 `v_bip_canonical` (CREATE OR REPLACE VIEW + refuse-don't-NULL flag + schema_meta row, `derived`).
- **WAC values reconcile live:** MUNICH 0.481005/kg, US05 206.010, W3470 339.066, GALAXY 33.9255, WATER_DEMIN 0.6246/L, SIMCOE 24.997304 (sc=2), MOSAIC 11.516701 (sc=2 multi-supplier), FARMHOUSE 209.412, VERDANT 186.921. 2026-04 `wac_snapshots`: **6 rows recomputed** (the snapshot-recompute set GREW 4→6 to include FARMHOUSE+VERDANT per-g snapshots), `replay_source=current_approximation`.
- **STILL-PENDING (carried forward — do NOT lose):**
  - **MUNICH `inv_deliveries.id=21` `qty_remaining=26` vs `qty_delivered=26000`** — operator-gated inventory-VALUE question. The RATE is correct (WAC reconciles); only the stock VALUE is affected. NOT yet resolved — flag, don't touch.
  - **`ref_mi_invoicing_units` completion** for the ~84 inventoried MIs still lacking a row (this build did seed-TOUCHED-only).
  - **Catalog-price (`ref_mi.price`) hygiene — ✅ DONE for all delivery-backed MIs** (migs 171 + 172/173). The 4-yeast bucket-D residuals + ALIGAL2_LIQ + SAFALE_BE134 + SIMCOE + LOG_SCOTCH all corrected to last-purchase native currency; catalog-vs-WAC scan CLEAN within [0.67,1.5]×. **The Phase-D COALESCE(WAC,catalog) catalog GATE is CLEARED.** Only remaining catalog consideration = the NO-DELIVERY inventoried MI set (POMONA + many NULL-price MALT/HOPS) — can't be auto-derived; manual/historical or accept-WAC-only-with-default at Phase-D wiring time (see RESUME POINT).
  - **Phase D** = wire COGS/COP + `ref_mi.price` writes to `v_mi_wac`. **MIND the `is_inventoried` scope divergence** vs the snapshot writer (the view and the snapshot writer scope MIs differently — reconcile before consumers read).
  - **CONSUMPTION-WIRING into the WAC/RM module (operator-flagged 2026-05-27, NEW pending):** "consumption will have to be wired to the module in order to be fully WAC/RM wired." = RM stock VALUE and consumption VALUATION must draw from `v_mi_wac` — consumption events valued at WAC; RM on-hand = deliveries − consumption, valued at WAC. **DEPENDENCY: downstream of unit-normalization being clean** (else consumption inherits the unit errors) AND of the `is_inventoried` scope divergence being reconciled (consumption reads the view). Sits AFTER catalog hygiene + alongside/just-after Phase D in the forward sequence. → billing-format-and-wac-unit-model.md.
  - Plus the standing queue: liquid dry-hop ml/l capability; operator visual review of recolour+hub then flip the Brassage+Fermentation saisies cards live; dual-nav reconciliation (scrapping-backlog #12).
  - ✅ **AUDITOR MANDATE DONE (2026-05-27, read-only sweep)** — report `reports/wac-silent-unit-audit-2026-05-27.md` (committed in `1d670d3`). PM triage: 0 silent-unit bugs across 185 Active inventoried rows EXCEPT the US05/W3470 2× the auditor MISSED (it tested `unit_price` plausibility, not the view's actual `total_value/qty_remaining` basis) → became migration 165, now applied. Bands + join path + the value/qty_remaining lesson recorded in billing-format-and-wac-unit-model.md (§silent-unit class, §plausibility bands, §auditor mandate, §165 as-built).

**✅ LE ZEPPELIN HUB — DONE + COMMITTED + PUSHED + DEPLOYED + PM-VERIFIED (`e65d0dd` + `897d008`, origin/main; passed RULE-2 after fixing 2 contrast misses).** Family-router landing page — the top of the Le Zeppelin IA. As-built EXACTLY to plan (full detail in index header above):
- `public/modules/le-zeppelin.php` + `public/css/le-zeppelin.css` NEW. 4 cards in derivation order (Salle des Machines / Salle de contrôle / Le Cockpit[disabled --soon] / Données générales[admin-only]). Le Cockpit deliberately a dead `<div>` (no route exists yet). Recette is NOT a tile (lives inside Salle de contrôle).
- **Dead-mockup divergence CLOSED:** `/_design/le-cockpit.html` link rewired → `/modules/le-zeppelin.php` (topbar.php L14 + salle-de-controle.php L796); PM-verified zero live `/_design/le-cockpit` links remain.
- **CONTRAST RULE (reinforced + cost a re-fix):** `--oak` as small TEXT FAILS AA on `--bg-elev` card surfaces (4.30 < 4.5) though it passes on `--bg` paper (4.62) → use `--oak-deep` for text on ELEVATED surfaces. The same bug had silently shipped on the LIVE saisies hub arrow → fixed in `897d008`. Recorded in conventions-and-helpers §STANDING UI/UX (light-theme contrast).

**✅ OBSERVED-PATH ml→mass FIX — DONE + COMMITTED + PUSHED `a4b7efb` (origin/main; passed RULE-2 GO, 12 checks clean; PM-verified live).** Completes the SQL/OBSERVED path (recipe-LOADER leg was already done in `12d6ce1`).
- **Migration 161 `v_bip_canonical` LIVE** (schema_meta row present). View over `bd_brewing_ingredients_parsed_v2 JOIN ref_mi` exposing density-aware `qty_priced DECIMAL(22,10)` (mirrors `unit_to_canonical_factor()`: g→0.001, kg→1.0, ml+pricing-kg→density×0.001, ml+pricing-l→0.001, else refuse) + `factor_unresolved INT`.
- All 7 inline `IF(bip.unit='g',0.001,1)` blocks rewired to `v_bip_canonical.qty_priced`. **PM-VERIFIED live (2026-05-26): 4897 rows factor_unresolved=0; ALL 652 ml rows resolve.**

**§OBS-PATH FOLLOW-UPS (2 non-blocking, from RULE-2 reviewer — RECORDED):**
- (1) **Operator-visibility gap:** `unresolved_count>0` currently goes to error_log ONLY. Promote to a `doc_review_queue` row so a future density-less ml MI surfaces to the operator, not just dev-blind logs. (Refuse-don't-NULL IS honored — this is the surfacing leg.)
- (2) **Parity-test drift:** `scripts/php/gate4_parity_test.php::unit_to_kg_multiplier()` still does ml→kg=0.001 with NO density — align it with the density-aware `v_bip_canonical` or it will false-flag.
- (3) **`scripts/python/refresh_recipe_profile.py`** does its own g/kg conversion on the OLD `bd_brewing_ingredients_parsed` (no ml there → SAFE today). FLAG for when it migrates to `_v2` (which has ml rows + needs density).

**🟡 FERMENTATION operator form — BUILT + DEPLOYED un-linked, NOT committed (RULE-2 reviewer running 2026-05-26; commit follows GO).** `public/modules/form-fermenting.php` (+css/js) on the Brassage/`form-racking` pattern; writes `bd_fermenting_v2`. PM-VERIFIED on VPS (`public/modules/form-fermenting.php` present) + local (uncommitted: `?? public/modules/form-fermenting.php` + css + js). **BOTH PM-flagged divergence risks PASSED:**
- **Event-branched ✅ (NOT flat-write):** writes per `event_type` to `bd_fermenting_v2` — DryHop→`dh_*` (one row per addition, line_idx 0…N), Purge→`comment_purge`, ColdCrash→`comment_cold_crash`, Reads→gravity/ph/temperature; NK `(submitted_at,event_type,beer_raw,batch,line_idx)`. PM-verified live event-type spread: Reads 5257 / ColdCrash 818 / Purge 323 / DryHop 277.
- **No migration 162 ✅:** reused existing `final_comments`/`comment_purge`/`comment_cold_crash` (PM-verified all 3 present) — nothing redundant added.
- `dh_unit` enum kg/g only (NO ml — no density landmine); gravity labeled °Plato.
- **MEMORY CORRECTION (recorded):** `bd_fermenting_v2` has EXPLICIT `beer_raw`+`batch` columns + `recipe_id_fk` resolved ON THE ROW (all 3 PM-verified present). The legacy free-text parsing in `reference_bd_fermenting_text_parsing.md` describes the OLD `bd_fermenting` table ONLY — v2 normalized it. Future builds must NOT expect text-parsing on v2. (Topic file lives under `…/projects/…/memory/`; correction recorded here in the PM index — flag for that file's next edit.)
- **FERMENTATION DEFERRED BACKLOG (known gaps, NOT blockers):** (a) **state-gating** — picker is UN-gated (inherited from form-racking, which loads all active recipes); the proper fermentation gate (post-brew / in-CCT / not-yet-racked, thresholds from commissioning settings) is v1-DEFERRED. (b) **Mock fields with no DB column, flagged-and-unwired** (need future migrations if wanted): Purge CO₂ pressure + dissolved CO₂; ColdCrash target-temp/current-temp/crash-date/planned-duration; the live-context strip (beer/batch/CCT/day-count banner).

**FORWARD SEQUENCE (operator-confirmed 2026-05-26, updated post-mig-165 2026-05-27d):** (1) ✅ obs-path fix `a4b7efb` → (2) 🟡 Fermentation form (await RULE-2 GO + commit; then operator visual + saisies card-flip — flips together with Brassage) → (3) ✅ `v_mi_wac` WAC view DONE+COMMITTED `d8c004d` (migs 162+163) → (4) ✅ Le Zeppelin hub DONE (`e65d0dd`+`897d008`) → (4b) ✅ **migration 164 (PEACH_TEA+MOSAIC unit fixes) APPLIED+COMMITTED `5de9fd6`** → (4c) ✅ **AUDITOR MANDATE DONE — full silent-unit sweep (read-only, PM-supervised) → report `reports/wac-silent-unit-audit-2026-05-27.md`; PM triaged → 0 silent-unit bugs except the US05/W3470 2× the auditor MISSED → became migration 165** → (4d) ✅ **migration 165 (yeast US05/W3470 full-qty re-model) APPLIED+VERIFIED+COMMITTED `1d670d3`** → (5) ✅ **WAC-HYGIENE BATCH (A1 ECO non-inventoried + A2 view month-exclude + A3 catalog hygiene) APPLIED+VERIFIED+COMMITTED `930e09a` — shipped as migs 169/170/171 (renumbered around the yeast 166/167/168 series); BOTH wac_unresolved flags CLOSED, v_mi_wac 98→96** → (5b) ✅ **CATALOG-HYGIENE ROUND 2 (BE134→125.40, SIMCOE→28.32, LOG_SCOTCH→3.24) APPLIED+VERIFIED+COMMITTED `6098585` migs 172/173 — catalog-vs-WAC scan now CLEAN [0.67,1.5]× for ALL delivery-backed MIs; yeast 166/167/168 recovered into git `ed8e7c6`** → (6) **Phase D = wire COGS/COP + ref_mi.price writes to v_mi_wac** (mind is_inventoried scope divergence; COALESCE(WAC,catalog) catalog gate now CLEAN for all delivery-backed MIs — only remaining catalog consideration = the NO-DELIVERY inventoried MI set, see RESUME POINT) → (6b) **CONSUMPTION-WIRING into WAC/RM module** (RM value + consumption valued at WAC; on-hand = deliveries − consumption; DEPENDS ON: unit-normalization clean + is_inventoried scope reconciled) → (7) Recette fold-in-vs-standalone decision. **Unit-normalization (164+165+auditor sweep) is now CLEAN + both wac_unresolved MIs RESOLVED (169/170) → consumption-wiring is unblocked on those fronts (still gated on is_inventoried reconciliation).** **Brassage form deployed un-linked, awaiting operator visual + saisies card-flip (flips with Fermentation).** **RULE-1 backlog: dual-navigation-IA reconciliation + `index.php` fate (scrapping-backlog #12/#13).**

**📋 v_mi_wac — DONE (was LOCKED-then-DISPATCHED). As-shipped facts kept for reference; full audit → billing-format-and-wac-unit-model.md + reports.** Shipped via migs 162 (data) + 163 (view), `d8c004d`. Key durable facts that survive the build (still load-bearing for Phase D + future ingest):
- **WAC basis = `total_chf/qty_delivered` (effective price), grouped by `ingredient_fk`** (multi-supplier MIs aggregate across suppliers — MOSAIC, SIMCOE sc=2). Refuse-don't-NULL flag = `wac_unresolved` (2 flagged: PKG_TEA_BOT_CH pu=month, PROC_ALIGAL2_ECO pu=NULL).
- **DISPUTED ABBAYE id=74/418 — NO FIX applied (correct):** source "2 pcs @ 89 = 178" = 2×500g bricks = 1.0 kg @ 178/kg; stored qty already=1.0kg, price already per-kg; operator's qty→0.5 fix would have INTRODUCED a ×2 error. **MOSAIC id=104 — RESOLVED 2026-05-27 (migration 164 SPEC):** the invoice 8389 says "Sachet **5 kg**" → 2×5kg=10kg @ **29 EUR/kg = COMMODITY pellet** (NOT the "specialty 145/kg" earlier read — that mistook per-sachet price for per-kg). FIX = qty 2→10kg silent-unit correction (fk=40 stays commodity, WAC 11.517→11.367). **The operator's later "reclassify to HOPS_MOSAIC_CRYO" ask is HELD — premise (Cryo) contradicted by source invoice; `HOPS_MOSAIC_CRYO`=ref_mi.id 54 already EXISTS so no INSERT needed either way.** Detail → billing-format-and-wac-unit-model.md §MIGRATION 164.
- **`ref_mi_invoicing_units` = billing-format SoT but DRIFTED + incomplete** (was 17 rows / ~98 MIs; col is `invoicing_unit` not pricing_unit). De-drift RULE: pack_size ALWAYS in canonical unit; normalize at ingest with invariant assertion. THIS BUILD seeded touched-only → ~84 inventoried MIs still lack a row (carried-forward pending).
- Schema facts (verified): `inv_deliveries.pricing_unit` 100% NULL → unit authority from `ref_mi` via `ingredient_fk` (populate at ingest is future); `exclusion_class` ENUM(recoverable_vat,immobilisation,taproom_opex,office_opex); `is_inventoried=1` excludes qty=0 credit rows; eur_to_chf 100% on EUR Active rows; 0 NULL ingredient_fk. **Reports: `wac-billing-format-audit-2026-05-26.md` + `wac-unit-truth-dba-2026-05-26.md` in `/home/kluk/projects/maltyweb/reports/` (committed in docs follow-up).**

**🎨 SITE-WIDE LIGHT-KRAFT RECOLOUR — COMPLETE (parallel operator-priority track, NOT in the forward sequence above).** Committed `3c57e5b`, pushed, deployed via `bin/deploy.sh --apply`, PM-verified live on the VPS (app.css :root paper #f1e8d4 / --hop #567020 / cct now-marker 0.85 / svg-tanks empty-fill #cfc6b2 all present at /var/www/maltytask). Full as-built in the index header + conventions §STANDING UI/UX DIRECTIVE. **REMAINING QUEUED (confirmed still pending, NOT yet done):** (a) operator visual review of the live recolour; (b) flip the Brassage + Fermentation saisies cards LIVE (flips together — gated on Fermentation commit); (c) the liquid dry-hop ml/l capability; (d) the `v_mi_wac` WAC view (= sequence step 3 above).

**OPEN ITEMS (full detail → sku-decomposition-tree.md):** F2 per-HL liquid-source decision (still load-bearing for the deferred full observed-liquid recompute — derive each member's per-HL basis from its OWN single-SKU liquid BOM, canonical-wins via observed `bd_*` NOT `ref_recipe_ingredients`); the §8.9 Stage-1 closure backlog below.

(Prior arc, still valid → build-history.md / volume-dimension.md / packaging-bom-model.md:) Stage-1 packaging pipeline COMPLETE. **Volume dimension Phase A LANDED** (`a8950ed`: migration 144 `ref_sku_bom.volume_hl` + `v_sku_volume` view) **+ bot/can/can33 volume_hl backfill DONE (105 distinct SKUs)**. **`-4` carton** (migration 145). **`-X` cage dedup** (migration 147 `dec4481`; 0 `mi_match` rows remain). **BLA4 first live white-label** (migration 148). **FR-alias cleanup + 4 Diversion recipe links DONE.** Migrations 131–155 applied + recorded (147+148 pushed to origin/main; 149/150/151/152/153/154/155 applied live; 150 PAD→PD8 alias + 151 deleted spurious format 22; 152 PAC format 7→23; 153 composite overwrap packaging_choices; 154 brewday add_comments; 155 COLLAB24→DGDB alias). Step ③ composite compiler-fold APPLIED-THEN-ROLLED-BACK 2026-05-26 (unit-fidelity cost bug — composite_* BOM rows back to 0; data restored from snapshot).

**NEW OPEN — packaging-MI gap (small):** BLO bottle + BLO/ZEP cans have NO `PKG_6X4_{BTL,CAN}` carton MI → recompute emitted RQ rows (BLO4=3, BLO4C=1, ZEP4C=1) instead of NULL lines (refuse-don't-NULL worked as designed). Operator action: create those 6×4 carton MIs OR confirm those SKUs are cartonless. See packaging-bom-model.md.

**✅ DONE — volume_hl backfill (2026-05-26):** `volume_hl` now on **105 distinct SKUs** (was 18; brief said 104, live=105). Done as a SCOPED recompute over bot/can/can33 container slots (keg/cuv/composite excluded by design) — so the ZEP6C gating-gap was NOT triggered. Migration 147 (`dec4481`) removed the spurious format-6 `outer_box`/`pallet` slots; 24 legacy `mi_match` dup rows manually DELETEd (0 remain DB-wide). **NEW compiler-hardening item: COMPILER-DELETE-PREDICATE GAP** (recompile DELETE keys on `source='Packaging'` only → missed the `mi_match` dups). See volume-dimension.md + packaging-bom-model.md.

**✅ DONE — decomposition-tree builds (2026-05-26):** Step ① FR-alias cleanup (6 FR* SKUs aliased+deactivated, 15 stray rows reverted); 4 Diversion recipe links (DIBB12/DIBBU→r26, DIGBU→r27, DIPBU→r28); Step ⑤ **BLA4 first live white-label** (migration 148, 3 BLA MIs, recipe MOO r44, packaging-choices override → 5-line BOM); **Step ② composite-slots POPULATED (migration 149) — PD8/XMAS/PAL/PAC, PACKDEC folded → PD8 alias, PAC count reconciled to 6+6=12, PAD HELD (see RESUME POINT).** See sku-decomposition-tree.md + packaging-bom-model.md.

**NEXT ACTIONS (Stage-1 closure backlog → build-history.md §8.9):** operator browser-QA of 4 surfaces (Formats subtab, Cuves-de-service card, filler_cuv machine, packaging-form picker); the 6 RQ to-dos (create EPH box-sticker MI + `PKG_4PACK_CAN_ZEP`, then bind); create the BLO/ZEP 6×4 carton MIs (or confirm cartonless); set scotch A/B for DIV/EMB/MOO/ZEP 24-boxes; schedule the nightly recompute cron; deferred full observed-liquid recompute (needs the canonical per-HL liquid-source decision first); **compiler-hardening — fix the DELETE-predicate gap** (recompile DELETE must clear ALL legacy non-liquid source tags, not just `'Packaging'` — also relevant to step ③ replacing the stale flat composite BOMs) **+ the ZEP6C boxing-capacity gating-gap** (gate format 10 OFF). **PAC composite-membership count discrepancy is RESOLVED** (operator → 6+6=12; migration 149 populated it).

**SKU decomposition tree (settled 2026-05-26) → sku-decomposition-tree.md:** The THIRD decomposition axis (per-recipe HL share → ingredient attribution). All four infra tables ALREADY EXISTED live (POPULATE+WIRE, not green-field). Build sequence + state: **(1) ✅ FR-alias cleanup + REVERT FR* packaging-BOM rows — DONE.** **(2) ✅ populate composite slots — DONE (migration 149, this session): PD8/XMAS/PAL/PAC populated, PACKDEC folded → alias of PD8; PAC count reconciled to 6+6=12; PAD HELD.** **(3) NEXT — fold composite decomposition into the ACTIVE `app/sku-bom-compile.php` (emit composite_liquid/packaging from slots), DELETE-and-REPLACE the stale flat BOMs, SCRAP the dead `app/sku-compiler.php`.** (4) wire collab-temporal recipe resolution (COLLAB12/24 → DGD r31 before the buildability gate). **(5) ✅ BLA4/white-label via packaging-choices override — DONE (migration 148, first live white-label).**

**NEW BACKLOG (settled 2026-05-26, volume pass):**
- **`PKG_CAGE` activable root SKU (FUTURE PM-led build):** the reusable metal crate (distinct from the `-X` contents BOM) must become an ACTIVABLE SKU commissioned in capacités / Salle des Machines + inputable in the packaging form (operators record cages filled). Reusable asset, never sold. Rides the capacités-activation pattern (same family as the cuv gate, migration 142). → packaging-bom-model.md.
- **ZEP6C compiler GATING-GAP (durable fix OPEN):** the BOM compiler doesn't gate on commissioned boxing capacity — a full recompile would wrongly build 6C packaging; today sidestepped by scoping recompiles to EXCLUDE ZEP6C. Durable fix = gate format 10 OFF (no active 6C filler/cartoner) so no process builds its packaging. Generalize: compiler should honor commissioned boxing capacity for ALL formats. → packaging-bom-model.md.
- **Multipack→`-X` stock-pull BY CHANNEL (FUTURE build, RULE recorded now):** Shopify+B2C multipacks draw bottle/cap/label cost DOWN from `-X` cage stock; privé (BC account 1080) = unboxed units. Cost lives ONCE in `-X` — no double-count. Stage-2-adjacent. → packaging-bom-model.md.

