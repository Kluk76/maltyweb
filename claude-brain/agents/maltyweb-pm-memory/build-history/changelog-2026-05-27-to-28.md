# Build-changelog — 2026-05-27 → 2026-05-28

> Part of the build-history changelog (rolling, append-only, newest-first). Parent index: [README.md](README.md). Covers session-framework infra atoms 3-6 + three-SKU-identity-layers + PPB C8 hardening + ZEP6C compiler-gate + bd_packaging_v2 SKU resolution + EPH BC→C fold + RawDB→MySQL re-baseline + #44 format→SKU batch + racking C1/C2/C3a.

### 2026-05-28 — SESSION-FRAMEWORK INFRA atoms 3-6 LANDED (2 commits, pushed + deployed): shell partial + JSON dispatch + framework JS + helpers + Journal de bord dashboard — INFRA COMPLETE, pilots can start
**Commit `9d40f44` — atoms 3-5 (shell + JSON dispatch + framework JS + helpers).** `public/modules/session-shell.php` (799 lines, server-rendered partial wrapping any phase form body). `public/api/session-action.php` (227 lines, JSON dispatch endpoint with 12 actions allowlisted). `public/css/session-shell.css` (863 lines, `ss-*` namespace, light-kraft palette per the UI STYLE CONSTRAINT). `public/js/session-framework.js` (273 lines, phase lifecycle driver — sibling of `form-framework.js`, composes-with not replaces). `app/sessions.php` +210 lines with TWO NEW read helpers: `session_firewall_status()` derives the 3 start-phase gates from `op_session_steps` (one-fact-one-table, no derived col), `session_labels()` is display-only string composer for header chips. UI/UX mockup `public/_design/session-shell.html` with 5-view switcher (DÉMARRAGE / EN COURS / FIN / CLOSED / ABANDONED). RULE-2: CLEAN; 2 HIGH + 2 MEDIUM fixed in-commit (JSON-403-not-302 documented, `$subLabelHtml` naming invariant, `JSON_HEX_QUOT|JSON_HEX_APOS` flags, `Cache-Control: no-store`).

**Commit `da29eb6` — atom 6 (Journal de bord dashboard).** `public/modules/sessions.php` (688 lines, read-only operations cockpit) — Direction C timeline PRIMARY + Direction B vessels TOGGLE (`?view=` deep-link or localStorage); filters bar (form_type / vessel_kind / status / opened_by / date range / show_abandoned) all whitelisted against ENUM constants; default = last 7 days; pagination preserves filters via `sd_filter_url()` composed from VALIDATED variables (NOT raw `$_GET` — XSS-safe by construction); N+1 batched via inline `sessions_last_steps_batch()` using `ROW_NUMBER` window (NOT one query per session). `public/css/sessions-dashboard.css` (629 lines, `sd-*` namespace; reuses `ss-*` phase pills; body class layered `body.home.sessions-dashboard` overrides `.home .main` centering). `public/js/sessions-dashboard.js` (88 lines, view toggle + abandoned toggle with honest reload when zero abandoned in DOM). `session-shell.php` 2-line link fix (back-link + Journal-↗ both rewired `/modules/sessions-list.php`→`/modules/sessions.php`). RULE-2: 3 HIGH (all fixed in-commit) + 3 MEDIUM (cleaned dead JS; flagged pagination cap + page-slice count as future-cycle).

**Architecture discipline calls.** Did NOT call `tanks.php` SVG helpers — vessel rework is a separate UI/UX track (the operator-confirmed reworked-from-scratch sketch, per session-model-arc §UI STYLE CONSTRAINT). Direction B uses simple badge cards in v1; full vessel-map awaits the dedicated UI/UX agent + `595cd86:app/svg-tanks.php` git-archaeology. Did NOT add a new fn to `app/sessions.php` for the dashboard's N+1 loader — `sessions_recent()` was sufficient + `sessions_last_steps_batch()` stays local until a second page needs it (then promote). Flagged as a follow-up.

**State of the session-framework arc** (matches session-model-arc.md §SEQUENCING atoms):
- Atom 1 — Schema (mig 192) ✓ shipped `0cc350b`
- Atom 2 — Lifecycle resolver (`app/sessions.php`) ✓ shipped `418f3ee`
- Atom 3 — Shell partial ✓ shipped `9d40f44`
- Atom 4 — Framework JS ✓ shipped `9d40f44`
- Atom 5 — JSON dispatch ✓ shipped `9d40f44`
- Atom 6 — Journal de bord dashboard ✓ shipped `da29eb6`

**INFRA COMPLETE. Pilots can now start.** Next track per Sequencing A = **Pilot 3 = racking** (re-home C1-C5 + C6b + C7 recap into the 3-phase shell: start = pare-feu QC (BBT CIP attestation per C6a + lot eligibility + observed-gravity firewall); in_progress = capture loop (one batch at a time, references C1-C5 inputs); end = recap + close). Then fermenting → packaging → brewing pilots.

**Open follow-ups (not blocking, flagged for future cycles).** (a) Add "Journal de bord" to the topbar `$modules` array — needs operator label confirmation; (b) promote `sessions_last_steps_batch()` from inline to `app/sessions.php` if other pages need it; (c) **vessel-rendering rework for Direction B** — separate UI/UX track per PM ruling, recover sketch quality from `595cd86:app/svg-tanks.php` + `_design/salle-des-machines.html`; (d) pagination cap mismatch — dashboard caps at page=999 but `sessions_recent()` internal cap is 500 rows (silent at high pages; decide: expose cap or change pagination approach); (e) page-slice count pill shows "X sessions" but X is the per-page slice — "X de Y au total" requires an extra COUNT query.

### 2026-05-28 — THREE SKU IDENTITY LAYERS recorded as architectural fact + LIVE BUG flagged (`bd_packaging_v2.sku_id_fk` mis-routing via brewing-side alias, FIX PLANNED this session, NOT YET BUILT)
**Architectural fact recorded.** Operator clarified that a packaging event carries up to THREE distinct SKU identities — **brewing recipe** (`recipe_id_fk`, the liquid), **packaging SKU** (`sku_id_fk`, the package), **sales SKU** (eshop/B2B feed, possibly unboxed). White-label/relabel SKUs (BLO=ZEP liquid + BLO branding; BLA=MOO liquid + BLA branding) split layer 1 from layer 2 systematically. Recorded in packaging-bom-model.md §THREE SKU IDENTITY LAYERS with full live blast-radius audit + fix pseudocode; cross-link added to sku-decomposition-tree.md (orthogonal to the three-axis decomposition framing) + refuse-don't-NULL register confirmation in conventions-and-helpers.md.

**Live bug, OPEN.** `scripts/python/ingest_bd_packaging_v2.py` line 333 keys SKU lookup `sku_map.get((recipe_id, format_suffix))` — has NO signal for raw `neb_beer` brand text. For BLO/BLA events, brewing-side recipe-alias correctly resolves the LIQUID (BLO→ZEP=57, BLA→MOO=44), then the SKU lookup returns the LIQUID-brand SKU (ZEP4C, MOO4) instead of the PACKAGE-brand SKU (BLO4C, BLA4). Audited 2026-05-28: **72 events, ~1,779 HL** of BLO/BLA finished product mis-routed across 5 years (BLO4C 28ev/918HL, BLO4 29ev/566HL, BLA4 6ev/68HL, BLOF 9ev/227HL).

**Downstream impact.** Sales-driven COGS (`build-sales-cogs.js`), BOM compile input signal (the gate-helper parity work in `de90755` is downstream of this), production dashboards, vendable-HL × SKU consumers. Sales side itself unaffected — `inv_sales_*` reads sales-channel SKU codes directly.

**Fix planned this session.** Add a literal `sku_code → id` primary lookup; keep `(recipe_id, format)` as fallback. Re-ingest the 72 affected events (or full re-ingest). Audit residual mis-attributions on other prefixes — only BLO/BLA known so far.

**Discipline note.** The "never speculate on assignments" rule protected us — we surfaced raw `neb_beer` text + asked the operator about the BLO/BLA semantics rather than inferring from prices/patterns. The operator confirmed the alias model. The gate-helper parity rule does NOT apply here (different concern: this is input parsing, not gate consistency).

### 2026-05-28 — PPB C8 HARDENING TRIO LANDED + verified live (uncommitted, RULE-2 gate pending): vendable_hl backfill + hourly sweep + cross-source dedup-key fix
**Three fixes shipped together as the PPB-hardening bundle, deployed to VPS, awaiting RULE-2 + commit + Kouros visual check.**
- **(1) `db/migrations/193_v_bd_packaging_v2_vendable.sql`** — view with echo-row guard. **Already committed by a parallel session this morning as part of 418f3ee**; the file in this session's working tree is a byte-identical re-write (no semantic change). Applied on VPS via `migrate.php`.
- **(2) `db/migrations/200_backfill_bd_packaging_v2_vendable_hl.sql` (new)** — re-applies the view idempotently + audits + backfills 1996 rows. **Originally filed as `194_backfill_...` and conflicted with the parallel session's `194_ref_beer_types.sql` (BSF-exit Phase 1).** RENAMED locally to 200, `schema_migrations` row UPDATEd to match, VPS 194_backfill file removed. `migrate.php --status` clean. Live state: 1996 populated / 240 NULL (all `contract_run`, correctly NULL by design — no Neb SKU); total vendable_hl = 607,332.57 HL.
- **(3) `scripts/php/recompute_vendable_hl.php` + `db/cron/maltytask-vendable-hl-sweep.cron`** — hourly minute 17, idempotent guard `vendable_hl IS NULL OR vendable_hl <> v.vendable_hl`, audits with `comment='vendable_hl_sweep'`, tolerance 0.01 HL to ignore rounding noise. Canonical derivation in `v_bd_packaging_v2_vendable` is now the single source, agnostic to writer (form-php / python ingester / future framework packaging pilot). Sweep dry-run 0 rows out of sync at deploy time.
- **(4) `app/loss-metrics.php` — PPB cross-source dedup-key fix** — `_loss_load_packaging_map()` keys dedup now includes SKU format suffix via new `_loss_extract_format_suffix()` helper. Same refuse-don't-correct posture as the Cuv `0e8be26` resolution: same-day rows for DIFFERENT SKUs ARE separate real draws and now sum correctly (e.g. EPH1 batch 26 on 2026-03-19 = EPH1B 10.091 HL + EPH1F 18.0 HL, previously collapsing one). Earlier in the same diff: data-coverage cutoff (LOSS_WORT_SALES const + `_loss_racking_data_floor` resolver + cast-out HAVING+skip) + wort-sales skip.
- **(5) `public/modules/tanks.php`** — earlier today: "Soutirage" → "Transferts" column rename + coverage caption surfacing the racking-data floor.

**Live verification post-deploy.** PPB C8: 803 brewed → 377 in PPB (data-coverage cutoff filter), **150 complete / 227 incomplete**. Was 113/264 pre-dedup-fix, 163/640 pre-everything (i.e. the dedup fix alone reclassified ~37 batches from incomplete→complete). Incomplete breakdown: 29 real WIP (<90d), 198 stale (≥90d) = 8 PKG_NULL + **190 DRAWN_SHORT** (residual). The 190 residual DRAWN_SHORT is now believed to be REAL short-draws (genuine losses, sample pulls, contract diversions, dump events) — NOT a resolver artifact; logged as a separate data-quality / loss-investigation backlog item, not a further resolver-patching exercise.

**Migration-numbering near-miss (recorded in standing checklist).** This session was operating ahead of an in-flight parallel session that committed 193-199 (BSF-exit Phase 1 foundation + ENUM fix). My backfill was originally filed as 194 and would have shadowed `194_ref_beer_types.sql` if both had landed unchecked. Caught + renamed before fresh-VPS-from-git rebuild → no actual drift. Lesson re-confirmed: the migration pre-flight `--status` check is the FIRST step, especially when a parallel session is in flight; re-check immediately before assigning a number.

**Cleanup.** All helper scripts removed local + VPS; diff = +114/-16 across 2 modified + 3 new files. Uncommitted; awaiting Kouros review + RULE-2 commit gate.

### 2026-05-28 — C8 KPI "Pertes par batch" LANDED on the Tank Board (commit `cf23504`, maltyweb `main`) + Cuv same-day dedup fix `0e8be26` (sim + C7 key-aligned, Zepp 209 74→94 HL, 0 non-Cuv regressions) + PACKAGING+BREWING PRE-FRAMEWORK PASS SCOPED (PM)
**C8 LANDED.** The Pertes par batch KPI section on `public/modules/tanks.php` + `public/css/tankboard-pertes.css` + `public/js/tankboard-pertes.js` is server-rendered from `app/loss-metrics.php` (`loss_metrics_for_batches()` + `loss_thresholds()`), 20 complete batches by default + "Tous" toggle, soft amber for flagged / `--ok` green for yield bonus / `--cold` blue for data oddity (Double Oat b64 = Finding 2) / `--ink-faint` dash for NULL. Drill-in via row expansion matching the cct-detail-modal.js event-delegation idiom. UI/UX expert designed, operator approved. Deployed + lint clean. **Cuv same-day dedup fix `0e8be26` LANDED** (already recorded above — recap: sim + C7 key-aligned around the dedup key, Zepp batch 209 74→94 HL, 0 non-Cuv regressions). Finding 1 now retracted as a blocker — the dedup IS the fix (the same-day rows ARE separate real draws and now SUM correctly). C7+C6a+C8 + the Cuv fix = the hybrid step-1 done. **Next operator direction (2026-05-28): "packaging et brewsheets" — a PRE-framework pass that ports the racking patterns to those two forms, BEFORE the session framework infra is built.** PM scoped: pattern inventory (P1-P19), per-form transfer map, hybrid sequencing (P1 packaging slice → P1.A loss_cause/loss_note + P1.B qc-thresholds rewire + P1.C/D soft validations + P1.E CIP-cadence-machines config; P2 brewing slice gated on operator answer to cooling-final_volume capture path), 3 operator clarifying Qs. Full → packaging-brewing-pre-framework-pass.md. The pre-pass leaves zero throwaway: when the session framework arrives, pilots 5+6 reduce to re-homing into the 3-phase shell + plugging the CIP-cadence soft-flag into the start-firewall card.

### 2026-05-28 — ZEP6C / 6C COMPILER-GATE HARDENING + DELETE-PREDICATE HARDENING LANDED (commit `de90755` + migration 191, maltyweb `main`)
**The 6C / ZEP6C compiler-gate-honor backlog from `#44.b` + the broader "compiler gate divergent-store smell" are CLOSED.** Two legs shipped in one commit + one migration; both gates (compiler + UI) now resolve IDENTICALLY.
- **`app/sku-bom-compile.php` — 7 edits, +147/-9 lines:**
  1. **Buildability gate hardened across 3 query branches** (normal at ~159, COLLAB UNION normalIds at ~194, COLLAB UNION collabIds at ~221): all three now INNER-JOIN the dbc commissioning chain `ref_filler_containers × ref_process_machines × dbc_packaging_format_templates × dbc_container_types`. **New helper `_compiler_gated_format_ids()` at EOF — same SQL signature as `sdc_gated_format_ids()` in `public/modules/salle-de-controle.php`.** Both helpers MUST stay in sync — comment documents the parity; PM enforces on any future gate-rule change.
  2. **Composite DELETE predicate widened** (~1169 live + ~1141 dry-run COUNT mirror): `bom_source IS NULL` → `(bom_source IS NULL OR bom_source IN ('composite_liquid','composite_packaging'))`. **Closed an active doubling bug** — any `--apply --sku 137,138,278,295` recompile would have doubled rows (211 + 211 = 422).
  3. **Non-composite DELETE predicate flipped** (~781): `source = 'Packaging'` → `(source <> 'Brewing' OR source IS NULL)`. Drift-proof: any future legacy tag (mi_match, Packaging, NULL) gets purged on regenerate; only Brewing survives. **Supersedes the prior compiler DELETE-predicate gap** (was a known item since the `-X` cleanup).
  4. **UoT (refuse-don't-NULL) assertion at function entry** (~499, after `$summary = []` + `$totalErrors = 0;`): for each requested SKU, if format NOT in `_compiler_gated_format_ids()` AND no composite slots → SKIP with specific error `"format_id={n} not commissioned (not in dbc gate) and not composite"`. CLI STATUS=ERROR row. **NEW REFUSE-DON'T-NULL PRECEDENT: assertion at function entry, runs before main processing loop, on requested-SKU level.**
  5. **Foreach skip-guard** (~561): prevents the pre-existing missingIds path from clobbering the UoT error message or double-incrementing `$totalErrors` when UoT already marked the SKU.
- **Migration 191 — ZEP6C (id=57) → is_active=0.** Defense-in-depth alongside the gate fix. Single 2024 contractor-packed run (1 bd_packaging_v2 event id=1213, 42912 cans ≈ 214.56 hl, 0 sales rows in our 2025-12+ window).
- **Verification artifacts:** pre-patch dry-runs (composite DEL=0 INS=211 = would double; ZEP6C INS=3 = phantom-fill) → post-patch dry-runs (composite DEL=211 INS=211 = no-op; ZEP6C errors=1 STATUS=ERROR with UoT-specific message; auto-detect 0/0/0 unchanged). Post-rsync production CLI matches `/tmp` test wrapper byte-for-byte. `php -l` clean.
- **CONTRACTED-OUT RECURRENCE MODEL recorded** (operator-confirmed ZEP6C was the ONLY contracted-out SKU ever; discontinued, no plan but possible to recur; 2024 contractor invoice hit account 4208 "frais de packaging externe" at period-level, no per-SKU allocation). **When it recurs (design then, not now):** (1) operator picks a SKU code; (2) `ref_packaging_bom_templates` row with `supply='client_supply'` (the dormant ENUM lever) — compiler's `we_supply` INNER-JOIN rejects packaging emission, liquid-only via `source='Brewing'`; (3) synthetic per-unit MI `PKG_EXT_PACKAGING_<CONTRACTOR>_<FORMAT>` on account 4208, fed by WAC; (4) result = per-SKU contractor cost in COGS, the thing 2024 couldn't do. Compiler gate naturally protects the rest — un-commissioned `client_supply` activation gets the UoT assertion error. **Full ruling → packaging-bom-model.md §CONTRACTED-OUT PACKAGING RECURRENCE MODEL.**
- **CLOSED:** `#44.b` (ZEP6C-specific) + the broader compiler-gate divergent-store smell (compiler and UI gates now identical). Refuse-don't-NULL pattern REINFORCED in the compiler (new precedent: UoT assertion at function entry).

### 2026-05-28 — bd_packaging_v2 packaging→SKU NULL RESOLUTION + EPH BC→C CANONICAL FOLD LANDED + VERIFIED LIVE (commits 849426d + cab522f, maltyweb `main`)
**The 248-NULL `bd_packaging_v2.sku_id_fk` resolution arc is CLOSED on the Neb-product slice; the BC→C fold is now reconciled on the CATALOG side too. 3 migrations (187/188/189), BOM-recompile cost-neutral.**
- **Migration 187 — EPH BC→C canonical fold.** Renamed `sku_code` EPH1BC/2BC/3BC/4BC → EPH1C/2C/3C/4C (ref_skus ids 29/32/35/38), repointed `format_id` 8→7 (C, same family as ZEPC), inserted 4 `ref_sku_aliases` EPH{n}BC→canonical id. **Format 8 (BC) now has 0 active SKUs → DORMANT, scrapping candidate.** C is now the canonical 24-box-cans-bulk suffix; BC is alias-only. Reaffirms `reference_fmt_suffix_bc_folds_to_c` — now reconciled on the catalog side, not just the parser side.
- **BOM recompile of the 4 EPH SKUs (mandatory after the format repoint): cost-NEUTRAL confirmed** — `pkg_cost_sum` 1.293950 unchanged for all 4, parity_violations=0, errors=0. The pre-existing EPH config-B box-sticker RQ row (no `PKG_STICKER_EPH*` MI) is unchanged, NOT worsened (still the §8.9 #1 open RQ item).
- **Migration 188 — bd_packaging_v2 data resolution.** Bucket B: 4 EPH rows → `sku_id_fk` 29/32/35/38. Bucket C: 4 Docks/DGD CollabIn rows → set neb_beer + suffix B/F + `sku_id_fk` DOCB/DOCF/DGDB/DGDF = 21/22/8/9. 240 true-contract rows relabeled `sku_unresolved → contract_run` (token-replace within the comma-list flag, scoped to `contract_beer NOT NULL AND neb_beer NULL AND nebuleuse_format_suffix NULL`, run AFTER Bucket C so the rerouted CollabIn rows aren't caught).
  - **SCHEMA CHANGE — `chk_sku_or_flagged` CHECK broadened (DROP+recreate).** Added a `contract_run` branch: now `sku_id_fk NOT NULL OR sku_unresolved OR contract_run OR row_origin='parallel'`. Integrity PRESERVED (broadened, not weakened) — every row still must carry SOME valid disposition. Necessary autonomous change to allow the relabel.
- **Migration 189 — cleared stale `sku_unresolved` flags** from the 8 now-resolved rows: 4 EPH keep `fmt_bc_folded,qa_extraction_overshot`; 4 collab rows got `collabin_rerouted_to_neb`.
- **Verified end-state (live):** `sku_id_fk IS NULL` 248→**240** (all 240 = true contract, all carry `contract_run`); 8 resolved rows correct (29/32/35/38 + 21/22/8/9), no stale unresolved flags; format_id=8 active SKUs = 0; EPH SKUs = EPH{n}C on format 7, BOM cost unchanged.
- **Normalizer root-cause fix (NOT committed — maltytask `scripts/normalize-rawdb.py`, operator-governed):** recipe loader now selects `classification`; packaging processor reroutes any `classification='Neb'` recipe (incl. CollabIn) to the Neb branch — sets neb_beer + nebuleuse_format_suffix from run_type (bot→B, keg→F), appends `collabin_rerouted_to_neb`. Forward-looking; we did NOT re-ingest (re-ingest would dup via row_hash change — the live fix WAS the 188 DB UPDATE). File LEFT UNCOMMITTED because mixed with a pre-existing uncommitted blob-parser fix belonging to the separate source-reconvergence task; operator commits it there.
- **STILL OPEN (recorded):** (1) the 2 prereq-2-lane items the 240 contract rows expose — `client_fk` resolution (parse contract_beer → ref_clients) + add `materials_supplied_by` flag to ref_clients — GATED on commissioning (= packaging-bom-model.md contract-NULL prereqs 1+2, unchanged). (2) normalize-rawdb.py uncommitted (source-reconvergence task). (3) format 8 (BC) dormant → scrapping backlog.
- **Source-of-truth divergence note:** the 188 resolution is a DB-only fix; the normalizer fix is forward-looking-only (no re-ingest). Same DB-vs-RawDB.xlsx divergence pattern as the date-swap + CO₂ fixes — converges when the source-reconvergence task runs.

### 2026-05-27r — RawDB→MySQL UPLOAD ARC RE-BASELINED from verified live numbers (orchestrator-fed + PM-verified live 2026-05-27)
**Corrects the STALE 2026-05-23 DBA-audit "0% resolved" claim. The bd_* v2 tables are LOADED AND USABLE — alias worklist (resolution arc b) is effectively DONE for recipes + ingredient-MIs.**
- **v2 row counts (the normalized-upload targets, PM-verified live):** bd_brewing_brewday_v2 803, bd_brewing_gravity_v2 8,436 (4 gravity sheets folded via event_type), bd_brewing_timings_v2 2,198, bd_brewing_ingredients_parsed_v2 4,897, bd_brewing_ingredients_v2 677, bd_fermenting_v2 6,804, bd_racking_v2 **400** (was 393; racking agent edited TODAY, updated 21:42 → date-swap status needs FRESH re-check before trusting), bd_packaging_v2 **2,236** (PM-verified). Old non-v2 still coexist (brewday 824, fermenting 6,629, packaging 2,186, racking 393) → **steps 3-4 legacy-decommission/rewire NOT started.**
- **Resolution quality (PM-verified live, supersedes the audit's 0%):** recipe_id_fk — brewday 100% / ferment 100% / packaging 100% / racking 99.8% (1 NULL = known Zepp batch-118 review row). ingredient→MI — `bd_brewing_ingredients_parsed_v2.mi_id_fk = 100%` (4,897/4,897).
- **THE ONE REAL RESOLUTION GAP — bd_packaging_v2.sku_id_fk = 88.9%, 248 NULL (PM-verified).** Split (PM-verified live): **244 = contract events** (contract_beer set, neb_beer NULL, `client_fk=0`/NULL on ALL) + **4 = Nébuleuse-product rows** that have a neb_beer but failed SKU resolution + 0 orphan. The 244 are exactly the recorded contract breweries (Chien Bleu/Jasper 26, BLZ Company/WestCoast PA 25, MeltingPote/Cropette 24, Brasserie du Château, BadFish, Abbaye de St-Maurice, Les Combières, Septentrion, Docks, DrunkBeard, L'Improbable, Le Traquenard, NYL, TM-*, …). Operator running read-only triage of the 248 → disposition sheet.
- **CONTRACT-SKU SEMANTIC RULING (PM, this session): the 244 contract events are CORRECTLY NULL for `sku_id_fk` BY DESIGN — there is NO La Nébuleuse SKU for another brewery's beer, and `ref_skus` carries NO contract-SKU concept (verified: no contract/client column on ref_skus implied; ref_skus is the Nébuleuse offer catalog).** sku_id_fk = NULL is the right state for them; reclassify the triage flag `sku_unresolved → contract_run`. They resolve on the CLIENT axis (`client_fk` → `ref_clients`), NOT a SKU. So of the 248: **~244 → "correctly NULL" (contract); ~4 → genuinely "needs fixing" (Neb-product SKU resolution).** See the two open builds below.
- **Two follow-on builds this exposes (recorded, both OPEN):** (1) `client_fk` is UNRESOLVED on all 244 (only the `contract_beer` "Client - BeerName" string is set) → the contract events need client resolution into `ref_clients` (parse `contract_beer` → client) for the material-responsibility branch to ever apply. (2) `ref_clients` has **NO materials-responsibility flag** (cols: id/name/notes/created_at only) — the recorded per-client COGS rule ([[contract-packaging-cogs-per-client]]) prescribes `materials_supplied_by = nebuleuse|client`; that column does NOT yet exist. Both are prereqs to the contract packaging-consumption branch (prereq-2 / production-SKU depletion arc — cross-linked in rm-retro-prereq-chain.md). Neither is a now-task.
- **Other genuine gaps (recorded):** ferment.dh_mi_id_fk 4.1% = almost-certainly semantic-NULL (most ferment rows aren't dry-hop events) — confirm only if dry-hop hop-consumption must feed COGS. Source-of-truth divergence still open (date-swap + CO₂ fixes live only in DB, NOT in RawDB-normalized.xlsx; the today racking edit means date-swap needs re-check). Legacy decommission + rewire (steps 3-4) not started.

### 2026-05-27q — #44 FORMAT→SKU GAP-CLOSING BATCH LANDED (migs 185+186, commit `7f86499`)
**One commit on `main`, both migrations applied live (recorded in `schema_migrations` 21:15) + PM-verified.** #44 essentially CLOSED.
- **Migration 185** — DEACTIVATED PBD (`ref_skus` id=294): was is_active=1 with 0 BOM/0 slots/0 alias = a zero-cost active silent COGS leak. Operator ruling = distinct DISCONTINUED product (NOT a PD8 alias) → is_active=0. **Safety-check finding: PBD has 210 `inv_sales_order_lines` spanning Dec 2025 — it really sold** → going-forward deactivation preserves them as read-only history, but the Dec-retro cost-basis gap is now flagged in rm-retro-prereq-chain.md.
- **Migration 186** — LINKED COLLAB4PACK (`ref_skus` id=302) to format_id=4 (4PB). Empirically validated: format 4 = "4-pack loose bottles (4×33cl)", run_type=bot, hl_per_unit=0.0132; 12 sibling `{BEER}4PB` SKUs all identical → COLLAB4PACK is the collab member. Holder slot (ref_packaging_items id=21) is INERT (NULL default) → no phantom holder billing; no outer_box slot → no phantom carton.
- **Operator convention confirmed (durable):** `-4P` = WITH holder; `-4PB` = WITHOUT holder (4 loose bottles). All 12+1 SKUs on format 4 (`4PB`) are loose.
- **187 (33C/V `ref_packaging_format_mis` edges) SKIPPED** — `ref_packaging_format_mis` read by nothing live; 33C/V already cost correctly; 33C edge also blocked on unpriced PKG_CAN_ALU_33 (id=617 NULL stub). Dormant metadata, revisit only if a live reader appears.
- **6C compiler-gate-honor fix → BACKLOG (deliberate hardening, not urgent).** Read-only scoping done: `sku-bom-compile.php` `compile_sku_bom_packaging()` has NO commissioning-gate JOIN; ZEP6C safe ONLY by accident (0 Packaging-NULL rows so auto-detect skips it; no hardcoded exclusion). Durable fix = gate 6C OFF + add commissioning-gate JOIN. Touch points: buildability gate ~147-160, auto-detect ~91-96, $formatVolumeMap ~425-470.
- **Residuals:** 6C-hardening (backlog), PAC composite format_mis (open architectural Q — does a composite need format_mis edges, or is slots+choices sufficient? PD8/PAL/XMASPACK have them, PAC doesn't), PBD-Dec-retro cost-basis gap (folded into the retro arc). Full rulings → packaging-bom-model.md §FORMAT→SKU RESIDUAL-GAP TRIAGE (#44).

### 2026-05-27p — RACKING REDESIGN C1 / C2 / C3a LANDED + DEPLOYED (de-hardcode COGS gate PASSED)
**Three commits on `main`, all committed + deployed:**
- **C1 `6db7368`** — TankSimulator (`app/tank-simulator.php`) `blend_info` kept lot-faithful on draw-down: PACKAGING branch + the deferred-deduction path now pro-rata-decrement each lot via new `_bbt_prorata_decrement`; new `tank_bbt_composition()` read API exposes the per-lot composition. **Invariant `sum(lots)==volume_hl` now holds — 0 violations across 7 BBTs.**
- **C2 `c80a428`** — migration **183**: `bd_racking_v2` +7 ALTER cols — `loss_source_hl`, `loss_dest_hl`, `loss_cause` ENUM, `loss_note`, `interrupted_flag`, `interrupted_reason`, `dest_bbt_still_clean`. All nullable / 0-default, applied + idempotent, 400 rows intact.
- **C3a `abc1d83`** — migration **184**: `commissioning_settings` section='pertes', 7 rows — `pertes_racking_loss_hl=0.9`, `pertes_packaging_loss_hl=0.15` (both COGS/WIP-labelled) + 5 advisory %-paliers (rack2 / packaging1 / brewing5 / total-effectif18 / total-nominal10). **DE-HARDCODE:** TankSimulator `RACKING_LOSS_HL` / `PACKAGING_LOSS_HL` read once in `__construct` from `commissioning_settings`, COALESCE to the kept class-consts (defaults preserved). + SDC `?sec=pertes` config panel + `update_pertes_config` handler (role-gated, `log_revision`).

**🟢 COGS GATE — PASSED (PM asked for this gate).** Full sim run dumped **21 tank states (8 BBT + 13 CCT)** before-vs-after the de-hardcode with seeded defaults → **byte-identical, zero diff.** Proof-of-wiring: setting `pertes_racking_loss_hl=1.5` moved every BBT down by exactly 0.6 HL (CCTs unchanged — racking loss hits BBT-fill only), then restored to 0.9 → zero diff again. The de-hardcode feeds COGS/WIP and is confirmed cost-neutral with seeded defaults.

**SCOPE SHIFT (sequencing impact):** C3a ALSO landed, DORMANT, the sim RACKING-branch `loss_dest_hl` pro-rata application + the `loss_source_hl`/`loss_dest_hl` read in `loadRackingEvents`. All live rows have losses=NULL → dormant → covered by the byte-identical gate. **Consequence: the sim loss_dest math is ALREADY IN PLACE, so C3b is now REDUCED to the Pertes FORM side only** — UI inputs + cause ENUM + loss_note + balance-preview + soft over-volume "impossible" flag + rack-stage palier reading `pertes_rack_warn_pct` + POST write + adding the mig-183 loss cols to `$hashCols`. No sim loss math left to build in C3b.

**Remaining queue (per Layer-5 ruling, updated):** C3b (Pertes form UI, form-side only) → C3c (forced-comment → loss_note via FormFramework commentTarget) → C4 (interrupted → reason + BBT-propre + source-CCT decrement + gate) → C5 (auto-blend dest UX) → C6 (CIP cadence) → C7 (total-loss derivation `app/loss-metrics.php`, DERIVED) → C8 (brewing-stage flag surfacing, DERIVED, operator-confirmed zero-input). C4/C5/C6 parallelizable. **DONE: C1, C2, C3a, + sim loss_dest math.** `migrate.php` now at **184**, NEXT free = 185.

