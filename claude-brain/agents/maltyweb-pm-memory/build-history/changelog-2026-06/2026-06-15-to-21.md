# Build-changelog — 2026-06

> Part of the build-history changelog (rolling, append-only, newest-first). Parent: [README.md](README.md) → [changelog-2026-06/README.md](changelog-2026-06/README.md).

## 2026-06-15 → 2026-06-21

### 2026-06-21 (BREWING-LOOKUP FOLDED INTO WORT + WORT BLANK-PAGE FIX + PACKAGING LOOKUP LABELLED — shipped+live+pushed, mig 428; SUPERSEDES the same-day standalone page; MIG HEAD now 428)
- **✅ REVISION of the same-day standalone brewing-lookup page — operator directive: fold it into `wort.php` instead.** 3 commits. Net effect: brewing lookup is now an **operator-level "Consulter" tab on `wort.php`** (data-tab=lookup, panel #wort-panel-lookup, operator/production gate), NOT a viewer-level standalone page; the earlier "for everyone / all-7-presets" access is GONE. SUPERSEDES mig 425 (the standalone page).
- **DERIVATION POSITION:** Salle de contrôle / production read surface. PURE READ — no DB writes, no fiscal lane, never feeds COGS/COP/WAC/BOM/beer-tax/inventory. Reuses the shared `lookup-panel` partial + `lookup-panel.js` (unchanged).
- **`5561145` — wort.php blank-page bug ROOT-CAUSED + FIXED.** ROOT CAUSE: `public/js/wort-kpis.js` aliased 6 `window.KpcCharts` primitives (escHtml, fmt, svgEl, MONTHS_FR, buildBarChart, paceTintResult) with top-level `const` → collided with `kpi-charts.js`'s top-level `function escHtml`/`fmt` across classic (non-module) `<script>` tags → Chromium `SyntaxError: Identifier already declared` → wort-kpis.js died AT PARSE → initTabSwitcher() never ran → whole page dead/no-data. FIX: changed those 6 to `var` (reuses global-object slot, no lexical clash). 🔴 **DURABLE LESSON:** two classic scripts sharing globals must alias shared funcs with `var`, NEVER `const`/`let`; `kpi-charts.js` exposes funcs BOTH as top-level `function` AND on `window.KpcCharts`, so ALL 3 KpcCharts consumers (wort, packaging, mon-tableau) must use `var` aliases — all 3 re-verified rendering clean. (Fold into the `ui` skill rendering-bug catalog on next `ui` touch.)
- **`aef78aa` — folded the brewing lookup INTO wort.php as a 3rd "Consulter" tab.** `$lookupConfig` + recipe-options query added to wort.php's top PHP block (guarded), reusing the shared `lookup-panel` partial. API `public/api/brewing-lookup.php` gate repointed `require_page_access('brewing-lookup')` → `('wort')` (endpoint KEPT, page GONE). **Migration 428** `428_retire_brewing_lookup_page.sql` APPLIED to prod + recorded in `schema_migrations` MANUALLY (NOT migrate.php — parallel sessions pending): audit-tombstoned `ref_pages` id 739 via `action='update'`+`after_json` `_tombstone` (action ENUM has no 'delete'), DELETEd id 739 + its 7 `ref_access_preset_pages` grants via CASCADE. **Live mig head now 428.** DELETED `public/modules/brewing-lookup.php` + `public/css/brewing-lookup.css` (VPS + git). Removed the 2 orphaned brewing-lookup Visite-guidée cards from `visite-guidee.php` → tour-gap-check critical=0 / no-orphan. (RULE 3: a removed page MUST have its tour card removed — orphan cards fail the gap-check.)
- **`61a0130` — packaging lookup labelled + saisie CTA removed.** Wrapped the existing packaging lookup panel on `packaging.php` in a labelled "Consulter un packaging" header (`pkg-lkp-` classes, CSS in `lookup-panel.css` with kraft-paper tokens) to match the brewing surface. Removed the "+ Saisir un conditionnement" CTA from packaging.php (bare `<a href=/modules/form-packaging.php>`, zero dependency; the dedicated saisie page covers entry).
- → read-only-lookup-modules.md (rewritten: brewing = wort 3rd tab, mig 425 superseded by 428, packaging = labelled section, + the var-alias durable lesson).

### 2026-06-21 (BREWING-LOOKUP STANDALONE PAGE shipped+live+pushed: `6075fb0` page+mig 425+API gate+panel removal / `8120817` tour card — ⚠️ SUPERSEDED same day by mig 428, see entry above)
- **✅ BREWING LOOKUP IS NOW A STANDALONE READ PAGE — RESOLVES the read-only-lookup arc's ROADMAP FLAG (brewing lookup no longer `saisies`-gated).** Operator wanted "anyone" to look up brewing entries; shipped as a dedicated `require_page_access('brewing-lookup')` page (min_role viewer + granted to all 7 non-admin presets + tour card). All live + validated.
- **DERIVATION POSITION:** Salle de contrôle read surface. PURE READ — no DB writes, no fiscal lane, never feeds COGS/COP/WAC/BOM/beer-tax/inventory. Reuses the shared `lookup-panel` partial + `lookup-panel.js` engine (unchanged; packaging.php still uses it).
- **MIGRATION 425** `425_brewing_lookup_page.sql` — APPLIED to prod + recorded in `schema_migrations` MANUALLY (NOT migrate.php — parallel sessions pending; avoided the apply-ALL cascade). **Live mig head now 425.** `ref_pages` id **739**: page_key `brewing-lookup`, label "Consulter un brassin", icon 🔍, href `/modules/brewing-lookup.php`, min_role viewer, domain general, category_key pilotage, category_sort 30, sort 17, is_active 1. Granted via `ref_access_preset_pages` to ALL 7 non-admin presets (manager, production_operator, logistics_operator, marketing, sales_manager, finance_viewer, smoke_viewer — operator "for everyone" → sales_manager INCLUDED, even though the journal-saisies mirror had only 6).
- **`6075fb0` (page + mig + API gate + panel removal):** NEW `public/modules/brewing-lookup.php` (page-chrome copied from `journal-saisies.php`) + `public/css/brewing-lookup.css`; gate `require_page_access('brewing-lookup')`. `public/api/brewing-lookup.php` gate changed `saisies`→`brewing-lookup`. Brewing-lookup PANEL REMOVED from `form-brewing.php` → form is pure input again (single canonical home for the lookup). Modes day|recipe+lot; reads bd_brewing_brewday_v2 + gravity/timings/parsed-ingredients; OG-not-FG; matches (recipe_id_fk,batch); v2-only — all discipline preserved.
- **`8120817` (tour card):** `$PAGE_DESCRIPTIONS['brewing-lookup']` + `$PAGE_ICONS['brewing-lookup']` added to `visite-guidee.php`; tour-gap-check now critical=0 minor=0 (RULE 3 satisfied — this page IS a new is_active ref_pages row, so the tour card was required).
- **HTTP SMOKE:** page / API / form-brewing all 302 (no fatals).
- → read-only-lookup-modules.md §Build 2 standalone as-built + §ROADMAP FLAG RESOLVED.

### 2026-06-21 (FOUR builds shipped+live+pushed: packaging-recap UNITS `d4a8861` / read-only lookup modules `e83c4d1` / recap per-cadence config `4dfe7b8` mig 424)
- **MIG HEAD now 424** (`424_recap_per_cadence_config.sql`). Applied via `ssh maltyweb "sudo mysql maltytask" < file` + MANUAL `schema_migrations` INSERT (NOT migrate.php — a parallel session's 423 was pending-then-applied; avoided the apply-ALL cascade). Live-verified: `uniq_user_cadence` UNIQUE(user_id_fk,cadence) replacing UNIQUE(user_id_fk); `send_dow` TINYINT present; `user_recap_tracker_selections`=112 rows. All four builds deployed via SURGICAL per-file `rsync --rsync-path="sudo rsync"` + chown maltytask:www-data + chmod 644 (NOT bin/deploy.sh — parallel email-orders session had dirty .gitignore + untracked files); committed by explicit pathspec.
- **Build 4 `d4a8861` — packaging daily-recap UNITS section.** Display-only, NO mig, NO fiscal lane. `app/kpi-handlers.php` `kpi_pkg_daily_recap`: +`$grandTotalUnits` + per-run `meta.units`, uses `bd_packaging_v2.prod_total_units` ONLY (no double-count). `public/js/kpi-charts.js` `renderKpiRecap` + `app/kpi-email-render.php` mirror (pure projection, no $pdo) + `public/css/mon-tableau.css` `.kpc-recap__units`. → kpi-tracker-catalog/daily-recap-refinements.md §UNITS.
- **Builds 1+2 `e83c4d1` — read-only lookup modules.** Shared `public/modules/partials/lookup-panel.php` + `public/js/lookup-panel.js` + `public/css/lookup-panel.css`. Build 1 packaging on `packaging.php` (require_login) via `public/api/packaging-lookup.php` (modes day|SKU+lot; bd_packaging_v2×v_bd_packaging_v2_vendable+bd_packaging_readings). Build 2 brewing on `form-brewing.php` (GATED `require_page_access('saisies')`) via `public/api/brewing-lookup.php` (modes day|recipe+lot; bd_brewing_brewday_v2+gravity/timings/parsed-ingredients; OG-not-FG; matches (recipe_id_fk,batch); v2-only). All read-only, parameterized, NO mig, NO ref_pages. 🔴 ROADMAP: brewing lookup gated behind `saisies` but operator wants "anyone" → flagged; relaxing cleanly = standalone require_login read page (new ref_pages row + tour card + preset grant). → read-only-lookup-modules.md.
- **Build 3 `4dfe7b8` — recap independent per-cadence config (Option A, operator-chosen). RE-OPENS+SUPERSEDES the one-sub-per-user recap model.** mig 424 (above) re-keyed `user_kpi_recap_subs` UNIQUE(user_id_fk)→UNIQUE(user_id_fk,cadence), added `send_dow` (ISO 1-7, CHECK ukrs_send_dow_chk), created `user_recap_tracker_selections(user_id_fk,cadence,tracker_id_fk,position)` UNIQUE+CASCADE+schema_meta(class=reference), backfilled 112 rows from existing subscribers' dashboard selections (6 users). `app/kpi-recap-schedule.php` `kpi_recap_next_due` gained `?int $dow` (weekly walks to chosen ISO weekday). `scripts/send-kpi-recap.php`: sub SELECT +send_dow; widget source now `user_recap_tracker_selections WHERE user_id_fk AND cadence` (NOT dashboard selection); role-floor+data_ready+is_active gates preserved; empty selection skips send but advances next_due_at. `public/modules/mon-tableau.php` handler `update_recap_cadence`→`update_recap_config` (txn: per-cadence upsert + delete-then-insert tracker selections; security via mt_build_allowed_set + kpi_stub_domains); UI = three independent Daily/Weekly/Monthly blocks. Cron `db/cron/maltytask-kpi-recap.cron` UNCHANGED (hourly self-gating loop now iterates one row per (user,cadence)). LIVE-VALIDATED: user 1 daily full HTML digest (24 trackers); user 5 weekly-with-0-selections skips. → kpi-tracker-catalog/shipped-tranches.md §RECAP — INDEPENDENT PER-CADENCE CONFIG.
- **TOUR-STEWARD: NOT dispatched.** None of the four created/activated/renamed a ref_pages row (panels/features on existing pages + API endpoints); the tour gap-check keys on ref_pages, so no flag. RULE 3 satisfied.

### 2026-06-15 (PLANNING PAGE — Phase 2 dynamic eligibility SHIPPED+COMMITTED; mig 364; commit `da202a9`)
- **✅ PLANNING PAGE NOW FULLY LIVE — both phases.** Phase 1 (manual week calendar on `pl_plan_days`/`pl_plan_items`, mig 364) was already in place; Phase 2 (dynamic process-eligibility) shipped this commit. `da202a9` = 6 files / 2090 insertions.
- **DERIVATION POSITION:** Salle de contrôle / production-scheduling surface. Reads the SOT chain (yeast/garde eligibility, racking, commissioning_settings) to ADVISE the operator which processes are eligible per day; writes ONLY its own plan rows. Does NOT feed COGS/COP/stock/WAC/BOM/beer-tax. Eligibility computation is a pure projection over canonical facts.
- **NEW: `app/planning-eligibility.php` (405L)** — `planning_week_eligibility(PDO $pdo, DateTimeImmutable $weekStart): array`. 🔑 **CARDINAL RULE: pure-read — zero writes anywhere; only SELECTs on bd_fermenting_v2, bd_racking_v2, commissioning_settings, ref_recipes, pl_plan_items.** Algorithm = forward-replay: seed CCT/BBT working state from TankSimulator snapshot → load the week's `pl_plan_items` → walk 7 days, computing eligibility BEFORE applying each day's items, then apply them to mutate working state. In-plan items (even `hors_process=1`) propagate their occupancy effect — a planned racking unlocks packaging on SUBSEQUENT days (same-day gate stays correct: NOT unlocked same-day). Return shape `['YYYY-MM-DD' => ['racking'=>[],'kze'=>[],'dry_hopping'=>[],'packaging'=>[],'brewing'=>['cct_conflicts'=>[]]]]`. GATES: racking = cold-crash date + effective_garde days (resolved via `app/yeast-eligibility.php`); packaging = racked_on + min_days_after_racking (commissioning_settings section='packaging'); kze/dry_hopping = any occupied BBT/CCT.
- **NEW: `public/api/planning-eligibility.php` (34L)** — thin JSON endpoint. require_login() + require_page_access('planning'); reads `?week=YYYY-MM-DD` (read-default-then-validate), snaps to Monday; Cache-Control no-store; json_encode JSON_UNESCAPED_UNICODE | JSON_HEX_TAG | JSON_HEX_AMP.
- **`public/modules/planning.php` (Phase 1→2, 908L total):** requires planning-eligibility.php on GET; computes `$eligibility = planning_week_eligibility()` and hydrates `window.PLANNING_ELIGIBILITY` (JSON_HEX_TAG|JSON_HEX_AMP). Server-side eligibility re-checks on add_wort POST (non-brewing, !hors_process, recipe set → reject if not in eligible set) and add_packaging POST (same). Packaging form persists recipe_id_fk + bbt_number.
- **`public/js/planning.js` (190L):** `populateEligibleSelect()` from `window.PLANNING_ELIGIBILITY[date][process]`; `initWortProcessToggle()` repopulates on non-brewing process change; `__hors_process__` override auto-checks the hors_process checkbox; `initPackagingForm()` populates packaging recipe select + syncs bbt_number hidden field.
- **`public/css/planning.css` (455L):** Phase 2 styles `.pl-eligible-badge` / `.pl-ineligible-warning` / `.pl-elig-select--override` / `.pl-section--override-active`.
- **FUNCTIONAL VERIFICATION (before commit):** (1) Packaging 2026-06-16 → 8 BBTs eligible (Alternative/8 racked 06-15, min_days=1 → PASS). (2) Racking 2026-06-16 → Embuscade/235 (garde=8≥7 PASS), Embuscade/236 (1<7 correctly absent), Moonshine/124 (7=garde PASS). (3) In-plan forward propagation: inserted test racking EMB/236 planned 06-16 → appeared in packaging eligible 06-17 source=in_plan (PASS); same-day gate correct (NOT in packaging 06-16); test row deleted cleanly.
- **PRE-COMMIT REVIEWER BLOCKERS FIXED:** B1 lowerBound was inverted (used today instead of weekStart) → always weekStart. B2 API endpoint missing JSON_HEX_TAG|JSON_HEX_AMP → added. B3 packaging drain matched recipe_id alone → fixed to recipe_id+batch (honors the canonical (recipe_id_fk,batch) keying rule — sql skill #25/#27).
- 🔴 **ARCHITECTURE NOTE for next session:** `ref_pages.planning` is `is_active=0` (operator/general) — the page is NOT reachable by operators yet. Therefore NO tour-steward dispatch is due (RULE 3 predicate requires is_active=1). When the operator activates the page, dispatch the tour-steward and verify topbar visibility. Push-status of `da202a9` per shared-clone discipline — confirm with Kouros.

### 2026-06-15 (QA / HACCP QUALITY-CONTROLS — `/modules/qa.php` SHIPPED+LIVE; migs 358-361; commits LOCAL-only NOT pushed)
- **✅ FIRST REAL BUILD ON THE `qa` PAGE — SHIPPED+LIVE+SMOKE-PASSED.** `qa` was an inactive `ref_pages` placeholder (id 11) with pre-written tour copy; mig `361_activate_qa_page.sql` flipped `is_active=1, href='/modules/qa.php'`. All on PM-recommended defaults; operator granted "go for it".
- **DERIVATION POSITION:** Salle de contrôle / QA-QC family. 🔴 **NON-FISCAL observation layer — reads canonical production facts, writes only its own observation rows, NOTHING reads back into the SOT chain (NEVER feeds COGS/COP/stock/WAC/BOM/beer-tax).** Dropdowns derive BY FK from real canonical tables, never string-copy: net-content→`bd_packaging_v2` LEFT JOIN `ref_skus`; cleaning-efficacy→`bd_cip_events` JOIN `ref_cip_types`; bottle-reception→`inv_deliveries` (status not cancelled/tombstoned); packaging-MI→`ref_mi WHERE category_id=8` (Packaging, confirmed via `ref_mi_categories`).
- **MIGRATIONS (maltyweb, committed local, applied+VPS-verified):** `358_qa_net_content_readings.sql` / `359_qa_cleaning_efficacy_checks.sql` / `360_qa_bottle_reception_checks.sql` — 3 `source`-class / `corrections_policy='allowed'` tables, `schema_meta` rows written, `writer_script='/modules/qa.php'`. FK types verified live: packaging/cip/delivery = BIGINT UNSIGNED; mi/user = INT UNSIGNED. Commit `0b52f13`. + `361_activate_qa_page.sql` (`eac31da`). ⚠️ benign mig-number collision: a parallel session also created `361_ord_orders_bc_and_maltytask_source.sql` — migrate.php tracks by filename so both coexist; theirs to commit. (Live HEAD now 364 — parallel sessions also applied 357 + 362-364.)
- **HANDLERS (async JSON, mirror `rm-stocktake-line-add.php`):** `public/api/qa-net-content.php` / `qa-cleaning-efficacy.php` / `qa-bottle-reception.php`. Envelope `{ok:true,id,…}` / `{ok:false,error}` / `{ok:false,reason:'expired',csrf}` / duplicate→`{ok:true,duplicate:true}`. Each: csrf_verify + `require_page_access('qa')` + audit revision + row_hash idempotency + read-`??`-then-validate + ENUM whitelist + FK-existence checks. Shared `parse_nullable_decimal()` added to EXISTING `app/db-write-helpers.php` — single canonical def, NOT copied (honors call-the-accessor rule). Commits `3ede620` → `01728e2` (PRG→JSON refactor).
- **UI:** `public/modules/qa.php` (747L) + `public/css/qa.css` (297L) + `public/js/qa.js` (314L) — 3 stacked panels, optional `?view=net|cip|recep`, async fetch + DOM prepend, escHtml, external CSS/JS (no inline). Commit `8734849`.
- **TOUR:** card for `qa` shipped by tour-steward (`f8478da`; + bonus journal-saisies card). Tour gap CRITICAL 2→1; remaining critical = `financier` (steward correctly flagged PM-RATIFY sensitive-COGS class → belongs to COGS-fiche arc, NOT this build).
- **SMOKE (webapp-testing PASSED):** all panels render; dropdowns A:50 / B:30 / C:50,125; 3 write round-trips OK; bad-enum rejected (400+JSON, NOT 500); test rows self-cleaned COUNT=0; test acct id=16 elevated→reverted.
- **DESIGN DEFAULTS LOCKED (non-destructive, operator may revisit):** weight=g / volume=mL via single `measured_value` + `measure_type` discriminator; `surface_label` free-text (no `ref_qa_surfaces` lookup); per-reading `target_value`+`tolerance_abs` (no central `qa_targets` master); `is_conforming` derived-at-write.
- **STATUS:** maltytask+maltyweb commits LOCAL-only NOT pushed (cage-session commits interleaved in shared clone — pushing would carry their in-flight work; deferred to Kouros). Work fully LIVE on VPS.
- **OPEN/FOLLOW-UP:** (1) panel-C MI dropdown shows whole Packaging cat (125) — tighten to glass/bottle if operator asks; (2) Feature A inline capture on conditionnement fiche (Q3 phase-2) NOT built — standalone qa-page only; (3) `ref_qa_surfaces` lookup + central `qa_targets` master = future normalization; (4) `financier` tour card still open (its arc owns it). → qa-haccp-controls-arc.md.

