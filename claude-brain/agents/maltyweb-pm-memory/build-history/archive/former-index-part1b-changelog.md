# Former-index archive — PART 1: rolling "Last updated" changelog (block B)

> Verbatim pre-2026-06-05 index content; archival, superseded by thematic topic files. Each block below is a single monolithic former-index "Last updated" line, preserved verbatim. Parent: [../README.md](../README.md).

> Last updated: 2026-05-27o (**PHASE D CORE COMPLETE & COMMITTED (maltytask repo — NOT maltyweb).** Four commits: `2c2426e` gate diagnostics (the corrected harness; the +564k RM swing decomposed → it was the g→kg conversion bug, NOT a revaluation) → `31af4a9` **Commit A** RM inventory valuation → MySQL HARD CUTOVER (NEW `lib/rm-stock-mysql.js` JS bridge `inv_rm_stocktake → ref_mi → v_mi_cost`; `valueCHF = finalQty × conversion_factor × cost_chf`; no_basis NULL preserved → 0+flagged at consumer; closePool wired into 4 builders, was a hang risk). **April RM VERIFIED 333,561 → 287,235 (−46,327, ENTIRELY conversion correction; no_basis-zeroing 0.00).** byAccount: Hops 34,277→36 / Ingredients 14,148→4,892 / Yeast 3,069→566 (the g→kg fix); Bouteilles/Cartons ±0.5–7% catalog→WAC drift. → `b7bdab9` **Commit B** COGS/BOM cost source → `v_mi_cost` via OPT-IN `loadMI({mysqlCost:true})` overlay (cache-isolated `_cached`/`_cachedMysql`; sets price=cost_chf + currency='CHF' [the FX lever] + conversion=ref_mi.conversion_factor; `createPriceResolver` auto-detects + bypasses weighted-prices.json). **Step 4 FOLDED IN:** `compute-weighted-prices --write-master` BSF writeback RETIRED; `build-cogs.js` HARD-DEPRECATED (process.exit(1), BSF-catalog builder, not in run-month-close). Cost-neutral <CHF 2.50/SKU; **MAIN movement = EUR-fix** (17 EUR MIs were multiplied RAW in build-sku-bom = latent under-costing → now correctly CHF, the §COMMIT-B Q4 predicted-correct delta). **COP indirect UNAFFECTED** — cop-indirect.js uses actual spend TOTAL_CHF not MI.price (so the SALE_CO2_BOUT=550 ref_mi oddity does NOT reach COGS — confirmed). → `483467e` **can-cost fix** (separate commit, own sign-off): `getContainerName` used `masterByName.get(name.toLowerCase())` but byName keys are normalize()'d (parens stripped) → `"Can ZEP (printed)"` never matched → ALL cans fell to a PHANTOM `"Can Ardagh 50cl"` fallback → 0 can cost. Fixed to `findIngredient()`. 19 canned SKUs +0.285 CHF/can (printed cans 0.28539 WAC); MOO4C 4.59→11.44 (+149%). Canned-product COGS had been understated by the WHOLE can. **ALL signed off.** Phase-D core (RM valuation + COGS cost source → canonical MySQL `v_mi_cost`, conversion-correct) DONE; no COGS-moving change pending. **8-ITEM FOLLOW-UP BACKLOG recorded (operator-paced, none blocking)** → phase-d-cost-source-swap.md §FOLLOW-UP BACKLOG: (1) DIV/BLO/EPH1-4 printed cans still no_basis → 0 can cost (need a price/delivery — surfaced by the can-fix); (2) weighted-prices.json ORPHANED (createPriceResolver bypasses it; compute-weighted-prices still emits but nothing reads — RULE-1 scrap; verify the script's WAC computation isn't fully dead before scrapping); (3) eurBase / sku-cost-dashboard EUR-rate SLIDER redundant under the overlay (costs are CHF) — RULE-1 scrap; (4) warehouse.php g→kg conversion bug (operator-confirmed backlog, scrapping #15); (5) lib/repos/mysql-inventory.ts dormant half-cutover (DO-NOT-WIRE-AS-IS — BSF cost + old conversion bug; adopt v_mi_cost+conversion when pipeline→TS, or delete); (6) SALE_CO2_BOUT=550 CHF/L ref_mi = wrong catalog (tank price on bottle MI; harmless to COGS, data error someday); (7) the ~32 no_basis MIs valued-0+flagged → flip the STRICT-FLIP seam (build-sku-bom if(price>0) guards + findPrice → emit gap rows) once operator clears the gap set; (8) **BSF-EXIT NOT FINISHED — A+B exited only the COST+RM leg; FG (loadFG StockTake+SKU_BOM), WIP (loadWIP Tank_Balances), Sales, ChargesBC, Consumption_Parsed, BeerTypes, Deliveries-tab are STILL BSF reads = the biggest remaining architectural arc.** Full as-built → phase-d-cost-source-swap.md §COMMIT A / §COMMIT B / §CAN-COST FIX / §FOLLOW-UP BACKLOG. | Prior 2026-05-27n: **PHASE-D STEP-3 GATE SIGNED OFF + SCOPE LOCKED** — see RESUME POINT. Operator: apply April-2026 ONWARD / warehouse.php conversion bug → BACKLOG / "completely out of BSF" directive. Scope = Commit A (RM valuation fully onto MySQL, hard cutover, 334,319→288,101) + Commit B (COGS/BOM cost swap, cost-neutral), each its own diff+sign-off, A then B. Claim-5 CASSONADE/SUGAR/STICKER anomalies are BSF-ONLY (MySQL ref_mi already cf=1.0) → no hygiene migration. **BACKLOG: warehouse.php/warehouse-export.php g→kg conversion bug (~1000× inflated stock display) — logged, NOT this session.** Full → phase-d-cost-source-swap.md §STEP-3 GATE. | Prior 2026-05-27m: (**RACKING KPI SECTION + KZE FORM ADDITION — LANDED + DEPLOYED + COMMITTED + PM-RECORDED.** TWO commits on `main`: `c38cea1` feat(fermentation): racking/transferts KPI section on `public/modules/tanks.php` (+ `public/css/app.css`) — built EXACTLY per plan: **operator strip** (racks + HL last-30d, racks this ISO week) + **per-beer historical table** (`?rack_year=` filter; volume racked, # racks, avg post-rack CO₂/O₂, CSS volume bars), **Nébuleuse vs contract kept SEPARATE** (never summed). Reads `bd_racking_v2` directly (`is_tombstoned=0`), beer via recipe FK join. **`avg_speed` + destination-split EXCLUDED as advised.** No JS/chart-lib/migration — server-rendered like ferm-stats. LIVE-VERIFIED: last-30d 14 racks / 1295.8 HL; total 399 racks / 33,233.6 HL (matches). `b40e73f` feat(transferts): **KZE flash-pasteurisation section on `form-racking.php` + migration 180** (`bd_racking_v2.kze_target_pu`, `kze_avg_pu` DECIMAL(6,2)) — when KZE in the CIP set (individual KZE OR Centri+KZE simultané), form reveals **Target PU + Average PU** (JS-toggled, required-on-visible, NO hidden-required deadlock; in row + hash basis). **MODEL = 1-to-1 scalar cols on `bd_racking_v2`, NOT a child table** (PU is one-per-rack scalar, not 1-to-many like CIP; correct — no parallel-store smell; rack-EVENT pasteurisation outcome is distinct from the KZE *CIP* clean which stays in `bd_cip_events`). **AUTONOMY:** operator lost internet mid-session having granted "use all agents/skills"; agent proceeded with the 5 recommended KPI defaults rather than block — read-only + trivially iterable, operator can redirect the headline-KPI set on return WITHOUT rebuild. **DATA ANOMALY (backlog/RQ candidate, NOT corrected — aggregate-as-is per spec): Diversion CO₂/O₂ outliers — avg CO₂ 113.97 g/L + avg O₂ 13,076 ppb**, consistent w/ the known Diversion gravity-scale anomaly; operator may investigate those `bd_racking_v2` Diversion rows. **migrate.php now at 180.** STILL OPEN (carried): logged-in browser walkthrough of forms + new KPI section; strain classification keeping the transfer gate Zepp-only; the 3 NULL-destination Diversion rows (data-quality, ids 60/66/278). Full as-built → racking-kpi-dashboard.md §AS-BUILT + §KZE. | Prior 2026-05-27l: **SALLE DES MACHINES BLANK-PAGE — RESOLVED + DEPLOYED.** Root cause was NOT the recolour `3c57e5b` (ruled out) — it was the nav refactor `9e87a3f` ("drop sidebar"): full-width `.main` let global `app.css:669 .home .main { align-items:center }` leak into the SdM full-bleed `.sdm-stage{width:400%}` sliding-stage, centering it off-screen to x=−2230px → blank w/ ZERO JS/PHP errors. FIX (deployed, Playwright-verified; NOT committed — waiting on Kouros): `salle-des-machines.css body.salle-des-machines .main += align-items:stretch`. Bonus same pass: fixed `salle-des-machines.php` L346 `array_unique` over assoc-array ROWS → "Array to string" Warning + always-1 stat; "Contenants actifs" now correct 5 formats. **DURABLE ANTI-PATTERN → conventions-and-helpers.md §UI/UX + `ui` skill: post-drop-sidebar, ANY full-bleed/sliding-stage page whose `.main` holds a `width>100%` flex child MUST set `align-items:stretch`/flex-start or it centers off-screen → blank; + a clean-rendering blank page is a LAYOUT bug (measure element rect), not a JS throw.** | Prior 2026-05-27k: **PHASE D STEP-3 PREREQUISITE RULED — inventory.js input_unit→pricing_unit CONVERSION GAP (load-bearing cost-model correction).** The Step-3 read-only harness's +CHF 564k/+169% RM swing is NOT a revaluation — it's a unit-conversion-application gap: `lib/inventory.js` parseRMRows L96 values RM `qty×priceCHF` with ZERO conversion (multiplies gram-qty × per-kg-price), while `build-cogs.js` L66/L254 correctly applies `qty×conversion×price`. Masked today on yeast only (BSF per-gram residual); migs 171/172's yeast per-kg correction REMOVES the masking → conversion fix is a HARD PREREQUISITE of the cost swap (else yeast explodes 1000×). RULINGS: **conversion source = `ref_mi.conversion_factor` (MySQL, option-(c)-ALREADY-SHIPPED)** — the finding's "no conversion column" was a FALSE NEGATIVE (col is `conversion_factor` DECIMAL(14,6) + `density_g_per_ml`, BOTH live + populated, 0 NULLs: g→kg 68@0.001, ml→kg 1@0.001+density, unit/kg/L all 1.0); reject BSF-read (dying) + reject string-derive (mis-handles ml→mass + parallel-store). Wire `conversionFactor` onto the MI entry via the SAME Phase-D loadMI→MySQL bridge. **Sequencing = SEPARATE PREREQUISITE COMMIT with its own RM diff BEFORE the cost swap** (keeps the +564k decomposable: conversion-fix delta vs real WAC delta on 96 MIs; folding them defeats the Step-3 sign-off gate). **FG OK** (skuCost×qty, BOM converts), **WIP IMMUNE** (hl×costPerHL, no MI qty); fix touches ONLY parseRMRows L96. **LIVE OPERATOR BUG flagged separate ticket (NOT this session):** `warehouse.php`/`warehouse-export.php` value `COALESCE(wac_snapshots.wac_chf, ref_mi.price×fx)×qty` with no conversion_factor → ~1000× inflated stock display for every g/kg MI right now; same canonical-source fix, keep on snapshot surface, outside racking/CIP locked files. Full ruling → phase-d-cost-source-swap.md §STEP-3 PREREQUISITE. | Prior 2026-05-27j: **SAISIE TRANSFERTS ROUND 2 + 3-FORM CIP CONVERGENCE FULLY LANDED + DEPLOYED + PM-VERIFIED LIVE.** 4 commits on `main`: `8c53e98` (CIP schema migs 175/176/177 + shared `app/cip-events.php` writer + `app/partials/cip-section.php` + css), `36b72c5` (Salle-de-contrôle editable CIP-types list-CRUD), `b269eee` (Transferts round-2 = 10 operator points + CIP consume + mig 179 `bd_racking_v2.yt_number`), `592641d` (brewing+packaging converge). `bd_cip_events` = SINGLE CIP store for ALL 3 forms; all forms STOPPED writing flat CIP cols + removed them from `bd_*_v2` hash basis (no drift); flat cols kept for legacy Sheets-ingest (scrapping #14). Atomic idempotent `cip_upsert` writer (4 review bugs fixed pre-consume). LIVE: ref_cip_types 4 rows, backfill racking 679 + brewing 412 + packaging 0, all times NULL (backfill-only). 🔑 REUSABLE ENTRY POINT for next CIP-on-a-form: `cip-section.php` ($cipConfig) + `cip_upsert()` after `bd_upsert` + add source_form ENUM + typed FK; do NOT re-model. cip-module.md = AS-BUILT/LIVE. Backlog: 3 Diversion racking rows (60/66/278) NULL dest_type excluded from backfill (data-quality/RQ candidate); browser walkthrough pending all 3 forms; Zepp-only time-gate until ~76 recipes classified. Prior 2026-05-27i: **PHASE D STEP 1 LANDED — migration 178 `v_mi_cost_resolver` committed `04b4d50` + applied + PM-verified live: `ref_fx` (CHF=1.0/EUR=0.945, replaces hardcoded 0.945, operator-editable) + `v_mi_cost` (thin per-MI resolver: wac > catalog[`price IS NOT NULL`, incl 0 = valid free cost] > no_basis[`price IS NULL` → NULL, never 0]; live 367 MIs = wac 96/catalog 141/no_basis 130) + `v_mi_cost_gap` (diagnostic, not the RQ: no_basis MIs in an active BOM OR RM stock = 32 rows). Resolver bug caught+fixed pre-commit: `COALESCE(price,0)>0` mis-classified stored price=0 free-samples as no_basis → fixed to `price IS NOT NULL`, 178 re-applied idempotent. GAP ANALYSIS: the 32 gap MIs have NO BSF price either (14 price=0, 18 absent) → the swap is COST-NEUTRAL on them; the gap view just makes today's silent-0 visible. OPERATOR = SOFT ROLLOUT (swap MySQL cost-neutral now; no_basis-in-BOM values at 0 + tracked via gap view; clear gaps at operator pace THEN flip build guard to strict-fail). Only the 96 WAC MIs MOVE COGS (vs old BSF MI.price) → gated behind before/after diff + sign-off. **STEP 2b DONE + COMMITTED (maltytask `a3bc9ed`): lib/repos/mysql-wac.ts loadMiCost() over v_mi_cost, 367 rows/0 errors, resolveCostChf() w/ no_basis, NO consumer yet. STEP 2a DEFERRED (CORRECTION, file-verified 2026-05-27): warehouse.php/warehouse-export.php read PERIOD-FROZEN wac_snapshots (LEFT JOIN wac_snapshots … period=MAX(period), COALESCE(w.wac_chf, m.price×FX)) — NOT live v_mi_wac. TWO WAC SURFACES BY DESIGN: v_mi_wac LIVE→COGS-via-v_mi_cost vs wac_snapshots FROZEN→warehouse display (differ — 7 MIs, e.g. HOPS_ELDORADO snap 19.75/live 13.85); refactoring warehouse→v_mi_cost would silently flip snapshot→live = behavior change, NOT identity refactor → backlog/operator product decision, NOT a blocker; don't "fix" warehouse to live without a decision (coherent split: period-anchored value vs real-time cost). STEP 3 BASIS CONFIRMED: today's COGS uses LIVE WAC → v_mi_cost is the correct swap source.** Step 3 = port lib/mi.js loadMI→MySQL (bridge) + rewrite build-sku-bom.js→TS + ⚠️ FX-careful (inventory.js L96 self-converts NATIVE vs build-sku-bom.js L273-400 raw — cost_chf can't drop into MI.price blindly, would double-convert) + diff + sign-off. Step 4 = retire BSF writeback. inventory.js + ~6 callers STAY JS (D.2). STAY CLEAR of racking agent's migs 175-177 / salle-de-controle.* / cip-*. Full arc → phase-d-cost-source-swap.md. | Prior 2026-05-27h: **SAISIE TRANSFERTS — FULLY LANDED + DEPLOYED + PM-VERIFIED LIVE 2026-05-27. ALL 3 PHASES DONE.** Two commits on `main`: `2d131de` feat(salle-de-controle): Biochimie family defaults + per-recipe yeast editor (`salle-de-controle.php` + `.css` + NEW `app/yeast-eligibility.php` shared resolver); `fa6221c` feat(transferts): rename Soutirage→Transferts + state-gated eligible-beer cards (`form-racking.php` + `saisies.php` + `racking-form.css/js` + mig **174** `bd_racking_v2_hors_process`). [Phase-1 yeast migs 166/167/168 were committed earlier `ed8e7c6`.] **OCCUPANCY RULING APPLIED EXACTLY as I ruled — TankSimulator (`app/tank-simulator.php`) is the occupancy authority on BOTH candidate paths (mirrors form-packaging L492/L495-524), NOT pure SQL.** Gated path = SQL(brew+coldcrash+no-later-brew) + garde HAVING + `NOT EXISTS(bd_racking_v2 rk ON neb_recipe_id_fk/neb_batch, is_tombstoned=0)` pre-filter → sim-filter. Hors-process path = SQL (no HAVING / no EXISTS) → sim-filter (relaxes the TIME gate, not physical reality). **LIVE-PROVEN: gated returns the 3 un-racked Zepp lots (CCTs 12/14/16, garde=14); sim-filter correctly drops 3 already-racked lots (Alternative b7→BBT1, Embuscade b233→BBT7, Moonshine b122→BBT6; hors-process 18→15). bd_racking_v2 = DESTINATION-only, never an occupancy source.** **PM-VERIFIED LIVE read-only 2026-05-27: mig 174 applied (`migrate.php` 162-174 all applied, 0 pending); recipe 57 Zepp → strain id=2 (W34/70, family='lager', type still 'unknown' — orthogonal, untouched), all 4 overrides NULL (inherits lager garde=14/9-13); ref_yeast_family_defaults = ale 7/18-22, lager 14/9-13, non_alcool 7/18-22 prod=1, spontane+mixte garde=NULL prod=0; bd_racking_v2.hors_process_flag tinyint(1) NOT NULL default 0 + hors_process_reason varchar(255) NULL.** **DATA STATE in prod (intentional, NOT a fixture): recipe 57 (Zepp) is the ONLY classified+linked recipe → it is the ONLY time-gated beer; all other ~76 recipes unclassified → route through hors-process until the operator classifies them via the Recettes→"Levure & garde" editor.** **NOT browser-walked yet** (verification was server-side: SQL live proofs + php -l + deploy + auth-redirect); operator may do a logged-in UI pass. **BACKLOG carried (gated on real need, refuse-don't-guess): (1) strain classification + recipe→strain linking for the other ~76 recipes — OPERATOR DATA ENTRY via the new editor, NOT a code task (optional operator-validated bulk seed migration if wanted); (2) CCT→CCT blend occupancy — out of scope, needs deliberate model extension (source-CCT column + sim CCT-as-destination), code comment left in form-racking.php; (3) SCD2 v2 coupling — when activated, `yeast_strain_id_fk` + the 3 override columns MUST join the version-stamped set or re-versioning loses/retro-applies overrides against historical racking eligibility.** Full as-built + occupancy ruling + verdicts → saisie-transferts-and-yeast-family.md. | Prior 2026-05-27g: **SAISIE TRANSFERTS — PHASE 1 (schema) + PHASE 2a (Biochimie page) LANDED + PM-VERIFIED LIVE; PHASE 2b IN FLIGHT.** Migrations **166/167/168** (yeast-family series, committed `ed8e7c6`) applied + PM-verified read-only: 166 `ref_yeast_strains.family` ENUM (all 30 family=NULL intentional); 167 `ref_yeast_family_defaults` seeded ale=7/18-22 + lager=14/9-13 + non_alcool=7/18-22 prod=1, spontane+mixte garde=NULL prod=0, schema_meta row class=reference; 168 `ref_recipes` + yeast_strain_id_fk(int unsigned FK→strains.id RESTRICT)+garde_days_min_override(int unsigned)+ferm_temp_min/max_override DECIMAL(4,1), NO garde-max, 0 recipes linked yet (77 active recipes / 29 active strains). Phase 2a = `salle-de-controle.php` #sec-biochem reads/edits the defaults table (5 cards incl Non-Alcool, garde-max dropped), new admin-gated `update_yeast_family` POST (two-step validate + CSRF + PRG + log_revision→audit_row_revisions); dead onBiochemBlur removed (YEAST_DEFAULTS JS const survives — feeds the non-persisting recipe-CREATION mock, expected). **PM DECISION-CHECK on the 3 in-flight decisions: (1) build per-recipe yeast editor now = SOUND; (2) strain→family classify INLINE on the recipe editor = APPROVED, caveat the write MUST target `ref_yeast_strains.family` (canonical owner) never a per-recipe copy [broken-FK anti-pattern], no separate strains page needed; (3) `app/yeast-eligibility.php` shared resolver + NULL-garde = not-time-gate-eligible (hors-process only, no invented floor) = CONFIRMED — supersedes the design's old fallback-floor-7. NO red flags on Phase 3 plan (form-racking label-rename + cold-crash/CCT gate consuming the resolver + hors-process toggle mirroring form-packaging).** GOVERNANCE: yeast 166/167/168 NOW committed `ed8e7c6` (no fresh-VPS drift). Full as-built + verdicts → saisie-transferts-and-yeast-family.md §AS-BUILT + §DECISION-CHECK. | Prior 2026-05-27f: **CATALOG-HYGIENE ROUND 2 + GOVERNANCE LOOSE-END BOTH CLOSED.** Migrations **172** (SAFALE_BE134 id=64 → 125.40 EUR/kg [was 62.70, per-0.5kg-brick mislabelled per-kg]; SIMCOE id=46 → last-purchase 28.32 EUR/kg [was stale 16.1327, matched no purchase]) **+ 173** (LOG_SCOTCH_ESHOP id=201 2.50 → 3.24 CHF/unit, operator-resolved the same-day 3.24-vs-5.06 ambiguity in favour of 3.24, the 36-unit main line) APPLIED to live DB + COMMITTED `6098585` (2026-05-27). A catalog-vs-WAC divergence scan (catalog_chf vs v_mi_wac.wac_chf, flag outside [0.67,1.5]×) found EXACTLY these 3 residuals beyond 171; all corrected to **last-purchase in NATIVE currency** (accountant-ruled basis). **PM-VERIFIED LIVE read-only: id=46 SIMCOE 28.32 EUR/kg, id=64 BE134 125.40 EUR/kg, id=201 LOG_SCOTCH 3.24 CHF/unit; migrate.php = 162-173 all applied; v_mi_wac unchanged 96 rows / 0 unresolved (catalog edits never touch the view); ZERO WAC/COP impact.** **GOVERNANCE LOOSE-END CLOSED:** the yeast migs 166/167/168 (applied-on-VPS-but-untracked) are now committed `ed8e7c6` ("chore(db): recover applied-but-uncommitted yeast-family migrations") BEFORE the 172/173 commit — a fresh-VPS-from-git no longer drifts. **KEY REFRAME (durable, recorded): catalog price (ref_mi.price) is ONLY the Phase-D `COALESCE(WAC, catalog)` fallback for MIs with NO Active deliveries — every delivery-backed MI is WAC-valued, so a stale catalog on a delivery-backed MI is HYGIENE, not a live COGS bomb. The scan is now CLEAN within [0.67,1.5]× of WAC for ALL delivery-backed MIs (both my flagged Phase-D gate items — SAFALE_BE134 and ALIGAL2_LIQ [171 fixed 550→0.37] — DONE).** **The genuine remaining Phase-D fallback risk = the NO-DELIVERY inventoried MI catalog set** (POMONA fk238 left at 0.233 EUR — no purchase history → no last-purchase basis; PM-verified MANY no-delivery inventoried MIs carry NULL/blank price + NULL currency too: id=2 MALT_PALE_ALE, id=5 MALT_VIENNA, numerous HOPS). Can't be auto-derived (refuse-don't-guess) → needs manual/historical values OR an explicit accept-WAC-only-with-sane-default decision AT Phase-D wiring time. NOT a blocker for WAC; the last catalog-gate consideration before Phase-D reads catalog as a fallback. Detail → billing-format-and-wac-unit-model.md §CATALOG-HYGIENE ROUND 2 + §GOVERNANCE (resolved) + §CATALOG-PRICE BASIS. | Prior 2026-05-27e: **WAC-HYGIENE BATCH APPLIED + VERIFIED + COMMITTED `930e09a` — BOTH standing wac_unresolved flags CLOSED.** The 3 migrations I specced as 166/167/168 SHIPPED AS **169/170/171** — RENUMBER: a parallel yeast-family series (166_yeast_strains_add_family / 167_ref_yeast_family_defaults / 168_ref_recipes_yeast_link) had already claimed 166-168 and was applied on the VPS; collision caught at apply-time before any clash wrote. **169 (A1):** ref_mi fk591 PROC_ALIGAL2_ECO is_inventoried 1→0 + pricing_unit NULL→'unit'; DELETEd stray wac_snapshot id=455; delivery id=445 (41.55 CHF) KEPT intact (GL 4300, still in COP); CO2 fk197 WAC UNCHANGED 0.384488. **170 (A2):** CREATE OR REPLACE v_mi_wac = mig-163 body + NULL-safe `AND (pricing_unit IS NULL OR pricing_unit<>'month')`; schema_meta writer bumped. **171 (A3 catalog):** ref_mi.price → last-purchase NATIVE currency — US05 fk65 109.00 EUR, W3470 fk66 179.40 EUR, FARMHOUSE fk194 221.60 EUR, VERDANT fk196 197.80 EUR, CO2 fk197 550→0.37 CHF; POMONA fk238 LEFT at 0.233 (no purchase history → refuse-don't-guess). **POST-APPLY VERIFIED read-only: v_mi_wac 98→96; wac_unresolved 2→0 (BOTH flags closed); CO2 WAC unchanged; delivery 445 + GL 4300 intact; snapshot 455 gone. migrate.php --status = 169/170/171 applied, 0 pending. None needed a snapshot recompute.** **DURABLE CONVENTIONS recorded (accountant-ruled): (1) fixed-per-delivery levy = non-inventoried own-line, NOT folded (contrast VOC which folds; sits alongside VOC-folds/RPLP-separate/TEA-4201); (2) catalog price (ref_mi.price) basis = LAST-PURCHASE in NATIVE currency, FX at point of use; (3) v_mi_wac excludes pricing_unit='month' — COP is GL/category-driven, NOT WAC-driven.** **TWO LOOSE ENDS TRACKED (not resolved): (a) GOVERNANCE — yeast migs 166/167/168 are APPLIED on VPS but UNTRACKED in local git (HEAD 930e09a lacks them); a fresh VPS rebuilt from git would lack them; whoever owns yeast-family work must commit them — flagged to operator; (b) future fixed-ECO ingest path — when Carbagas bills the eco-fee as flat ~50 CHF/delivery, the carbagas parser must map it to fk591 (now is_inventoried=0) as a fixed-fee line; log-don't-build, revisit on first fixed-fee invoice. POMONA fk238 catalog left at 0.233 pending first delivery = not a bug, just no basis yet.** Full as-built + COP-routing proof + conventions → billing-format-and-wac-unit-model.md §WAC-HYGIENE BATCH / §GOVERNANCE LOOSE-END / §CATALOG-PRICE BASIS / §ECO-FEE CONVENTION. | Prior 2026-05-27d: **MIGRATION 165 APPLIED + VERIFIED + COMMITTED `1d670d3` — the live 2× yeast WAC bug is CLOSED.** US05 (del 426/fk65) + W3470 (del 425/fk66) re-modelled to FULL qty: qty_delivered 0.5→1.0, unit_price→109.00/179.40 EUR/kg, qty_remaining stays 0.5, total_original/total_chf untouched. POST-APPLY VERIFIED (matched predictions exactly): v_mi_wac US05 206.010→**103.005**/kg, W3470 339.066→**169.533**/kg; inventory value halved (51.5025 / 84.7665); wac_snapshots id=448/447 recomputed with new hashes `0314ff80…`/`fcd6c532…`; MI count stays **98**. FARMHOUSE id=427 (1 pack confirmed) + VERDANT id=429 untouched. The auditor report `reports/wac-silent-unit-audit-2026-05-27.md` committed in the SAME commit. `migrate.php --status` = **162/163/164/165 all applied, 0 pending** (mig-164 PEACH_TEA+MOSAIC was already applied+committed `5de9fd6` — last night's "164 unapplied" worry was a false alarm; --status is the source of truth). RESUME POINT CLEARED. PARKED (NOT done, roadmap): PROC_ALIGAL2_ECO id=445 is_inventoried fee-vs-item (1 of 2 standing wac_unresolved); 4-yeast bucket-D catalog-price hygiene (US05/W3470/VERDANT/FARMHOUSE ref_mi.price g→kg residuals — no WAC impact, must land as its own migration BEFORE any Phase-D COALESCE(WAC,catalog)); band-check method note (exclude pricing_unit='month' MIs); consumption→WAC/RM wiring at step 6b (gated behind clean unit-normalization + is_inventoried reconciliation). | Prior 2026-05-27b: **SILENT-UNIT LESSON RECORDED + AUDITOR MANDATE ISSUED + CONSUMPTION-WIRING task logged.** (1) The silent-unit-error class with the Mosaic "expensive ≠ different product" insight is now in billing-format-and-wac-unit-model.md §silent-unit class (qty stored as pack/sachet/100g COUNT with compensating price → invariant HOLDS → invisible to Pass-1; detect via plausibility band / catalog-divergence / read-the-invoice; RULE = unit-bug suspect FIRST, product-difference SECOND; PM correctly HELD the Cryo reassignment on a contradicted invoice premise). (2) AUDITOR MANDATE for a read-only Sonnet sql+coder sweep of ALL inv_deliveries rows — method (plausibility bands + catalog-divergence vs FX baseline {0.945 EUR / 1.000 CHF} + qty-shape heuristics + READ source-invoice OCR for every candidate), guardrails (read-only, candidate-corrections report only, no migration, no auto-MI, flag product-vs-unit ambiguities as operator Qs, no helper scripts), output = dated report in reports/; PM triages → validated set → migration. LIVE BANDS recorded per category (Malt kg 0.47-0.93, Hops kg 7.6-242.9, Yeast kg 118-339, etc.); OCR join path verified (inv_deliveries.file_id_fk → doc_files.id = doc_invoices.file_id → doc_invoices.ocr_text; 100% coverage on 185 Active inventoried rows); 21-row small-int-qty suspect set identified (most legit; id=104 MOSAIC the one confirmed bug, ABBAYE the confirmed non-bug). (3) CONSUMPTION-WIRING into WAC/RM logged in forward sequence at step 6b — RM value + consumption valued at WAC; on-hand = deliveries − consumption; DEPENDS ON unit-normalization clean + is_inventoried scope reconciled. | Prior: **v_mi_wac PRESSURE TEST = 98/98 reconcile (model CORRECT) + MIGRATION 164 SPEC WRITTEN (propose-only, not applied).** `db/migrations/164_wac_data_corrections_peachtea_mosaic.sql` written to the repo — TWO operator-confirmed delivery-row silent-unit fixes verified read-only against live: (1) **ADJ_PEACH_TEA** `inv_deliveries.id=249` qty 320→32 kg / qty_remaining 284→28.4 / unit_price 4.99→49.90/kg (was billed per-100g; total_original 1596.80 unchanged) → WAC 4.99→**49.90/kg = the catalog price** (the earlier "catalog ×10 stale" flag was WRONG — catalog was right, the delivery row was the ×10-low one); snapshot id=425 recompute to 49.90/qty_at_close 28.4 with new row_hash `58f8447f…` (recipe re-verified live). (2) **HOPS_MOSAIC** `inv_deliveries.id=104` qty 2→10 kg / unit_price 145→29/kg (invoice 8389 = "Sachet **5 kg**" → 2×5kg, total 290 EUR unchanged) → fk=40 WAC 11.517→**11.367/kg**. **The operator's "reclassify id=104 to HOPS_MOSAIC_CRYO" request is HELD: the source invoice CONTRADICTS the Cryo premise (plain Mosaic pellet @ commodity 29/kg, no Cryo/LupuLN2 anywhere) → reassignment commented-out behind an operator-gate banner; `HOPS_MOSAIC_CRYO` ALREADY EXISTS (ref_mi.id=54) so NO new-MI INSERT was needed either way.** Mosaic touches NO wac_snapshot (fk=40 & fk=54 have none). +2 `ref_mi_invoicing_units` seeds (PEACH_TEA pack/0.1kg; MOSAIC pack/5kg supplier-19). v_mi_wac row count stays **98** in the recommended no-reassign path. Migration idiom = 162; idempotent WHERE-guards + rollback; no schema change. Detail → billing-format-and-wac-unit-model.md §MIGRATION 164. | Prior: **v_mi_wac WAC VIEW — LANDED + VERIFIED + COMMITTED + PUSHED.** Migrations **162** (data fixes: unit-normalize 8 delivery rows + 5 yeast MIs→`pricing_unit=kg`) + **163** (`v_mi_wac` view) both applied to the VPS via `migrate.php` (both OK), committed `d8c004d` ("feat(wac): unit-normalize deliveries + add v_mi_wac view"), pushed origin/main; audit reports (billing-format-audit + wac-unit-truth-dba) in a follow-up docs commit. **GATE PASSED LIVE, READ-ONLY:** all 8 touched delivery rows invariant `qty×price=total_original` PASS; all 5 yeast MIs (US05/W3470/FARMHOUSE/VERDANT/POMONA) now `pricing_unit=kg`; `v_mi_wac` = **98 rows, 2 flagged** (PKG_TEA_BOT_CH pu=month + PROC_ALIGAL2_ECO pu=NULL — exactly as predicted by the audit). **WAC values reconcile live:** MUNICH 0.481005/kg, US05 206.010, W3470 339.066, GALAXY 33.9255, WATER_DEMIN 0.6246/L, SIMCOE 24.997304 (sc=2), MOSAIC 11.516701 (sc=2 multi-supplier across ingredient_fk), FARMHOUSE 209.412, VERDANT 186.921. 2026-04 `wac_snapshots`: 6 rows recomputed, `replay_source=current_approximation`. **STILL-PENDING carried forward (do NOT lose):** (a) MUNICH `inv_deliveries.id=21` `qty_remaining=26` vs `qty_delivered=26000` — operator-gated inventory-VALUE question (rate correct; only stock value affected); (b) `ref_mi_invoicing_units` completion for the ~84 inventoried MIs still lacking a row (this build seeded touched-only); (c) catalog-price (`ref_mi.price`) hygiene — ALIGAL2 ×1400, PEACH_TEA ×10, FARMHOUSE ×11.5 stale — BEFORE any Phase-D `COALESCE(WAC, catalog)` wiring; (d) **Phase D** = wire COGS/COP + `ref_mi.price` writes to `v_mi_wac` (MIND the `is_inventoried` scope divergence vs the snapshot writer). Detail → billing-format-and-wac-unit-model.md. | Prior: **LE ZEPPELIN HUB — LANDED + DEPLOYED + VERIFIED. TWO commits pushed origin/main, deployed via `bin/deploy.sh --apply`, PM-verified on the VPS:** `e65d0dd` ("feat: Le Zeppelin hub landing page (family router)") + `897d008` ("fix: saisies hub card arrow contrast on paper"). **NEW `public/modules/le-zeppelin.php`** = family-switcher chrome + `.sh-grid` of 4 cards IN DERIVATION ORDER: Salle des Machines → `/modules/salle-des-machines.php`; Salle de contrôle → `/modules/salle-de-controle.php`; Le Cockpit = DISABLED `sh-card--soon` `<div>` (no href, "À venir"); Données générales (admin-only via `is_admin($me)`) → `/modules/reglages-generaux.php`. `$active_module='zeppelin'`, body class `home zeppelin-hub`, filemtime cache-bust, reuses the `saisies.php` card pattern; icons = inline SVG `stroke="currentColor"` (token-colour-inheriting, upgrade from saisies' emoji). **NEW `public/css/le-zeppelin.css`** — scoped `body.zeppelin-hub`, 100% token-based (zero hardcoded colours). **DIVERGENCE CLOSED: the dead-mockup link `/_design/le-cockpit.html` → `/modules/le-zeppelin.php` in topbar.php L14 + salle-de-controle.php L796 — PM-verified ZERO live `/_design/le-cockpit` links remain.** Recette confirmed NOT a hub tile (lives in Salle de contrôle) — decision shipped intact. Operator defaults taken: title "Données générales" (canonical); `index.php` untouched (hub at its own route); Le Cockpit disabled --soon card. **RULE-2 NO-GO caught TWO `--oak`-as-text contrast misses (4.30:1 on `--bg-elev` card surface < AA 4.5) → fixed to `--oak-deep` before commit; the SAME bug existed on the already-LIVE saisies hub (`saisies.css .sh-card__arrow`) → fixed in `897d008` (a recolour regression).** STANDING LIGHT-THEME CONTRAST RULE REINFORCED: `--oak` passes on `--bg` paper (4.62) but FAILS as small text on `--bg-elev` card surfaces (4.30) → use `--oak-deep` for text on ELEVATED surfaces. **TWO RULE-1 backlog items LOGGED (both gated/deferred — see scrapping-backlog #12 + #13):** (a) dual-navigation-IA reconciliation — migrate salle-des-machines/salle-fournisseurs/approvisionnement off the old topbar flat module-list onto the family-switcher IA, then retire the obsolete topbar module-list; (b) `index.php` (DB-status splash) fate — decide whether `/` should redirect to the hub. STILL QUEUED: operator visual review of the live recolour + hub; flip Brassage + Fermentation saisies cards LIVE; liquid dry-hop ml/l capability; `v_mi_wac` WAC view. | Prior: **SITE-WIDE LIGHT-KRAFT RECOLOUR — LANDED + DEPLOYED + VERIFIED, committed `3c57e5b` ("ui: site-wide light-kraft recolour (sunlight legibility)"), pushed origin/main, deployed via `bin/deploy.sh --apply`, PM-verified on the VPS.** 15 files, COLOURS ONLY (no layout/structure/sketch/component change). Mechanism = the planned Option (a): a TRUE global `:root` with the kraft-light values added; the two dark token re-declaration blocks (`body.home`, `body.auth`) had their dark lines DELETED so they inherit `:root`; `--kf-*` promoted out of `body.approv-fournisseurs` into `:root` (token-orphan pages — `body.sdc-page`/`body.rg-page` — fixed for free). ~592 hardcoded colours swept to tokens / color-mix across app.css + 9 page CSS files (salle-de-controle, reglages-generaux, salle-des-machines, salle-fournisseurs, packaging-form, admin-ingest, saisies, form-brewing, form-fermenting) + 3 PHP modules' inline styles (approvisionnement, reglages-generaux, salle-de-controle). `cct-detail-modal.js` chart now reads palette from `:root` via getComputedStyle. **SVG TANKS — OPERATOR CHOSE OPTION B (recolour the steel, NOT keep a dark display-case):** `svg-tanks.php` tanks now sit directly on paper as LIGHT-ground metal — specular sheens inverted to dark edge-shadows, empty-fill #cfc6b2 (`--tank-empty`). RULE-2 reviewer ran NO-GO with 2 blockers + 3 borderline AA misses; ALL folded in before commit. **Final resolved AA+ tokens (on paper #f1e8d4):** `--hop` #567020 (darkened from #5f7320 — primary app-wide accent), `--ok` #3d6826, `--cat-malt` #8a5210 (was #b6731f failing 3.18:1 — feeds brewing/fermenting form category chips, fixed AT SOURCE), salle-fournisseurs `.cat-*` label set darkened+kept-distinguishable (cat-malt #8a5210 / cat-adj #8a5a2a / cat-pkg #6a4a9e / cat-clean #256a77 / cat-util #5f6a35 / cat-maint var(--steel-mid) / cat-rd #5238a0 / cat-trans #4f5f7a / cat-proc #546018), `.home .settings-pill--lager` text → var(--steel-mid) (steel #8a8f94 failed as text), salle-des-machines `--pkg-band` fallback #9b7cc8→#7a56b4 (4 occ; body.salle-des-machines doesn't inherit sdc-page's --pkg-band so the literal was live), cct now-marker line opacity 0.70→0.85 (was below 3:1 graphical threshold). **RULE-1 scrap DONE: `body.approv-fournisseurs` `--kf-*` block promoted to `:root` (scrapping-backlog #11 SCRAPPED).** **STANDING LIGHT-THEME RULE (recorded): any mid-tone token used as TEXT must be re-checked for AA on #f1e8d4 — dark-era accents that read fine light-on-dark FAIL dark-on-light when too light; `--steel` is light-metal grey (fails as text → use `--steel-mid` / `--ink-mute` for text).** STILL PENDING (not in this commit): operator visual review of the live recolour; flipping Brassage + Fermentation saisies cards live; liquid dry-hop ml/l capability; `v_mi_wac` WAC view. Detail → conventions-and-helpers.md §STANDING UI/UX DIRECTIVE (now LANDED, values resolved). | Prior: **OBSERVED-PATH ml→mass FIX — DONE + COMMITTED + PUSHED: `a4b7efb` (origin/main, PM-verified).** Migration 161 `v_bip_canonical` live (schema_meta row present); all 7 inline `IF(bip.unit='g',0.001,1)` rewired to `v_bip_canonical.qty_priced`. PM-VERIFIED LIVE: `v_bip_canonical` 4897 rows, factor_unresolved=0; 652 ml rows, ALL resolve. The obs-path AND recipe-loader-path density legs are now both committed. **2 NON-BLOCKING BACKLOG items raised by the RULE-2 reviewer (recorded → §OBS-PATH FOLLOW-UPS below):** (1) promote `unresolved_count>0` to a `doc_review_queue` row (currently error_log-only — refuse-don't-NULL is honored but operator-invisible); (2) align `scripts/php/gate4_parity_test.php::unit_to_kg_multiplier()` (ml→kg=0.001 NO density) with the density-aware `v_bip_canonical`. Also: `scripts/python/refresh_recipe_profile.py` does its own g/kg conversion on the OLD `bd_brewing_ingredients_parsed` (no ml there → safe today; FLAG for when it migrates to _v2). **FERMENTATION FORM — BUILT, deployed un-linked, NOT committed (RULE-2 reviewer running; commit follows GO).** Both PM-flagged divergence risks PASSED: event-branched per `event_type` to `bd_fermenting_v2` (NOT flat-write); no redundant migration 162 (reused existing `final_comments`). **ACTIVE NEXT after Fermentation commit = `v_mi_wac` WAC view** (PM-verified ABSENT on VPS). See RESUME POINT. **STEP ③ COMPOSITE COMPILER-FOLD — DONE (re-applied + PM-verified live 2026-05-26).** The unit-fidelity fix landed: `app/sku-bom-compile.php` composite_liquid now carries the source row's `ing_unit` and SCALES the source's already-correct `cost` (no `price×qty` recompute). PM-verified live: composite_liquid costs PD8(137)=0.4614, XMASPACK(138)=0.2209, PAL(278)=0.7810, PAC(295)=0.7038; `ing_unit` correctly g/kg per source row; composite_packaging overwrap present (137→167; 138→186+171; 278→166+201; 295→166+201; MI IDs are the actual bound MIs from migration 153, not the label-names in the close-out brief); ZERO stale `bom_source IS NULL` on the 4 composites; ZERO NULL-mi DB-wide; COLLAB12(300) packaging-only (5 rows). composite_* rows now: composite_liquid=204, composite_packaging=7. Migrations **149–156 all live** (156 normalized MALT_CASSONADE + MALT_SUGAR to kg — both now `pricing_unit=kg`, cost identical). Dead `app/sku-compiler.php` SCRAPPED (local + VPS confirmed absent) — RULE-1 scrapping item CLEARED. Nothing committed yet. RESUME POINT advanced. Prior: STEP ③ FOLD — APPLIED-THEN-ROLLED-BACK (unit-fidelity cost bug); FIX + AUDIT PENDING — The composite compiler-fold was coded into `app/sku-bom-compile.php`, applied live, and immediately ROLLED BACK after verification caught a ~1000× cost inflation on composite HOP lines. Root cause (code, not data): the composite_liquid path recomputed `cost = price × qty` and forced `ing_unit = mi.pricing_unit ?? 'kg'`, IGNORING the source single-SKU BOM row's actual `ing_unit` (hops stored in **grams**; price per **kg**; source cost already correctly converted). Malt (kg) unaffected → the bug hid. PD8 wrote cost 233.84 vs correct ~0.44; PAL 429.18 vs ~0.78. **Recipe/source data + per-bottle volume scaling are CORRECT.** ROLLBACK PM-verified live: `ref_sku_bom` restored (137=42, 138=7, 278=35 flat NULL-source; 295/300/301 = 0 BOM rows); **ZERO `composite_*` rows DB-wide.** Migrations **152** (PAC format 7→23, 2×6 box), **153** (overwrap packaging_choices: 4 composites' outer_box/verre/scotch_eshop), **155** (COLLAB24→alias of DGDB sku 8 + COLLAB24 row deleted from collab_temporal) were KEPT — independent of the code bug + correct. COLLAB12 (sku 300) = real DGD 12-pack, KEPT. FIX (pending): carry source `ing_unit` through + scale source's already-correct `cost` (`costPerHl = source.cost / source.hl_per_unit × slotHl`), symmetric with qty scaling, no `/1000` hardcode; source query must add `b.ing_unit, b.cost`. **UNIT-FIDELITY lesson recorded as a coding-discipline DIVERGENCE FLAG (conventions-and-helpers.md): cost recompute MUST honor `ing_unit` vs `pricing_unit`; never assume a BOM qty is in the pricing unit.** A code+SQL audit for similar issues elsewhere is underway. NB schema fact: there is NO `is_composite` column on `ref_skus`. Detail → sku-decomposition-tree.md §STEP-③. Prior: **STANDING UI/UX DIRECTIVE RECORDED — site-wide lighter-palette rollout (operator, this session): COLOURS ONLY (not layout/structure/sketches/composition), all existing + future pages, via the shared token layer (one place, whole-site), NEVER per-page hardcoded colours. PM-verified there is NO single global `:root` for the app tokens — dark walnut tokens are re-declared PER BODY CLASS (`body.home` app.css ~L83, `body.auth` ~L2940); the light `--kf-*` set is scoped to `body.approv-fournisseurs` only (~L10128). Clean rollout = introduce a true global `:root` token block, else swap values in every body-class block. EXACT lighter accent values = OPEN ITEM needing operator confirm before the CSS pass — no hex invented. Recorded in conventions-and-helpers.md + the `ui` skill (corrected its stale "flip the global :root" mechanism). No CSS changed. Detail → conventions-and-helpers.md §STANDING UI/UX DIRECTIVE.** Prior: **PAD RECONCILED + 2 migrations landed (PM-verified live) + STEP ③ SPEC DELIVERED.** Migrations **150_pad_alias_of_pd8.sql** (applied 01:26) + **151_delete_spurious_pad_format.sql** (applied 01:30): operator confirmed **PAD (sku 277) = the SAME 8-beer sampler as PD8** (NOT the 80-bottle pack the format implied) → PAD folded to alias of PD8 (`ref_sku_aliases` PAD→137 ✓), sku 277 `is_active=0` / `format_id=NULL` / `recipe_id=NULL` ✓; spurious format 22 DELETED ✓ (zero dangling refs — ref_packaging_items/format_mis/composite_slots.member_format_id all clear of 22, PM-verified). **PAD-HELD open item is now RESOLVED — was the 80-vs-8 contradiction.** Step ② prior: migration `149_composite_pack_membership.sql` (applied 01:22) populated `ref_sku_composite_slots` [19 rows / 4 SKUs: PD8(137):8 ×1, XMAS(138):3 ×1, PAL(278):6 ×2, PAC(295):2 (SPY×6+DIB×6)], all member_format_id=5 (BU 33cl). PACKDEC(287) folded → alias of PD8 (PACKDEC→137, FRPACKDEC→137, PACKDECX8→137). PAC "12-vs-24" reconciled → 6+6=12. PAL = "Pack Louis" (NOT pallet). (Step ③ spec was delivered then the fold was coded/applied/rolled-back — see the current state at the top of this header.) Lean-index migrations line now **131–155**. → RESUME POINT + sku-decomposition-tree.md.** Prior (still valid): **SKU-BOM volume-backfill pass + decomposition-tree first builds — LANDED + verified; migrations 147+148 committed/pushed to origin/main:** (1) **volume_hl backfill COMPLETE** — `volume_hl` now on **105 distinct SKUs** [was 18; close-out brief said 104, live=105]; keg/cuv/composite excluded by design [SKU-level via `v_sku_volume`]. (2) **`-X` cage dedup DONE** — migration **147** (`dec4481`) removed the spurious format-6 `outer_box`/`pallet` slots [reusable crate, not a consumable]; the 24 legacy `source='mi_match'` dup rows MANUALLY DELETEd [**0 `mi_match` rows DB-wide**] → exposed a **COMPILER-DELETE-PREDICATE GAP** [recompile DELETE keys on `source='Packaging'` ONLY, missing other legacy tags → compiler-hardening item]. Each `-X` now = liquid + 3 packaging (bottle vol 3.3891 + caps + label). (3) **4 Diversion recipe links DONE** — DIBB12(62)→r26, DIBBU(84)→r26, DIGBU(87)→r27, DIPBU(88)→r28 + recompiled + volume-backfilled. (4) **FR-alias cleanup DONE** — 6 FR* SKUs now is_active=0 + recipe_id NULL + 0 BOM + aliased [FRDIBB12→62, FRDOAB12→296, FRMOOB12→64, FRSPYB12→63, FRPACKDEC→287, FRPACKCHEF→295(PAC)]; 15 stray packaging rows reverted. (5) **BLA4 white-label DONE = FIRST LIVE WHITE-LABEL** — migration **148** created 3 BLA MIs [PKG_LABEL_BLA 646 / PKG_4PACK_BTL_BLA 647 / PKG_6X4_BTL_BLA 648, price NULL]; BLA4(id4,fmt3)→recipe MOO(r44); 3 `ref_sku_packaging_choices` Tier-1 overrides force label/outer_tray/outer_box→BLA brand; recompiled → 5-line BOM; is_active=0. **"Blanche des Romands"(BLA) ≠ "Blonde des Romands"(BLO,r10) — conflation flag RESOLVED. WHITE-LABEL PATTERN [liquid-from-recipe-X + packaging-brand-overridden-to-Y via `ref_sku_packaging_choices`] CONFIRMED LIVE — template for all future white-labels; proves sku-decomposition-tree step ⑤.** (6) PAC composite membership CAPTURED for the future composite-slots build [PAC(295)=12×SPYBU+12×DIBBU+1×PKG_12PACK_ESHOP+LOG_SCOTCH_ESHOP; ⚠️ COUNT DISCREPANCY: operator "12 bottles" vs 12+12=24]. — Prior: Volume Phase A LANDED `a8950ed` [migration 144 `volume_hl` + `v_sku_volume` view LIVE incl drift detector; CREATE VIEW granted to `maltytask_app`]; migration 145 `-4` 3-layer outer_box slot. Still OPEN: BLO/ZEP 6×4 carton MIs missing [BLO4=3, BLO4C=1, ZEP4C=1 RQ]; ZEP6C gating-gap; compiler-DELETE-predicate gap.)


---

## Archived 2026-06-11 (compiled out of index line 5 when CIP-CADENCE-ON-BBT-CARDS landed)

**✅ FINANCIER STAGE 4 (BUDGET) SHIPPED+LIVE+PUSHED — COP/COGS BOARD-GRID ARC CLOSED (all 4 stages done).** Mig **327** `ref_budget_cogs` (21 GL-line rows, fiscal_year 2026, `budget_chf_per_hl`, schema_meta classified) seeded from operator's board-sheet COGS&YIELDS BUDGET column, total **75.76 CHF/HL**. maltyweb `b477565`: cogs-grid module loads budget per line → BUDGET (month+ytd, flat annual rate) + Vs BUDGET = (actual−budget)/budget % columns now LIVE on the COGS grid (April: Malts −37.3%, Hops −20.6%, TOTAL COGS VARIABLE −8.0%); COP grid unchanged (no budget on its sheet); N-1/Vs-N-1 still "—" (2025 GL not ingested). v1 APPROX (documented): Transport + QA/QC budgets monthly-profiled in source but seeded as annual rate. ALL COMMITS PUSHED — remote == deployed (maltytask `a08a718`, maltyweb `b477565`). GIT HYGIENE INCIDENT (2nd bare-commit sweep in one session): `b477565` swept a parallel expeditions session's staged DELETION of `public/css/expeditions-sku-history.css` (SAFE — file unreferenced, expeditions work shipped in `e7962ce`) → REINFORCES pathspec-commit hard-rule. Arc backlog (non-blocking): N-1 cost (2025 GL CSV ingest), COGS-YTD-starts-March coverage chip, May utilities booking, dead `fin_ytd_months(rolling6m)` path. → sales-commercial-surface/README.md §BOARD COGS/COP P&L GRID §STAGE 4 BUDGET.

**✅ DAILY-RECAP REFINEMENTS #4 + PERF SHIPPED+DEPLOYED+COMMITTED (commit `06e1339`; prior recap batch `ca1dde5`).** (1) cuv-leaf reassignment now VISIBLE — `kpi_pkg_daily_recap` WHERE swapped stale pre-317 `reuses_packaging_id_fk IS NULL` → structural `NOT EXISTS (...c.reuses_packaging_id_fk=b.id AND c.is_tombstoned=0)` → ZERO COGS/beer-tax impact. (2) SKU per recap row (LEFT JOIN ref_skus). (3) PERF — `v_bd_packaging_v2_vendable` ~2271 rows via 2 correlated EXISTS → mon-tableau cold ~4.3s; **Migration 320 = covering composite index `idx_bdpv2_reuse_ts (reuses_packaging_id_fk, is_tombstoned)` INSTANT DDL → kpi_pkg_daily_recap 4343ms→23ms (~190×)**; `kpi_cache_get/set` = in-REQUEST PHP array only. Migs 310/313/319/320 ALL applied + tracked. → kpi-tracker-catalog/README.md §REFINEMENTS #4 + PERF.

**✅ DAILY-RECAP REFINEMENTS #3 (mig 319 = card rename only).** Packaging recap buckets per-run rows into Bouteille/Canette/Fût/Cuve (`RUN_TYPE_SECTION` in kpi-charts.js); handler query UNCHANGED (zero row drop). Mig 319 renames `ref_kpi_trackers.label` `packaging_daily_recap`→"Conditionnement — résumé du jour". Wort: intra-day CV SCRAPPED → per-brassin rolling variation vs last 10 SAME-recipe brews. → kpi-tracker-catalog/README.md §REFINEMENTS #3.

**✅ FINANCIER PER-SKU BOM DRILLDOWN MODAL (rode GL commit `d0c7e60`).** SKU-code → `<button.sku-drilldown-btn>` opening `<dialog#fin-sku-modal>`; `financier-data.php?module=sku-detail&sku=CODE` runs VERBATIM sku-cost-detail.php BOM query. Entangled financier.* set all committed in `d0c7e60`. → build-history changelog.

**✅ DAILY-RECAP REFINEMENTS #2 (no mig).** FIX1 perte-bière label, FIX2a run-type tags, FIX2b keg/cuv beer-loss double-count of `loss_liquid_other_units` corrected, EXPANSION3 wort quality. → kpi-tracker-catalog/README.md §REFINEMENTS #2.

**✅ SKU-COSTS RETIREMENT (#2) (mig 318, `e72e64e`).** `sku-costs.php` DELETED + ref_pages is_active=0; Financier "Coût par SKU" tab renders per-SKU table SERVER-SIDE reusing the EXACT ref_sku_bom query. → scrapping-backlog #32 RETIRED.

**✅ RÉASSIGNER CUVE (mig 317, `4af2155`).** Serving-tank reassignment + vendable HEAD/LEAF split: vendable+liner-cost MOVE to leaf, tax+loss STAY on head. Blues Rules 6740→Rincette verified; batch-211 cuv totals conserved. `4af2155` also swept mig-313 objective_hl wiring. → loss-types-and-form-changes-arc.md §RÉASSIGNER CUVE.

**🏁 PHASE 2b KPI BUILD COMPLETE — 55→158 live trackers / 270 active; 103 handlers in 10 atomic batches, 112 honest stubs = Phase 2c backlog.** All 11 domains have a `kpi_handler_<domain>` fn. Commits `6a013ad`/`09006ce`/`d264111`/`4f6a98c`/`598f788`/`a353ea2`/`e32d472`/`ea10fb7`/`7cbedf7`/`b3a1e02`; migs 305-309/311/312/314/315/316 mine + 310/313/317 swept. → kpi-tracker-catalog/README.md §PHASE 2b BUILD COMPLETE.

---

