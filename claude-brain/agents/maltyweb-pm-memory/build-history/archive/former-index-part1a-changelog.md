# Former-index archive — PART 1: rolling "Last updated" changelog (block A)

> Verbatim pre-2026-06-05 index content; archival, superseded by thematic topic files. Each block below is a single monolithic former-index "Last updated" line, preserved verbatim. Parent: [../README.md](../README.md).

# PART 1 — former "Last updated" changelog block (former index line 5)
_Source: former-index-last-updated.md (verbatim)._

# Build-changelog appendix — rolling "Last updated" verbatim (former index line 5)

> Part of the build-history changelog. Parent index: [README.md](README.md). This block was lifted VERBATIM out of the LEAN INDEX during the 2026-05-27 index-lean refactor — it is the rolling "Last updated" changelog that used to bloat index line 5. Archival; superseded by the structured period changelogs.

> Last updated: 2026-06-10 (**🟢 COGS COST-SOURCE CUTOVER — ZEPV/cuv GATE ✅ CLEARED: cuv `ref_skus.hl_per_unit` reverted 1.0→0.01 (EMBV/MOOV/ZEPV 27/46/61) + liquid recompile (0 parity) + COMPILER DEFECT FIXED in `app/sku-bom-compile.php` cuv liner branch (hardcoded per-HL `qty=1.0`/`cost_direct=cuvLinerCostPerHl` → now scales BOTH by `$skuHlPerUnit`; deployed, packaging recompiled, 0 parity; CHF/HL display unchanged). Opus live-verified: `Σref_sku_bom.cost` ZEPV 0.096594/EMBV 0.224576/MOOV 0.121062, skuHL=0.01; ZEPV sales material ≈937 CHF (~100× phantom GONE); cuv view id 1298 reconciled 5.0==stored; `--cost-source=diff` 2026-05 no cuv diff lines. DURABLE LESSON: a COGS-bearing compiler line gated only on the CHF/HL DISPLAY (cost/qty) silently broke in the SUMMED per-unit sales-material path — per-unit lines MUST scale by the SKU's own hl_per_unit, never hardcode a basis; gate COGS changes on the sales-material path not display/conservation. CONSISTENCY RULE LOCKED: litre-denominated SKUs carry `ref_skus.hl_per_unit=format.hl_per_unit`. 🏁 CUTOVER FLIPPED+SHIPPED+COMMITTED 2026-06-10 — maltytask `0129bdb` (build-sales-cogs.ts default sheet→mysql ~L131 + run-month-close.js step-3 fold; deps maltyweb `6b8f6a0` + `7fbeecb` mig 302). Post-flip verify: dry-run NO flag → cost-source=mysql, May 2026 mysql = 73 SKUs / COGS 59,676.02 / NonBeer 31,617.70 / BeerTax 12,735.39 / ZEPV 936.96; RULE-2 GO. TWO residuals PARKED: 3 unknown sales SKUs (triage post-close); contract BAMB/JASP* null beer-tax (accountant sign-off, don't guess). Rollback snaps `_snap_refskus_cuv_hlrevert_20260610`/`_snap_skubom_cuv_hlrevert_20260610`. migs 300/301 staged-not-deployed = separate sales-commercial arc, unchanged. → walkthrough §COGS COST-SOURCE CUTOVER (FLIP DONE block) + RESUME #0.**) Prior: (**🧊 BBT-VIDE v2 ✅ DEPLOYED + MIGRATED + LIVE IN PROD (Kouros-verified VPS ground-truth 2026-06-10): deploy gate steps 1–3 DONE — `form-racking.php` on VPS carries the feature (16 markers); mig 296 `bd_racking_v2.bbt_vide_scrapped_hl DECIMAL(8,3) NULL` APPLIED; mig 297 `commissioning_settings pertes_bbt_empty_threshold_hl=2.5 (section='pertes', is_active=1)` APPLIED; `migrate.php --status`=0 pending; maltyweb HEAD=`281082e`, main==origin (commits `a10431a`+`281082e` pushed). ONLY operator smoke (gate steps 4–5) + first real override-row REMAIN — `bbt_vide_scrapped_hl IS NOT NULL` count=0, feature not yet exercised by a real save. SMOKE GATING (Playwright/webapp-testing): 4 checks span saisies(min_role=operator)/fermentation=tanks.php(operator)/salle-de-controle?sec=pertes(require_login only, view-by-anyone); **smoketest acct `users.id=16` is a VIEWER (role=viewer, access_preset_id_fk=NULL, no user_page_access grants) → drives ONLY check #3 (pertes settings VIEW); #1/#2/#4 need operator-or-higher (operator suffices); check #1 (edit BBT-vide row) is UNEXERCISABLE until a real BBT-vide row is saved (0 exist today)**; creds `/home/kluk/projects/maltytask/secrets/maltyweb-smoketest.env` (MALTYWEB_USER/MALTYWEB_PASS). This SUPERSEDES the stale "BBT-vide v2 arc: migs 296/297 … BUILT lint-clean but UNCOMMITTED + NOT applied + NOT deployed — HELD" note further down in this header (under the SAME-DAY-TIEBREAK 'Prior' block). → racking-redesign-ws1-4.md §BBT VIDE v2 + §SMOKE-TEST GATING NOTE.**) Prior: (**🚚 WEEKLYORDERS IMPORT ✅ APPLIED to prod (morning, Kouros present) — `ord_orders` 12→79** [web=12 byte-stable ids 3-14 source_ref NULL + import=67; apply imported=67/skipped=0; `ord_order_lines`=252, `ord_order_status_events`=160]. Predictions held EXACTLY: Festival de la Cité = **13 separate orders** ✓; internal channels leaked = **0** (Taproom/Eshop/échantillons TS skipped, ruling #3 proven live); web-overlap-collision bucket (12 rows r7-r17,r38) correctly **HELD OUT** → 1:1 to existing web #3-#14, no twins (ruling #6 proven); **4 empty-order rows NOT imported** (Street Cellar/Galac, Jardins de Louis/Cuves, Gotham×2/T7 — pending Kouros phone calls; idempotent re-apply folds them + Cité "5 bars" r231 + ZEPV-litres caveat once operator data-Qs resolve). **STILL DEFERRED (separate, NOT this entry):** migs **300/301** (`ref_customers.sale_class` + `inv_sales_ledger` — sales-commercial-surface arc) NOT applied/NOT on VPS; importer commit **e57eda5** still LOCAL-ONLY unpushed (push not requested). **MIGRATION HEAD = 299 applied / NEXT FREE = 300** (re-verify at dispatch). → fulfilment-expeditions-arc/README.md §APPLIED 2026-06-10.) Prior: (**🚚 FULFILMENT ITEM 1 FULLY SHIPPED — stock-movement saisie + inter-site transfers + per-site SALES attribution + Nausikraft delivery-site admin (mig 299, 3 commits `3c3eb33`/`49a0386`/`edfcf78` pushed+deployed+Opus-verified; autonomous run, Kouros offline).** mig **`299_fulfilment_site_attribution.sql` APPLIED → MIGRATION HEAD = 299** (one atomic file: CREATE `inv_stock_movements` [from<>to + qty>0 CHECKs, FK RESTRICT→ref_skus/ref_sites, tombstone-only] + ALTER `ord_orders.fulfilment_site_id_fk` + ALTER `ref_customers.default_delivery_site_id_fk` SET NULL = the Nausikraft mechanism). **THE single resolver** `app/fulfilment-site.php::resolve_fulfilment_site()` (precedence per-order override > customer default > channel default; sites DB-DERIVED by role warehouse=id2/pos=id3/production=id1, NEVER hardcoded) is the spine — BOTH order-create selector AND `fg_stock_location_snapshot()` sales-attribution call it (NO inline channel→site map → divergent-store anti-pattern avoided). Snapshot now attributes SALES per site (replicates `fg_stock_compute()` steps 4/5/6 cutoffs EXACTLY) + applies `inv_stock_movements` transfers (+to/−from; net-zero to global → `fg_stock_compute()` TOTAL unchanged; cards may go negative, honest); **Σ(cards)==Σ(physique)=8642 Opus-verified.** New `expeditions.php ?view=mouvements` (operators record / admin tombstone, NEVER DELETE) + ship-from selector on order saisie + `import_weeklyorders.php` sets it + `reglages-generaux.php ?sec=delivery_sites` admin assigns the Nausikraft client list. E2E: DIV4 Renens→Crissier qty5 shifts split, Σ unchanged, tombstone reverts, CHECKs reject from==to/qty<=0. **This CLOSES the "sales-leg per-location settlement" carryover (ATTRIBUTION done).** KNOWN LIMITATIONS: (1) sales+transfer cutoffs still on GLOBAL anchorDate (prod leg got the per-site tiebreak; same-class refinement remains — apply the ROW_NUMBER per-(sku,site) tiebreak); (2) **DATA pending — operator enters the Nausikraft client list TOMORROW via `?sec=delivery_sites`** (UI+arch ready). STILL PARKED: ITEM 2 mini side-stock/giveaways (blocked on concurrent form-packaging.php + migs 293/294 + Kouros D-Q5/6/7); ITEM 3 Phase-2 cage/COGS engine (Shopify pickup-signal ON HOLD + cage anchor count Mon 2026-06-15). → fulfilment-expeditions-arc/README.md §ITEM 1 SHIPPED.**) Prior: (**✅ SAME-DAY COUNT+PACKAGE CARRYOVER CLOSED via Option 2 (surgical tiebreak, NO migration) — committed `d3fb954`+pushed+live; data-layer only (`app/fg-stock.php`).** This CLOSES the OPEN PF-stock carryover (1) (DATE→DATETIME) — resolved WITHOUT a DATETIME migration. `fg_prod_since_anchor()` per-SKU production anchor now uses a single **ROW_NUMBER() OVER (PARTITION BY sku_id_fk ORDER BY counted_at DESC, id DESC)** window pick (rn=1) exposing the WINNING production-site count's BOTH `counted_at` AND `created_at` (NOT two blind MAXes — the PM-flagged gotcha avoided). Cutoff predicate: `event_date > COALESCE(prod_anchor,$globalAnchor) OR (prod_anchor IS NOT NULL AND event_date = prod_anchor AND submitted_at > prod_anchor_ts)` — **primary ordering on business `event_date`; `submitted_at>created_at` only breaks SAME-DATE ties**; NULL-safe for never-counted SKUs (pure global cutoff); empty-prodSites fallback + all prior guards (cuv/tombstoned/white_label/FLOOR-per-event/COALESCE) kept. SINGLE helper → both `fg_stock_compute()` + `fg_stock_location_snapshot()` inherit it. **OPUS INDEPENDENTLY VERIFIED (old-vs-new predicate diff across ALL SKUs): exactly ONE SKU changed — EMBF, the genuine 2026-06-08 collision (prod count created 13:54:13; EMBF pkg event_date 06-08 submitted 14:53:07 → after count; OLD dropped=0 → NEW=300 kegs recovered); NO other SKU perturbed** (DIV4=412/physique=511, DIVB=108, ZEPV absent, never-counted=0; **Σ(locations)==Σphysique=8312**). DECISIONS LOCKED: `counted_at` STAYS DATE (business-truth, mirrors `bd_packaging_v2.event_date`); `created_at` is canonically "when counted" for maltyweb-form/manual rows; `form_timestamp` NULL = latent Google-Form-era gap, NOT fixed now; **NO DATETIME migration (rejected for blast radius on every DATE-equality consumer)**; sales legs (expedie/eshop/taproom) STILL deferred to stock-movement-saisie arc. **ACCEPTED RESIDUAL (inline SCOPE NOTE in code): a run physically-before but submitted-after a same-day count → rare double-count, left un-clamped on purpose; if it ever bites, fix = explicit count timestamp, NOT a clamp.** Smoke = 302 login-redirect (no page creds); data-layer change, both stock fns proven non-throwing + hand-verified → accepted. **NO migration number consumed — 293/294/295 are parallel sessions'; MIGRATION HEAD = 295. BBT-vide v2 arc: migs 296 `296_bd_racking_v2_bbt_vide_scrapped_hl.sql` + 297 `297_pertes_bbt_empty_threshold.sql` = BUILT lint-clean 2026-06-09 but UNCOMMITTED + NOT applied + NOT deployed — HELD for a coordinated commit (a parallel "journal des saisies/submitted_by_user" session co-edited `form-racking.php` at non-overlapping ranges; that session's mig 298 also pending); Kouros approved the hold, review+apply+deploy gate 2026-06-10. NEXT FREE = 299 (296/297/298 all claimed) — re-check `migrate.php --status` + `ls db/migrations` before assigning. → racking-redesign-ws1-4.md §BBT VIDE v2.** → fulfilment-expeditions-arc/README.md §SAME-DAY TIEBREAK.**) Prior: (**✅ PF-STOCK ITEM B SHIPPED+committed `82526cb`+pushed+live — `reglages-generaux.php` (Données générales sites admin) now exposes `site_type` (select Production/Entrepôt/Point de vente/Consignation) + `holds_fg_stock` (toggle) on BOTH add_site AND edit_site forms; handlers read via post_str??default, validate site_type w/ `must_be_one_of` (throws, mirrors country dropdown), coerce holds_fg_stock→0/1, persist in INSERT/UPDATE, before/after via log_revision; helper line "Le conditionnement alimente le stock PF des sites de type « Production ».". NO new setting / NO new column / NO mig — `site_type` stays the single SoT (Q1 verdict HELD). Reused rg-select/rg-toggle markup, no inline CSS. Verified VPS lint clean + rollback-wrapped UPDATE probe + 4 sites pre-select correct (id1 production/id2 warehouse/id3 pos/id4 consignment). Operator can now SET in settings which site(s) packaging feeds (type=Production) — the capability he framed as "set in settings". PF-stock arc item B = SHIPPED (was DEFERRED). **MIGRATION HEAD = 295 APPLIED (live-verified VPS 2026-06-09: 291–295 all ✓; 293 `293_loss_model_no_liquid_subtraction.sql` = THIS session's loss-model fix shipped at `7a8b63a`; 294 `294_ord_orders_source_ref.sql` + 295 `295_submitted_by_user_fk.sql` = parallel sessions). PENDING on VPS: 296 `296_bd_racking_v2_bbt_vide_scrapped_hl.sql` + 297 `297_pertes_bbt_empty_threshold.sql` (parallel BBT-vide arc). NEXT FREE = 298 — but re-check `migrate.php --status` + `ls db/migrations` immediately before assigning (parallel sessions lead).** PF-stock carryovers: (1) ✅ CLOSED 2026-06-09 — same-day count+package carryover resolved via **Option 2 (surgical tiebreak, NO migration, commit `d3fb954`)**; `counted_at` STAYS DATE [see lead entry below]; (2) OPEN — sales-leg per-location settlement (stock-movement-saisie arc); (3) FUTURE item E packaging mini side-stock — build WITH the packaging-form "units from older runs" field. → fulfilment-expeditions-arc/README.md §ITEM B + §FULL-BOX FLOORING + §SAME-DAY TIEBREAK.**) Prior: (**✅ PF-STOCK BUILD: FULL-BOX FLOORING + PER-LOCATION PRODUCTION ATTRIBUTION SHIPPED+committed `d71d890`+pushed+live — NO mig (data+PHP: `app/fg-stock.php` + `public/modules/expeditions.php`; EQUIP coder+sql+ui +webapp-testing). MIGRATION HEAD = 292 / NEXT FREE = 293, unchanged.** Built per the design we consulted; supersedes the `566b9d3` prod-leg fix with the operator's full-box PF rule + per-location production crediting. (1) **FULL-BOX FLOORING** — production = `SUM(FLOOR(prod_total_units/units_per_pack))` floored PER EVENT then summed; **operator rule: PF = full boxes ONLY, loose units from a partial run are NOT PF** (= future "mini side-stock", item E); DIV4 9901→412 boxes (was 412.54); keg/cuv upp=1 → floor no-op; `prod_qty`+`physique` now INT. (2) **PER-LOCATION PRODUCTION ATTRIBUTION** — `fg_stock_location_snapshot()` credits floored production-since-prod-site-count to the PRODUCTION-site card (DB-derived `site_type='production'`, `$isProdSite`-gated); Renens DIV4 37→449 (37+412); other sites anchor-only; **new invariant Σ(locations)==physique when sales=0** (449+39+23=511 verified). (3) **SINGLE-SOURCED (Q2)** — new private helper `fg_prod_since_anchor(PDO,$globalAnchor)`→`{prod_site_ids, by_sku:{prod_qty:int,prod_events:int}}` called by BOTH `fg_stock_compute()` AND the snapshot → card/SKU-total CAN'T DRIFT. (4) **INTEGER DISPLAY (Q5#4)** — unit/box cols 0-dec; HL stays decimal. (5) **STOCK ORDER** — `$stockFamilyOrder` Keg→Bot→Can→Can33→multipack→Cuve; single-unit SKUs (`sku_code` `/(BU|CU)$/`, taproom-only) → "À l'unité (taproom)" rendered LAST; applied to total table + filter chips + per-location grouping. **Q1 verdict HELD: NO new setting, NO `receives_packaging` col — `site_type='production'` stays the single SoT.** STILL OPEN — **item B (DEFERRED, likely next pending operator confirm):** expose `ref_sites.site_type`+`holds_fg_stock` in Données générales sites admin (`reglages-generaux.php edit_site` currently does NOT expose `site_type`) + helper line "le conditionnement alimente le stock PF des sites de type Production". FUTURE — **item E (LOGGED, build WITH the form field not before): packaging "mini side-stock"** — remainder = `prod_total_units MOD units_per_pack`/run; lives between `bd_packaging_v2` and beer-tax (NOT COGS); resolves: completes-a-box-later (future packaging-form field, must work in ALL parallel runs)→FG | giveaway→beer-offered→beer-tax (tax paid, no CHF) | year-end unconsolidated→beer-offered; canonical balance Σ(prod MOD upp)−Σ(drawn-to-complete)−Σ(giveaways). Carryovers unchanged: same-day `counted_at` DATE→DATETIME before Mon 2026-06-15 cage count; sales-leg per-location settlement (stock-movement-saisie arc). → fulfilment-expeditions-arc/README.md §FULL-BOX FLOORING.**) Prior: (**✅ TWO PF-STOCK FIXES SHIPPED+pushed+deployed — NO mig (data+PHP), origin/main afe1dcd..566b9d3, selective rsync + chown www-data. ⚠️ OWNERSHIP/PARALLEL-SESSION NOTE (corrected 2026-06-09): both `ae297c2` (the OUTAGE FIX, see (1)) and `566b9d3` (the PRODUCTION-LEG FIX, see (2)) were authored+landed by KOUROS/Kluk76 (15:57), NOT by the build agent — `ae297c2` is Kouros's prod-catch of THIS session's `afe1dcd` 500 (which this session had mis-reported as a clean ship; smoke skipped). `566b9d3` is CONCURRENT work by ANOTHER session, left untouched by this one. **LIVE DIRTY-TREE HAZARD (deploy rule §line 14):** working tree currently carries UNCOMMITTED `app/fg-stock.php` + untracked `data/merge-suggestions-2026-06-08.md` (concurrent fg-stock session) AND unrelated dirty `racking-form.css/js`, `form-racking.php`, `expeditions.php` — a `bin/deploy.sh --apply` from this tree would leak all of it live; commit-before-deploy / `git status` before any `--apply`. (1) EXPÉDITIONS OUTAGE FIX `ae297c2` — `exp_dow_fr()` was defined NESTED inside the stocktake render fn → undefined at the EARLIER ~L2824 fresh-chip ("✓ compté") call site, which only fires when a count exists this week → today's count tripped "Call to undefined function exp_dow_fr()" → 500 on the WHOLE PF-stock page (introduced by `afe1dcd` fresh-chip feature, same day — which was mis-reported to PM as a CLEAN ship: `php -l` passed [valid syntax / conditional decl] and the deployed-page smoke was SKIPPED, so it shipped broken). FIX (BY KOUROS, not the build agent) = hoisted `exp_dow_fr` to the top-level date-helper section ~L934 (compile-hoisted; also kills a latent cannot-redeclare on double render). DURABLE LESSON: PHP nested fn defs are runtime-declared in CALL ORDER — a helper used before its (later) nested def fatals; keep page helpers TOP-LEVEL. (2) `fg_stock_compute()` PRODUCTION-LEG FIX `566b9d3` — the bug PM predicted: R4 anchor SUM is per-(sku,location) but the flow windows stayed on a single GLOBAL `$anchorDate`; today's partial count (Taproom/pos-site only, 06-09) poisoned the global cutoff → same-day production-site packaging DROPPED (DIV4 9901 btl, DIVB 2592 btl invisible in physique). FIX exactly as consulted: production site id(s) DB-DERIVED `ref_sites WHERE site_type='production' AND is_active=1` (id1 Zgeg/Renens today, SET-VALUED no hardcode); per-SKU prod cutoff = MAX(counted_at) for that SKU at prod site(s), COALESCE→global $anchorDate fallback for SKUs never counted there; `run_type='cuv'` EXCLUDED from prod (cuve-de-service = filled-at-client = sold, never FG; 3 independent guards = no anchor + no ord_order_lines path + stocktake_scope='none' → EMBV/MOOV/ZEPV vanish from the computed FG table, no negatives); SALES legs (expedie/eshop/taproom) STILL on the global cutoff — per-location settlement DEFERRED to the stock-movement-saisie arc, SCOPE NOTE inline (prod-fixed ≠ all-fixed). Live (www-data probe): DIV4 prod=412.54 (9901/24) / DIVB=108 (2592/24) / ZEPV absent; non-prod-anchored SKUs (DIB4/DIVF/EPH4B) prod=0, no crash. DURABLE fulfilment facts: packaging inflow ALWAYS lands at the production site (Renens, DB-derived); inter-site moves (Renens→Crissier→Nausikraft→Taproom) = future stock-movement saisie (not built); ref_sites = id1 Zgeg/Renens prod, id2 Zeubi/Crissier warehouse, id3 Taproom/Renens pos, id4 Nausikraft consignment(cust_fk=1), all holds_fg. OPEN/DEFERRED: sales-leg per-location settlement (stock-movement-saisie arc); same-day count+package edge — `counted_at` is DATE + strict `>` → needs DATETIME granularity, worth doing before/with the Mon 2026-06-15 cage count. → fulfilment-expeditions-arc/README.md §TWO PF-STOCK FIXES. MIGRATION HEAD = 292 / NEXT FREE = 293, unchanged.**) Prior: (**✅ TAPROOM SINGLE-UNIT-SKU GAP CLOSED + WEEKDAY-CHIP BUG FIXED — three landings, NO migration (data + one PHP file), idempotent, /tmp helper cleaned: (1) DGDBU CREATED `ref_skus` id=315 (recipe 31 DrunkBeard - Galactic Drift, format_id=5 BU) via replicated SDC create path — hl_per_unit=0.00330 read from `ref_packaging_formats.id=5` (NOT hardcoded), bom_template_id=5, row_hash=sha256('31|5|DGDBU'), units_per_pack=1, `stocktake_scope='single'` (so it shows at Taproom/POS), is_packaging_line=0+is_direct_sales=0 (matches DGDB/DGDF family — flagged to Kouros), log_revision; `sdc_recompile_recipe_packaging(31)`→4-line packaging BOM (bottle/caps/label DGD/TEA), liquid line comes from Node pipeline=0 at PHP-recompile (same as ALL BU siblings, expected); 6/6 sibling-conform. (2) CONTRACT BEERS OUT OF TAPROOM — BAMB(313)/BAMF(309)/JASPB(312)/JASPF(308) `stocktake_scope` 'base'→'cage' (prod+warehouse, excludes POS; Chien Bleu never at taproom), log_revision/row; JASPL already 'none'; **DECISION: NO contract singles — BAMBU/JASPBU NOT created**. (3) WEEKDAY-CHIP BUG FIXED `public/modules/expeditions.php` — FG-stocktake freshness chip hardcoded "compté lun."; added `exp_dow_fr(DateTimeImmutable)` mirroring sessions.php::sd_date_fr() French-day array, both chip sites now DERIVE weekday (verified 2026-06-09→"mar."), committed `afe1dcd`, php -l clean, on origin/main. ✅ BOTH OPEN FLAGS NOW CLOSED (Kouros decided 2026-06-09, verified live): (a) DOCB(id21)+DOCF(id22) scope 'base'→'cage' (Docks NEIPA not taproom-sold) — full set scoped OUT of taproom POS = BAMB/BAMF/JASPB/JASPF (contract) + DOCB/DOCF (collab) = 6 SKUs all 'cage'; (b) DGDBU is_direct_sales→1 (single bottle IS direct-sales; box DGDB/keg DGDF stay wholesale =0) — only id315 flipped, UNBLOCKS Phase-2 cage/COGS engine is_direct_sales gate for DGD singles; (c) DEFERRED count-move DONE (taproom stocktake fully inputted) via one-shot CLI REPLICATING expeditions.php FG-stocktake write path (snapshot loc3→txn→log_revision, upsert+STORED-generated-row_hash, NO ad-hoc bare SQL): inv_fg_stocktake id=351 DGDBU@loc3 qty=21 source='manual-adjustment' submitted_by='Olivier Barral' active, DGDB stopgap id=301 is_active=0, taproom DGD 0.0792HL→0.0693HL, snapshot loc3-20260609-132930.json, idempotent/no-mig/cleaned. NET: every taproom-sold beer has its BU/CU single at scope='single', non-taproom (contract+Docks) = 'cage'; NO structural unit gaps remain for taproom. → fulfilment-expeditions-arc/README.md §TAPROOM SINGLE-UNIT-SKU GAP ✅ ALL FLAGS CLOSED. MIGRATION HEAD = 292 / NEXT FREE = 293, unchanged.**) Prior: (**✅ BURST-BAG / PACKAGING-LIQUID-LOSS FIX SHIPPED+COMMITTED — maltyweb `3d96847`, mig 292 `292_floor_keg_cuv_vendable_view.sql` (applied direct CREATE OR REPLACE VIEW to dodge parallel session's in-flight 291; schema_migrations row manual). MIGRATION HEAD = 292 / NEXT FREE = 293.** Compute-fn vendable-floor + view mirror + loss routes to typed `loss_keg_liquid_l` + tank-sim drains pure-loss rows; 6 pure-loss rows backfilled (+23.72 HL to packaged-HL AND beer-tax base, 2026-04/05 OPEN); 5 void rows hard-deleted+tombstoned. **🔴 CRITICAL: the read-path view recomputes ALL 245 cuv PRODUCTION rows at 100× because EMBV/MOOV/ZEPV still carry un-reverted `ref_skus.hl_per_unit=1.0` — STORED cols correct, VIEW hot; self-resolves on the held -V revert (#0/#0a5) → that revert is now URGENT, live consumers reading the VIEW see 100× cuv today.** Row 1303 STI4C-can mis-entry STILL OPEN (awaiting Kouros true-loss restatement). → RESUME #6b.**) Prior: **🏁 BOM-VALIDATION ARC KEYSTONE CLOSED — ④ DIRECT-SALES TRACKER MVP "Tap&Shop" SHIPPED+Opus-verified LIVE (page_key='tap-shop', ref_pages id=186, read-only logistics page, mig 291, commit `a13a9d0`; **topbar-visibility fix LANDED 2026-06-09 — min_role manager→operator data-only UPDATE+audit on id 186, Olivier user 19 now sees the link**; **access-ordering FOOTGUN now FIXED 2026-06-09 (was flagged deferred): `user_can_access()` explicit per-user grant overrides role floor — commit `aefbaad`, code-only, NO mig, no SOT/derivation/head impact; regression-clean (0 real granted=1 rows affected) → user-auth-admin-arc.md §resolution ORDER**); ①②③④ all done, ④ Phase 2 cage/COGS engine DEFERRED behind Shopify pickup-signal + cage anchor 2026-06-15. MIGRATION HEAD = 291 / NEXT FREE = 292. → RESUME #0 + #0b.** Prior: **🚚 EXPÉDITIONS / FULFILMENT v1 IS LIVE FOR THE WHOLE TEAM — mig 290 `290_marketing_access_preset.sql` APPLIED (parallel session; flipped `ref_pages.expeditions.is_active` 0→1, "Fulfilment v1 GO-LIVE, operator-accepted 2026-06-09"; mig 290 still UNTRACKED locally — not ours to commit). MIGRATION HEAD NOW = 290 / NEXT FREE = 291** [mig 289 `289_rri_scd2_open_uniqueness.sql` parallel-session = commit `a1c68dc`; mig 290 parallel-session = untracked]. **✅ FULFILMENT TASK #10 BOARD + FG-STOCKTAKE-EDIT FEATURE SHIPPED** [maltyweb main LOCAL-ONLY commits `24d510a` board + `41b9a69` edit; arc surface = 4 unpushed: `f97bcb9` multipack / `d1a0e40` mig288 / `24d510a` / `41b9a69`]: TODO ①(per-location fg_stock dim) + ②(in-page stock BOARD) DONE. `fg_stock_compute()` anchor rebuilt per PM ruling = latest count per (sku_id_fk, location_id_fk) via MAX(id), anchor_qty SUMMED across locations, anchor_date=MAX(counted_at)=2026-06-08 (was a future month-end — bug fixed); new `fg_stock_location_snapshot()` groups per location. **GATE PASS Σcards==Σanchor==7080** (Prod 550 / Logistique 5976 / Nausikraft 554 / Taproom 0). Stock PF = 4 location cards + TOTAL strip (physique + dispo-semaine + dispo-futur, the two −orders lines) + per-SKU table by display_family (multipacks split); pinned summary on Commandes. EDIT feature: admin/manager date-scoped FG-stocktake edit, all 7 PM design points ($canBackdate=is_manager()||is_admin() w/ counted_at→today coercion for operators; ?date= unlock; date-scoped prefill; cage inverse-convert; blank=skip/0=real-zero; no location-change; tombstoned-April untouchable; log_revision); board gate re-passed 7080/7080; +2 self-review fixes. **APRIL RETIRED: 41 pre-split April rows is_active=0 (audit-logged); PD8→0 (Kouros backfills via the edit feature); 81 active rows remain** (Prod 27/Logistique 28/Nausikraft 26/Taproom 0). gate ⑥ (back-date role-gate) now COMMITTED `41b9a69` (was LIVE-but-uncommitted). → fulfilment-expeditions-arc/README.md §TASK #10 + §2026-06-09 GO-LIVE. Prior 2026-06-09: (**✅ RECETTE TEMPORAL VERSIONING v2 (SCD2 write-side) SHIPPED+VERIFIED+PUSHED — commit `a1c68dc`, mig 289. RESUME #0c CLOSED.** The textbook close-then-insert was BLOCKED by UNIQUE `uq_recipe_mi_stage` (dates not in key → 1062 on first edit) — CORRECTS the old #0c "likely NO migration" guess: mig **289 `289_rri_scd2_open_uniqueness.sql`** WAS required (Option A): new STORED generated col `open_key = CASE WHEN effective_until IS NULL AND is_active=1 THEN 1 ELSE NULL END`, DROP `uq_recipe_mi_stage`, ADD UNIQUE `uq_rri_open(recipe_id,mi_id_fk,stage_key,boil_time_key,open_key)` → at most ONE open row per slot, unlimited closed history (MySQL ignores NULL open_key). Backfill: 164 rows stamped `effective_from='2025-01-01'`, `effective_until` NULL (verified 0 null_from / 164 open / 164 at genesis). Code: `app/recipe-ingredients-loader.php` gains `rri_close_version(PDO,$id)` (co-located w/ read predicate; read-side L174-175 UNCHANGED); `public/modules/salle-de-controle.php` 3 hop write-actions converted — `set_hop_stage`(L1534)+qty/stage edits → close-then-insert; `add_hop_addition`(L1659) → effective_from=CURDATE/until=NULL, resurrect resets dates; `delete_hop_addition`(L1882) → **CLOSE (effective_until=CURDATE), NOT tombstone** (is_active stays 1, historically readable); each txn-wrapped; display query L2148 gained `AND effective_until IS NULL`. Boundary: effective_from INCLUSIVE / until EXCLUSIVE / boundary=CURDATE() server-side (matches read-side; same-day double-edit → exactly the new row). 3 reviewer bugs fixed in-build (stale row.id after close→insert: server returns old_id+new id, JS updates row.id; resurrect resets dates; delete txn-wrapped). **Opus INDEPENDENTLY VERIFIED LIVE:** uq_recipe_mi_stage gone / uq_rri_open present / open_key STORED GENERATED ✓; backfill counts ✓; mig 289 in schema_migrations ✓; helper deployed ✓; 1062-avoidance + 3-asof boundary test PASS (agent self-cleaned); EMB r32 reads full 11-ingredient set at asof=today (no regression) ✓; **`ref_sku_bom` UNTOUCHED (2427 rows)** ✓. SCOPE: only the 3 hop actions write `ref_recipe_ingredients` today (no generic malt/mineral qty editor) — close helper is reusable so a future generic editor inherits SCD2 free. STILL OUT: version-history display UI in SDC; as-of awareness for form-fermenting.php dry-hop EXISTS check; v3 full retrofit unchanged. **NEXT in BOM arc = ④ Stage-2 direct-sales consumption tracker (LAST, long-pole, Shopify pickup-vs-shipped flag; cage anchor feeds it) + deferred WIP stage-split (after ④) + F2 follow-ups (finings saisie input; EMBV hl_per_unit; warehouse.php $glSt check). → walkthrough §F2 LIQUID APPLIED + project_recipe_versioning_temporal.md.**) Prior 2026-06-09 (**🔑🏁 F2 LIQUID BOM ✅ APPLIED LIVE — KEYSTONE ③ OF THE BOM-VALIDATION ARC IS DONE.** Full run (after EMBBU canary): **147 SKUs applied, 1260 deleted → 1557 liquid rows inserted, 0 parity violations, 0 apply errors.** Post-apply `ref_sku_bom`: Brewing/`liquid` **1557** (all currency='CHF'); Packaging/`packaging` 576; Brewing/`composite_liquid` 204; Packaging/`composite_packaging` 83; Brewing/NULL 7 (out-of-scope, cosmetic). Code shipped+pushed maltyweb `9012c8d`+`0956471`: `app/sku-bom-compile.php` `compile_sku_bom_liquid()` apply branch (per-SKU txn; DELETE `source='Brewing' AND bom_source IN(NULL,'liquid') AND mi_id NOT NULL`; INSERT proposed; **packaging/composite PARITY GATE** via new `_bom_packaging_composite_snapshot()` 1e-6-or-rollback; `apply_stats`; idempotent regenerate-at-apply) + `scripts/sku-bom-liquid-cli.php --apply` unblocked. **🔒 2 CONVENTIONS LOCKED (COGS-impacting): (1) liquid rows CHF-canonical** — `cost`=CHF (v_mi_cost WAC / oldest-invoice chf_unit), currency='CHF', price=round(cost/qty_per_unit,6); FIXES the latent EUR-summed-as-CHF bug the OLD liquid rows carried (all downstream `SUM(b.cost)` labels CHF); **(2) `slot_name` stores the STAGE** ('brewhouse'/'dry_hop') under UNIQUE `uniq_sku_ing_src_slot(sku_id,ingredient_raw,source,slot_name)` — keeps same MI in kettle+dry-hop distinct; all 6 readers audited, none need liquid slot_name=NULL. **Operator decisions ratified (AskUserQuestion): DIG PROC_YEASTVIT FIXED (ref_recipe_ingredients id=37 0.000354→8.0 g/HL, the ~22000× unit slip — direct SQL); DIV finings KEPT as gap-fill + FORWARD BUILD ITEM = add finings input to brewing/racking saisie forms so it flows observed; SPY/DIB floor 2025-10-07 ratified; gate-15 oldest-invoice ratified (27 subs). DGD MALT_PILSENER 18 kg/HL CLEARED — verified vs raw brew record (Galactic Drift double-IPA, 24.4 kg/HL total, single-brew), NOT an error.** Follow-ups (non-blocking): EMBV CHF/HL≈3423 packaging/hl_per_unit oddity (liquid correct 21.8); warehouse.php $glSt ~L564 one-brew÷batch-HL check; 7 cosmetic out-of-scope NULL rows. **NEXT = recette temporal versioning v2 write-side (RESUME #0c) before operator next edits a recette.** → walkthrough §F2 LIQUID APPLIED + RESUME #0.**) Prior 2026-06-08: (**MULTIPACK DISPLAY-FAMILY SPLIT ✅ SHIPPED+verified+committed `f97bcb9` [LOCAL-ONLY, not pushed]; mig 287 `287_ref_packaging_formats_display_family.sql` APPLIED — head=287 / NEXT FREE 288, PM live-verified.** **⬆️ SINCE: mig 288 `288_inv_fg_stocktake_row_hash_generated.sql` APPLIED + RECONCILED + PROVEN (row_hash STORED generated col + BOTH fk_fg_sku AND fk_fg_stocktake_location re-added ON DELETE RESTRICT, DELETE_RULE='RESTRICT' both live; reconciled via manual schema_migrations INSERT after a mid-run conn drop, migrate.php --status clean; expeditions.php INSERT drops row_hash from col list; rolled-back-txn PROOF = location edit auto-mutates hash → divergence structurally impossible; committed maltyweb main `d1a0e40` LOCAL-ONLY [prior multipack `f97bcb9` also unpushed]) → head=288 / NEXT FREE 289 (PM live-verified 2026-06-08; see RESUME #0a2 + fulfilment-expeditions-arc/README.md §row_hash §SHIPPED).** New per-format `ref_packaging_formats.display_family VARCHAR(32) NULL` (the PM-recommended mechanism, NOT a hardcode) seeds `'multipack'` on ids **19/20/21/23** = the curated cross-beer gift packs ONLY [Pack Découverte PD8, Xmas Pack, Pack Louis, Pack du Chef + future DGD]; **6×4 -4 SKUs + the 24-pack carton STAY Bouteille/Canette** [same-beer presentation, not a multipack]. `expeditions.php ?view=stocktake` LEFT JOINs + groups by `COALESCE(display_family, format)`, new "Multipacks" header after Keg; cage -X + `stocktake_scope` logic UNCHANGED; 153 active SKUs unchanged; **ZERO COGS/SOT impact — `fg_stock_compute()` keys on sku_id.** Today only PD8+XMASPACK render [scope='base']; PAC/PAL scope='none' hidden. FORWARD COUPLING: `display_family` is now the editable per-format display-grouping field for **task #12 stock-config admin** [alongside `ref_skus.stocktake_scope`] AND the **task #10 Stock PF rebuild MUST split multipacks there too — fg-stock.php grouping was deliberately NOT changed this commit; carry display_family into `fg_stock_compute()` then.** → fulfilment-expeditions-arc/README.md §Multipack display-family split.**) Prior same day: (**✅ F2 MULTI-BREW BASIS BUG FIXED + OPUS-VERIFIED [maltyweb commit `9012c8d`, NOT pushed; `app/sku-bom-compile.php` `compile_sku_bom_liquid`, no mig]: §2a GROUP BY +`COUNT(*) AS n_brews`→`$batchNBrews`; §2b malt+hops_kettle × n_brews per-batch BEFORE MAD (dry-hop/minerals/gap-fill/oldest-invoice/packaging untouched; n_brews defaults 1). apply-gate-16 (a)-(e) ALL PASS [EMB total malt 21.03 kg/HL vs recette 22.83 ≈ 8% real drift; STI 17.52; DIG byte-UNCHANGED 10.27 = regression guard; 0 errors/0 unresolved; EMB b181 347×4÷124.5=11.1486 EXACT]. CHECK: N_brews = cooling-row count reliable; the correct scaling ALREADY EXISTS in warehouse.php/warehouse-export.php WIP costing — F2 was the LONE omitter (no shared helper). apply-gate-16 RESOLVED-pending-operator-rewalk; digest REBUILT off corrected preview, operator resumes walk. 2 follow-ups logged: DGD 18 kg/HL = single-brew data-entry issue NOT basis bug; warehouse.php $glSt ~L564 may share one-brew÷batch-HL mismatch (review after F2). → walkthrough §MULTI-BREW + RESUME #0.**) Prior same day: (**EXPÉDITIONS DATA-FIX ✅ RECORDED [PM-verified live; no code/schema]: (1) `ref_sites` renamed to canonical display names id1 «La Nébuleuse - Zgeg (Production)» / id2 «La Nébuleuse - Zeubi (Logistique)» — site_type + ids untouched (Zgeg=Renens/prod, Zeubi=Crissier/logistique; 3=Taproom/pos, 4=Nausikraft/consignment unchanged); (2) corrected 06-08 stocktake misfile — moved 28 `inv_fg_stocktake` rows loc1→loc2 (count-guarded, one txn as kouros id1, log_revision), final = loc1:41 April anchor (month_closed='2026-04') / loc2:28 June (2026-06-08) / loc3-4 empty → fg_stock anchor now per-location, see R4 → fulfilment-expeditions-arc/README.md §2026-06-08 ref_sites.**) Prior same day: (**À-VALIDER → TRIAGE MERGE ✅ SHIPPED+COMMITTED+LIVE-PROVEN [maltyweb main `484cc6a` "feat(triage): À-valider queue as triage tab + fix delivery double-write", 8 files]: triage `?tab=valider` tab + shared partial `partials/valider-queue.php` (invoice-validate.php → thin wrapper) + Lenzlinger double-write CLEARED via Fix A list-query NOT EXISTS + Fix C triage-alias stamps validated_at/_by at $openLeft===0 chokepoint [reject.php NOT stamped]. **WATCH-ITEM CLEARED — live Valider PROVED OUT: operator validated clean Tinguely invoice (doc_invoices.id=209, FAC 01-26050180) in prod → commit worker ran E2E [recap-lines.php → /api/invoice-validate.php → ingest-one-local-commit.sh → delivery_write_plan replay], 7 Active inv_deliveries written (source Invoice-OCR), validated_at + validated_by=1 stamped, HT 641.61 reconciles.** Bug fixed during proof: tsx cold-start can exceed UI 2-min poll cap → successful commit showed false red "✗ Échec"; FIXED in public/js/invoice-validate.js (poll cap 2→4 min + final status re-check on timeout + neutral "Toujours en cours / Recharger" state). Also fixed same commit: `.iv-card-overlay` CSS bug (unconditional display:flex overrode [hidden], bleaching every card on tab+standalone) → gated on `:not([hidden])`. 2 backlog flags REMAIN: commit-side precheck "b" deferred + TWO recap-shape sources [valider-queue.php batch query ≠ canonical fetch_recap_for_invoice]; bulk "Tout valider" fan-out throttle still backlog (N concurrent cold-start workers unthrottled — the false-red bug is the single-invoice canary for it). → RESUME #12b + gate-arc §À-VALIDER MERGE — SHIPPED+PROVEN.**) Prior same day: (**INGESTION VALIDATION GATE + SIE/SIL ENERGY WEB-PATH ✅ SHIPPED+LIVE+E2E-VERIFIED — migs 282/283/284, head now 284 / NEXT FREE 285; worker stages a write-plan, NOTHING auto-writes inv_deliveries anymore, operator validates via « À valider » → `--commit` replays the same writer; Drive sourcing RETIRED; one untested seam = live browser Valider-click → RESUME #12 + ingestion-validation-gate-arc.md §SHIPPED RECORD.**) Same day: (**F2 LIQUID = HANDOFF CHECKPOINT — dry-run REGENERATED post-`9fa60a7` [gate-15 incl.; 142 scope / 79 gain / 49 lose / 14 no-change / 0 errors / 0 unresolved], digest BUILT [`reports/f2-liquid-review-digest-2026-06-08.md`, 1104 lines / 18 beers], operator now DRIVES the walk → ONE apply dispatch [apply predicate NOT YET BUILT]; 2 MUST-DECIDE-BEFORE-APPLY = DIG PROC_YEASTVIT ~22000× scale error + DGD MALT_PILSENER 18.04 kg/HL → RESUME #0 + walkthrough §F2 REVIEW HANDOFF CHECKPOINT.**) Prior same day: (**EXPÉDITIONS PRE-FLIP COMPLETE overnight — merge pipeline `9803849`/`6dd5521` + stock hints `134bbdf` + tour content `a59d94d` + preset pre-wired; main==origin @ `6dd5521`; only operator morning runbook remains → RESUME #10(b) + fulfilment-expeditions-arc/README.md §2026-06-08 OVERNIGHT.**) Same day: (**VISITE GUIDÉE CHAPTER EXPANSION `91f8c82` SHIPPED+LIVE+PUSHED — 25 steps production / 11 logistics, vetoes grep-verified; queued polish: form-packaging.php raw-field-name helper text → §SHIPPED-ARCS register + user-auth-admin-arc.md §VISITE GUIDÉE + build-history changelog-2026-06.**) Prior 2026-06-07: (**EVENING CLOSE-OUT, latest sync: interstitial COMMITTED `94d5a0d` as-built=spec [login.php wiring since COMMITTED via `ca65c22` — closed]; `f7fe151` stale MT_STUBBED_DOMAINS const hid 21 trackers from picker → truth now `kpi_stub_domains()` in kpi-handlers.php + viewer min_role floor 12 pages; `b838cca` terminology [Soutirage→Transferts; O₂ 34/35→packaging]; `6f0e773` fermentation mini-tranche → **55 trackers LIVE**; recap cron ACTIVE `1d8672b` [house convention: PHP crons run as `maltytask` NOT www-data]; viewer seed-tuning applied `803d3f8`; smoke E2 PASS via permanent smoketest acct id=16 [creds secrets/maltyweb-smoketest.env]; 820px topbar overflow QUEUED→parallel topbar a11y arc, do NOT double-edit; **PUSH ✅ DONE (post-midnight sync 2026-06-07, PM-verified `git log origin/main..main`=0): main == origin @ `6f0e773`; login.php COMMITTED (carried by `ca65c22`, tour-redirect + interstitial wiring both in) — divergence CLOSED, clean-checkout deploy safe.** → kpi-tracker-catalog/README.md + user-auth-admin-arc.md §EVENING CLOSE-OUT + build-history/changelog-2026-06.md.**) (LATE NIGHT — **VISITE GUIDÉE first-login onboarding tour SHIPPED+LIVE `ca65c22` + mig 280** [tour_seen_at; default landing now mon-tableau; → user-auth-admin-arc.md §VISITE GUIDÉE]; **mig-280 collision REMEDIATED, head=280, NEXT FREE=281**. Earlier same day: ui-ux Phases 0+1+2+C2 closed, svg-tanks.php sole vessel library; Mon tableau v2 landed). **UI-UX ARC NOW FULLY CLOSED — Phase 3 landed+pushed `e05b18c`/`e679efa`/`aba178a` (RESUME #9 ✅; 4-item residual backlog incl. bom-review post-F2 carve-out → arc file §RESIDUAL BACKLOG).**. **🏁 BOM WALK COMPLETE (2026-06-07): EVERY active SKU walked+operator-validated** — ALT/EPH-family/EMB/MOO/DIV-family/STI/SPY/DOA + closing batch EST/DOC/DGD/composites (PAC/PAL confirmed; COLLAB12/COLLAB4PACK semantics; 5 composite gaps → batch). **CAGE FORM SHIPPED** (`91ce8c0` deployed — 'X' suffix, operator-typed bottle counts; 2026-06-08 = cage inventory → missing -X SKUs → `inv_fg_stocktake` anchor). **BUILD SEQUENCE STATE (2026-06-07 PM): ① WAC switch ✅ APPLIED (−13.5%) → ② template-enrichment ✅ EXECUTED (Dispatch A compiler `c7d2c3e`+`052a8cb`; Dispatch B `c060b54` = migs 272 data + 273 schema [`ref_sku_bom.slot_name` + 4-col unique key; choices `qty_per_unit` DECIMAL(14,6)]; TEA-WAC lump-sum poisoning incident root-fixed; ZEPC recipe-altitude scotch defect fixed via choice 47; final verified packaging 528 rows/2001.37 CHF, brewing byte-identical, Gate B ALL PASS, idempotent) — FIX-7 keg-share ✅ EXECUTED per PM ruling (maltyweb `b65cfe3` = mig 274: `derived_from_format_id` BIGINT UNSIGNED FK self-ref seeded 16/17→15 keg-F verified live; generic gate Arm B inherit-parent-dbc; metaQuery unified UNION derived-branch; auto-detect Arm 4; 26 SKUs × 2 rows, ALTP25 0.001275+0.000820; packaging now **2001.45 CHF / 580 rows**, idempotent, others byte-identical) — **② 100% COMPLETE, operator SIGNED OFF the aggregate delta ("let the new costs flow")** → ③ F2 liquid — **③-prereq window-guard ✅ (mig 275, `5bbc601`) AND ③-COMPILER ✅ NUMERICALLY VERIFIED 2026-06-07 (commits `888473b`/`f99fa2a`/`c9ff4ef`+`2a803d4`/`2d59b4e`; 4 incidents found+FIXED — v1 dry-hop cross-recipe contamination, per-MI-window never-age-out, 2×MAD-on-consistent-data + missing-DH basis pollution; Opus hand-computed = preview EXACT, EMBF dry-hop 566.38 g/HL; ALL dry-run only, ref_sku_bom untouched 2135/2250.003568) → NEXT = joint per-beer review → ③-APPLY (gates 1-14, walkthrough §F2 COMPILER VERIFICATION RECORD + §F2-WINDOW-GUARD RULING)** → ④ Stage-2 tracker LAST.** Whirlpool: mig 276 appended `'whirlpool'` LAST to `hop_addition_stage` (+`2a803d4`; stage ordering = explicit FIELD()/rank maps NEVER ENUM order; operator re-attributing stages in SDC live). IN FLIGHT same dispatch: SDC recette UX batch (hops always g / process-order / qty-per-BRASSIN input) + **P3 recap emails CONSUMING MIG 277**. mig head 2026-06-07 LATE NIGHT (live-verified ~23:30): **280_users_tour_seen_at APPLIED — the 278 collision is REMEDIATED (rename→280 + paired schema_migrations UPDATE + manual rm of the stale 278 file on the VPS: rsync deploy does NOT delete removed files); 277/278_kpi_seed_v2_batch/279 also applied. ⚠️ POST-MIDNIGHT VERIFY: untracked local `281_ref_customers_contact_cols.sql` (parallel CRM/customer-bootstrap session, NOT yet on VPS — live head still 280) holds 281 → NEXT FREE = 282.** **✅ MON TABLEAU v2 LANDED 2026-06-07 evening** (`2a8f2b5`/`8800a9e`/`db2da33`/`b66244a`/`e8f5336` + mig 278_kpi_seed_v2_batch — dense grid + category grouping, 21 production handlers, REAL MP days-of-supply Opus-verified 147.0 j, honesty sweep, **51 trackers live**, 15 sub-pages ACL-gated, recap-guard session-payload fix #3; → kpi-tracker-catalog/README.md §2026-06-07 EVENING + user-auth-admin-arc.md). **✅ EXPÉDITIONS ARC PM-BRIEFED + RECORDED 2026-06-07** (pre-approval brief approved by Kouros; full v1 LIVE: mig 279 `bbc49fe` + module `f9535ef` + Commandes dashboard `af6e0ea` + Stock PF `fg_stock_compute()` `4a8f49e`/`b676c9b`; page HIDDEN `is_active=0` = go-live gate; customer bootstrap GATED on CRM xlsx; 06-08 inventory re-anchors the depletion gap; parallel-session note: mig 280 taken by visite-guidée, Expéditions deploys selective-rsync'd) → [fulfilment-expeditions-arc/README.md](maltyweb-pm-memory/fulfilment-expeditions-arc/README.md). Full record + §STEP-② BUILD PLAN → [sku-bom-validation-walkthrough/README.md](maltyweb-pm-memory/sku-bom-validation-walkthrough/README.md) §CLOSING BATCH; composite gaps mirrored in [sku-decomposition-tree.md](maltyweb-pm-memory/sku-decomposition-tree.md).** **✅ P2b PERSONAL DASHBOARD SHIPPED (maltyweb main, commits 99d5d54 + 2c37c9d, built to the 6 constraints):** `public/modules/mon-tableau.php` (min_role=viewer, registered in `ref_pages` via idempotent upsert) reads `user_kpi_selections`, runs `kpi_dispatch()` per tracker, renders via the NEW shared `public/js/kpi-charts.js`. Chart primitives EXTRACTED out of `wort-kpis.js` → `kpi-charts.js` (`window.KpcCharts`, 9 viz_types); wort.php loads kpi-charts.js BEFORE wort-kpis.js (both `defer`) — wort page verified unchanged. Tracker picker = server-filtered by domain×role×data_ready; POST rebuilds the allowed-set server-side (`mt_build_allowed_set`) + REFUSES out-of-scope selections (forged manager-tracker POST by viewer correctly rejected); CSRF+log_revision+PRG; NO new compute path. 4 `utilities` data_ready=1 trackers map to a STUB handler → detected, excluded from picker/render, surfaced to an ADMIN-ONLY notice (constraint #1). Feed-2 WAC-divergence sub-flag added to `bom-review.php` (0 rows today, lights up when real) per the interim-guard ruling. **Role-filter test: viewer=0 selectable / manager=30 / admin=39 trackers** — viewers get an EMPTY personal dashboard (NO data_ready=1 tracker has min_role='viewer'; SEED-TUNING question for next seed-touch, NOT a bug). **MIGRATION HEAD UNCHANGED = 270 (P2b needed no migration, verified 2026-06-06); NEXT FREE = 271 (RESERVED for P3 recap mig `user_kpi_recap_subs`).** **✅ BOM-REVIEW BROWSE/AUDIT VIEW SHIPPED (maltyweb main, commit 43022d7, LIVE 2026-06-06, ZERO mig)** — second in-page tab "Parcourir les BOM" on `bom-review.php`; 153 active SKUs grouped classification→subtype (`ref_recipes.classification`/`.subtype` via `ref_skus.recipe_id→ref_recipes.id`), format-filterable (`ref_packaging_formats.display_name`); LEFT JOIN census 99/34/6/7 + 7 composite + 2 format-NULL = NO drop; per-SKU provenance tier + snapshotted `ref_sku_bom.cost` (no recompute on render); edits reuse the SAME `set_recipe_binding`/`set_sku_choice`/`set_mi_price` + `sdc_recompile_recipe_packaging()` path (recipe-25 parity 0); confirm-blast-list + inline parity + closed-month COGS warning; brewing read-only. **CLOSES the BOM REVIEW SURFACE ARC** (defect tab + browse tab + WAC-divergence guard). **⚠️ ARCHITECTURAL RELOCATION: `sdc_recompile_recipe_packaging()` + `sdc_flash_bom_result()` MOVED out of `salle-de-controle.php` (a page) INTO `app/sku-bom-compile.php` (the lib), `function_exists`-guarded, verbatim; both pages now `require_once` the lib (salle-de-controle:27, bom-review:34). CANONICAL HOME of the sdc_* recompile helpers is now `app/sku-bom-compile.php`, NOT salle-de-controle.php** — verified only those 3 files reference them, all lint clean. → packaging-bom-model/README.md §SHIPPED BROWSE. NEXT BUILD = **P3 KPI recap emails** (PHP CLI sharing `kpi-handlers` + live `send_mail`, mig 271, cron-disabled-pending-smoke, cadence UI) — **PM RULING: P3 WAITS ON ONBOARDING** (can't be smoke-tested end-to-end until users have emails + tracker selections; agreed with operator). → [kpi-tracker-catalog/README.md](maltyweb-pm-memory/kpi-tracker-catalog/README.md). **PRIOR (2026-06-06): THREE LANDINGS:** (1) **DIV33C can-fix COMPLETED at the durable altitude** — NOT a binding repoint; a Tier-1 per-SKU choice `ref_sku_packaging_choices` id=11 (sku_id=14, slot='can', mi_id_fk=617 PKG_CAN_ALU_33, effective_from=2026-06-06). PM-verified-by-operator: fresh `--apply` recompile of recipe 25/DIV33C holds 617, parity_violations=0, idempotent. `ref_recipe_packaging_bindings` id=30 LEFT at 616 (only exercised via override since DIV33C is the only Diversion can SKU; flagged to repoint to PKG_CAN_DIV id 626 IF a 50cl Diversion can is ever packaged). inv_consumption corrected (CAN_ALU_50 206369→192065, CAN_ALU_33 0→14304, total preserved). +3 WAC priced: PKG_PALETTE_NOMOQ 30 EUR / PKG_INTERCAL_NOMOQ 1.50 EUR / PKG_TOP_FRAME_NOMOQ 10 EUR (cost_basis='wac'). (2) **BOM REVIEW SURFACE SHIPPED** (commit 7730bc4) — `public/modules/bom-review.php` (1256L)+css+js; `require_page_access('bom-review')` (ref_pages page_key=bom-review, min_role=admin, domain=admin, sort=185); defect-driven 3 feeds; edit-altitude rules (binding/per-SKU choice/ref_mi price); reuses `sdc_recompile_recipe_packaging()` (NO parallel recompile path); brewing rows read-only-w/-provenance; CSRF+log_revision+PRG; NEVER writes ref_sku_bom; altitude round-trip tested (DIV33C re-assert→recompile→617, parity 0); reconcile-pages 0 new drift. (3) **P2a KPI FRAMEWORK SHIPPED** (commit 0a99b8b) — migs 269 `ref_kpi_trackers`(268 trackers seeded: 43 data_ready, 6 hero) + 270 `user_kpi_selections`(sparse, CASCADE FK), both schema_meta'd; `app/kpi-handlers.php` registry dispatches on source_domain w/ whitelisted params; IMPLEMENTED: wort (Cooling-row canonical, matches wort.php:187) / cogs (consumes pipeline JSON, never recomputes) / ops_health / rm_procurement; STUBBED (return stub:true, never error): tanks/racking/packaging/fg_stock/sales/qa_qc/equipment/logistics. **MIGRATION HEAD NOW = 270 (266–270 all applied live, 0 pending, verified 2026-06-06); NEXT FREE = 271.** NEXT BUILD = **P2b** (`mon-tableau.php` + `kpi-charts.js` extract + tracker-selection UI + register in ref_pages) — CONFIRMED. → [kpi-tracker-catalog/README.md](maltyweb-pm-memory/kpi-tracker-catalog/README.md) + [packaging-bom-model/README.md](maltyweb-pm-memory/packaging-bom-model/README.md) §ACTIVE BUILD. **✅ SHIPPED: packaging-consumption 24× SCALING DEFECT FIXED+APPLIED+COMMITTED (maltytask `942431e`).** `scripts/derive-packaging-consumption.ts` re-bases the carton denominator on `ref_skus.units_per_pack` (option b, BOM-self-consistent) + FAIL-LOUD guard; rebuilt inv_consumption packaging DELETE 6504/INSERT 8565/0 dupes; PKG_BOT_CROWN_CAPS 138M→5.83M; caps≈bottles≈labels, 4pack=bottles/4. NO mig. Independent VP-Brewing audit (`/home/kluk/projects/maltyweb/docs/packaging-consumption-audit.md`) verdict: **engine TRUSTWORTHY FOR COGS** (arithmetic hand-traced). Remaining findings are all REFERENCE DATA / coverage, NOT the engine — PM verdicts + sequencing recorded in [rm-retro-prereq-chain.md](maltyweb-pm-memory/rm-retro-prereq-chain.md) §PREREQ 2: **(a) ❌ DIV33C BOM → wrong can body PKG_CAN_ALU_50 (correct _33 exists, COGS-neutral today but FIX NOW, unambiguous); (b) ⚠️ 43 unpriced PKG MIs = 681k units @ CHF0 — 39/43 are `no_basis` (CATALOG DATA-ENTRY, not a WAC re-run; only 3 WAC-able+1 catalog); (c) ⚠️ contract/WL gap = known Phase-2; (d/e) BLO4 + dropped-losses defer.** PM ruling: do (a) + price-entry pass BEFORE Phase-2 KPI dashboard ships COGS trackers (gate packaging-COGS trackers on `data_ready`); non-COGS KPI work proceeds in parallel. Does NOT unpause the RM retro (still GATED on commissioning + Stage-2). **✅ NEW: User-account deepening arc Phase 1 (per-page access control) SHIPPED+LIVE — migs 266 `ref_pages`(18 rows, retired hardcoded `$modules`; topbar renders from table)/267 `ref_access_presets`+`ref_access_preset_pages`+`users.access_preset_id_fk`(3 presets)/268 `user_page_access`(sparse override); `app/auth.php` `user_can_access()`/`require_page_access()`; `reglages-generaux.php` preset-picker + per-user tri-state matrix + page-registry admin (sec=access). Commits c065743/c747006/2466869. MIGRATION HEAD NOW = 268, NEXT FREE = 269 (verified live 2026-06-06, 0 pending). Phase-1 STILL-OPEN: reconcile CLI (FS↔ref_pages drift) + onboard 9 email-less users (parallelizable, NOT a KPI blocker). Phase 2 = KPI personal dashboard → [kpi-tracker-catalog/README.md](maltyweb-pm-memory/kpi-tracker-catalog/README.md) (catalog at docs/kpi-catalog.md, 250 trackers, PM-blessed seed+handler model).** **✅ BUILD-STATE: the entire fermenting/saisie/tank-sim correction chain + packaging parallel-run fix is COMMITTED + PUSHED.** **maltyweb main → HEAD `815e725` (PUSHED, in sync w/ origin):** `a22d2d8` tank-sim [recipe_id-keyed drain + mig263 EPH + mig265 parallel-run + recipe-resolver + packaging-loss-types] → `a69091a` saisie [edit modules all 4 forms + racking fatal fix + form-framework sticky-on-error] → `815e725` fermenting [co2o2 reads + mig259 dryhop ENUM + mig260 purge pressure]. **maltytask main → HEAD `dd6ca28` (PUSHED):** normalize-rawdb.py Mode-A parallel-run fix. This supersedes all the older "uncommitted / committed-not-pushed" notes that used to fill this header. **STILL-OPEN (carried forward): (1) per-recipe historical re-derivation for batches 5/63/169 (the mig265 reattribution); (2) Node `parse-tank-simulation.ts` STILL name-keyed (COGS/WIP-impacting — port the `(recipe_id_fk,batch)` keying lesson, RESUME POINT #3).** Auth arc (HEAD `993d5ae`, migs 261/262/264) was a SEPARATE prior commit chain — pushed only if operator confirmed separately. (Historical "NEXT FREE MIG" claims in this paragraph are superseded — the CURRENT head/next-free lives ONLY in the dated "mig head" sentence above + §CURRENT BUILD-STATE.)

