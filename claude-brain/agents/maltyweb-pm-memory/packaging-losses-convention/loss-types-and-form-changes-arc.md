> Extracted from [packaging-losses-convention.md](../packaging-losses-convention.md) during the 2026-06-05 compaction. Holds the DATA-DRIVEN LOSS TYPES arc (A-LT1..A-LT5, A-LT1/mig 234 DONE, view-codegen A-LT2 PAUSED) + the 4 OPERATOR PACKAGING-FORM CHANGES arc (-V/-F cuv-vs-keg model; P1+PKG-CLIENT mig237+P2 cuve-réutilisée mig238 SHIPPED; P3/C1 derived-QA) + LINER CONSUMPTION deferred-wiring (mig 239 captured identity) + the IN-TANK/IN-FILLING CO₂/O₂ REMODEL arc-split + the SUPERSEDED lot-day-read design lineage + OPEN carry-overs. Read when touching the loss-types catalog/view-codegen, the packaging-form -V/-F changes, liner consumption, or the CO₂/O₂ in-tank-vs-in-filling model.

## 🟡 NEW ARC 2026-05-31 — DATA-DRIVEN LOSS TYPES (operator requirement, DESIGNED not built)
Operator verbatim: *"these losses variants should be part of general settings packaging in zeppelin page and should allow for créer nouvelle perte and allow for selective loss building and not be hardcoded. Creating a new loss should automatically create the section in packaging form."* The 9 hardcoded dispositions (migs 231/233) become DATA-DRIVEN, operator-managed from the Zeppelin "Données générales" surface. **PM DESIGN VERDICT (given to orchestrator 2026-05-31, design-only, NOT built):**

**KEYSTONE DECISION — wide columns + CODEGEN'D view, NOT EAV, NOT a runtime-dynamic view.** The dynamism lives in 2 layers only: (a) a seeded `ref_packaging_loss_types` catalog driving form rendering + CRUD UI; (b) a codegen'd `v_bd_packaging_v2_vendable` regenerated from the catalog on every settings-save. Per-session counts STAY as wide columns on bd_packaging_v2 (added on-demand by the "create loss" action via `ALTER ADD COLUMN` INSTANT). **Why not EAV:** just shipped 231/233 as columns; 8 downstream readers consume the 3 computed view columns; EAV forces a GROUP-BY child-table aggregate view for ZERO user-facing gain and rewrites the readers. **Why not runtime-dynamic view:** the only data-driven path with wide columns is regenerating the column list = codegen; produces the SAME fast per-row CASE view we have now. RECORD so a future session doesn't "normalize" to EAV.

**`ref_packaging_loss_types` attribute set (FULLY covers the 9 + the A3 6th):** `code`, `label_fr`, `active`, `sort_order`, `is_system` (guard: the 9 seeded types = relabel/reorder/deactivate ONLY, no delete/no column-drop), `measure_unit ENUM('units','litres')`, `liquid_fraction DECIMAL(3,2)` (1.0 full / 0.5 half / 0.0 material — half-filled's 0.5 lives HERE not a flag), `affects_vendable`, `is_taxed`, `counts_as_loss`, `goes_to_segregated_stock` (taproom), `run_type_applicability SET('bot','can','can33','keg','cuv')`, `column_name VARCHAR(64) NOT NULL UNIQUE` (bridge to the storage column — view-codegen reads it), and **`bom_treatment ENUM('full','minus_crown_cork','material_only','none')` — LOAD-BEARING, MISSING from the operator's instinct list.** A plain `is_material_scrap` boolean CANNOT express "full BOM minus crown cork" (sans-capsule) — if a build agent uses a boolean, sans-capsule BOM silently reverts to full → wrong packaging consumption → wrong COGS. `is_material_scrap` is derivable (`bom_treatment='material_only'`). **QA library/analyses need NO special flag** — fully expressed by `counts_as_loss=0, affects_vendable=1, is_taxed=0` (pulls a unit, not a loss, not taxed). The 3 view outputs each read a DIFFERENT subset of the booleans → that orthogonality is exactly what makes codegen work.

**9-type seed → reuse existing columns (NO new ALTER for incumbents):** invendable→`unsaleable_units`(u,1.0,tax Y,loss Y,vend Y,bom full); perte-liquide-autre→`loss_untaxed_full_units`(u,1.0,tax **N**,loss Y,vend Y,bom full — only diff from invendable = tax); sans-capsule→`loss_uncapped_units`(u,1.0,tax N,loss Y,vend Y,bom **minus_crown_cork**); half-filled→`loss_half_filled_units`(u,**0.5**,tax N,loss Y,vend Y,bom full); bibliothèque QA→`qa_library_units`(u,1.0,tax N,**loss N**,vend Y,bom full); mesures QA→`qa_analyses_units`(same); perte liquide fût→`loss_keg_liquid_l`(**litres**,tax N,loss Y,vend Y,bom none); fût taproom→`taproom_keg_l`(litres,tax **Y**,**loss N**,vend Y,**segregated Y**,bom none); capuchon fût→`loss_keg_save_units`(u,0.0,all-N,bom **material_only**).

**Settings surface = EXTEND `public/modules/reglages-generaux.php`** (verified live = the "Données générales" Zeppelin admin page, 866 lines, admin-gated `is_admin()` 302 + csrf_verify→validate→write→log_revision→PRG; ALREADY has a `ref_sites` operator-editable CRUD section = the EXACT template). Add `?sec=packaging-losses` cloning the ref_sites section. Create/edit calls a thin `app/packaging-loss-types.php` helper that owns the ALTER + `pkg_regenerate_vendable_view()` (same way the module leans on `app/settings-helpers.php`). Consistent with supplier-fiche / capacités / white-label `ref_white_label_clients` operator-editable-ref pattern.

**Form auto-gen:** `form-packaging.php` injects `window.PF_LOSS_TYPES` (same window.JSON pattern as `PF_CANDIDATES`); `packaging-form.js` replaces the 3 hardcoded const arrays (`BOTTLE_DISPOSITION_FIELDS`/`BOTTLE_SCRAP_FIELDS`/`KEG_DISPOSITION_FIELDS` @ L285/295/309) with the injected JSON, filtered per row's `run_type` via `run_type_applicability` membership; `updateRowDispositionGroups` (L412) stays, iterates catalog nodes; `fieldHtml(f)` already renders from a descriptor. POST read-loop + `bd_upsert` payload become catalog-driven. **Make `compute_packaging_vendable_hl()` catalog-driven at RUNTIME** (it's per-submit, can loop the active types cheaply); keep the SQL view CODEGEN'D — both converge on the same arithmetic, test against each other (the A1/A3 "fn+view in lockstep" discipline → "both derive from the one catalog").

**Downstream readers of the 3 view columns (verified 2026-05-31, must need ZERO change if output contract holds):** maltyweb `public/modules/form-packaging.php`, `public/modules/packaging.php`, `app/loss-metrics.php`, `app/sb-board.php`, `app/tank-simulator.php`; maltytask `lib/beer-tax.js`, `scripts/parse-tank-simulation.ts`, `scripts/build-efficiency-data.ts`. The view's output contract (`vendable_units`,`vendable_hl`,`beer_tax_base_hl`,`loss_kpi_hl`,`run_type`,`sku_id_fk`,`hl_per_unit`,`units_per_pack`) is FROZEN — codegen changes only the internals.

**ATOM SEQUENCE (A-LT1 DONE as mig 234; NEXT FREE MIG = 235; re-check --status at build-start):**
- **A-LT1 mig 234 — 🟢 DONE + LIVE + COMMITTED 2026-05-31 (commit `f5685b3`, NOT pushed per operator commit-don't-push).** CREATE `ref_packaging_loss_types` (17 cols, utf8mb4_unicode_ci, UNIQUE on code + column_name, run_type_applicability as SET, bom_treatment ENUM, NO FK/CHECK) + 9-incumbent seed pointing at existing bd_packaging_v2 columns, `is_system=1` all. schema_meta row added (class=reference, writer='web (admin)', policy=allowed). **VERIFIED read-back byte-correct:** COUNT=9; only is_taxed=1 rows = invendable + taproom; half_filled liquid_fraction=0.50; sans_capsule bom=minus_crown_cork; capuchon bom=material_only; is_system=1 all. **234 recorded in schema_migrations.** `v_bd_packaging_v2_vendable` byte-IDENTICAL to its mig-233 form (untouched) — catalog is INERT, ZERO behaviour change, operator's packaging backlog unaffected. Material scraps + `loss_liquid_other_units` correctly NOT seeded (per scope ruling). **NEXT FREE MIG = 235.** ORIGINAL SPEC (verified against live view dump + ref_sites DDL template, 2026-05-31):
  - DDL: `int unsigned` PK; charset `utf8mb4_unicode_ci` (matches ref_* family, avoids 0900_ai_ci JOIN-collation mismatch); cols `code`(uq)/`label_fr`/`column_name`(uq — bridge to bd_packaging_v2 storage col, NOT a row FK)/`measure_unit ENUM('units','litres')`/`liquid_fraction DECIMAL(3,2) DEFAULT 1.00`/`is_taxed`/`counts_as_loss`/`affects_vendable`/`goes_to_segregated_stock`/`bom_treatment ENUM('full','minus_crown_cork','material_only','none')`/`run_type_applicability SET('bot','can','can33','keg','cuv')`/`is_system`/`active`/`sort_order smallint unsigned`/`notes`/timestamps/`updated_by`. UNIQUE on BOTH code + column_name. **NO CHECK, NO FK** → zero MySQL-8 gotchas.
  - **`run_type_applicability` stored as SET** (not CSV/JSON/ENUM) — A-LT4 filters per row via FIND_IN_SET; mirrors bd_packaging_v2.run_type domain exactly.
  - **9-SEED ATTRIBUTES DECODED FROM LIVE `v_bd_packaging_v2_vendable` DUMP (byte-for-byte reproduction):** subtracted-from-vendable_units→affects_vendable=1; subtracted-from-beer_tax_base_hl→is_taxed=0 (NOT subtracted→is_taxed=1); added-to-loss_kpi_hl→counts_as_loss=1. Rows (code|column|unit|frac|taxed|loss|vend|seg|bom|run_types|sort): invendable|unsaleable_units|units|1.0|**1**|1|1|0|full|bot,can,can33|10 · loss_untaxed_full|loss_untaxed_full_units|units|1.0|**0**|1|1|0|full|bot,can,can33|20 · sans_capsule|loss_uncapped_units|units|1.0|0|1|1|0|**minus_crown_cork**|bot,can,can33|30 · half_filled|loss_half_filled_units|units|**0.5**|0|1|1|0|full|bot,can,can33|40 · qa_library|qa_library_units|units|1.0|0|**0**|1|0|**full**(pulls real filled+labelled unit)|bot,can,can33|50 · qa_analyses|qa_analyses_units|units|1.0|0|**0**|1|0|full|bot,can,can33|60 · loss_keg_liquid|loss_keg_liquid_l|**litres**|1.0|0|1|1|0|none|keg,cuv|70 · taproom_keg|taproom_keg_l|litres|1.0|**1**|**0**|1|**1**|none|keg,cuv|80 · loss_keg_save|loss_keg_save_units|units|**0.0**|0|0|**0**|0|**material_only**|keg,cuv|90. ALL is_system=1, active=1.
  - **KEY MATH CONFIRMATIONS:** invendable vs loss_untaxed_full differ ONLY on is_taxed (invendable NOT subtracted from beer_tax_base arm = stays taxed; untaxed_full IS subtracted). QA holds = `bom_treatment='full'` NOT 'none' (a library bottle IS a real consumed unit; only counts_as_loss=0 makes it neutral). half_filled fraction 0.5 lives in `liquid_fraction`, view applies ×0.5 in all 3 arms.
  - **MATERIAL SCRAPS OUT OF A-LT1 SCOPE (confirmed):** loss_4pack/wrap/label/crown_cork/can_lid/container/collar are NOT in the 9-seed — they touch NONE of the 3 view arms (A1 removed all 11 subtractions), are BOM-consumption-only (material_only), and belong to a FOLLOW-ON seed after A-LT2 proves the core arithmetic equivalence. Seeding them now wrongly forces A-LT2 to model material consumption pre-gate.
  - **`loss_liquid_other_units` RULING (operator removed its form input 2026-05-31):** do NOT seed it (not a managed disposition — legacy generic sub-unit residual bucket). Live view STILL subtracts `/100` in ALL 4 arms (keg+bottle, vendable_hl AND beer_tax_base_hl) — now a dead-input/live-subtraction divergence. A-LT2 codegen drops the term; equivalence HOLDS because the form no longer writes it → every live row =0/NULL → `/100`=0 contribution. A-LT2 must assert equivalence on real rows + document the dropped dead term. Logged to scrapping-backlog: drop view subtraction + column once A-LT2 confirms 0 across all live rows.
  - schema_meta INSERT: class=reference, writer='web (admin)', policy=allowed (matches ref_sites; NOT 'blocked' like dbc_* pure-seed — this catalog is operator-editable via A-LT3). is_system note in upstream_hint (G2 guard).
  - **A-LT1 ships ZERO behaviour change** — catalog INERT, no reader until A-LT2; hardcoded view (mig 233) stays authoritative; form/CRUD untouched. RULE-2 must confirm diff = 1 migration file, zero existing-object changes. Equip Sonnet `sql`+`coder` (no `ui`).
- **A-LT2** — `app/packaging-loss-types.php::pkg_regenerate_vendable_view()`; regenerate view from seed + **regression-assert byte-equivalent result vs current view** across 5 run_types × 9 types. KEYSTONE GATE G1 — if not equivalent, the seed attributes are wrong, fix the seed NOT the view.
- **A-LT3** — CRUD section in reglages-generaux.php (clone ref_sites): list/create/edit/toggle/reorder. Create = catalog row(active=0) → `ALTER ADD COLUMN`(INSTANT) → regen view → active=1. `is_system` = relabel/reorder/deactivate only.
- **A-LT4** — form auto-gen (form-packaging.php + packaging-form.js) + runtime-catalog-driven compute fn. **FILE-CONTENTION ATOM: serialize with Build C white-label (both touch form-packaging.php).**
- **A-LT5** — downstream-reader verification (no-code); confirm any reader hitting a RAW disposition column (not via the view) is made view-based/catalog-aware.

**HARD GATES:** G1 = don't regen/cutover the view until seed proven equivalent (operator's own flag, correct). G2 = never drop/rename the 9 incumbent columns (is_system + corrections_policy note). G3 = A-LT4 ⊥ Build C serialize.

**TIMING vs BACKLOG — DO NOT block backlog. Operator backlogs NOW on the hardcoded form; cut over after (clean).** The 9 columns the hardcoded form writes today are EXACTLY the 9 the catalog reuses → backlogged rows land in the right columns, fully valid post-cutover (column-reuse, not column-migration). Convention file confirms columns empty + operator about-to-backlog → either order safe, but blocking a ready operator on a 5-atom re-arch is the wrong trade. ONE caveat for operator: a NEW loss type discovered mid-backlog can't be captured until A-LT3 ships — note it, do NOT force-fit into a wrong existing column (never-guess-COGS-mapping rule). Engineering doesn't touch the operator flow until A-LT4 deploys.

**Divergence flags (this build):** (1) EAV temptation — record the wide-column+codegen decision; (2) runtime-dynamic view — codegen'd CASE only, output contract frozen; (3) `bom_treatment` ENUM load-bearing (boolean can't express minus-crown-cork → wrong COGS); (4) `is_system` guard or operator deletes a seeded type with historical rows + view dependency; (5) DDL-on-create order = row(active=0)→ALTER→regen→active=1 (else view references missing column → 500s); (6) store `column_name` explicitly, don't let form re-derive independently of view-codegen (drift).

## 🟢 NEW ARC 2026-05-31 — 4 OPERATOR PACKAGING-FORM CHANGES (-V/-F cuv-vs-keg model) — P1 + PKG-CLIENT + P2 SHIPPED + LIVE + COMMITTED; P3 (C1) PENDING
> **AS-BUILT SUMMARY (2026-05-31, supersedes the design block below where they differ):**
> - **MIGRATION RENUMBER (load-bearing — the design said 236/237, reality diverged):** mig **236** went to the brewday backfill (21 contract brews, NK-collapse root cause, commit `939829f`, NOT a packaging-form mig). The client atom got NEW mig **237**; cuve-réutilisée (was "C4/P2") renamed to mig **238**. **The LINER-MI-DROPDOWN atom then consumed mig 239** (see bullet below). C1 catalog (P3) is now mig **240**. **NEXT FREE MIG = 240.**
> - **🟢 LINER-MI-DROPDOWN (mig 239) SHIPPED — commit `63977f6`:** operator ruling "liner transport AND client = a dropdown of liner MIs, not Oui/Non" → NEW `bd_packaging_v2.liner_client_mi_id_fk` + `liner_transport_mi_id_fk` (both `INT UNSIGNED NULL` FK→`ref_mi(id)` ON DELETE RESTRICT; FK `fk_bdpv2_liner_client_mi`/`fk_bdpv2_liner_transport_mi`; idx `idx_bdpv2_liner_client_mi`/`idx_bdpv2_liner_transport_mi`). **The P1/C3 free-text bools `new_liner_client`/`new_liner_transport` are KEPT as forward-only provenance** (bool-set=legacy vs FK-set=new is the discriminator; new rows write NULL to both bools + the FK). Allowed-set = `ref_mi WHERE subcategory_id=(SELECT id FROM ref_mi_subcategories WHERE name='Liner')` (PM-VERIFIED LIVE — FK-derived JOIN, NOT string copy; 6 active MIs ids 102-106+193 = PKG_LINER_{5,10}HL_*; NO hardcoded ids/pattern; same query in POST handler L477-486 AND GET render L1371-1386); default = `ref_packaging_items` slot_name='liner_client' `default_mi_id_fk` (PM-VERIFIED =102 = PKG_LINER_10HL_EDS25; both slots default 102) — nothing hardcoded. Server: two-step isset+'' read → validate-against-allowed-set → cuv-only gate. **BUG caught in Opus review + fixed before commit (DURABLE LESSON, now in §AS-BUILT below):** allowed-set was built only in the GET render path (unreachable from the redirecting POST handler) → every submit would have NULLed the liner → fix = build the allowed-set in the POST handler before the per-format loop. Live FK gate PASSED. **Consumption wiring DEFERRED per ruling — only CAPTURE landed** (4-point deferred spec recorded under §LINER CONSUMPTION (DEFERRED) below + the packaging-consumption-gap arc). **This SUPERSEDES the C3 free-text-bool liner model — C3's `new_liner_*` bools are now provenance, the MI identity is the FK.**
> - **P1 (Changes C2+C3) SHIPPED — commit `a14fa45`:** per-row cuv client/liner; keg (-F) hides the cuve-de-service block; client-vs-liner split visibility. Its client-source was SUPERSEDED by the PKG-CLIENT atom below.
> - **PKG-CLIENT (mig 237) SHIPPED — commit `192c1c2`:** NEW canonical table **`ref_packaging_clients`** — SEPARATE from `ref_clients` (brewers) and `ref_white_label_clients`. **The three-client-tables rule HOLDS: packaging-delivery clients, contract brewers, and white-label clients are three distinct facts, three distinct tables.** 18 rows seeded (17 historical distinct `keg_client_delivered` values + "Les Jardins de Louis" added live by operator, sort_order 180, no history). `bd_packaging_v2.client_fk` FK **re-pointed `ref_clients`→`ref_packaging_clients`** (old `fk_bdpv2_client … ON DELETE SET NULL` dropped; new `fk_bdpv2_pkg_client … ON DELETE RESTRICT`). Backfill: 96 cuv rows resolved by exact free-text match; 0 non-cuv promoted (keg/bot session-bleed left as provenance — forward-only, never inferred). CRUD shipped in `reglages-generaux.php` ("Clients packaging" block, cloned from ref_sites). Form cuv dropdown repointed to `ref_packaging_clients`, **gated `run_type='cuv'` only** (P1's contract/WL gating UNDONE); free-text `keg_client_delivered` input REMOVED. **⚠️ The C3/C4 design ruling "client_fk→ref_clients" is RETRACTED — canonical packaging client = `ref_packaging_clients`, NOT `ref_clients`.**
> - **P2 cuve réutilisée (mig 238) SHIPPED — commit `192c1c2`:** `reuses_packaging_id_fk` self-FK exactly as designed (nullable FK is the discriminator, no new run_type ENUM). View zeroes ALL 4 computed outputs for reuse rows; tank-simulator + loss-metrics + sb-board (3 queries) carry `reuses_packaging_id_fk IS NULL`; 2-week candidate dropdown excludes already-reused cuves; operator-asserted (no `returned_full` flag, lean v1 as designed). **#1 DIVERGENCE RISK (BBT7-class double-count) GATE PASSED LIVE:** transaction test confirmed a reuse row contributes 0 to SUM(vendable_hl) and SUM(beer_tax_base_hl); delta exactly −row's old value; rollback clean.
> - **P3 (C1) STILL PENDING — would be mig 240:** QA "Mesures" derived from CO2/O2 pair count; operator CONFIRMED coefficient = **1.000** (1 unit per pair, settable). Catalog cols input_mode/derived_source/derived_coefficient on ref_packaging_loss_types; server-derive on the MAIN bottle/can row only; forward-only (don't touch 966 historical). Converges with A-LT2/A-LT4 (loss-types view-codegen + form-gen, paused) — three view concerns meet there: contract run_type fallback (item B), reuse-skip (now shipped DIRECTLY in mig 238 — A-LT2 must PRESERVE it, not re-author), derived-qa.
>
> Original design verdict (retained as design record) below.

PM design verdict given to orchestrator 2026-05-31 (design-only). All 4 touch form-packaging.php + packaging-form.js → SERIALIZE with paused A-LT2/A-LT4 + Build C. **Live-data facts verified this consult (read-only VPS):** `client_fk` int unsigned ALREADY EXISTS on bd_packaging_v2 (0% populated; free-text `keg_client_delivered` varchar128 is the legacy path ~256 rows); client/liner currently SESSION-LEVEL written identically to every format row (L408-417 read once → L651-658 written per-row from session vars) — that's the bug; disposition fields (qa_analyses_units) ALREADY per-format (`formats[N][...]`); **co2o2 pairs are SESSION-LEVEL anchored to the ONE `row_origin='main'` row (≤20), qa_analyses is PER-FORMAT → grain mismatch**; qa_analyses_units historically populated 966/2244 (hand-entered, NO co2o2 children); co2o2 child table 11 rows; run_type dist bot 916/keg 867/cuv 244/can 195/can33 22; client/liner populated across ALL run_types incl cuv 96/244 + keg 70/867 (session-bleed). bd_packaging_v2 = class=source policy=allowed. ref_clients=16 rows.
- **C1 — QA "Mesures" → DERIVED from co2o2 pair count.** qa_analyses_units = co2o2_pair_count × coefficient, server-DERIVED (never client), JS live-displays read-only. (a) 1-unit-per-pair is DEFAULT NOT law → settable `derived_coefficient` (never-guess-COGS); bottle/can ONLY (keg/cuv hidden). (b) GRAIN MISMATCH: derive on the MAIN bottle/can row only (where co2o2 anchors), parallel rows=0. (c) **A-LT catalog tie-in = ADD `input_mode ENUM('operator','derived')` + `derived_source VARCHAR(48)` + `derived_coefficient DECIMAL(6,3) DEFAULT 1.000` to ref_packaging_loss_types**; qa_analyses→derived/co2o2_pair_count/1.0; A-LT4 form-gen renders input_mode='derived' read-only; VIEW-codegen UNAFFECTED (derivation at write-time, view stays pure — catalog governs INPUT, codegen governs OUTPUT, don't entangle). **FORWARD-ONLY — NEVER retro-derive the 966 historical rows** (no co2o2 children = operator truth).
- **C2 — keg(-F) HIDES client/liner; cuv(-V) ONLY.** RECONCILES `[[reference_packaging_keg_form_fields]]` (that memory was POSITIONAL — BSF cols 69-73 — NOT a semantic ruling; client/liner ARE cuv concepts: deliver a serving TANK + install a LINER; a 20L keg has neither). Pure JS render-gate, NO migration; server NULLs client/liner on keg. **NEVER auto-reclassify the 70 keg + 88 bot/can rows w/ client/liner as mis-entered cuv** (session-bleed unrecoverable by inference; forward-only).
- **C3 — per-row client/liner for N parallel cuv events/submit.** Scope-PROMOTION not new cols: read `formats[N][client_fk|new_liner_client|new_liner_transport]` like qa_analyses today; cols already per-row-writable. NO migration. Each parallel cuv row = full independent depletion event (own vendable_hl, own tank, v2-only sim = distinct NK-upsert, no double-count). Canonical client = `client_fk` (ref_clients FK), deprecate free-text for new entries; NEVER auto-map free-text→ref_clients.id (operator picks; surface master-data lacuna). Cuv beer still Swiss-taxed.
- **C4 — "Cuve réutilisée" = transfer of consumption ownership, ZERO new volume.** DECISION: NEW LINKED ROW, not source mutation (source event physically happened, immutable, class=source). Schema mig 236 = `reuses_packaging_id_fk BIGINT UNSIGNED NULL` self-FK→bd_packaging_v2.id ON DELETE RESTRICT (no junction, NO new run_type ENUM — the nullable FK IS the discriminator; forking run_type breaks every `run_type IN(...)`). Reuse row: `vendable_hl=0` + `audit_flags+='cuv_reuse_no_deplete'` + carries NEW client_fk; FK back to source. Sim+loss-metrics+view+beer-tax SKIP `reuses_packaging_id_fk IS NOT NULL` (grep EVERY vendable_hl consumer). Original event's depletion STAYS (volume left tank originally). COGS/WIP: reuse adds ZERO — beer costed at first packaging; **#1 DIVERGENCE RISK = BBT7-class double-count if a reuse row leaks into packaged-HL; dry-run-verify the guard.** 2-week candidate query = cuv ∧ event_date≥CURDATE()-14d ∧ reuses_id IS NULL ∧ not-already-reused. "returned_full" = OPERATOR-ASSERTED at reuse time (lean v1, no source-state flag); strict `returned_full` state = future backlog. Do NOT infer returned-full from sim volume.
- **A-LT2/A-LT4 + Build-C serialization:** C2+C3 = pure JS/POST, independent, land FIRST (ATOM P1, no mig). C4 = ATOM P2 (mig 236, view+sim+loss-metrics skip-predicate). C1 catalog cols = ATOM P3 (mig 237). C1 read-only RENDERING folds into A-LT4 (don't double-render). **THREE concerns now converge on `v_bd_packaging_v2_vendable` (A-LT2 codegen): contract run_type fallback (item B), reuse-skip, derived-qa — fold ALL into A-LT2, don't ship competing CREATE OR REPLACE VIEW.** Build C white-label sequences after P1 (per-row client scope is its foundation). **NEXT FREE MIG = 236** (C4) then 237 (C1 catalog). **TODO: correct `[[reference_packaging_keg_form_fields]]` — client/liner are cuv, not keg.**

## 🟡 LINER CONSUMPTION (DEFERRED — mig 239 captured identity; wiring NOT built)
> mig 239 added the two liner-MI FK columns so a cuv install now records WHICH liner MI was used. The COGS/consumption wiring is DEFERRED (operator ruling — capture first). **When that wiring lands it MUST (4 points, all PM-ruled):**
> - **(a) consume 1 unit of the SPECIFIC liner MI per install, at its `ref_mi` price.** Read `liner_client_mi_id_fk` / `liner_transport_mi_id_fk` per cuv event, deplete 1 unit each at that MI's price. (Two slots = two installs = up to two liner consumptions per event.)
> - **(b) honor `reuses_packaging_id_fk IS NULL`** — a REUSED cuve consumes NO new liner. The mig-238 reuse-skip predicate applies to liner consumption exactly as it does to vendable_hl; a reuse row's liner FK (if present) is provenance, not a fresh consumption.
> - **(c) legacy rows (bool=1, FK=NULL) fall back to the SKU-default liner** = `ref_packaging_items.liner_client.default_mi_id_fk` (currently 102). **Do NOT backfill the 107/97 legacy rows to specific MIs** — that is a COGS-guess, forbidden by the never-guess rule. The bool-set/FK-NULL state IS the signal to use the SKU default; the default is the canonical fallback, not an invented per-row identity.
> - **(d) the 5HL/10HL liner-vs-serving-tank-size gate is a FUTURE refinement** — a 5HL tank can't take a 10HL liner. Not blocking the consumption wiring; record as the next refinement once basic consumption is live.
> Cross-ref: also recorded at orchestrator memory [packaging-consumption-gap arc](feedback_packaging_consumption_pipeline_gap.md). This sits in the same DOWNSTREAM-debt bucket as the broader bd_packaging→inv_consumption gap (packaged backlog won't deplete RM until that pipeline is wired) — liner is one more consumable on that same gap.

## 🟢 SERVING-TANK RÉASSIGNATION — PM CONSULT 2026-06-10 (design-only, pre-build; batch 211 Blues Rules audit follow-on)
> Two asks. **ASK 1 = data backfill** (batch 211 client mis-key); **ASK 2 = new "réassigner cuve" mode** in the packaging form. Verified live VPS read-only this consult.
>
> **GROUND TRUTH (batch 211 Zepp, recipe_id_fk=57, neb_batch='211', event_date 2026-06-03):** 3 cuv rows, each 5.000 vendable_hl + 5.000 beer_tax_base_hl, all `reuses_packaging_id_fk=NULL`. #6738 client_fk=2 (Jetée) liner_client=104/transport=104; **#6739 client_fk=2 (Jetée) liner_client=104/transport=104**; #6740 client_fk=19 (Blues) liner_client=105/transport=NULL. ref_packaging_clients: 2='Jetée de la Compagnie', 19='Blues Rules'.
> - **ASK 1 VERDICT = client_fk correction, NOT an INSERT (CONFIRMED).** Fix = `UPDATE bd_packaging_v2 SET client_fk=19 WHERE id=6739` (operator-asserted mis-key, not inferred). Volume-neutral: row keeps its 5.000 vendable + 5.000 tax on Zepp 211 — only the per-client rollup re-derives. An INSERT would double-count +5 HL into Zepp 211 beer-tax + vendable. Must go via the form's edit path / a guarded UPDATE with `log_revision` (audit_row_revisions action='update', before/after_json) — NEVER raw. **FLAG (recorded): #6739 carries its OWN liner_client_mi_id_fk=104 + liner_transport=104 — those are PHYSICAL install facts of that fill and must NOT change when client flips** (#6740, the already-Blues parallel leg, has a DIFFERENT liner_client=105/transport=NULL — they are independent installs). Touch client_fk ONLY. liner consumption is not yet wired downstream (deferred), so no COGS impact today regardless, but keep the liner cols intact for when it is.
>
> **ASK 2 — "réassigner cuve" mode. ARCHITECTURE MAP:**
> - **NO standalone reassignment module exists.** The "reassignment module we already have" = the OPTIONAL "Cuve réutilisée" sub-block on each cuv format row inside `public/modules/form-packaging.php` (JS `public/js/packaging-form.js` L470-512 builds it; CSS `public/css/packaging-form.css` L667+). It is a per-row `reuses_packaging_id_fk` dropdown (mig 238), not a beer-selection-step option.
> - **The reuse mechanism (mig 238) IS the exact substrate a reassignment must use — CONFIRMED.** A reassignment = a NEW `bd_packaging_v2` row, `run_type='cuv'`, `reuses_packaging_id_fk`→source fill id, NEW `client_fk`, `vendable_hl=0`/`beer_tax_base_hl=0`/`loss_kpi_hl=0` (forced in POST L848-850), audit token `cuv_reuse_no_deplete` (L876-878). NO new volume, no second beer depletion. The view `v_bd_packaging_v2_vendable` zeroes all 4 outputs for reuse rows; tank-sim + loss-metrics + sb-board carry `reuses_packaging_id_fk IS NULL`. So the DATA MODEL is net-zero already.
> - **What's NET-NEW = the UI bypass flow**, not the data model. Today: operator must pick a tank, get the full disposition/loss/SKU machinery, then optionally tick reuse. Kouros wants: a beer-selection-step option "réassigner cuve" that BYPASSES tank-selection + all disposition inputs; every field inherits from the SOURCE fill row (the source `bd_packaging_v2` cuv row); operator picks WHICH source tank-fill + a NEW final client. The source-of-truth for the inherited fields = the source row (recipe_id_fk, neb_batch, format, hl, liner_client). The beer-selection step is the "Sélection lot source (BBT/CCT)" section (`form-packaging.php` ~L2585 `pf-selected-tank`); the bypass replaces tank-pick with source-fill-pick.
> - **LINER SWITCH (ASK 3) — the consume-vs-reallocate rule. CONFIRMED MECHANICS:** liner consumption is NOT YET WIRED downstream (grep: only the form captures it + the BOM compiler reads it; `derive-packaging-consumption.ts` does NOT touch liner). The ONLY place liner cost enters COGS today = `app/sku-bom-compile.php` cuv branch (L739-797): it amortizes liner CHF across a recipe's cuv `vendable_hl`, and its query filters **`AND vendable_hl > 0`** → **reuse rows (vendable_hl=0) are ALREADY EXCLUDED from liner amortization. That `vendable_hl>0` predicate IS the existing double-count guard.** So a reassignment row's liner FKs are pure provenance for the compiler — they cannot double-cost. CONSEQUENCE for the spec: (i) the source fill's `liner_client` reused-as-`liner_transport` for lineage = store it on the new row's `liner_transport_mi_id_fk` (provenance only; the `vendable_hl=0` + future point-(b) `reuses IS NULL` guard suppress its consumption — it is NOT a fresh install); (ii) the NEW `liner_client_mi_id_fk` IS a fresh physical install and SHOULD be consumable — but because the row has `vendable_hl=0`, the current per-HL amortization will NOT pick it up. **OPEN DESIGN Q for Kouros: where does the NEW liner_client of a zero-volume reassignment row get costed?** Options: (a) accept it rides into the next real fill's amortization basis (under-attributes), (b) when liner-consumption wiring lands (the DEFERRED §LINER CONSUMPTION 4-point spec), make it consume the NEW liner_client even on reuse rows while still honoring point-(b) "reused-liner = no consume" for the reallocated transport. The clean rule = **reuse row: liner_transport = old liner_client (reallocated, NOT consumed) + NEW liner_client (consumed) chosen by operator** — matches Kouros's ask exactly; the consume/reallocate switch = "is this slot the reallocated source-liner (no consume) vs a fresh pick (consume)". `new_liner_transport`/`new_liner_client` bools are forward-only provenance (bool-set=legacy, FK-set=new); they are NOT the consumption switch — the switch is `reuses_packaging_id_fk IS NOT NULL` + slot-identity.
> - **DOWNSTREAM TOUCHED:** beer-tax (zeroed via view ✓), vendable/general-FG (zeroed ✓), COGS/COP liner amortization (excluded via vendable_hl>0 ✓), tank-sim (skip-predicate ✓). The ONLY live behavioral change a reassignment row makes = the per-client packaging-clients rollup gets a new owner for that 5 HL — which is the entire point ("only the FINAL client is reallocated"). SOT chain intact: source fill remains the immutable depletion event; reassignment is a lineage-linked zero-volume re-owner.
> - **SEQUENCE:** ASK 1 first (1-row guarded UPDATE, no build). ASK 2 is UI+POST wiring over the EXISTING mig-238/mig-239 schema — **likely NO migration** (reuse FK + liner FKs already exist); confirm at build-start with `migrate.php --status`. RULE-1 scrapping: the new "réassigner cuve" mode may make the inline per-row reuse sub-block redundant for cuv (or they coexist — operator call). RULE-2 review pre-commit. Smoke the deployed form (nested-fn/500 lesson).
> - **EQUIP: ui + coder + sql + webapp-testing.**

### 🔴 KOUROS DECISIONS 2026-06-10 — VENDABLE/TAX DECOUPLE (CHANGES the mig-238 model) + decision 2 (single-path) — PM VALIDATED
> ASK 1 backfill DONE (id=6739 client_fk 2→19, audit_row_revisions #13978, volume-neutral). The two answers below CHANGE the derivation model from my prior consult above (which had reassignment row=vendable 0 / head keeps vendable). **The new model is CORRECT and tree-sound — record it as the authoritative reassignment model.**
>
> **NEW MODEL (CONFIRMED against the tree): tax = chain HEAD, vendable = chain LEAF. They DECOUPLE.**
> - `beer_tax_base_hl` STAYS on the initial fill (production-time liability, taxed when first packaged). Reassignment row tax=0. ✓ matches loss convention §2 (tax base = packaging-time event).
> - `vendable_hl` follows the FINAL client = LEAF of the reassignment chain. A fill THAT HAS BEEN reassigned → its vendable becomes 0; the reassignment row carries the sellable HL (that client is invoiced → must match). A fill NEVER reassigned keeps its vendable as today. Net across the chain: tax counted ONCE (head), vendable counted ONCE (leaf). No double-count.
> - `loss_kpi_hl` STAYS at the head (losses happen at packaging). Reassignment row loss=0. CONFIRMED — a reassignment is a pure re-owner, it loses nothing.
> - **MULTI-HOP rule:** vendable follows to the FINAL leaf (the row that is NOT itself reassigned by a later row). Detection = "has a child" via `EXISTS(SELECT 1 FROM bd_packaging_v2 c WHERE c.reuses_packaging_id_fk = this.id AND c.is_tombstoned=0)`. ANY row with a child → vendable 0. A row with no child AND non-null `reuses_packaging_id_fk` = the leaf, carries vendable. A row with no child AND null reuses = a normal never-reassigned fill.
>
> **DECISION 2: réassigner-cuve bypass REPLACES the inline per-row "Cuve réutilisée" sub-block for cuv (single path).** RULE-1 scrapping CONFIRMED — remove/hide the old inline reuse UI for cuv; the bypass flow is the one path. (The `reuses_packaging_id_fk` SCHEMA stays — both UIs write the same column; only the inline UI is retired.)
>
> **Q-ANSWERS (the 6 the orchestrator asked):**
> 1. **Does decouple break any derivation? NO.** sales-COGS does NOT read vendable_hl and does NOT join by client — `build-sales-cogs.ts` reads `inv_sales_ledger` (customer-resolved BC item-ledger), values via `sku_code → ref_skus → ref_sku_bom × WAC`. Per-client sales come from the LEDGER's customer_id_fk, NOT from packaging vendable_hl. So moving vendable to the leaf does NOT touch sales reconciliation either way — they are independent surfaces. vendable_hl feeds: beer-tax base (now decoupled, tax stays head), general-FG/`fg_stock_compute` packaging leg, COP packaged-HL, tank-sim drain, cuv liner amortization. The leaf-vendable is the CORRECT operand for FG (the leaf client's stock is what's sellable) and for the packaging-clients rollup (the entire point). **The one REGRESSION RISK = cuv liner amortization (Q4).**
> 2. **VIEW REWORK — exact logic.** Current view (mig 238) zeroes ALL 4 outputs when `reuses_packaging_id_fk IS NOT NULL`. NEW behavior needs THREE cases, split per-output:
>    - **`vendable_hl`** = 0 IF `has_child` (EXISTS a non-tombstoned row whose reuses_packaging_id_fk=this.id), ELSE the normal computed value (works for BOTH a never-reassigned fill AND a leaf reassignment row — the leaf has reuses NOT NULL but NO child). ⚠️ This means the leaf reassignment row must compute a REAL vendable, so the view's `reuses IS NOT NULL → 0` short-circuit must be REMOVED for the vendable arm and REPLACED by the has_child test. The leaf needs `sku_id_fk` + `units_per_pack`/`hl_per_unit` to compute — see Q3 (the leaf must carry sku_id_fk, or vendable is view-derived from the SOURCE's hl via a self-join).
>    - **`beer_tax_base_hl`** = 0 IF `reuses_packaging_id_fk IS NOT NULL` (ANY reassignment row, head or leaf — tax stays on the original fill which has reuses=NULL), ELSE normal. UNCHANGED from mig 238 for this arm.
>    - **`loss_kpi_hl`** = 0 IF `reuses_packaging_id_fk IS NOT NULL`, ELSE normal. UNCHANGED.
>    - **`vendable_units`** mirrors vendable_hl (has_child→0).
>    - So: tax/loss keyed on `reuses IS NOT NULL`; vendable keyed on `has_child`. The two predicates DIVERGE on the leaf (reuses NOT NULL but has_child FALSE) — that divergence IS the decouple.
>    - **MIGRATION YES** = `CREATE OR REPLACE VIEW` (the has_child correlated EXISTS is new logic). Does NOT need a stored flag IF vendable is fully view-derived (preferred — see Q3). A-LT2 view-codegen (paused) MUST carry this 3-case split when it regenerates — log it as the 5th concern converging on this view (alongside contract run_type fallback, reuse-skip, is_tombstoned, derived-qa). The codegen now needs the head-vs-leaf split, not a flat reuse-zero.
> 3. **Where does the leaf's sellable HL come from — stored or view-derived? VIEW-DERIVED, keep SOT single.** The cleanest model: the reassignment (leaf) row carries `sku_id_fk` + `prod_total_units` COPIED FROM THE SOURCE at reassignment time (it IS the same physical fill, same SKU, same litres) — then the view computes vendable from the leaf's own `prod_total_units × hl_per_unit` exactly like a normal cuv fill, and zeroes it only if the leaf itself later gets a child. The HEAD's vendable is zeroed by the has_child test — NO stored mutation of the head needed (the view does it). **DO NOT store vendable_hl as the ownership signal** (that resurrects the parallel-store fragility the volume-dimension arc fought — `ref_skus.hl_per_unit` copy lesson). vendable_hl IS stored today (form computes on submit) but the VIEW is the canonical read for every downstream COGS/tax/FG consumer; make the view the sole arbiter of head-vs-leaf and let the stored column be a stale convenience (or re-null the head's stored col on reassignment for tidiness, but the VIEW is truth). Net: reassignment row stores its OWN prod_total_units (from source) + reuses_fk + new client; view derives everything. Single source = the row set + the FK chain.
>    - ⚠️ ALTERNATIVE if the leaf must NOT carry prod_total_units (operator wants the reassignment row visually "empty"): view self-joins the leaf to its source-fill via `reuses_packaging_id_fk` and reads the SOURCE's prod_total_units. Works but is a heavier correlated join; the copy-prod_total_units-onto-leaf approach is simpler + matches the bypass "inherit all fields from source" UX. RECOMMEND copy-onto-leaf.
> 4. **🔴 LINER-CONSUMPTION WIRING — the REGRESSION the new model introduces.** Liner cost enters COGS ONLY via `app/sku-bom-compile.php` cuv branch (L739-797): per-recipe, it sums liner CHF over rows WHERE `vendable_hl > 0` and divides by total vendable_hl → CHF/HL amortized onto the recipe's cuv BOM. **Under the NEW model the HEAD fill's vendable_hl goes to 0** → the head DROPS OUT of the `vendable_hl > 0` basis → its liner cost vanishes from the numerator AND its HL vanishes from the denominator. Meanwhile the LEAF now has vendable_hl > 0 and ENTERS the basis carrying ITS liner FKs. **Net effect: the amortization basis silently shifts from head-liners to leaf-liners.** This is mostly SELF-CORRECTING for total recipe cost (same HL, same recipe) BUT mis-attributes WHICH liner: the head's PHYSICAL liner install (a real consumed liner at first fill) stops being costed if the head's vendable is zeroed, and the leaf's NEW liner gets costed. For a reassignment where leaf has a fresh liner that IS the same physical situation, fine; but the per-recipe SUM means it nets out only if every head has a matching leaf. **THE CLEAN FIX = move liner costing OFF the `vendable_hl > 0` amortization predicate and ONTO per-EVENT consumption** (the DEFERRED §LINER CONSUMPTION 4-point spec): consume 1 liner of the row's `liner_client_mi_id_fk` per fresh install at write-time into `inv_consumption`, gated on slot-identity (liner_client = fresh = consume; liner_transport = reallocated source-liner = NO consume), honoring `reuses IS NULL` ONLY for the TRANSPORT slot (a reassignment's NEW liner_client IS a fresh consume even though reuses NOT NULL — Kouros's exact ask). Then the compiler's per-HL amortization is REPLACED by reading actual liner consumption. **DECISION FOR KOUROS:** the new model makes the `vendable_hl > 0` amortization predicate UNSAFE (it now excludes reassigned heads that DID install a liner). Either (a) change the compiler predicate from `vendable_hl > 0` to `reuses_packaging_id_fk IS NULL` (= cost liners on the ORIGINAL fills, which is where the physical liner install happened — the head — NOT the leaf), OR (b) ship the per-event liner consumption now. **(a) is the minimal correct patch** and it INVERTS the current behavior intentionally: liner belongs to the head (physical install), tax belongs to the head, vendable belongs to the leaf. Recommend (a) as the patch shipped WITH this arc, (b) as the deferred durable wiring. NB: `vendable_hl > 0` currently excludes reuse rows; `reuses IS NULL` excludes reuse rows TOO but ALSO keeps reassigned heads (whose vendable is now 0) in the basis — that's the fix.
> 5. **MIGRATION + BACKFILL: minimal. 🟢 ZERO existing reuse rows in bd_packaging_v2 (PM-verified live 2026-06-10, count=0).** So there is NO history to restate — no risk of silently re-deriving past vendable ownership. The migration is a single `CREATE OR REPLACE VIEW` (the 3-case head/leaf split) + (if compiler patch (a)) NO migration for that (it's PHP). NO new column strictly required IF the leaf copies prod_total_units (existing col) + the view does has_child. Re-check `migrate.php --status` for the free number (head was 305 / next ~306, VERIFY — parallel sessions). schema_meta n/a (view replace, no new table).
> 6. **EQUIP + SEQUENCE:** (a) **view replace** (the head/leaf 3-case split) — `sql` — lands FIRST, it's the model change; (b) **compiler liner predicate patch** `vendable_hl>0 → reuses IS NULL` — `coder`+`sql` — lands WITH (a) (they are the same correctness unit — don't ship the view without fixing the amortization basis or you silently drop reassigned-head liner cost); (c) **UI bypass flow** (réassigner-cuve beam, inherit-from-source, copy prod_total_units + new client onto the leaf, RETIRE inline reuse sub-block for cuv) — `ui`+`coder`+`sql`+`webapp-testing` — lands after (a)/(b); (d) **downstream re-derive** = recompile affected cuv recipes' BOM + re-run COP/sales-COGS for any month with reassignment rows (none yet → no-op today). **WORKED NUMERIC EXAMPLE for Kouros to confirm before any write:** Zepp 211, head fill 5.00 HL to client A, reassigned to client B. Head: tax 5.00, vendable 0.00, loss 0.00, liner costed (predicate (a)). Leaf: tax 0.00, vendable 5.00 (owned by B, B is invoiced), loss 0.00, liner = provenance only. Chain totals: tax 5.00 (once), vendable 5.00 (once, on B), FG shows 5.00 HL sellable under B. ✓ no double-count, tax+vendable each counted once, B's invoice matches B's vendable. EQUIP line for the build brief: **EQUIP: sql + coder + ui + webapp-testing.**

## 🟢 IN-TANK / IN-FILLING CO₂/O₂ REMODEL — ARC SPLIT 2026-06-01 (SUPERSEDES the §LOT-DAY READ ruling below)
> **The model evolved from "anchor-by-reference on bd_packaging_v2" (the §LOT-DAY READ block below, mig 240) to a REAL `bd_tank_readings` DIMENSION** (woolly-hugging-wirth.md, the approved arc). Read THIS block as authoritative; the §LOT-DAY READ block below is retained as the design lineage only.
>
> **The two facts, finally separated:**
> - **In-tank read** = ONE (CO₂ g/L, O₂ ppb) pair on the BBT/source tank BEFORE filling. A property of a **(beer, batch, read_date)** lot-day. NOT a loss. Validates ALL keg/can/bot/cuv drawn from that tank. Had NO v2 store — lived only in v1 `bd_packaging.tank_co2`/`tank_o2` (~2149 rows). **Now owned by the NEW `bd_tank_readings` dimension.**
> - **In-filling reads** = MULTIPLE pairs off finished units pulled off the line DURING filling. ARE losses → feed QA "Mesures." Live in `bd_packaging_readings` (5391 historical, v1-linked) + the stray `bd_packaging_co2o2_measures` (11 rows, today's web only). **Consolidated onto `bd_packaging_readings`.**
>
> **SCHEMA (approved migs 241–246; 241/242 APPLIED + PM-VERIFIED LIVE 2026-06-01):**
> - mig **241** = CREATE `bd_tank_readings` (recipe_id_fk FK→ref_recipes + neb_batch / contract_beer / contract_batch + read_date + co2_gl + o2_ppb + TWO UNIQUE lane keys on (beer,batch,read_date)) + schema_meta row. **This is the dimension. It is divergence-flag-CLEAN — it REPLACES the v1 in-tank fact, it does NOT duplicate a v2 fact (there was no v2 in-tank store).**
> - mig **242** = bd_packaging_v2 ADD `tank_read_id_fk` FK→bd_tank_readings (repurposes the old mig-240 FK slot); bd_packaging_readings ADD `packaging_v2_id` FK→bd_packaging_v2.
> - migs 243/244 (deferred — historical backfill, see BLOCKING FINDING); 245/246 = mandatory gate + P3 derived Mesures (forward).
>
> **🔴 BLOCKING FINDING — bridge `bd_packaging.sheet_row_index → bd_packaging_v2.source_sheet_row_index` is SEMANTICALLY BROKEN (PM-VERIFIED LIVE 2026-06-01).** Of 2102 bridged v1 tank-read rows only **976 (46.4%) match on BOTH batch AND date** (987/47% beer+batch; 1062/50.5% beer alone) → ~53% mis-assign. **ROOT CAUSE = ROW-SHIFT between two INDEPENDENT numbering schemes** (v1=BSF sheets, v2=RawDB-normalized.xlsx; both individually unique → equi-join "resolves" coincidentally; no tab disambiguator). Verified off-by-one shift: v1[1017]=DIV4/12/14Aug → v2[1018]; v1[1018]=SPYF/37/15Aug → v2[1019]. The earlier COVERAGE "verification" (5328/5391 resolve) was a FALSE positive — coverage ≠ semantic correctness. **Per never-guess-mappings the bridge CANNOT carry the historical backfill** (would mis-assign ~53% of in-tank reads + of the 5391 packaging_v2_id links → silent QA/COGS corruption).
>
> **PM RULING (delivered 2026-06-01) — ENDORSE the forward-safe-now / defer-historical split:**
> - **DO NOW (forward / v2-direct, NO bridge):** (1) form rework — drop the wrong Build-2 in-filling gate; add the mandatory in-tank gate after beer/batch/date (single pair; (beer,batch,day) auto-inherit via the UNIQUE → ER_DUP_ENTRY → read-back; cuv included; reused-cuve exempt via `reuses_packaging_id_fk IS NULL`; admin/manager override mirroring hors_process; **gate logic reachable from the POST path** — the liner-MI lesson, build it in POST not only GET). On submit INSERT the dimension row then set v2.tank_read_id_fk. Retarget the existing multi-pair card to write bd_packaging_readings with `packaging_v2_id` = the v2 id the form just inserted (NO bridge — the form knows the id); relabel "Mesures en cours de soutirage." (2) Fold the 11 `bd_packaging_co2o2_measures` rows into bd_packaging_readings (directly v2-linked via packaging_id_fk = v2 id — no bridge) + DROP co2o2_measures + its schema_meta row (grep `co2o2_measures` across maltyweb+maltytask first). (3) 6710 (Emb b233/26-May) one-off OPERATOR-CONFIRMED insert (5.350 g/L CO₂ / 10.800 ppb O₂ from v1) — direct, NOT via the bridge. (4) P3 derived QA Mesures from in-filling pair count per v2 row.
> - **DEFER (separate sub-project, re-engage senior-DBA):** historical backfill of bd_tank_readings from v1's 2149 in-tank reads + packaging_v2_id for the 5391 historical readings. Until a validated bridge exists, historical reads STAY in v1 (semantic NULL on the v2 side — acceptable, data not lost).
> - **Proper bridge to validate = NATURAL business key:** v1 `neb_beer` (SKU code) → `ref_skus` → (recipe_id, format), match v2 on (recipe_id, neb_batch, read_date[, format]); contract lane = (contract_beer, contract_batch, read_date). **VIABLE — PM-VERIFIED: 1906/2149 v1 tank-rows (89%; 99.8% of the 1910 non-contract) join ref_skus cleanly AND carry recipe_id; 239 = contract lane.** **`ref_skus.format` is 100% populated** (format_id only 4/169 NULL — the stale "format_id 100% NULL" memory is WRONG; format leg is clean). Binding constraint = recipe_id (17/169 SKUs NULL, ~0.2% of tank rows). REAL DBA validation = does (recipe_id, neb_batch, read_date[, format]) land each v1 read on EXACTLY ONE v2 row (uniqueness + read_date-vs-event_date axis).
>
> **Q-ANSWERS:** Q1 ENDORSE (forward parts don't gate on history). Q2 DROP co2o2_measures NOW = SAFE (only the rewired form path + the dead §240 resolver consume it; 11 rows fold cleanly; grep first; drop schema_meta same mig). Q3 natural-key bridge RIGHT; format_id-NULL does NOT sink it (use `ref_skus.format`/SKU code); open risk = read_date axis + multi-format-same-day uniqueness. Q4 A-LT4 ⊥ on form-packaging.php → SERIALIZE, rebase the loser; NO A-LT2 view-codegen collision (QA-gate FK, not a tax/vendable view arm). §240 `tank_reading_session_id_fk` anchor col is DEAD (superseded) → scrapping-backlog for drop once the dimension forward-arc ships.
> **MIGS: 241/242 applied; forward co2o2_measures DROP = next truly-free (re-check `--status`; leave 243/244 to the deferred sub-project). Skills `sql`+`coder`+`ui`.**

---

## 🟡 SUPERSEDED 2026-05-31 — IN-TANK CO₂/O₂ = LOT-DAY READ (anchor-by-reference; design lineage only — see §REMODEL above)
> Operator: "reads at racking ≠ next-day packaging. ONE set of in-tank reads per unique day — first packaging run of a (beer,batch) on a day MUST be operator-inputted; subsequent same-day same-(beer,batch) runs RETRIEVE from that first input." In-tank reads (dissolved CO₂ g/L on SOURCE TANK + O₂ ppb) are the crucial validating QA metric before packaging, valid for ALL SKUs (keg/can/bot/cuv) drawn from that tank — NOT in-bottle reads. **mig 232's `bd_packaging_co2o2_measures` anchors reads PER-SESSION → fails "one read per lot-day."** Live proof (verified 2026-05-31): Embuscade b233 packaged as keg (id 6710, 0 reads) + can (id 6711, 3 reads) — same recipe_id_fk=32, neb_batch=233, **event_date=2026-05-26** (NOT DATE(submitted_at)=2026-05-31 — operator backlogged), same source_tank_type=BBT → ONE lot-day, read on one session, missing on the other. 355 multi-session lot-days exist historically; exactly 1 is asymmetric-with-reads today (6710←6711).
>
> **PM RULING (given 2026-05-31, design-only NOT built):**
> - **ANCHOR MODEL = hybrid, anchor-session owns by reference. DO NOT build `bd_tank_readings` parent** (parallel store — the packaging row ALREADY pins the lot-day key recipe/batch/event_date; a parent duplicates it). KEEP `bd_packaging_co2o2_measures` (mig 232) UNCHANGED in shape; its 11 rows stay put. Reads stored ONCE on the **anchor** = first `main` row of the lot-day; every other same-lot-day `main` row carries NEW nullable self-FK `tank_reading_session_id_fk → bd_packaging_v2.id` → anchor (NULL on anchor). Resolve read-set via `COALESCE(tank_reading_session_id_fk, id)` JOIN co2o2.
> - **IDENTITY KEY = `(recipe_id_fk, neb_batch, event_date)` for Neb; `(contract_beer, contract_batch, event_date)` when recipe_id_fk NULL (244 contract lane). read_date = `event_date`, NEVER DATE(submitted_at)** (load-bearing: 6710/6711 event_date=26 May, submitted 31 May — lot-day axis = real packaging day). source_tank = GUARD not key component: same lot/day across different tanks = REFUSE-DON'T-NULL, surface it.
> - **INHERITANCE = normalized (stored once, referenced); inherited READ-ONLY by default. Re-measure escape hatch: a re-measure FORKS a NEW anchor** (own co2o2 rows, FK NULL); resolver picks LATEST anchor ≤ the inheriting session. >1 read-set/lot-day possible but rare, each owned by the session that measured. Do NOT hard-forbid (would force overwrite/data-loss).
> - **GATE = soft-block at lot-day level in form-packaging.php POST handler, DOWNSTREAM of the racking→cold-crash candidate gate + the QA validé-pour-racking gate (distinct lifecycle stages, don't fold).** First run of lot-day w/ no read = HARD block (operator enters); 2nd+ = auto-satisfied via inheritance. Admin override w/ logged reason (YELLOW+override pattern), never silent. Confirm w/ operator whether cuv carries the same hard gate before shipping.
> - **P3 ENTANGLEMENT (load-bearing): lot-day reads DOUBLE-COUNT P3's per-session Mesures derivation.** P3 (`qa_analyses_units = pair_count × coeff`) under inheritance: anchor=3 pairs, 2 inheritors reference SAME 3 → lot-day reports 9 for 3 physical → silent FG/COGS corruption (qa_analyses decrements vendable + full BOM). FIX: derive Mesures on the **anchor only** (`tank_reading_session_id_fk IS NULL`), inheritors derive 0. **THIS BUILD IS A PREREQUISITE OF P3** — build lot-day re-anchor BEFORE P3, then P3 adds the `IS NULL` guard. P3's catalog cols unchanged; only its predicate gains the guard.
> - **BACKFILL = auto-resolved, deterministic, NO guess.** 6710 gets `tank_reading_session_id_fk=6711` via the canonical lot-day JOIN (recipe 32/batch 233/event_date 26 May, both BBT) = the "seed from canonical data is NOT guessing" carve-out. One-time idempotent UPDATE only where a lot-day has EXACTLY ONE session with reads. The 355 multi-session lot-days mostly have NO co2o2 children (predate A2) → no anchor → stay as-is, forward-only gate (mirrors P3's never-retro-derive-966 rule). 2 read-sets / no canonical anchor → ReviewQueue, don't pick.
> - **DISTURBS (named): (1)** resolver MUST filter `row_origin='main'` for anchor; parallel rows ride their main's FK. **(2)** EXCLUDE `reuses_packaging_id_fk IS NULL` from anchoring (reused cuve drew from no fresh tank). **(3)** verify cuv hard-gate w/ operator. **(4)** volume/loss/COGS UNTOUCHED — read is a QA gate not a volume operand; only P3's qa_analyses path is volume-adjacent (now guarded).
> - **MIGRATION = next free 240** (`240_packaging_tank_reading_anchor.sql`: `ADD COLUMN tank_reading_session_id_fk BIGINT UNSIGNED NULL AFTER reuses_packaging_id_fk` + self-FK ON DELETE SET NULL + idx; INSTANT, no IF NOT EXISTS, no schema_meta; + one-time backfill UPDATE for the single live asymmetric lot-day). **P3 becomes mig 241** (rebased on `IS NULL`). A-LT2/A-LT4 UNAFFECTED (QA-gate/anchor col, not a vendable/tax view arm — no view-codegen collision, same as liner FK). A-LT4 ⊥ this on form-packaging.php → SERIALIZE, this lands first. Skills `sql`+`coder`+`ui` (form renders inherited reads greyed + gate banner + re-measure escape). RULE-1 (scraps P3's per-session anchoring assumption — log) + RULE-2 (resolver: latest-anchor-≤-session, exclude reuse/parallel). Re-check `--status` at build-start. **NEXT FREE MIG = 240; this build 240, P3 241.**

## ✅ RÉASSIGNER CUVE — SERVING-TANK REASSIGNMENT + VENDABLE HEAD/LEAF SPLIT — SHIPPED + REAL OPERATOR REASSIGNMENT VERIFIED + COMMITTED+PUSHED 2026-06-10 (maltyweb `4af2155` on main)
> Successor to P2 (mig 238 `reuses_packaging_id_fk`) + the liner FK (mig 239). P2 made a reused cuve contribute ZERO to ALL 4 view outputs (head keeps the fact; reuse row = pure skip). THIS feature inverts the half that matters for a *commercial reassignment*: when a head's beer is REASSIGNED to a new client/liner, the **vendable + liner-cost MOVE to the leaf** (leaf now owns the sale), while **tax + loss STAY on the head** (head still brewed/lost the liquid). The asymmetry is the whole point. Operator about to input his real **Blues Rules** reassignment via the UI.
>
> **WHAT SHIPPED (3 layers):**
> 1. **Migration `317_vendable_view_head_leaf_split.sql` (APPLIED on VPS; numbered 317 not 316 — 316 was taken by a parallel KPI session `316_kpi_logistics_equipment_batch10`).** Reworks `v_bd_packaging_v2_vendable`: **`vendable_units`/`vendable_hl` now ZERO when EXISTS a non-tombstoned child (`reuses_packaging_id_fk = this.id`), compute normally otherwise → the LEAF owns vendable.** `beer_tax_base_hl` + `loss_kpi_hl` KEEP the original P2 predicate `reuses IS NOT NULL → 0` (they STAY ON THE HEAD). Verified **NO-OP on current data** (2270 rows, 0 diff — zero reuse rows existed pre-feature). The asymmetry: vendable migrates head→leaf via `has_child` EXISTS; tax/loss stay head via `reuses IS NULL`.
> 2. **`app/sku-bom-compile.php` cuv liner-amortization predicate fix (DEPLOYED).** WHERE `vendable_hl>0` → `(reuses_packaging_id_fk IS NULL OR vendable_hl>0)` so a REASSIGNED HEAD (vendable now 0 via the new view) STILL costs its liner; transport-liner cost SKIPPED on leaf rows (liner reallocated, not re-consumed). No-op on current data. **Same compiler lane as the 🟢🟢 cuv hl_per_unit / liner-branch fix (RESUME #0) — this branch is fragile, exercise carefully.**
> 3. **`form-packaging.php` + `packaging-form.js` + `packaging-form.css` (DEPLOYED) — "Réassigner une cuve" BYPASS MODE at the tank-selection step.** LEAF row INHERITS `recipe_id_fk/sku_id_fk/neb_beer/batch/suffix/prod_total_units` from source, `vendable_hl=source.vendable_hl`, **`beer_tax_base_hl=0`, `loss_kpi_hl=0`**, `reuses_packaging_id_fk=source`, `client_fk=new`, `liner_client=new` (`new_liner_client=1`), `liner_transport=source.liner_client` (`new_liner_transport=0`), **NO `bbt_source_fk`/`cct_source_fk`/`source_tank_type` (NO BBT deduction — liquid already drawn at the head)**; HEAD UPDATE `vendable_hl=0` (tax/loss kept) + audit. **`row_hash` via canonical `bd_row_hash()` with a `'REASSIGN'`+source_id DISCRIMINATOR** (avoids the dedup-collapse hazard — same class as the multi-SKU/parallel NK problem). Inline "Cuve réutilisée" sub-block RETIRED for cuv (single path now). Multi-tank: first leg=main, additional=parallel via EXISTING parallel machinery.
>
> **DERIVATION-TREE FIT (clean):** ONE fact = the packaging event in `bd_packaging_v2`; head+leaf = TWO rows of ONE reassignment, NOT a parallel store. Vendable (commercial fact) → leaf; tax/loss (production fact) → head. The view is the single arbiter; the compiler reads the view. SOT chain intact — no string copy (inheritance is column-copy WITHIN one table by self-FK), no `mi_id=NULL`, no guessed COGS mapping (liner transport inherits `source.liner_client` = canonical, not invented).
>
> **SMOKE TEST (Playwright, live site):** renders; toggle works; all 3 pickers populate (251 source tanks / 21 clients / 7 liners); labels clean (`Zepp — B.212 — 09/06/2026 — 5.0 HL — Jetée`). DB-verified on a REAL test reassignment (source 6753 Zepp212 Jetée 5HL → Les Arches): HEAD vendable 0 / tax 5 / liner 104 kept; LEAF vendable 5 / tax 0 / liner_client 105 / liner_transport 104 / new_liner_transport 0 / bbt null; **VIEW == stored exactly.** Test row DELETED + head vendable restored to 5.000. Blues Rules batch-211 ×2 untouched. **ONE BUG FOUND+FIXED:** toggling left `formats[0][row_origin]='main'` in the DOM → PHP "exactly one main" guard fired → fix = CLEAR formatsContainer on toggle-ON (redeployed), re-test SUCCESS. (Smoke acct users.id=16 temporarily elevated viewer→operator to reach the gated form, then REVERTED to viewer — confirmed.)
>
> **KNOWN LIMITATIONS / FOLLOW-UPS (logged, NOT blockers for the Blues Rules input):**
> - **Multi-tank capped at 2 legs (main+parallel)** — `uq_natural_key` has only `row_origin` to disambiguate same-beer/batch/suffix legs → reassigning 3+ same-beer tanks in ONE submission needs the dedup-key design ruling already flagged on the multi-SKU/parallel arc. Different-beer legs unaffected.
> - **Reassignment UNDO gap (stored/view divergence):** tombstoning a leaf does NOT auto-restore the head's STORED `vendable_hl` (view recomputes via `has_child`, but the stored column stays 0 → stored≠view on undo). Documented in a code comment; needs a tombstone handler. OUT OF SCOPE today. **Inverse of the standing "stored==view" invariant — watch if an undo path is ever built.**
> - **Liner full per-event consumption into `inv_consumption` STILL DEFERRED** (amortization predicate fix is the ship-now correctness patch; no-op today, zero reuse rows). Same deferral as the mig-239 LINER CONSUMPTION (DEFERRED) spec above.
>
> **REAL OPERATOR REASSIGNMENT LANDED + VERIFIED 2026-06-10:** Kouros reassigned **Blues Rules tank 6740 → Rincette** via the new UI. New LEAF row **6767** (vendable 5 HL, tax 0, `reuses=6740`, liner_client=new + `liner_transport=source's 105 reallocated` / `new_liner_transport=0`, NO bbt/tank source). HEAD 6740 vendable→0, tax 5 STAYS. **Batch-211 cuv totals CONSERVED (33.5 HL vendable + 33.5 HL tax, each counted once); view == stored.** The asymmetric head/leaf split worked on live data exactly as designed. ⚠️ **Still ONE un-reassigned Blues tank (6739, 5 HL)** — if the operator later reassigns it too, that's a second leaf (independent install, its own liner facts).
>
> **COMMIT STATE: ✅ COMMITTED + PUSHED — maltyweb `4af2155` on main** (5 files: `form-packaging.php`, `packaging-form.js`, `packaging-form.css`, `sku-bom-compile.php`, mig 317). Was deployed-but-uncommitted via surgical scp; now landed. ⚠️ **`4af2155` ALSO swept in the already-deployed mig-313 `objective_hl` wiring that sat uncommitted in `form-packaging.php` / `packaging-form.js`** (noted in the commit body) — **FLAG for the objective_hl / mig-313 (daily-recap Feature C) parallel session: that wiring is now committed here, do NOT double-commit it.**

## OPEN (carry-over, lower priority)
- ✅ A2 CO2/O2 child table (`bd_packaging_co2o2_measures`) SHIPPED (mig 232) — this is where the dropped tank reading lands; ≤20 pairs/session anchored to `row_origin='main'`, delete-and-reinsert idempotent, CHECK not-both-null, per-pair QC via `bd_qc_flag`. Will later feed daily-shell single-pair-append (pilot work).
- **✅ Atom C white-label — SHIPPED + DEPLOYED + PUSHED (HEAD `40b00be`, prior `815e725`) 2026-06-05 as the ADDITIVE model (NOT subtractive); operator-validated live (WL additive + parallel-card carve case + QA count re-sync all confirmed). Commit also carried the QA draft-restore re-sync fix. 3 files: form-packaging.php / packaging-form.js / packaging-form.css. See §ATOM C WHITE-LABEL — ADDITIVE 2026-06-05(d) below — that is the live truth; the (c)/(b)/(a) subtractive carve-out verdicts are RETRACTED.** Free-text client name (no FK table — `ref_white_label_clients` CANCELLED), no migration (cols pre-exist), no backfill. WL = an appended additive parallel leg (`is_white_label=1`, `-WL` NK-suffix), card row entered as-is. Only remaining packaging build now CLOSED.
  **LIVE-VERIFIED 2026-06-05 (read-only VPS):** (1) `ref_white_label_clients` does **NOT exist** (Build C never built). (2) `bd_packaging_v2.white_label_name` (varchar128) is **0% populated** — form has the toggle but the name NEVER reached v2. (3) `is_white_label` is a NN col; only **12 historical rows** =1, **ALL `row_origin='parallel'`**, each with its OWN full taxed `prod_total_units` (e.g. ZEPB 5184u→17.107 HL, `beer_tax_base_hl>0`). (4) WL is read SESSION-level (`$isWhiteLabel`/`$whiteLabelName` form L421-422) + stamped identically onto EVERY leg in the write loop (L930-931) — **same grain bug C2/C3 already fixed for client/liner.**
  **⛔ DESIGN VERDICT OF 2026-06-05 (the "scope-promote per leg, WL qty = leg's own prod_total" verdict) IS RETRACTED — see §ATOM C WHITE-LABEL — RE-ARCHITECTED 2026-06-05(b) below.** It rested on the false premise that the 12 historical WL rows were a UX precedent (additive parallel legs = WL). They are NOT — they are normalizer ARTIFACTS (`is_wl = ts_year<2025 AND flipped_suffix=='B'` heuristic, normalize-rawdb.py L1547-1548 — every pre-2025 24-pack-box parallel leg mass-stamped WL). Operator confirmed they NEVER entered a WL run via the web form, and chose a CARVE-OUT UX (WL = subset of one card's total), not an additive separate leg. The new verdict is below.
- Taproom-segregated stock = FUTURE module (A1 captures taproom litres + tax flag + excludes from general FG; the separate stock isn't built).

