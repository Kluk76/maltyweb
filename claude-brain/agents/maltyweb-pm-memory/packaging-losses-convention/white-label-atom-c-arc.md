> Extracted from [packaging-losses-convention.md](../packaging-losses-convention.md) during the 2026-06-05 compaction. The ATOM C WHITE-LABEL arc history: (d) ✅ SHIPPED+DEPLOYED+PUSHED ADDITIVE (HEAD `40b00be` 2026-06-05) is the LIVE truth; (b) re-architected operator carve-out facts + (c) ⛔ RETRACTED SUBTRACTIVE reduced-scope are kept for trace only. Read when touching white-label packaging (ref_white_label_clients, form-packaging.php white-label path, packaging-form.js/.css).

## §ATOM C WHITE-LABEL — RE-ARCHITECTED 2026-06-05(b) (operator carve-out facts; SUPERSEDES the 2026-06-05 verdict above)
**GROUND TRUTH (PM-verified live VPS 2026-06-05, latest applied mig=265, next free=266):**
- The 12 `is_white_label=1` rows are **NORMALIZER ARTIFACTS, not a UX precedent.** All 12: `row_origin='parallel'`, `run_type='bot'`, suffix `-B` (24-pack box), `mode_b_extraction`, `session_id_fk=NULL`, `white_label_name=NULL`, all pre-2025. Each sits beside a `-4` (and sometimes `-F`/`-V`) MAIN leg of the SAME beer/batch/date — e.g. id1133 ZEPB-B pu5184 beside id1132 ZEP-4 pu2856; id1013 EMBB-B pu240 beside EMB-F + EMB-4. The flag comes from `normalize-rawdb.py` L1547-1548 `is_wl = (ts_year<2025 and flipped_suffix=='B')` — a blanket heuristic that mis-stamped every pre-2025 24-pack-box parallel leg as WL. **These are Nébuleuse-brand boxed runs, NOT white-label.** `white_label_name` empty on all 12 confirms the free-text path was never used.
- Operator NEVER entered a WL run via the web form. Operator CHOSE the **CARVE-OUT UX**: one run card, total produced + sub-field "dont white-label: X units, client Y". WL is a SUBSET of the card total; Neb-brand remainder = total − wl_units. This SUBTRACTS — the OPPOSITE of the additive parallel-leg convention (`packaging-form.js`: "prod_total on main; qte_unites on parallels ADD not subtract").

**RE-ARCHITECTED VERDICT (canonical answers):**
- **A. Storage = B-SPLIT (split the card into two `bd_packaging_v2` rows at save).** At POST, when wl_units>0 on a card: write the Neb-brand row (`prod_total_units = total − wl_units`, is_white_label=0) + a SECOND row (`prod_total_units = wl_units`, is_white_label=1, white_label_client_id_fk set), same `session_id_fk`, same recipe/batch/format/event_date. **Rejected A-cols** (one row + white_label_units col): would force EVERY downstream consumer (beer-tax, vendable, SKU resolution, packaging-material COGS, deplete-deliveries) to learn to split one row's units two ways — a new arithmetic seam in N places, the "one fact one canonical handle" violation. B-split keeps the existing per-leg storage shape homogeneous: every consumer already sums rows; each row already carries its own full taxed/vendable HL from its own prod_total_units. **The carve-out lives ONLY in the form's POST splitter; storage stays leg-homogeneous.** NB: these split legs are NOT `row_origin='parallel'` in the additive sense — they are two output identities of one session; use `session_id_fk` to group, set row_origin appropriately (main = the Neb remainder; the WL leg can be parallel OR a flagged sibling — decide at build, but it must NOT trigger the additive-parallel double-count beyond what its own prod_total represents, which is fine because each row already = its own units).
- **B. Beer-tax + vendable:** WL beer = same liquid, same Swiss tax. B-split keeps BOTH rows in the tax base (Neb remainder + WL portion = original total). NO units dropped, NO double-count (we SPLIT the total, not duplicate it). `beer_tax_base_hl`/`vendable_hl` are computed per-row from each row's own prod_total → automatically correct under B-split, NO view change. (Contrast: A-cols WOULD need the view to learn the split.)
- **C. SKU / packaging-material COGS:** WL portion is the SAME bottle, client's label/branding. CAPTURE units + client + flag NOW; **DEFER the WL packaging-material COGS wiring** (different sku_id_fk for the client brand + client-label MIs via the BLA4 `ref_sku_packaging_choices` mechanism). For now the WL leg can resolve to the SAME sku_id_fk as the Neb leg (liquid + format identical) — the brand override is a later atom (liner-mig-239 pattern: capture identity, wire consumption later). Operator's lean = CONFIRMED.
- **D. Client capture = managed FK `ref_white_label_clients`** (free-text 0/12 used → retire it). Three-client-tables rule HOLDS (≠ ref_clients brewers, ≠ ref_packaging_clients delivery). **SEED LIST MUST come from operator before INSERT** (the old Bao Canteen / Freeride / Carougeoise / EBike / Bains Payés / Arias guess is UNVERIFIED — do not seed it). Per-client material-responsibility flag (contract-packaging "we procure+recharge" vs "client supplies own") — DEFER onto the table when the material-COGS atom lands; out of scope for capture-now.
- **E. Atom sequence (carve-out):** WL-1 = `ref_white_label_clients` table + CRUD (clone `ref_packaging_clients` block in reglages-generaux.php). WL-2 = ADD `white_label_client_id_fk INT UNSIGNED NULL FK→ref_white_label_clients ON DELETE RESTRICT` to bd_packaging_v2; keep `white_label_name` as legacy provenance, write NULL on new rows, NEVER backfill. **Under B-split, NO new units column is needed** (units carried by each split row's own prod_total_units) — this is the key simplification vs the retracted verdict and vs A-cols. WL-3 = form: per-card "dont white-label: X units + client Y" sub-field + POST splitter that emits the two rows; **build the client allowed-set in the POST path, not only GET** (the liner-MI bug). Skills `ui`+`sql`+`coder`. File-contention: form-packaging.php + packaging-form.js — SERIALIZE w/ any paused A-LT/tank-read work.
- **F. The 12 artifact rows:** they are mislabelled — `is_white_label=1` should be 0 (Neb-brand boxed legs). **Recommend a surgical homogenizing migration (mig 266-class): UPDATE the 12 to is_white_label=0** (they're parallel -B legs of Neb -4 runs, NOT WL) so historical + new data are homogeneous under the carve-out model. ALSO fix `normalize-rawdb.py` L1547-1548 — DELETE the `ts_year<2025 and flipped_suffix=='B'` heuristic (future-seed-correctness; RawDB frozen so no re-run, pure code fix). VERIFY before the UPDATE that none of the 12 was a *genuine* pre-2025 white-label run (ask operator: "were any 24-pack boxed runs before 2025 actually white-label for a client?") — if yes, exclude those from the un-flag. Net beer-tax/vendable UNCHANGED (flag flip only, no HL moves). **Next free mig = 266.**

## §ATOM C WHITE-LABEL — ✅ SHIPPED + DEPLOYED + PUSHED (HEAD `40b00be`) 2026-06-05 — ADDITIVE; SUPERSEDES (c)/(b)/(a). The SUBTRACTIVE B-split is RETRACTED.
**🟢 SHIPPED STATE OF RECORD (2026-06-05):** commit **`40b00be`** on `main` (origin/Kluk76/maltyweb in sync; prior HEAD `815e725`). **3 files:** `public/modules/form-packaging.php` + `public/js/packaging-form.js` + `public/css/packaging-form.css`. Deployed via `bin/deploy.sh --apply` and **operator-validated live** — white-label additive + parallel-card carve case + QA count re-sync all confirmed working. This commit ALSO carried the **QA draft-restore re-sync fix** (draft-restore now re-syncs the QA count correctly). NO migration, NO backfill (cols pre-exist). Build-state complete.
**🟢 OPERATOR ROUND-TRIP VERDICT (authoritative — record permanently). The carve-out/SUBTRACTIVE model of (c) below is WRONG and is RETRACTED.** On live round-trip the operator rejected subtraction: the "Dont white label" label misled (it implied WL units are *deducted* from the run total). Verbatim: "it is the run's total and WL's total, which is then summed for the real session's total." **WL is ADDITIVE, identical to the existing parallel-leg convention ("prod_total on main; qte_unites on parallels — ADD, not subtract").**
**FINAL CANONICAL MODEL (shipped + deployed + pushed 2026-06-05, HEAD `40b00be`):**
- The card's **"Unités produites" = the Nébuleuse run total, entered AS-IS, NOT reduced.** `is_white_label=0` on the card row. The card row is unchanged — the splitter NO LONGER mutates it.
- The WL units are a **SEPARATE ADDITIVE LEG**: an APPENDED `bd_packaging_v2` row — `row_origin='parallel'`, `is_white_label=1`, `white_label_name` (free text), `prod_total_units = special_qty_units = wl_units`, `sku_id_fk` COPIED from the card row, `nebuleuse_format_suffix = <suffix>-WL`.
- **Session total = card units + wl units** (an addition, like any parallel leg). Both legs in the beer-tax base; per-row `compute_packaging_vendable_hl` reads each row's own prod_total → correct; NO double-count, NO subtraction edge case, **zero new arithmetic** in any consumer.
- **Splitter: NO card-row mutation; NO `wl_units <= cardTotal` guard; NO invariant assert** (legs are independent — none needed). The (c) "carve-out subtraction lives in the splitter" is GONE.
- **`-WL` suffix NK-distinguisher RETAINED** (still required so the WL leg and a same-format parallel card don't collide on `uq_natural_key` — the (c)-§C latent break still applies; the fix is unchanged).
- Form label changed: **"Dont white label" → "Unités white label (additif)".**
- **NO DB migration** (`is_white_label` + `white_label_name` already exist). **NO data backfill** (operator had submitted ZERO web WL rows → no subtractive rows exist in the wild; nothing to undo). Build was purely `form-packaging.php` + `packaging-form.js`. Skills `ui`+`coder`.
- **Derivation-tree impact = CLEANER than subtractive:** the WL leg is summed like any parallel leg; the SOT chain (recipe→format→SKU→BOM→COGS) is untouched; both legs share the card's `sku_id_fk` (defer WL packaging-material COGS = the BLA4 brand-override atom, later, unchanged).
- **What carries over from (c) UNCHANGED:** per-card grain (not session grain); WL leg = same `sku_id_fk` (material-COGS deferred); `special_qty_units` on the WL leg = wl_units (RULE-1 consumption provenance); `-WL` NK-distinguisher; edit mode renders TWO independent cards (no re-merge); free-text client name (no FK table); normalize-rawdb.py L1547-1548 heuristic LEFT as-is (forward-only, RawDB frozen); the 12 historical artifact rows LEFT alone (`is_white_label=1`, forward-only).

> ⛔ EVERYTHING BELOW IN (c)/(b)/(a) THAT SAYS "SUBTRACT" / "carve-out" / "remainder = total − wl_units" / "splitter subtraction" IS RETRACTED. Kept only for trace. The ADDITIVE model in (d) above is canonical.

## §ATOM C WHITE-LABEL — ⛔ RETRACTED (SUBTRACTIVE) — REDUCED SCOPE 2026-06-05(c) — kept for trace only; SUPERSEDED by (d) ADDITIVE above
**⛔ THIS SUBTRACTIVE B-SPLIT VERDICT WAS REJECTED ON OPERATOR ROUND-TRIP — see (d). Do NOT build from this section.**
**OPERATOR FINAL DECISIONS (PM-confirmed architecturally sound):**
1. **KEEP FREE TEXT for client name.** `ref_white_label_clients` table + FK CANCELLED (WL-1 + WL-2 dropped). Operator explicitly chose free-text over managed dropdown. → WL leg carries `white_label_name` (varchar128, already exists) as TEXT, not an FK.
2. **LEAVE the 12 historical rows alone.** NO un-flag, NO backfill (the mig 266 un-flag of (b)F is CANCELLED). Operator: "we don't try to match WL past data, focus forward with proper tracking." The 12 stay `is_white_label=1` / `white_label_name=NULL` — accepted historical noise. Forward-only.

**CONSEQUENCE → NO MIGRATION.** Both target columns (`is_white_label` tinyint(1) NN def 0; `white_label_name` varchar128 NULL) ALREADY EXIST on `bd_packaging_v2` (PM-verified live 2026-06-05). The build is PURELY `form-packaging.php` + `packaging-form.js`. **Skills: `ui`+`coder` only — NO `sql` this round** (no schema, no new query beyond the existing per-card resolver).

**FINAL BUILD (carve-out, B-split, free-text, per-card):**
- Move `is_white_label`+`white_label_name` from SESSION grain (form-packaging.php L420-422 parse-once → L929-931 stamped on every row) to PER-CARD (`formats[N][is_white_label]`, `formats[N][wl_units]`, `formats[N][white_label_name]`).
- POST splitter: for any card with `wl_units>0`, emit TWO rows sharing `session_id_fk` — the REMAINDER (`prod_total_units = card_total − wl_units`, is_white_label=0, name NULL, keeps the card's OWN origin) + the WL LEG (`prod_total_units = wl_units`, is_white_label=1, name=text). Carve-out subtraction lives ONLY in the splitter. Sum of two = original card total.
- WL leg resolves to the **SAME `sku_id_fk`** as the remainder (same liquid+format) — defer WL packaging-material COGS (the BLA4 brand-override atom, later).

**PM ANSWERS TO THE 6 QUESTIONS:**
- **A. NO migration — CONFIRMED.** Both cols live. Nothing else changes in the DB.
- **B. B-split correct with free-text — CONFIRMED, one gotcha (same as FK version):** WL leg carries name as text; downstream consumers all sum per-row prod_total → beer-tax/vendable auto-correct. The gotcha is the NK collision (see C), unchanged by text-vs-FK.
- **C. row_origin = `'parallel'` is the RIGHT value (ENUM is only `main|parallel`, no third value possible without a mig — which we're avoiding). BUT THE LATENT BREAK: the WL leg MUST carry a DIFFERENT `nebuleuse_format_suffix` from the remainder, OR they collide on `uq_natural_key (submitted_at, neb_beer, neb_batch, contract_beer, contract_batch, row_origin, nebuleuse_format_suffix)` and `bd_upsert` SILENTLY CLOBBERS one leg → half the volume vanishes from tax base.** The collision fires specifically when the carved card's OWN origin is already 'parallel' (a WL carve-out ON a parallel format line): then BOTH legs are row_origin='parallel' + same suffix → same NK. **Resolution: the WL leg needs a distinguishing token in the suffix (e.g. append `-WL` / a wl-marker) so the 7-col NK differs.** This also disambiguates edit-mode pre-image lookup (form L1058-1068 keys on submitted_at+row_origin+suffix). **special_qty_units on the WL leg: set it = wl_units** (matches the existing parallel convention — special_qty_units = the leg's own qte_unites as consumption provenance, RULE-1, form L892; all 67 live parallel rows carry it). NOT NULL, NOT the card total. The remainder leg keeps whatever special_qty its own origin dictates (NULL if main, its own units if the card was parallel).
- **D. Edit mode = render as TWO separate cards, NO re-merge — CONFIRMED/BLESSED.** Same as any parallel run today (the form already reloads parallel siblings as independent cards). Re-merge would be fragile coupling for zero gain. The operator edits the two legs independently; if they re-carve, the splitter just re-runs. Acceptable.
- **E. Beer-tax/vendable: no subtlety beyond C.** Both legs in the base, no double-count BECAUSE we SPLIT (total−wl + wl = total), never duplicate. Per-row compute_packaging_vendable_hl reads each row's own prod_total → correct. The ONLY way this breaks is the NK collision of C (one leg clobbered → that leg's HL silently leaves the base). Fix the suffix-distinguish and E is clean.
- **F. normalize-rawdb.py L1547-1548 heuristic: LEAVE IT (operator forward-only + RawDB frozen → no live effect). Low-priority code hygiene only.** If a Sonnet is already in that file for another reason, ride-along delete the `is_wl = (ts_year<2025 and flipped_suffix=='B')` line; otherwise not worth a dedicated touch. NOT part of this build.

**STILL-TRUE from (b):** carve-out SUBTRACTS (opposite of additive parallel); storage stays leg-homogeneous; WL leg = same sku_id_fk (defer material COGS); File-contention form-packaging.php + packaging-form.js — SERIALIZE w/ any paused A-LT/tank-read work.

> NEXT FREE MIG = **240** (235=event_date backfill, 236=brewday backfill, 237=ref_packaging_clients, 238=cuve-réutilisée, 239=liner-MI FK cols — all applied 2026-05-31). A-LT1 (`ref_packaging_loss_types` catalog + 9-seed + schema_meta, INERT) landed as mig 234 (commit `f5685b3`). A3 (`loss_untaxed_full_units` col + view rebuild) landed as mig 233 (commit `cef2a4a`). Re-check `migrate.php --status` before write. (NB: the prior "next free 266" line elsewhere is the live-verified number as of 2026-06-05; this 240 line is stale and pre-dates the saisie/auth arcs — trust the §close-out header's 264/266.)

