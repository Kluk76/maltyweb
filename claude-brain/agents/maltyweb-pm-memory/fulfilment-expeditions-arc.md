# Fulfilment / Expéditions arc — «Expéditions» christens Le Cockpit (LIVE-BUILT 2026-06-07)

> Read when touching: `ord_orders`/`ord_order_lines`/`ord_order_status_events`, `ref_customers`, `ref_transporters`, `public/modules/expeditions.php`, `api/expeditions-status.php`, `app/fg-stock.php` (`fg_stock_compute()`), the Commandes dashboard, customer bootstrap, or the future commandes@ email ingestion. Companion: [sales-commercial-surface.md](sales-commercial-surface.md) (eshop/BC two-source model), [sku-bom-validation-walkthrough.md](sku-bom-validation-walkthrough.md) RULE #3/#4 + Stage-2 tracker, [mother-shell-architecture.md](mother-shell-architecture.md) EXPÉDITION Stage-3 placeholder.

## Provenance / approval
PM pre-approval brief delivered + **approved by Kouros 2026-06-07** with three PM recommendations confirmed: (1) saisie + actionable dashboard (not display-only); (2) «Expéditions» is the christening build of **Le Cockpit** (commercial family); (3) full v1 in one arc. All below DEPLOYED to VPS, linted, smoke-tested, committed 2026-06-07.

## Schema — mig 279 `279_expeditions_orders.sql` (commit `bbc49fe`)
- **`ref_transporters`** — seeded Galliker/Loxya. PENDING: add **Transport Express** (17 mentions in the sheet).
- **`ref_customers`** — commercial sold-to master: `name` UNIQUE, `bc_customer_no` UNIQUE NULL, `trade_channel`, `is_private`, `default_transporter_id_fk`, `needs_review`. **Intentionally DISTINCT from `ref_clients`** (= contract-brewing companies; documented in-mig).
- **`ord_orders`** — `order_type` + `customer_id_fk` + `internal_channel` exactly-one-of CHECK (RESTRICT FK); `status` ENUM entered→confirmed→picked→bl_printed→shipped + cancelled **as CACHE only**; email-ingestion-ready cols: `source`, `source_file_id_fk`→`doc_files.id`, `parse_confidence`, `review_status`.
- **`ord_order_lines`** — `sku_id_fk` RESTRICT, `qty` DECIMAL(10,2) in **SKU units** CHECK>0; HL derived at read.
- **`ord_order_status_events`** — append-only TRUTH; cache updated same-transaction.
- 5 `schema_meta` rows; constraints probed live; fixtures self-cleaned. `ref_pages`: page_key `fulfilment`→`expeditions`, href set, **`is_active=0` — flip = go-live gate, after operator smoke**.

## Module (commit `f9535ef`)
- `public/modules/expeditions.php` — 3 tabs + saisie POST: one transaction, `log_revision` throughout, PRG, inline new-customer (`needs_review=1`), `?edit=` prefill, shipped/cancelled rows read-only.
- `api/expeditions-status.php` — advance/revert/cancel via explicit **rank map, NEVER ENUM order** (mig-276 lesson honored).
- `public/css/expeditions.css` (`exp-` prefix), `public/js/expeditions-form.js` (typeahead over `window.EXP_*` injections) + `expeditions.js`.
- Fix applied during build: `require`→`require_once` (csrf.php double-include 500).

## Commandes dashboard (commit `af6e0ea`)
ISO-week nav + **date-range mode** (Kouros mid-build request): `?view=commandes&mode=range&du=&au=` GET contract, ≤92 j, bookmarkable — the past-delivery pull the legacy sheet lacked. Filters client/SKU/statut/canal. Day blocks with per-day HL + counts, SKU pills, status chips one-click advance. **Eshop/taproom rows = READ-ONLY auto rows from `inv_sales_*`** (muted, never double-counted into ord_*). 3 queries, no N+1.

### Stock-warning DETAIL MODAL ✅ SHIPPED 2026-06-10 (commit `badd153` on main, NOT pushed; 4 files: `expeditions.php`/`.css` + `expeditions.js` + `expeditions-form.js`)
Per-order stock-risk chip → click → modal short-list of the SKUs that fall short. **NO second compute, NO API round-trip** — built inside the EXISTING `$view==='commandes'` `fg_stock_compute` rollup pass and injected as `window.EXP_CMD_STOCK_DETAIL` = `{oid: [{sku_code, requested, available, physique, short_by}]}`. Removed the early `break` in the per-order rollup so ALL short lines are captured (was lossy — only first short line before). `$cmdStockMap` enriched to also carry `physique`.
- **Headline / short_by math keys off `live_futur` (=disponible)** to MATCH the chip trigger exactly (Kouros-specified); `physique` shown as a SECOND advisory column alongside disponible, not the driver.
- Chip = focusable `<button class="exp-stock-risk-chip" data-order-id>`; modal = single `<dialog id="exp-stock-detail-modal">` opened from **`expeditions.js`** (the Commandes-view JS — NOT `expeditions-stock.js`). Closes ✕/backdrop/Escape. Built per `bom-review.js` idiom; `dialog[open]`-scoped CSS, no display-trap, no z-index hack (UI-skill rendering discipline honored).
- **Stays advisory — never blocks.** KNOWN: shipped orders can still show the chip (pre-existing rollup filters only cancelled, not shipped). Flagged to Kouros as a possible SEPARATE tweak; not requested.
- VERIFY: `node --check` + `php -l` clean; live data probe = 9 orders flagged with correct short-lists (#6 STIB −1 + DIB4 −11; #28 DIV4 −48). Browser click-smoke NOT completed (Playwright snagged on login) — verified by code soundness + data probe; Kouros asked to confirm the click opens it.
- ⚠️ COMMIT NOTE: `badd153` ALSO bundled a PRE-EXISTING uncommitted mouvements-form stock-hint feature (`EXP_MOV_STOCK_MAP`, another session's in-flight work) — 24 intermixed hunks couldn't be safely split. NOT pushed. The 2-week predictive compute (`fg-stock.php`) was committed out-of-band by another session earlier. (Parallel-session tree churn ongoing: financier `6a013ad`/untracked financier.php+css; mon-tableau kpi-handlers Phase 2b `09006ce`/`d264111`/`4f6a98c`/`598f788`.)

## Stock PF (commits `4a8f49e` + fix `b676c9b`)
`app/fg-stock.php` pure-SELECT library **`fg_stock_compute()`**:
`anchor` (`inv_fg_stocktake` latest month_closed — currently 2026-04) **+ production** (`bd_packaging_v2` **÷ `ref_skus.units_per_pack`** → production in PACK units; caught a 24× inflation, same defect class as maltytask `942431e`) **− shipped** (ord lines) **− eshop** (`inv_sales_orders` — SOLE eshop source) **− taproom** (**`inv_sales_bc`** — inspection showed `inv_sales_orders` has NO taproom rows; documented in code).
4 metrics + survendu/<2-semaines flags + drill-down ledger + dormant toggle. Velocity = trailing 8 wk from BC+eshop history.

## ✅ SEMAINES-RESTANTES / FORWARD SEASONAL BURN ENGINE — SHIPPED + LIVE on prod 2026-06-11 (MVP)
**DEPLOYED + LIVE on `?view=stock` (Expéditions Stock-PF). Built EXACTLY to the rulings below — no architecture deviation.** Single burn path honored (S=cron-materialized, L+sim=render); all 6 divergence flags clean.
- **Mig `325` `kpi_sku_seasonal_index` APPLIED** (live-verified: table exists, **795 cache rows, 15 families** [14 own-curve + 1 global], composite **`idx_sl_sku_posting (sku_id_fk, posting_date)`** present on `inv_sales_ledger`, **11 `system_settings` section='stock' `burn_*` params**). NB built families=15 (14 own + global), not the 25-distinct pre-build count — only families with enough history get their own curve; the rest fall to the global curve (by design, the <2yr fallback).
- **`app/seasonal-burn.php`** = the 8-function SHARED LIB = THE single burn path (cron writes S; render reads S + computes L + runs the forward sim). Deployed.
- **`scripts/seasonal-index-cli.php`** = weekly rebuild CLI (`--dry-run` default; `--apply` = wholesale TRUNCATE+rebuild in one txn = idempotent). Deployed. **Ran `--apply` once manually → cache populated** (`computed_at` 2026-06-11 11:33).
- **`db/cron/maltytask-seasonal-index.cron`** = weekly Mon 04:00 UTC. **DISABLED — file deployed to repo but NOT installed to `/etc/cron.d`** (awaiting Kouros enable, per the recipe-profile cron convention). Cache stays fresh by manual `--apply` until enabled.
- **`app/fg-stock.php` Step 8/9 rewired to the engine.** New per-SKU row fields: `rythme_base`, `burn_status` (normal/provisoire/eol), `burn_cache_cold`, `burn_index_computed_at`. Deployed (byte-identical local↔VPS verified).
- **`public/modules/expeditions.php` + `public/css/expeditions.css`** = display triad: primary semaines + muted "rythme de base" (L) + provisoire chip + seasonal-freshness note. Deployed.
- **LOCKED SEMANTICS (as ruled, evidence-backed):** burn = **−SUM(qty_signed) ALL doc_types**, net-of-returns floored ≥0/wk; sale_class deny = **`customs_artifact` ONLY**; family = `ref_skus.recipe_id` via FK JOIN; 25% NULL-sku rows **excluded + counted** (coverage gap surfaced). Deliberate divergence from `v_sales_ledger_order_lines` (B2B display lens) **documented in code** — burn has its own SELECT over the base table.
- **OPUS INDEPENDENT VERIFICATION (PLAUSIBILITY, not just existence):** global seasonal peak **ISO-week 26** (summer ✓ — decomposition NOT inverted). **ZEPF**: engine L=297/wk vs naive 349 (deseasonalization+recency correct); Zepp family peaks **3.2× at wk26**; forward sim **40% SHORTER** runway than naive heading into the June peak — the early-warning the flat mean destroyed. `fg_stock_compute()`: 53 rows / 110ms / `burn_status` 40 normal · 12 provisoire · 1 eol / **0 errors**.
- **OPEN ITEMS (2026-06-11):**
  1. **Enable the weekly cron** (one-line install to `/etc/cron.d`; smoke passed). PM recommendation below.
  2. **Authenticated VISUAL smoke** of `?view=stock` — functional/headless smoke done; browser screenshot offered to Kouros, NOT yet run.
  3. **ALL changes UNCOMMITTED.** Net-new files (`seasonal-burn.php`, `seasonal-index-cli.php`, the `.cron`, mig 325) + `fg-stock.php` are cleanly mine; **`expeditions.php`/`expeditions.css` are ENTANGLED with a parallel session's uncommitted edits in the same files** (shared clone). Commit boundary below.
  4. **CAVEAT to relay (NOT a bug):** many SKUs show short cover (0.7–2.9 wk) = correct summer warning + the known 06-08-anchor B2B-depletion understatement (gate ⑤). Expected.
  5. **PRE-EXISTING parallel-session `Undefined array key "needs_review"` Warning at `expeditions.php:2318`** — NOT from this build (Carnet/CRM code); flag to whichever session owns it.
- **FAST-FOLLOW:** **#1 ORDER-AWARE COVERAGE — ✅ SHIPPED + LIVE 2026-06-11 (see §ORDER-AWARE COVERAGE SHIPPED below).** Remaining fast-follows: Croston's for sporadique (ADI>1.32) SKUs; lead-time-relative reorder bands (needs per-family `L_repl` from Kouros); overstock ceiling; cron freshness chip.
- **CROSS-REFS:** the ITEM-1/RESUME-POINT index entry for this engine; the §SHIPPED-ARCS register. The rulings of record (A–D2) that this build implements follow below.

---
## 🆕 ORDER-AWARE COVERAGE (FAST-FOLLOW #1 to the burn engine) — PM RULINGS 2026-06-11 (pre-build; render-only, NO mig)
Kouros flagged a real inconsistency: the seasonal headline `semaines_stock` is pure `physique÷burn` and IGNORES the committed open-order book, while `live_futur` (=physique−open_total) already encodes it. PROOF SKU **ALTF**: physique=40, rythme_base(L)=2.4/wk → semaines=14.4, BUT 45 units on the open book (overdue/near 30+6+6 + a 3 due 07-03) → live_futur=−5 (survendu). 14.4 is arithmetically fine but operationally blind. **DECISION (Kouros): order-aware coverage — the forward sim subtracts committed open orders at their due dates FIRST, then burns the rest at the seasonal rate.** Reconciles `semaines_stock` with `live_futur`. RENDER-TIME ONLY (orders already queried Step 7) — NO migration.
- **RULING Q1 — RECONCILIATION = `max()`, RULED.** Per forward week: `demand[week] = max(L×S[week], open_orders[week])`. Adding double-counts (history `L×S` is already mostly B2B orders; the open book is the *realization* of that same demand). `max()` = take the better-informed estimate per week: a big committed order dominates its week; an order-free week still burns the seasonal baseline. **REJECTED "orders-replace-forecast-for-ordered-weeks, baseline-for-the-rest"** — a small order in a high-baseline peak week would UNDER-burn (false runway = the dangerous direction). Overdue + current-ISO-week orders FOLD into forward-week-1 (mirrors live_futur treating the whole open book as immediately-committed). **GUARD: the fractional-stockout denominator (`($h-1)+$stock/$forecast`) must use the reconciled `demand[week]`, not raw L×S — apply `max()` INSIDE the loop before the `$stock<=$forecast` test.**
- **RULING Q2 — SINGLE BURN PATH = CONFIRMED, with one HARD invariant.** Order-aware logic lives INSIDE `sb_forward_weeks()` (the lone sim consumer) — it gains ONE param `array $openByWeek` passed in; the runway derivation stays single. The bucketed open-orders query (Step 8) **MUST mirror the live_futur query (Step 7, fg-stock.php:558-567) EXACTLY**: same `FROM ord_order_lines l JOIN ord_orders o ON o.id=l.order_id_fk`, same `WHERE o.status NOT IN ('shipped','cancelled')`, **NO upper date bound** (live_futur subtracts the ENTIRE open book). **HARD INVARIANT / build-time assertion: Σ(openByWeek across all buckets per SKU) == that SKU's `open_total_qty` from Step 7.** Ideally derive both from ONE fetch so they can't drift. **AXIS: bucket by weeks-from-today (`FLOOR(DATEDIFF(requested_date,today)/7)` clamped ≥0 for overdue/current), NOT YEARWEEK-of-year** — the sim loops by horizon offset `$h`, so the PHP bucket the sim consumes must be the `$h` axis (SQL may still compute YEARWEEK, convert to offset before feeding the sim).
- **RULING Q3 — MEANING SHIFT = SAFE, no downstream breaks (every consumer traced).** `semaines_stock` shifts from 'burn-runway on physical stock' → 'runway accounting for committed orders + burn'. All consumers take it as opaque `float|null`; a smaller number simply lands a lower (more urgent) health tier = INTENDED. 6-tier `exp_classify_*` (expeditions.php:2577-2624, WEEKS_CRITICAL=1/LOW=3/HIGH=10), `flag_low_stock` (fg-stock.php:677 `<2.0 && physique>0`), `exp_fmt_semaines`, gauge bars — ALL unchanged. **🔴 THE ONE CARE POINT: tier 6 `sans_rotation` = `physique>0 && semaines_stock===null` means 'no burn data / EOL', NOT 'order-depleted'. An order-aware stockout in week 1 must return a small POSITIVE float (or 0), NEVER null. ALTF → ~0 weeks (critique), NOT sans_rotation. Keep null reserved strictly for no-level/EOL (sb_forward_weeks already returns positive on stockout — preserve).** Divergence with live_futur is REDUCED not raised (that's the deliverable): post-change both encode order-awareness → can no longer contradict (one survendu / other 14wk). Invariant: live_futur<0 ⇒ semaines≤~0-1wk (week-1 bucket carries all overdue/current orders > physique). Bands/legend/`sans rotation` logic UNCHANGED — only the number. COPY tweak (ui, not logic): legend/drill may note "couverture (stock − commandes engagées)" so operator reads it as order-aware.
- **RULING Q4 — DRILLDOWN = EXTEND the existing `exp-stock-drill` with a THIRD face "Couverture". NOT a modal, NOT the exp-hist client panel.** The `?view=stock` per-SKU drill is ALREADY a `role=tablist` segmented toggle (commit `711905c` 2026-06-10): Face "Stock" + Face "Activité", delegated `button[data-evt-tab]` scoped `closest('.exp-stock-drill')` + `resetDrillFace()` on collapse. **Add a 3rd tab/face.** Modals are reserved for the Commandes view (`exp-stock-detail-modal`, opened from expeditions.js) — a modal here forks the idiom (UI-level divergence smell). The `exp-hist-*` panel is per-CLIENT (wrong grain). The "Couverture" face carries Kouros's breakdown: physique+flows, open-order book by date (reuse the already-loaded data, no new query), live_futur, rythme de base L + lumpiness note (Q5), the family seasonal curve, and the **week-by-week projection table** (demand=max(L×S,orders) · running stock · week-hits-0 · status) — the high-value artifact making `max()` legible. Feed via the established `window.EXP_*` page-wide injection (sim emits an optional per-week trace), NO AJAX/endpoint (mirrors "Activité récente" + the risk modal). A11y: real `<button role=tab>`/`role=tabpanel`, `resetDrillFace()` resets across all 3 faces on every collapse path, arrow-nav extends to 3.
- **RULING Q5 — CROSTON'S STAYS FAST-FOLLOW (confirmed). SHOW lumpiness, don't change level math.** Kouros chose 'Order-aware' not 'Both' → sporadique/Croston's (ADI>1.32) stays deferred. The "Couverture" face SHOWS `nonzero_weeks/53` + recent-spike note as DIAGNOSTIC context (e.g. "17/53 sem. avec ventes · pic récent"), NOT a recomputed L. **Order-awareness is the load-bearing fix for ALTF, not Croston's** — with the 45 committed units folded in, ALTF reads ~0wk regardless of whether L=2.4 or Croston's-corrected; the lumpy L misreading the spike is 2nd-order. Folding Croston's in now = two level-math paths in one build = scope-creep against single-burn-path discipline. Keep it a clean separate fast-follow.
- **VERDICT + SEQUENCING:** architecturally sound; render-time-only, NO migration (orders already queried). Single-path integrity hinges on ONE query matching live_futur exactly + the Σ(buckets)==open_total_qty assertion. Safe in one arc. No `ref_pages` change → NO tour-steward dispatch.
- **EQUIP: sql + coder + ui + webapp-testing.** sql = bucketed open-orders query (mirror Step 7 filter/join exactly) + Σ-reconciliation EXPLAIN/dry-run; coder = `sb_forward_weeks()` order-aware param + Step 8 wiring + per-week trace emission; ui = the 3rd "Couverture" drill face (mirror `711905c` tablist idiom, inline NOT modal) + projection table + lumpiness note; webapp-testing = read-only `?view=stock` prod smoke (ALTF reads survendu/~0, 3rd face opens, projection renders, no 500).

---
## ✅ ORDER-AWARE COVERAGE (FAST-FOLLOW #1) — SHIPPED + LIVE on prod 2026-06-11
**Built, deployed, verified-live on `?view=stock`. Implements EVERY ruling Q1-Q5 above — no architecture deviation. Render-only, NO migration (orders already queried Step 7), HEAD unchanged.**
- **FUNCTION REFACTOR (note for future refs):** `sb_forward_weeks()` → **`sb_forward_sim()`** returning `{weeks, trace}` (was scalar weeks). **Back-compat wrapper kept** under the old name. So the lone sim consumer is now `sb_forward_sim` in `app/seasonal-burn.php`; the per-week `trace` is what feeds the Couverture projection table.
- **Q1 max() — IMPLEMENTED:** per forward week `demand = max(L×idx, open_orders[h])`; overdue + current-ISO-week orders fold into `h=1` (mirrors live_futur's whole-book-immediate treatment). Order-depleted stockout returns **~0, never null** (Q3 care-point honored — EOL stays the ONLY null).
- **Q2 single path + HARD INVARIANT — PASSES:** order-awareness lives inside the one sim; bucketed open-orders query **mirrors the Step-7 `live_futur` filter EXACTLY**; bucketed by **weeks-from-today offset** (`h=1` for overdue/current), not yearweek-of-year. **Build-assert `Σ(openByWeek) == open_total_qty` PASSES 32/32 SKUs, no warnings.**
- **Q3 meaning-shift — clean:** normal SKUs (no open orders) byte-unchanged — `max()` is a no-op without orders, so all downstream tiers/gauges/`flag_low_stock`/`sans_rotation` untouched. 54/54 rows carry `couverture`.
- **Q4 3rd "Couverture" face — SHIPPED on the existing `exp-stock-drill` tablist** (server-rendered inline, NO modal/AJAX — Q4 honored); `fg_stock_compute` emits a per-SKU `couverture` drill payload (physique+flows, open_book, live_futur, rythme_base, lumpiness, recent_spike, seasonal now/peak, projection trace, status). Week-by-week projection table with **stockout-row highlight** (colour+border+glyph). **Fixed a pre-existing tab-switcher JS bug** (hardcoded ternary mis-routed any 3rd tab → generic regex) — was latent before this build added the 3rd face.
- **Q5 Croston's — stays fast-follow:** lumpiness shown as DIAGNOSTIC only, level math unchanged. Confirmed.
- **LIVE VERIFICATION:** **ALTF 14.4 → 1.86 wk** (trace shows the 42u 20/06 order landing at h2 → stock −5.76 survendu — the exact reconciliation with live_futur this build exists to deliver); normal SKUs byte-identical (max() no-op); 54/54 rows carry couverture; 0 errors / 0 assertion-warnings; `php -l` clean on all touched files.
- **FILES DEPLOYED, UNCOMMITTED:** `app/seasonal-burn.php`, `app/fg-stock.php` (cleanly this build's). `public/modules/expeditions.php`, `public/css/expeditions.css`, `public/js/expeditions-stock.js` **ENTANGLED with a parallel session's uncommitted edits** in the same 3 files (same shared-clone entanglement as the morning burn-engine build) — **commit by PATHSPEC, do NOT sweep the parallel hunks.** `seasonal-burn.php` + `fg-stock.php` commit cleanly on their own.

---
## 🆕 SEMAINES-RESTANTES / BURN-RATE — SCOPE UPGRADED to FORWARD SEASONAL ENGINE (S&OP expert ruling) — PM ARCHITECTURE RULING 2026-06-11 (✅ NOW BUILT — see SHIPPED block above)
Kouros uploaded weekly sales per SKU back to 2021 → wants real "semaines restantes de stock" on `?view=stock`. **The flat trailing-24mo-mean swap is SUPERSEDED.** Kouros consulted an industry S&OP/demand-planning expert; the locked model is a **forward, seasonal, family-pooled depletion engine**: family multiplicative seasonal index `S[family][1..52]`, per-SKU deseasonalized EW level `L[sku]` (λ=0.95, half-life ~13wk), forward week-by-week depletion sim charging `L×S[isoweek]`, status chip (normal/provisoire/sporadique-Croston/EOL), display triad. Full locked model in the consult message of record.
- **KEY FINDING (still holds) — this is a VELOCITY-SOURCE SWAP at the seam, not a new surface.** `semaines_stock` ALREADY EXISTS in `app/fg-stock.php` (Step 8, ~L586), wired into `?view=stock`: 6-tier health (`WEEKS_CRITICAL=1`/`WEEKS_LOW=3`/`WEEKS_HIGH=10`, `exp_classify_*` ~L2447-2494), `exp_fmt_semaines()` "∞" formatter ~L3852, progress bars ~L4278, `flag_low_stock`. Today velocity = trailing-56-day flat mean off `inv_sales_bc`+`inv_sales_order_lines`. The new engine REPLACES the Step-8 numerator/window with `L×S` forward sim; `physique` numerator + all downstream classification/render UNCHANGED (thresholds may need re-tuning to forward semantics — fast-follow).
- **DATA SOURCE = `inv_sales_ledger`** (live-verified 2026-06-11): `posting_date` DATE (idx `idx_sl_posting`), `sku_id_fk` INT UNSIGNED (idx `idx_sl_sku`), `qty_signed`/`qty_invoiced` DECIMAL(14,4) SKU units, `hl_resolved`, `doc_type` ENUM(shipment/invoice/credit/return_receipt). **2021-01-05→2026-06-09, 44,164 rows, 133 SKUs.** ⚠️ **25% NULL `sku_id_fk`** (unresolved sku_code_raw) — coverage-gap surface, NEVER drop. ⚠️ **NO composite (sku_id_fk, posting_date) index — only two single-col idxs; mig must add the covering composite** for the per-SKU 52wk pull + the cron's full decomposition.
- **🔴 FAMILY POOLING DATA TRUTH (live-verified — the expert model assumed `recipe_id_fk`; THE DB DOES NOT HAVE IT):** `inv_sales_ledger` has **NO `recipe_id_fk`** (only `sku_id_fk`). `ref_skus` has **NO `recipe_id_fk` either — the family key is `ref_skus.recipe_id` (INT UNSIGNED, NULLable)**. So **family = `ref_skus.recipe_id`, reached by JOIN `inv_sales_ledger.sku_id_fk → ref_skus.id → recipe_id`** (NOT a direct column, NOT a string). **25 distinct recipe families** in the resolved ledger. **NULL-`recipe_id` rows: 6/182 active SKUs + 146 ledger rows** → these CANNOT pool to a family → **fall back to the GLOBAL brewery seasonal curve** (same path as <2yr-history families). The 25% NULL-`sku_id_fk` rows can't even resolve a SKU → excluded from both S and L (coverage gap, surfaced not dropped).
- **physique = `fg_stock_compute()`** — already the numerator (`$physique`). No change.

### A. CRON + CACHE TABLE — BLESSED (revising the earlier "no cron / materialize only if slow")
The seasonal index `S` genuinely needs precompute: 52×family ratio-to-centered-MA decomposition over 44k rows, STABLE week-to-week, NOT render-cheap. **BLESS a new cache table + weekly cron. This stays SINGLE-HOMED = ONE burn path:** `S` is materialized by the cron, `L` is computed at render, the forward sim (render) consumes both — there is exactly one velocity derivation, no parallel store. Divergence-clean because S is a *materialized projection* of `inv_sales_ledger` (the SoT), recomputed wholesale each cron run (idempotent overwrite — no incremental drift), never hand-edited, never a second authority. This is the SAME materialization pattern already blessed for the daily-recap perf index — heavy stable analytic precomputed, render reads cache.
- **Table name (house convention `kpi_` prefix for materialized analytic caches): `kpi_sku_seasonal_index`.** Grain = ONE row per (family_recipe_id, iso_week 1..52) carrying the multiplicative index + a `is_global_fallback` flag for the brewery-wide curve (store the global curve as its own pseudo-family, e.g. `recipe_id=NULL`/sentinel row-set, so fallback is a lookup not a branch). Columns (sql agent finalizes): `recipe_id` (NULL=global curve), `iso_week` TINYINT, `seasonal_index` DECIMAL, `sample_years` / `n_obs` (provenance for the <2yr fallback decision), `computed_at`, plus a small `kpi_seasonal_index_meta` row OR a `computed_at` MAX read for freshness-chip. `schema_meta` row required (new table).
- **Cron:** weekly (S is stable; daily is wasteful). Wire as a maltyweb cron / scheduled PHP CLI, NOT a maltytask Node script (strangler: new compute homes in maltyweb+MySQL). Recompute wholesale, INSERT-fresh into a temp/swap or TRUNCATE+repopulate in one txn (idempotent). Emit a freshness timestamp so the render can show "indice saisonnier au {date}" and degrade gracefully (if cache empty/stale → fall back to the current flat-mean so the page NEVER hard-fails on a cold cache).
- **L stays at RENDER** (per the model): one indexed aggregate on `inv_sales_ledger(sku_id_fk, posting_date)` for the trailing 52 weeks + EW-mean in PHP. Cheap with the new composite index. This is the part that legitimately belongs in `fg_stock_compute()` Step 8.

### B. DERIVATION-TREE PLACEMENT
- **S (cached family seasonal shape)** = a NEW materialized leaf hanging off `inv_sales_ledger` (SoT) via the `sku_id_fk→ref_skus.recipe_id` family rollup. It is a projection, not a new fact. Upstream: `inv_sales_ledger` + `ref_skus.recipe_id`. Downstream: the forward sim only. It is NOT in the Le Zeppelin commissioning chain and NOT COGS/beer-tax-bearing (it's a demand-shape analytic, unit-counts only) — so it does not touch the SOT chain. Single canonical owner = the cron.
- **L (per-SKU level)** = render-time scalar, lives in `fg_stock_compute()` Step 8, owned by that function. Upstream: `inv_sales_ledger` (sku-grain) + `S` (to deseasonalize). Downstream: the sim.
- **Forward sim + semaines_restantes** = render-time, `fg_stock_compute()` Step 8, owned there. Upstream: `physique` (already computed) + `L` + `S`. Downstream: existing health-tier classification + chip + progress bars (unchanged seam).
- **Pooling collision check (resolved):** family=`ref_skus.recipe_id` does NOT collide with anything — it's the existing recipe-family key already used across the BOM/SKU tree. The ONLY friction is the NULLs (6 active SKUs + 146 ledger rows + the 25% unresolved-SKU rows) → all route to the global-curve fallback / coverage-gap surface; none silently corrupt a family aggregate.

### C. PHASING — AGREED (with one addition)
- **MVP** (delivers the CORRECT forward seasonal number): family seasonal index `kpi_sku_seasonal_index` + weekly cron + render-time `L` (EW λ=0.95, weeks-since-first-sale divisor for new SKUs) + forward depletion sim + net-of-returns (qty_out=−Σsigned_qty floored ≥0/week) + EOL "sans rotation" label + display triad (primary semaines_restantes + secondary "rythme de base" L + status chip with normal/provisoire). **Cold-cache graceful fallback to current flat-mean is part of MVP** (so the page can't 500 on an un-run cron). This is your MVP list — AGREED.
- **Fast-follow:** Croston's level for sporadique SKUs (ADI>1.32) + lead-time-relative bands (needs per-family `L_repl` data **Kouros must provide** — until then bands stay on the current absolute `WEEKS_*` constants) + overstock ceiling + threshold re-tune to forward semantics. **ADD to fast-follow:** the `kpi_sku_seasonal_index` **freshness-chip on the page** (same maltyweb-prefers-live discipline that's still open on the COP tiles — surface "indice au {date}", don't let a silently-stale cron pass as fresh). AGREED.

### D. MIGRATION / EQUIP / SEQUENCING / FLAGS
- **Migration number: `325` is the next free slot** (live-verified 2026-06-11: no `325_*` file local OR on VPS, absent from `schema_migrations`; 326/327 already applied — there is a GAP at 325). 328 also free if a second mig is needed. **RE-RUN `migrate.php --status` + `ls db/migrations` at build-start — parallel sessions live; the gap could be claimed.** One mig suffices: `325_kpi_sku_seasonal_index.sql` = CREATE TABLE `kpi_sku_seasonal_index` (+`schema_meta` row) + the composite index `ADD INDEX idx_sl_sku_posting (sku_id_fk, posting_date)` on `inv_sales_ledger` (INSTANT/INPLACE) + the `system_settings` param INSERTs (section e.g. `prevision_stock`). MySQL-8 syntax (no IF NOT EXISTS on ALTER, no bare SELECT).
- **PARAMS → `system_settings`** (the canonical central params table, NOT hardcoded): λ, season=52, horizon=104, clamp[0.2,4.0], ADI cutoff 1.32, EOL K=26wk, provisional thresholds (13wk/6 non-zero/full-summer), smoothing window=3, half-life. Shape fits: `section`/`key_name`/`value_num DECIMAL(10,4)`/`default_num`/`unit_fr`. Per-family `L_repl` lead-times = a SEPARATE keyed set Kouros provides (fast-follow) — likely `commissioning_settings` or a small `ref_*` per-family table, NOT `system_settings` scalars (it's per-family). NB `value_num` is DECIMAL(10,4) — fine for λ=0.95 and clamps.
- **EQUIP: sql + coder + ui + webapp-testing.** sql owns mig 325 + the cron query + the composite-index plan + EXPLAIN on the 52wk render pull. coder owns the cron CLI + `fg_stock_compute()` Step-8 rewrite + cold-cache fallback. ui owns the display triad + chip + freshness label. webapp-testing = read-only `?view=stock` prod smoke (semaines renders, chip states, no 500 on cold cache).
- **DIVERGENCE FLAGS:** (i) **family JOIN, never string/name** — pool on `ref_skus.recipe_id` via FK JOIN, NEVER on beer-name (the match-on-recipe_id-not-beer-name rule). (ii) **single burn path** — S=cron-materialized, L=render; the sim must be the ONLY consumer; do NOT let the old flat-mean and the new engine BOTH feed `semaines_stock` except as the explicit cold-cache fallback. (iii) **NULL families → global curve, surfaced** — never silently drop the 6 NULL-recipe SKUs / 146 ledger rows / 25% unresolved-SKU rows; they go to the global fallback + a coverage-gap count, refuse-don't-NULL discipline. (iv) **not COGS/beer-tax** — unit-counts only; the cron must NOT touch `inv_consumption`/`ref_sku_bom`/CHF; it's a demand-shape analytic, outside the SOT chain. (v) **idempotent cron** — wholesale recompute/overwrite, no incremental accumulation that could drift S from the ledger. (vi) **cold-cache must degrade** — page renders (flat-mean fallback) when the cron hasn't run, never 500.

### D2. BURN-QUANTITY SEMANTICS — PM RULING 2026-06-11 (recon-driven; resolves 3 quantity-correctness forks the S&OP expert could not, having no BC-export visibility)
Live recon (2026-06-11): `inv_sales_ledger` 44,164 rows 2021-01-05→2026-06-09, 133 resolved SKUs, 25% NULL `sku_id_fk`. doc_type qty_signed sums: **shipment −1,080,057 (n=31257); invoice −753,202 (n=6309, SAME sign as shipment); credit +526,362 (n=6580, dominated by ±58,206 correction PAIRS w/ invoice); return_receipt +98 (n=18).** Columns: posting_date, doc_type ENUM(shipment/invoice/credit/return_receipt), sku_id_fk, qty_signed DEC(14,4), customer_id_fk, sales_amount_chf, hl_resolved. Composite (sku_id_fk,posting_date) idx CONFIRMED MISSING (two single-col idxs only) → mig 325 must add it.
- **🔴 RULING 1 — SHIPMENT-ONLY: SUPERSEDED FOR BURN 2026-06-11 (escalation-driven reversal; the verify-gate fired exactly as designed).** The original ruling (shipment-only, on a mirror-double-count hypothesis) was REVERSED when its own VERIFY-BEFORE-CODE clause (b) surfaced invoice-only goods-out. **Burn = −SUM(qty_signed) across ALL doc_types** (shipment+invoice outbound − ; credit+return_receipt back-in + ; net naturally via qty_signed sign). EVIDENCE (live re-verified 2026-06-11, sale_class×doc_type net −SUM): **eshop invoice 77,537 vs shipment 4,722 (94% of eshop goods-out is INVOICE-documented); taproom invoice 11,451, shipment 0 (100% invoice, ZERO shipment) → shipment-only DROPS taproom entirely + 94% of eshop.** Mirror hypothesis FALSIFIED: (i) `bc_source_no` is CUSTOMER-grained (~8 docs/source, 1064 distinct vs 8265 shipment docs) — the "252 shared source_no" = 252 customers using both flows, NOT per-order mirrors; (ii) each (doc_type, bc_line_seq) is a DISTINCT BC Item-Ledger movement — credits visibly net against shipments same-week, no shipment+invoice pair narrates one physical line; (iii) eshop literally migrated doc_type ERA (shipment-posting 2021 → invoice-posting later, same goods). The −/− sign agreement encodes DIRECTION (both outbound), not duplication. **b2b CONSISTENCY (make explicit so no one re-restricts b2b later): b2b also posts invoice +664,143 alongside shipment +1,072,632 — same doc_type-era migration, b2b-wide. The all-doc-type rule applies to b2b too; do NOT special-case b2b back to shipment-only.** SAFE DIRECTION: shipment-only under-counts burn on eshop/taproom-heavy SKUs → over-states weeks-remaining → false runway on the fastest-growing channels (the dangerous direction Kouros flagged). All-doc-type = honest/slightly-conservative runway. **Shipment-only REMAINS correct as the B2B-DISPLAY lens (mig-326 `v_sales_ledger_order_lines`, Commandes surface) — a legitimately different question (one-shipment-per-order-line for display); the two coexist by design. Do NOT point burn at that view.**
- **RULING 2 — channel scope = OPTION (a): ledger, ALL physical-stock sale_classes (incl. taproom + eshop where present), accept pre-2024 eshop-history gap as a NAMED FAST-FOLLOW (not MVP blocker).** [NB 2026-06-11: "shipment-only" qualifier here is OBSOLETE — RULING 1 reversed to all-doc-type; channel scope itself stands, now even more so since taproom/eshop are invoice-documented.] Rejected (b) eshop-splice from inv_sales_orders = a 2nd burn path across two stores/grains/temporal-seam → violates single-burn-path flag (ii). Rejected (c) strict-canonical B2B-only = OVER-states weeks-remaining on taproom/eshop-heavy SKUs (the dangerous direction: false runway on core beers). Burn universe must MATCH the `physique` numerator's depletion universe (b2b+taproom+eshop). Family-pooled S already absorbs eshop's seasonal shape wherever eshop hits the ledger; eshop-only L precision = fast-follow. VERIFY-BEFORE-CODE: `SELECT c.sale_class,l.doc_type,COUNT(*),ROUND(-SUM(l.qty_signed)),MIN/MAX(posting_date) FROM inv_sales_ledger l JOIN ref_customers c ON c.id=l.customer_id_fk GROUP BY 1,2` → proves which sale_classes/eshop are in the ledger + date-span → decides if eshop-splice fast-follow is even needed.
- **RULING 3 — sale_class universe = DENY-LIST `customs_artifact` ONLY (REFINED 2026-06-11 to what's actually in the ledger).** Filter = `sale_class IS NULL OR sale_class NOT IN ('customs_artifact')`. Live ledger sale_classes present: b2b, eshop, taproom, event, giveaway, staff, customs_artifact, + 1 NULL-customer shipment row. INCLUDE everything physical: b2b/taproom/eshop + giveaway/staff/event (each unit physically left FG stock = real depletion) + the NULL-customer row (+228, physical goods-out; NULL = coverage-gap to surface, refuse-don't-drop). EXCLUDE only customs_artifact (fiscal artifact; nets to 0 anyway, but exclude explicitly so a future non-zero customs row can't leak). **`transfer`/`sample` are NOT present in `inv_sales_ledger`** (they live elsewhere, operator-confirmed) → dropped from the deny-list to keep it honest to the table; if a transfer/sample row EVER appears in this ledger that's a schema surprise → ESCALATE, do NOT pre-emptively sweep it. ⚠️ sales-side `sale_class='giveaway'` is WHOLLY DISTINCT from `inv_side_stock_ledger movement_type='giveaway'` (mig 303) — burn reads the SALES ledger ONLY.
- **🔴 NET DERIVATION (one line, CORRECTED 2026-06-11):** Burn = **−SUM(qty_signed) over inv_sales_ledger WHERE sku_id_fk IS NOT NULL AND (sale_class IS NULL OR sale_class NOT IN ('customs_artifact'))** — **ALL doc_types** (shipment+invoice+credit+return_receipt net via sign) — floored ≥0/week, family-pooled sku_id_fk→ref_skus.id→recipe_id (S) / sku-grain (L). **DELIBERATE DIVERGENCE from `v_sales_ledger_order_lines`: that view is the B2B shipment-only DISPLAY lens; burn is all-doc-type all-physical-channel. DO NOT reuse the B2B view as the burn source — burn needs its own SELECT over the base table. Comment this in cron+Step-8 so a future reader doesn't "fix" the discrepancy by pointing burn at the view.** (Prior shipment-only NET DERIVATION superseded — see RULING 1.)
- **VERIFY-GATES — RESOLVED 2026-06-11 (the gate worked: R1(b) surfaced invoice-only goods-out → escalated → RULING 1 reversed).** R1 mirror-check: mirror hypothesis FALSIFIED (bc_source_no customer-grained, not order-mirror; doc_types are distinct Item-Ledger movements). R2 channel-presence: b2b/eshop/taproom/event/giveaway/staff + 1 NULL present; transfer/sample absent. R3 ENUM: deny-list narrowed to customs_artifact only. All three CLOSED — no remaining pre-code blocker on the burn-source leg. **EQUIP for the verify+burn-source leg: sql + coder** (ui+webapp-testing stay equipped for the render/smoke legs of the broader build).

## ✅ HISTORICAL-COMMANDES DATA LANE — SHIPPED + LIVE on prod 2026-06-11 (mig 326; 3 read-only VIEWs over `inv_sales_ledger`)
The read-side data lane for browsing PAST commandes (pre-cutover B2B history) — SHIPPED with ZERO `expeditions.php` edits, so it does NOT collide with the parallel expeditions UI overhaul. The UI chat wires the Commandes gather to read these views.
- **Migration `326_sales_ledger_historical_views.sql` APPLIED on prod** (maltyweb commit `78c231e`, **NOT pushed**). Read-contract doc `docs/historical-commandes-read-contract.md` committed alongside — the canonical interface spec the expeditions UI chat consumes.
- **3 read-only VIEWs over `inv_sales_ledger`:**
  - `v_sales_ledger_orders` — **7,497 historical orders.** `synthetic_order_id = CONCAT('BC:', bc_document_no)`, + `posting_date`, `customer`, `line_count`, `total_units`, `total_hl`. **MAX(posting_date) = 2026-06-05** (cutover cap holds — no forward leakage).
  - `v_sales_ledger_order_lines` — **23,529 lines.** `synthetic_order_id`, `sku_id_fk`, `sku_code`, `qty`, `hl`.
  - `v_sales_ledger_unresolved` — **222 rows / 549,387 units** = the audit tail (retired/deposit/CO2 codes), surfaced SEPARATELY, NEVER guessed into an FG SKU.
- **🔑 GRAIN REFINEMENT to the earlier PM read-contract (durable — supersedes the allnet-display assumption):** order CARDS are built from **shipment documents** (`doc_type='shipment'`), NOT the allnet-netted figure. Rationale: a commande = a delivery; credits/invoices are separate correction *documents* that cannot net *within* a document-grouped order and would clutter a browse view. **`allnet` stays the AUDIT-total basis** (the reconciliation report); **shipment-doc grain is the DISPLAY basis** for browsing past commandes. Both coexist cleanly — display ≠ audit, by design.
- **Basis (confirmed):** resolved-FG only; **B2B-scope** = `ref_customers.sale_class NOT IN ('eshop','taproom','customs_artifact','transfer','sample')` — eshop/taproom stay owned by their existing auto-rows (no double-count, R1 XOR preserved). **Cutover = 2026-06-08** (earliest forward `ord_orders.requested_date`; forward ≥ cutover, historical < cutover → clean partition with the forward `ord_*` lane).
- **Division held (lane discipline):** the data lane (I) shipped the VIEWs + read-contract with **ZERO `expeditions.php` edits** — no collision with the parallel expeditions overhaul. The **expeditions UI chat** wires the Commandes gather to read these views per `docs/historical-commandes-read-contract.md` (their lane, when their overhaul lands).
- **Year profile (orders):** 2021 858 / 2022 1294 / 2023 1123 / 2024 1435 / 2025 1835 / 2026-partial 952.
- **🔴 STILL-OPEN (carried forward):**
  1. **214/222 unresolved-SKU review CSV** (Kouros owns; SELF-HEALS as codes resolve — the audit tail shrinks, never silently absorbed).
  2. **COGS-cutover coupling flag** (maltyweb `489ab70`) → **allnet for qty, invoice lines for CHF** (the audit-side valuation rule; distinct from the shipment-doc DISPLAY grain above — don't conflate the two grains when wiring COGS).
  3. **Expeditions UI chat to wire the Commandes browse read** against the 3 views per the read-contract.
- **EQUIP (UI-wiring lane): sql + coder + ui + webapp-testing** (read-only prod smoke: past-commande browse renders shipment-grain cards + the unresolved tail surfaced separately, no eshop/taproom double-count).

## ✅ HISTORIQUE TAB (per-week-per-client historical sales) — SHIPPED + LIVE on prod 2026-06-11 (mig 329; 4th view + 8th Expéditions UI view)
**This COMPLETES the historical-sales endpoint Kouros asked for — BC-canonical history is now normalised (data lane above) AND visible per-week-per-client in Expéditions.** Only the per-week-per-client AGGREGATE was requested + built; the per-DELIVERY historical cards (the original read-contract blend) remain optional/unbuilt.
- **Commit maltyweb `3646e4f` — NOT pushed** (`feat(expeditions): Historique tab — per-week-per-client historical sales (BC views)`). 5 files / 879 insertions: mig `329_sales_ledger_weekly_client_view.sql`; `docs/historical-commandes-read-contract.md` (appended); `public/css/expeditions.css`; NEW `public/js/expeditions-historique.js` (drill collapse); `public/modules/expeditions.php`.
- **4th VIEW `v_sales_ledger_weekly_client` (mig 329) APPLIED on prod — 6,292 client-week rows.** Grain `(YEARWEEK mode-3, customer)`; SAME WHERE as the other 3 views (shipment-doc, resolved-FG, B2B-scope, cutover < 2026-06-08).
- **8th Expéditions UI view `?view=historique`:** week-blocks → client rows (sorted HL desc) → expandable per-SKU drill (reads `v_sales_ledger_order_lines`), muted unresolved footnote. **Read-only, NO status chips, NO CHF** (read-contract rules honored). Reuses the existing ISO-week filter plumbing.
- **🔑 DURABLE GOTCHA (worth carrying — any future consumer of these views MUST key on `iso_yearweek`, not `week_start`):** MySQL `YEARWEEK(d,3)` vs `STR_TO_DATE(CONCAT(YEARWEEK,' Monday'),'%X%V %W')` DISAGREE at some week boundaries → the build filters/joins on the integer `iso_yearweek` bucket DIRECTLY (NOT on the derived `week_start` date), so the weekly view and the SKU-drill window ALWAYS match.
- **Deployed SURGICALLY** (scp only the 5 files — tree dirty with packaging.php/tanks.php from other sessions; no blanket deploy). **Authenticated smoke PASSED:** KW23 (1–7 June) = 364.80 HL / 44 client rows; AlloBoissons SA drill → 8 SKU rows (MOOF/EMBF/ZEPF…); week nav → W22; zero JS errors. Smoketest account (users.id=16) reverted to viewer. Committed by pathspec; tree clean of my files; no foreign sweep.
- **🔴 STILL-OPEN (carried forward, unchanged):** (1) unresolved-SKU review CSV (self-heals); (2) COGS-cutover coupling `489ab70` (allnet for qty / invoice lines for CHF); (3) the per-DELIVERY historical cards (the original read-contract blend) remain optional/unbuilt — only the per-week-per-client aggregate was requested + built.

## KNOWN GAPS / GATES (as of 2026-06-07)
1. **Depletion gap (by design):** `ord_orders` empty; May–June B2B shipments live only in the sheet → physique OVERSTATES (e.g. ZEPB computed 806.58 vs sheet 396) until the **2026-06-08 full inventory re-anchors**. NO backfill planned — new anchor trues it up.
2. **Customer bootstrap GATED on the CRM client-list xlsx** (new mid-build input from Kouros). Bootstrap reworked: **CRM = roster truth; BC = fiscal links; sheet = enrichment**. Dry-run v1 done: 81 parens-ID links, 41 exact, 425 sheet-only, 29 parens-IDs absent from BC, 155 BC-only. Report `data/bootstrap-ref-customers-report.md`; script `scripts/bootstrap_ref_customers.php` (one-shot guard).
3. **Remaining to go-live:** authenticated `webapp-testing` smoke → flip `ref_pages.is_active=1` → logistics access-preset wiring + team onboarding. Plus Transport Express → `ref_transporters`.
4. **Email ingestion (commandes@)** = LATER arc; schema already carries the readiness columns.
5. ⚠️ **Mig-number coordination:** mig 280 was taken by the parallel visite-guidée session; next Expéditions mig must re-run `--status` + `ls db/migrations` first (index checklist rule). All Expéditions deploys were selective-rsync to avoid shipping the other session's uncommitted work.

## PM divergence-watch / risks (flagged 2026-06-07, OPEN)
- **R1 — FG-depletion is now a DERIVED READ, keep it single-homed.** `fg_stock_compute()` is the only FG-stock derivation; the Stage-2 direct-sales tracker (BOM arc ④) will WRITE `inv_consumption` (packaging pulls) — different fact (packaging consumption vs FG units), but at Stage-2 design time verify no second FG-depletion path appears and that fg-stock vs tracker agree on which source feeds what (eshop=`inv_sales_orders`, taproom=`inv_sales_bc`, B2B=`ord_*`).
- **R2 — -X cage vs -B box stock states.** RULE #3 (walkthrough): cage XOR box. Once the 06-08 cage inventory creates -X SKUs + anchors, fg-stock's per-SKU ledger must not double-count or mis-attribute -X vs -B units (eshop pulls are cage-first w/ -B fallback). Re-examine `fg_stock_compute()` after the cage anchor lands.
- **R3 — production term scope.** Verify `fg_stock_compute()` production excludes `is_tombstoned=1` and contract/white-label runs (`ref_recipes.classification`) that never enter Neb FG stock, and that parallel-run (-4/-B) rows attribute to the right SKU.
- **R4 — anchor semantics on 06-08.** Full inventory + cage anchor both write `inv_fg_stocktake`; ensure ONE coherent month_closed anchor set (anchor = latest month_closed; partial writes could split the anchor month).
- **R5 — taproom staleness.** `inv_sales_bc` is monthly → intra-month taproom depletion invisible; physique drifts up until BC ingest. Acceptable, but document on the dashboard if operators ask.
- **R6 — customer identity single-homing.** Four customer-ish surfaces now exist: `ref_customers` (sold-to), `ref_clients` (contract brewers), Shopify customers (`inv_sales_*`), BC sold-to. Bootstrap must define the overlap rule (a contract brewer who also buys beer = ref_customers row LINKED, not duplicated identity-fact).
- **R7 — visite-guidée convention.** Standing rule (parallel session, same day): every new `ref_pages` entry gets a bespoke tour content row. Add one for `expeditions` at is_active flip time.

## 2026-06-07 (LATE) — CRM-primary bootstrap + Carnet clients SHIPPED (all deployed+committed, PM live-verified: mig head=281, census 3046/68/368, Transport Express in ref_transporters)
1. **Mig 281 `281_ref_customers_contact_cols.sql` APPLIED** — `ref_customers` contact cols (email, address_line1/2, postal_code, city, canton, country_code); **`name` UNIQUE dropped → plain KEY** (`bc_customer_no` is the identity; CRM has legit duplicate names — supersedes the mig-279 UNIQUE in §Schema above); Transport Express seeded into `ref_transporters` (gate #3 transporter item CLOSED). NB parallel-arc numbering: 278+280 were taken by other sessions; **next free mig = 282**.
2. **CRM-primary bootstrap APPLIED (commit `73879bc`)** — input: full BC client export `Clients.xlsx` (2,678 rows, N° 1001–3943) from Kouros. Result: **3,046 `ref_customers`** = 2,678 CRM (68 «NE PLUS UTILISER» → `is_active=0`) + 368 sheet-only `needs_review=1`. Validation clean: all 270 `inv_sales_bc` customer_nos present in CRM; all 29 formerly-unmatched parens-IDs resolved. **Precedence model (canonical): CRM = roster truth; BC = fiscal validation; sheet = trade_channel/alias enrichment; NO fuzzy links anywhere.** Gate #2 (bootstrap gated on CRM xlsx) CLOSED.
3. **Carnet clients tab (commit `ff70261`)** — review queue + **MERGE tool**: one transaction — reassign `ord_orders` FKs → target, copy missing enrichment, `'alias:'` note on target, tombstone dup `is_active=0` + `'merged_into:'`, **NEVER delete**. Paginated directory w/ inline edits (whitelisted fields, `log_revision`). Saisie typeahead now ranks bc-linked rows first + «⚠ à valider» last (prevents picking unmerged dups).
4. **Off-trade business rule (Kouros, captured + applied):** `off_trade` = Cobra-channel retailers — COOP, Manor, Denner, Aldi, Lidl, Cobra Traders accts (incl. WITTICH), Migros EXCL. Migros Partenaire/MP, Aligro, Otto's. Applied to 50 CRM rows by **EXPLICIT ID list — substring matching is UNSAFE** (Baldinger≠Aldi, Grotto≠Otto's, PricewaterhouseCoopers≠COOP). 3 ambiguous left NULL pending Kouros: C.D.D. SA - Denner Partenaire (3230), Giro Migros (2817), Migros Golf Golfparc (2887). **Future-arc hook:** Kouros says channel classification "shall soon be automatic" — derive from Cobra-account linkage in BC rather than manual flags.
5. **R6 (ref_clients↔ref_customers link rule) NOT yet settled** — no contract brewer surfaced in the needs_review set demanding it; carry the watch into the operator merge pass.

## 2026-06-08 (OVERNIGHT, third update — all deployed+committed+PUSHED, PM-verified main==origin @ `6dd5521`)
1. **Merge-decision pipeline (`9803849` + safety fix `6dd5521`)** — suggestion engine bucketed the 368 needs_review rows: **138 strong fuzzy candidates / 58 weak / 89 privés / 83 puzzles** → review xlsx with DÉCISION columns on Kouros's desktop. Apply chain: `scripts/convert_merge_xlsx.py` (local) → JSON → `scripts/apply_merge_decisions.php` (VPS; dry-run default, `--apply` = single transaction; mirrors client_merge semantics exactly; `log_revision` actor `'batch-merge-cli'`; idempotent re-runs no-op). **Safety ruling baked in (`6dd5521`): sheet-A blank decision = NON (leave in queue), NEVER merge — a deleted cell must not act.** Fuzzy = suggestions ONLY; exemplar false match preserved in the tables: «Arches»→«Marchés» 0.92 (accent-strip artifact) — reaffirms the no-fuzzy-links canon (§2026-06-07 LATE item 2).
2. **Live stock hints in saisie (`134bbdf`)** — `fg_stock_compute()` → `window.EXP_STOCK_MAP` `{sku_id: {live_futur, physique}}`; advisory amber when qty > live_futur, **NEVER blocks (overselling is legal — production may be planned)**; edit mode adds back the order's own original qty; try/catch silent degrade. NB: hints inherit the depletion gap (gate #1) until the 06-08 re-anchor — amber thresholds only true up after it.
3. **Tour content (`a59d94d`)** — expeditions entry in `$PAGE_DESCRIPTIONS`+`$PAGE_ICONS` of visite-guidee.php. **R7 CLOSED** — flip-safe.
4. **Logistics preset — NOTHING TO BUILD:** auth arc had already wired expeditions (as `fulfilment`; id-based, rename carried) into manager + logistics_operator presets; activates automatically at flip. Gate #3 preset item CLOSED.

## 2026-06-10 — Customer name-correction (Blind Tigers) + NO customer-alias-table finding + `$editableFields` whitelist backlog
1. **Name correction APPLIED (data-only, no mig, no commit):** `ref_customers` id=1572 (`bc_customer_no`='2789') renamed «Blind Tigers Sàrl - Le Vestibule» → «Blind Tigers Sàrl». Operator chose to keep the legal form «Sàrl» (not bare «Blind Tigers»). Method = direct audited MySQL txn UPDATE on the VPS, run as www-data via `app/db.php`+`app/db-write-helpers.php`, mirroring app governance: captured `before`, `log_revision(...,'ref_customers',1572,before,{'name':...},'normal','Correction nom client: retrait du suffixe Le Vestibule')`, attributed `user_id=1` (Kouros). **Audit row id 13799 confirmed.** `bc_customer_no` stays '2789'; dead row id=2789 (Liechti) untouched; **0 orders attached → FK propagation moot.** SOT-clean, COGS-neutral.
2. **🔑 FINDING — there is NO customer-alias table.** Alias tables exist only for `ref_mi`/recipe/sku/supplier/yeast — NOT customers. Entity resolution keys on `bc_customer_no`, not a name alias, so the alias-preservation concern that applies to MI/supplier name edits is **MOOT for customers**: a customer name is free to correct without orphaning a resolution path. (The Carnet merge tool's `'alias:'` note is a free-text breadcrumb on `ref_customers` itself, not a separate alias store.)
3. **🟡 LATENT BACKLOG (no action requested):** `expeditions.php` inline client editor `$editableFields` whitelist **EXCLUDES `name`** (item-3 §2026-06-07 LATE / line ~49), so customer name corrections currently require raw SQL each time. **If name-correction recurs, add `name` to `$editableFields` with validation** (UNIQUE was dropped to plain KEY per mig 281, so dup names are legal — validate for non-empty/trim, not uniqueness) rather than hand-editing. EQUIP coder+sql+ui if built.
5. **Auth smoke DEFERRED to Kouros:** temp smoke-user creation DENIED by the permission classifier (account provisioning needs explicit operator authorization — correct call; NB the permanent smoketest acct id=16 exists, creds `secrets/maltyweb-smoketest.env` — candidate for next time instead of a temp user). Unauthenticated smoke all green (302 login redirect, lint clean, fg compute clean). Authenticated click-through = Kouros's first morning item.

**Morning runbook (Kouros, 2026-06-08):** xlsx → `convert_merge_xlsx.py` → dry-run → `--apply`; admin click-through; flip `is_active=1`; full inventory re-anchor (**R2/R3 re-exam of `fg_stock_compute()` trigger THEN**); rule on the 3 ambiguous off-trade IDs.

## 2026-06-08 — TASK #10 Stock PF board: PM ANCHOR+FLOW MODEL RULING (PRE-BUILD; resolves R4)
PM ruling delivered to orchestrator before delegating the Stock PF rebuild. Live state confirmed: loc1 = 41 April (`month_closed='2026-04'`) + **4 June** (06-08) rows; loc2 = 28 June; loc4 = 26 June; loc3 (Taproom) EMPTY (counted 06-09). Model P in force (global-pool depletion, no per-order/per-production location tag).

**THE LOAD-BEARING CALLS (canonical):**
1. **Per-location CARD = that location's latest-count-DATE snapshot only.** loc1 Production card shows the **4 June SKUs, NOT the 37 April-only leftovers** (pre-split bootstrap; a fresh full physical is a complete statement of what's standing there). Card query: per (sku, location) take rows where `counted_at = MAX(counted_at) for that location`. **Do NOT delete/tombstone the 37 April rows** — card *visibility* ≠ anchor *membership* (see #3).
2. **Cards STATIC (frozen Monday snapshot) / TOTAL LIVE.** `TOTAL physique = Σ_loc(latest-count qty) + global production since anchor − global shipments/eshop/taproom since anchor`. The Σ-anchor term and the flow window MUST be the same count rows. The 2 derived lines (−semaine-open, −all-open) come off the live TOTAL only. Orders are global (no location tag) → subtract from TOTAL, never from a card.
3. **🔴 R4 RESOLVED — anchor is PER (sku) = SKU's latest count across all locations, NOT one global `MAX(month_closed)`.** The current `fg_stock_compute()` does global `MAX(month_closed)` → picks `2026-06` → **silently ZEROES the 37 loc1 April-only SKUs** (no June row → anchor 0). THAT is the concrete R4 bug. Correct model: `anchor_qty(sku) = Σ_loc(qty at that loc's latest count of that sku)`; `flow_window_start(sku) = MAX(counted_at) over the rows feeding that anchor`. SKUs only in April bootstrap anchor on April + deplete by prod/ship since April (NOT zeroed); SKUs counted 06-08 anchor on June. Staggered counts (Taproom 06-09) advance per-SKU automatically. **This per-SKU anchor fix is a CORRECTNESS fix to fg_stock_compute() independent of the board — do it regardless.** "Stop showing April on the Production card" ≠ "stop summing April into the anchor".
4. **Taproom empty → graceful-zero** ("pas encore compté", contributes 0, NEVER blocks). Its SKUs anchor on whatever other location last counted them (or April) until 06-09.

**ARCHITECTURE (B): two functions, clean split.** Keep `fg_stock_compute()` GLOBAL for TOTAL + derived lines + per-SKU live table (**but fix to per-SKU anchor model #3**). ADD pure-read `fg_stock_location_snapshot(PDO)` keyed `[loc][sku]=>latest-count qty` for cards (NO flow math). Both read same `is_active=1` rows. **Verification gate: after a no-flow Monday count, `Σ cards == TOTAL physique`** (proves anchor model); and the 37 April-only loc1 SKUs appear in TOTAL but NOT on the Production card.

**SOT/derivation hazards preserved:** R1 (both functions READ-only, no 2nd FG-depletion path; Stage-2 tracker writes inv_consumption = different fact); R2 (cage -X vs box -B — card model is safer, just confirm eshop cage-first netting when -X anchors land); R3 (verify production leg excludes is_tombstoned/is_white_label AND contract recipes — Bamberg/Jasper +5 contract SKUs must NOT leak into Neb total, check `ref_recipes.classification`); **C5 — carry `display_family` into the board grouping** (mig 287 added it; fg-stock grouping deliberately NOT yet changed — Task #10 IS the place to split multipacks PD8/Xmas/Louis/Chef under their own header, LEFT JOIN ref_packaging_formats, group `COALESCE(display_family, format)`).

**PLACEMENT (D):** redesigned Stock PF tab (`view=stock`, currently placeholder) = 4 location cards (canonical `ref_sites.name` display strings, never re-derive from site_type) + live TOTAL + 2 derived lines + display_family-grouped per-SKU live table below; PLUS compact pinned summary (total physique / −semaine / −ouvertes) atop Commandes (the "better visual, not another page" Kouros asked for).

**BUILD ORDER:** (1) fix fg_stock_compute() → per-SKU anchor (R4 correctness fix); (2) add fg_stock_location_snapshot(); (3) rebuild Stock PF tab; (4) pinned Commandes summary. **EQUIP: sql + coder + ui + webapp-testing** (read-only prod smoke: ?view=stock renders 4 cards, Taproom "pas encore compté", multipacks split, Σ-cards==TOTAL).

### ✅ TASK #10 — SHIPPED 2026-06-09 (maltyweb commit `24d510a`, LIVE+PM-VERIFIED). Spec request 2026-06-09 was answered "already built".
**ALL 4 build-order items DONE in `24d510a` "feat(expeditions): per-location FG stock board".** The arc's "currently placeholder" wording is SUPERSEDED — do not repeat it.
- **R4 per-SKU anchor fix = SHIPPED** (NOT a follow-up; it landed inside the board commit AND earlier — `fg-stock.php` header documents anchor = latest count per (sku_id_fk, location_id_fk) via `MAX(id)` among `is_active=1`, anchor_qty = SUM across locations, anchor_date = MAX(counted_at)). The old global `MAX(month_closed)` zeroing bug is GONE.
- **`fg_stock_location_snapshot(PDO)` = SHIPPED** — keyed [loc]=>{rows[],total_units,total_hl,last_counted}; reads `ref_sites WHERE holds_fg_stock=1 AND is_active=1 ORDER BY sort_order` (so ALL 4 sites render incl. uncounted); same anchor subquery as compute() → Σ identity holds.
- **`expeditions.php ?view=stock` rebuilt** (render block L2591+; both fns called L1257-1258): stock header (anchre = comptage du <date>), 4 location cards (name + site_type label + freshness chip + HL/units; uncounted → "⚠ pas encore compté" + "—"/"0 unités"), TOTAL strip (Physique total + −sem. courante + −toutes ouvertes derived lines), display_family-grouped per-SKU live table (multipacks split, `$stockFamilyOrder` Bot/Can/Can33/Keg/multipack/Cuve), survendu/low-stock flags. Pinned compact stock strip atop Commandes (L1820, `exp-cmd-stock-strip`).
- **LIVE-VERIFIED 2026-06-09 (PM, via app/fg-stock.php CLI):** anchor_month=2026-06, anchor_date=2026-06-08, 34 SKU rows; cards loc1=550 / loc2=5976 / loc4=554 / loc3(Taproom)=NULL→0; **Σcards==Σanchor 7080==7080 PASS** (commit msg also cites 7080=7080). 8 active cage -X SKUs exist (ZEP/MOO/EMB/STI/SPY/DIV/DOA/ALT-X, ids 279-286). HTTP 301 (http→https redirect, expected).
- **⚠️ NOTE — live anchor state ≠ the §Task #10 ruling's "loc1=41 April + 4 June" premise.** Live census (PM-probed 2026-06-09): loc1/loc2/loc4 ALL on `month_closed='2026-06'`, all counted 2026-06-08 (loc1=27, loc2=28, loc4=26 rows); loc3 Taproom still 0. The April anchor and the staggered per-location-month case the R4 ruling worried about are NOT the current reality — the operator re-anchored everything to 2026-06-08. The per-SKU anchor model still HANDLES the staggered case if it ever recurs; today every SKU anchors on the same date so the model degenerates safely.
- **Taproom (loc3) empty handling = SHIPPED + correct:** location_snapshot emits the row with `last_counted=null`, card shows "⚠ pas encore compté" + 0 units, contributes 0 to TOTAL, never blocks. Its SKUs anchor on whatever other location last counted them (per-SKU model). No special-casing needed when the 06-09 Taproom count lands — it just advances loc3's card + those SKUs' anchor automatically.
- **✅ GATE ⑥ RESOLVED 2026-06-09 — back-date role gate + Stock PF board ALL COMMITTED + PUSHED to origin/main.** The previously-uncommitted `expeditions.php` back-date role gate (`$canBackdate = is_manager()||is_admin()`; operators coerced to `counted_at=today`; explicit '0' writes a real stock-out, blank skips) is now committed (`41b9a69`), along with the parallel session's Stock PF visuals (`f6d2124` stock-health visuals, `55a2010` clickable location cards). **prod == git == origin — no uncommitted prod code on the fulfilment surface anymore.** Marketing/preset work also pushed: `16f725f` = mig 290 (marketing preset id 8 + expeditions go-live) — see §2026-06-09 GO-LIVE. The "live-but-uncommitted" gate ⑥ concern is fully cleared.
- **Forward couplings still live:** task #12 stock-config admin (expose `ref_packaging_formats.display_family` + `ref_skus.stocktake_scope` + handle row_hash UNIQUE-collision on location edit per §row_hash forward-coupling); R2 cage -X vs box -B netting re-exam once -X SKUs get anchor rows (the 8 -X SKUs exist but check whether they carry stocktake rows / eshop cage-first pull).

### ✅ PF-STOCK BUILD — FULL-BOX FLOORING + PER-LOCATION PRODUCTION ATTRIBUTION SHIPPED 2026-06-09 (committed `d71d890`, pushed, live; NO migration — data + PHP: `app/fg-stock.php` + `public/modules/expeditions.php`; EQUIP coder+sql+ui +webapp-testing)
**Built per the design we consulted; supersedes the `566b9d3` production-leg fix with the operator's full-box PF rule + per-location production crediting. No mig (data + PHP).**

1. **FULL-BOX FLOORING (operator PF rule).** production = `SUM(FLOOR(prod_total_units / units_per_pack))` — floored **PER EVENT then summed** (NOT sum-then-floor). **Operator rule: PF = full boxes only; loose units from a partial run are NOT PF** — they are the future "mini side-stock" (item E below). DIV4 9901 → **412 boxes** (was 412.54). kegs/cuv `units_per_pack=1` → floor is a no-op. `prod_qty` + `physique` are now INTEGERS.
2. **PER-LOCATION PRODUCTION ATTRIBUTION.** `fg_stock_location_snapshot()` now credits floored production-since-prod-site-count to the **production-site card** (DB-derived `site_type='production'`, gated by `$isProdSite`). Renens DIV4: 37 count → **449** (37 + 412). Other sites are anchor-only. **New invariant: Σ(locations) == physique when sales=0** (verified 449+39+23 = 511).
3. **SINGLE-SOURCED (drift-proof — the Q2 requirement).** New private helper **`fg_prod_since_anchor(PDO, $globalAnchor)`** returns `{prod_site_ids, by_sku:{prod_qty:int, prod_events:int}}`; **BOTH `fg_stock_compute()` AND the snapshot call it**, so the SKU-total card and the per-location card cannot drift.
4. **INTEGER DISPLAY (Q5 #4).** unit/box columns render 0-decimals; **HL stays decimal**.
5. **STOCK ORDER.** `$stockFamilyOrder` now **Keg → Bot → Can → Can33 → multipack → Cuve**; single-unit SKUs (`sku_code` suffix `/(BU|CU)$/`, taproom-only) bucketed into **"À l'unité (taproom)" rendered LAST**. Applied to total table, filter chips, AND per-location snapshot grouping.

**Q1 verdict held: NO new setting, NO `receives_packaging` column — `site_type='production'` remains the single SoT.**

**STILL OPEN — item B (DEFERRED, likely next pending operator confirm):** expose `ref_sites.site_type` + `holds_fg_stock` editing in the Données générales sites admin (`reglages-generaux.php` `edit_site` handler currently does NOT expose `site_type`), with helper line "le conditionnement alimente le stock PF des sites de type Production" — so the operator can SEE/SET it.

**✅ ITEM 2 / item E — MINI SIDE-STOCK / GIVEAWAYS TRACKER — SHIPPED + PUSHED 2026-06-10 (maltyweb `1c95e23` on origin/main; mig 303; UNIT-COUNTS ONLY, NEVER COGS).** Packaging loose-unit remainder = `bd_packaging_v2.prod_total_units MOD ref_skus.units_per_pack`. Lives between `bd_packaging_v2` and beer-tax. As-built:
- **mig 303 `inv_side_stock_ledger`** — append-only, tombstone-only, `table_class='source'`, `corrections_policy='allowed'`. Per-SKU keyed (`sku_id_fk` INT UNSIGNED FK→`ref_skus` RESTRICT). `movement_type` ENUM(`accrual`+ / `complete_box`− / `giveaway`− / `year_end_offered`− / `adjustment`±) with enforced `chk_ssl_sign`. `bd_packaging_id_fk` BIGINT UNSIGNED FK→`bd_packaging_v2` SET NULL. `fiscal_year` CHAR(4). `submitted_by_user_fk` FK→`users` SET NULL.
- **DEVIATION (recorded):** `accrual_key` is a PLAIN nullable VARCHAR(48) written by PHP (`"$bdPkgId:$skuId"`), NOT a STORED generated column — MySQL 8.0.46 returns err 1215 when ALTER-adding a STORED generated col whose expr references a FK column that has ON DELETE SET NULL (`bd_packaging_id_fk`). `uq_ssl_accrual` UNIQUE on it gives idempotency (NULLs don't collide). *(Generalizes the mig-288/mig-292 generated-column lessons — here the SET-NULL FK was the blocker, not the FK type.)*
- **DISTINCTNESS (critical):** this is the PACKAGING-remainder fact, wholly distinct from the SALES-side `sale_class='giveaway'` (migs 300/301, sales-commercial-surface arc). NOT COGS (unit counts only; never `inv_consumption` / `ref_sku_bom` / CHF). Production-site-homed. OUT of `fg_stock_compute()` FG legs — `app/fg-stock.php` byte-unchanged (verified).
- **Writer (`form-packaging.php`):** accrual banked per-format row, hung off the per-format `resolve_packaging_sku_id()` result `$rSpec['sku_id_fk']` → **mig-265-safe parallel -4/-B attribution** (each leg shares session recipe_id, differs by format_suffix → own SKU). `complete-a-box` draw field (`side_stock_complete_box`) with refuse-don't-NULL balance check (French error, rollBack). `units_per_pack=1` (keg/cuv) → remainder 0 → no accrual.
- **Re-save / edit idempotency = TOMBSTONE-PRIOR-THEN-REINSERT** (caught + fixed on RULE-2 review; first build had 3 lifecycle bugs): on every packaging-row write, prior live `accrual`+`complete_box` rows for that `bd_packaging_id_fk` are tombstoned + `accrual_key` NULLed, then fresh rows inserted. Fixes: complete_box no longer double-inserts on edit; accrual qty updates when prod_total edited; tombstone frees the key. `giveaway`/`year_end_offered`/`adjustment` rows UNTOUCHED (distinct real-world events, not run-derived).
- **Surface (`expeditions.php ?view=side-stock`):** per-SKU live balances + ledger breakdown; operator giveaway entry (CSRF + balance-checked refuse-don't-NULL); admin tombstone (CSRF + `is_admin` gated; UPDATE sets `accrual_key=NULL` + `log_revision`); 60s live-refresh JS (maltyweb-prefers-live-visibility canon). CSS in `/public/css/`, JS in `/public/js/`.
- **Operator decisions baked in:** per-SKU keying; giveaways accumulate + settle year-end; parallel-run attribution.
- **Verification:** 22 assertions incl. full A–F edit lifecycle (B no double-insert on re-save; C accrual qty updates; F tombstone re-bank); render-smoke 302 (not 500); idempotency confirmed; clean tree.
- **🔴 OPEN DOWNSTREAM (the remaining Item 2 work) = YEAR-END-SETTLE CRON, DOCUMENTED-NOT-BUILT.** Beer-tax hook inline-JOIN shape recorded in mig 303: `SELECT sku_id_fk, SUM(qty_units) FROM inv_side_stock_ledger WHERE movement_type='year_end_offered' AND fiscal_year=? AND is_tombstoned=0` → units→HL via `ref_skus.hl_per_unit`. No-double-count rationale included (accruals untaxed; `complete_box`→FG via normal path; giveaways net into year-end residual, taxed once). NOT YET BUILT: a year-end-close cron that WRITES the `year_end_offered` draws + feeds beer-tax. **Canonical balance: Σ(prod MOD upp) − Σ(complete_box) − Σ(giveaway) − Σ(year_end_offered) ± Σ(adjustment).**

**STILL-OPEN CARRYOVERS:** sales-leg per-location settlement (stock-movement-saisie arc). **⛔ NO DATE→DATETIME migration to do — REDUNDANT, do NOT dispatch one.** Same-day settlement is ALREADY fully solved with NO migration: prod-leg via Option-2 tiebreak (`d3fb954`), and ESHOP + cross-site TRANSFER legs via **Task A** (`2192153`, 2026-06-10, `app/fg-stock.php`, NO mig). Mechanism = the EXISTING `inv_fg_stocktake.created_at` (already a `timestamp` — live-verified 2026-06-10; `counted_at` STAYS `date` = business truth) used purely as the same-day tiebreak. The only same-day gaps that REMAIN are **B2B** (no ship-time finer than `requested_date`) and **taproom** (month-grained period). Nothing is gated on the Mon 2026-06-15 cage count for this — the cage count just lands an anchor. **🔴 CORRECTION 2026-06-10: the B2B gap is NOT "intentional/safe" — it is a REAL computation bug (global anchorDate pollution drops legitimate same-day ships); reclassified + ruled in §B2B EXPEDIE LEG = REAL COMPUTATION BUG below. The taproom month-grained gap is still genuinely deferred.**

### ✅ TWO PF-STOCK FIXES SHIPPED 2026-06-09 (committed + pushed + deployed; outage fix `ae297c2`, production-leg fix `566b9d3`; EQUIP coder+sql)
**Both on origin/main (afe1dcd..566b9d3), deployed (selective rsync + chown www-data). No migration — data + PHP only.**

1. **Expéditions OUTAGE FIX (`ae297c2`) — 500 on the whole PF-stock page.** `exp_dow_fr()` (the French-weekday helper added in `afe1dcd`, the fresh-chip feature, an earlier same-day session) was defined NESTED inside the stocktake render function → undefined at the EARLIER call site (~L2824, the "✓ compté" fresh-chip branch). PHP nested function defs are **runtime-declared in call order**, so a helper used before its (later) nested definition fatals. That branch only fires when a count EXISTS this week → today's stocktake tripped "Call to undefined function exp_dow_fr()" → 500. **Fix: hoisted `exp_dow_fr` to the top-level date-helper section** (compile-time hoisted; also kills a latent cannot-redeclare on double render). **DURABLE LESSON: keep page helpers TOP-LEVEL — never nest a helper inside a render function if any earlier code path may call it.** (Convention candidate for the `coder`/`ui` skill if it recurs.)

2. **`fg_stock_compute()` PRODUCTION-LEG FIX (`566b9d3`) — partial count poisoned the global cutoff, dropped same-day production.** The bug PM predicted: R4 anchor SUM is per-(sku, location) but the flow windows still ran off a single global `$anchorDate`. A partial count (today: Taproom/pos-site only, 2026-06-09) advanced the global cutoff → same-day production-site packaging vanished from physique (DIV4 9901 btl, DIVB 2592 btl invisible). **Fix applied exactly as consulted:**
   - **Production site id(s) DB-derived** from `ref_sites WHERE site_type='production' AND is_active=1` (id=1 Zgeg/Renens today; **set-valued, no hardcode**).
   - **Per-SKU production cutoff = MAX(counted_at) for that SKU at the production site(s)**; `COALESCE` fallback to global `$anchorDate` for SKUs never counted there.
   - **`run_type='cuv'` EXCLUDED from the prod leg** (cuve-de-service = filled-at-client = sold, never FG stock). Three independent guards make cuv SKUs (EMBV/MOOV/ZEPV) vanish from the computed FG table with no negatives: cuv has no anchor + no `ord_order_lines` depletion path + `stocktake_scope='none'`.
   - **Sales legs (expedie/eshop/taproom) STILL on the global cutoff** — per-location settlement DEFERRED to the stock-movement-saisie arc; documented inline with a SCOPE NOTE near `$anchorDate` (prod-fixed ≠ all-fixed; taproom period>anchorMonth can exclude the current month on partial-count days).
   - **Verified live (www-data probe):** DIV4 prod_qty=412.54 (9901/24), DIVB=108 (2592/24), ZEPV absent; non-production-anchored SKUs (DIB4/DIVF/EPH4B) compute prod_qty=0, no crash.

**DURABLE — packaging-inflow site model:** packaging inflow ALWAYS lands at the **production site (Renens, DB-derived)**. Inter-site moves (Renens→Crissier→Nausikraft→Taproom) = the future **stock-movement saisie arc** (NOT built). `ref_sites` today: id1 Zgeg/Renens (production, holds_fg), id2 Zeubi/Crissier (warehouse, holds_fg), id3 Taproom/Renens (pos, holds_fg), id4 Nausikraft (consignment, holds_fg, customer_id_fk=1).

**OPEN / DEFERRED (flag at stock-movement-saisie arc):**
- **Sales-leg per-location settlement** — expedie/eshop/taproom legs still use the global `$anchorDate`; per-location settlement deferred to the stock-movement saisie arc (the prod leg is now per-SKU-per-site; the sales legs are NOT). Documented inline as a SCOPE NOTE.
- **Same-day count+package edge — `counted_at` is a DATE with strict `>`** → a SKU counted AND packaged on the same date can mis-settle (the count and the production happen on the same calendar day; DATE granularity + `>` cannot order them). Needs **DATETIME granularity on `counted_at`** — **worth doing before/with the Monday 2026-06-15 cage count** (cage anchor is the next big partial-count event that could re-trip this class).

## ✅ ITEM B SHIPPED 2026-06-09 — settings-driven «which sites packaging feeds» (committed `82526cb`, pushed, live; NO migration — PHP only: `reglages-generaux.php`; EQUIP coder+ui)
The capability the operator originally framed as "set in settings" is now real: in **Données générales → sites admin**, both the **add_site AND edit_site** forms expose:
- **`site_type`** — select (Production / Entrepôt / Point de vente / Consignation). Handler reads via `post_str ?? default`, validates with `must_be_one_of` (throws on invalid, mirroring the adjacent country dropdown), persists in INSERT/UPDATE.
- **`holds_fg_stock`** — toggle, coerced to 0/1.
- Both before/after states logged via `log_revision`.
- Helper line under the sites card: **"Le conditionnement alimente le stock PF des sites de type « Production »."** (makes the SoT coupling explicit to the operator).

**Q1 verdict HELD: NO new setting, NO new column (`receives_packaging` NOT added), NO migration — `site_type` stays the single SoT.** Setting a site's `site_type='production'` is now how the operator declares which site(s) packaging feeds; `fg_stock_compute()`'s production-leg already DB-derives prod sites from `ref_sites WHERE site_type='production'` (see PRODUCTION-LEG FIX above), so this is purely the missing admin UI for an existing SoT — no derivation change. Reused existing `rg-select`/`rg-toggle` markup, no inline CSS. **Verified:** VPS `php -l` clean; rollback-wrapped UPDATE probe valid; 4 sites pre-select correctly (id1 production / id2 warehouse / id3 pos / id4 consignment).

**PF-stock arc carryovers after item B (item B was the last DEFERRED PF-stock UI piece):**
1. ✅ **CLOSED 2026-06-09** — same-day count+package carryover resolved via Option 2 surgical tiebreak (NO DATE→DATETIME migration). See §SAME-DAY TIEBREAK below.
2. Sales-leg per-location settlement — stock-movement-saisie arc (see PRODUCTION-LEG FIX OPEN/DEFERRED).
3. Future **item E** packaging mini side-stock — build WITH the packaging-form "units from older runs" field (see FULL-BOX FLOORING §item E).

**⚠️ MIGRATION-NUMBER CONTENTION (flagged 2026-06-09):** a concurrent session is taking **293** (`293_loss_model_no_liquid_subtraction.sql`) + **294** (`294_ord_orders_source_ref.sql`) and modifying `app/packaging-stats.php` + `form-packaging.php` — NOT this session's work. **MIGRATION HEAD = 292 applied / NEXT FREE for this session = 295.**

## ✅ SAME-DAY TIEBREAK SHIPPED 2026-06-09 — same-day count+package carryover CLOSED via Option 2 (surgical tiebreak, NO migration; committed `d3fb954`, pushed, live; data-layer only — `app/fg-stock.php`; EQUIP coder+sql)
Closes PF-stock carryover (1) WITHOUT the DATE→DATETIME migration. The bug: a beer counted AND packaged on the SAME date (e.g. EMBF 2026-06-08) had its same-day production-site packaging DROPPED, because the per-SKU production anchor used a strict `event_date > prod_anchor` cutoff against a DATE `counted_at` — the same-day packaging tied the cutoff and fell out.

**Implementation (`fg_prod_since_anchor()`):**
- Per-SKU production anchor now uses a single **`ROW_NUMBER() OVER (PARTITION BY sku_id_fk ORDER BY counted_at DESC, id DESC)`** window pick (rn=1), exposing the WINNING production-site count's BOTH `counted_at` (→ `prod_anchor`) AND `created_at` (→ `prod_anchor_ts`). **NOT two blind MAXes** — the PM-flagged gotcha (two independent MAX() could pair a `counted_at` from one row with a `created_at` from another). One window row = one coherent count.
- Cutoff predicate: `event_date > COALESCE(prod_anchor, $globalAnchor) OR (prod_anchor IS NOT NULL AND event_date = prod_anchor AND submitted_at > prod_anchor_ts)`.
- **Primary ordering on business `event_date`**; `submitted_at > prod_anchor_ts` (created_at) only breaks SAME-DATE ties. NULL-safe for never-counted SKUs (collapses to pure `$globalAnchor` cutoff). Empty-prodSites fallback unchanged. All prior guards kept (cuv-exclude, tombstoned, white_label, FLOOR-per-event, COALESCE).
- SINGLE helper → both `fg_stock_compute()` AND `fg_stock_location_snapshot()` inherit the predicate; card vs SKU-total can't drift.

**OPUS INDEPENDENT VERIFICATION (old-vs-new predicate diff across ALL SKUs):** exactly ONE SKU changed — **EMBF**, the genuine 2026-06-08 collision (prod-site count `created_at` 13:54:13; EMBF packaging `event_date` 06-08 submitted 14:53:07 → after the count → tie-broken IN → **OLD dropped=0, NEW=300 kegs recovered**). NO other SKU perturbed: DIV4=412 / physique=511, DIVB=108, ZEPV absent, never-counted (DIB4/DIVF/EPH4B)=0. **Σ(locations)==Σphysique=8312.**

**Decisions LOCKED (durable):**
- `counted_at` STAYS DATE — business-truth column, mirrors `bd_packaging_v2.event_date`.
- `created_at` is canonically "when counted" for maltyweb-form / manual rows.
- `form_timestamp` NULL = latent Google-Form-era gap; NOT fixed now.
- **NO DATETIME migration** — rejected for blast radius on every DATE-equality consumer.
- Sales legs (expedie/eshop/taproom) STILL deferred to the stock-movement-saisie arc.

**ACCEPTED RESIDUAL (inline SCOPE NOTE in `app/fg-stock.php`):** a run physically-before but submitted-after a same-day count → rare double-count, left un-clamped ON PURPOSE. If it ever bites, the fix is an explicit count timestamp — NOT a clamp.

**Smoke:** 302 login-redirect (no page creds in this context); change is data-layer only, both stock functions proven non-throwing + data hand-verified → accepted. **NO migration number consumed** (293/294/295 are parallel sessions'). MIGRATION HEAD = 295; NEXT FREE = 298 (296/297 pending on VPS, BBT-vide arc) — re-check `migrate.php --status` + `ls db/migrations` before assigning.

## 2026-06-08 — Multipack display-family split (mig 287; SHIPPED+verified+committed `f97bcb9` local-only; PM-verified live)
**Stocktake (`expeditions.php` `?view=stocktake`) now groups curated/assortment gift packs under their own "Multipacks" header, split away from Bouteille.**

**CONFIRMED TAXONOMY (Kouros via AskUserQuestion 2026-06-08) — Multipacks = the curated/assortment gift packs ONLY:** Pack Découverte (PD8, format_id **19**), Xmas Pack (XMASPACK, **20**), Pack Louis (PAL, **21**), Pack du Chef (PAC, **23**), + future DGD pack when it exists. **The 6×4 packs (-4 SKUs) STAY under Bouteille/Canette** — a 6×4 is a presentation of the same-beer 24-pack, not a multipack. The 24-pack carton itself stays Bouteille (that's the "bottle" the multipacks are split away from). Rule of thumb: multipack = **cross-beer assortment**; -4/-B = same-beer presentation.

**Mechanism (the PM-recommended one — a per-format display-grouping column, NOT a hardcode):**
- **Mig 287 `287_ref_packaging_formats_display_family.sql` (APPLIED, head=287, 0 pending; PM live-verified — NEXT FREE = 288):** `ALTER TABLE ref_packaging_formats ADD COLUMN display_family VARCHAR(32) NULL` (placed after `derived_from_format_id`; `run_type` is not the last col — no functional difference) + `UPDATE … SET display_family='multipack' WHERE id IN (19,20,21,23)`. All other formats keep `display_family NULL`. **Live-confirmed 4 seed rows = 19/20/21/23.**
- **`expeditions.php` (view=stocktake):** `$stAllSkus` LEFT JOINs `ref_packaging_formats` and selects `COALESCE(f.display_family, s.format) AS display_family`; family-grouping groups by `display_family` (fallback to format); `'multipack'` added to `$stFamilyOrder` (after Keg) + label "Multipacks"; `exp_format_family()`/`exp_family_label()` gained the `'multipack'` case. **Cage extraction (suffix -X) and `stocktake_scope` visibility logic UNCHANGED.** JS is DOM-driven (`data-family`) — no JS change.
- **Practical render today:** only **PD8 + XMASPACK** appear under "Multipacks" (both `stocktake_scope='base'`); **PAC/PAL are `scope='none'` → stay hidden** until activated (matches "packs découvertes et xmas currently are the ones we have activated").
- **Verified:** 153 active SKUs (count unchanged after the JOIN), `php -l` clean, curl 302. **ZERO COGS/SOT impact** — `fg_stock_compute()` keys on `sku_id`, untouched.
- **Commit `f97bcb9` on maltyweb main — NOT pushed (local only).**

**FORWARD COUPLING (carry into the cited future builds):**
- **Task #12 (Stock-config admin section):** `ref_packaging_formats.display_family` is now the **editable per-format display-grouping field**, alongside `ref_skus.stocktake_scope` — BOTH get exposed in that config UI.
- **Task #10 (Stock PF board rebuild reading `fg_stock_compute`):** the Stock PF tab's OWN format-family grouping was **deliberately NOT changed** in this commit — **multipacks must be split there too** when `fg-stock.php`'s query is rebuilt; carry `display_family` into `fg_stock_compute()` then. (This is the one place the new grouping is not yet honored — flag at Stock PF rebuild time.)

## 2026-06-09 — TAPROOM SINGLE-UNIT-SKU GAP: ✅ DGDBU SHIPPED + contract beers rescoped out of taproom + weekday-chip bug fixed (build LANDED; the spec below is now as-built)

### ✅ SHIPPED 2026-06-09 (three landings, no migration — data + one PHP file; idempotent; /tmp helper cleaned)
1. **DGDBU CREATED — `ref_skus` id=315** (recipe 31 DrunkBeard - Galactic Drift, format_id=5 BU), via the **replicated SDC create path** (the canonical format-activation INSERT). `hl_per_unit=0.00330` (read from `ref_packaging_formats.id=5`, NOT hardcoded — zero-drift chain held), `bom_template_id=5` (`sdc_bom_template_for_format`), `row_hash=sha256('31|5|DGDBU')`, `is_packaging_line=0`, `is_direct_sales=0` (matches DGDB/DGDF family — **flagged to Kouros** since these family flags are 0), `units_per_pack=1`, `stocktake_scope='single'`, `last_modified_by='web'`; `log_revision` written. `sdc_recompile_recipe_packaging(31)` produced a **4-line packaging BOM** (PKG_BOT_PIVO bottle, PKG_BOT_CROWN_CAPS, PKG_LABEL_DGD, PKG_TEA_BOT_CH); **the liquid line comes from the Node build pipeline (0 at PHP-recompile time — same as ALL BU siblings, NOT a defect).** Conforms 6/6 to the BU sibling template. So the original "create DGDBU, then set scope='single' as a separate one-shot" plan was executed in one create with scope set at INSERT.
2. **CONTRACT BEERS OUT OF TAPROOM (Kouros decision):** BAMB(313), BAMF(309), JASPB(312), JASPF(308) `stocktake_scope` **'base'→'cage'** (= production+warehouse only, EXCLUDES POS/Taproom) — Chien Bleu contract beers are never at the taproom. `log_revision` per row. JASPL was already 'none'. **DECISION RECORDED: NO contract singles created (BAMBU/JASPBU NOT created)** — supersedes the "create only if Kouros confirms" open question with a definitive NO.
3. **WEEKDAY-CHIP BUG FIXED — `public/modules/expeditions.php`** FG-stocktake freshness chip hardcoded the weekday literal "compté lun." (assumed Monday counts). Added `exp_dow_fr(DateTimeImmutable)` mirroring `sessions.php::sd_date_fr()`'s French day array; **both chip sites now DERIVE the weekday** (no hardcode). Verified 2026-06-09 → "mar.". Committed **`afe1dcd`**, deployed (`php -l` clean), **on origin/main**.

### ✅ BOTH OPEN FLAGS NOW CLOSED (Kouros decided, 2026-06-09 — verified live)
- **DOCB rescoped OUT of taproom — DONE.** Kouros: Docks NEIPA not sold at taproom. **DOCB(id21)+DOCF(id22) `stocktake_scope` 'base'→'cage'** (production/warehouse only, excludes POS), `log_revision` written. So the FULL set now scoped OUT of taproom POS (base→cage) = **BAMB, BAMF, JASPB, JASPF (contract) + DOCB, DOCF (collab, not taproom-sold)** = 6 SKUs, all verified live = 'cage'.
- **DGDBU `is_direct_sales` RESOLVED → 1 — DONE.** Kouros: "not only taproom sold, but units yes" = the single bottle IS direct-sales; the box DGDB / keg DGDF stay wholesale at `is_direct_sales=0`. **Only id 315 (DGDBU) flipped to 1**, verified live. This UNBLOCKS the Phase-2 cage/COGS engine's `is_direct_sales`-gated SKU set for DGD singles. (Supersedes the "flagged to Kouros, family flags are 0" note in landing #1 — the single is intentionally 1, the pack/keg intentionally 0.)
- **DEFERRED count-move — DONE** (Kouros confirmed taproom stocktake fully inputted). Executed via a one-shot CLI that **replicated the canonical `expeditions.php` FG-stocktake write path** (snapshot loc3 → txn → `log_revision`; NO ad-hoc bare SQL on the hash key — used the upsert + STORED-generated-`row_hash` path the handler uses, per the row_hash incident). Result: **`inv_fg_stocktake` id=351 DGDBU @ loc3 (Taproom)**, counted_at 2026-06-09, qty=21, source='manual-adjustment', submitted_by='Olivier Barral', active (row_hash unique, no collision); the **DGDB stopgap row id=301 (qty 1.0 box) set `is_active=0`**. Corrected taproom DGD contribution **0.0792 HL (1 box) → 0.0693 HL (21 units)**. Snapshot `loc3-20260609-132930.json` written. All idempotent, /tmp helpers cleaned, no migration.

**Net taproom-stocktake state is now correct:** every taproom-sold beer has its BU/CU single at `scope='single'`; non-taproom beers (contract Chien Bleu + Docks collab) are `scope='cage'` (production/warehouse only). **No structural unit gaps remain for taproom.**

---
*(The pre-build spec below is retained as the model reference; the candidate-set / create-path / scope-lever reasoning is now as-built above.)*

**Context:** Olivier inputting taproom inventory. Taproom counts singles (-BU bottle / -CU can). Some beers have a 24-box (-B, `format_id=1`) but NO single-unit SKU → operator stopgap entered fractional packs (e.g. 21/24 against DGDB) to make HL come out right. Kouros: create the missing single SKUs (so they appear in taproom stocktake going forward) — NO hardcoding, reroute from the SKU master.

**THE SKU MASTER + VOLUME DERIVATION (verified live):**
- **`ref_skus` is the SKU master.** Key cols: `recipe_id`(→ref_recipes.id), `format_id`(→ref_packaging_formats.id), `format`(display label e.g. 'Bot'/'Can'), `hl_per_unit`(DECIMAL — COPIED from the chosen format's hl_per_unit at create), `units_per_pack`(NOT NULL DEFAULT 1), `stocktake_scope` ENUM('base','cage','single','none') DEFAULT 'none', `is_packaging_line`, `is_direct_sales`, `row_hash`.
- **Single-unit ≠ a column on the pack — it is its OWN `ref_skus` row** on the SAME recipe but a DIFFERENT `format_id`. A pack and its single are siblings under one recipe, NOT parent/child. Bottle pack = `format_id=1` (B, 24×33cl, hl_per_unit 0.0792). Bottle single = `format_id=5` (BU, "Bottle unit single 33cl", hl_per_unit 0.0033). Can single = `format_id=14` (CU, single 50cl, hl_per_unit 0.005). So DGDBU = a NEW ref_skus row: recipe 31, format_id 5, format 'Bot', hl_per_unit 0.0033, units_per_pack 1, stocktake_scope 'single'.
- **Volume auto-derives, NEVER hardcoded:** `ref_skus.hl_per_unit` is copied from `ref_packaging_formats.hl_per_unit` of the chosen format (itself = units_per_format × `dbc_container_types.hl_per_unit`, zero-drift chain — volume-dimension.md). A new DGDBU created via the canonical path inherits 0.0033 from format 5 automatically.

**WHAT MAKES A SKU APPEAR IN TAPROOM STOCKTAKE = `ref_skus.stocktake_scope` (the channel flag — NO hardcoding needed).** `expeditions.php` `exp_st_scope_allowed($scope,$siteType)` (the visibility matrix, ~L1626): `base`→all sites; `cage`→production+warehouse; **`single`→pos ONLY (= Taproom)**; `none`→never. So a single-unit SKU **MUST be `stocktake_scope='single'`** to show for Olivier at Taproom (site_type='pos'). This is also the **DOCB exclusion lever**: DOCB/DOCF are `scope='base'` today; we simply do NOT create a DOCBU (or if created, leave scope≠'single') — no hardcode, the absence of a 'single'-scoped Docks SKU keeps it out of taproom count. The 26 existing 'single'-scope SKUs are exactly the BU/CU singles.

**THE GAP (verified live 2026-06-09) — recipes with a bottle/can PACK but NO single sibling, active SKUs:**
- **r31 DGDB (DrunkBeard - Galactic Drift)** → missing **DGDBU** (THE live problem; DGDB has stocktake rows incl. the 8/24 + 121 stopgaps at loc1).
- **r20 BAMB (Chien Bleu - Bamse, Contract)**, **r21 JASPB (Chien Bleu - Jasper, Contract)** → missing BU. **Contract beers — likely NOT taproom-sold; confirm with Kouros before creating + scoping 'single'.**
- **r29 DOCB (Docks - NEIPA)** → **EXCLUDED per Kouros (not sold in taproom).**
- The `recipe_id NULL` group (COLLAB12/COLLAB4PACK/PAC/PAL/PD8/XMASPACK) is a FALSE POSITIVE — composites/multipacks, not single-beer packs; ignore.
- **So the real "create a single" candidate set = DGDBU (now/deferred), + BAMBU/JASPBU IFF Kouros confirms Chien Bleu sells singles at taproom.** All core Neb beers already HAVE their BU/CU (the 26 'single' SKUs). DOC excluded. Verify the list with Kouros — do NOT auto-create contract singles.

**CANONICAL CREATE PATH = format-activation in Salle de contrôle (`public/modules/salle-de-controle.php` ~L262, action that INSERTs ref_skus).** Operator picks recipe + format → INSERT `(sku_code, recipe_id, format_id, format, hl_per_unit FROM the format, is_active=1, row_hash=sha256(recipe_id|format_id|sku_code), bom_template_id, last_modified_by='web', last_seen_at, imported_at)` + `log_revision` + post-commit `sdc_recompile_recipe_packaging($recipeId)`. **⚠️ GAP in that action: it does NOT set `stocktake_scope` (defaults 'none') or `units_per_pack` (defaults 1).** For a single, units_per_pack=1 is correct, but **scope stays 'none' → the new single would NOT appear in taproom until scope is set to 'single'.** Two clean options: (a) one-shot direct SQL after creating via SDC (set scope='single'); (b) better/durable — the planned **task #12 stock-config admin** exposes `stocktake_scope` (alongside `display_family`) as an editable field, which is the proper home for flipping a SKU's channel visibility. PM rec: create DGDBU via SDC format-activation (auto-correct hl_per_unit + audit + BOM recompile), then set scope='single' (direct SQL one-shot now; task #12 UI later). `sku_code` is operator-chosen ('DGDBU'); UNIQUE collision-guarded on (recipe,format).

**COGS / derivation implications:** a single-unit SKU is a REAL packaging line (`is_packaging_line=1` like its siblings) and gets a BOM via `sdc_recompile_recipe_packaging` — it = 1× (bottle+cap+label+TEA+liquid), the INTRINSIC single-bottle BOM (walkthrough RULE #3). It maps to the **SAME recipe** as the pack (recipe 31 for DGDBU) → same liquid/COGS basis; only the format (and thus units_per_pack=1, hl_per_unit=0.0033) differs. NOT a pure count surface — it flows into BOM/COGS like any SKU. Double-count guard (walkthrough cardinal invariant): a bottle is produced+consumed ONCE — selling a single vs selling it in a box are different sale events drawing the same physical bottle; no double-count AT SALE.

**STEP 1 (deferred, AFTER stocktake input) — correct the DGDB stopgap → DGDBU:** counts live in **`inv_fg_stocktake`** (cols: sku_id_fk, location_id_fk, counted_at, month_closed, qty DECIMAL(10,4), source ENUM, is_active, **`row_hash` = STORED GENERATED** `sha256(fgct|sku_id_fk|location_id_fk|counted_at)` since mig 288 — location & hash can't diverge). Correcting = (i) create DGDBU; (ii) the stopgap row was qty=8 (or 21) recorded against DGDB sku_id at the 24-box scale — to "move into DGDBU as 21 units": INSERT/UPSERT a DGDBU row qty=21 at the same location/counted_at AND zero-out (or delete via is_active=0 + audit) the fractional DGDB stopgap row. **Do it via the EXISTING edit feature** (`expeditions.php` FG-stocktake date-scoped edit, manager/admin) NOT ad-hoc SQL — the row_hash incident (2026-06-08, see §INCIDENT) proves ad-hoc UPDATEs that touch sku/location WITHOUT going through the form/generated-hash path corrupt the dedup key. The generated row_hash now makes a sku-change auto-rehash, but route through the audited edit path regardless. Snapshot + log_revision.

**SEQUENCING:** (a) NOW = identify gap (done above) + confirm the create-list with Kouros (DGDBU yes; BAMBU/JASPBU only if Chien Bleu taproom-sold; DOC excluded). (b) Create the singles via SDC format-activation + set scope='single'. (c) DEFERRED to after taproom input = the DGDB→DGDBU count correction via the edit feature. **EQUIP for the build: sql + coder + ui + webapp-testing** (the create touches SDC PHP + a scope flip + a stocktake correction via the UI edit path; smoke ?view=stocktake at Taproom shows the new single).

## 2026-06-08 — ref_sites canonical-name fix + 06-08 stocktake misfile correction (DATA + NAME only, no code/schema; PM-verified live)
Two live fixes done in ONE transaction as kouros (user id 1), `qc_flag='normal'`, comment marks Claude-assisted; full `log_revision`. **PM-verified against live DB 2026-06-08.**
1. **`ref_sites` renamed (name col only; `site_type` untouched, ids stable):** id1 → **«La Nébuleuse - Zgeg (Production)»** (still `site_type=production`); id2 → **«La Nébuleuse - Zeubi (Logistique)»** (still `site_type=warehouse`). «Zgeg»=Renens/Production, «Zeubi»=Crissier/Logistique — Kouros's colloquial nicknames, now the **canonical display names**. id3=Taproom(pos), id4=Nausikraft(consignment) unchanged. → use these exact display strings in any UI/report that names a site; never re-derive from site_type.
2. **06-08 stocktake misfile corrected:** Kouros entered Crissier/Logistique stock under the **Production** location (loc1) on 2026-06-08. **Moved 28 `inv_fg_stocktake` rows loc1→loc2** (`WHERE month_closed='2026-06' AND DATE(counted_at)='2026-06-08'`), row-count-guarded at exactly 28. **Final state (LIVE-VERIFIED): loc1 = only the 41 April (`month_closed='2026-04'`) anchor rows; loc2 = the 28 June (`2026-06-08`) rows; loc3/loc4 still EMPTY** (Kouros still inputting Taproom + Nausikraft).
   - **Anchor-semantics note (R4):** the 06-08 inventory now lives under `month_closed='2026-06'` at the **warehouse** location, distinct from the April **production** anchor — so `fg_stock_compute()`'s "latest month_closed" anchor moves to 2026-06 for loc2's SKUs while loc1 stays on 2026-04. When the re-anchor / cage-anchor lands, confirm `fg_stock_compute()` resolves the anchor **per location** (not globally) or the location split will mis-pick. Cage (-X) + Taproom + Nausikraft anchor rows still pending.

## 2026-06-08 — INCIDENT + DURABLE FIX: `inv_fg_stocktake.row_hash` divergence (hash-keyed row mutated without rehash)
**INCIDENT (root cause understood, data ALREADY repaired+verified by operator):** the 06-08 misfile *correction itself* corrupted 28 rows. The stocktake dedup/upsert key is `row_hash = sha256("fgct|{sku_id_fk}|{location_id_fk}|{counted_at}")`, app-written, `UNIQUE uniq_row_hash`, INSERT … ON DUPLICATE KEY UPDATE (qty only, NOT location). The ad-hoc SQL `UPDATE location_id_fk 1→2` did **NOT recompute row_hash** → 28 rows sat at loc2 still hashed as loc1; the operator's later loc1 (Production) submit hash-COLLIDED with them and overwrote qty. **Repaired** in one txn from `audit_row_revisions` (before=Crissier/batch1, after=Production/batch3): restored 23 Crissier + recreated 23 Production + fixed 5 stale hashes; final loc1=27/550u, loc2=28/5976u, loc4=26/554u; **row_hash integrity 0 mismatch across all 81 June rows.** Data clean.
**DURABLE FIX — PM RULING 2026-06-08 (APPROVED, with one mandatory amendment): convert `row_hash` to a STORED GENERATED column** `CHAR(64) AS (SHA2(CONCAT_WS('|','fgct',sku_id_fk,location_id_fk,counted_at),256)) STORED`, UNIQUE preserved → location_id_fk and hash can NEVER diverge again (fix-by-construction; the "remember to recompute hash on location change" lesson is thereby MOOT). Generated col, NOT a trigger (a trigger is bypassable by direct UPDATE exactly as this incident was; generated col is structural). STORED not VIRTUAL (UNIQUE index stability). **PM-VERIFIED LIVE before ruling:**
  - **SOLE writer = `expeditions.php:746` INSERT.** Every other ref to `inv_fg_stocktake` (warehouse.php, warehouse-export.php, app/fg-stock.php, maltytask lib/inventory*.{js,ts}, mysql-inventory.ts) is **read-only SELECT**. `sync-stocktake.js` writes BSF Sheets, never MySQL. **`source='bsf-stocktake'` has ZERO code writers** (appears only in mig 066's ENUM def). → converting is safe from rejected-INSERT; the form INSERT drops `row_hash` from its column list, ON DUPLICATE KEY fires off the generated UNIQUE.
  - **🔴 MANDATORY AMENDMENT — the premise "data is 100% hash-consistent" is FALSE for 41 rows.** `WHERE row_hash <> SHA2(CONCAT_WS('|','fgct',sku_id_fk,location_id_fk,counted_at),256)` = **41 rows**, ALL the `source='bsf-stocktake'` **April anchor** (`month_closed='2026-04'`, `counted_at IS NULL`), hashed by the OLDER legacy-import formula. June (81) is consistent; April (41) is NOT. On DROP+ADD, those 41 silently re-mint to `fgct|sid|loc` (CONCAT_WS SKIPS NULL counted_at — note PHP `sha256` would emit a trailing `|`, so PHP-formula ≠ generated-formula in the NULL case; generated col is the more-correct canonical). **Tolerable ONLY because nothing re-derives those hashes** (no bsf-stocktake writer). **Collision check PASS: all 122 rows → 122 distinct generated hashes (0 collisions); 2nd UK `uniq_sku_month_location_response` also 122/122 distinct.** Build must do this KNOWINGLY + document it in the migration comment; verify post-convert: 0 rows where `row_hash <> SHA2(...)`, 122 distinct, April-2026 active anchor still 41 rows.
  - **BUILD: own migration (confirm next free # via `migrate.php --status` + `ls db/migrations` — parallel sessions take numbers, the 278 collision is precedent; local index showed head 287). DDL = DROP INDEX uniq_row_hash → DROP COLUMN row_hash → ADD generated STORED (collation `utf8mb4_unicode_ci` to match, or mixed-collation throw) → ADD UNIQUE uniq_row_hash. No new schema_meta row (existing table). `expeditions.php` edit: drop row_hash from INSERT col-list + binding (~L748/749/763/782); KEEP the `$rowHash = hash('sha256',…)` compute — still used by the before-snapshot `SELECT … WHERE row_hash=?` at L767; ON DUPLICATE KEY UPDATE stays (counted_at no-op since match is BY the hash). EQUIP: sql + coder (NO ui — pure backend). Proof: dry-run 0 mismatch; idempotent double-submit → 1 row; UPDATE location_id_fk auto-shifts hash; self-clean test rows.**
  - **🔗 FORWARD COUPLING for task #12 stock-config admin + task #10 Stock PF:** once row_hash is generated, any admin UI editing `location_id_fk` on a stocktake row AUTO-REHASHES — desired, but it can now collide into an existing (sku,loc,date) row → the UNIQUE rejects/the ON DUPLICATE merges. The admin edit path MUST handle the UNIQUE-violation/target-exists case (the exact scenario that caused this incident). Note in the #12 brief.

**✅ SHIPPED + LIVE (mig 288, 2026-06-08 — agent conn dropped mid-run; PM live-verified + reconciled).** Generated col IS live and working: `row_hash CHAR(64) GENERATED ALWAYS AS SHA2(CONCAT_WS('|','fgct',sku_id_fk,location_id_fk,counted_at),256) STORED NOT NULL`, `UNIQUE uniq_row_hash`. 122 rows / 122 distinct hashes / 0 collisions; June repair intact (loc1=27/550u, loc2=28/5976u, loc4=26/554u); April anchor 41. Form deployed (row_hash dropped from INSERT). 0 orphan + 0 NULL sku_id_fk.
  - **FK RULING (PM, decisive): `fk_fg_sku` re-added as ON DELETE RESTRICT.** To add the generated col referencing sku_id_fk the agent had to DROP `fk_fg_sku` (was sku_id_fk→ref_skus.id ON DELETE **SET NULL** — MySQL 8 forbids SET NULL/CASCADE on a generated-col input) and did NOT re-add it ("app-level integrity"). PM ruled: RESTORE the FK as **ON DELETE RESTRICT** (MySQL 8 ALLOWS RESTRICT/NO ACTION on a generated-col input; 0 orphans/0 NULLs → clean; strictly stronger than the moot SET NULL; a NULL sku_id_fk on a count row is a corrupt fact, not a tolerable orphan — aligns w/ FK-discipline + minimize-NULL). `fk_fg_stocktake_location` (location_id_fk→ref_sites ON DELETE RESTRICT) was KEPT throughout (RESTRICT always allowed on gen-col inputs). **Both FK columns are now BOTH FK-enforced (RESTRICT) AND row_hash inputs** — fully-wired canonical state; any future mig touching either column's type/nullability must account for both the FK and the generated expression.
  - **RECONCILIATION (schema_migrations drift — DDL live but unrecorded; agent applied DDL without recording).** Ruled: (1) EDIT the 288 file so it fully describes live = gen col + uniq_row_hash + `DROP FOREIGN KEY fk_fg_sku` + `ADD CONSTRAINT fk_fg_sku … ON DELETE RESTRICT` (fold the FK re-add into 288, NOT a separate 289 — the FK drop+re-add is a consequence of the gen-col DDL, one atomic migration matching live exactly). (2) Run ONLY the missing ALTER on live (gen col + UNIQUE + DROP already live; only the RESTRICT re-add outstanding): `ALTER TABLE inv_fg_stocktake ADD CONSTRAINT fk_fg_sku FOREIGN KEY (sku_id_fk) REFERENCES ref_skus(id) ON DELETE RESTRICT;`. (3) `INSERT INTO schema_migrations (filename, applied_at) VALUES ('288_inv_fg_stocktake_row_hash_generated.sql', NOW());` (the known drift pattern; confirm exact on-disk filename first). (4) `migrate.php --status` must show 288 no longer pending + nothing to apply — migrate.php must NOT re-run 288 (would error re-DROPping/re-ADDing). schema_meta needs no new row (existing table); verify the existing row is present (don't duplicate). EQUIP sql+coder.

## 2026-06-09 GO-LIVE — Expéditions live for the whole team + first logistics-team user onboarded
- **Expéditions / Fulfilment v1 IS LIVE FOR THE TEAM.** mig 290 `290_marketing_access_preset.sql` (marketing preset id 8 + expeditions go-live, flipped `ref_pages.expeditions.is_active` 0→1) and the editable-username work are now COMMITTED + PUSHED to origin/main (commit `16f725f` mig 290; `2169a81` editable username). Page-hidden go-live gate is CLEARED.
- **Olivier onboarded + logged in 2026-06-09** — user id 19, username `OlivierBarral`, role=operator, preset=marketing (id 8). He tested the FG stocktake live: entering 5 then 3 for STIB at Taproom (loc3) correctly **UPSERTed** — last-write-wins on the key `(sku_id_fk, location_id_fk, counted_at)`, fully audited 5→3. Confirms (a) the stocktake UPSERT/dedup behaves on a real operator's input, and (b) the editable-username rename propagated correctly into the audit actor (`OlivierBarral`). Taproom (loc3) now has its first count.
- **🔒 SECURITY DRIVER for the user-auth hardening backlog:** Olivier is on his OWN personal, unmanaged work computer (tailnet node `olivier` = 100.115.87.61, Windows) — a device we do NOT control now has full ERP access via Tailscale. This is the exact surface the planned **2FA (TOTP)** hardening should address (the narrow admin-Tailscale-gate is moot — the whole app is already tailnet-gated). See user-auth-admin-arc.md §PENDING. Onboarding-path note: `app.maltytask.ch` is tailnet-only (public IP 83.228.215.243 → 403; resolves to tailnet 100.125.142.25 via tailnet-wide split-DNS); a Windows client needs `tailscale set --accept-dns=true` — and `nslookup` is MISLEADING (it bypasses NRPT) → verify via browser or `curl`.

## 2026-06-09 — WeeklyOrders SHEET INGESTION arc: PM PRE-BUILD RULINGS (dry-run→match-report→Kouros reviews unmatched→apply)
Goal: ingest the live WeeklyOrders sheet (Production/Logistic Masterfile, Google Sheet `1WA-GfwrD-lgMiWJ2ejg-92zq1Ps0CZaK6Sn11isbXyI`, tab WeeklyOrders, row-6 SKU header, per-week blocks) into `ord_orders` so operators continue on the web "where they left off." Revenue/fulfilment-bearing → never-speculate rule applies HARD; dry-run-first, flag-over-decide.

**LIVE STATE VERIFIED (PM-probed 2026-06-09):** `ord_orders`=12 / `ord_order_lines`=56 / `ord_order_status_events`=39. **The "12 test orders" ARE NOT throwaway — they are real KW24 customer orders, `source='web'`, requested_date 2026-06-08/09, all BC-matched (ids 3-14; e.g. 6=AlloBoissons BC1393, 7=Bevanar BC1579, 8=Marché Flag Aubonne shipped, 9=Département Marketing BC1807, 14=Sophie Degrauwe privé no-BC).** They have full event cascades (entered+confirmed+picked stamped at one timestamp). **NO `source_ref`/dedup column on ord_orders; NO UNIQUE business key** — only PK + non-unique indexes (requested_date, status, customer, source+review_status). This is the crux of Q6.

**THE 7 RULINGS (decisive):**
1. **SCOPE = AGREE.** Import KW24 full current week (incl. already-delivered Mon/Tue) + ALL future booked (next-Monday + coming-weeks, through last dated future row before the Feb archive). **EXCLUDE the KW6/Feb archive** (delivered months ago = not operational handoff; "past" is satisfied by KW24's delivered rows). No bounded recent-past window — the handoff is "continue where you left off," not "rebuild history." If Kouros wants a longer past tail for the Commandes range-view, that's a SEPARATE future-history-backfill arc, not this ingestion.
2. **STATUS = rightmost-checked-wins AGREE; emit EVERY reached stage as an event (events=truth), NOT just final.** Same pattern the 12 web rows already use (entered+confirmed+picked all stamped). occurred_at = requested_date (00:00) for ALL reached stages — we have no per-stage timestamps from Excel; stamping reached stages at the order date is the honest reconstruction (collapsing to final-only would LOSE the audit fact that the order passed through them). Mark imported events `comment='import:weeklyorders'` so they're distinguishable from operator-driven transitions. Cache `status`=rightmost stage. (`user_id_fk` NULL on import events — matches the existing 12.)
3. **INTERNAL CHANNELS = SKIP Taproom/Eshop/samples/Marketing-as-internal/privé-marketing notes — DO NOT import as ord_orders.** This is the R1 one-fact-one-depletion rule. Eshop depletes via `inv_sales_orders`, Taproom via `inv_sales_bc` (the `fg_stock_compute()` legs) — creating ord_orders for them = DOUBLE-DEPLETION on the board. WeeklyOrders Taproom/Eshop rows are operational notes, not the depletion truth. **CAVEAT (verify, don't assume): "(Marketing)"/"Département Marketing" is AMBIGUOUS — order id 9 is already a `customer` order to "Département Marketing (BC 1807)" (a real BC sold-to that depletes via ord_*), NOT an internal channel.** So the skip-list is: rows whose CLIENT cell is literally Taproom / Eshop / "échantillons TS"/samples — the internal-consumption channels that have their OWN depletion source. A BC-numbered Marketing sold-to is a customer order (keep). When in doubt → match report, never auto-skip a BC-numbered row.
4. **CUSTOMER MATCHING = AGREE, with the canon already established (no-fuzzy-links).** Precedence: parens BC# → `ref_customers.bc_customer_no` (exact); else name → exact, then `'alias:'`-noted rows from the merge pipeline, then **STOP** — **NO fuzzy auto-link** (the §2026-06-07 LATE item-2 + §OVERNIGHT canon: «Arches»→«Marchés» 0.92 false match proves it). Fuzzy ≥ threshold = SUGGESTION in the match report ONLY, never an auto-link. Unmatched/below-threshold/privé-not-validated → SURFACE in match report, NEVER auto-create or guess (revenue-bearing). Point the agent at the EXISTING infra: the merge-suggestion engine (`scripts/` merge pipeline, `9803849`) + the Carnet-clients typeahead ranking (bc-linked first, ⚠-à-valider last) — REUSE that resolver, don't write a new one. privé individuals → match to validated private customers (the 89 privés bucket from §OVERNIGHT) else flag.
5. **CITÉ MULTI-LOCATION FESTIVAL = AGREE, N separate ord_orders.** One customer (BC 1823), ~13 distinct delivery rows = 13 ord_orders, each its own `requested_date` + line(s) + sub-location (Courtine/Grand Canyon/Pierre Viret/Stock F&B…) in `comment`. The order grain is the delivery event, not the customer — distinct dates/locations are distinct fulfilment obligations. (No festival-grouping entity needed in v1; comment carries the sub-location string verbatim.)
6. **DEDUP/IDEMPOTENCY = the real architectural decision. RULING: ADD a `source_ref VARCHAR` column to `ord_orders` capturing the sheet-row identity, with a UNIQUE/idempotency guard — do NOT rely on a (customer,date,line-hash) heuristic.** Rationale: (a) the 12 existing rows are `source='web'` real orders that OVERLAP KW24 — a content-hash dedup would either falsely-merge a legit second same-day order to the same client or fail to recognise the operator-entered twin; (b) line-hash is brittle (qty edits on the sheet, parallel -4/-B). **`source_ref` = stable sheet coordinate** e.g. `weeklyorders:{tab_gid}:{week_label}:{row_index}` (or a sheet-row content fingerprint if row indices shift). New mig: `ALTER TABLE ord_orders ADD COLUMN source_ref VARCHAR(190) NULL` + `UNIQUE uniq_ord_source_ref (source_ref)` (NULL allowed → web/email orders unaffected; only import rows carry it) → re-run = INSERT…ON DUPLICATE KEY skip/update, structurally idempotent. **The 12 web rows: do NOT reconcile/replace/import-over them.** Match report flags any KW24 sheet row that collides with an existing web order (same customer_id_fk + same requested_date) as "⚠ possible duplicate of existing web order #N — operator decides keep-both / skip-import." NEVER auto-delete an operator's web order; NEVER auto-merge. The 12 are the operator's ground truth for the rows they already entered; the importer fills the REST of KW24 + the future book around them.
7. **OFF-TRADE = AGREE, customer attribute not order attribute.** The col2 OFF flag is `ref_customers.trade_channel` (already bootstrapped by explicit-ID list, §2026-06-07 LATE item-4 — Cobra-channel, substring-UNSAFE). Ignore it on the order; trust ref_customers. If a sheet row's OFF flag DISAGREES with the customer's stored trade_channel, that's a data-quality signal → surface in the match report (don't silently override either way).

**DIVERGENCE FLAGS for this build:** (i) double-depletion if Taproom/Eshop become ord_orders (ruling #3 prevents); (ii) fuzzy auto-link corrupting a revenue-bearing customer FK (ruling #4 — flag-only); (iii) the 12-web-row overlap silently double-creating KW24 (ruling #6 — source_ref UNIQUE + collision-flag); (iv) a BC-numbered Marketing/privé sold-to wrongly skipped as "internal" (ruling #3 caveat). **SEQUENCING:** mig (source_ref col + UNIQUE) → parser/dry-run producing match report (matched / fuzzy-suggested / unmatched / internal-skipped / web-overlap-collision buckets) → Kouros reviews unmatched+collisions → `--apply` (single txn per order; ord_orders + lines + cascaded events; log_revision; idempotent on source_ref). **EQUIP: sql + coder + xlsx-or-sheets-read** (the source is a live Google Sheet — service-account read; the existing bootstrap used a downloaded xlsx, mirror that or read via the sheets client; NO ui unless a match-report viewer page is wanted — recommend a plain markdown/CSV report first, like the bootstrap report). Mig number: re-run `migrate.php --status` + `ls db/migrations` first (parallel sessions; head was 290, next free likely 291 — VERIFY).

### ✅ APPLIED to prod 2026-06-10 (morning) — WeeklyOrders import LIVE, `ord_orders` 12→79
**State-of-record 2026-06-10. `--apply` RAN (Kouros present). VERIFIED PROD: `ord_orders` TOTAL = 79** (web=12 byte-stable ids 3-14 source_ref NULL + import=67; apply run imported=67 / skipped=0). **`ord_order_lines` = 252; `ord_order_status_events` = 160.** Predictions held exactly:
- **Festival de la Cité = 13 separate orders ✓** (ruling #5 — sub-location in comment).
- **Internal channels leaked = 0** (Taproom/Eshop/échantillons TS skipped, ruling #3 proven on live write).
- **Web-overlap-collision bucket (12 rows r7-r17,r38) correctly HELD OUT** — 1:1 to existing web #3-#14 (same customer+date); no twins created (ruling #6 collision-flag proven).
- **4 empty-order rows NOT imported** (Street Cellar/Galac, Jardins de Louis/Cuves, Gotham×2/T7) — still pending Kouros phone calls; idempotent re-apply folds them in (also Cité "5 bars" r231 + ZEPV-litres caveat) once the operator data-Qs below resolve.
- **3 internal rows skipped by design** (échantillons TS, Taproom, Eshop).

**STILL DEFERRED (separate, NOT this shipped entry):** migs **300/301** (`ref_customers.sale_class`, `inv_sales_ledger` — the BC item-ledger / sales-commercial-surface arc) NOT applied / NOT on VPS; importer commit **e57eda5** still LOCAL-ONLY unpushed (Kouros hasn't requested push).

---
*(historical — pre-apply state-of-record 2026-06-09 EOD, kept for provenance:)*

**Migration (DONE):** mig **294 `294_ord_orders_source_ref.sql`** = `source_ref VARCHAR(190) NULL` + `UNIQUE uniq_ord_source_ref` (exactly ruling #6) **APPLIED to prod** — got swept into a parallel-session commit `d3fb954` via a broad `git add` (NOT this arc's commit, but it IS committed; cross-references the §SAME-DAY TIEBREAK note that `d3fb954` was the tiebreak commit — the broad add caught the 294 file too). MIGRATION HEAD = 295 (per index; 294 source_ref + 295 submitted_by_user_fk both applied). The 294 ruling-vs-fulfilment-site distinction (§ITEM 1 code-fact 1) HOLDS: 294 is the WeeklyOrders idempotency key, independent of the future `fulfilment_site_id_fk`.

**Code (committed locally, UNPUSHED):** importer `scripts/import_weeklyorders.php` = commit **e57eda5** (local only, not pushed). Extractor = `/tmp/wo-extract.js` (cutover tool, UNCOMMITTED — a /tmp helper, NOT in the repo) → writes `/tmp/wo-parsed.json`. SKU col→code map read from sheet **row 6 at runtime** (112 cols; NonAlc placeholder cols skipped) — NOT hardcoded. Two date formats handled. Cité = 13 separate orders with sub-location in `comment` (ruling #5 honored). Internal channels (Taproom/Eshop/échantillons TS) skipped (ruling #3). Off-trade ignored on order per ruling #7.

**Dry-run buckets (86 in-scope sheet rows):** **67 matched / 12 web-overlap-collision / 0 unmatched / 3 internal-skip / 4 empty.** Scope = effective date ≥ 2026-06-08 (KW24 Monday) through coming-weeks, hard-stop BEFORE the `26/KW2` Jan–Feb archive (ruling #1). The 12 web-overlap-collisions = exactly the 12 existing `source='web'` KW24 rows (ruling #6 collision-flag working — flagged, NOT auto-merged).

**2 new accounts created (Kouros-authorized, NOT speculative):** `ref_customers` #3054 **Club Sportif Le Mouret** (bc 3952) + #3056 **Stoked Mountain Experience AG** (bc 3954), each with an alias stub (#3055 "Le Mouret", #3057 "Stocked Mountain" — typo preserved) carrying `notes='merged_into: <canonical>'`. (So the typeahead/resolver can find the sheet's spelling and route to canonical.)

**MORNING APPLY runbook (Kouros, 2026-06-10):** `node /tmp/wo-extract.js` (if the sheet changed) → `scp /tmp/wo-parsed.json maltyweb:/tmp/` → `ssh maltyweb 'sudo -u www-data php /var/www/maltytask/scripts/import_weeklyorders.php --apply'` → verify `ord_orders=79` (12 web byte-unchanged + 67 new) → spot-check Cité / future / KW24 rows. **HELD for Kouros's ruling (4 items, do NOT guess — revenue-bearing):** (a) **4 empty-orders** (Galac fûts / Cuves / T7 — SKUs not in the row-6 grid → 4 empty bucket; need a SKU mapping or a skip decision); (b) **Cité "5 bars" r231 ZEPV×3500** (separate order vs aggregate?); (c) **ZEPV-in-litres CUV caveat** (cuv qty expressed in litres on the sheet vs SKU-unit grain — unit-discipline check before it inflates).

### 2026-06-10 — WeeklyOrders AUDIT-RESIDUALS RULINGS (privé double-deplete · empty-order/event-material · week-scoped search). PM-ruled, LIVE-DB-VERIFIED, pre-build.
**Three audit residuals after the import. All verified against live prod 2026-06-10.**

**① PRIVÉ / SHOPIFY-PICKUP ORDERS — ruled SAFE TO WRITE AS CUSTOMER ORDERS; NO double-deplete on these two rows.** The two sheet rows (r285 Joël Golinucci ZEPF×2 EMBF×2; r298 Alice Buache ZEPF×2 MOOF×1) are **NOT in `inv_sales_orders`** (LIVE-VERIFIED: surname search Golinucci/Buache = 0 hits; only same-first-name strangers) → they are **manual sheet click&collect entries, NOT Shopify-synced** → writing them as `ord_orders` (order_type='customer') has **no eshop-leg twin → no double-deplete**. So 1c sub-option **(ii) is the correct one for these rows.**
  - **a. NO canonical "Privé" fold-account exists** — Kouros's "fold to one account" premise is NOT current reality. Live: **83 `is_private=1` rows, one per individual, all `trade_channel='on_trade'`, no `bc_customer_no`** (the 89-privé needs_review bucket largely became these). **RULING: do NOT invent a single shared Privé account in this build** — it would CONTRADICT the existing per-individual model + the no-fuzzy-link/never-guess canon, and there's no operator-confirmed merge map. Per-individual privé rows ARE the convention; create the 2 missing ones as their OWN `is_private=1` rows (name as on sheet, `is_active=1`, `trade_channel='on_trade'`, `bc_customer_no=NULL`) — exactly like the 83 that exist. If Kouros truly wants ONE fold-account, that's a SEPARATE merge-decision arc (reuse the Carnet merge tool → tombstone individuals `merged_into:`), not a side-effect of this import. FLAG the contradiction to Kouros; do not resolve it by guessing.
  - **b. Individual name preservation:** the per-individual `ref_customers.name` already IS the name (no fold). If Kouros insists on folding, the name lives in the **`ord_orders.comment`** free-text (there is **NO `contact_name` column** on `ord_orders` — LIVE-VERIFIED schema) — do NOT add one for 2 rows.
  - **c. 🔴 THE DOUBLE-DEPLETE MECHANISM (durable, generalized):** `fg_stock_compute()` eshop leg depletes **ALL `inv_sales_orders WHERE channel='eshop' AND DATE(created_at) >= anchor`** — it does **NOT filter `fulfillment_status`** (LIVE-VERIFIED query in `app/fg-stock.php` ~L482). So pickup-vs-ship is INVISIBLE to depletion: any eshop order in `inv_sales_orders` already depletes. **THE GATING RULE (canonical): an order belongs in `ord_orders` (B2B/expedie leg) XOR `inv_sales_orders` (eshop leg) — NEVER both. The discriminator is membership in `inv_sales_orders`, not a flag we invent.** For THESE two rows: absent from `inv_sales_orders` → ord_orders write is safe. **FORWARD HAZARD (the ④ Phase-2 gate, still ON HOLD):** once the Shopify pickup-signal rework lands and pickup orders DO sync into `inv_sales_orders`, writing the SAME pickup order to `ord_orders` for the picking workflow WOULD double-deplete. The clean solution at that time = pickup orders deplete via the eshop leg (already do), and the Expéditions picking view READS them from `inv_sales_orders` (like the existing muted read-only eshop/taproom rows on the Commandes board, expeditions.php ~L1591) — it does NOT re-create them as depleting `ord_orders`. Do NOT solve this now; it's gated on the Shopify rework. (Note: `internal_channel` ENUM already carries `'cage','shop'` beyond taproom/eshop — but internal_channel is for `order_type='internal'`, a different fact; a privé pickup is a real customer sale, not internal.)
  - **d. Write path:** these 2 rows resolve to NO customer today → the importer bucketed them UNMATCHED (it has no "fold to Privé" path — confirmed by the privé-not-validated→flag ruling #4). Convention-correct write = **create the 2 `is_private=1 ref_customers` rows first (Kouros-authorized, name-as-stated = NOT speculative), THEN re-run `import_weeklyorders.php --apply`** (idempotent on `source_ref`; resolver now matches the new rows; lines DGDF/ZEPF/EMBF/MOOF all resolve). Do NOT hand-write ord_orders bypassing the importer/resolver/event-cascade path.

**② EMPTY-ORDER ALLOCATION — split: ONE is a real FG order, the rest are NON-PRODUCT event/marketing material that must NOT become FG order lines.**
  - **a. `DGDF` EXISTS** (LIVE-VERIFIED `ref_skus` id=9, recipe 31 DrunkBeard "Galactic Drift", format 15 = keg/Fass, is_active=1). So **r80 Street Cellar "3 Galac fûts" → DGDF×3 IS a valid FG order line** (Galac = Galactic Drift = the keg). Kouros's "Galac fûts = DGDF" is CONFIRMED correct. This one row becomes a normal customer order.
  - **b. `Cuves`, `T7`, `Galac` are NOT sku_codes** (LIVE-VERIFIED: T7/CUVES/GALAC exact + substring = 0 hits in `ref_skus`). "Cuves" (r102 Jardins de Louis) and "T7" (r199 Gotham/Marketing, r223 Gotham + "1 pack dec gratuité") do NOT resolve to any SKU.
  - **c. 🔴 RULING — event/serving/marketing material does NOT live in `ord_orders` and must NOT deplete FG.** "T7" (a serving-tank / tap-tower style equipment loan), "Cuves" (serving cuves = equipment, NOT the -V cuv SALE SKU), and marketing giveaways are **non-product / equipment / promo**, not Neb FG beer. Forcing them into `ord_orders` lines = wrongly depleting beer SKUs (the never-guess-a-mapping + one-fact-one-table rule). **DO NOT allocate T7/Cuves to beer SKUs.** Options to put to Kouros (do not guess which): (i) these are equipment loans → out of scope for `ord_orders` entirely (skip; they have no FG depletion fact); (ii) the "1 pack dec gratuité" on r223 IS a real product giveaway → route to the **`inv_side_stock_ledger` `movement_type='giveaway'`** path (mig 303, the unit-count giveaway ledger §ITEM 2) keyed to the actual SKU, NOT an ord_orders line — that's the canonical home for "offered/gratuité" units. The "3 Galac fûts" (DGDF) is the ONLY one of the 4 empty-orders that is a real product order. **Keep the other 3 in the held-out bucket pending Kouros: equipment-loan-skip vs giveaway-ledger; never auto-allocate as beer SKUs.**

**③ WEEK-SCOPED SEARCH (Blind Tigers) — CONFIRMED root cause; fix = drop the date window when a client search term is present. In scope as a quick follow-up.**
  - **Root cause LIVE-VERIFIED** (`expeditions.php`): Commandes defaults `mode=week` / current ISO week (L1205); the Commandes query ALWAYS seeds `$where=['o.requested_date BETWEEN ? AND ?']` (L1475) and ANDs `c.name LIKE` onto it (L1479-1481). Blind Tigers #84/#85 are req_date 2026-06-29 (~3 wk out) → current-week window returns nothing despite a name match. (NB the Carnet-clients directory search L1884-1886 is already date-unscoped — only the Commandes ORDER list is week-trapped.)
  - **HOUSE-PATTERN RULING: when a client search term is present, the client-name filter spans ALL dates — suppress the `requested_date BETWEEN` clause (or widen to an all-time bound) for that query.** Rationale: a name search is an intent to FIND a specific client's orders, not to browse a week; the date window is a browse affordance, not a search constraint. This matches Kouros's expectation and the already-date-unscoped Carnet search. Keep the week/range nav for the no-search browse case. Preserve the SKU/statut/canal filters as-is. Small, surgical, low-risk; **YES in scope as a quick follow-up.** Do NOT build a separate global-search surface — reuse the existing Commandes `client` filter, just un-gate the date window when it's non-empty. **EQUIP: coder + sql + ui + webapp-testing** (read-only prod smoke: search "Blind Tigers" with no kw → #84/#85 appear).

**SEQUENCING:** ① + ② are data + a re-run of the existing idempotent importer (no new build needed beyond creating the 2 privé rows + DGDF allocation + putting the T7/Cuves/giveaway question to Kouros). ③ is the only code change — a surgical edit to one query's WHERE assembly in `expeditions.php`. None are gated on each other. ① forward-hazard (pickup double-deplete) stays PARKED behind the Shopify rework (④ Phase 2).

### ✅✅ WeeklyOrders AUDIT FULLY CLOSED 2026-06-10 (all outcomes VERIFIED LIVE ON PROD)
**Audited the WeeklyOrders sheet (correct tab `gid=1955899648` — the gid in Kouros's URL doesn't exist as a tab) vs `ord_orders`.** Of the apparent misses: **r107 Ailec = NOT a gap** (operator entered the wrong date; same row as web #83 — dropped). **4 orders written** via the importer path (`source='import'` + `source_ref` + events + site + audit, idempotent on `source_ref`); the rest correctly held.

**The 4 writes (verified live):**
- **#101 Street Cellar** (id 833) ← r80, **DGDF×3** (resolves ②a — Galac fûts = Galactic Drift keg, the real-FG empty-order).
- **#102 Joël Golinucci** ← r285, **ZEPF×2 EMBF×2** — NEW `is_private=1` customer **id 3058** (resolves ①d for this row).
- **#103 Alice Buache** ← r298, **ZEPF×2 MOOF×1** — NEW `is_private=1` customer **id 3059** (resolves ①d).
- **#107 Département Marketing** (id 673, BC 1807) ← r223, **PD8×1** `line_comment='gratuité 100%'`, order comment names the Gotham recipient (resolves ②c for the giveaway — see NEW CONVENTION below).

**CONVENTION CONFIRMED — privé = individual `is_private=1` rows** (Kouros option A), NOT a single fold-account. This ratifies ruling ①a: the per-individual model stands; #3058/#3059 created exactly like the existing 83. (Kouros separately wants a Shopify-integration plan-mode later — distinct future arc.)

**🆕 NEW CONVENTION — marketing giveaways (order-attached):** a 100%-rebate giveaway is booked as an `ord_orders` line to the central **Département Marketing account (id 673, BC 1807)**, with the actual recipient named in the **order comment**, the **real SKU at real qty**, so that **Σ(that account's lines × SKU value) = the annual marketing-giveaway total.** This **SUPERSEDES** the earlier ②c suggestion to route order-attached giveaways to `inv_side_stock_ledger movement_type='giveaway'` — that ledger remains the home for loose-unit/non-order giveaways, but an order-attached giveaway is booked on the Marketing customer order. **`ord_order_lines` has NO price/rebate column** — the 100% rebate is expressed via `line_comment='gratuité 100%'`; the value is DERIVABLE from SKU cost, not stored.

**SKU NOTE — `PACKDEC` → `PD8`:** `PACKDEC` is an INACTIVE Shopify alias of the canonical active SKU **PD8 (id 137)** "Pack Découverte 8×33cl" — use PD8.

**③ SEARCH FIX SHIPPED + DEPLOYED** (NOT committed — parallel-session tree; `expeditions.php` Commandes): when a find-intent is active (client-name search, OR `status='ouvertes'`/`'expediees'`), the `requested_date BETWEEN <week>` clause is DROPPED so results span ALL dates; added a new **"Expédiées (toutes dates)"** status option; a **"Résultats — toutes dates · N"** banner + greyed-out week-nav when find-intent active; `LIMIT 500` surfaced. Browse-mode (no filter) unchanged. Fixes the "can't find Blind Tigers" symptom (#84/#85 dated 2026-06-29 were week-trapped). Matches the ③ house-pattern ruling above (un-gate the date window on search-intent; reuse the existing Commandes filter, no separate global-search surface).

**🔴 IMPORTANT BUG RECORDED — `import_weeklyorders.php` line-level idempotency is BROKEN (latent data-integrity risk).** `scripts/import_weeklyorders.php` (~L665) does a plain `INSERT INTO ord_order_lines (order_id_fk, sku_id_fk, qty)` with **NO `ON DUPLICATE KEY UPDATE` and NO UNIQUE on (order_id_fk, sku_id_fk)**. The HEADER is idempotent on `source_ref`, but the LINES are NOT → **re-running the importer DOUBLES every order's lines.** This actually bit us: an idempotency re-run doubled lines on #101/#102/#103 (DGDF×3 became 2 rows, etc.); detected on verification and **deleted the dup line ids 314–318 — final state now correct.** **⚠️ This corrects the PM's prior "re-running import `--apply` is safe/idempotent" guidance — that is WRONG at the LINE level** (only the header is idempotent). **RECOMMENDED FIX (do NOT apply yet — needs team confirm):** add `UNIQUE KEY (order_id_fk, sku_id_fk)` + `ON DUPLICATE KEY UPDATE qty=VALUES(qty)`. **CAVEAT:** only safe if NO legitimate order ever carries the same SKU on two lines (different lot/tier/split) — confirm with the team first. **Also note:** `log_revision` has no `'delete'` action (ENUM is insert/update only — see migration-discipline canon), so the dup-line deletes were NOT audited.

### 🔒 DURABLE RESOLVER RULE — customer resolution MUST follow merge-tombstones to the canonical active record (REUSABLE — all customer-resolving ingests inherit it)
**Generalizes the supplier-alias resolution rule to `ref_customers` + merge-tombstones. Record prominently — this is a recurring-craft rule, candidate for the `sql`/`coder` skill if it bites a third surface.**

**RULE:** ANY customer resolution — the WeeklyOrders importer, the future **commandes@ email-order ingest**, AND the **BC sales-ledger ingest** (the one ruled on in §ITEM 1 / sales-commercial-surface) — MUST:
1. **Follow `ref_customers.notes` `merged_into: N`** to the canonical record (depth-capped to avoid cycles), and
2. **REFUSE tombstoned (`is_active=0`) matches** — never point an order/sale at a dead stub.

**WHY (the bug this prevents, observed in the original importer):** the first importer matched **tombstoned alias stubs** directly. Because a stub's id ≠ its canonical's id, the `(customer_id_fk + date)` collision check **silently missed 7 of the 12 KW24 web-overlap duplicates** (stub-id never equals the web order's canonical customer_id_fk → no collision detected) AND would have written orders pointing at dead records. The merge-tombstone follow-through is what makes the collision check correct AND keeps FKs on live customers. **The BC sales-ledger ingest you ruled on (§ITEM 1) MUST inherit this exact rule when resolving N° origine → ref_customers** — same tombstone-follow, same refuse-dead-stub discipline.

## 2026-06-09 — ITEM 1: PER-SITE SALES ATTRIBUTION + INTER-SITE TRANSFERS — FINAL SPEC (dispatch-blocking; Kouros's forks resolved)
**Scope = FULL** (transfers + per-order ship-from attribution); SKU total unchanged. Resolved against live code 2026-06-09.

**CODE-FACTS RESOLVED:**
1. **mig 294 `294_ord_orders_source_ref.sql` = `source_ref VARCHAR(190) NULL` + `UNIQUE uniq_ord_source_ref` — the WeeklyOrders idempotency key (ruling #6), NOT a site/fulfilment column. ZERO collision with `fulfilment_site_id_fk`.** Both can ALTER ord_orders in separate migrations; additive ADD COLUMN, no overlap. Parallel session owns 294/295(submitted_by_user_fk on bd_*)/296(bbt_vide)/297(pertes threshold)/**298(journal_saisies, ALREADY taken locally)**. → THIS build's mig must be **≥299** (verify at dispatch).
2. **Serving-tank-clients page = `public/modules/reglages-generaux.php` section `?sec=pkg_clients`** ("Clients packaging · Livraison cuve de service · venues · festivals"), table `ref_packaging_clients` (mig 237; venues/events for cuv keg delivery; full CRUD: add ~L280, edit ~L315, toggle ~L337, list ~L1510+). **BUT: `ref_packaging_clients` = venues/events (cuv destinations), a DIFFERENT identity from `ref_customers` (B2B sold-to). The Nausikraft-delivered clients are `ref_customers` rows, NOT packaging-clients.** So the Nausikraft list is a SIBLING admin section on the same page (`?sec=` new tab) that flags `ref_customers` rows — it mirrors the pkg_clients CRUD *pattern* but operates on `ref_customers`. (NB `ref_serving_tanks` mig 142 = in-house TANK capacity registry, irrelevant.)
3. **Order creation = `public/modules/expeditions.php`** saisie POST: INSERT `ord_orders` L521-551, UPDATE (edit) L467-490, lines DELETE+INSERT L497-551, status event L567. `transporter_id_fk` already follows the "customer default → per-order override" pattern — **mirror it exactly for site.** `scripts/import_weeklyorders.php` is the bulk-import twin (must set fulfilment_site_id_fk via the resolver too).

**A. SCHEMA (types verified: `ref_customers.id` + `ref_sites.id` both `INT UNSIGNED`; `ord_orders.id` BIGINT UNSIGNED):**
- **`inv_stock_movements`** (NEW table) — inter-site transfers: `id` BIGINT UNSIGNED PK; `sku_id_fk` INT UNSIGNED NOT NULL FK→ref_skus(id) RESTRICT; `from_site_id_fk` + `to_site_id_fk` INT UNSIGNED NOT NULL FK→ref_sites(id) RESTRICT (CHECK from≠to); `qty` DECIMAL(10,2) NOT NULL CHECK>0 (SKU units, mirror ord_order_lines); `moved_on` DATE NOT NULL; `comment` TEXT NULL; `is_tombstoned` TINYINT(1) NOT NULL DEFAULT 0 (admin tombstones, NEVER delete — mirrors event-table discipline); `created_by_user_id` INT UNSIGNED NULL FK→users(id) SET NULL; created_at/updated_at. + schema_meta row (table_class='transactional', corrections_policy: tombstone-only). Both sites must `holds_fg_stock=1` (validate in handler, not CHECK).
- **`ord_orders.fulfilment_site_id_fk`** INT UNSIGNED NULL FK→ref_sites(id) ON DELETE RESTRICT (mirror `transporter_id_fk` nullability/FK-action). NULL = historical/pre-feature → resolver falls back to channel default at read. Separate migration from 294 — no contention.
- **`ref_customers.default_delivery_site_id_fk`** INT UNSIGNED NULL FK→ref_sites(id) ON DELETE SET NULL (mirror `default_transporter_id_fk` exactly). This IS the "Nausikraft list" mechanism: Nausikraft-delivered clients = `ref_customers` rows with this set to the Nausikraft site (id4). NO separate flag/table — a nullable default-site FK is the minimal, single-homed mechanism (mirrors the transporter default already proven). NULL = no client default → resolver uses channel default.

**B. RESOLVER (single source — ONE helper both order-create-default AND snapshot-sales-attribution call):** `app/fulfilment-site.php` `resolve_fulfilment_site(PDO, $order_row): int` returning a ref_sites.id. **Precedence CONFIRMED (Kouros's inferred order is correct):**
  1. **per-order override** — `ord_orders.fulfilment_site_id_fk` if NOT NULL (operator's explicit pick wins).
  2. **client default** — `ref_customers.default_delivery_site_id_fk` (the Nausikraft list) if order_type='customer' and set.
  3. **channel default** — wholesale/B2B (order_type='customer', no client default) → Crissier (warehouse, id2); internal eshop → Crissier (id2); internal taproom → Taproom (id3). (Kouros: eshop default=Crissier, taproom base=Taproom.)
  Channel-default site ids come from `ref_sites` (DB-derived by site_type/role, NOT hardcoded — eshop/wholesale→warehouse site, taproom→pos site; if multiple, the canonical default is config — recommend a `commissioning_settings` row or the lowest-sort warehouse). The resolver is PURE (no writes); used at order-create time to seed the selector default AND at fg-stock snapshot time to attribute sales.

**C. fg_stock_location_snapshot() sales attribution:** per-site sales = Σ(sales legs) WHERE `resolve_fulfilment_site(order)` = this site. MUST reuse the ONE resolver (no inline channel→site map). Keep the Σ(cards)==physique invariant: the prod leg already credits the production site; sales legs now SUBTRACT from the resolved ship-from site (today they subtract globally per §PRODUCTION-LEG FIX OPEN/DEFERRED — THIS build closes that "sales-leg per-location settlement" carryover). Historical orders with NULL fulfilment_site → resolver returns channel default (step 3) — never errors, always attributes. eshop=`inv_sales_orders`, taproom=`inv_sales_bc`, B2B=`ord_*` (R1 source map unchanged).

**D. DISPATCH SEQUENCE (Kouros's order CONFIRMED, one reorder note):** (1) migrations [inv_stock_movements + ord_orders.fulfilment_site_id_fk + ref_customers.default_delivery_site_id_fk — 3 ALTERs/CREATE, can be ONE migration file or three; recommend ONE]; (2) **resolver helper `app/fulfilment-site.php`** (build BEFORE both consumers — it's the single-source spine); (3) order-creation ship-from selector in expeditions.php (defaulted by resolver, overridable) — **do this BEFORE snapshot attribution** so test orders carry real sites; (4) snapshot sales-attribution rewire (consumes resolver); (5) Nausikraft-client admin list on reglages-generaux.php (sets `ref_customers.default_delivery_site_id_fk`); (6) Mouvements transfer tab on expeditions.php (operators+ record; admin tombstone). **EQUIP per sub-build:** (1) sql+coder; (2) coder+sql; (3) coder+sql+ui+webapp-testing; (4) sql+coder+webapp-testing; (5) coder+sql+ui; (6) coder+sql+ui+webapp-testing.

**E. MIGRATION NUMBERS:** local head includes 298 (journal_saisies, parallel/untracked). NEXT FREE = **299** for this build (re-verify `migrate.php --status` + `ls db/migrations` AT DISPATCH — parallel sessions lead; 294-298 all parallel/untracked). If three separate migs: 299/300/301; if one: 299. Recommend ONE migration `299_fulfilment_site_attribution.sql` (CREATE inv_stock_movements + 2 ADD COLUMN + 2 FK + schema_meta) — atomic, fewer numbers to contend.

**F. LANDMINES:** (i) **ord_orders contention** — do NOT touch 294's source_ref; your ADD COLUMN is independent; never re-ALTER in a way that re-declares their column. (ii) **single-resolver** — order-create default and sales-attribution MUST call the same `resolve_fulfilment_site()`; an inline duplicate map = the divergent-store anti-pattern → cards/total drift. (iii) **beer-tax neutrality** — site attribution is a LOCATION fact, not a quantity fact; total packaged-HL and beer-tax base unchanged (Σ across sites = same total). (iv) **no parallel FG-depletion path** (R1) — `inv_stock_movements` is a TRANSFER (moves stock between sites, net-zero to global physique), NOT a sale; it must NOT enter the global depletion legs, only shift the per-site split. fg_stock_compute() global total ignores transfers; ONLY fg_stock_location_snapshot() applies them (+to_site, −from_site). (v) **Nausikraft identity** — Nausikraft is BOTH a `ref_sites` row (id4 consignment, customer_id_fk=1) AND its delivered clients are `ref_customers` — don't conflate; the client list sets `default_delivery_site_id_fk=4`. (vi) **import_weeklyorders.php** must also resolve+set fulfilment_site_id_fk (else bulk-imported orders all NULL→channel-default, acceptable but flag). (vii) **transfers vs cuv** — cuv (serving-tank) is filled-at-client = already excluded from FG; a transfer of FG between sites is distinct, don't let cuv leak into inv_stock_movements.

## ✅ ITEM 1 SHIPPED — stock-movement saisie + per-site sales attribution (2026-06-09, mig 299, autonomous run)
Built EXACTLY to the §A-F design above. 3 phases, 3 commits pushed to origin/main, all deployed live + render-smoked + invariant-verified (Opus independent). Kouros offline; he picks up the one DATA item tomorrow.

**Phase 1 — mig 299 + the single resolver (commit `3c3eb33`):**
- **mig `299_fulfilment_site_attribution.sql` APPLIED** (one atomic file, per §E recommendation). New table **`inv_stock_movements`** (id, sku_id_fk, from_site_id_fk, to_site_id_fk, qty DECIMAL(10,2), moved_on DATE, comment, is_tombstoned, created_by_user_id, created_at/updated_at; **CHECK from<>to AND qty>0**; FKs RESTRICT→ref_skus/ref_sites, user SET NULL; schema_meta source/tombstone-only). ALTER `ord_orders` ADD `fulfilment_site_id_fk` INT UNSIGNED NULL FK ref_sites RESTRICT. ALTER `ref_customers` ADD `default_delivery_site_id_fk` INT UNSIGNED NULL FK ref_sites SET NULL (mirrors `default_transporter_id_fk` — the Nausikraft mechanism).
- **`app/fulfilment-site.php`** = THE single resolver (the §B/landmine-(ii) spine): `resolve_fulfilment_site(PDO,$ctx):int` + `fulfilment_default_sites(PDO):array`. Precedence per §B: per-order `fulfilment_site_id_fk` override > customer `default_delivery_site_id_fk` > channel default (taproom→pos, eshop/wholesale→warehouse). Sites DB-derived from `ref_sites` by role (warehouse=Crissier id2 / pos=Taproom id3 / production=Renens id1), **never hardcoded**. Has a `_customer_default_site_id` prefetch short-circuit (added Phase 2) to avoid N+1.

**Phase 2 — sales attribution + ship-from selector + Nausikraft admin (commit `49a0386`):**
- **`fg_stock_location_snapshot()` now attributes SALES per site** — replicates `fg_stock_compute()` steps 4/5/6 cutoffs EXACTLY, grouped by `resolve_fulfilment_site` (NEVER an inline map → landmine-(ii) honored) + applies TRANSFERS from `inv_stock_movements` (+to/−from, is_tombstoned=0, moved_on>anchorDate). `fg_stock_compute()` TOTAL unchanged (landmine-(iv): transfers shift the split only, net-zero to global). Cards CAN go negative (honest signal, not clamped). **Invariant Σ(cards)==Σ(physique) holds** — Opus-verified 8642==8642, 0 negatives today (in-window sales=0). This CLOSES the long-deferred "sales-leg per-location settlement" carryover (attribution done; see KNOWN LIMITATION 1 for the cutoff-granularity remainder).
- **`expeditions.php` order saisie + `scripts/import_weeklyorders.php`:** "Site d'expédition" selector — "Automatique" (NULL→resolver) or explicit holds_fg_stock site; mirrors transporter_id_fk; persisted INSERT/UPDATE + log_revision; import resolves+sets per row (landmine-(vi) honored).
- **`reglages-generaux.php` NEW `?sec=delivery_sites`:** admin assigns/clears `ref_customers.default_delivery_site_id_fk` (the Nausikraft-delivered-client list). 2688 customers via datalist; 4 holds_fg_stock sites. **Operator enters the actual Nausikraft client list TOMORROW via this UI** (= the one DATA item pending; architecture + UI ready).

**Phase 3 — Mouvements transfer tab (commit `edfcf78`):**
- **`expeditions.php` new `?view=mouvements`** (operators+ record; admin tombstone; immediately live): record form (sku/from/to/qty/date/comment) → `add_movement` (CSRF, validate from<>to + strict int qty>0 + whitelist sites, INSERT, log_revision, PRG); recent-movements list; `tombstone_movement` (admin-only, is_admin defense-in-depth, `is_tombstoned=1` NEVER DELETE — tombstone-only per schema_meta). CSS in `/public/css/expeditions.css` scoped `body.expeditions`. schema_meta.writer_script set to this tab.
- **End-to-end verified:** DIV4 transfer Renens→Crissier qty5 → Zgeg 449→444, Zeubi 39→44, Σ unchanged; tombstone reverts; CHECK constraints reject from==to & qty<=0 (landmine-(iv)/(vii) hold — transfer is net-zero, distinct from cuv/sales).

**VERIFICATION (Opus independent):** all 5 surfaces render-smoke OK via forged-admin-session CLI harness (form/stock/mouvements/commandes/delivery_sites, no fatals); invariant Σphysique==Σcards==8642; resolver 5 precedence cases pass; **only intended files touched each commit** — the concurrent session's form-packaging/packaging-stats/migs 293-298 NOT committed by this run.

### ✅ MULTI-SKU MOUVEMENT INPUT SHIPPED + LIVE + COMMITTED + PUSHED 2026-06-10 (`badd153` on origin/main — bundled, UNDER-DESCRIBED) — `?view=mouvements` extended scalar→multi-line; NO migration (HEAD stays 303); built to PM recommendation, zero divergence; EQUIP ui+sql+coder +webapp-testing
Extends Phase-3's single-SKU `add_movement` to a **shared-header + repeatable SKU+Qté line grid** (operator confirmed "same route, pick once" — Site origine / Site destination / Date / Commentaire chosen ONCE; per-line destination override deferred). +551/−95 lines across 3 files; git status clean of stray files.
- **Handler `add_movement` (expeditions.php L936+):** scalar → parallel arrays `mov_sku_id[]` / `mov_qty[]`. Header validated once; each line validated independently (active SKU, strict-integer qty>0, blank rows skipped); **ALL errors collected BEFORE `beginTransaction()` → all-or-nothing** (any bad line → ZERO writes, flash names "Ligne N"); one transaction wraps N inserts; **per-row `log_revision` PRESERVED inside the loop**; flash "N mouvements enregistrés."; PRG.
- **SINGLE-HOME PRESERVED (the key architectural verdict):** writes the SAME N `inv_stock_movements` rows — **NO batch/header table, NO new migration.** A multi-SKU transfer is N transfer facts sharing a route, not a new entity. One fact, one canonical table.
- **NET-ZERO INVARIANT VERIFIED:** `fg_stock_location_snapshot` total == `fg_stock_compute` physique = **8599** before AND after a 3-row test submit (test rows tombstoned + self-cleaned). Transfer leg stays net-zero/compute-exempt per ITEM 1 landmine-(iv) — multi-line changes nothing here (still N net-zero rows).
- **UI:** 3 rows pre-shown + "+ Ajouter une ligne" + × per-row remove; advisory per-line amber over-transfer hint from `window.EXP_MOV_STOCK_MAP` (injected via `fg_stock_compute()`, ADVISORY-ONLY, never blocks). **Key fix:** `expeditions-form.js` `<script>` tag was previously loaded ONLY on `view=form` → now also on `view=mouvements` (new `initMovLines()`). Files: `public/modules/expeditions.php`, `public/js/expeditions-form.js`, `public/css/expeditions.css` (`.exp-mov-line*`, 44px touch targets, 700px/480px breakpoints — floor-tablet discipline).
- **Smoke (webapp-testing, authenticated):** 3 rows render + add/remove work; 3-line submit → 3 rows + correct flash; qty=0 on line 2 → ZERO rows + "Ligne 2" flash (all-or-nothing proven); net-zero held. Opus spot-checked handler diff: all-or-nothing + per-row audit + single-home confirmed.
- **Visite guidée:** NO new `ref_pages` row — same page, extended form; no tour card touched (correct — tour anchors on pages/views, not form-internal layout).
- **✅ COMMITTED + PUSHED (NOT as planned):** landed in **`badd153`** `feat(expeditions): Stock PF predictive-column readability + order stock-warning detail modal` — already on origin/main. A PARALLEL session (mon-tableau Phase 2b) committed FIRST and BUNDLED three in-flight features into one commit: (1) our multi-SKU mouvements (`movValidLines` loop + per-row log_revision at expeditions.php:1013-1054, `mov_sku_id[]` parallel array, `initMovLines` in expeditions-form.js); (2) order stock-risk detail modal (`exp-sdm`, `EXP_CMD_STOCK_DETAIL`); (3) Stock PF grouped-header restyle (`exp-st-th-group`, colspan 8→11). ⚠️ **The commit message names only features 2+3 — multi-SKU mouvements is committed but UNMENTIONED (under-described).** Handler VERIFIED intact in HEAD (loop + per-row audit present). Our planned per-feature commit split was PRE-EMPTED. **🚨 TWO-CONCURRENT-SESSIONS HAZARD (today, this repo):** at the time of writing HEAD=`e32d472` (mon-tableau sales Phase 2b, unpushed, ahead 1) with a DIRTY tree (kpi-handlers, kpi-charts, packaging-form, mon-tableau.css, migs 310/313) — all the OTHER session's work, not ours. With deploy-pushes-the-working-tree, an `--apply` from either session leaks the other's uncommitted changes live (see checklist hazard). Confirm `git status` before any deploy.

**KNOWN v1 LIMITATIONS (refine with operator):**
1. ~~Snapshot transfer + sales cutoffs use the GLOBAL `anchorDate`, NOT per-site.~~ **✅ CLOSED for ESHOP + TRANSFER by Task A 2026-06-10 (commit `2192153`); see §TASK A below.** Eshop snapshot+compute legs now share the IDENTICAL per-(sku,site) `>=`+created_at tiebreak (symmetric → Σcards==Σphysique under same-day); transfer leg = atomic per-row UNIT-GATE (net-zero, compute-exempt). **REMAINING per-site cutoff refinement: B2B leg = strict-global symmetric — 🔴 RECLASSIFIED 2026-06-10 as a REAL BUG (global anchorDate pollution drops same-day ships), NOT "intentional"; ruled in §B2B EXPEDIE LEG = REAL COMPUTATION BUG; taproom leg = month-grained (deferred); per-site same-day TRANSFER precision (the unit-gate trade-off — see Task A).**
2. Nausikraft client list = DATA pending (operator enters tomorrow via `?sec=delivery_sites`). Architecture + UI ready.

## ✅ TASK A SHIPPED 2026-06-10 — same-day cutoff fix (eshop symmetric + transfer net-zero unit-gate); commit `2192153` origin/main; NO migration; data-layer only (`app/fg-stock.php`); EQUIP coder+sql
Closes KNOWN LIMITATION 1 of ITEM 1 for the **eshop + transfer** legs — built to the PM (A)-corrected ruling exactly. The bug class: `fg_stock_compute()` (the consumption-stock derivation Tap&Shop + others call) and `fg_stock_location_snapshot()` (the per-site card view) used DIVERGENT same-day cutoff logic on the eshop leg → Σcards could drift from Σphysique inside the same-day window; and the snapshot transfer leg admitted +to/−from per-side, so per-site anchor divergence could break per-row net-zero.

**FIX 1 — eshop leg made SYMMETRIC (compute now mirrors snapshot).** `fg_stock_compute()` Step 5 (eshop) now uses the IDENTICAL per-(sku, warehouse-site) `>=` + `created_at` tiebreak as the snapshot eshop leg: built `fg_site_sku_anchor_map()` + one `resolve_fulfilment_site(['channel'=>'eshop'])` call at the top of Step 5; per-row pass test against that map; **fallback = strict `> $anchorDate`** for SKUs with no warehouse count. Symmetric by construction → Σcards==Σphysique holds under the same-day window.

**FIX 2 — snapshot transfer leg = atomic per-row UNIT-GATE.** `$fromPass`/`$toPass` still computed per-side from each site's per-SKU anchor, but admission is atomic: `$rowPass = ($fromPass || $toPass)`; both +qty(to) and −qty(from) apply together or neither → per-row net-zero GUARANTEED regardless of per-site anchor divergence. Compute stays transfer-exempt by construction (no transfer term — correct; transfers are net-zero to global physique per ITEM 1 landmine-(iv)).

**FIX 3 — SCOPE NOTE at top of `fg_stock_compute` rewritten** to the as-built PER-LEG cutoff table: eshop = per-site symmetric; B2B = strict-global symmetric; taproom = month-grained (deferred); transfers = unit-gate net-zero, compute-exempt.

**Intentionally UNCHANGED:** B2B leg (strict-global symmetric) + taproom leg (month-grained). These are NOT the same-day class.

**⚠️ ACCEPTED TRADE-OFF (PM-ruled acceptable):** the transfer unit-gate sacrifices per-site precision in the rare case of a same-day INBOUND transfer to a `to_site` that was ALSO counted later that same day → that card transiently OVERCOUNTS by qty until its next count, in exchange for the HARD global net-zero invariant. Per-site same-day-transfer settlement remains the DEFERRED stock-movement-saisie refinement.

**VERIFICATION (real, not empty-window):** synthetic same-day fixture — eshop order (SKU60 qty5, today after warehouse anchor_ts) + a **DIVERGENT-ANCHOR transfer** (SKU39: from site2 counted 06-08, to site3 counted 06-09 12:43, moved_on 06-09 08:00 → fromPass=T / toPass=F). **10/10 assertions:** baseline 8599==8599; with fixtures Σcards==Σphysique held; eshop physique −5 exactly; transfer global unchanged (74) + cards ±5 net-zero; teardown restored baseline; no stray scripts. **The divergent-anchor transfer fixture WAS constructible** (PM-flagged to confirm) — site2 warehouse vs site3 taproom anchors give the T/F split.

**Status:** Task A had NO migration → MIGRATION HEAD stays **299**. KNOWN LIMITATION 1 (eshop+transfer same-day) now CLOSED. Remaining deferred: per-site same-day TRANSFER precision (unit-gate trade-off) + taproom month-granularity.

## 🔴 B2B EXPEDIE LEG = REAL COMPUTATION BUG (2026-06-10 — SUPERSEDES the earlier "label-only" / "intentionally-deferred B2B remainder" finding)
**The earlier ruling that the B2B same-day case was a benign deferred remainder ("count already reflects the shipment, double-count-safe") was WRONG.** Operator + live DOAF (sku 20) raw data confirm a genuine miscomputation. RECLASSIFIED: this is a CORRECTNESS bug in `fg_stock_compute()` Step 4 (and symmetrically in `fg_stock_location_snapshot()` Leg 1), NOT a label issue and NOT acceptably-deferred.

**ROOT CAUSE (CONFIRMED).** Step 4 deducts shipped B2B orders with `o.requested_date > $anchorDate` where `$anchorDate` = the **GLOBAL `MAX(counted_at)` across ALL sites** (fg-stock.php L276). The anchor SUM is correctly per-(sku,location), but the B2B cutoff is global. DOAF anchors: loc1 Production 06-08 (8), loc2 Warehouse 06-08 (36), loc3 Taproom 06-09 (1) → global anchorDate = **06-09** (polluted by Taproom counting a day later). ord#4 (req 06-08, shipped, 6u) fails `06-08 > 06-09` → NOT deducted → physique stuck at 45. Reality: warehouse+production were counted 06-08, the 6 shipped from them on 06-08 is NOT in those 06-08 counts → it MUST come off. **This is the exact production-leg pollution class that `566b9d3` already fixed for the production leg — the B2B leg is the LAST surviving global-cutoff leg that has a per-site finer anchor available** (eshop closed by Task A; prod by 566b9d3/d3fb954).

**RULING — the canonical fix is per-(sku, ship-from-site) anchor cutoff on the B2B leg, mirroring how the prod + eshop legs already settle.** Use the SAME `fg_site_sku_anchor_map()` the eshop leg + snapshot already build. The ship-from site = `resolve_fulfilment_site()` for the order (fulfilment_site_id_fk → customer default_delivery_site → channel default), which the snapshot Leg 1 ALREADY computes per order. Predicate becomes per-row: deduct when `requested_date > siteAnchorDate(ship-from)`, with **COALESCE→global `$anchorDate`** for SKUs uncounted at that site (mirrors prod-leg COALESCE fallback). For DOAF: ord#4 ships from warehouse (loc2, anchor 06-08) → `06-08 > 06-08`? NO under strict `>`. → SAME-DAY BOUNDARY MUST BE RULED (below).

**SAME-DAY BOUNDARY RULING — option (a): per-location anchor + INCLUSIVE `requested_date >= siteAnchorDate` for the B2B leg.** Operator needs the same-day-as-warehouse-anchor shipment (ord#4, req 06-08 == warehouse anchor 06-08) deducted. The created_at tiebreak the eshop leg uses is NOT available here — the existing comment is correct that `ord_orders.created_at` is ENTRY time (back-datable), so it can't order a same-day ship vs count. Two sub-cases at the same calendar day: (i) order shipped from a site BEFORE that site's count that day → already in the count, must NOT re-deduct; (ii) shipped AFTER the count → not in count, must deduct. With only DATE granularity we cannot distinguish (i) from (ii). **OPERATOR'S OPERATIONAL MODEL resolves the tie: a same-day shipment is NOT yet reflected in that day's count (the count is a morning/point-in-time snapshot, shipments flow after) → default to DEDUCT same-day → inclusive `>=` on the SHIP-FROM site's anchor.** This does NOT reintroduce the genuine "shipped strictly BEFORE an earlier count" double-count: an order with `requested_date < siteAnchorDate` still fails `>=` and stays in the count (correct — a 06-05 shipment is already absent from a 06-08 count). Option (b) (keep `>` but de-pollute the global max) would still drop the legitimate same-day ord#4; option (c) (explicit ship timestamp) is the eventual real fix but needs a SOURCE column (no DATETIME ship-time exists) — DEFER, not required now. **CANONICAL = (a).**

**ACCEPTED RESIDUAL (inclusive `>=` trade-off, PM-ruled OK).** A B2B order shipped from a site on the same calendar day, where that shipment WAS already physically captured in that day's count, will be double-deducted (counted-out AND deducted). This is the symmetric inverse of the eshop residual and is rare (same-site count + same-day pre-count ship). Accept it for the operator's stated default (count = point-in-time, ships flow after). The only true fix is a ship timestamp (option c) — log as the deferred precision item, same bucket as taproom month-granularity.

**Q3 — SNAPSHOT IS NOT ALREADY RIGHT; both functions share the SAME global-cutoff B2B leg → they are SYMMETRIC, so there is NO Σcards != Σphysique divergence today.** Verified in code: snapshot Leg 1 (L859-884) uses identical `o.requested_date > ?` against the same global `$anchorDate` (L832-836). So the invariant Σcards==Σphysique HOLDS through this bug (both under-deduct ord#4 identically) — which is precisely why the bug is invisible to the invariant gate and only the operator's ground-truth catches it. **CONSEQUENCE: the fix must land in BOTH legs in lockstep (compute Step 4 AND snapshot Leg 1) to PRESERVE symmetry.** Fixing only one breaks Σcards==Σphysique. This is a both-functions fix, NOT compute-only.

**FIX LOCUS (precise).** `app/fg-stock.php`: (1) `fg_stock_compute()` Step 4 (~L370-390) — the `o.requested_date > ?` global predicate; (2) `fg_stock_location_snapshot()` Leg 1 (~L859-884) — the matching `o.requested_date > ?`. Snapshot Leg 1 ALREADY resolves the per-order ship-from site (`resolve_fulfilment_site`, L887) and ALREADY builds `fg_site_sku_anchor_map()` (L842) — so the snapshot has every input; compute Step 4 must add the same per-row resolve + map lookup (compute currently SUM/GROUPs in SQL — it will need to fetch per-order rows like the snapshot does, or resolve ship-from per (order) before aggregating). Update the SCOPE NOTE B2B bullet (L300-303) from "strict-global > (symmetric)" to "per-(sku, ship-from-site) anchor, inclusive >= same-day, COALESCE→global; symmetric across both functions". Adjust comments at L859-864 (which currently justify the strict-`>` no-tiebreak as correct) to the new ruling.

**INVARIANTS THAT MUST NOT BREAK (canonical stock SoT):** (1) Σcards==Σphysique — fix both legs in lockstep; (2) the genuine "shipped strictly before a count" guard — orders with requested_date < ship-from anchor stay un-deducted (the `>=`/`>` boundary only changes SAME-day behavior); (3) eshop + transfer same-day handling untouched (Task A); (4) beer-tax neutrality (location/cutoff fact, not a quantity fact — total deducted qty changes only by genuinely-missed same-day ships, which is the correct correction); (5) no new FG-depletion path / single-home (R1) — this is a predicate change inside the one existing B2B leg, no new store.

**THE 36-vs-39 GAP: NOT mine to resolve — operator ground-truth.** Operator: "45 − 6 on Monday → 36 today" but 45−6=39. The 3-unit gap is invisible in the DB (no June flows: physique=45=anchor exactly). DO NOT invent a flow to close it. Orchestrator reconciles with operator (hypotheses: operator tracks warehouse loc2=36 as the operational number with 6 shipped from production loc1; or the 45 aggregation itself is being read differently). The compute fix makes physique = 45 − 6 = **39** (ord#4 deducted), which is the architecturally-correct number; the operator's "36" is a separate reconciliation, NOT a target the code should hit.

**EQUIP: sql + coder + webapp-testing.** BOTH compute + snapshot (not compute-only). No migration (predicate change, data-layer `app/fg-stock.php` only — DATETIME ship-time is the DEFERRED option-c, not this fix). Verification gate: synthetic divergent-anchor B2B fixture (warehouse 06-08 + taproom 06-09, shipped order req 06-08) → ord#4-class order DEDUCTED; Σcards==Σphysique held; an order req < ship-from anchor NOT deducted (guard intact); teardown clean. DOAF live check: physique 45→39 after fix.

### ✅ SHIPPED + OPERATOR-CONFIRMED 2026-06-10 — B2B expedie per-(sku, ship-from-site) anchor fix is LIVE and SETTLED
**The fix landed and is CORRECT per the operator.** Operator's verbatim ruling: *"inventory take is always at start of week. following orders, even same day, must deduct."* → the start-of-week count is the baseline; same-day-and-later shipments deduct. **LIVE-VERIFIED: DOAF (sku 20) physique = 39** (Zeubi warehouse 36 − 6 Monday ship = 30; Production 8; Taproom 1); deployed + Σcards==Σphysique held 7455==7455.
- **🔧 AS-BUILT = THREE-tier predicate (CORRECTS the earlier 2-tier ruling above; the 2-tier `>=`/COALESCE→global model is SUPERSEDED).** Implemented IDENTICALLY in BOTH `fg_stock_compute()` Step 4 AND `fg_stock_location_snapshot()` Leg 1 (symmetric, lockstep — invariant preserved):
  - **(a) SKU counted at the ship-from site** → `requested_date >= site_anchor_date` (INCLUSIVE — operator's same-day-deducts model).
  - **(b) SKU counted elsewhere but NOT at this site** → `requested_date >= global anchorDate` (INCLUSIVE).
  - **(c) SKU NEVER counted anywhere** → `requested_date > global anchorDate` (STRICT `>`). Needed to keep Σcards==Σphysique for never-counted SKUs (e.g. ZEPV sku 61): their anchor-date shipments would otherwise deduct ASYMMETRICALLY — in the snapshot (→ negative row) but NOT in compute (dropped via `!isset($byId)`). Tier (c) preserves the pre-fix behaviour for untracked SKUs and restores symmetry.
- The earlier "label-only / double-count-safe / intentionally-deferred remainder" finding (above §B2B EXPEDIE LEG header) is **FULLY SUPERSEDED** — the bug was real, the fix is per-(sku,ship-from-site) anchor, and tier (c) is the symmetry-preserving addition the 2-tier ruling missed for never-counted SKUs. The ACCEPTED RESIDUAL (same-day pre-count ship double-deduct, option-c ship-timestamp the eventual real fix) STILL stands and is unchanged. Only same-day TRANSFER precision + taproom month-granularity remain deferred.

## ✅ ANCHOR TIEBREAK FIX — DEPLOY-APPROVED 2026-06-12 (PM go); operational fix ships single-file, COGS discriminator is a SEPARATE gated follow-up.
**PM RULING 2026-06-12 (Opus-relayed status + new operator instruction "differentiate weekly operational stocktake vs end-of-month COGS stocktake"):**
- **#1 GO — operational `fg-stock.php` anchor fix ships NOW via single-file targeted deploy.** ROW_NUMBER() OVER(PARTITION BY sku_id_fk,location_id_fk ORDER BY counted_at DESC, id DESC)=1 in BOTH `fg_stock_compute()` + `fg_stock_location_snapshot()` (byte-symmetric — §B2B invariant). SQL-verified DOAF→45, ZEPF prod→11. COGS-INDEPENDENT (separate code path, separate query). Deploy mechanism = `rsync --rsync-path="sudo rsync"` of ONLY `/tmp/fgstock-hotfix.php`→`/var/www/maltytask/app/fg-stock.php` — NOT `bin/deploy.sh --apply` (tree is DIRTY with unfinished cage/repack build: settings/expeditions/financier/repack + migs 336/337 → blanket rsync would leak that live; this is the codified dirty-shared-tree surgical-deploy rule, coder skill). Repack leg LEFT UNGATED as live (out of scope). Post-deploy: webapp-testing smoke `?view=stock` + per-(sku,loc) >1-active spot-check; Opus verifies INDEPENDENTLY (FG/COGS-adjacent).
- **#2 DISCRIMINATOR DESIGN (durable, the operator's real ask) — gated follow-up, BEFORE any June COGS run:**
  - **The hazard, PM-probed live 2026-06-12:** `inv_fg_stocktake` has NO weekly/month-end discriminator. `cogs-monthly-compile.ts` (maltytask, 2 sites: L~323 + L~734) reads `SELECT sku,sku_id_fk,qty FROM inv_fg_stocktake WHERE month_closed=? ORDER BY sku` — **NO is_active filter, NO location aggregation, NO latest-pick**. May 2026 = 34 rows / 34 sku / **1 location** / 1 date → COGS correct BY ACCIDENT (single-site census). June 2026 = 109 rows / 52 sku / **4 locations** / 2 dates (06-08:81 + 06-09:27 active, different SKUs — NO sku×loc has >1 June row today, so no current double-count). **TWO defects the discriminator must close: (a) weekly counts carry the current month_closed tag → when the real June-30 census lands IN ADDITION, the WHERE month_closed query sums weekly+monthly ≈ doubles FG; (b) even one census now spans 4 locations → COGS must aggregate cross-site per SKU (today it happens to sum all rows, which IS the right cross-site total, but is unguarded against the weekly contamination).**
  - **Canonical model = add `count_type ENUM('operational','month_end') NOT NULL DEFAULT 'operational'` to inv_fg_stocktake** (NOT a separate table — one fact, one table; the count IS an FG census row, its PURPOSE is the new attribute). Durable-modeling honored: NOT NULL + DEFAULT (no NULL fall-through), ENUM vocabulary not free-text. NEW migration (338, RE-VERIFY --status; head currently 337) — NO new table so NO schema_meta row needed (ALTER on existing).
  - **Writer:** the Saisies PF-card / `expeditions.php?view=stocktake` entry path sets `count_type`. Default path = 'operational' (weekly). Month-end census = an explicit operator affordance (toggle/"Clôture mensuelle" mode on the form) that writes 'month_end'. Confirm the exact entry-point with operator — do NOT guess which form writes the census.
  - **Backfill (mig 338 DML, evidence-based, NOT guessed):** May 34 rows (counted_at=2026-05-31, the census May COGS correctly consumed) → `count_type='month_end'`. June 109 rows (06-08/06-09 weekly) → stay `'operational'` (the DEFAULT). April 41 inactive rows → leave default (already excluded by is_active=0; seeded period, immutable). This is seeding from existing canonical state = NOT guessing.
  - **COGS query change (both sites in cogs-monthly-compile.ts):** `WHERE month_closed=? AND count_type='month_end'` AND pick latest-per-(sku,location) within the period (ROW_NUMBER ORDER BY counted_at DESC,id DESC) THEN SUM across locations per sku. Mirror the is_active handling already there (April seeded rows are is_active=0 — keep the no-is_active-filter ONLY if month_end rows are reliably the census; safer: filter is_active=1 for computed months, but April-seed rows would break that → PM note: keep the existing "no is_active filter" but rely on count_type='month_end' as the discriminant; VERIFY April seed rows are NOT count_type='month_end' OR are intentionally included). 🔴 Opus verifies the May fiche reproduces TO THE CENT after the query change (regression invariant — May is immutable/published; the new query MUST still yield 47 701.56 FG).
  - **EQUIP: sql + coder** (migration + writer PHP + cogs-monthly-compile.ts TS) **+ ui** (month-end toggle on the saisie form) **+ webapp-testing** (smoke the census-mode write).
- **#3 SEQUENCING — CONFIRMED:** ship operational fix TODAY (June not closing today; May immutable/correct). Discriminator = NEXT work item, MUST land + be May-regression-verified BEFORE any `cogs-monthly-compile --apply --month 2026-06`. Do NOT bundle — the operational fix is a clean COGS-independent single-file hotfix; the discriminator is migration+multi-file+regression-gated.

### ✅ OPERATIONAL ANCHOR FIX — SHIPPED + VERIFIED on prod 2026-06-12 (Opus-relayed). #1 done; #2 discriminator STILL OPEN.
- **Deploy:** `/tmp/fgstock-hotfix.php` → `/var/www/maltytask/app/fg-stock.php` via SINGLE-FILE targeted rsync (NOT `bin/deploy.sh` — tree still dirty with the cage/repack build; the codified surgical-deploy-on-dirty-shared-tree rule, honored). Owner `maltytask:www-data`, mode 644.
- **Verified:** `php -l` clean; deployed == staged artifact byte-for-byte; delta vs old live file = EXACTLY the 2 anchor subqueries (22 lines) in `fg_stock_compute()` + `fg_stock_location_snapshot()`; repack leg left ungated/untouched (out of scope).
- **Numbers (REAL functions, www-data probe):** DOAF anchor **83→45 ✓**; ZEPF global **2502→1256 ✓** (the +1246 Production inflation removed); **26/26 recounted Production SKUs corrected**; **Σ(location total)=8892 == Σ(compute physique)=8892 — INVARIANT HELD** (the old Σcards≠Σphysique gap is closed; consistent with HEAD `d9f7976`). The §never-counted-SKU asymmetry (L586+) was the OTHER half of that gap — now both closed (gate `stocktake_scope != 'none'` + this anchor fix together).
- **Rollback artifact:** `/tmp/deployed-fgstock.php` = exact pre-fix live file; re-push to revert.
- **🔴 LOCAL ≠ DEPLOYED CAVEAT (durable, do NOT trip on it):** local working-tree `app/fg-stock.php` now has the anchor fix TANGLED with the repack build's UNCOMMITTED gate change (`if(repack_depletion_live())` wraps the repack leg). So **local has the gate, DEPLOYED runs repack UNGATED.** When the repack-build owner commits + deploys via `bin/deploy.sh`, the GATED version ships (requires `repack_depletion_live()` defined — `app/repack.php` is currently UNTRACKED, must land with it) and CARRIES THE ANCHOR FIX ALONG — idempotent/fine. **Do NOT revert the local anchor edit.** Do NOT "fix" local≠deployed by re-deploying local (would leak the ungated→gated repack change live without `repack.php`).
- **STILL OPEN = #2 discriminator** (weekly-operational vs month-end census), unchanged from the ruling above: mig 338 `count_type ENUM('operational','month_end')` + writer affordance + backfill (May 34 rows→`month_end`, June weeklies→`operational`) + COGS query `WHERE month_closed=? AND count_type='month_end'` latest-per-(sku,loc)-then-sum. **GATE: May-fiche FG=47 701.56 regression must hold TO THE CENT. MUST land before any `cogs-monthly-compile --apply --month 2026-06`. NOT started.**

*(Original LOCALIZATION record retained below for derivation rationale.)*

## 🔴 ANCHOR TIEBREAK BUG — `MAX(id)` ≠ latest COUNT → stale-count inflates the anchor (LOCALIZED 2026-06-12, PM read-only probe; operator-flagged via DOAF).
**Operator audit 2026-06-12: "DOAF (Double Oat 20L keg) is NOT 83 across all stocks — inflated."** PM live-probe CONFIRMS 83 IS the bug, ground truth = 45. ROOT CAUSE: `fg_stock_compute()` / `fg_stock_location_snapshot()` define the per-(sku,location) anchor as **`MAX(id)` among `is_active=1`**, NOT `MAX(counted_at)`. When a location has >1 active count and they were INSERTED out of count-date order, `MAX(id)` picks the row with the highest **autoincrement id**, which is NOT necessarily the latest **physical count**.
- **DOAF (sku_id_fk=20, recipe_id=30, format_id=15 Keg 20L, scope='base') live rows:** loc1 Production = id27 (April, inactive) + **id377 counted 2026-05-31 qty 46 (active)** + **id286 counted 2026-06-08 qty 8 (active)**; loc2 Zeubi/Logistique = id209 counted 06-08 qty 36; loc3 Taproom = id320 counted 06-09 qty 1; loc4 Nausikraft = none.
- **The defect:** loc1 `MAX(id)=377` → anchor uses the 05-31 count of **46**, IGNORING the genuinely-later 06-08 count of **8** (id286 < id377 because the May row was entered AFTER the June row). Anchor = 46+36+1 = **83 (WRONG)**. Correct anchor (latest count = `MAX(counted_at)`, tiebreak `MAX(id)`) = 8+36+1 = **45**.
- **Why `MAX(id)` was chosen:** it was a same-day tiebreak (Task A / Option-2, `created_at`-as-tiebreak narrative). It is RIGHT for two counts on the SAME date, WRONG as the PRIMARY order key — count recency must be `counted_at` FIRST, then id as the same-date tiebreak. The arc header (L11-22 of app/fg-stock.php) literally says "anchor = latest count per (sku, location) — MAX(id) per pair" — the doc conflates "latest count" with "MAX(id)". They diverge exactly when an older-dated count is inserted later (backfill, correction, parallel-run).
- **Naive `SUM(qty) WHERE is_active=1` = 91** (all 4 active rows incl. BOTH loc1 rows double-counted) — that is the OTHER wrong number; neither 91 nor 83 is right; **45** is.
- **FIX (the correct anchor subquery):** per (sku, location), pick the row with `MAX(counted_at)`, tiebreak `MAX(id)` for same-date. NOT `MAX(id)` alone. Apply IDENTICALLY in BOTH `fg_stock_compute()` AND `fg_stock_location_snapshot()` (the §B2B invariant-coupling rule — byte-symmetric or Σcards≠Σphysique). The flow-window start (anchor_date) ALSO derives from this row's `counted_at` → fixing the tiebreak auto-fixes the depletion window. **EQUIP: sql + coder + webapp-testing.** No migration (data-layer predicate). Verify: DOAF anchor 83→45; Σcards==Σphysique held; spot-check other SKUs with >1 active count per location (correction/backfill candidates) didn't shift wrongly. 🔴 Opus verifies INDEPENDENTLY (FG/COGS-adjacent — the anchor feeds the COGS-fiche FG census).
- **⚠️ INTERACTS with the never-counted-SKU gap below (do NOT conflate):** that one is a ROW-EXISTENCE asymmetry for scope='none' SKUs; THIS one is a wrong-row pick for SKUs WITH counts. Both live in the same two functions; a combined fix pass is reasonable but they are DISTINCT defects.

## 🔴 NEVER-COUNTED-SKU ROW-EXISTENCE ASYMMETRY = the Σcards≠Σphysique gap (LOCALIZED 2026-06-12, PM read-only probe). NOT same-day; NOT a predicate boundary; tier-(c)'s "restores symmetry" claim was WRONG.
**Live state 2026-06-12: Σphysique(compute)=8177 vs Σcards(snapshot)=6255 → GAP 1922** (the operator's "22" was an earlier, smaller instance of the SAME class; magnitude grew with new ZEPV sales). Root cause is NOT a same-day cutoff and NOT the `>=`/`>` boundary — it is a **ROW-EXISTENCE asymmetry between the two functions for SKUs that have NO anchor row anywhere** (`inv_fg_stocktake` has zero active rows for them; here `stocktake_scope='none'` cuve SKUs):
- **Dominant: ZEPV (sku 61, "Cuve de service", scope='none', NEVER counted).** Two B2B `shipped` orders req 2026-06-11 (1000 + 900 = 1900). `fg_stock_location_snapshot()` Leg 1 tier (c) (strict `>` global anchor 06-09; 06-11>06-09 passes) **manufactures NEGATIVE cards from the sales side alone** — −1000 at Zeubi warehouse (ship-from) + −900 at Nausikraft consignment — Σ −1900. `fg_stock_compute()` deducts the same 1900 but the SKU **has no anchor entry, so Step 9 NEVER EMITS A ROW for it** (`!isset($byId[sku])` → dropped) → contributes **0** to Σphysique. compute=0, snapshot=−1900 → 1900 of the 1922 gap. **The −112 on the Nausikraft consignment card is purely this (sales-only negatives on a never-anchored site).**
- **Tail (the other 22): sku 62/64/66/68/69/70/71/72/74/77/297/302** — same class, 1–4 units each, never-counted cuve/keg SKUs whose handful of B2B/eshop sales the snapshot admits as small negatives while compute drops the row. **THIS tail IS the operator's original "22-unit gap."** So the headline 1922 = 1900 (ZEPV alone) + 22 (the long-standing tail).
- **WHY tier (c) did NOT fix it (the recorded reasoning at §B2B …THREE-tier (c) is mis-stated):** tier (c) only changes the *predicate boundary* to strict `>`. But ZEPV's orders are req 06-11, comfortably **>** the 06-09 global anchor — they PASS tier (c) in BOTH functions identically. The divergence is downstream of the predicate: the **snapshot emits a row whenever a leg touches a (site,sku) pair** (sales create negative rows), whereas **compute only emits rows seeded by a positive anchor**. For a never-counted SKU there is no seed → compute has no row to deduct from → 0; the snapshot has nothing but the deduction → negative. Predicate symmetry ≠ row-set symmetry. **The arc's line-560 claim "tier (c) preserves … Σcards==Σphysique for never-counted SKUs" is FALSIFIED by live data — correct it when the fix lands.**
**🔴 OPERATOR-CORRECTED RESOLUTION (2026-06-12, PM-ratified) — supersedes options (1)/(2) below.** Operator reframed + PM live-probe CONFIRMED: the 13 divergent SKUs are NOT "missing counts" — they **legitimately do not belong in the packaged-FG depletion model at all**. All 13 carry `stocktake_scope='none'` + `is_direct_sales=1` and split into two classes: **ZEPV (61)** = -V cuve, goes BBT→client direct, never passes a packaged-inventory stage (belongs in WIP/liquid COGS + beer-tax via HL, already excluded once gated); **12 tail (62/64/66/68/69/70/71/72/74/77/297/302, format_id 2=B12 / 4=4PB)** = Shopify bundle SKUs whose real packaged inventory is the COMPONENT bottles, not the bundle line. ⟹ **the FIX is to GATE the FG-depletion legs on `stocktake_scope != 'none'`** (don't deplete packaged FG by a SKU that isn't packaged-inventorised), NOT to surface them as honest negatives. This makes the derived FG view's membership predicate match the canonical fact the column already owns (one-fact-one-table; refuse-don't-NULL spirit preserved — we're not zeroing a real depletion, we're declaring non-membership).
  - **Discriminator = `stocktake_scope` (CANONICAL), NOT `is_non_stock`.** Live: `stocktake_scope` fully populated across all 182 active SKUs (base 52 / cage 42 / none 61 / single 27); **`is_non_stock=1` on ZERO rows** — it was mig 322 for T7 tap machines, but T7 was ruled OUT of `ref_skus` entirely (§② equipment ruling), so the flag is inert and the eshop leg's existing `rs.is_non_stock = 0` filter defends a class that doesn't exist. Replace that dead filter with `stocktake_scope != 'none'` (or AND them). Flag-hygiene: decide `is_non_stock` fate w/ operator (backfill-from-scope OR scrap → scrapping-backlog RULE 1); do NOT split the gate across both flags.
  - **WHICH LEGS (verified against live `app/fg-stock.php`):** GATE the 3 SALES legs — **Step 4/Leg 1 (B2B)** [ZEPV's path], **Step 5/Leg 2 (eshop)** [the 12 tail's path; replace the dead is_non_stock filter here], **Step 6/Leg 3 (taproom)** [no scope=none rows today but gate for symmetry] — in **BOTH functions, identically** (the §B2B invariant-coupling rule: predicate must stay byte-symmetric or Σcards≠Σphysique). Gate at the leg level so `salesBySiteSku` never contains a scope=none SKU → the snapshot's "sales-but-no-anchor → create row" block (~L1285-1316, the actual phantom factory) never synthesizes them. ALSO add the predicate to **Step 3 production leg** for defense (today it already excludes via run_type='cuv'/no-events). **Do NOT gate the TRANSFER leg** (unit-gated net-zero by design; scope=none SKUs shouldn't appear there, and gating risks breaking the atomic from/to pairing).
  - **🟢 SEPARATION PROVEN (the pressure-test that validates the gate):** live snapshot shows 18 negative rows / 17 SKUs = TWO classes. The gate removes ONLY the 13 scope=none phantoms (ZEPV −1900, bundles −1..−4). It correctly LEAVES UNTOUCHED the genuine honest-negatives on REAL inventoried SKUs — **ALTB(2,base −1), DGDF(9,base −3), DIB4(10,base −2/75), STI4C(52,base −6/30)** — which are the designed "shipped from a site with less attributed than was there" signal (per-order site override + transfers correct those). `scope != 'none'` cleanly splits suppress-phantom from keep-honest-negative.
**SUPERSEDED EARLIER OPTIONS (kept for context, do NOT implement):** ~~(1) compute emits a negative row for any leg-touched SKU~~ / ~~(2) snapshot suppresses sales-only negatives for never-counted SKUs~~ — both treated the 13 as un-anchored-but-belong; operator clarified they DON'T belong. The line-560 "tier (c) preserves Σcards==Σphysique for never-counted SKUs" claim stays FALSIFIED; correct it when the gate lands.
**eshop-BUNDLE COMPONENT DECOMPOSITION = DEFERRED to Phase 4 (cage engine), NOT this fix.** A bundle sale DOES consume real packaged goods (component bottles + cage + box + labels) — correct model = BOM decomposition (bundle sale → COMPONENT inventory drawdown), which is the ④-Phase 2 cage/box/COGS engine (gated on 2026-06-15 cage anchor + Shopify pickup-signal ON HOLD). **Prerequisite before ANY bundle→component build = settle the base↔single↔bundle UNIT NESTING** (24→1→12/4 ladder; which SKU is inventory-of-record) — same dimension-discipline gate as F2; a "quick" bundle→single decompose before that = the 24× collapse hazard (`942431e` class). Base/single bottles ARE inventoried + anchored (DIBBU qty 33, etc.) so the target inventory exists. **Flag the "component depletion via eshop bundles not yet reflected" gap to operator NOW — do not silently close it** (this was already the recorded ④-Phase 2 position).
**EQUIP: sql + coder + webapp-testing** (+ ui only if `?view=stock` needs blank-state handling for the now-absent rows). No migration (data-layer `app/fg-stock.php`, predicate change in both fns + dead-filter swap). Verification gate (🔴 Opus verifies INDEPENDENTLY — COGS-adjacent): (a) zero negative rows from scope=none SKUs; (b) the 4 genuine honest-negatives ALTB/DGDF/DIB4/STI4C UNCHANGED; (c) `Σ(snapshot per-sku) == compute physique per-sku` for every scope!=none SKU; (d) Σcards==Σphysique to the unit. Probe recipe (read-only, reusable): `fg_stock_compute()` Σphysique vs `fg_stock_location_snapshot()` Σtotal_units, then per-SKU diff.

## ✅ 2-WEEK OPEN-ORDERS PREDICTIVE BUCKET SHIPPED 2026-06-10 — deployed+verified-live, STAGED-not-committed (awaiting Kouros); NO migration (HEAD stays 303); EQUIP coder+sql+ui +webapp-testing
Built exactly to the PM build map. Extends the open-orders predictive view from a single current-week bucket to a NESTED current-week + current+next-week (2-semaines) set. **GLOBAL + per-SKU only — per-site predictive extension explicitly DROPPED (Kouros ruled global sufficient).**

**COMPUTE — `app/fg-stock.php`, `fg_stock_compute()` Step 7:**
- REUSED existing `fg_stock_iso_week_end()` (no new helper). Added `$iso2wkEnd = (new DateTimeImmutable($isoWeekEnd))->modify('+7 days')->format('Y-m-d')`.
- New CUMULATIVE bucket `twowk_qty = SUM(CASE WHEN o.requested_date <= $iso2wkEnd …)`; **param order week-then-2wk (verified)**.
- Emits two new per-SKU fields: `open_2wk_qty` and `live_2sem = physique − open_2wk_qty`.
- Monotonic nesting `live_semaine ≥ live_2sem ≥ live_futur` holds BY CONSTRUCTION (cumulative predicate ≤). **VERIFIED on prod: 0 violations across all 52 SKU rows.**

**RENDER — `public/modules/expeditions.php`:**
- TOTAL strip now 4 KPIs: Physique 8,599 / −sem. courante 7,066 / −2 semaines 6,828 / −toutes ouvertes 5,231.
- Per-SKU table: new column "Sem. courante + suivante" between Sem. courante and futur (colspans 7→8 updated on family-header + drill-down rows).
- Drill-down ledger: middle row gated `open_2wk_qty > open_week_qty`.
- Pinned Commandes strip: 3rd KPI "dispo 2 sem." (8,599 / 7,066 / 6,828 / 5,231).
- Labels: "Sem. courante + suivante" (column) / "− commandes 2 semaines" (total strip) / "dispo 2 sem." (compact).

**OUT OF SCOPE — confirmed UNTOUCHED:** EXP_STOCK_MAP/cmdStockMap saisie hints; flag_survendu/flag_low_stock/hasAlerts row logic; `fg_stock_location_snapshot()`. Per-site predictive extension NOT built (dropped scope).

**VERIFY:** Playwright smoke on app.maltytask.ch `?view=stock` + `?view=commandes` both green; compute-probe as www-data confirmed nesting on all 52 rows.

**Files STAGED (not committed):** `app/fg-stock.php`, `public/modules/expeditions.php`. ⏳ Awaiting Kouros commit+push.

## ✅ ROLE-BASED DUAL STOCK VISUALS — SHIPPED + LIVE + COMMITTED 2026-06-10 (matched the PM ruling; NO migration; data/render-layer only; EQUIP coder+sql+ui +webapp-testing)
**As-built (LIVE-verified end-to-end on prod data):** each user now sees **physique total + their home-site physique** on the Expéditions stock surfaces, home-site DERIVED team→site_type per the ruling below — NO new column, NO migration, zero new compute/store. Built exactly to the ruling (R1 single-home untouched, no second FG-depletion path).
- **Helper `exp_user_home_site_type(array $user): ?string` — TOP-LEVEL** in `public/modules/expeditions.php` (top-level per the nested-fn-fatal lesson). Precedence: (1) `users.manager_scope` (`production`→production, `logistics`→warehouse, `all`→**NULL** [all-scope = no single home → all-sites default, per ruling #4]); (2) else `ref_access_presets.preset_key` via `access_preset_id_fk` (`production_operator`→production, `logistics_operator`→warehouse, `marketing`→pos, else NULL). **preset_key resolved once per request via one prepared SELECT** on `access_preset_id_fk` (string key, not numeric id — ruling honored).
- **`exp_resolve_home_site($snapshot, $siteType)`** matches the snapshot location by `site_type` → `{units,name}`, sourced from `fg_stock_location_snapshot()` (NO re-query, NO compute change — ruling #3).
- **Renders on BOTH views:** `?view=stock` header (`exp-st-home-site` KPI) and `?view=commandes` pinned strip (`exp-cmd-stock-strip__kpi--home`); the commandes view got a `fg_stock_location_snapshot()` call ADDED (was missing per ruling #5). Both null-guarded → admin/all-scope sees total + all-sites, no home KPI (ruling #4). CSS in `public/css/expeditions.css` (oak-tint accent + responsive variants). Labels use canonical `ref_sites.name` from the snapshot (no-DB-nomenclature, ruling #6).
- **LIVE-verified:** Nathan(logistics)→Zeubi 5540u · prod ops→Zgeg 1492u · Olivier(marketing)→Taproom 446u · Kouros/Arthur(admin/all)→total + all-sites (no home KPI). `php -l` clean, deployed, smoke 200 on both views.
- **Commit state:** the B2B expedie fix is its OWN clean commit `97e7a7c` (`app/fg-stock.php` only). The role-visual render + relabel landed in the OPERATOR'S OWN commit **`badd153`** — bundled with their stock-risk-detail-modal + mouvements-multi-line features (intermixed across the same files `expeditions.php`/`.css`/`.js`, NOT separable). Working tree clean afterward. **9 commits unpushed on main** (operator backlog; not pushed by this session). NB §Stock-warning DETAIL MODAL above already noted `badd153`'s multi-feature bundling — this section is the FG-stock side of that same commit.

*(Below is the now-HISTORICAL PM pre-build ruling, kept verbatim for the derivation rationale; superseded by the SHIPPED record above.)*

## 2026-06-10 — ROLE-BASED DUAL STOCK VISUALS (physique total + team's home-site physique): PM PRE-BUILD RULING
Operator wants two figures per logged-in user on the Expéditions stock surface: **physique total** + **the physique of the user's team's home site**. Logistics (Nathan/Stéphane/Joelson) → Zeubi/warehouse; Production → Zgeg/production; Marketing (Olivier) → Taproom/pos.

**LIVE USER/SITE MODEL (PM-probed 2026-06-10 — verified, do not re-probe unless schema changes):**
- **NO `users.site_id_fk` / `home_site_id` exists.** `users` cols: role ENUM(`admin`,`operator`,`viewer`,`manager`), `manager_scope` ENUM(`production`,`logistics`,`all`) NULL, `access_preset_id_fk`→`ref_access_presets`. NO department/team/site column.
- **Team partition lives in TWO places, neither alone covers everyone:** managers carry `manager_scope` (production/logistics/all); **operators have `manager_scope=NULL`** and are partitioned ONLY by `access_preset_id_fk`. Presets: 1=manager, 2=production_operator, 3=logistics_operator, 8=marketing. Roster confirms: Nathan(8,mgr logistics)+Stéphane(9)+Joelson(10) on preset 3; Maxime/Charles/Xavier/Alex on preset 2; Olivier(19) on preset 8.
- **`ref_sites.site_type` ENUM(`production`,`warehouse`,`pos`,`consignment`)** is the clean SoT for the target-site dimension. Rows: 1 Zgeg=production, 2 Zeubi=warehouse, 3 Taproom=pos, 4 Nausikraft=consignment.

**RULING (decisive):**
1. **DERIVE team→site_type at the SoT level — do NOT hardcode names, do NOT add a `users.home_site_id_fk` migration.** The home-site is a TEAM attribute, not a per-user fact; a per-user FK would be a parallel/divergent store of something `manager_scope`+`preset` already encode. The mapping is **team → site_type**, then `ref_sites WHERE site_type=? AND is_active=1` (set-valued, exactly how `fg_stock_compute()` already derives prod sites). Mapping: **logistics→warehouse, production→production, marketing→pos**. `consignment` (Nausikraft) has no team → never a home site.
2. **Team resolution = a single helper `exp_user_home_site_type($user): ?string`** with this precedence: (i) if `manager_scope` set → `production`/`logistics`/`all`; (ii) else map `access_preset_id_fk` via `ref_access_presets.preset_key` (`production_operator`→production, `logistics_operator`→logistics, `marketing`→pos, `manager`→all). Map by **`preset_key` (the stable string), NOT the numeric id** — ids are environment data. `all`/admin/viewer/unmapped → NULL (no single home site). This reads existing columns, NO new column, NO migration. (NB: build the team→site_type map from `preset_key` + the `manager_scope`/`site_type` enums — both are stable enum vocabularies, so the only "config" is the 3-row correspondence table in PHP, which is the legitimate place for an enum↔enum mapping that has no DB home.)
3. **SOURCE the per-site physique from `fg_stock_location_snapshot()`, NOT a re-query.** It already returns `locations[]` each with `id`,`name`,`site_type`,`total_units`,`total_hl` (verified L1269-1277). The home-site figure = the snapshot location whose `site_type` == the user's home_site_type (sum if a team maps to >1 active site of that type — currently 1:1, but keep it set-valued). Physique total = the existing TOTAL strip number (`fg_stock_compute()` physique sum) — already computed. So this is a PURE RENDER-LAYER change reading two existing derivations; ZERO new compute, ZERO new store. **Confirmed: R1 single-home is untouched — no new FG-depletion path.**
4. **GRACEFUL DEFAULT for no-home-site users (admin, manager_scope=all, viewer, unmapped):** show **physique total + a compact all-sites breakdown** (the 3-4 location cards already exist on `?view=stock`; on the strip, total only with a "tous sites" affordance). NEVER fabricate a home site; NEVER hide total. Admin/Kouros wants the full picture, not one site.
5. **RENDER LOCUS — both surfaces, but reuse the snapshot:**
   - `?view=stock` header (`expeditions.php` render block ~L2591+) ALREADY calls `fg_stock_location_snapshot()` (L1653) → add the home-site highlight there for free.
   - The pinned `exp-cmd-stock-strip` on `?view=commandes` (~L2389) currently does NOT call the snapshot (only `fg_stock_compute` for `$cmdFgPhysique`, L2380). To show the home-site figure on the strip, **add a `fg_stock_location_snapshot()` call on the commandes view** (it's a pure-SELECT lib fn, operator-frequency load — acceptable, same justification as the existing extra `fg_stock_compute` calls L1963/L1986). Strip shows: **physique total + physique <home-site-name>** (use the canonical `ref_sites.name` display string from the snapshot row, e.g. "Zeubi (Logistique)", NEVER re-derive from site_type).
6. **NO-DB-NOMENCLATURE:** label with the canonical site name from `ref_sites.name` (already in the snapshot payload), not the enum (`warehouse`/`pos`) and not a hardcoded "Zeubi". The team→site_type derivation is internal; the operator sees "physique Zeubi (Logistique)".

**DIVERGENCE FLAGS:** (i) hardcoding the three names = the exact anti-pattern to refuse (ruling #1/#2 derive instead); (ii) a `users.home_site_id_fk` migration = a parallel store of team-encoded data (refused — derive from `manager_scope`+`preset_key`); (iii) re-querying per-site stock instead of reading the snapshot = a second derivation path that can drift from the cards (ruling #3 forbids); (iv) `consignment` has no team — never surfaces as a home site.

**SCOPE NOTE FOR ORCHESTRATOR — one operator choice to confirm before dispatch:** the operator named individuals but described teams; the `manager_scope`+`preset_key` derivation covers all of them today (verified). The only judgment is the no-home-site default for `manager_scope='all'` / admin (Gonzalo/Yves are production managers → production; but a future `scope='all'` manager, and Kouros-admin, get the all-sites breakdown per ruling #4). Confirm with the operator that an `all`-scope user seeing total + all-sites (not one site) is the intended behaviour. Everything else is derivable with no choice.

**EQUIP: ui + sql + coder + webapp-testing** (render-layer change in `expeditions.php` + the team→site_type helper reading `ref_access_presets`/`users`; smoke `?view=stock` and `?view=commandes` as a logistics user shows physique total + physique Zeubi; as admin shows total + all-sites). NO migration (HEAD stays 303). Files: `app/fg-stock.php` (NONE — read-only reuse), `public/modules/expeditions.php` (helper + strip + stock-header render), `public/css/expeditions.css` (strip second-figure styling, no inline CSS).

## ✅ PER-LOCATION PHYSIQUE BREAKDOWN in the stock drill-down ledger — SHIPPED + LIVE 2026-06-10 (NO migration; render-only — `public/modules/expeditions.php`; SELECTIVE deploy; NOT committed — operator parallel-work tree)
Inside the `?view=stock` per-SKU `exp-stock-ledger` drill-down, a **"Répartition par site"** sub-block now lists each location's qty + HL for that SKU, placed directly after the `exp-ledger-physique` total row and before the open-orders `exp-ledger-sep`.
- **Reads the already-loaded `$fgLocationSnapshot`** — NO re-query, NO compute change (R1/R3 honored: pure render of an existing derivation).
- **Gate `qty != 0`** — surfaces negatives with the `exp-st-neg` class (DELIBERATE asymmetry vs the inline `>0` cell mini-string; commented in code).
- Each row = full canonical `$lc['name']` + muted `exp_site_type_label(site_type)` qualifier (no-DB-nomenclature honored).
- **Residual guard:** `$locSum != round($physique)` → muted "écart de comptage / en transit" line (NO assert/throw — same-day-transfer residual is an accepted state). DOAF live-verified: Zgeg 8 / Zeubi 30 / Taproom 1 = 39, no écart.
- **CSS:** reuses already-deployed `exp-ledger-*` + `exp-st-neg` classes (no CSS dependency). New wrappers `exp-ledger-loc-header` / `-row` / `-ecart` are MARKUP-ONLY (no CSS rule yet).
- **Deploy = SELECTIVE — only `public/modules/expeditions.php` rsynced** (chown maltytask:www-data, 644). `bin/deploy.sh` NOT run: working tree has ACTIVE PARALLEL OPERATOR WORK uncommitted (form-packaging.php + un-run migs 310/313, kpi-handlers.php, mon-tableau.css, kpi-charts.js, packaging-form.js, AND a 168-hunk in-progress overhaul of expeditions.css) → deploy-pushes-working-tree leak hazard. Verify: php -l clean (pre + deployed), curl 302 (not 500), markup grep-confirmed live.
- **NOT committed** — tree left for the operator (parallel-work + leak hazard live).
- **🟡 TWO KNOWN FOLLOW-UPS:** (1) one inline `style="opacity:.7"` on the qualifier span VIOLATES the no-inline-CSS rule — left because expeditions.css can't deploy without shipping the operator's overhaul; convert to a class when css next deploys. (2) the markup-only `exp-ledger-loc-*` classes get proper CSS refinement on the operator's next expeditions.css deploy.

## ✅ "Activité récente" SKU ACTIVITY TIMELINE in the stock drill-down — SHIPPED + LIVE 2026-06-10 (NO migration; render-only `expeditions.php` + NEW `public/css/expeditions-sku-history.css`; SELECTIVE deploy; NOT committed — operator parallel-work tree)
4th section inside the `?view=stock` `exp-stock-ledger` drill-down, **below "Répartition par site"** (per-location block §above). Single chronological (date DESC) merged list of the SKU's last events, 3 types: customer **Order** (↓, date·client·qty), packaging **Production/Conditionnement** (↑, date·qty), stock **Mouvement** (↔ neutral, date·"from → to"·qty). Empty → muted "Aucune activité récente".
- **Architecture = exactly per PM ruling, pattern (A) bulk-bucket (NOT per-SKU WHERE):** 3 bulk queries TOTAL for the whole page regardless of SKU count, each `ROW_NUMBER() OVER (PARTITION BY sku_id_fk ORDER BY date DESC) <= 15` + 90-day window + historical-only (`date <= CURDATE()`); bucketed into `$timelineBySkuId`; merged + usort DESC + cap-15 at render. **NO AJAX, NO endpoint, NO second derivation path** (R1 honored — read-only, writes nothing).
- **Data sources + predicate parity:** **Orders** = `ord_order_lines×ord_orders×ref_customers`, `status<>'cancelled'` AND `order_type='customer'` (internal eshop/taproom EXCLUDED — already separate ledger legs), client=`COALESCE(NULLIF(ref_customers.name,''),'—')` escHtml'd. **Packaging** = `bd_packaging_v2` **mirroring `fg_prod_since_anchor()` predicates EXACTLY** (is_tombstoned=0, is_white_label=0, sku_id_fk IS NOT NULL, **`run_type<>'cuv'` load-bearing**, FLOOR(prod_total_units/units_per_pack)) — the ONE INTENDED divergence = anchor-cutoff dropped for the 90-day window history (code-commented). **PREDICATE-PARITY VERIFIED** packaging-vs-`fg_prod_since_anchor` (identical except anchor-cutoff) → the R3 production-divergence risk is CLOSED. **Movements** = `inv_stock_movements×ref_sites(from,to)`, is_tombstoned=0, neutral.
- **UI:** reused `.exp-ledger-*` table rhythm + NEW scoped `.exp-evt-*` / `.exp-ledger-evt--order|--pkg|--move` classes; sketch-arrow glyphs (NOT emoji); dates via `exp_fmt_date()` (day-first); type colour via row class.
- **NEW stylesheet `public/css/expeditions-sku-history.css`** (PM-blessed separate file to dodge the operator's in-progress 168-hunk expeditions.css overhaul): 126 lines, 100% var()-token, 0 hardcoded hex, 0 illegal `.exp-ledger-*` redefinitions, `?v=filemtime` cache-bust `<link>`.
- **Deploy = SELECTIVE rsync of ONLY `expeditions.php` + the new css file** (chown maltytask:www-data 644). `bin/deploy.sh` NOT run (leak hazard still live: form-packaging.php + migs 310/313, kpi-handlers.php, mon-tableau.css, kpi-charts.js, packaging-form.js, 168-hunk expeditions.css overhaul). Verify: php -l clean (pre+deployed), css 200, page 302 (not 500), markup grep-confirmed. DOAF live: 06-08 Au Grain d'Orge ×6 + 16-04 Production ×74; SKU 42 shows all 3 types DESC.
- **NOT committed** — tree left for operator. No migration, HEAD unchanged, no schema_meta row (no new table).
- **🟢 RESOLVED 2026-06-11 (commit `3e368d2`):** folded into `expeditions.css`, standalone file deleted, inline `opacity:.7` → classes. See §CSS FOLD-IN CLOSED.

## ✅ FLIPPABLE DRILL-DOWN (Stock | Activité faces) — SHIPPED + LIVE + BROWSER-VERIFIED 2026-06-10 (NO migration; render-only `expeditions.php` + `expeditions-sku-history.css` + `expeditions-stock.js`; SELECTIVE deploy of 3 files; ✅ NOW COMMITTED — clean 3-file commit `711905c` on main, LOCAL/UNPUSHED)
The `?view=stock` per-SKU `exp-stock-ledger` drill is now SPLIT INTO TWO FACES behind a `role=tablist` **segmented toggle (NOT a 3D flip)** — exactly per PM ruling. Supersedes the layout of the two §above (per-location + Activité were stacked in one pane; now they live on separate faces, uncramming the drill).
- **Face "Stock"** (default) = anchor/+/− ledger → Physique total → Répartition par site → open-order/disponible block, all in a `.exp-evt-pane`.
- **Face "Activité"** = the Activité récente timeline relocated into its OWN `<table>` inside `.exp-evt-pane--activite` (`role=region`, `tabindex=0`, **max-height 24rem/384px + overflow-y:auto** scroll container — the load-bearing "uncrammed" fix).
- **Caps raised:** per-SKU activity 15→50, window 90→180 days (all 3 bulk ROW_NUMBER queries + the render `array_slice`). Still 3 bulk queries page-wide (pattern (A) bulk-bucket preserved — NO AJAX, NO endpoint, NO 2nd derivation; R1 still honored).
- **JS (`public/js/expeditions-stock.js`):** ONE delegated tab handler on tbody keyed by `button[data-evt-tab]`, scoped to `closest('.exp-stock-drill')` (NO per-row listeners — mirrors the existing `data-loc-id` delegation idiom); `resetDrillFace()` hooked into **ALL 3 collapse paths** (row-click, `applyVisibility` force-collapse, sort restore) so re-expand always starts on Stock; ArrowLeft/Right keyboard nav. The existing row-click guard (`closest('a,button,input')` early-return) means tab clicks don't collapse the drill. **`expeditions-stock.js` confirmed clean before edit.**
- **CSS:** new `.exp-evt-tabs` / `.exp-evt-tab` / `.exp-evt-pane` / `.exp-evt-pane--activite` in `expeditions-sku-history.css` (banner broadened to "drill-wide toggle + Activité timeline"); ≥44px touch targets, focus-visible ring, aria-selected active state; **100% var()-tokens, 0 hex, ZERO `.exp-ledger-*`/`.exp-stock-*` redefinitions.**
- **a11y:** real `<button>`s, role=tablist/tab/tabpanel, aria-selected, aria-controls/id pairing, arrow-key nav, ≥44px, scroll pane role=region+tabindex.
- **BROWSER-VERIFIED 18/18 via Playwright** (smoketest bot temporarily elevated viewer→operator then reverted EXACTLY — before/after SELECT confirmed, zero residual): default Stock, flip to Activité, panes show/hide, **independent per-drill state** (drill 20 Stock unaffected when drill 42 flipped), reset-on-collapse, keyboard arrows, scroll-bounded pane. Zero app JS errors.
- **Deploy = SELECTIVE rsync of ONLY 3 files** (`expeditions.php`, `expeditions-sku-history.css`, `expeditions-stock.js`). `bin/deploy.sh` NOT run (operator parallel-work leak hazard still live). php -l clean (pre+deployed), page 200. No migration, HEAD = `711905c`.
- **✅ COMMITTED 2026-06-10 = clean 3-file commit `711905c` on main (LOCAL/UNPUSHED).** Exactly the 3 files (`public/modules/expeditions.php`, `public/js/expeditions-stock.js`, `public/css/expeditions-sku-history.css`) — all three §above ships (Répartition par site + Activité timeline + flippable toggle) consolidated into this one commit.
- **⚠️ PATHSPEC INCIDENT (caught + recovered, never left local) — DURABLE LESSON:** the FIRST commit attempt was a bare `git commit`, which swept in the operator's STAGED parallel work (`kpi-handlers.php`, `mon-tableau.css`, `kpi-charts.js` — the operator had `git add`-staged them mid commit-prep). Caught immediately → `git reset --soft HEAD~1` + re-committed via pathspec `git commit -- <the 3 explicit paths>` (which preserves the operator's exact staging). Net: bad commit never left local, operator's staging fully restored, `711905c` is clean-3-files. **RULE: on this shared tree ALWAYS `git commit -- <explicit paths>` (pathspec form), NEVER bare `git commit` — the operator routinely leaves files staged mid-prep, so bare `git add -A`/bare `git commit` will entangle their work.** (Same anti-entanglement intent as the daily `git commit -- <files>` discipline; this is the realized hazard, now codified.)
- All existing gates/comments preserved verbatim (the `qty != 0` Répartition comment + the packaging anchor-cutoff-divergence comment).
- **🟢 CSS fold-in debt — NOW CLOSED 2026-06-11 (commit `3e368d2`). See §CSS FOLD-IN CLOSED below.**

## ✅ CSS FOLD-IN DEBT — CLOSED + COMMITTED + PUSHED 2026-06-11 (commit `3e368d2`, range `b477565..3e368d2` on origin/main; NO migration; CSS+PHP only)
The three deferred CSS items (standalone `expeditions-sku-history.css` fold-in + per-location `exp-ledger-loc-*` CSS + inline `opacity:.7` violation) are ALL resolved. Closes the §FLIPPABLE / §PER-LOCATION / §"Activité récente" fold-in backlog.
- **Standalone `public/css/expeditions-sku-history.css` is GONE.** Its rules (8 `.exp-evt-*` tabs/panes/scroll + per-location `.exp-ledger-loc-*`) folded into `public/css/expeditions.css` as a scoped, commented section — var() tokens only, collision-grep confirmed clean (NO `.exp-ledger-*`/`.exp-stock-*` redefinition).
- **Inline `opacity:.7` styles → classes** `.exp-ledger-loc-qualifier` / `.exp-ledger-loc-ecart` (no-inline-CSS house rule honored).
- **Dangling `<link>`** to the standalone file removed from `expeditions.php`.
- **Deploy = selective rsync** (`expeditions.php` + `expeditions.css`; standalone `rm`'d on VPS). php -l clean; flip-smoke re-verified 20/22 (2 explained false-fails: visible tabs confirmed 44px; CSP report-only warnings pre-existing).
- **State:** HEAD == origin/main; all migrations applied (Pending: 0). Expeditions stock-drill suite (per-location + Activité timeline + flippable + folded CSS) fully shipped/committed/pushed/deployed/browser-verified. **NO open items on the expeditions stock-drill arc.**
- **⚠️ REALIZED two-sessions-one-clone hazard (the worktree lesson):** this fold-in collided with a SECOND concurrent Claude session working the Financier pages in the SAME `maltyweb` clone. Sequence: my build agent `git rm`-staged the standalone file → the Financier session's BARE `git commit b477565` swept that staged deletion + pushed it, leaving HEAD linking a now-404 CSS file; my fold-in commit `3e368d2` repaired it (removed the link, added the rules). Net is correct, but the root cause is two sessions sharing one git index. I committed strictly by pathspec (`git commit -- expeditions.css expeditions.php`) so the Financier session's in-flight `tanks.php` + untracked mig 324 were untouched. **RECOMMENDATION TO OPERATOR (standing): give each concurrent session its own `git worktree`/clone — committing two sessions into one shared index is exactly what produced b477565's accidental sweep.** Reinforces the operator's git-discipline memory ("two strikes = treat bare commit as hard error") and the pathspec rule at §FLIPPABLE line ~591.

## ✅ SHOPIFY ⇄ maltyweb FULFILMENT INTEGRATION — ROADMAP APPROVED 2026-06-10 (Kouros; plan `~/.claude/plans/sleepy-snuggling-brooks.md`). RESOLVES the long-PARKED ④ Phase-2 / pickup-signal ON-HOLD gate (the §① "FORWARD HAZARD" at lines ~310 + MEMORY active-arc).
> Trigger "Shopify"/"pickup"/"click&collect"/"retrait"/"shipping_lines"/"fulfilment_mode"/"eshop picking"/"push-shopify"/"write-back"/"ref_shipping_methods"/"inv_sales_fulfilment" → read THIS section first.

### 🔑 KEY FINDING — the pickup/click&collect signal lives in `shipping_lines[].title` (LIVE Shopify-API-probed during planning), NOT a tag and NOT `fulfillment_status`.
- `fulfillment_status` is a COMPLETION-state field (a false friend for pickup-vs-ship — do NOT use it to classify mode). The ingest does **NOT currently fetch `shipping_lines`** at all.
- **Live pickup titles:** "La Tap & Shop (mercredi - samedi ; 14h-22h)", "RETRAIT A CRISSIER"/"à CRISSIER", "La Nébuleuse SA".
- **Live delivery titles:** "La Poste - Vinologue (fragile)", "Vinologue Suisse - Fragile", "Standard Suisse", "Livraison Offerte/offerte/gratuite".
- Messy free-text (team is mid-standardization) → **curated map + `review` bucket, NEVER guess** (never-guess-a-mapping canon). The plan threads this directly into the §① pickup double-deplete forward-hazard: once pickup orders carry a mode and sync into `inv_sales_orders`, they deplete via the eshop leg and the picking view READS them — they are NEVER re-created as `ord_orders` (XOR holds).

### Approved phased roadmap
- **Phase 0 — FEED FOUNDATION (buildable NOW, do FIRST; zero depletion risk):** (1) fix the ingest **line-idempotency bug** (below) FIRST; (2) capture `shipping_lines` → raw `shipping_method_title` on `inv_sales_orders`; (3) curated classification — new **`ref_shipping_methods`** (title_norm UNIQUE, `fulfilment_mode` enum pickup/delivery, seeded from the verified live titles) → ingest sets new **`inv_sales_orders.fulfilment_mode ENUM('pickup','delivery','review')`** (unknown→`review`, operator-actionable); (4) fix the hardcoded `--until 2026-05-01` (cause of the **Apr-30 data gap**) → default "now" + one backfill run + **cron the ingest on a trailing window with a flock**. Mig `320_inv_shipping_method_map.sql` (table + the 2 `inv_sales_orders` cols). NO gate.
- **Phase 1 — PER-ORDER ESHOP PICKING SURFACE (buildable now, READ-ONLY re depletion):** replace the date-aggregate eshop auto-row on the Commandes board with **per-order eshop rows** (customer, SKU pills, **pickup/delivery badge** 🏬 Retrait / 🚚 Livraison / ⚠ à classer, per-day pickup-vs-ship counters). Pure read from `inv_sales_orders`+lines (XOR trivial). Rows carry `data-eshop-order-id` (NOT `data-order-id`) so existing status JS never touches them; no actions yet. Include eshop in the cross-date search. Gate: Phase-0 `fulfilment_mode`.
- **Phase 2 — FULFILMENT TRACKING + BIDIRECTIONAL SHOPIFY WRITE-BACK (the big piece; build after 0+1):** eshop workflow state in a **dedicated 1:1 side table** `inv_sales_fulfilment` (keyed on `inv_sales_orders.id`) + `inv_sales_fulfilment_events` (append-only) — MIRRORS the `ord_orders` + `ord_order_status_events` pattern; lazy-created on first action (absent row ⇒ implicit `new`/`idle`); mode-aware lifecycle (delivery new→picking→picked→fulfilled; pickup …→ready_for_pickup→picked_up); cols incl. `shopify_sync_state` enum idle/pending/pushed/failed + `shopify_fulfillment_id` + push_error/attempts. Mig `321_inv_sales_fulfilment.sql`. UI: `exp_render_eshop_chips()` + sync badge + `public/js/eshop-fulfilment.js`. API: `public/api/eshop-fulfilment-status.php` (clone of `expeditions-status.php`; one txn = event + cache upsert + audit; push-triggering status sets `shopify_sync_state='pending'`, does NOT call Shopify inline). Worker: **`scripts/push-shopify-fulfilment.ts`** (cron+flock) drains `pending` → Shopify Admin **Fulfillment Orders API** (delivery=create fulfillment→fulfilled; pickup=ready_for_pickup→picked_up); idempotent (check open fulfillment-order + stored id; "already fulfilled"=convergence); capped retry + operator re-arm. **Reverse reconcile** via the now-idempotent ingest (`source='shopify_reconcile'`). **CONFLICT POLICY: maltyweb-wins-forward — reconcile only ever ADVANCES local to match Shopify, never reverts an operator action.** GATE: confirm the Shopify Admin token has fulfillment **write** scopes (read-only blocks write-back).
- **Phase 3 — PICKUP HARDENING + TAP MACHINES (gated on the team standardizing shipping-method names):** prune/extend `ref_shipping_methods` once names settle (drive `review`→0; admin to map unknowns without deploy; split board into Retrait/Livraison lanes). **Tap machines (T7) = NON-STOCK:** new `ref_skus.is_non_stock` flag (mig `322_*`, orthogonal-flag doctrine per mig 210); fulfillable + on the picking view but excluded from FG sum via `AND rs.is_non_stock = 0` added **SYMMETRICALLY** to the eshop leg in BOTH `fg_stock_compute()` AND `fg_stock_location_snapshot()` — verify `Σcards==Σphysique` after. (Resolves the §② "T7/Cuves = equipment, must NOT deplete FG" ruling at the SKU-config level.)
- **Phase 4 — CAGE/BOX COGS ENGINE = OUT OF SCOPE (named only).** The Stage-2 / ④ Phase-2 cage/box draw-down engine (downstream CONSUMER of this fulfilment data, gated on the 2026-06-15 cage anchor) is a SEPARATE arc, not designed here.

### Hard constraints carried (non-negotiable)
- **XOR depletion:** order ∈ `ord_orders` XOR `inv_sales_orders`, never both. Eshop fulfilment workflow creates **NO** `ord_orders` rows. The discriminator is membership in `inv_sales_orders`, not a flag we invent (§①c canon).
- **Single-homed FG derivation:** `fg_stock_compute()` is the only FG depletion. ANY filter added to its eshop leg (`app/fg-stock.php` ~L462-515) MUST be mirrored in `fg_stock_location_snapshot()` (~L814-850) or `Σcards ≠ Σphysique`. **P0/P1 add NO fg-stock filter → zero depletion risk. The only symmetric-filter change is P3 (`is_non_stock`).**
- **Raw vs computed:** `inv_sales_orders`/`inv_sales_order_lines` are `class='source'`, `corrections_policy='blocked_with_redirect'` (LIVE-VERIFIED 2026-06-10) — operator workflow state goes in the `inv_sales_fulfilment` SIDE TABLE, NEVER on the raw table. An ingest-derived `fulfilment_mode` column IS OK on `inv_sales_orders` because it's a PURE FUNCTION of the Shopify pull (same class as the existing `fulfillment_status`/`financial_status`/`period` derived cols), not operator-entered state.

### 🔴 INGEST LINE-IDEMPOTENCY BUG (Shopify side — must fix FIRST, Phase-0 item 1)
`scripts/ingest-shopify-orders.ts` `insertOrderBatch()` L378-384: `const wasDupe = orderRes.affectedRows === 0; if (wasDupe) { ordersDupe++; continue; }` — when an order PRE-EXISTS (INSERT IGNORE no-op), it `continue`s at ORDER level → **its lines are never re-evaluated and mutable header fields never updated.** So a re-pull of a CHANGED order silently no-ops; you can NEVER enrich existing orders with `shipping_lines`/`fulfilment_mode`, advance `fulfillment_status`, or do reverse-reconcile. **Fix: on pre-existing order, still (a) `INSERT IGNORE` the lines (already row_hash-idempotent), (b) `UPDATE` the mutable header fields (`fulfillment_status`, `shipping_method_title`, `fulfilment_mode`).** This is the SHOPIFY-side parallel of the already-recorded `import_weeklyorders.php` `ord_order_lines` UNIQUE/ODKU bug (§WeeklyOrders status one-liner) — same "header idempotent, lines/mutables not" class, different table.

### Phase-0 build facts (PM-verified live 2026-06-10)
- **MIGRATION HEAD = 319 applied (`migrate.php --status`); next free = `320`.** (Parallel sessions live — re-`--status` + `ls db/migrations` at build-start per checklist; 312 already collided once 312_drop_invoice_validate_topbar vs 312_kpi_sales — so a fresh check is mandatory.)
- **`schema_meta` for the NEW `ref_shipping_methods` table:** `table_class='reference'`, `corrections_policy='allowed'`, `upstream_hint` ≈ "Shopify shipping-method title → pickup/delivery classification map. Seed from verified live titles; add/edit via Expéditions admin or direct MySQL; unknown titles land orders in fulfilment_mode='review'." (Mirrors `ref_transporters`/`ref_customers` which are exactly `reference`/`allowed`.) ONE `schema_meta` row required per the per-new-table rule.
- **The TWO new `inv_sales_orders` columns** (`shipping_method_title` VARCHAR, `fulfilment_mode` ENUM('pickup','delivery','review')): **NO schema_meta change needed** — the table is ALREADY `class='source'`/`blocked_with_redirect`; ALTER-adding ingest-derived columns does NOT change its classification (schema_meta is per-TABLE, not per-column). Just add the columns; do not touch its schema_meta row.
- **INGEST HOST / CRON = the maltytask LOCAL host, NOT the VPS.** `ingest-shopify-orders.ts` runs `npx tsx scripts/ingest-shopify-orders.ts` on the maltytask box: it reads `secrets/shopify.env`, connects to MySQL **over the SSH tunnel (13306)** via `withConnection`/`withTransaction`, emits `logAudit` to `data/audit-log.jsonl`. It is run MANUALLY today (no cron). **There is NO existing maltytask user crontab** (`crontab -l` empty). The flock pattern to MIRROR = **`scripts/send-ingest-recap.ts`** (the only flock-guarded script; "designed to be run by a flock-guarded cron", writes preview to `/tmp`, exits 0 silently when nothing to do). Follow its lock idiom for the P0 ingest cron (and the P2 worker cron). NB: the SSH tunnel needs `-o ServerAliveInterval=30 -o ExitOnForwardFailure=yes` for an unattended cron run (existing keepalive convention).
- **APR-30 GAP cause = hardcoded `--until 2026-05-01`** in the ingest's default window (`'fulfillment_status','total_price',…` field set is fine; the date bound is the gap). Default it to "now" + run one backfill to today, THEN cron a trailing window. Dry-run-verify the backfill BEFORE going live (double-run = 0 orders/lines inserted but a changed `fulfillment_status` updates; FG totals UNCHANGED since no eshop-leg filter is added in P0).

### EQUIP — Phase-0 Sonnet build: **coder + sql** (+ webapp-testing only if P0 surfaces any rendered page — it does NOT, P0 is ingest+migration only, so coder+sql suffices). The TS edit (`ingest-shopify-orders.ts`, maltytask) = coder; the maltyweb migration `320` = sql. NB cross-repo: the migration lands in **maltyweb** (`db/migrations/`, applied via `migrate.php` on the VPS), the ingest edit lands in **maltytask** (`scripts/`, run locally over the tunnel) — two repos, one phase.

### Phase-0 gotchas (durable)
- **TS ingest edit:** preserve the `logAudit({...})` discipline — the `wasDupe` branch currently SKIPS audit; the new "update existing order" path must emit a `logAudit` event (e.g. `action:'order-updated'`) so the change shows in `data/audit-log.jsonl`. Keep `--dry-run` working (the `DryRunRollback` throw + ROLLBACK pattern at L425-434) for both the line re-eval and the header UPDATE. The order **INSERT IGNORE → upsert** change: do NOT switch the INSERT to `ON DUPLICATE KEY UPDATE` blindly — `row_hash` is part of the UNIQUE; a content change yields a NEW row_hash, so the upsert key must be the STABLE key (`UNIQUE(source, external_order_id)`), and the UPDATE must target only the mutable header cols (not re-stamp `created_at`/`period`). row_hash stays a change-detector, not the upsert key.
- **Migration:** MySQL 8 (no `IF NOT EXISTS` on `ADD COLUMN`; no bare SELECT in the .sql — `migrate.php` exec() leaves result sets open). FK type on `ref_shipping_methods` PK and any FK = match exactly. The `inv_sales_orders` ALTER adds 2 columns (INSTANT DDL on MySQL 8 — cheap). ENUM `fulfilment_mode` default should be `'review'` (so an un-classified order is operator-visible, never silently mis-moded). schema_meta INSERT for `ref_shipping_methods` in the same migration.

### Sequencing verdict (CONFIRMED)
- **P0 is SAFE to build + land NOW with ZERO change to `fg_stock_compute()` → zero depletion risk.** P0 adds an ingest-derived column + a classification map + cron + a bug-fix; it touches NO fg-stock leg. The first fg-stock change in the whole roadmap is P3's symmetric `is_non_stock` filter. P0+P1 ship first (immediate value, no depletion risk), then P2 (bidirectional sync, gated on the Shopify write-token scope), then P3 (gated on team name-standardization + the cage anchor).
- **Cron/backfill MUST be dry-run-verified before going live** (the ingest's `--dry-run` already ROLLBACKs; verify the `fulfilment_mode` distribution + a small `review` bucket + a double-run = 0 new rows but a changed `fulfillment_status` updates + FG totals unchanged, BEFORE arming the cron).

### ✅ PHASE 0+1 LANDED + COMMITTED 2026-06-11
- **maltyweb `7e1c001`** = mig **321** `inv_shipping_method_map` (`ref_shipping_methods` classification table + the 2 `inv_sales_orders` cols `shipping_method_title`/`fulfilment_mode`). **APPLIED on VPS** (PM-verified `migrate.php --status`: 321 ✓, Pending 0). NB roadmap predicted mig **320** for this; it actually landed as **321** (320 was the perf index `idx_bd_packaging_v2_reuse_ts` from the parallel mon-tableau session). Don't trust the roadmap's "320" — `--status` is truth.
- **maltytask `bac5501`** = the Shopify ingest edit (line-idempotency bug fix + `shipping_lines` capture + `fulfilment_mode` set + `--until` default-now). **maltytask `e7962ce`** = Phase-1 per-order eshop rows on the Commandes board + cross-date eshop search (`data-eshop-order-id` namespaced).
- **FG delta from the May/June backfill = −5 units (corrective, small).** Acceptable, recorded.

### ✅ PHASE 2 SPLIT 2A/2B — PM CONFIRMED 2026-06-11 (build 2A; 2B disarmed)
**Split is sound and approved.** 2A = maltyweb-internal fulfilment tracking (side tables + status chips + API) → **goes LIVE** (team marks préparé/prêt/remis in maltyweb with NO Shopify dependency). 2B = `push-shopify-fulfilment.ts` write-back worker → BUILT + dry-run-verified but **DISARMED (no cron)**.
- **🔑 2B ACTIVATION PREREQUISITE (RECORDED, for Kouros's review):** the Shopify Admin token has **NO write/fulfillment scopes** (verified via `access_scopes.json` → none). 2B is **DOUBLE-GATED**: (a) PM/operator standing boundary = no live customer-facing Shopify writes unsupervised, AND (b) the token literally cannot write until the team grants **`write_merchant_managed_fulfillment_orders`** + **`write_fulfillments`** and **reissues** the token. Clean external gate. Do NOT arm the 2B cron until both scopes are granted + token reissued + Kouros reviews.
- **2A SCHEMA — CONFIRMED as designed** (`inv_sales_fulfilment` 1:1 UNIQUE `order_id_fk` + `inv_sales_fulfilment_events` append-only; lazy-create-on-first-action ⇒ absent row = implicit `new`/`idle`; mode-aware lifecycle delivery new→picking→picked→fulfilled / pickup …→ready_for_pickup→picked_up; `shopify_sync_state` ENUM idle/pending/pushed/failed default `'idle'`). **Mig = `322_inv_sales_fulfilment.sql` (322 PM-VERIFIED FREE 2026-06-11: --status 321 applied, Pending 0; re-check at build, parallel sessions live).** Do NOT denormalize `fulfilment_mode` onto the side table — key the advance/revert map on live `inv_sales_orders.fulfilment_mode` at action time (mode is re-classifiable; snapshotting = second store of one fact).
- **FK TYPES — verify live before the migration** (don't trust the brief's recollection): `order_id_fk`→`inv_sales_orders.id` (confirm BIGINT UNSIGNED via SHOW CREATE), `fulfilment_site_id_fk`→`ref_sites.id` (INT UNSIGNED), `*_user_id`→`users.id` (INT UNSIGNED). FK-type mismatch = our #1 migration-failure class.
- **schema_meta — MIRROR THE `ord_*` SIBLINGS, do NOT default to `'allowed'`:** `inv_sales_fulfilment` is the eshop twin of `ord_orders` → copy `ord_orders`'s schema_meta `corrections_policy` exactly; `inv_sales_fulfilment_events` is the twin of `ord_order_status_events` (append-only) → copy that row, NEVER `'allowed'` on an immutable event log. Both `table_class='operational'`; one schema_meta INSERT per table in mig 322; writer hint = "eshop-fulfilment-status.php API + future push-shopify-fulfilment.ts worker".
- **API `eshop-fulfilment-status.php` — CONFIRMED clone of `expeditions-status.php`. ROLE GATE = login-only, NO role check** (PM-verified live: `expeditions-status.php` gates purely on `current_user() !== null` → 403 if not logged in; access control is at the PAGE level via the Expéditions `ref_pages` gate, NOT in the handler). Clone that: CSRF first-gate w/ fresh-token retry hint, ONE txn (event insert + `inv_sales_fulfilment` upsert + `log_revision`), PRG/JSON. **Push-deferral CONFIRMED + load-bearing:** reaching `fulfilled`/`ready_for_pickup`/`picked_up` sets `shopify_sync_state='pending'` and returns — does NOT call Shopify inline. This is why 2A is LIVE-safe pre-scopes (nothing in 2A touches the Shopify API).
- **2A GOTCHAS (flag all to the agent):** (1) lazy-create = `INSERT … ODKU` on UNIQUE `order_id_fk` inside the txn (not SELECT-then-branch race); render treats "no row" as `new`/`idle`, never NULL-crash. (2) **`fulfilment_mode='review'` order has NO lifecycle → handler must REFUSE advance** ("à classer d'abord"), not pick a default map (never-guess applies to lifecycle selection). (3) `data-eshop-order-id` namespacing (Phase-1 convention) — `eshop-fulfilment.js` binds ONLY on it; verify no collision with `expeditions-status.js`'s `data-order-id` delegation. (4) **`log_revision` ENUM has no `'delete'`** — any cancel/abandon tombstones via `action='update'` + an event row, never a delete (and the roadmap lifecycle has no cancel terminal — decide cancel semantics explicitly, don't add a delete path). (5) **DIRTY-SHARED-TREE HAZARD ACUTE** — tree holds foreign session work (financier-data.php, in-flight 168-hunk expeditions.css overhaul, kpi-handlers churn); deploy SURGICALLY (file-scoped scp + diff deploy-delta, `coder` skill "Dirty shared tree"), NOT `bin/deploy.sh --apply`; commit by PATHSPEC; put new chip CSS in a NEW file `eshop-fulfilment.css` to dodge the expeditions.css collision (same dodge as expeditions-sku-history.css).
- **6. CONFIRMED — 2A touches NO `fg_stock_compute()`, creates NO `ord_orders` rows.** XOR preserved; side table invisible to depletion. Agent verification step must grep the diff for `fg_stock_compute`/`fg-stock.php`/`ord_orders` INSERT → ZERO hits. Do NOT read `inv_sales_fulfilment.status` into fg-stock; depletion stays on `inv_sales_orders` membership.
- **EQUIP 2A: `sql + coder + ui + webapp-testing`** (CONFIRMED per matrix; webapp-testing smoke READ-ONLY on prod). New tour card? `inv_sales_fulfilment` adds NO `ref_pages` row (same Expéditions surface) → no tour-steward dispatch needed.

## ✅ FULL SHOPIFY⇄MALTYWEB FULFILMENT BUILD COMPLETE — autonomous run 2026-06-11 (Kouros away; reviews on return)
All phases SHIPPED+LIVE+COMMITTED on respective `main` branches + deployed. Recorded as-built below.

### Phase 0 — shipping-method classification + ingest (maltyweb mig **321**; maltytask ingest `bac5501`)
- `ref_shipping_methods` (16 rows) + `inv_sales_orders.shipping_method_title` / `fulfilment_mode`. Ingest now captures `shipping_lines`, classifies pickup/delivery/review, **line-idempotency bug FIXED** (the §INGEST LINE-IDEMPOTENCY BUG above — pre-existing order now re-evals lines + UPDATEs mutable header), `--until=now` default.
- **LIVE BACKFILL: May+June ingested → 3127 orders total; FG delta −5 units (corrective, recorded).**
- **Distribution: delivery 2587 / pickup 488 / review 66** (review = mostly null-shipping-title orders).
- **15-min ingest cron ARMED — on the LOCAL WSL box over the SSH tunnel (13306), NOT the VPS.** ⚠️ NOT robust (depends on box being on + tunnel up). **FOLLOW-UP RECOMMENDED: VPS-host the ingest.**
- ⚠️ maltytask `bac5501` cosmetically bundled the ingest with 3 unrelated logtrack files from a parallel session (couldn't cleanly split on a concurrently-committed branch; harmless, local).

### Phase 1 — per-order eshop rows (maltytask `e7962ce`)
Per-order eshop rows on Commandes board (customer, SKU pills, 🏬/🚚/⚠ badge), read-only, **NO `ord_orders` twins, FG untouched** (XOR preserved). Same commit also carries the earlier-session cross-date search + wider/larger layout.

### Phase 2A — internal fulfilment tracking, LIVE (maltyweb `5508be1`, mig **322**)
`inv_sales_fulfilment` + `inv_sales_fulfilment_events` side tables; `eshop-fulfilment-status.php` API (login+csrf, mode-aware lifecycle delivery→fulfilled / pickup→picked_up, **review REFUSES advance**, push-trigger sets `shopify_sync_state=pending`, **NO inline Shopify call**); action chips + sync badge on eshop rows; new `eshop-fulfilment.css`/`.js` (dodged the expeditions.css collision per plan). **XOR preserved, fg-stock untouched, fixtures self-cleaned.** Team can mark préparé/prêt/remis NOW with zero Shopify dependency.

### Phase 2B — write-back worker, BUILT + DISARMED (maltytask `a08a718`)
`push-shopify-fulfilment.ts` write-back worker — **DISARMED** (dry-run default, no `--apply` scheduled, no cron) + **DOUBLE-GATED**: token has NO write scopes (GET 403 confirmed). 3 idempotency gates, retry cap, audit. Reverse-reconcile in ingest = INNER JOIN on `inv_sales_fulfilment` (only already-tracked orders, advance-only, never bulk-backfills) → structural no-op until team uses the workflow.
- **🔑 2B ACTIVATION (gated, for Kouros):** team grants `write_merchant_managed_fulfillment_orders` + `write_fulfillments`, reissues the Shopify token, verifies one order with `--apply`, THEN arms a cron. The internal 2A tracking works NOW without this.

### Phase 3c — `is_non_stock` flag, NO-OP infra (maltyweb `f0c57de`, mig **323**)
`ref_skus.is_non_stock` flag + symmetric `JOIN ref_skus rs … AND rs.is_non_stock=0` in BOTH `fg_stock_compute()` AND `fg_stock_location_snapshot()` eshop legs. **Verified NO-OP** (FG total 8003=8003, per-SKU byte-identical; 0 SKUs flagged) — infra ready for T7 tap machines (the §② equipment-must-not-deplete ruling, now at SKU-config level).

### DEFERRED (gated, documented for Kouros)
- **Phase 2B activation** — see 2B gate above.
- **Phase 3a/3b** (review-title self-service admin + pickup/delivery lane split) — gated on team standardizing Shopify shipping-method names (in progress).
- **Phase 4** (cage/box COGS engine) — out of scope, gated on 2026-06-15 cage anchor (separate arc).

### 🔴 NEWLY-SURFACED OPEN DATA-INTEGRITY ISSUE (NOT caused by this work — separate from the Shopify arc)
**Σcards ≠ Σphysique is ALREADY BROKEN by ~22 units on the FG stock page:** `fg_stock_location_snapshot()` sums to **7981** vs `fg_stock_compute()` **8003** (delta 22). git-stash CONFIRMED the gap PREDATES the Phase-3c edit (existed on old VPS code too). PM's standing invariant "Σcards==Σphysique holds" currently does NOT hold. Likely introduced by a recent parallel-session change to one of the two functions (or a same-day-tiebreak drift). **OPEN — needs investigation; recorded for Kouros as a standalone item.** (The §C fg_stock_location_snapshot sales-attribution carryover ~L371 is the prime suspect surface — re-derive both legs and diff.)

### Operational caveats (for the review)
- Phase-0 ingest cron on LOCAL box over tunnel = not robust → recommend VPS-hosting (above).
- maltytask `bac5501` bundled 3 foreign logtrack files (harmless, local).
- **Parallel sessions actively committing to BOTH repos' main branches.** The build did git history surgery once (a reset that briefly undid a parallel commit; restored it). **LESSON (reinforces CHECKLIST): only `git add <my-files> && commit` on these shared branches, NEVER reset.**

### 📋 KOUROS REVIEW SUMMARY — PM flags to raise
1. **OPEN data-integrity bug (top priority):** Σcards 7981 ≠ Σphysique 8003 (Δ22), predates this work — assign investigation; PM's invariant is currently violated.
2. **2B activation needs operator action** (Shopify write-scopes + token reissue) before any live customer-facing write — correctly disarmed until then.
3. **Phase-0 ingest cron is on the LOCAL box** — single point of failure; VPS-host as follow-up (consistent with strangler "consume the host" + maltyweb-prefers-live).
4. **Phase 3a/3b gated on Shopify shipping-name standardization** (team in progress) — 66 orders sit in `review` until then.
5. **Shared-tree commit hygiene held** (file-scoped adds; one reset incident caught + restored) — separate-worktrees-per-session recommendation stands.

## §FG STOCKTAKE → SAISIES SURFACING + month_closed ATTRIBUTION — PM RULING 2026-06-11 (pre-build; triggered by May-2026 COGS close blocked on missing 2026-05 FG anchor)
**Context:** Closing May 2026 COGS. Of the 4 close inputs (RM closing / WIP tank_balances / sales / FG stocktake), only the **FG May-31 stocktake** is missing. Live DB (PM-probed 2026-06-11): `inv_fg_stocktake` active rows = **ONLY `month_closed='2026-06'`** (107 `maltyweb-form` counted 06-08/06-09 + 1 `manual-adjustment`); the **April anchor is GONE** (re-anchored to June) → no 2026-05 row AND no April fallback. Kouros asked: create the FG month-end stocktake and "have it sit on Saisies."

**KEY FINDING — the FG count form ALREADY EXISTS and is the canonical writer.** It is `expeditions.php?view=stocktake` (the 2026-06 rows came from it, `source='maltyweb-form'`). `inv_fg_stocktake` has **ONE writer = `expeditions.php:746` INSERT** (mig 288 generated `row_hash`; every other ref is read-only SELECT). The "absence" is purely **discoverability** — Saisies has 5 cards (packaging/racking/brewing/fermenting + **RM stocktake** `form-rm-stocktake.php`) but NO FG card. FG's count surface was built later inside the Fulfilment arc, so it never got a Saisies card.

**RULING 1 — SURFACE, don't FORK. Add a Saisies card LINKING to `expeditions.php?view=stocktake`. Do NOT factor a `form-fg-stocktake.php`; do NOT migrate the writer.** Factoring the form = a SECOND writer to a single-writer COGS-bearing table = the parallel-store divergence smell. The FG form is entangled with the Expéditions view (per-location anchor, `exp_st_scope_allowed` channel matrix, `display_family` grouping, mig-288 generated-hash UPSERT, manager-backdate, freshness chips) — re-homing is large surface, zero canonical benefit. Saisies is a **card-grid of LINKS (discovery hub)**, not a rule that every count form physically lives there (RM's own module is incidental). The racking split-card already links OUT (`form-racking.php` "Saisie classique →") — same pattern. `inv_fg_stocktake` stays Fulfilment-owned; Saisies gains a 6th card "Inventaire produits finis (PF)" → `/modules/expeditions.php?view=stocktake`. Additive — nothing superseded, no scrapping.

**RULING 2 — CORRECT the operator's premise: RM does NOT auto-attribute early-next-month counts to the prior month.** RM gives an explicit `period` month-picker (`form-rm-stocktake.php` L28-32, default = current month) and the operator CHOOSES `2026-05`. FG instead derives `month_closed = substr(counted_at,0,7)` (`expeditions.php:785`) with NO override. So the real inconsistency = **"FG has no period control; RM does"** — NOT "RM has a −1-month rule." Fix = give FG the SAME operator lever (explicit, default-but-overridable `month_closed` field), NOT a silent "if day ≤ N attribute to prior month" heuristic (= a guessed COGS-impacting attribution; would silently mis-file a legitimately-late June count into May). **Explicit operator choice over silent inference on a COGS key, always.** Note: managers CAN already backdate FG (`$stDefaultCountedAt = $stSelDate` for managers; operators always today) — so the May close is reachable today with `counted_at=2026-05-31`.

**SEQUENCING:**
- **Phase 0 (NO build — unblock May NOW):** Kouros opens `?view=stocktake`, manager-date = `2026-05-31`, enters the May-31 FG census → `month_closed='2026-05'`. ⚠️ **Per-SKU anchor means a PARTIAL May census silently leaves some SKUs anchored elsewhere — enter a COMPLETE May set.** ⚠️ **If no real May-31 physical count exists (only the June-8 one), that is a DATA-AVAILABILITY question — do NOT synthesize a May close from June data; surface it to Kouros for an explicit nearest-count decision (never-guess-COGS line).**
- **Phase 1 (small follow-up build):** Saisies 6th card → `?view=stocktake`. `saisies.php` + `saisies.css` only; NO mig, NO writer/schema change. RULE 3: card links to `expeditions` (already tour-covered) and `saisies` is tour-special-cased — check the tour saisies-chapter form-card map; dispatch tour-steward only if `tour-gap-check.php` flags. **EQUIP: ui + coder + webapp-testing.**
- **Phase 2 (small, deferrable — the durable RM/FG consistency fix):** explicit operator-chosen `month_closed` field on the FG form (default `substr(counted_at,0,7)`, overridable) mirroring RM's period picker — lets an operator count June 1-2 and attribute to May WITHOUT backdating the physical count date. `month_closed` column already exists → NO schema change, populate-path change only. **EQUIP: ui + coder + webapp-testing.**
- Phases 1 & 2 independent; BOTH safe to defer past the May close. **Do NOT gate the May close on either.**

**DIVERGENCE FLAGS:** (1) refuse any `form-fg-stocktake.php` = 2nd writer to single-writer COGS table; (2) no silent prior-month heuristic; (3) partial-May-census trap (per-SKU anchor); (4) don't fabricate a May count from June data.

## Status one-liner
Expéditions v1 LIVE for the team (mig 290, go-live 2026-06-09). PF-stock board + full-box flooring + per-location production attribution + same-day tiebreak + settings-driven prod-site = all SHIPPED. **✅ ITEM 1 SHIPPED 2026-06-09 (mig 299, commits `3c3eb33`/`49a0386`/`edfcf78`):** stock-movement saisie + inter-site transfers + per-site SALES attribution + Nausikraft delivery-site admin — closes the "sales-leg per-location settlement" carryover (attribution; per-site cutoff granularity remains, KNOWN LIMITATION 1). **WeeklyOrders importer: ✅ APPLIED to prod 2026-06-10 — `ord_orders` 12→79, then AUDIT FULLY CLOSED → 4 more written (#101 Street Cellar DGDF×3, #102 Joël Golinucci + new privé id 3058, #103 Alice Buache + new privé id 3059, #107 Département Marketing PD8×1 `line_comment='gratuité 100%'`).** Conventions confirmed: privé = per-individual `is_private=1` rows (Kouros opt A); marketing giveaways = order-attached `ord_orders` line on the Marketing account (id 673, BC 1807) w/ recipient in comment + real SKU/qty (SUPERSEDES side-stock-ledger for order-attached giveaways; rebate via `line_comment`, no price col); `PACKDEC`→`PD8` (id 137). ③ week-scoped-search fix SHIPPED+deployed (`expeditions.php`, find-intent drops the week window + "Expédiées (toutes dates)" + LIMIT 500; NOT committed — parallel tree). 🔴 **BUG RECORDED: `import_weeklyorders.php` ~L665 line INSERT has NO `ON DUPLICATE KEY`/UNIQUE on (order_id_fk,sku_id_fk) → re-run DOUBLES lines** (header idempotent on source_ref, lines NOT — corrects the prior "import re-run is idempotent" guidance; bit us, dup line ids 314-318 deleted). Fix = UNIQUE + ODKU (do NOT apply yet — team must confirm no order legitimately splits a SKU across 2 lines); log_revision has no 'delete' action so deletes unaudited. See §WeeklyOrders AUDIT FULLY CLOSED. `scripts/import_weeklyorders.php` `e57eda5` still local-unpushed. **✅ TASK A SHIPPED 2026-06-10 (commit `2192153`, NO migration):** same-day cutoff fix — eshop leg made SYMMETRIC (compute mirrors snapshot per-(sku,site) `>=`+created_at) + transfer leg = atomic per-row UNIT-GATE (net-zero); closes KNOWN LIMITATION 1 for eshop+transfer; divergent-anchor fixture 10/10. See §TASK A. **✅ MULTI-SKU MOUVEMENT INPUT SHIPPED + LIVE + COMMITTED + PUSHED 2026-06-10 (`?view=mouvements` extended scalar→multi-line; NO mig; single-home preserved = N `inv_stock_movements` rows, NO batch table; all-or-nothing txn + per-row log_revision; net-zero 8599==8599 both sides). COMMITTED IN `badd153` (origin/main) — BUNDLED by a parallel session with the order stock-risk modal + Stock PF header restyle, and UNDER-DESCRIBED (commit msg omits mouvements); handler verified intact in HEAD. — see §MULTI-SKU MOUVEMENT INPUT.** **✅ ITEM 2 (mini side-stock / giveaways) SHIPPED + PUSHED 2026-06-10 (maltyweb `1c95e23`; mig 303 `inv_side_stock_ledger`; UNIT-COUNTS NOT COGS) — see §ITEM 2.** Packaging loose-unit remainder ledger + giveaway entry + admin tombstone + `expeditions.php ?view=side-stock`; mig-265-safe parallel-run attribution; tombstone-then-reinsert edit idempotency (3 lifecycle bugs fixed on review); distinct from sales-side `sale_class='giveaway'`. **🔴 REMAINING ITEM 2 DOWNSTREAM = YEAR-END-SETTLE CRON (documented-not-built):** the beer-tax JOIN shape lives in mig 303 (`year_end_offered` SUM → HL via `ref_skus.hl_per_unit`, no-double-count rationale included), but the year-end-close cron that WRITES the `year_end_offered` draws + feeds beer-tax is NOT YET BUILT. **✅ 2-WEEK OPEN-ORDERS PREDICTIVE BUCKET SHIPPED 2026-06-10 (NO migration; STAGED-not-committed — `app/fg-stock.php` + `public/modules/expeditions.php`, awaiting Kouros):** nested current-week + current+next-week buckets; new per-SKU `open_2wk_qty`/`live_2sem`; cumulative `twowk_qty` in `fg_stock_compute()` Step 7 reusing `fg_stock_iso_week_end()`+7d; GLOBAL+per-SKU only (per-site predictive DROPPED — Kouros); monotonic nesting 0 violations / 52 rows live; total strip + Commandes strip now 4 KPIs. See §2-WEEK OPEN-ORDERS PREDICTIVE BUCKET. **✅ ROLE-BASED DUAL STOCK VISUALS SHIPPED + LIVE + COMMITTED 2026-06-10 (NO migration; data/render only):** each user sees physique total + their home-site physique, home-site DERIVED team→site_type via top-level `exp_user_home_site_type()` (manager_scope first, preset_key fallback — NO new column, ruling honored) + `exp_resolve_home_site()` reading `fg_stock_location_snapshot()` (no re-query); renders on `?view=stock` + `?view=commandes` strip (snapshot call added there); null-guarded admin/all→total+all-sites; LIVE-verified Nathan→Zeubi 5540u / prod→Zgeg 1492u / Olivier→Taproom 446u / Kouros+Arthur→all-sites. B2B expedie fix = clean commit `97e7a7c` (app/fg-stock.php); role visuals landed in OPERATOR'S commit `badd153` (intermixed w/ stock-risk-modal + mouvements-multi-line, not separable). **9 commits unpushed on main** (operator backlog). See §ROLE-BASED DUAL STOCK VISUALS SHIPPED. **✅ PER-LOCATION PHYSIQUE BREAKDOWN in the stock drill-down ledger SHIPPED + LIVE 2026-06-10 (NO mig; render-only `expeditions.php`; SELECTIVE deploy; NOT committed — operator parallel-work tree):** "Répartition par site" sub-block in `exp-stock-ledger` (after `exp-ledger-physique`, before `exp-ledger-sep`); reads existing `$fgLocationSnapshot` (no re-query), `qty != 0` gate w/ `exp-st-neg` negatives, residual écart guard (muted, no throw); DOAF Zgeg8/Zeubi30/Taproom1=39. 2 follow-ups deferred to operator's next expeditions.css deploy (1 inline `opacity:.7` no-inline-CSS violation; markup-only `exp-ledger-loc-*` classes need CSS). See §PER-LOCATION PHYSIQUE BREAKDOWN. **✅ "ACTIVITÉ RÉCENTE" SKU ACTIVITY TIMELINE SHIPPED + LIVE 2026-06-10 (NO mig; render-only `expeditions.php` + NEW `public/css/expeditions-sku-history.css`; SELECTIVE deploy; NOT committed):** 4th drill-down section (below "Répartition par site"), single date-DESC merged feed of Order(↓)/Production(↑)/Mouvement(↔) per SKU; pattern (A) = 3 BULK ROW_NUMBER-window queries (90d, ≤15/SKU, historical-only) bucketed+merged at render — adds exactly 3 queries to the page regardless of SKU count, NO AJAX/endpoint/2nd derivation (R1 honored). Packaging predicates MIRROR `fg_prod_since_anchor()` EXACTLY (run_type<>'cuv' load-bearing) except anchor-cutoff→90d window — PREDICATE-PARITY VERIFIED, R3 production-divergence risk CLOSED. Orders exclude internal (eshop/taproom = separate legs). New css = 100% var()-token / 0 hex / `?v=filemtime`, dodges operator's in-flight 168-hunk expeditions.css overhaul (fold-in candidate when it deploys). DOAF live: SKU 42 shows all 3 types DESC. See §"Activité récente". **✅ FLIPPABLE DRILL-DOWN (Stock | Activité faces) SHIPPED + LIVE + BROWSER-VERIFIED 18/18 2026-06-10 (NO mig; render-only — `expeditions.php` + `expeditions-sku-history.css` + `expeditions-stock.js`; SELECTIVE 3-file deploy; NOT committed — operator parallel-work tree):** `?view=stock` per-SKU drill split into two faces via a `role=tablist` SEGMENTED TOGGLE (not a 3D flip) — Face "Stock" (default: anchor/+/−→Physique→Répartition par site→open/disponible) + Face "Activité" (timeline in its OWN scroll-bounded table, max-h 384px + overflow-y:auto = the uncram fix). Caps raised 15→50 events / 90→180d across all 3 bulk ROW_NUMBER queries + slice (still 3 page-wide queries, pattern (A) intact). JS = ONE delegated tab handler scoped to `closest('.exp-stock-drill')` (mirrors data-loc-id delegation, NO per-row listeners) + `resetDrillFace()` on ALL 3 collapse paths (re-expand starts Stock) + arrow-key nav; row-click guard means tab clicks don't collapse. New CSS classes `.exp-evt-tabs/-tab/-pane/--activite` = ≥44px / focus-ring / aria-selected / 100% var()-token / 0 hex / 0 `.exp-ledger-*`|`.exp-stock-*` redefinitions. a11y = real buttons + role tablist/tab/tabpanel + aria-controls/id + region+tabindex scroll pane. Browser-verified incl. INDEPENDENT per-drill state + reset-on-collapse (Playwright bot viewer→operator→reverted exactly, zero residual). php -l clean, page 200. **✅ NOW COMMITTED = clean 3-file commit `711905c` on main (LOCAL/UNPUSHED), consolidating all 3 drill ships (Répartition + Activité + flippable). PATHSPEC INCIDENT recovered: bare `git commit` swept in operator's staged kpi-handlers/mon-tableau/kpi-charts → `reset --soft HEAD~1` + `git commit -- <3 paths>` → clean; LESSON = always pathspec-form commit on this shared tree.** **🟢 CSS FOLD-IN DEBT NOW CLOSED 2026-06-11 (commit `3e368d2`, pushed; range `b477565..3e368d2` origin/main):** standalone `expeditions-sku-history.css` deleted + rules folded into `expeditions.css` (scoped/commented, var-only, collision-clean), inline `opacity:.7`→`.exp-ledger-loc-qualifier`/`-ecart` classes, dangling `<link>` removed; flip-smoke re-verified 20/22 (2 explained false-fails). HEAD==origin/main, Pending:0, NO open items on the expeditions stock-drill arc. **Realized two-sessions-one-clone hazard during this commit (Financier session's bare `b477565` swept my staged file-delete + pushed a 404-link; my `3e368d2` repaired). STANDING RECOMMENDATION TO OPERATOR: per-session `git worktree`/clone — shared index is the root cause.** See §CSS FOLD-IN CLOSED + §FLIPPABLE DRILL-DOWN. **MIGRATION HEAD = 303 applied** (302 claimed before 303; migs 300/301 sale_class+inv_sales_ledger = sales-commercial-surface arc, separate — `migrate.php --status` is truth). STILL PARKED: ITEM 3 (Phase 2 cage/COGS engine — externally blocked: Shopify pickup-signal ON HOLD, cage anchor count starts Mon 2026-06-15). **DATA pending: operator enters the Nausikraft client list tomorrow via `reglages-generaux.php ?sec=delivery_sites`.**
