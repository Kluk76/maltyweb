# Saisie-forms 5-correction batch — ✅ SHIPPED + LIVE (2026-06-02)

Operator pre-cleared ALL design Qs (batch-clear-then-full-send mode); Kouros built fully
independently, no operator supervision, delegating code to Sonnet agents. PM consulted at
arc-start; this file is the locked decision record + as-found code map + final landing.

---

## 🔴 STANDING OPERATIONAL FACT — operator incremental-entry workflow (applies to ALL saisie/brewing form work, not bug-specific)
> Triggers: "corriger" / "modifier" / "saisie form" / "incremental entry" / "Gonzalo" / "form POST" / "re-submit" / "partial data". Read this BEFORE building OR reviewing ANY saisie/brewing form. Elevated 2026-06-16 from the 1055-hotfix severity note (it is reusable craft, not specific to that bug).

**The fact:** Gonzalo (brewing/saisie operator) does NOT fill a form in one pass. His pattern: create the brewday/saisie **header**, then repeatedly re-open the SAME record via **"Corriger/modifier"**, add a few datapoints, and re-submit — **many times over the life of one brewday.** Consequence at the server: the **entire POST handler — every resolver, every validation, every row-builder/insert/upsert — re-runs IN FULL on every single `corriger` submit, against partial/incomplete data.** There is no "submitted once when complete" moment.

**Two durable consequences for every future form build / review:**
1. **DESIGN — idempotent + partial-tolerant POST path.** Any resolver / validation / insert / upsert in a form's POST path MUST be idempotent and tolerant of partial/incremental data across repeated submits. It must NOT assume the record is complete, must not duplicate rows on re-submit (NK-preserving upsert, not blind INSERT), and must not hard-block on a not-yet-entered field. A bug in ANY POST-path query blocks the operator on EVERY re-save, mid-entry — not just once at the end. (This is exactly why the 1055 `ONLY_FULL_GROUP_BY` regression was a HARD-BLOCK, not an edge case: the recipe resolver runs unconditionally on every `corriger`.)
2. **TESTING — smoke with the corriger LOOP, not a single happy-path submit.** Smoke-test saisie/brewing forms by submitting MULTIPLE times (header → corriger → corriger …), not one complete submit. The dangerous bug class here is "fires on every re-submit / on partial data" — a single-submit smoke can pass while every real re-save fails. Repeated-submit (open → add → re-submit ×N) is the CORRECT smoke for these forms. (Pairs with the webapp-testing default in the build-launch checklist.)

**EQUIP when this surfaces:** ui + coder + sql (+ webapp-testing for the corriger-loop smoke).

## ✅ SHIPPED + LIVE + VERIFIED — universal "Corriger" on past submissions, hard-UPDATE = truth (2026-06-04)
Operator: "add the modification option on past submitted forms, exactly like the other forms; modifications do HARD changes in DB (modified data = truth); make ALL modification modules in ALL forms act that way." **As-built deployed via `bin/deploy.sh --apply`, 4 files lint-clean, NO migration. PM-verified. Working tree UNCOMMITTED (operator commits the session).** Net: all 4 saisie forms now have hard-overwrite edit ("modified row = truth") with correct update-audit; racking was the only gap and it's closed.
- **AS-BUILT — Part A (racking edit):** `public/modules/form-racking.php` gained `?edit=<bd_racking_v2 id>` mode. Entry = a "Corriger" column added to the form's OWN inline recent-submissions table (~L1815; no partial delegation, no new page). Full-form prefill, "Modifier Soutirage" + identity strip, hidden `edit_submitted_at` (strict-regex `Y-m-d H:i:s(.u)` validated) + hidden `edit_id`. NK = `[submitted_at, neb_beer, neb_batch, contract_beer, contract_batch, seq]`; original `submitted_at` preserved → `bd_upsert` UPDATEs in place (no dupe). `racked_vol_hl` RE-DERIVED from the edited flowmeter via the existing COALESCE(end−start, manual) path (mig 258), not passed through. row_hash recomputed from edited POST values via form-racking.php's single live hasher (the 3-block byte-identical footgun did NOT apply — shell path is dead).
- **AS-BUILT — Part B (uniform audit precision, all 4 forms):** the MAIN-upsert edit path now fetches the pre-image and passes it as `$before` to `log_revision` → edits log `action='update'` with real before_json (+ `,correction` appended to audit_flags), across form-racking, form-brewing, form-packaging, and **fermenting-phase-submit** (the `bd_upsert` ~L854, NOT the already-correct DryHop shrink-tombstone call). New submissions keep `$before=null` → `action='insert'`. Confirms the Part-B-CONFIRMED-REAL ruling: `log_revision` derives `$action=($before===null)?'insert':'update'` (app/db-write-helpers.php L248).
- **VERIFICATION (self-cleaning synthetic edits per form, ALL PASSED):** UPDATE-in-place (id stable, NK preserved, zero dupes); `action='update'` + non-null before_json; row_hash changed/consistent; racking flowmeter re-derive (129.5−100=29.5, end<start refused, manual fallback null); a fresh-`submitted_at` control proved the preserve-guard. All synthetic data + their audit rows DELETEd same session. No helper scripts left; no git commit.
- **CIP-GATE LANDMINE — independently confirmed NON-BLOCKING (no action needed):** `racking-form.js` gates submit on `cipMachineActive()` (≥1 machine CIP checkbox). `app/partials/cip-section.php` re-checks those machine checkboxes from the `existing` CIP events (`$anchorIsDone/$isDone/$exInline` → checked), and `cip_events_for('racking',id)` returns the 5-machine structure for every racking (incl. backfills) with done-state → editing a normally-submitted racking does NOT trip the gate. Confirmed for the real use case.
- **No migration** (Part A/B add no columns). Structure now matches brewing/packaging exactly — the corrected-as-built finding stands: live racking writer is monolithic `form-racking.php`, the `partials/racking-phase-*` + `racking-phase-submit.php` two-phase shell is dead (session_id_fk NULL on all 404 rows). The reusable mechanics live in §SAISIE EDIT-MODE CORRECTION PATTERN below.

## ✅ FINAL STATE — ALL 5 ITEMS SHIPPED + LIVE + PM-VERIFIED (2026-06-02)
- **4 commits on maltyweb main, COMMITTED-NOT-PUSHED (local ahead of origin/main by 4):**
  - `68ae0b4` #2 racking flowmeter (mig 258).
  - `039d4e3` shared module `public/js/multi-submit-reads.js` + #4 turbidity (mode=average).
  - `b42e8b7` #3 in-filling CO₂/O₂ refactored to module serialize mode.
  - `619b422` #5 all 17 per-format pertes → SUM widgets + #1 qa_analyses auto-derive.
- **Mig 258 APPLIED LIVE** (`258_racking_flowmeter_counter.sql`, schema_migrations 2026-06-02 18:14). PM-verified on VPS: `bd_racking_v2.flowmeter_start_hl` + `flowmeter_end_hl` both `decimal(8,1)` NULL=YES. Only schema change in the batch.
- **Shared module shipped:** `public/js/multi-submit-reads.js` (vanilla IIFE; modes serialize / sum / average; min-1; contiguous re-index; blank-excluded aggregate; pre-submit sync; `destroy()` fixed) + `msr-` scoped CSS (NOT inline — house rule honored).
- **Item #1 reverses the 2026-06-01 never-derive ruling PER OPERATOR** (deliberate, recorded). `qa_analyses_units` now auto-derived read-only = count of in-filling reads, MAIN-row only (parallel rows = 0). Server change one line at `form-packaging.php:723`; compute path untouched; FORWARD-ONLY (~966 historical hand-entered rows unchanged).
- **Verification (Kouros):** `php -l` clean all changed PHP; `node --check` clean all JS; headless module test (avg=0.6, sum 3+5+2=10, blank-excluded, serialize re-index contiguous); compute INVARIANT TEST 13/13 PASS (vendable_hl / beer_tax_base_hl / loss_kpi_hl byte-unchanged — the COGS/tax gate for #5+#1 held); both forms clean 302 unauth, zero php-fpm errors; VPS checksums match local; ownership `maltytask:www-data 644`.
- **PLACEMENT DECISION (recorded):** flowmeter_start captured on racking **in_progress** phase (not the literal firewall 'start' phase, which doesn't write to `bd_racking_v2`); flowmeter_end on end phase (end UPDATE recomputes `racked_vol_hl` via COALESCE: derived end−start when both present, manual fallback otherwise, negative refused). 3 `$hashCols` blocks updated identically (form-racking.php + in_progress + end). If operator later wants it literally on the start screen, that's a follow-up.
- **NOT YET DONE (operator-dependent, the only open thread):** real authenticated round-trip submit on each form — operator enters reads/pertes/flowmeter and we confirm the DB rows land. This is first-use confirmation, not a build gap.
- **PUSH PENDING:** 4 commits not yet pushed to origin (operator hasn't asked — house rule: push only on request).
- **NOTE (not a divergence):** untracked `app/packaging-loss-types.php` in the working tree belongs to the PAUSED A-LT data-driven-loss-types arc (codegen for `v_bd_packaging_v2_vendable` from `ref_packaging_loss_types`), referenced by nothing in HEAD — leftover scaffolding from that separate arc, not part of this batch.

## Migration number
- **Next free = 258.** Verified 2026-06-02 against live `schema_migrations` (max applied = `257_backfill_historical_intank_contract_lane.sql`, 2026-06-01 14:02) AND repo `db/migrations/` (highest file = 257; note a benign DOUBLE-252 already in repo+applied — `252_create_inv_rm_stocktake_lines.sql` + `252_cuve_late_may_2026_data_correction.sql`; not our concern). Only item #2 needs a migration. Re-run `php scripts/migrate.php --status` at build-start per pre-flight rule (drift caught every time historically).

## LOCKED DECISIONS

### Item #1 — QA "mesures pertes" qa_analyses_units AUTO-DERIVE (reverses 2026-06-01 never-derive ruling; operator reopened)
- 1:1: each O2/CO2 read row = 1 full unit destroyed (full BOM: liquid+label+container+cap). `qa_analyses_units` = count of read rows.
- Counts ANY read row entered (even CO2-only or O2-only); bottle sacrificed regardless.
- FORWARD-ONLY: ~966 hand-entered rows UNTOUCHED. Auto-derive on new/edited sessions only.
- FULLY LOCKED: read-only, NO operator/admin override. Always == read-row count.
- It's a BUILD not a regression-fix: field is currently plain manual (`$fQaAnalyses` from `$f['qa_analyses_units']`), no auto-sum wired.
- **🐛 FOLLOW-ON BUG (pre-existing in this same item, surfaced 2026-06-05 during the WL round-trip; FIXED).** The read-only `qa_analyses_units` auto-derive (count of in-filling CO₂/O₂ read pairs) did NOT recompute after a DRAFT RESTORE. Root: FormFramework `loadDraft` sets input values WITHOUT dispatching `change`/`input` events, and the only post-restore `syncQaAnalysesDisplay()` call was gated inside the edit-mode block → on a draft restore the display kept the stale (usually 0) count. **Fix: call `syncQaAnalysesDisplay()` (a) after `FormFramework.init()` and (b) on the co2o2-mount `input` event.** Shipped in commit `619b422` ("qa_analyses auto-derive #1") — it was live BEFORE the WL deploy; operator caught the gap during WL testing. General lesson for derived-readonly fields: any auto-derived display MUST be resynced after a programmatic value-set (draft restore / edit-load), because framework setters don't fire DOM events.

### Item #2 — Racking flowmeter (NEEDS MIG 258)
- ADDITIVE: `racked_vol_hl = flowmeter_end − flowmeter_start` (read-only) WHEN both present; manual `racked_vol_hl` kept as FALLBACK when a reading is missing.
- Store BOTH raw start+end (immutable event facts) AND the derived delta.
- Counter xxxxx.x hl (max 99999.9), never resets/wraps mid-session; still guard+flag negative deltas.
- Cols: `flowmeter_start_hl` / `flowmeter_end_hl` `DECIMAL(8,1)` on `bd_racking_v2`. Add to `$hashCols` in BOTH paths. Wire into legacy `form-racking.php` AND shell `racking-phase-submit.php` (start/end partials).

### Item #3 — O2/CO2 reads multi-submit module
- Reusable module (like RM inventory) but BATCHED (rows held in form, written on submit) on the STANDARD packaging form — because `packaging_v2_id` doesn't exist until submit.
- FORWARD-COMPATIBLE so it drops into the packaging daily-shell when pilot 5 is built. Packaging daily-shell does NOT exist yet (confirmed) — wire on standard form now.
- Populates EXISTING `bd_packaging_readings` (no new reads DB).

### Item #4 — Turbidity multi-submit on racking
- Same module, min 1 input, computes SESSION AVERAGE.
- Store ONLY the average into existing `bd_racking_v2.avg_turbidity`. NO child table, NO migration.

### Item #5 — ALL pertes multi-submit
- ALL pertes/disposition fields → multi-entry SUMMED (bottle/can unit losses, keg litre losses, QA/library — everything) EXCEPT `qa_analyses_units` (now auto-derived per #1).
- min 1 input. Stored value = SUM of entries.
- MUST produce IDENTICAL stored numbers AND identical downstream vendable_hl / beer_tax_base_hl / loss_kpi_hl as typing the total directly — only entry UX changes, never the computed result.

## AS-FOUND CODE MAP (verified 2026-06-02)

### Packaging — `public/modules/form-packaging.php`
- **O2/CO2 multi-read ALREADY BUILT.** POST shape `co2o2[N][co2|o2]`, up to 20 pairs, batched, fully-blank rows skipped (`$co2o2Pairs`, lines 386-408). Written to `bd_packaging_readings` via idempotent DELETE-then-INSERT keyed on `$mainPackagingId` (lines 1119-1140, cols `(packaging_id, packaging_v2_id, reading_idx, o2, co2)`). JS UI ALREADY BUILT in `public/js/packaging-form.js` (`pf-co2o2-list`, `pf-add-co2o2`, add/remove rows, `MAX_CO2O2_ROWS` cap). ⇒ **Item #3 is LARGELY ALREADY DONE** — verify/harden the existing module + factor it into the shared module shape, don't build from scratch.
- **qa_analyses_units write path:** `$fQaAnalyses = $f['qa_analyses_units']` (line 723), stored on row (line 893) AND fed into `compute_packaging_vendable_hl()` partialRow (line 817) where it is **SUBTRACTED from vendable_units** (line 264) ⇒ directly drives `vendable_hl`/`beer_tax_base_hl`/`loss_kpi_hl`. So item #1 auto-derive must replace the SOURCE of `$fQaAnalyses` with the read-row count, leaving the compute untouched.
- **KEY ARCHITECTURAL TENSION (item #1):** `co2o2Pairs` are SESSION-LEVEL, keyed to `$mainPackagingId` (the MAIN format row; line 1052-1053). But `qa_analyses_units` is stored PER-FORMAT-ROW. ⇒ auto-derived count can only correctly attach to the MAIN row. Parallel/format rows must get qa_analyses_units=0 (or NULL) under auto-derive — NOT a copy of the session count (would multi-count the sacrificed bottles). This is the one non-trivial design call in #1; the read=destroyed-bottle is a single physical event tied to the main run.
- Edit-mode re-seed: `$pfStickyInFilling` loads existing readings keyed on main row id (line 1260, 1399-1412) — must round-trip through the shared module on edit.

### Racking — `public/modules/form-racking.php` + `public/api/racking-phase-submit.php`
- NO flowmeter cols exist yet (grep clean). `avg_turbidity` ALREADY EXISTS (`post_decimal`, line 155/417, in `$hashCols`). `racked_vol_hl` is manual `post_decimal` (line 152/415).
- **`$hashCols` is DUPLICATED** in form-racking.php (lines 365-385) AND racking-phase-submit.php (in_progress lines 292-310 + end-phase lines 472+). Comment in submit handler: "must use same canonical order as form-racking.php $hashCols". ⇒ **flowmeter cols MUST be added to ALL THREE hashCols blocks in the SAME position** or in_progress/end/legacy row-hashes diverge → bd_upsert idempotency breaks (phantom duplicate rows). This is the #1 footgun for item #2.
- Shell racking partials exist: `public/modules/partials/racking-phase-start.php` / `racking-phase-end.php` / `-in-progress.php` / `-recent.php`; submit handler `public/api/racking-phase-submit.php` (in_progress INSERT, end UPDATE). Flowmeter inputs: start reading at phase-start, end reading at phase-end.

## SHARED MODULE
- `public/js/multi-submit-reads.js`, config-driven modes: (a) batched-serialize (#3 co2o2 → co2o2[N][...]), (b) running-sum (#5 pertes → single hidden total OR sum on submit), (c) running-average (#4 turbidity → single avg). CSS in `public/css`. The existing `pf-co2o2-*` JS is the de-facto v0 of mode (a) — generalize from it. SHIPPED 2026-06-02 (commit 039d4e3), proven on #4 turbidity (avg mode, headless smoke passed). #2 flowmeter shipped mig 258 / commit 68ae0b4.

## PROGRESS 2026-06-02
- #2 (flowmeter) LANDED+LIVE: mig 258, all 3 $hashCols blocks, in_progress captures start / end captures end + recomputes via COALESCE. commit 68ae0b4.
- shared module + #4 (turbidity avg) LANDED+LIVE: commit 039d4e3.
- REMAINING: #3 (co2o2 → serialize), #5 (all pertes → sum), #1 (qa_analyses auto-derive). Sequence stays #3 → #5 → #1.

## CRITICAL CORRECTION — TWO co2/o2 subsystems on packaging-form.js (memory was under-described)
The packaging form has **TWO PHYSICALLY DISTINCT co2/o2 paths** that #3 must NOT conflate (this is the in-tank vs in-filling split, per the CO₂/O₂ dimension-vs-loss ruling):
1. **IN-TANK gate read** (single pair, NOT the multi-read module): `pf-tank-co2`/`pf-tank-o2` inputs; resolveTankRead/setTankReadInheritMode/clearTankReadInheritMode/updateTankReadState (JS ~906-1014). Writes to `bd_tank_readings` (Step A, ~`$tankReadMode==='own'`), FK'd via `tank_read_id_fk`. The override block `pf-co2o2-override-block` + `pf-tank-reading-override-checkbox`/`-reason-row` belongs to THIS subsystem (the inherit/override gate, JS 1016-1034), DESPITE the misleading `co2o2` id. #3 MUST NOT TOUCH this.
2. **IN-FILLING multi-reads** (the actual #3 target): `.pf-co2o2-row` / `co2o2_N_co2` / `co2o2_N_o2`, addCo2O2Row, MAX_CO2O2_ROWS, seeded 3 rows. POST `co2o2[N][co2|o2]` → `$co2o2Pairs` (PHP 386-408) → Step D write to `bd_packaging_readings` (PHP 1119-1140) keyed on `$mainPackagingId`. This is the loss-lane.
3. **INVERSION GUARD** (JS 1036-1110): submit-listener over `.pf-co2o2-row`; co2Flag/o2Flag mirror `db-write-helpers.php bd_qc_flag` (co2 outlier <2.5||>6.0, elevated <3.5||>5.0; o2 outlier >200, elevated ≥50); if swapped severity < as-entered → window.confirm → `co2o2InversionConfirmed=true` + programmatic form.submit() bypass. Operates ONLY on in-filling rows. #3 MUST PRESERVE: re-implement as rowValidator/onSubmit hook, keep the confirm-don't-block + bypass-flag + focus-first-inverted behavior byte-identical.
4. **Edit-mode re-seed** (PF_EDIT_STICKY_FILLING = in-filling pairs array; PF_EDIT_STICKY_TANK = single in-tank pair). In-filling round-trips through #3's module; in-tank stays on the gate subsystem.

## ITEM #1 auto-derive — EXACT insertion point (verified 2026-06-02)
- `$fQaAnalyses` set per-format-row at PHP **line 723** (`$f['qa_analyses_units']`). Consumed by `compute_packaging_vendable_hl` partialRow (line ~817) and stored on row (line ~893). In the compute fn (bottle/can branch ONLY, lines 240-300) it is `bcsub`-tracted from vendableUnits (line 264). KEG/CUV branch does NOT use qa_analyses at all.
- Auto-derive plan: `$co2o2Pairs` is already parsed at line 386-408 (BEFORE the format loop at 657). So `count($co2o2Pairs)` is available when building each row. Override `$fQaAnalyses` = `count($co2o2Pairs)` ONLY for the MAIN row (`$fOrigin==='main'`), force `0`/null for parallel. This must be applied at BOTH the partialRow build (line ~817, so the compute sees it) AND the stored row (line ~893) — same value, both places, or stored ≠ computed-input.
- $mainPackagingId is the row id of the row with `origin==='main'`, assigned in the write loop at line 1052-1053 — but that's POST-compute, too late for #1. The main/parallel discriminant available at row-build time is `$fOrigin` (read from `formats[N][row_origin]`, validated ∈ {main,parallel} at line 734). USE `$fOrigin==='main'`, NOT $mainPackagingId, to gate the auto-derive.
- Counts ANY blank-excluded read pair (CO2-only or O2-only still counts — match #1 ruling). `$co2o2Pairs` already skips fully-blank rows (386-408), so `count()` is correct IF a CO2-only/O2-only row survives that filter — VERIFY the filter keeps half-filled pairs before wiring (footgun: if the parse drops a row missing one value, the count under-reports the sacrificed bottles).

## ITEM #5 — COMPLETE pertes inventory (verified PHP 707-733)
Multi-entry SUM candidates (operator: ALL except qa_analyses_units). Two groups by COGS/tax role:
- **BEER-disposition (FEED compute_packaging_vendable_hl → COGS/tax-critical, INVARIANT TEST mandatory):** `unsaleable_units`, `loss_uncapped_units`, `loss_half_filled_units` (counts ×0.5!), `loss_untaxed_full_units`, `qa_library_units`, `loss_liquid_other_units`; keg/cuv: `loss_keg_liquid_l`, `taproom_keg_l`. These are the ones the compute fn reads (240-300).
- **MATERIAL-scrap (stored, NOT subtracted from vendable — decision 6):** `loss_4pack_btl_units`, `loss_4pack_can_units`, `loss_wrap_btl_units`, `loss_wrap_can_units`, `loss_label_btl_units`, `loss_keg_collar_units`, `loss_crown_cork_units`, `loss_can_lid_units`, `loss_keg_save_units`, `loss_container_btl_units`, `loss_container_can_units`. Still multi-entry SUM (operator said all), but NO compute impact → lower blast radius.
- DO NOT make multi-entry: `prod_total_units` / `qte_unites` (production counts, not pertes), `qa_analyses_units` (now auto-derived #1). No rate/percentage fields exist among pertes — all are integer unit counts except `loss_keg_liquid_l`/`taproom_keg_l`/`loss_liquid_other_units` (litres/decimal) which still SUM cleanly.
- INVARIANT: the summed stored value must be byte-identical to direct-total entry. `loss_half_filled_units` ×0.5 happens INSIDE compute (line 261) — sum the RAW count, never pre-halve in the widget.

## BUILD SEQUENCE (confirmed)
#2 (flowmeter, isolated, needs mig 258) → shared module proven on #4 (turbidity, no mig, simplest sink) → #3 (co2o2, mostly refactor of existing) → #5 (all pertes, COGS/tax-critical, biggest blast radius) → #1 LAST (auto-derive, depends on #3's read module being final + is COGS/tax-critical).

## GATES
- RULE-2 review hard-gate before EACH lands (operator unsupervised ⇒ non-negotiable).
- #5 + #1 are COGS/beer-tax-impacting ⇒ INVARIANT TEST mandatory: prove stored vendable_hl/beer_tax_base_hl/loss_kpi_hl byte-identical to direct-total entry on a spread of fixtures (keg/bot/can/cuv + parallel), then DELETE fixtures same session (test-fixtures-must-self-clean).
- No self-commit; no helper scripts left in repo; CSS/JS separated; targeted rsync deploy.

## §SAISIE EDIT-MODE CORRECTION PATTERN — `?edit=<id>` (proven cross-form: brewing + packaging + fermenting + RACKING — ALL 4 forms shipped 2026-06-04, working tree uncommitted)
The reusable "Corriger une saisie" arc — re-open a past `bd_*_v2` row, prefill the form, re-POST through the SAME idempotent handler (NO new endpoint). Now proven on ALL FOUR saisie forms (racking closed the last gap 2026-06-04). Canonical mechanics (honor on every future form's edit add):
- **Entry:** per-row "Corriger" link/column on the recent-submissions surface → `?edit=<row id>`. For brewing/packaging/fermenting this is the `*-phase-recent.php` partial; for racking it is the form's OWN inline recent table (~L1815) since the monolithic form has no recent partial. The module page (`form-<x>.php`) does GET detection `$editId`/`$editMode`, loads the row by PK with `is_tombstoned=0`, builds `$prefillEdit` + `$editBanner`, and **captures the original `submitted_at`** into a hidden field (`edit_submitted_at`) + hidden `edit_id`.
- **THE LOAD-BEARING INVARIANT — preserve `submitted_at`:** the handler sets `$submittedAt = $isEditMode ? $editSubmittedAt : date('Y-m-d H:i:s.u')`. Original `submitted_at` is PART OF THE NATURAL KEY → preserving it makes `bd_upsert` UPDATE-in-place. A fresh `date()` in edit mode = the bug: it changes the NK → INSERTs a DUPLICATE instead of correcting. **Always regex-validate `edit_submitted_at` (strict `Y-m-d H:i:s.u` shape) before trusting it** — it's a hidden field, an attacker/garbage value must not poison the NK.
- **⚠️ NK-FIELD EDIT ≠ in-place correction under `bd_upsert` (RULED 2026-06-04, packaging `?edit=6737` ZEP210→211).** Preserving `submitted_at` only guarantees UPDATE-in-place when the OTHER NK cols are unchanged. `bd_upsert` is `INSERT … ON DUPLICATE KEY UPDATE` keyed on `uq_natural_key` AND `uq_row_hash`. Editing ANY NK field — `neb_batch`, `neb_beer`, `nebuleuse_format_suffix` (pkg), `seq` (racking) — yields a NK that collides with NEITHER unique key → MySQL **INSERTs a new row, leaves the original orphaned = DUPLICATE, not a correction.** (`bd_packaging_v2` NK = submitted_at,neb_beer,neb_batch,contract_beer,contract_batch,row_origin,nebuleuse_format_suffix; `bd_racking_v2` NK = …,seq. Both include neb_batch.) All 4 forms share this latent flaw; today they're safe ONLY because identity is card/hidden-field-driven and the operator can't freely retype it. **To actually correct an NK field: UPDATE BY PK (`edit_id`), NOT upsert-by-NK** — `UPDATE bd_*_v2 SET … (incl. new NK value + recomputed row_hash) WHERE id=:edit_id`, GUARDED by a pre-check that the new NK doesn't already exist on a DIFFERENT id (refuse with flash if it does; never silent-merge/dupe). This is a PHP change → an NK-field correction is NEVER JS-only. The pre-image audit lookup (pkg L1056, keyed on submitted_at+row_origin+suffix, NOT neb_batch) ALSO mis-snapshots under an NK-field edit: it finds the OLD row, logs `update` w/ old before_json against what is actually a fresh INSERT → incoherent audit. Fix the audit path in lockstep with the PK-UPDATE.
- **Closed-session lock (daily-shell forms):** the edit path loads the orig row's `session_id_fk`. If session-linked AND `op_sessions.status != 'open'` → REJECT ("correction verrouillée") — do NOT reopen a closed session. If `session_id_fk IS NULL` (legacy/backfill rows) → proceed UNLOCKED. Corrections never advance lifecycle: `session_advance_phase` is SKIPPED in edit mode (a correction is not a new event/step).
- **Firewall SKIPPED in edit render** (the start-phase eligibility gate is about *new* events; a correction to a past event must not be blocked by current vessel/cadence state).
- **Edit render shape:** banner + a NON-editable identity strip (the NK fields the operator must not change) + an editable `event_date` + the prefilled readings/dispositions. `auditFlags` gains `,correction` (e.g. `'web_entry,session_event,correction'`); `log_revision` is inherited from the handler.
- **VERIFICATION RECIPE (synthetic, self-cleaning — proven on fermenting):** insert a synthetic row, run the edit path twice: (1) preserved-submitted_at path → asserts exactly **1 row UPDATEd** (id stable, only the edited field changed); (2) deliberately-wrong fresh-submitted_at path → asserts **2 rows** (duplication = demonstrates the guard's necessity); then DELETE the synthetic rows (assert COUNT=0). ZERO real-data mutation. This is the standard proof the preserve-submitted_at guard holds before declaring done.
- **`event_date` has NO COGS/occupancy impact** on the bd_*_v2 family (the occupancy/cost anchors are elsewhere) → editing it in a correction is safe. `corrections_policy=allowed` for `bd_fermenting_v2` (check `schema_meta.corrections_policy` per table before enabling edit on a new form).
- **⚠️ EDIT PREFILL MUST NOT HYDRATE FROM THE LIVE CANDIDATE PICKER (RULED 2026-06-04, pkg `?edit=6737`; same lesson as racking edit).** In edit mode, identity (`neb_beer/neb_batch/contract_*/recipe_id`) + the SKU mosaic/format grid come from the STORED sticky payload (`PF_EDIT_STICKY_HEADER`), NEVER from a live tank-card's `dataset`. **Packaging bug:** `packaging-form.js` edit-restore (~L1244-1281) called `selectTank(foundCard)` when the stored source tank was still a live candidate; `selectTank` (L200) OVERWRITES `fldNebBeer/fldNebBatch/fldRecipeId` + re-renders the mosaic from `card.dataset` = the tank's CURRENT occupant. Because a NEWER lot was racked into that BBT, the card carried the new batch → editing the old row hydrated as the new lot, clobbering stored identity + format data (a COGS-bearing lot→SKU→BOM fact). FIX: delete the `selectTank` branch in edit mode; always run the direct-populate path (stored values win); a found card is visual-only (`--selected` class, no dataset read); **explicitly call `renderSkuMosaic(stickyH.recipe_id_fk)` in that path** (it was only ever called by `selectTank`) so the mosaic shows the STORED recipe BEFORE the format-restore loop. Same discipline for the `hors_process` round-trip (already correct: set hidden value, do NOT dispatch the checkbox change handler — that swaps the candidate list).
- **⚠️ PACKAGING FORMAT-RESTORE LANDMINE — hidden `formats[i][format_id]` is NOT restored (RULED 2026-06-04).** `applySkuTile` sets run_type + format_suffix + hidden `format_id` (mosaic L191) but the edit-restore loop (L1372+) sets only run_type/format_suffix/`reuses_packaging_id_fk`, leaving hidden `format_id` empty → a plain re-save can silently NULL the format→SKU binding (MORE dangerous than the identity clobber: silent). Either add `format_id` to `pfStickyFormats` + restore it, OR confirm the handler re-derives format_id server-side from (recipe+suffix). Verify the hidden field is non-empty in the browser before declaring done. (Suffix `<select>` options come from the static `window.FORMAT_SUFFIXES` global, NOT the mosaic — so suffix restore is mosaic-independent; cuv 'V' + `reuses_packaging_id_fk` ARE handled by the loop.) Skills for any edit-prefill fix that touches NK semantics: `ui`+`coder`+`sql` (sql NOT optional — NK/upsert/PK-update is the sql skill's corrective-upsert NK-preservation anti-pattern).
- **IN-PLACE CONVERSION sub-pattern (new 2026-06-03, fermenting cold-crash):** a correction can also CONVERT an event's type in place (e.g. non-ColdCrash → ColdCrash via a checkbox), not just edit fields. Mechanics when it does: (a) **single transaction, UPDATE-then-advance** — `beginTransaction → UPDATE the bd_*_v2 row in place (id stable, recomputed row_hash, NK preserved) → session_advance_phase(...) → commit`; catch rolls back. Order matters: a failed UPDATE after an advance would strand the session in the new phase with the un-converted row. (b) **terminal advance is value-driven on the WRITTEN event_type**, gated `in_array($phase,['start','in_progress'])` so it's idempotent; never trust a client "terminal" flag. (c) **duplicate/row_hash collision (1062) → flash + rollback**, never a partial write. (d) **provenance preserved:** APPEND the conversion markers to the row's ORIGINAL `audit_flags` (dedup), never overwrite with the `web_entry` default — a converted migrated/backfill row keeps its true origin. (e) an already-converted row renders the control TICKED + LOCKED (no un-convert).
- **POST-SUBMIT REDIRECT CONVENTION — ✅ SETTLED + SHIPPED+LIVE 2026-06-03 (supersedes the earlier "default stays on-lot" note).** Canonical rule across session forms: **on SUCCESS, return to the bare cards page** (the lot-selector view); **on ERROR/exception, stay on the lot form** (so the operator can fix). The earlier "default redirect stays on-lot" framing was WRONG for success — landing the operator back on the same lot's form after a good submit is the bug (operator re-enters the same lot). FIX (fermenting was the sole offender): `public/api/fermenting-phase-submit.php` added `$redirectSuccess = '/modules/form-fermenting.php';` (bare, `ff_phase='none'` cards), used at the 2 success points (general success ~L811, cold-crash conversion success ~L426); error/exception paths keep `$redirectBase` (carries `?beer&batch`). **`flash_render()` sits ABOVE the phase dispatcher** so the 'ok' flash shows on the cards view. VPS lint clean, deployed.
  - **CROSS-FORM AUDIT (operator's "fix on all forms" instruction — DONE, fermenting was the only offender):** Brewing (`form-brewing.php` self-post) already redirects bare to `/modules/form-brewing.php` ✓. Packaging (`form-packaging.php` self-post) already redirects bare to `/modules/form-packaging.php` ✓. **Racking** (`api/racking-phase-submit.php` JSON + `session-framework.js` fetch) is an AJAX terminal one-shot — `window.location.reload()` after advance_phase; the racked lot drops off candidates on reload, so it does NOT exhibit the dump-back-on-same-lot problem → **left untouched (correct, not a debt).** This RETIRES the prior "racking parity debt" note: racking needs no mirror because its AJAX reload model already lands the operator on a fresh candidate list.

## ✅ Cold Crash UX move — checkbox + in-place-conversion arc — SHIPPED + LIVE 2026-06-03 (working tree UNCOMMITTED, operator commits)
**LANDED.** Deployed to VPS, `php -l` clean, error log clean. The checkbox-instead-of-dropdown ruling below is now as-built; ALSO landed a NON-PLANNED in-place CONVERSION path in the CORRECTION form (the standout of the arc). As-built:
- **Cold Crash removed from BOTH event-type dropdowns** (operator did the selector dropdown, build did the form dropdown). Recurring events Reads/DryHop/Purge remain in the `<select>`.
- **New-entry form: checkbox "Cold Crash — termine la fermentation"** → server maps `cold_crash_flag=1` → persists `event_type='ColdCrash'` (ONE row, carries readings). Retired the broken "Terminer fermentation" dual-CTA (`ferm-btn-terminate`).
- **Correction form: same checkbox = IN-PLACE CONVERSION (the new mechanic).** Ticking on a non-ColdCrash row → in-place UPDATE conversion: `bd_fermenting_v2` UPDATEd (id STABLE, recomputed row_hash, NK preserved, NO new row), then terminal session advance. Already-ColdCrash rows render TICKED + LOCKED (no un-terminate).
- **THREE GUARDRAILS verified in deployed code (PM-blessed at arc-start, code-confirmed at landing):**
  1. **Single transaction, UPDATE-then-advance:** `beginTransaction → UPDATE bd_fermenting_v2 (in-place, id stable) → session_advance_phase → commit`, in that order. Catch rolls back + surfaces error. (Never advance-then-update — a failed UPDATE after an advance would leave the session terminal with the un-converted row.)
  2. **Advance handles in_progress→end:** `if (in_array($sessionPhase,['start','in_progress'])) session_advance_phase($pdo,$sid,'end',...)` — value-driven on the conversion writing `event_type='ColdCrash'`. Non-conversion corrections still SKIP phase-advance (a correction is not a new event).
  3. **Duplicate/row_hash collision surfaces to UI:** catch maps code 1062 → flash "Un cold crash existe déjà pour ce lot/date — conversion impossible"; rollback, no partial write.
- **AUDIT-PROVENANCE FIX (PM note, applied):** the cold-crash markers (`correction,cold_crash_conversion`) are now APPENDED to the row's ORIGINAL `audit_flags` (with dedup), NOT a hardcoded `'web_entry,session_event,correction,cold_crash_conversion'`. So a converted migrated/backfill row keeps its true origin instead of being mislabeled `web_entry`. **DURABLE RULE: a conversion/correction must PRESERVE the row's provenance flags, only ADD its markers — never overwrite origin with the web-entry default.**
- **Server discipline:** server maps `cold_crash_flag=1` → `event_type='ColdCrash'` in BOTH new-entry and conversion; downstream terminal/garde/racking logic UNCHANGED (keys on the persisted ENUM value + date, never the client flag).
- **VERIFICATION (self-cleaning synthetic, all passed):** new-entry (Reads+flag → ColdCrash row); conversion (id stable, event_type flipped, count stays 1, NK unchanged); duplicate-key guard (1062 → clean error, rollback); session advance (in_progress→end). Real ZEP 213 row **13362 NOT touched** — operator converts it live (open Corriger on 13362, tick Cold Crash, save). `event_date` correction also confirmed working (Stirling 171 corrected by operator earlier).
- **OPEN FOLLOW-UPS from this session (both DEFERRED, tracked):** (1) racking parity for the "← Choisir un autre lot" return-to-cards link (still fermenting-only — see EDIT-MODE PATTERN parity-debt below); (2) Node `parse-tank-simulation.ts` occupancy-keying port (COGS-side) — the tank-sim occupancy-keying lesson must port to the maltytask Node WIP path or PHP-UI vs Node-WIP occupancy diverges for reused batch numbers (COGS-impacting).

### Original ruling (now as-built above) — checkbox instead of dropdown event-type (BLESSED 2026-06-03)
Operator request: remove `ColdCrash` from the fermenting event-type `<select>` (leaving Reads/DryHop/Purge = recurring events only); express cold-crash as a dedicated, clearly-labelled checkbox/toggle so teams understand it's terminal. PM RULING (skills `ui`+`coder`, **no sql** — write path UNCHANGED):
- **DATA MODEL UNTOUCHED.** The persisted fact stays `bd_fermenting_v2.event_type='ColdCrash'`. That ENUM value is the SOT the whole downstream chain keys on: NotColdCrashed/garde gate, session phase→'end' advance, racking "validé-pour-racking" eligibility, and the selector's "hide already-cold-crashed lots" exclusion. None of them inspect HOW the UI captured it → decoupling the affordance from the dropdown breaks nothing. The dropdown was only the affordance, never the SOT.
- **ONE row, not two.** Today's JS section-map `ColdCrash:['section-readings','section-coldcrash']` already writes ONE ColdCrash row carrying the readings (cold-crash temp 0–4°C on that row). PRESERVE THAT. Checkbox ticked → sets hidden `event_type=ColdCrash`, reveals coldcrash section + keeps readings visible, relabels submit CTA to terminal wording. NEVER emit a separate Reads + separate ColdCrash row (double-counts the garde "earliest ColdCrash date" + racking-eligibility timestamp).
- **Terminal advance stays SERVER-SIDE + value-driven.** `fermenting-phase-submit.php` advances session start→'end' on `event_type==='ColdCrash'` already; checkbox only sets the hidden field. Server must NEVER trust a client "this is terminal" flag independent of the written event_type.
- **Retires the broken dual-CTA.** The `ferm-btn-terminate` button that JS only enabled when `event_type==='ColdCrash'` (form-fermenting.js ~L55-64) IS the awkwardness the operator reacted to ("I don't see a checkbox" — correct, today it's a dropdown-gated terminate BUTTON). Fold its terminal semantics into the checkbox + a single relabelled submit. One control, one meaning.
- **Label:** visible French/floor term "Cold Crash" + subtitle "termine la fermentation" (matches "Houblonnage à froid"/"Purge CO₂"). Persisted value stays ENUM `ColdCrash` (no DB token in UI). Any date the control surfaces = jj/mm/aaaa DMY (central system-settings). Gate 3 (earliest-ColdCrash-date) is DISPLAY-ONLY in the start firewall — does not block submit, not a derived FK → checkbox change can't break it; leave as-is.
- **EDIT-MODE CAUTION:** edit path locks `event_type` as hidden field (fermenting-phase-start.php ~L207-208). The new checkbox must render TICKED + LOCKED in edit mode for an existing ColdCrash row — NOT an editable "un-terminate". Don't let correction mode offer to undo a recorded cold crash.

## DRY-HOP INPUTS FIX — ✅ BUILT+DEPLOYED+MIG-APPLIED 2026-06-03 (operator round-trip pending; commit pending operator in-browser test)
LANDED exactly per the PM spec below, with the deviations/confirmations noted here:
- **MIG 259** (`db/migrations/259_dryhop_widen_category_unit.sql`, APPLIED LIVE): `bd_fermenting_v2.dh_category` ENUM('hops_dry')→ENUM('hops_dry','adjunct','mineral','process'); `dh_unit` ENUM('kg','g')→ENUM('kg','g','ml'). **Dropped+recreated `chk_fermenting_event_payload`** — the build agent correctly DISCOVERED this CHECK gates `dh_category` (its DryHop arm was hardcoded `dh_category='hops_dry'`), so the ENUM widening would be rejected at INSERT until the CHECK's DryHop arm was widened to `dh_category IN (4 values)`. Reads/Purge/ColdCrash arms preserved byte-identical to mig 104 (orchestrator diffed). **NEW DURABLE LESSON: widening an ENUM that a CHECK constraint enumerates requires recreating the CHECK in the same migration, or the INSERT still fails.**
- **`dh_unit` widened to ml** (not in the original 3-part spec — operator's "other ingredients" include liquid adjuncts; correct add).
- **MI picker catalog** broadened from Hops-only to `c.name IN ('Hops','Brewing Adjunct','Brewing Mineral','Process Chemical')` (4 confirmed ref_mi_categories names), ordered by category then name. Header relabeled "Houblon (MI)"→"Ingrédient (MI)". Server **DERIVES `dh_category` per MI from its ref_mi category** (Hops→hops_dry, Brewing Adjunct→adjunct, Brewing Mineral→mineral, Process Chemical→process), validated against the allowlist — replaced the hardcoded 'hops_dry' (server-derives, never trusts client; per spec).
- **DryHop SECTIONS map → dropped `section-readings`** → no OG/pH on dry-hop (the operator's core complaint fixed).
- **TEMPERATURE — deviation from spec, ARCHITECTURALLY SOUND:** spec said reuse the existing `temperature` INPUT on line_idx=0; build instead added a NEW input **`dh_temperature`** inside section-dryhop (DISTINCT form-field name from the still-in-DOM hidden `temperature` input, to avoid a POST name-collision — chosen over disable-hidden-inputs JS as lower-risk). It STILL WRITES TO THE EXISTING `temperature` COLUMN on line_idx=0. ✅ NO parallel store of the fact (the one rule that mattered) — distinct form-field name, same canonical column. Comment served by the existing always-visible `final_comments` card (no new field).
- **Refuse-don't-NULL ENFORCED:** a dry-hop line whose mi_id doesn't resolve to an FK, OR whose derived category isn't in the allowlist, now REJECTS THE WHOLE SUBMIT (flash + redirect) — never writes a NULL-FK / out-of-allowlist row that would silently zero a COP component. ✅ matches the DERIVATION-TREE GUARD.
- **Orchestrator verification:** CHECK recreation diffed faithful vs mig 104; synthetic 2-line round-trip (Hops + Adjunct, ml unit) passed — correct per-line category slugs, temp on line_idx=0 only, FKs resolved, rows self-cleaned; git tree clean of stray scripts.
- **✅ OPERATOR LIVE-TEST CONFIRMED 2026-06-03 (Speakeasy 66 dry-hop):** real submission wrote rows 13380 (Mosaic 30 kg) + 13381 (El Dorado 15 kg), both `dh_mi_id_fk` resolved, `temperature=21.7°C` on line_idx=0, session 19. End-to-end form→DB path proven against live operator data. (Commit still pending — operator commits the session's work.)

### DRY-HOP → COP CONSUMPTION LINKAGE — architecture CONFIRMED (no code change), one latent ml-unit gap flagged
- **inv_consumption is NOT written at submit time** — it is BATCH-DERIVED downstream by maltytask `scripts/derive-bd-consumption.ts` (`deriveFermenting`, reads `bd_fermenting_v2 WHERE event_type='DryHop' AND is_tombstoned=0`, writes `source_event='fermenting'` keyed `fermenting|<id>|<mi_id_fk>`). The operator's new Speakeasy-66 rows pass EVERY derivation filter (date present, qty>0, FK resolved, not tombstoned) → they WILL link on the next derivation / month-close run. 277 historical fermenting consumption rows already link this way, **ALL unit='g'**. This is the canonical model: dry-hop COP is **month-close-derived, NOT real-time**; the derivation can be run on demand. ✅ No parallel store, FK-keyed derivation — derivation-tree-clean.
- **⚠️ LATENT GAP — `dh_unit='ml'` is UNTESTED downstream (record for the next COGS-side touch).** Mig 259 added `ml` to `dh_unit` for liquid adjuncts, but `derive-bd-consumption.ts` carries the unit through VERBATIM ("NO multiplier", ~L240) and **all 277 existing fermenting consumption rows are grams.** Before any operator dry-hops a liquid adjunct **in ml**, the COGS valuation path (`inv_consumption` → MI price) must be checked/wired for **ml → MI-pricing-unit conversion** (an MI priced per-kg/per-L won't value an `ml`-quantified consumption line correctly without conversion). **Hops are ALWAYS g, so all current/historical data is safe** — this is a forward-only gap that only bites the first ml dry-hop. FLAG: surface this whenever the next COGS-side / derivation work happens; do NOT let an ml line reach valuation unconverted (would mis-cost a COP component). Equip `sql`+`coder` when wired.
- **❗STALE-NOTE CORRECTION (recorded):** the prior-session worry that "DryHop hashCols must match `form-fermenting.php` byte-identical" is **FALSE / does not apply**. `form-fermenting.php` does NOT compute a row hash — it is NOT among the `bd_row_hash` callers. ONLY `public/api/fermenting-phase-submit.php` hashes for fermenting. There is NO second fermenting hasher to desync (unlike RACKING, where the 3-hashCols-blocks-byte-identical footgun is real — that pattern does NOT generalize to fermenting). Do not re-raise this as a DryHop constraint.

### PM VERDICT on the 3 tracked gaps (2026-06-03)
1. **DryHop edit-mode multi-line prefill** → **✅ BUILT + DEPLOYED + VPS-VERIFIED 2026-06-03 (still UNCOMMITTED).** Was: `?edit=<pk>` prefilled only one line by PK; the additions table wouldn't re-populate existing sibling lines, and (root-cause) edit mode "opened a read page" because JS init read a null `<select>` (edit renders event_type as a HIDDEN input) and defaulted to 'Reads', showing section-readings instead of section-dryhop. **AS-BUILT (3 files, NO migration):** (a) `public/modules/form-fermenting.php` edit handler — when the clicked-PK row is `event_type='DryHop'`, re-SELECTs ALL siblings on `(submitted_at, beer_raw, batch)` + `event_type='DryHop' AND is_tombstoned=0` ORDER BY line_idx, builds `$prefillDhLines`, injects `window.FERM_EDIT_DH_LINES`; works from EITHER line's Corriger PK (recent-partial renders one Corriger link per row); non-DryHop edit path unchanged. (b) `public/js/form-fermenting.js` — init now derives active event type from the **hidden** `event_type` input when the `<select>` is absent (the null-select→'Reads' default WAS the "opens a read page" root cause); added `prefillDhLines()` repopulating the picker via the existing `addDhRow()` machinery (match on `option.value === dh_raw_name`). (c) `public/api/fermenting-phase-submit.php` — **shrink-tombstone** block after the upsert loop, guarded `if ($isEditMode && $eventType==='DryHop')`, inside the txn: fetches orphan rows `line_idx >= newLineCount`, appends `dryhop_edit_shrink` to `audit_flags`, `UPDATE is_tombstoned=1`, emits `log_revision(... action='update')` with `after_json._tombstone='shrunk_by_dryhop_edit'` (ENUM-no-delete house rule honored — refuse-don't-delete, tombstone not DELETE). Phase-advance block left untouched (already `!$isEditMode`-gated). **VERIFICATION — 19 synthetic assertions PASS:** date-only edit (both rows UPDATE in place, ids stable, only event_date+row_hash change, no dupes/tombstones); shrink 2→1 (line_idx=1 is_tombstoned=1 NOT deleted, audit_row_revisions.action='update', marker present); control (fresh submitted_at duplicates — proves the preserve guard). Self-cleaned to COUNT=0 both tables, zero real-data mutation. `php -l` clean both PHP; deployed via `deploy.sh --apply`; VPS confirms shrink-tombstone + JS hidden-input fallback present. **PM-confirmed CLOSED.** Honors derivation tree: edited/shrunk DryHop rows re-derive cleanly on next `derive-bd-consumption.ts` (tombstoned siblings excluded by the `is_tombstoned=0` filter → no zombie COP line).
2. **Multi-day dry-hop firewall** (`$andNotDryHopped` drops a batch from the DryHop card list once it has ANY DryHop event → a 2nd separate-day dry-hop is blocked) → **DEFER, but it's a REAL latent limitation, not just polish — flag to operator.** The firewall is the same observed-wins / no-double-count guard that protects re-entry; relaxing it to allow N DryHop events per batch on distinct days is legitimate (real beers ARE dry-hopped on multiple days) BUT must be scoped carefully so it doesn't reopen the firewall for accidental duplicate same-day re-submits. The right relaxation is per-DAY (key on dry-hop date, allow one DryHop event-set per distinct date), NOT unconditional. Don't touch until the operator confirms a real multi-day batch is being backfilled — premature relaxation weakens an idempotency guard for a case we haven't hit. When it IS needed: equip `sql`+`coder`, scope by date, preserve the no-same-day-dup guarantee, dry-run.
3. **No optgroup in MI dropdown** (clusters by category via ORDER BY, not visually grouped) → **DEFER, pure polish.** `<optgroup>` by category is a small `ui` follow-up; bundle it into the next fermenting-form touch rather than a standalone deploy.

Net (UPDATED 2026-06-03): **gap 1 ✅ CLOSED** (multi-line DryHop edit built/deployed/VPS-verified — see above; uncommitted). gap 3 = clean defer (optgroup polish). **gap 2 = multi-day dry-hop firewall, STILL DEFERRED + FLAGGED TO OPERATOR** — edit is unaffected, but second-day *entry* on a batch is blocked by `$andNotDryHopped`; operator was told to flag any real multi-day batch and the per-day relaxation gets built then (scope by date, preserve no-same-day-dup guard). None block the commit / operator round-trip.

---
## DRY-HOP INPUTS FIX — PM-scoped 2026-06-03 (build pending, handed to orchestrator)
Operator: "fix Dry hopping inputs. There is NO OG/pH reads for dry hop. We input only the hops and/or OTHER ingredients used (same function as brewday ingredient selection), a box for temperature of dry hop, and comments (optional)."

**KEY FINDING — the data model + write path + COGS chain ALREADY support this; the gap is UI + ONE enum widening. This is mostly UI-only.**

AS-FOUND (verified live + repo 2026-06-03):
- **Write path complete:** `public/api/fermenting-phase-submit.php` already parses multi-line `dh_mi_id[]`/`dh_qty[]`/`dh_unit[]`/`dh_lot[]` (L519-557), resolves `dh_mi_id_fk` via `ref_mi` (L559-573), writes one `event_type='DryHop'` row per line (line_idx 0…N) with `dh_category='hops_dry'`, `dh_qty`, `dh_unit`, `dh_lot`, and **`temperature`/`gravity`/`ph`/`final_comments` ONLY on line_idx=0** (L588-639). NK = `(submitted_at,event_type,beer_raw,batch,line_idx)`.
- **DryHop picker UI EXISTS** (`form-fermenting.js` `addDhRow` L118-165, `dh-tbody` table in all 3 phase partials start/in-progress/end) — multi-line MI select + qty + g/kg unit + lot + add/remove. **BUT it's Hops-ONLY** (`window.FERMENTING_HOPS` = `ref_mi` WHERE category='Hops', form-fermenting.php L306-325).
- **THE BUG operator hit:** section-map `DryHop:['section-readings','section-dryhop']` (form-fermenting.js L29) shows the FULL readings card (gravity+pH+temperature together) ON DryHop. Operator wants NO OG/pH on dry-hop. The temperature input lives INSIDE `#section-readings` (the 3rd of 3 reading-cards), so you can't just hide the card — temperature must be relocated/added to `#section-dryhop`.
- **COGS/COP chain WORKS for ANY MI, not just hops:** maltytask `scripts/derive-bd-consumption.ts` `deriveFermenting()` reads `bd_fermenting_v2 WHERE event_type='DryHop'` keyed on `dh_mi_id_fk`+`dh_qty` → `inv_consumption` source_event='fermenting'. **It does NOT filter `dh_category`** → a non-hops dry-hop MI flows to COP/COGS fine AS LONG AS it lands as a DryHop row with a resolved `dh_mi_id_fk`.

THE ONE SCHEMA CONSTRAINT: **`dh_category` is `ENUM('hops_dry')` single-valued** (live-verified). "hops and/or OTHER ingredients" needs non-hops slugs → migration to widen the ENUM (e.g. `ENUM('hops_dry','adjunct','mineral','process')` mirroring brewing's category vocabulary minus malt/hops_kettle). **NEXT FREE MIG = 259** (max applied/repo = 258; benign double-252 noted). `corrections_policy='allowed'` for bd_fermenting_v2 → edit/correction stays enabled.

BUILD = 3 parts: (1) MIG 259 widen `dh_category` ENUM. (2) Picker: broaden `$hopsMi` catalog to brewing-relevant categories (Hops + Adjunct + Mineral + Process — operator's "other ingredients"), mirror brewday's `BREWING_MI` shape incl. per-MI category slug so the row can set `dh_category` from the picked MI (server must derive/validate slug, not trust client). (3) UI: DryHop section-map → `['section-dryhop']` ONLY (drop section-readings); add a single temperature input + optional comment INSIDE section-dryhop (temperature → existing `temperature` col on line_idx=0; comment → existing `final_comments` on line_idx=0 — NO new columns). Server: on DryHop, gravity/ph stay NULL (already null on line_idx>0; for line_idx=0 stop reading gravity/ph since the inputs are gone).
- **DERIVATION-TREE GUARD:** dry-hop = observed fermenting consumption → observed-data-wins; each line MUST resolve to `dh_mi_id_fk` (refuse-don't-NULL: unresolved MI → operator-visible reject, the form already flashes "ligne MI invalide"-style; keep that, never write a NULL-FK DryHop line silently — it'd be a zeroed COP component). Must NOT double-count vs `ref_recipe_ingredients` dry_hop arm: that ref arm is gap-fill ONLY (observed wins), and the not-yet-dry-hopped firewall predicate (form-fermenting.php L183-205) already gates re-entry per batch.
- **Equip `ui`+`sql`+`coder`.** RULE-2 review before land (operator unsupervised). Self-cleaning synthetic test on the edit/correction round-trip (preserve submitted_at). No COGS INVARIANT test needed unless touching qty math (we don't — qty path unchanged).
- **TEMPERATURE convention CONFIRMED:** reuse existing `bd_fermenting_v2.temperature` DECIMAL(5,2) on line_idx=0 — NO dedicated dry-hop-temp column (would be a parallel store of the same fact). Comment reuses `final_comments`. Both already round-trip in edit mode.

## FERMENTING `op_sessions.recipe_id_fk=NULL` — ROOT-CAUSED ordering bug (2026-06-03, fix pending)
Symptom: recent-inputs list shows "Recette inconnue" because fermenting sessions are created with `recipe_id_fk=NULL`. **ROOT CAUSE (PM-verified on VPS):** in `session-body-fermenting.php` the session-open block (~L119-186) runs BEFORE the brewday-CCT resolver (~L205-226) that derives `$ff_recipeId` from `bd_brewing_brewday_v2` by `(beer,batch)`. So unless recipe arrived on the URL param, `session_open()` is called with `$ff_recipeId` still NULL and writes `recipe_id_fk=NULL` (nullable-at-open by design). **This is FERMENTING-SPECIFIC ordering**, not a universal session-open gap — racking/brewing resolve recipe before their session-open. (`recipe_id_fk` is nullable-at-open across all 4 form_types by design; the firewall is SUPPOSED to populate it pre-advance — fermenting sequenced the populate after the open.)
- **DURABLE FIX = hoist the brewday recipe_id_fk resolution ABOVE the session-open block** so `$ff_recipeId` is set before `session_open()`. Fix at the root, not a post-hoc UPDATE. SIDE BENEFIT: the mother-shell auto-link `if($ff_sessionId>0 && $ff_recipeId!==null)` currently silently SKIPS for fermenting when recipe is NULL → this hoist also repairs mother-linking for fermenting (second silent breakage).
- Display-side fallback (resolve beer from linked event rows when session recipe NULL) = acceptable resilience layer for legacy data, but band-aid; the hoist is the real fix.
- Backfill of existing NULL fermenting sessions = data-driven (resolve from each session's bd_fermenting_v2 / brewday recipe by (beer,batch)), honors refuse-don't-NULL; equip `sql`, dry-run-to-count first, self-clean fixtures.
- Corrections save-bug FYI: before "fixing" a silent reject, confirm it isn't the **closed-session lock** firing legitimately (session-linked + status!='open' → "correction verrouillée" is CORRECT for a cold-crashed/closed lot). If a genuine pre-write reject, it MUST surface to operator UI, never silent-log.

## PURGE FORM FIX — SHIPPED+LIVE+MIG-260-APPLIED+VPS-VERIFIED 2026-06-03 (option a)
Operator-requested: Purge arm carried a full readings card (gravity/pH/temperature) it didn't need. Reduced Purge to **pressure(optional) + comment only** — same shape pattern as the DryHop "drop the readings card" fix.
- **Mig 260** (`db/migrations/260_fermenting_purge_pressure.sql`, applied live; `SHOW COLUMNS` verified `decimal(5,2) NULL`): `ALTER TABLE bd_fermenting_v2 ADD COLUMN purge_pressure_bar DECIMAL(5,2) NULL AFTER comment_purge`. **No CHECK change** — Purge arm is unconditional TRUE (pressure optional, never required).
- **Handler** `public/api/fermenting-phase-submit.php`: `$purgePressureBar = ($eventType==='Purge') ? _fps_decimal($_POST['purge_pressure_bar'] ?? null) : null;`. Added in lockstep to: non-DryHop `$hashCols` **slot 10** (between comment_purge@9 and comment_cold_crash@11) + non-DryHop row payload; DryHop payload `=> null` and NOT added to the DryHop hash array (DryHop/non-DryHop hash asymmetry preserved); ColdCrash-conversion path `$convHashCols` slot 10 = '' + `UPDATE ... SET purge_pressure_bar = NULL` + `$convAfter`.
- **IDEMPOTENCY GUARD (PM-noted):** build agent personally slot-by-slot verified that the non-DryHop write `$hashCols` and the conversion `$convHashCols` are index-aligned (purge_pressure at slot 10 in BOTH). Consequence: a ColdCrash row reached by conversion hashes IDENTICALLY to one written natively → `bd_upsert` ON DUPLICATE KEY UPDATE stays a no-op on re-submit (the row_hash invariant the whole fermenting endpoint depends on holds).
- **JS** `form-fermenting.js`: `SECTIONS.Purge = ['section-purge']` (readings card dropped — mirrors DryHop single-card treatment).
- **Partials** (start / in-progress / end phase): dropdown LABEL "Purge CO₂"→"Purge" (ENUM `value="Purge"` UNCHANGED — display-only relabel); event-type display map "Purge CO₂"→"Purge"; `#section-purge` title "— purge CO₂"→"— purge"; removed ONLY the Purge "Non câblé" notice (ColdCrash "Non câblé" notice left intact); added optional "Pression cuve (bar)" numeric input (step 0.01, min 0) above the comment. Net Purge = pressure(optional) + comment.
- **Edit round-trip** `form-fermenting.php`: SELECT + `$prefillEdit` carry `purge_pressure_bar` so a Purge correction reloads its pressure.
- **Verification:** self-cleaning smoke PASSED — Purge row pressure=1.50 and Purge row NULL both persist; row_hashes differ; gravity/ph/temp NULL on both; idempotent re-submit = no-op (ON DUPLICATE KEY UPDATE); cleaned to COUNT=0. VPS confirms 7 `purge_pressure_bar` refs in handler, single-card `SECTIONS.Purge`, 0 "Purge CO" strings in in-progress partial, `php -l` clean.
- **Uncommitted set additions:** `db/migrations/260_fermenting_purge_pressure.sql` + purge edits across handler, form-fermenting.php, form-fermenting.js, 3 phase partials. All UNCOMMITTED (operator commits the session).

### HOUSEKEEPING DEBT (pre-existing, NOT introduced by purge fix — TRACKED, ride next fermenting touch)
`public/api/fermenting-phase-submit.php` ~L625-626 / L642 / L692 carries STALE comments asserting the hashCols "must match form-fermenting.php hashCols exactly" / "byte-for-byte identical to form-fermenting.php." FALSE: fermenting has a SINGLE hasher (this endpoint); `form-fermenting.php` computes NO hash (its POST handler was stripped at fermenting pilot-4 P-C, commit `1844ad6`). The comments point a future dev at a non-existent sync target. **RULING: retire on the NEXT fermenting touch (ride-along edit), not a dedicated pass — avoid scope creep.** Whoever next opens this file for a real change clears the three comment sites.

---

## ✅ FERMENTING BEER-IDENTITY FRACTURE FIX — COMPLETE + DEPLOYED + VPS-VERIFIED (2026-06-03/04)
**Symptom:** batch EPH2/26's `bd_fermenting_v2` events were split across multiple `beer_raw` spellings; the fermenting in-progress/end `<select>` re-derived phase by counting events keyed on `beer_raw + batch` → under-counted → lot stuck `in_progress`, CCT unresolved; phantom open session id=28 (0 linked events) showed in recent list.
**Fix (read-side only, NO migration, write NK untouched, simulator untouched):**
- phase detection in `session-body-fermenting.php` re-keyed on `(recipe_id_fk, batch)` when recipe_id known, `beer_raw + batch` fallback only when recipe_id NULL (legacy deep-links).
- CCT firewall + early recipe-resolve from brewday keyed on canonical FK.
- in-progress/end `<select>` re-keyed on `(recipe_id_fk, batch)`.
- card-click nav now carries `recipe_id` in the URL.
- recent-list display-filters 0-linked-event open sessions (phantom id=28 hidden).
**Proof:** full batch-census dry-run = **812 (recipe,batch) pairs, 0 regressions** — every batch resolves to its true most-advanced phase; EPH2/26 flips `in_progress`→`end`, `cct=8` resolves.
**This is the READ-side companion to the #25 replay/write-side fix (tank-simulator CCT occupancy).** Both say: key on `(recipe_id_fk, batch)`, never on a free-text label that fragments across spellings.

## ✅ CROSS-FORM ANTI-PATTERN AUDIT (operator directive: "make sure this is not present in other forms… make sure pm and skills are also updated") — RULED 2026-06-04
Anti-pattern signature audited: a session/operator form that **routes / detects-phase / firewalls / looks-up on a fragmented free-text identity** (`beer`/`beer_raw`/short-code) instead of canonical `recipe_id_fk` + batch. Audit was code-read (not guessed) across all four forms; verdicts:
- **fermenting** — WAS affected (the fracture above). FIXED. ✅
- **racking** — **SAFE (canonical model).** Candidate builder keys on `bfw.recipe_id_fk` (JOIN `ref_recipes`); cards carry `data-recipe-id`; `racking-form.js` L765-782 sets hidden `neb_recipe_id_fk` + `applyQcThresholds(recipeId)` from the card; override list dedups on `('cct', source_cct)`; **phase read straight off `op_sessions.phase`** (authoritative server state-machine, NOT a string-derived count); `racking-phase-submit.php` keys EVERYTHING on `session_id` (int op_sessions.id); the `<select>`s are tank/destination/cause, never identity. It is the model fermenting was *meant* to mirror.
- **packaging** — **SAFE (currently; one latent edge flagged).** Cards carry `data-recipe-id`; SKU mosaic + in-tank read resolver (`recipe_id_fk + neb_batch`) + session-open all key on `recipe_id_fk`. The server `resolve_packaging_sku_id()` DOES have a string step (`neb_beer → ref_skus.sku_code`) tried BEFORE the `(recipe_id, format_code)` JOIN — BUT `bd_racking_v2.neb_beer` holds recipe *display names* ("Zepp","EPH2",…) and **0 of 17 distinct values collide with any `sku_code`** (probed live), so the string step never fires for the card path → always falls through to the canonical recipe-id JOIN. Theoretically-imperfect (a future recipe-name that equals a sku_code would wrong-resolve via Step-2-before-Step-3) but **currently exploit-free** — FLAG, don't churn. (Contract lane keys `(contract_beer, contract_batch)` — that string pair IS the canonical contract identity, no recipe_id by design — SAFE.)
- **brewing** — **SAFE (it ORIGINATES the canonical pairing).** Recipe `<select>` option `value` = `ref_recipes.name`, `data-recipe-id` syncs hidden `recipe_id_fk` (`form-brewing.js` L354-361); `beer = post_str('beer_select')` is never free-typed; the intra-submission `(beer,batch)` self-joins to sibling ingredient/gravity/timing rows are internally consistent (one operator-chosen recipe per submission). Brewing is the UPSTREAM that prevents fragmentation — it's the origin, not a victim.
**Ruling: NO further fix needed.** fermenting was the only genuinely-affected form; racking/packaging/brewing are canonical-FK-clean. The one watch-item is packaging's Step-2-before-Step-3 string SKU lookup (safe today by data shape; re-audit if a recipe name ever equals a sku_code).
**Skill codified:** `~/.claude/skills/sql/SKILL.md` index item **#27** + `references/anti-patterns.md` `### 27.` — READ-side companion to #25; full per-form safe/affected catalog + the 0-regression census gate embedded. (Added 2026-06-04.)

---

## §MAGNITUDE GUARD — per-format unit/magnitude sanity check at saisie submit (✅ SHIPPED 2026-06-09, scope NARROWED to cuv fill-volume)

### ✅ SHIPPED RECORD (2026-06-09) — committed + deployed live, NARROWED scope vs the spec below
- **Status: SHIPPED + deployed live + committed.** maltyweb commit `f019526` "feat(saisie): magnitude guard on cuv serving-tank fill volume". Files: **`public/js/magnitude-guard.js` (NEW)**, `public/js/packaging-form.js`, `public/modules/form-packaging.php`.
- **SCOPE NARROWED — cuv serving-tank FILL VOLUME only** (Kouros's explicit choice "volume/litre fields only"). **Count fields (keg/bot/can `prod_total_units`/`qte_unites`) and loss/taproom litre fields are NOT guarded** — a count has no HL-vs-L slip mode, and an OOM band on counts would false-flag small contract runs (6-keg Jasper JASPF/BAMF, 100-bottle Chien Bleu JASPB/BAMB). **This SUPERSEDES the broader brewing/racking-form ambition in the spec below** — those forms remain a POSSIBLE future extension via the same reusable `MagnitudeGuard` helper, but are NOT built.
- **Band derived from canonical `ref_serving_tanks`** (live tanks: 5×5HL, 2×10HL, 1×30HL) → clean window **50–30 000 L**: lower = MIN(capacity_hl)×100/10, upper = MAX(capacity_hl)×100×10. **Server re-evaluates from `ref_serving_tanks` fresh on every POST** (never trusts the client `window.CUV_FILL_BAND`). If the table is unavailable the guard **no-ops** (degrades to pre-feature soft behavior — never blocks a submit).
- **Mechanism:** wired through the existing `FormFramework.extraWarnings()` hook + the `outlier`-level confirm-with-comment dialog; token **`magnitude_outlier_confirmed=1`** round-trips into `bd_packaging_v2.audit_flags`. Edit-mode suppresses re-nag on unchanged confirmed values via `data-mag-confirmed-fields`. **Stored value byte-identical on confirm.**
- **Tests:** 28/28 client unit + 8/8 server-isolation (against live DB). **NOT auto-tested:** the integrated live UI round-trip (dialog fires through FormFramework, token persists) — webapp-testing session lacked the production role; a 4-step manual operator test was handed to Kouros.
- **Historical-data audit** (Kouros asked "do past entries need correcting?"): **255 cuv rows in `bd_packaging_v2`, 0 out-of-band fills** — saisie was already normalised. Confirms the ZEPV 100× was purely the `ref_skus.hl_per_unit` config in the sales→COGS/compiler lane, **NOT operator input.** Separately noted 9 cuv rows with prod_L=0 and zero/negative vendable_hl (ids incl. 1297, 1320, 2160 [−10HL, 2026-04-09], 2217 [−5HL, 2026-05-13]) = tank emptyings/transfers/corrections — flagged to Kouros as a DIFFERENT (non-magnitude) category, not touched here.
- **STILL-HELD UPSTREAM DEPENDENCY (this came out of #0 COGS cutover):** the **-V cost cutover is still HELD** pending the compiler/liner session reverting `ref_skus.hl_per_unit` 1.0→0.01 for EMBV/MOOV/ZEPV and recompiling at per-litre basis; once that lands → re-run `build-sales-cogs --cost-source=diff`, confirm ZEPV ≈ 959 CHF, flip default sheet→mysql. (Status only — no action; the magnitude guard does NOT depend on it landing, the guard shipped first.)

---

### Original PM scope (HISTORICAL — broader than what shipped; kept for the brewing/racking future-extension)
Operator request (verbatim): *"maybe the serving tank inputting used the wrong format. Can we block inputting on the format expected? you see 8.5 on a serving-tank packaging form instead of 850, it flags at submit 'seems hl instead of l, please check'. Do that with all formats — less risk operators input wrongly."* Born from the **ZEPV/-V per-litre vs per-HL 100× conflict** (#0 COGS cutover): a `-V` cuv "unit" = 1 litre (format-18 hl_per_unit=0.01); a parallel session set `ref_skus.hl_per_unit=1.0` (per-HL) → 9700 L × per-HL cost = ~96k phantom. This guard is the **INPUT-SIDE twin** of that data-side fix — stop the wrong magnitude entering at the form. **(As shipped 2026-06-09 the scope was narrowed to cuv fill-volume only — see SHIPPED RECORD above.)**

### VERDICT (the 5 answers)
1. **Forms in scope (PHP + fields):**
   - **`public/modules/form-packaging.php` = PRIMARY** (the literal conflict site). Cuv litre fields `formats[N][loss_keg_liquid_l]` + `taproom_keg_l` (litres — an HL value reads ~100× low). The per-unit count fields `formats[N][prod_total_units]` / `qte_unites` feed `compute_packaging_vendable_hl` via `hl_per_unit/units_per_pack` → a magnitude slip there silently mis-scales vendable_hl/beer-tax. run_type ∈ {bot,can,can33,keg,cuv}; each row carries `run_type` + `format_suffix` → the selected `ref_packaging_formats` row is known client-side.
   - **`public/modules/form-brewing.php`** — `cool_final_volume[]` (HL). Expected band = CCT capacity (30–240 HL live) / brewhouse size 30 HL; a litres-as-HL or HL-as-litres slip is catchable.
   - **`public/modules/form-racking.php`** (handler `public/api/racking-phase-submit.php`) — `racked_vol_hl`, `blend_hl`, `flowmeter_start_hl`/`flowmeter_end_hl`, `loss_source_hl`/`loss_dest_hl` (all HL). Expected band = serving-tank 5/10/30 HL + CCT/BBT 30–240 HL.
   - Fermenting carries no headline volume field → out of scope for v1 (readings are gravity/pH/temp, already threshold-guarded).
2. **Canonical magnitude source — `ref_packaging_formats(run_type, hl_per_unit)`.** This table ALREADY encodes the expected per-unit volume per format (cuv V id18=0.01→1 L/unit; keg F id15=0.20→20 L; bot BU=0.0033→0.33 L; etc.). **No new table, no hardcode.** Per-unit expected litres = `hl_per_unit × 100`. For the HL-denominated brewing/racking fields, the vessel-capacity tables are the band: `ref_cct`/`ref_bbt` (30–240 HL), `ref_serving_tanks.capacity_hl` (5/10/30), `ref_brewhouse_size.size_hl` (30). A "expected magnitude per format" need NOT be invented — derive the order-of-magnitude window from these canonical numbers (e.g. ±1 order of magnitude around the format's nominal). Container atoms live in `dbc_container_types.volume_l` if a finer per-container floor is wanted.
3. **Soft confirm-at-submit, NOT hard block** — operator's lean is correct and matches house philosophy (import-then-flag, never reject) AND the real partial-liner-fill case (we amortize on announced volume, not nominal capacity → a genuinely-low cuv fill must be enterable). Confirm-with-reason override, exactly like the existing `co2o2InversionConfirmed` window.confirm + bypass-flag and the `edit_shared_tank_confirmed`/`tank_reading_override` server tokens.
4. **Architecture — it is NOT a new mechanism; it's populating an empty `thresholds:{}`.** All 4 saisie forms already wire `FormFramework.init()` (`public/js/form-framework.js`) which provides per-field `warn`/`outlier` ranges (`evaluateField`), a `detectSwaps` order-pair detector, `extraWarnings()` hook, and a confirm-with-comment dialog that **requires a comment when any warning is level 'outlier'** (the soft-confirm we want). Brewing & packaging pass `thresholds:{}` (empty) today; packaging/racking also already use the `extraWarnings` + comment-routing path. **BUILD = (a) a shared magnitude validator** that, per the selected format/run_type, looks up the format's nominal unit-volume and flags an entered value that is off by ~1 order of magnitude (the "seems hl instead of l" case) as an `outlier`-level warning with the operator-facing message — wire it through `extraWarnings()` (per-format, because the format is a runtime selection, not a static field threshold) **+ (b) a server-side soft-confirm token** mirroring the existing `*_confirmed=1` pattern so the override is auditable (and a forged/garbage bypass can't poison a value). The per-format nominal must reach the client: either inject a `window.FORMAT_MAGNITUDES` map (id/run_type → hl_per_unit) from PHP (sourced from `ref_packaging_formats`), or read it off the already-present `data-*`/mosaic dataset the packaging form carries per format. Server reads the same `ref_packaging_formats` row to re-evaluate (never trust the client's claim of "in range"). **Roadmap fit:** independent of recette v2 (#0c DONE) and of the -V compiler revert (#0 COGS cutover), BUT **sequence AFTER the -V revert lands** so the canonical per-litre basis it validates against is already live — building it before would risk codifying the very ambiguity (per-HL vs per-litre) the revert resolves. No conflict with anything in flight; parallelizable with WeeklyOrders ingestion (#0a4). Honors: CSS/JS in /public, no inline; RULE-2 review before land; self-cleaning synthetic round-trip; COGS-invariant not needed (no qty-math change — this only ADDS a pre-submit nudge, the stored value on confirm is byte-identical to today).
5. **EQUIP: ui + coder + sql** (sql = read the per-format thresholds from `ref_packaging_formats`/vessel tables; ui = the FormFramework threshold/extraWarnings wiring + dialog; coder = the server soft-confirm token) + **webapp-testing** smoke step (submit a deliberately-100×-off cuv value, assert the confirm fires; submit a genuine partial fill + override, assert it writes).

### FOOTGUNS to brief the agent on
- The packaging cuv path: `prod_total_units`/`qte_unites` are INDIVIDUAL-ITEM counts for bot/can/keg but cuv V is **LITRES** (volume-dimension.md §vendable_hl). So the cuv magnitude check is on a LITRE quantity, not a unit count — the threshold logic must branch on run_type='cuv' (litres) vs item-count formats.
- Do NOT make the band so tight it false-flags legit partial liner fills or a small pilot brew — the check is ORDER-OF-MAGNITUDE ("~100× off"), not a precision range. A 30% under-fill must pass clean; a 100× slip must flag.
- Server re-evaluation must read `ref_packaging_formats` itself (canonical), not trust a client-passed nominal — same discipline as the tank-reading override (server enforces, client only requests).
- Edit-mode: a correction re-opening a past row must NOT re-fire the magnitude confirm on values that already passed/were-overridden once (or it nags on every edit). Carry the override token / suppress on unchanged values.

## §SAISIE BRASSAGE 1055 HOTFIX — form-brewing POST-side recipe resolver (✅ SHIPPED + LIVE 2026-06-16)
**Bug:** regression `9dc0a8a` (deployed 2026-06-15) had rewritten the POST-side server recipe resolver in `public/modules/form-brewing.php` (~L80-93) to `SELECT id FROM ref_recipes WHERE name=? AND is_active=1 GROUP BY name HAVING COUNT(*)=1` — non-aggregated `id` under a `GROUP BY name` violates MySQL 8 `ONLY_FULL_GROUP_BY` → **error 1055 on every POST**.
**Severity = HARD-BLOCK, not edge case:** Gonzalo's data-entry pattern is to create a brewday header then repeatedly re-open via "Corriger/modifier" and re-submit to add datapoints incrementally. The resolver runs UNCONDITIONALLY on EVERY POST, so once `9dc0a8a` deployed, every `corriger` on his in-progress brewday threw 1055 → hard-blocked mid-entry. **This workflow is now a STANDALONE STANDING FACT at the top of this file (§STANDING OPERATIONAL FACT — operator incremental-entry workflow) — reusable across ALL form work, not specific to this bug.**
**Fix (option = minimal deterministic rewrite — PM-recommended; applied by Kouros directly, NOT an agent):** plain `SELECT id FROM ref_recipes WHERE name=? AND is_active=1` → `fetchAll()` + `if (count($rows) !== 1) throw` (same French "introuvable ou ambiguë" message) + `$recipeId = (int)$rows[0]['id']`. **NO `ANY_VALUE`** — the "refuse on ambiguity / never guess" contract is preserved EXACTLY; the resolved `recipe_id_fk` stays deterministic (it's the load-bearing FK feeding every downstream tank/consumption/COGS join). Re-runs cleanly on each `corriger` → Gonzalo's incremental-edit loop works again.
**Deploy:** surgical single-file rsync of `form-brewing.php` ONLY (NOT `bin/deploy.sh --apply`) because the shared tree had unrelated WIP (kpi-handlers.php, expeditions.css, mon-tableau.css, kpi-charts.js) that must not go live — the codified dirty-shared-tree workaround. VPS md5 == local (`d7c7cf37ca974cf28cc715f3f77d1726`); `php -l` clean on VPS. **NOT committed yet** (operator manages the tree; other WIP present).
**🔴 FAST-FOLLOW OPEN (RULE 1):** converge this ONE-OFF resolver onto the canonical `app/recipe-resolver.php` `resolve_recipe_id()` helper so we don't carry two recipe resolvers (the canonical→alias→normalized path from the SDC Recettes work, migs 219-225). This one-off only handles exact-active-name; the canonical helper also resolves aliases — a contract-renamed recipe could resolve here but not there (or vice-versa) and silently fragment the FK. Logged for tracking; NOT done in this hotfix. EQUIP coder+sql.
