# SKU/BOM validation — ④ Direct-sales tracker, cage anchor, eshop-consumption matrix gap, Lightspeed POS, consumption-tracker design

## 🔎 2026-06-28 STOCK-TRACKING AUDIT — THE ESHOP LEAK ROOT-CAUSE, CORRECTED (read before any "eshop leak" / I1 / C2 work)
Orchestrator audit (live-probed prod): eshop leg leaked ~467u since the 06-22/23 census (~93%); chronic 1100-2000u/mo for 6mo. **The audit's first-pass root-cause (PACKDEC/PACKDECX8/EPHB12/EPH4PB/SPYB24/COLLAB24 are "unregistered seasonals invisible to the board") is WRONG — PM live-probed 2026-06-28 and OVERTURNED it.** Those 6 codes are ALREADY correctly modelled: every one is a row in `ref_sku_aliases` → an ACTIVE canonical SKU (PACKDEC/PACKDECX8→PD8 137; EPHB12→EPH4B12 67; EPH4PB→EPH44PB 76; SPYB24→SPYB 49; COLLAB24→DGDB 8). The is_active=0 ref_skus rows (287/288/303/304/305/301) bearing "[Shopify alias of …]" labels are the FOLDED-OUT parallel-store remnants — leave them is_active=0; **DO NOT activate/scope them** (that re-creates a parallel store of one fact + double-counts PD8's 242 counted units).
**THE REAL LEAK = two causes, both at INGEST-RESOLUTION + the designed scope gate, NOT master-data:**
- **(A) NULL `sku_id_fk`: 3074 eshop lines / 10742u** — the dominant leak. Shopify ingest (`scripts/ingest-shopify-orders.ts` in maltytask) failed to resolve `inv_sales_order_lines.sku_code` → `sku_id_fk`. Fix at ingest: resolve via `sku_code` → `ref_sku_aliases` → `ref_sku_shopify_variants` (bridge, mig 377) before leaving NULL; backfill existing NULL lines.
- **(B) scope='none' bundles deplete NOTHING — BY DESIGN, awaiting wire-up.** All 3 `fg_stock_compute()` legs gate `WHERE rs.stocktake_scope <> 'none'`. Every eshop pack format (EMBB12/STIB12/…B12, …4PB, …12C cans, COLLAB12, EPH24P, ESTB12, EXP12C, PD8's family) is scope='none' → resolves to an ACTIVE SKU but the leg drops it. This is the ④-Phase-2 / repack-engine job, NOT a master-data fix. ~4000u of the post-census leak is here.
- **(C) 1086 `sales-sku-unknown` RQ rows = a DIFFERENT, fiscal backlog** — from the one-shot BC sales-LEDGER load (`ingest_bc_sales_ledger.py`, created 06-09/10), tokens mostly 2021 discontinued SKUs (MALB/AMW4/EPH1020) + non-beer (CO2, ART-*). NOT the live eshop FG leak; do not conflate. BC order/ledger ingests ALREADY resolve through `ref_sku_aliases`.
**Composite/bundle decomposition (C2) ALREADY EXISTS, gated:** PD8 composite BOM = 122 `ref_sku_bom` rows (composite_liquid + composite_packaging) LIVE; `ref_sku_composite_slots` populated (PD8/PAL/XMAS/PAC); the repack engine (`app/repack.php`, `inv_repack_events` mig 336, `rq_repack_unresolved_bundle` mig 337) + the fg-stock repack leg are BUILT and the flag **`repack_depletion_live`=1 (ON, section='features')**. So C2 is "WIRE the eshop none-bundle sale → repack/component drawdown + fix ingest resolution," NOT "build a bundle table." Target = base-box (MVP) now; cage (-X) is the Phase-2 end-state, still gated on the Shopify pickup-vs-shipped signal (G3, never cleared) + cage feed. See §④-Phase 2 / repack engine below for the full ruling. 🔴 Kouros-only decisions (never guess, COGS/beer-tax-bearing): the EPHB12/EPH4PB temporal-reuse caveat (their alias notes literally say "if Shopify reuses code next season, convert to ref_sku_collab_temporal" — the audit's "EPHB12 = Huguette AND Malt Capone" is exactly this) + the base-vs-cage depletion-target flip timing.

## 🟢 ④ DIRECT-SALES TRACKER — OPERATOR DECISIONS 2026-06-09 + MVP SPLIT (Phase 1 visual NOW / Phase 2 cage-COGS later)
**Operator answers 2026-06-09 (recorded — these RE-SHAPE the build into two phases):**
1. **Shopify pickup-vs-shipped signal → ON HOLD.** Team is actively changing Shopify's shipping-vs-pickup setup; we wait for the final mechanism before keying on it. → **Phase 0 (ingest pickup-flag) + cage-vs-loose COGS routing are DEFERRED** until that lands.
2. **Cage anchor → physical count starts Monday 2026-06-15** (NOT 06-08 as earlier — the cage leg slipped). Not loadable yet → the **cage draw-down engine is GATED on this.**
3. **MVP = VISUAL FIRST, and it is a NEW TOP-BAR PAGE, NOT inside Le Cockpit** (Cockpit = the settings side of sales/marketing/finance). The new page tracks **Shopify + taproom sales** and shows the **virtual consumption stock**, which **MUST be FULLY WIRED to the PF (FG) stock in Expéditions — it will be part of the weekly stocktake.** Operator wants a NAME (their placeholder "B2C").
4. **Keg-share (draft pours) → handled SEPARATELY** in the packaging-template batch (Rule #4), NOT in ④.

**⇒ THE TRACKER IS NOW A TWO-PHASE BUILD:**
- **④-MVP (Phase 1, BUILDABLE NOW — read-only visual page):** Shopify (`inv_sales_orders`/`inv_sales_order_lines`, channel='eshop') + taproom (`inv_sales_bc`, channel='taproom') sales per beer/SKU/period, + the **virtual consumption stock READ FROM `fg_stock_compute()`** (the SINGLE FG-stock derivation — do NOT build a 2nd). Charts via `kpi-charts.js` (`window.KpcCharts`). New top-bar page. **WRITES NOTHING to stock** (read-only on FG; consistent with the R1 single-homed-derivation boundary).
- **④-Phase 2 (DEFERRED, gated on pickup-flag + 06-15 cage anchor):** cage-vs-loose routing, the per-beer cage/box residual ledger (the one genuinely-new table), `inv_consumption` writes (`source_event='sales_derived'` + the cage/box draw-down disposition), COGS-into-COP. The MVP must leave HOOKS so Phase 2 slots in without rework.

**PAGE IDENTITY (PM recommendation):** new top-bar page, `ref_pages` row. **NAME — recommend «Ventes directes»** (matches the FR house naming: Expéditions/Approvisionnement/Saisies; "B2C" is operator placeholder, avoid the anglicism + it's really D2C). Alternatives: «Ventes & Boutique», «Boutique & Taproom», «Eshop & Bar». **Domain = `logistics`** (sibling of Expéditions, which it reads), `min_role='manager'` (commercial/finance view, not floor-operator), `sort≈105` (right after Expéditions sort=100). **NOT in Le Cockpit** (Cockpit = admin-domain settings cluster sort 150-180). The page is the visual front; Le Cockpit stays the settings side.

**DATA-READINESS (live, PM-verified 2026-06-09):** `inv_sales_orders` 2472 rows, channel='eshop' ONLY, 2025-12-01→2026-04-30; `inv_sales_order_lines` 4156 lines, **3893/4156 SKU-resolved (93.7%)**, 263 unresolved (merch/non-beer, NULL title — immaterial for a per-beer view, surface as an "non rattaché" bucket). `inv_sales_bc` 3071 rows 2025-12→2026-05, channels = **enum('b2b','taproom') ONLY** — taproom 208 rows (179/208=86% resolved), b2b 2863. **🔑 DOUBLE-COUNT GUARD IS STRUCTURAL: `inv_sales_bc` has NO 'eshop' channel** — the BC 'Eshop'/1080 mirror is already excluded at BC ingest; eshop comes SOLELY from `inv_sales_orders`, taproom SOLELY from `inv_sales_bc`. Same source split `fg_stock_compute()` already uses (see fg-stock.php header L21-30). MVP just mirrors that split — no new dedup logic needed. **GAP to flag operator:** eshop date coverage ends 2026-04-30 (no May/June eshop yet ingested); BC taproom runs to 2026-05. The page is meaningful on Dec-Apr history now; recency depends on the Shopify/BC ingest cadence.

**FG-STOCK WIRING (exact, PM-verified):** `app/fg-stock.php` → **`fg_stock_compute(PDO $pdo): array`** returns `{anchor_month, anchor_date, rows[], computed_at}`; each row carries `sku_id, sku_code, format, display_family, hl_per_unit, anchor_qty, prod_qty, expedie_qty, eshop_qty, taproom_qty, physique, open_week_qty, open_total_qty, live_semaine, live_futur, velocity_weekly, semaines_stock, flag_*`. Also `fg_stock_for_skus(PDO, array $skuIds)` for a filtered subset and `fg_stock_location_snapshot(PDO)` per-location. **The "virtual consumption stock" = `physique` (the derived live FG units) = the SAME number the weekly stocktake's expected-vs-counted board shows** (Expéditions `?view=stock` already renders it). So the ④ page surfaces the IDENTICAL `fg_stock_compute()` output the stocktake uses — it does NOT recompute, does NOT re-derive, does NOT write. The weekly stocktake remains the place stock is COUNTED (writes `inv_fg_stocktake`); ④ is a READER of the same derivation. R1 hazard (single-homed FG depletion) preserved: no 2nd FG-stock path.

## ✅ CAGE ANCHOR LANDED + UNIT-MODEL DECIDED + SCOPE-POLLUTION FIXED — AS-BUILT 2026-06-15 (Opus-verified, committed by pathspec, NOT pushed)
**THE GATE ④-Phase 2 was waiting on (cage anchor) IS NOW LANDED + CLEAN.** Three things shipped together (maltyweb main, deployed to VPS; the OTHER Phase-2 gate, Shopify pickup-signal, is still ON HOLD):

**Commit `80f36b9` `feat(cage): track cages in cage-units (decimal), drop -X hyphen, fix PF display` — mig 363 + 4 code files.** The TWO findings I predicted (the CAST truncation fix + the rename) BOTH shipped exactly as ruled, plus the scope-pollution fix (a NEW discovery, `e205522` / mig 362):
- **CAST fix (predicted ✅) — but `80f36b9` only HALF-fixed it; the snapshot stayed floored until `23fbec4` (2026-06-16):** `app/fg-stock.php` — `80f36b9` removed `CAST(s.qty AS SIGNED)`/`(int)` flooring of the stocktake anchor in **`fg_stock_compute()`** (decimal there), but the parallel **`fg_stock_location_snapshot()`** STILL floored the cage anchor at its merge step (`'qty' => (int) $ar['qty']`, ~L1365). 🔴 **CORRECTION (2026-06-16):** that floor zeroed the 7 decimal cage anchors (ZEPX 0.2171 … STIX 1.0078) on the per-location cards → **Σcards fell short of Σphysique by exactly 2.4655 (the cage-decimal sum) — a live, board-visible R1 violation present even with the repack flag OFF.** Fixed (int)→(float); committed `23fbec4` (pushed; pathspec, clean 1-line). **So: cage qty is decimal in `fg_stock_compute()` since `80f36b9`; in `fg_stock_location_snapshot()` it was floored until `23fbec4`.** This fix was the prerequisite that unblocked Layer-A repack R1 (whose invariant IS Σcards==Σphysique). `stocktake_scope` carried into rows.
- **Rename (predicted ✅) — and the cage-detection repoint that made it safe shipped WITH it:** mig 363 renames `ref_skus.sku_code` ALT-X..ZEP-X → **ALTX DIVX DOAX EMBX MOOX SPYX STIX ZEPX** (8 rows) + syncs denormalized `inv_fg_stocktake.sku` (9 rows); audited; **NO alias** (cages never sold direct — exactly the PM rec). `expeditions.php` cage-detection DECOUPLED off `substr(sku_code,-2)==='-X'` onto canonical `stocktake_scope='cage'` at all 3 sites (the one-fact-one-source fix I ruled mandatory before rename); `expeditions-stocktake.js` likewise. `salle-de-controle.php` cage SKU-code generator now mints `<prefix>X` not `<prefix>-X` (2 sites L201/L2547 — **the ROOT that created the hyphen; loop closed**). BOM (`ref_sku_bom.sku_id` INT) + stocktake JOINs unaffected (INT FK, as I noted).
- **Verified (Opus canonical gate):** cage set == 8 (ALTX..ZEPX); 0 `-X` left in `ref_skus` or `inv_fg_stocktake.sku`; today's counts preserved under new codes; STIX (id 282) = 20 BOM rows intact; board renders cage decimals + DOCB integer (regression gone); scope math reconciles.

**🔑 OPERATOR MODEL DECISION (DURABLE, COGS-RELEVANT) — supersedes ANY "÷1027" assumption I'd recorded.** **Cages are tracked in CAGE-UNITS directly. 1027 is used NOWHERE in the math.** A cage is "often ~1027 bottles but variable" (STI counted 1.0078 = 1035 btl). `units_per_pack=1027` stays as a NOMINAL LABEL ONLY — NEVER a discriminator, NEVER a divisor (Kouros explicit carve-out). The saisie now inputs cage-units DIRECTLY: removed the ÷1027 save, the ×1027 prefill, the "1 cage = 1027 bouteilles" hint; hl = cageUnits × hl_per_unit. **This RETRACTS the earlier finding** that "saisie inverse-converts bottles÷1027" and the "surface in bottles (qty×1027)" rec — both are GONE; the unit IS the cage-unit end to end. Today's 7 counts kept as-is (loc 2/Crissier): ALTX 0.4888, DIVX 0.5346, EMBX 0.5161, MOOX 0.3340, SPYX 0.3671, STIX 1.0078, ZEPX 0.2171; DOAX uncounted (=0). FUTURE: bottles-per-cage MAY be derived from accumulated unit data later (Kouros: "move into 1=1027 once we have data, or not") — do NOT assume 1027 until then.

**🔴 SCOPE-POLLUTION FIX (NEW DISCOVERY — was NOT in the two predicted findings) — commit `e205522` / mig 362.** Root cause CONFIRMED: a 2026-06-09 claude-cli edit (audit ids 12077-80/12175-76, comment "Taproom: hors inventaire POS") had wrongly set `stocktake_scope='cage'` on **34 non-cage SKUs**. Fixed per ruling + Kouros's decision: DOCB/DOCF (Neb/CollabIn) → **base** (restores Nausikraft/consignment visibility — Kouros explicitly needed DOCB countable there; `exp_st_scope_allowed`: base ⇒ all site types incl consignment); 32 class=Contract → **none** (Kouros: Néb does NOT stock-count client product). Guard `sku_code NOT LIKE '%-X'`. 34 rows audited. **RESULT: `stocktake_scope='cage'` == EXACTLY the 8 cages (ALTX..ZEPX).** New scope counts: **base=54 / single=27 / none=93 / cage=8.** Column default already 'none' (no recurrence vector; salle-de-controle INSERT doesn't set scope). This REPLACES the earlier "base 52 / none 61 / cage 42 / single 27" distribution recorded at §④-Phase 2 below — that was the POLLUTED state; the §④-Phase 2 anchor-coverage numbers keyed off cage=42 are now stale (cage is 8, all real).

**RECONCILIATION against my "TWO AS-BUILT FINDINGS" prediction:** both predictions LANDED (CAST fix, rename). The cage-detection-repoint I ruled "mandatory before rename" shipped with the rename, as ruled. The scope-pollution was the one NEW thing — it surfaced because the rename grep/probe exposed 42 `cage` rows when only 8 -X cages exist, and I traced it to the 06-09 cli edit. The unit-model decision (cage-units, no ÷1027) is the one place reality DIVERGED from my recorded assumption — I had it as bottles÷1027; the operator ruled cage-units-direct. Memory now corrected.

**STILL OPEN / NEXT:** (1) NOT pushed — shared clone holds other sessions' uncommitted WIP (kpi-handlers, planning.*, ingest_bc_sales_orders.py); I committed ONLY my 6 files by pathspec, left theirs alone; Kouros to confirm push. (2) DOCB awaits its Nausikraft count (operator); DOAX uncounted. (3) The cage anchor being landed + clean = the gate Kouros was waiting on for **④-Phase 2 cage/COGS engine** — but it REMAINS DEFERRED on the OTHER gate (Shopify pickup-signal, still ON HOLD). At June close, today's `count_type='operational'` cage counts will need a `month_end` census to feed COGS (the cage-units feed the per-cage COGS the engine consumes).

## 🔴 ④-Phase 2 / "ESHOP CONSUMPTION MATRIX" — STRUCTURAL DRAWDOWN GAP PINNED (PM live-probe 2026-06-12)
Operator greenlit the "eshop consumption matrix" (bundle→component decomposition) 2026-06-12 — this IS ④-Phase 2 (the cage/COGS engine), narrowed to "wire bundle sales into FG depletion". Live findings (PM-verified, read-only):
- **`ref_skus.stocktake_scope` is a 4-VALUE ENUM `('base','cage','single','none')`** — NOT the 3-value I'd half-recorded. This column IS the materialized nesting-ladder/inventory-of-record decision. ⚠️ **DISTRIBUTION BELOW IS THE PRE-CLEANUP (POLLUTED) STATE — superseded 2026-06-15 by mig 362 to base=54/single=27/none=93/cage=8 (see §CAGE ANCHOR LANDED above; cage=42 was 34 mis-scoped contract/collab rows + 8 real cages).** Polluted snapshot: ~~base 52, none 61, cage 42, single 27.~~ Semantics from live rows (ZEP family): **base** = the packaged-output unit-of-record (ZEP4/ZEPB/ZEPC/ZEPF, upp=24 or keg upp=1, is_packaging_line=1) — what the packaging form produces + what stocktake counts; **cage** = the `-X` 1027-bottle WIP crate (ZEP-X, upp=1027, pl=1); **single** = loose unit (ZEPBU/ZEPCU, upp=1, pl=0); **none** = the BUNDLE/unboxing SKUs (ZEPB12/ZEP4PB/ZEP4PC/ZEP12C/ZEPP25/ZEPP50/ZEPV, pl=0) — derived sales handles, NOT independently inventoried.
- **🔴 THE GAP, EXACTLY: `fg_stock_compute()` depletes with `WHERE rs.stocktake_scope <> 'none'` on ALL THREE legs** (shipped L410/L1035, eshop L498/L1094, taproom L538/L1133). **⟹ a sale of a `none`-scope BUNDLE (ZEPB12, 4PB…) currently depletes NOTHING.** That is the scope-gate carried forward by d9f7976 — correct (bundles were wrongly hitting packaged-stock as if independently produced), but it leaves bundle sales drawing on air. The matrix's job = route a bundle sale to draw down its COMPONENT base/single units instead.
- **ANCHOR COVERAGE (the drawdown-target reality check):** with_anchor in `inv_fg_stocktake` — **base 38/52, single 16/27, cage 3/42, none 1/61.** So the BASE SKUs (the natural drawdown target) DO carry anchors + are counted; `none` bundles essentially do not (correct — they shouldn't). **There IS a real target to draw down** (base/single), but cage anchors barely exist yet (06-15 count fixes that).
- **NO bundle-BOM/component-map table exists for unboxing bundles.** `ref_sku_composite_slots` exists + is populated but ONLY for the 4 MIXED composites (PD8/PAL/XMAS/PAC — multi-recipe samplers); a single-recipe bundle (ZEPB12 = 12× ZEPBU of ONE recipe) has NO slot rows and needs none structurally — its decomposition is `units_per_pack` + recipe_id (ZEPB12 upp=12, recipe_id=ZEP → 12 ZEPBU-equivalents). The component map for unboxing bundles = **derivable from `units_per_pack` + the format's container, NOT a new table** (composite slots only earn their keep for MULTI-recipe packs).
- **PM READ:** the STRUCTURAL drawdown (bundle sale → component unit drawdown, wired into the 3 depletion legs by replacing `<>'none'` with a decomposition step) is SEPARABLE from + can ship BEFORE the cage/box-COGS VALUATION layer (gated on 06-15 cage anchor + pickup-flag). Full scoping returned to orchestrator 2026-06-12 — see below if recorded.

### 🔴 REPACK LOOSE-REMAINDER MODEL — PM DEFINITIVE RULING 2026-06-12 (Model B; PM live-probed)
**THE CORRECTNESS CRUX, RECONCILED. RULING = Model B (loose tracked as the scope='single' SKU), NOT Model A (separate "vrac restant" pool excluded from physique).** Decisive live facts (PM-probed 2026-06-12): **`inv_repack_events` ALREADY has `to_sku_id_fk`/`to_qty`/`loose_units`/`to_kind` ENUM('bundle','pd8','loose','adjustment') in schema (mig 336) AND IS EMPTY (0 rows live)** → Model B's structure is physically present; adopting it = NO migration, NO backfill, ZERO downstream risk. The compute leg (`app/fg-stock.php` Step 5.5 + snapshot ~L1266) currently reads ONLY `from_qty` as a raw physique subtraction (= the broken "loose discarded" leg: a ZEPB12 sale opens a 24-box but only 12 ship → over-depletes by 12 every bundle). **Model B fix:** box-open event nets HL 0 = −1 base box (scope='base', counted) + base_upp loose SINGLES (scope='single', counted) → nothing lost; the BUNDLE SALE decomposes in the eshop leg onto the (recipe,container) single (ZEPB12 → −12 ZEPBU), net physique −12 (the shipped bottles, exact). ONE accurate physique number (operator's "accurate stock" need = correctness, NOT a UX preference → PM RULES it, does not escalate the data model).
- **Why B over A (derivation-tree):** the repack fact = one physical conversion (box ceases / singles appear) = ONE fact, two legs, and the canonical table already holds both. Model A discards the `to`-leg at compute and rebuilds "vrac" as a derived aggregate OUTSIDE physique = a 2nd stock number for a fact the ledger already records = parallel-store smell at the arithmetic level; physique deliberately incomplete. A ALSO needs the same net-against-loose propose logic B needs, so A = strictly more surface for strictly less correctness.
- **VIOLATION CHECK — B clean:** R1 (Σcards==Σphysique) preserved (box-open nets 0; sale draws ONE counted single card; no phantom rows). Single-homed FG depletion preserved (eshop leg still the SOLE bundle-depletion path; B changes WHICH sku it lands on = the single, not how many paths). **d9f7976 gate: B works WITH it** — ZEPBU is scope='single' (≠'none') so the existing `<>'none'` gate already passes it; NO gate edit; the bundle SKU itself (scope='none') stays suppressed → no double-count (repack event = box→single net 0; sale = single-draw; two distinct facts).
- **`inv_side_stock_ledger` (mig 303) is NOT the Model-A precedent** — it's the PACKAGING-line loose-remainder accrual ledger (`prod MOD upp`→year-end-offered), a different fact at a different altitude, no box→single conversion. What it DOES set = loose tracked as its own per-SKU ledger line (NOT folded invisibly into a box pool) → precedent FAVORS B.
- **Auto-propose = RUNNING single-balance per (site, single_sku), NOT per-day aggregate, NOT ceil-per-line.** Process chronologically; box-open proposed only when running single balance would go negative for the next bundle draw; opens = `ceil(deficit/base_upp)`. Negative single balance = the "unbox more" signal; composes with the existing same-day tiebreak ordering. Per-day aggregation mis-orders intra-day (a morning sale should consume an afternoon-prior open) → reject it.
- **Loose pool grain = (site_id, single_sku_id); the single is per (recipe, CONTAINER) — NOT one-per-recipe.** Live: recipe 32 has BOTH EMBBU (bottle, id79) AND EMBCU (can, id94); recipe 25 DIVBU+DIV33C. Bottle bundle (…B12/…4PB)→BU single; can bundle (…4PC/…12C)→CU single. Do NOT collapse to (site,recipe) (merges bottle+can loose, mis-depletes). Site matters (repack is physical, singles counted per-site).
- **Single anchor coverage = 16/27** (11 unanchored). Unanchored single → running balance starts 0/from box-opens forward; a genuine negative = honest "unbox more / count me" signal, NOT a phantom (mirror base/cage anchor handling); surface, never NULL.
- **ONE thing to confirm with Kouros (physical-process, not data-model):** "box-open = a real shelf action; team picks from loose bottles until they run out, then opens another." B encodes exactly this. All evidence (bundles scope='none' virtual handles, zero bundle production events, engine = "unboxing") says bundles are virtual → STATE the assumption, PROCEED on B, only adjust if contradicted. Do NOT pause the build.
- **EQUIP: sql + coder + ui + webapp-testing.** Verify (Opus-independent, COGS-bearing): R1 gap=0 with synthetic box-open+bundle-sale fixtures; ZEPB12 sale moves physique −12 (not −24); box-open alone moves physique 0; single negatives only where anchor-absent; self-clean fixtures.

### 🔴 REPACK-ENGINE TARGET DECISION — PM RECONCILED RULING 2026-06-12 (PM live-probed; `app/repack.php`+mig336/337 BUILT, UNCOMMITTED)
The repacking-capture engine (`app/repack.php`, 596 lines, `?? untracked`; mig 336 `inv_repack_events`, mig 337 `rq_repack_unresolved_bundle`) decomposes eshop bundle sales (ZEPB12/4PB/composites) into a draw-down + loose-remainder. Phase-A brief chose **base box** as the draw target; Opus surfaced it as shaky. PM ruling after live re-probe:
- **🎯 TARGET = BASE BOX, NOT the cage. The cage is a TRAP for this engine.** Decisive live evidence (PM-probed 2026-06-12): the CORE `-X` cages (ZEP-X/ALT-X/EMB-X/MOO-X/SPY-X/STI-X/DIV-X/DOA-X, upp=1027 — the exact eshop pull target) have **~0 production events AND ~0 anchors** (DOA-X 2 evt, DIV-X/ZEP-X 1 anchor each, the rest 0/0). My 2026-06-07 "cages invisible" finding STANDS for the CORE cages — their production is still recorded inside `-B` box events (no cage option in the packaging form). **CORRECTION to a half-recorded claim: the 101-event/595k-unit "cage-scope production" I'd have cited is ENTIRELY contract/collab beers parked in scope='cage' (WEST/JASP/CROP/BAM/CAND/FEBR/LUME/DXV/MOSA, upp=24 or 1) — NOT the core eshop cages.** scope='cage' is HETEROGENEOUS (core 1027-crate vs re-scoped contract beers). The BASE box by contrast is fed (809 bot evt / 5.84M units) AND anchored (38/52). ⟹ cage-targeting = depleting an unfed/unanchored pool = guaranteed phantom negatives (the d9f7976 class). Base = a real, fed, counted pool. The rule-correctness of `-X` (operator ④#3) is a PHASE-2 truth (once the cage is fed by the 06-15 anchor + a packaging-form cage option); for the MVP capture engine, base is the only target with a pool behind it.
- **scope-gate / FG-leg arithmetic CONFIRMED intact (PM-read `app/fg-stock.php` L410/498/538 + L1035/1094/1133):** all 3 depletion legs gate `rs.stocktake_scope <> 'none'`. BOTH base AND cage pass the gate (neither is 'none'); the gate does NOT distinguish them — so the engine's CHOICE of target is what matters, not the gate. Σcards==Σphysique holds for EITHER target (the rewrite is a sale-side redirection, not a new card). The d9f7976 carry-forward (bundles wrongly hit packaged-stock) is what the engine FIXES — by redirecting the `none`-bundle sale onto a real base-box draw.
- **(B) string-prefix base-box tie-break (B12→B, 4PB→4) = ACCEPTABLE as an auto-PROPOSE default, NOT a silent commit.** The engine is auto-propose→human-confirm (writes `inv_repack_events` proposals; unresolved → `rq_repack_unresolved_bundle`). The house rule "never guess a COGS-impacting mapping" forbids SILENT inference; an operator-confirmed PROPOSAL with the inference shown is the SANCTIONED pattern (= triage). **GUARDRAIL: when the prefix rule yields ≠1 match, the engine MUST emit an RQ row, NEVER pick arbitrarily** (it already does — `_repack_find_base_box` returns null → `_repack_emit_rq` "base box ambiguous"). So the heuristic is fine PROVIDED ambiguity always routes to RQ + the proposal is operator-gated before any COGS write. Not a hard violation to remove; it IS a violation to let it auto-commit.
- **(C) composite aliasing (PACKDEC/PACKDECX8/FRPACKDEC → PD8 id 137):** there IS a canonical resolver — **`ref_sku_aliases`** (sales-SKU→canonical, already the mechanism for EPH/SPY folds). The engine should resolve a sold discovery-pack code through `ref_sku_aliases` to the canonical composite SKU FIRST, THEN fan out via `ref_sku_composite_slots`. If the alias rows don't yet exist for PACKDEC*, that's a SEEDING task (operator-explicit, from the canonical PD8=137 mapping already recorded — NOT a guess). Do NOT add a parallel alias map in the engine.
- **(D) recipe-NULL collab/FR SKUs (~25%: COLLAB12/24/4PACK, FRPACKCHEF/FRDOAB12/FRMOOB12…):** **RQ-and-manual-capture for MVP.** No recipe resolver should GUESS these (COGS+beer-tax bearing). FR-market SKUs are usually a known core beer re-listed (FRMOOB12 = MOO) → those ARE `ref_sku_aliases` candidates (operator-seeded, like the SPY folds). True collabs (COLLAB*) may be genuinely recipe-NULL until classified (the strain/validation arc). MVP: route both to `rq_repack_unresolved_bundle`, surface, never NULL-deplete.
- **(E) PROCEED, don't pause Kouros on the TARGET.** Base-box is the PM-confident target (it's the only fed+anchored pool; cage is provably a phantom-negative trap right now). The prefix heuristic is sound BECAUSE it's propose→confirm. What to RELAY (not block on): (1) target=base for MVP, cage deferred to Phase-2-post-06-15-anchor + packaging-form cage option; (2) PACKDEC*/FR-relist alias rows need an operator seeding pass (explicit mapping, ~10 codes); (3) the engine is uncommitted — commit-gate it through RULE 2 review. The ONE thing genuinely needing Kouros = whether the eventual Phase-2 cage feed (06-15 anchor + a cage option in form-packaging.php) is in scope now or later; that's his physical-process call, but it does NOT block the base-box MVP.

### 🎯 REPACK TARGET — FINAL RECONCILED RULING 2026-06-12 (operator ground-truth folded; PM live re-probed)
**Operator ground-truth (Kouros, exact):** Model-B physical assumption "team picks loose until empty then opens a box" — "it is right yes but not always the case. Sometimes unpacking a box is easier (split 2×12 into two orders). But usually yes, **priority is given to cages**." ⟹ eshop loose comes PRIMARILY from the **-X cage** (the loose pool), box-open is the SECONDARY/convenience path. So the operator-correct loose pool the bundle depletes is the **cage (-X)**, NOT the single (BU) Model-B ruled. This does NOT overturn the base-box MVP target — it confirms the **Phase-2 end-state**. The two are SEQUENCED, not in conflict.
- **TWO-HORIZON MODEL (canonical, reconciled):**
  - **MVP-NOW (target = BASE BOX):** unchanged. Cage is STILL an unfed/unanchored phantom-negative trap TODAY — PM re-probed live 2026-06-12: 8 core -X cages (upp=1027) have pkg_evt 0 (except DOA-X=2), anchors 0 (except DIV-X/ZEP-X=1). Base = 56 SKUs / 1776 evt / 162 anchors (fed+anchored). `scope='cage'` is HETEROGENEOUS — the event-rich rows (BAMB/WESTB/JASPF etc., upp=24/1) are CONTRACT/COLLAB beers parked in cage scope, NOT core eshop cages. So base is the only pool with stock behind it now.
  - **PHASE-2 END-STATE (target = CAGE -X):** the derivation-correct model Kouros's "cage-first" describes. bundle sale → deplete -X cage; box-open repack event → −1 base box + base_upp into -X cage (refill, the convenience/secondary path); cage FED by packaging once form-packaging.php gains a cage option (so -X production stops hiding in -B). **Single (BU) is RESERVED for a DIFFERENT pool** — taproom/bar loose pours (Lightspeed-grained), NOT the eshop loose pool. This corrects Model-B's "loose = single" identification: eshop loose = cage; taproom loose = single. Two distinct loose pools.
  - **GATE between them = the 06-15 cage anchor + the packaging-form cage option.** Until BOTH land, cage reads negative. After: flip the engine's draw target base→cage; box-open becomes a cage-refill leg.
  - **🚦 FLIP HAS THREE GATES, not one (clarified 2026-06-16 — "gate CLEAR" was imprecise, meant only gate 1).** G1 cage ANCHOR ✅ LANDED+CLEAN (migs 362/363, §CAGE ANCHOR LANDED). G2 cage FED FORWARD by the packaging form — form 'X' option SHIPPED (`91ce8c0`) so the feed CAN flow, but core -X cages still have ~0 production events (§200/§210): flipping depletion onto an anchored-but-unfed pool = honest-negatives (d9f7976 class). G2 needs forward production rows accrued, not just the form capability. G3 Shopify pickup-vs-shipped signal — ON HOLD (§148, team mid-Shopify-change); without it no sale routes to cage-vs-box at all = HARD external blocker. **⟹ The flip is PARKED on G2+G3. It is NOT parked on arithmetic (the flip is fully defined, HL-grained, 1027-free — see GRAIN note above). Do NOT dispatch a coder for the flip until G3 lands + G2 has forward feed.**
- **🔧 ENGINE/FG-LEG REWORK ASSESSMENT (PM-read the code):** MOST of the built code SURVIVES; the change is a TARGET REDIRECTION + ONE NEW LEG, not a rewrite.
  - **SURVIVES as-is:** the whole auto-propose→human-confirm scaffold (`inv_repack_events` proposals + `rq_repack_unresolved_bundle`); `ref_sku_aliases` composite resolution (C); recipe-NULL RQ routing (D); the bundle→(recipe,container) decomposition; the `<>'none'` scope-gate (cage passes it too); Σcards==Σphysique invariant.
  - **CHANGES at Phase-2 flip:** (1) draw target `_repack_find_base_box` → `_repack_find_cage` (resolve bundle→ -X by recipe+container); (2) NEW box-open leg = the refill (−1 base box + base_upp → cage), which is the SECONDARY path the engine PROPOSES when cage runs low (mirrors the running-balance auto-propose, just one altitude up — cage-low instead of single-low); (3) the FG eshop leg decomposes none-bundle → deplete CAGE (not single). The single-balance auto-propose logic (Model-B §176) GENERALIZES to a cage-balance — same chronological running-balance shape, different pool grain.
  - **🔴🔴 GRAIN OF THE FLIP — READ BEFORE CODING IT (reconciliation 2026-06-16, an orchestrator correctly refused on a phantom blocker; this resolves it).** The flip is **HL-grained, NOT bottle-grained — `1027`/bottles-per-cage is NEVER a divisor anywhere in the flip** (consistent with §171's "1027 used NOWHERE"). The phantom blocker: "to subtract 12 bottles from a 0.2171-cage-unit balance you need bottles-per-cage." FALSE. NEITHER leg converts bottles↔cage-units. Both conserve LIQUID in HL, the canonical dimension every SKU carries via `hl_per_unit`: (3) `none`-bundle sale → component units via `units_per_pack × recipe_id` (bundle's OWN count, 1027-free) → HL via component `hl_per_unit` → `cage_units -= drawn_HL / 3.38910` (3.38910 = the cage-unit's own `hl_per_unit`, line 421). (2) box-open → `+cage_units = (base_upp × box_bottle_hl_per_unit) / 3.38910`. **The cage's grain-in-bottles is undefined and STAYS undefined — the draw is expressed in HL, never bottles; the only scalar is `hl_per_unit` (canonical, populated, per-SKU incl. -V cuv §548).** The cardinal liquid-conservation invariant (§276a, cage XOR box) is conserved in HL because HL is the conserved quantity. ⟹ The "second gate = a defined bottles-per-cage conversion" is a NON-gate; it does not exist and the flip never needed it. The flip's REAL gates are the two below (line 212), neither a conversion scalar.
  - **NET:** ~70-80% of `app/repack.php` is target-agnostic plumbing that stands. The base→cage swap is a localized resolver + one added leg. Building base-box MVP NOW is NOT throwaway — it builds the scaffold, the habit, and the capture surface; Phase-2 reuses it.
- **🔴 SEQUENCING RECOMMENDATION = OPTION (b), the capture/depletion split.** BUILD NOW: the CAPTURE (the `inv_repack_events` ledger + operator panel to log box-opens, the auto-propose, the RQ routing, the alias/recipe resolution) targeting BASE for the proposal arithmetic. HOLD: do not flip the live FG-depletion target to cage until 06-15 (anchor) + the packaging-form cage option land. Capture is immediately useful (builds the operator habit + the ledger of box-opens) and cannot phantom-negative because it's propose-not-deplete. Why NOT (a) "build cage-first now": it reads honest-negatives for 3 days on an unfed pool = operator sees broken stock, erodes trust, for no gain. Why NOT (c) "wait 4 days build all at once": loses the capture-habit head-start + bundles the cage-feed form change with the engine in one big-bang (harder to review/gate). (b) decouples cleanly: capture proves out on base, depletion-target flips to cage when cage is real.
- **🟡 form-packaging.php CAGE OPTION belongs in THIS arc but as a SEPARATE, PARALLEL build item — NOT inside the repack engine commit.** It is the thing that makes -X production REAL (stops cage hiding in -B; FORMAT_SUFFIXES lacks 'X' — §194 project arc). It is a packaging-FORM change (Salle des Machines / capacités altitude), architecturally distinct from the sales-side repack engine. Sequence it to LAND BY 06-15 alongside the anchor so the cage-feed is live when the Phase-2 target-flip happens. It can be built in parallel now; it does not gate the base-box MVP capture.
- **THE ONE THING TO PUT TO KOUROS (he owns the 06-15 timing):** "Confirm the 06-15 cage count proceeds, and approve sequencing: we build the repack CAPTURE now (proposes against base box, no live cage depletion), add the cage option to form-packaging.php in parallel to land by 06-15, then flip the FG-depletion target base→cage once the 06-15 anchor + cage-feed are live. Single (BU) stays the TAPROOM loose pool, not eshop — confirm." **What PM RULES without him:** Model-B's "eshop loose = single" is SUPERSEDED → eshop loose = cage (Phase-2), single = taproom loose; the engine is mostly target-agnostic so base-MVP is not throwaway; option (b) capture-now/depletion-later is the build call; the cage form-option is a separate parallel item by 06-15. Only the physical-process timing + the eshop-vs-taproom loose-pool split need his nod.

### 🟢 ESHOP-BUNDLE DECOMPOSITION WIRING — PREP DONE 2026-06-16 (flip STILL PARKED on G3; this is data-resolution prep, NOT a live depletion change)
**Session 2026-06-16 — operator gave explicit decomposition rules → 3 of the Bucket-A recipe-NULL codes (§(D)) now RESOLVE. NO flip coder dispatched. These rows are PREP: they make the codes decomposable, but the repack/depletion flip onto cage does NOT run live until G3 (Shopify pickup-vs-shipped) clears (§213 — G1 cleared 06-15, G3 never did).** The 3 use the engine's NATIVE single-bundle path (`bundle.recipe_id` + `units_per_pack` → `_repack_find_base_box` finds the scope=base bot box), NOT `ref_sku_aliases`:
- **COLLAB12 (`ref_skus` id 300):** was `recipe_id=NULL` → SET `recipe_id=31` (DGD / Galactic Drift). Engine: 12 bottles → opens 1 DGDB (24-box), 12 loose. `last_modified_by='web'`.
- **COLLAB4PACK (id 302):** SET `recipe_id=31` (DGD). 4 bottles → opens 1 DGDB, 20 loose. `last_modified_by='web'`.
- **ESTB12 (id 344, NEWLY CREATED this session):** had NO `ref_skus` row at all (order lines `sku_id_fk` NULL). Created mirroring MOOB12 — `recipe_id=33` (Estafette), `format_id=2` (B12 bot), `upp=12`, `hl_per_unit=0.03960` (=12×0.0033), `stocktake_scope='none'`, `last_modified_by='web'`. The **33 historical eshop order lines were backfilled** (`sku_id_fk=344`). Operator rule: "ESTB12 takes 12 bottles out of EST4." Verified: unresolved-codes probe now shows all 3 gone.
- **🔴 ROTATION CAVEAT (operational, COGS+beer-tax-bearing):** COLLAB12 / COLLAB4PACK (and the pre-existing COLLAB24→DGDB alias) ALL point at DGD (recipe 31) = the CURRENT collab. **The COLLAB* codes ROTATE per-collab** (operator-stated). When the collab changes, ALL THREE COLLAB codes must be RE-POINTED to the new recipe. `last_modified_by='web'` PROTECTS them from ingest overwrite — which means a stale `recipe_id` persists SILENTLY until manually re-pointed (that's the risk to watch: a wrong-beer COGS/tax until someone re-points). The proper long-term fix is the **`ref_sku_collab_temporal`** mechanism the alias notes already reference (§353; SCD2-style time-boxed collab→recipe binding).

### 🔴 CRITICAL DATA POINT — PBD ≠ PD8 (prevents a future wrong alias; 2026-06-16)
**I nearly aliased PBD→PD8 (id 137) this session; rolled it back. They differ by EXACTLY ONE beer — PBD must NEVER be folded into PD8.**
- **PBD (DISCONTINUED — PD8 replaced it; `ref_skus` id 294, left with NO alias / NO slots per operator "no stress"):** 8 BU singles = Zepp / DoubleOat / Diversion / Moonshine / Embuscade / **ESTAFETTE** / Stirling / Speakeasy.
- **PD8's actual 8 slots swap Estafette → ALTERNATIVE (recipe 6).** One-beer difference → aliasing PBD→PD8 would mis-decompose every historical PBD as an ALT pack (wrong COGS + wrong beer tax).
- **If historical PBD replay is ever needed:** give PBD id 294 its OWN 8 composite slots (recipes **57,30,25,44,32,33,52,51**; `member_format_id=5` BU; `units_per_recipe=1`). Do NOT reuse PD8's slots.

### 🟢 BUCKET CLASSIFICATION of the remaining recipe-NULL / title-only eshop codes (2026-06-16)
Refines §(D). Three buckets of leftover sales codes:
- **Bucket-A — recipe-resolvable via the native single-bundle path** (recipe_id + upp; engine fans out): COLLAB12 / COLLAB4PACK / ESTB12 — ✅ ALL 3 RESOLVED this session (above). Bucket-A is now EMPTY of unresolved codes per the probe.
- **Bucket-B — title-only, NO `sku_code`, NOT alias-seedable (an ingest-LINKING gap, not a resolver gap):** **Pack de Noël** (3 promo variants, ~141 qty), **Beer Des Rosses**, **Pack Explorer**. These have no SKU code to alias against; they need the order-line→SKU LINK fixed upstream at ingest before any decomposition can apply (refuse-don't-NULL — route to `rq_repack_unresolved_bundle`, never NULL-deplete).
- **Bucket-C — correctly NON-beer, NO repack (leave alone):** VER25 / VER50, TSHIRT* / HOODIE*, Gobelets Réutilisables, Tireuse Portable, Caution, Carte E-cadeau, stray "fiche mois de mars". These are merch / deposits / non-stock — they SHOULD NOT decompose to a beer; the "non rattaché" bucket is their correct home.

## 🟢 ④ LIGHTSPEED POS / TAPROOM INTEGRATION — PM INVESTIGATION 2026-06-11 (pre-build brief; nothing built)
**Context:** Kouros wants to wire maltyweb to the taproom's Lightspeed POS (paid custom API, monthly fee; access via a Monday.com intake form). PM-briefed the architectural fit. Live DB re-probed 2026-06-11.

**🔑 LOAD-BEARING FINDING — TAPROOM TODAY IS BC-DERIVED, MONTHLY, PACKAGING-SKU-GRAINED — NOT POS receipts.** `inv_sales_bc` channel='taproom' = **208 rows, period='YYYY-MM' (monthly), 2026-01→2026-05**, lines carry packaging-SKU descriptions (e.g. `sku_code='ALTB'`, `'Alternative Box 24 Btls 33cl'`, `qty_invoiced=31`, `unit_of_measure='24B'`, `sales_amount_chf`, `customer_no='3822' 'La Nébuleuse TAP/SHOP'`). It comes from the monthly BC GL export (`ventejanvier.xlsx`…), NOT from a POS feed. **So today's taproom signal is a coarse monthly aggregate in WHOLESALE packaging units (24-boxes), with a one-month lag** — exactly the R5 "taproom staleness" + the §0a6 single-unit-SKU stopgaps (DGDBU etc.) the team has been hand-patching. **Lightspeed would REPLACE this coarse monthly BC-taproom feed with per-receipt, per-day, single-unit-grained sales** — the real upgrade.

**WHERE LIGHTSPEED SALES LAND (PM ruling — SoT discipline):**
- **A NEW raw append-only table `pos_sales` (+ `pos_sales_lines`), one row per receipt / per receipt-line. RAW, append-only, idempotent.** This is the BC-taproom analogue but at receipt grain. It is NOT `ord_orders` (that's the B2B/WeeklyOrders booked-order lane — taproom is settled-at-till, not a booked order; mixing them = the same parallel-store/double-count smell). It is NOT `inv_consumption` (that's the computed produce/sale consumption ledger — a downstream WRITE target, never an ingest landing). Mirror the `inv_deliveries`/`inv_sales_ledger` discipline: `dedup_key` UNIQUE (= Lightspeed `saleLineID` or `saleID:lineIdx`), `row_hash`, `source_file`/`imported_at`, `sku_id_fk` resolved at ingest (refuse-don't-NULL → unresolved lands in a "non rattaché" bucket, surfaced, never dropped).
- **It then feeds the SAME computed views the BC-taproom channel feeds today** — `fg_stock_compute()` taproom leg + (Phase 2) `inv_consumption` sales_derived. **The cleanest cutover: make the existing taproom *channel* read from `pos_sales` instead of (or in addition to, by date) `inv_sales_bc` channel='taproom'.** This is a strangler swap at the seam, NOT a new derivation path. The Stage-2 cage/COGS engine (④-Phase 2) is UNCHANGED — it just gets a finer, daily, single-unit input.
- **Derivation-tree placement:** Lightspeed is a SALES-SIDE leaf feeding the FG-depletion + COGS legs. It derives DOWNSTREAM of: `ref_skus` (must map Lightspeed product → our SKU), `ref_recipes` (via sku→recipe_id_fk for liquid+packaging decomposition), `ref_customers` (taproom walk-ins → a generic taproom customer, like `customer_no='3822' TAP/SHOP`). It does NOT touch commissioning/capacities. It is the same altitude as the Shopify `inv_sales_orders` feed — a second direct-sales channel.

**WHAT WE ACTUALLY NEED FROM LIGHTSPEED (endpoints/objects — to close taproom COGS+sales):**
- **PRIMARY = Sales / receipts (READ).** Per-receipt line items: product/item ID, qty, unit price, tax, discount, timestamp, payment type, receipt/sale ID, completed/voided flag, refund linkage. This is the whole loop-closer — daily single-unit sell-through per SKU → FG depletion + COGS. (R-Series/X-Series: `Sale` + `SaleLine`; K/L-Series restaurant: orders/checks. Get LINE granularity, not just totals.)
- **SECONDARY = Products / catalog (READ, once + on change).** To build the Lightspeed-product-ID → `ref_skus.id` map. Pull `itemID`, `description`, `customSku`/`manufacturerSku`, `UPC/EAN`, price, category. The mapping table is the integration's spine (see next block).
- **Refunds / voids (READ).** Returns physically re-enter FG; must net (the `inv_sales_ledger` doc_type credit/return_receipt convention — a refund = a sign-flipped depletion). Do NOT ignore.
- **Inventory (SKIP / DO-NOT-SYNC).** maltyweb FG stock is canonical and single-homed (`fg_stock_compute()`); do NOT let Lightspeed's own inventory counts become a 2nd FG store (parallel-store divergence). We may later PUSH our number to Lightspeed for the staff's benefit, but that's a write-back nicety, not the loop.
- **Customers (MOSTLY SKIP).** Taproom = anonymous walk-ins → one generic taproom customer. Only relevant if they run loyalty/named tabs.
- **READ-ONLY for the loop. No writes needed to close taproom COGS.** (Same disarmed-by-default stance as the Shopify write-back worker, §0a11 Phase 2B.) A future inventory-push to Lightspeed would be the only write, and it's optional.

**SKU-MAPPING CONCERN (the spine — flag hard to Kouros):** Lightspeed product IDs must reconcile to `ref_skus` so a taproom sale decrements liquid + packaging and flows into COGS. **This is exactly the `ref_mi_aliases` / `ref_sku_aliases` discipline applied to a POS catalog.** Build a `pos_product_map` (Lightspeed itemID → `ref_skus.id`, with the Lightspeed customSku/description carried for audit). **NEVER guess the mapping from price or name pattern** (house rule — a wrong SKU map silently mis-costs COGS + mis-routes beer tax). Seed it from an explicit operator pass over the Lightspeed catalog (the same "surface unresolved, operator decides" pattern as triage). Taproom sells SINGLE units (BU bottle / CU can / draft pour P25/P50) and 24-boxes — the §0a6 single-unit-SKU work (DGDBU, scope='single', contract-beers-rescoped-out) was the GROUNDWORK for exactly this: those single-unit SKUs are the targets a Lightspeed line maps to. Draft pours map to `-P25`/`-P50` SKUs (keg-share BOM, Rule #4). **Unresolved POS lines = "non rattaché" bucket, visible, never a NULL sku_id_fk depletion.**

**WHICH LIGHTSPEED PRODUCT — UNKNOWN, MUST CONFIRM WITH KOUROS BEFORE THE MONDAY FORM.** Lightspeed sells Retail (R-Series legacy / X-Series new) vs Restaurant (K-Series / L-Series/Order Anywhere) vs Golf — **different APIs, different objects, different intake.** A brewery taproom that pours pints + sells bottles-to-go could be EITHER a retail POS or a hospitality/restaurant POS. **We do not currently know which one La Nébuleuse runs** — this determines the endpoints, the API tier to request on the Monday.com form, and the receipt object shape. **Confirm the exact product + version before requesting API access** (asking for the wrong series' API = paying a monthly fee for the wrong endpoints).

**⚠️ DOUBLE-COUNT / GATING WARNINGS (relay to Kouros):**
1. **Taproom XOR everything else (the cardinal R1 invariant).** A bottle sold at the till must NOT also be counted via BC-taproom (the monthly mirror) NOR via Shopify pickup NOR via WeeklyOrders/`ord_orders`. **At cutover, the BC-taproom channel and the Lightspeed feed cover the SAME physical sales** — pick ONE per date range (PM rec: Lightspeed from its go-live date forward; BC-taproom for history before). Do NOT sum both.
2. **Shopify pickup-at-brewery vs taproom till.** A Shopify order picked up at the brewery (the §0a11 pickup/delivery classification, `fulfilment_mode`) is NOT a taproom POS sale — but if the customer instead just walks in and buys, that's Lightspeed. Keep the channels disjoint; the pickup-signal hold (Phase 2 gate) is about Shopify, independent of Lightspeed.
3. **④-Phase 2 cage/COGS engine is still gated** on the cage anchor (count started 2026-06-15) + the Shopify pickup-signal. Lightspeed does NOT un-gate Phase 2 by itself, but it makes the taproom leg MUCH cleaner (daily single-unit vs monthly 24-box) and is arguably the better first integration to land — it directly improves the taproom COGS the cage engine will consume.
4. **Monthly→daily cadence is itself a win** — R5 "taproom staleness" (`inv_sales_bc` monthly → intra-month taproom depletion invisible, physique drifts up) is LARGELY SOLVED by a live POS feed.

**PM RECOMMENDATION (one-liner for Kouros):** Worth doing — Lightspeed is the right way to replace the coarse monthly BC-taproom aggregate with real daily single-unit sell-through. Before requesting API access: (a) confirm WHICH Lightspeed series the taproom runs (Retail vs Restaurant); (b) request READ on Sales/receipts (line grain) + Products + Refunds only; (c) land it as a raw `pos_sales` append-only table that feeds the EXISTING taproom channel (strangler swap at the seam), with a `pos_product_map` seeded by an operator catalog pass (never guess); (d) hold the XOR — Lightspeed forward, BC-taproom for history, never both. EQUIP for the eventual build: sql + coder (+ ui for the Ventes-directes/Tap&Shop surface; parser-coder NOT needed — this is a structured API, not OCR).

## 🟢 DIRECT-SALES STOCK CONSUMPTION TRACKER — HIGH-LEVEL DESIGN (= the Stage-2 sales/eshop consumption matrix, now sharpened by cage-vs-box channel routing; build AFTER all BOMs validated)
**Identity:** this is NOT a new concept — it IS the long-planned **Stage-2 sales/eshop SKU consumption matrix** (derivation-tree-and-schema.md §Stage-2; sku-decomposition-tree.md §-X-cage-interaction). The walk gives it its missing routing rule: **cage-pull-with-B-box-fallback**. Recorded as ONE build, not two. **NB 2026-06-09: SPLIT into ④-MVP (visual, now) + ④-Phase 2 (cage/COGS, deferred) per the operator-decisions block above — this high-level design IS ④-Phase 2.**

**INPUTS:**
- **Shopify orders** (direct — DISREGARD the BC 'Eshop' account; it mirrors Shopify → double-count). Each order = a set of (sales SKU × qty) + a **pickup-vs-shipped flag** (⚠️ STILL the load-bearing dependency — without it, cage-vs-box and consolidation can't be computed; the Shopify feed MUST carry it).
- **Taproom / BC 'privé' (acct ~1080)** sales lines.
- **Cage (`-X`) stock levels + Box (`-B`/`-B12`) stock levels** per beer (cage basis = packaging-form fills; box basis = packaging output less depletion).

**ROUTING LOGIC (per direct-sale line):**
1. **Eshop/Shopify shipped** → pull loose bottles from the beer's **`-X` CAGE while cage stock > 0** (draw-down, NO fresh bottle/cap/label — already booked in `-X`); **FALLBACK if cage empty → pull from a `-B` BOX** (consume the box + its box-MIs, unbox). + ADD eshop outer pkg (order-composition dependent, see below).
2. **Shopify pickup-at-brewery OR BC privé (1080)** → the existing "unboxed" path (loose-unit consumption); no eshop shipping carton.
3. **Taproom** → pull from **`-B`/`-B12` BOX** → consume the box + its MIs.
4. **Draft pour (P25/P50 at taproom)** → keg-share (Rule #4 below); channel-exclusive with -F keg sale.

**ORDER-CONSOLIDATION PACKING (eshop shipped only):** group the order's bottle count, pack into FEWEST `PKG_BOX_24_BTL_BLANC` (24) then `PKG_BOX_12_BTL_BLANC` (12) then carton12+renforts for a lone 12. Emit the consolidated outer-pkg MIs + `LOG_SCOTCH_ESHOP` ONCE per box, NOT per SKU. This is the order→box bin-packing the per-SKU BOM structurally cannot express.

**MI EMISSION / WRITE TARGET:** writes to **`inv_consumption`** (the canonical produce/sale consumption ledger) with `source_event='sales_derived'` for the eshop outer-pkg (beer-agnostic, mirrors today's `derive-eshop-consumption.ts`) and a NEW disposition for the cage/box draw-down of intrinsic content (beer-specific, per-recipe). The cage→0 transition is itself a packaging/RM event (opens a new cage). Month-close snapshots the residual matrix → exact RM consumption + COP/COGS. This is the SAME `inv_consumption` the brewing/packaging forms feed — ONE ledger, no parallel store.

**TABLES IT NEEDS (mostly EXIST — populate/wire, do NOT green-field):**
- `ref_sku_composite_slots` (the theoretical per-recipe map — built) + `ref_sku_aliases` (sales-SKU→canonical — built).
- A **per-beer cage/box residual state** table (the Stage-2 matrix's stock ledger) — NEW; keyed (period × beer × stock_state{cage,box} × measure), parallel to `cop_monthly`/`cogs_monthly`. This is the only genuinely new table.
- `inv_consumption` (write target — exists).
- `ref_skus.is_direct_sales` (mig 211 flag — exists; gates which SKUs the tracker routes).
- Shopify feed + pickup flag (sales-commercial-surface/README.md — the feed exists; the pickup flag is the gap to add).

**CARDINAL INVARIANTS (the things that silently corrupt COGS — hold them):** (a) a bottle is produced+consumed ONCE — cage XOR box, never double; (b) `-X`-pulled multipack = DRAW-DOWN, never re-adds bottle/cap/label; (c) order-level eshop pkg emitted per consolidated BOX, never per SKU; (d) a mixed deco pack draws from MULTIPLE per-beer cages or loose units, never one cage; (e) DISREGARD BC 'Eshop' acct (Shopify is the source).
