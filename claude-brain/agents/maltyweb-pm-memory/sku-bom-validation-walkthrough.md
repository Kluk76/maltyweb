# SKU-by-SKU BOM Validation Walkthrough — framework + canonical RULES + 📒 LIVING per-SKU RECORD (THE keystone correctness arc)

> 📒 **THIS FILE IS THE LIVING STEWARD RECORD** for the BOM-validation walkthrough (Kouros standing directive 2026-06-07, NON-NEGOTIABLE: "we cannot forget about this anymore"). EVERY rule AND EVERY per-SKU decision, as each SKU/format/recipe is validated live, gets APPENDED here (see §LIVING PER-SKU RECORD at the bottom) — 100% capture, no exceptions. Cross-referenced with Kouros's project memory `project-bom-validation-arc.md` (his side of the arc). The PM owns this file; append on every walkthrough turn.
>
> Load when: walking SKUs to validate liquid+packaging+combination BOM with the operator; settling the liquid-composition model; building the liquid compiler; touching the 3 packaging buckets (branded / eshop-scotch / neither); the eshop-scotch altitude; the missing-17-liquid SKUs; or the BOM Review surface additions for this validation. Framework PM-blessed 2026-06-07 (Kouros declared this "the single most important validation we need"). Worked example = Alternative (recipe_id=6). **PACKAGING walk STARTS NOW (live, simplest-format-first); LIQUID (Phase C) gated on F2 compiler — F2 NOW FULLY UNBLOCKED (rule ① + basis BOTH settled 2026-06-07).** Rule #1 + buckets + BASIS all CONFIRMED by Kouros 2026-06-07 (see STATUS). Cross-ref: packaging-bom-model.md (resolver altitudes, scotch model), sku-decomposition-tree.md (F2 liquid-source — NOW SETTLED here), recipe-ingredients-drift-surface.md (observed>ref_recipe precedence).

## WHY THIS ARC
Precise financials are impossible without per-SKU BOM truth. The machinery (bindings/choices/templates/composite-slots/recompile/BOM-Review surface) is ALL SHIPPED — what's missing is (a) a SETTLED liquid-composition model so the liquid side can be REBUILT (F2 was deferred — the existing 1280 `source='Brewing'` liquid rows are a pre-canonical BSF-import built observed-only with NO gap-fill), (b) the eshop-scotch bucket modeled IN the production BOM at the right altitude, (c) this canonical RULE doc, (d) a few review-surface columns. Validate liquid + packaging + their combination, fix the DB at the right altitude, persist the per-SKU truth in DB + the RULES in memory.

## 🟢 CANONICAL RULE #1 — LIQUID-COMPOSITION MODEL (SETTLES F2; rule CONFIRMED + BASIS DECIDED by Kouros 2026-06-07 — fully settled)
**Per-SKU liquid line = `Σ over ingredients of (liquid_basis_per_hl(recipe,mi) × ref_skus.hl_per_unit)`** where the per-HL basis is a TWO-SOURCE precedence:
1. **OBSERVED-ONLY for malt + hops — NEVER gap-filled from ref_recipe (REFINED + AUTHORITATIVE, Kouros 2026-06-07).** Basis = the observed aggregate of `(observed_qty ÷ cool_final_volume_hl)` for that recipe, aggregated via the **VOLUME-WEIGHTED TRAILING AVERAGE** (DECIDED — see basis block below). The operator has ALWAYS entered malt/hops on the brewing + dry-hop forms → **observed IS the truth for malt/hops, full stop.**
   - ⛔ **A recipe-listed hop/malt that is ABSENT from observed brewing/dry-hop data is NOT added to the BOM.** It is an **intentional brew-day deviation** → it becomes a **KPI flag (recipe-vs-actual drift) — a tracked feature, NOT a correction.** The liquid compiler MUST NOT pull ANY MALT_/HOPS_ line from ref_recipe that isn't in observed. (Sharpens [[feedback_observed_malt_hops_canonical_no_gapfill]].)
   - Observed drift is INTENTIONAL + canonical (ALT's 5 observed hops vs recipe's 2 = correct; the compiler emits 5, and the 2 recipe-only-or-extra appear on the drift KPI, not the BOM).
2. **ref_recipe GAP-FILLS only NON-malt/NON-hops ingredients** — minerals / process-aids / phosphoric / finings / yeast-vit. The observed parsed `category` ENUM is `malt|hops_kettle|hops_dry` ONLY → it PHYSICALLY cannot carry PROC_PHOSPHORIQUE, salts, etc. For these: **observed wins where the form captured it; ref_recipe fills where it didn't.** This is the ONLY place a theoretical substitute for observed is allowed. **This slice is cost-AFFECTING** (gate-4 proved +4158 CHF/month rides on it). ALT's dropped phosphoric = this exact defect (liquid built observed-only, NO gap-fill pass).
   - **Finings (Clarex / Dehaze / Nagardo) are inconsistently entered by the operator** → their inclusion is decided **PER-BEER during the walkthrough**, NOT by a blanket gap-fill rule. (They are a gap-fill candidate, but the operator confirms presence beer-by-beer.)
3. **Zero-observed beers fall FULLY to ref_recipe.** ALT has 0 v2 brewing records → its ENTIRE liquid basis = `ref_recipe_ingredients` (all 7 lines incl. phosphoric — incl. its malt/hops, since there's no observed to win). Precedence degrades gracefully: observed-where-present (malt/hops + captured non-malt/hops) → ref_recipe-gap-fill-for-uncaptured-non-malt/hops → ref_recipe-entirely when observed empty.
4. **Dimension discipline:** canonicalize WITHIN dimension (mass kg↔g, volume l↔ml), NEVER cross. Observed is mass-only; volume MIs (ml phosphoric) only ever come from ref_recipe. 1000× phantom drift is the #1 risk on each axis (sku-decomposition-tree §STEP-③ history = exactly this bug).

**The rule, stated once:**
> For malt/hops: `liquid_basis_per_hl = observed_aggregate_per_hl IF observed exists FOR THIS RECIPE; ELSE ref_recipe_ingredients.qty_per_hl (zero-observed beer only)`. **A recipe malt/hops line with NO observed counterpart is NEVER on the BOM — it is a drift KPI.** For non-malt/hops: `observed_per_hl IF captured; ELSE ref_recipe_ingredients.qty_per_hl IF present; ELSE absent (finings: per-beer operator call)`. Then `qty_per_unit = basis_per_hl × ref_skus.hl_per_unit`.

### ✅ BASIS AGGREGATOR — DECIDED by Kouros 2026-06-07 = VOLUME-WEIGHTED TRAILING AVERAGE (option a, the proper one)
For a beer's standard per-HL malt/hops/etc., the observed aggregator is the **VOLUME-WEIGHTED TRAILING AVERAGE**, defined as:
- **Window:** last ~5–8 brews OR trailing 12 months, **whichever yields ≥3 brews.**
- **Weighting:** each brew's per-HL contribution is weighted by that brew's **batch HL** (available in `bd_brewing` → feasible from the start). NOT a flat mean, NOT a median.
- **Outlier rejection:** reject brews ~2 MAD from the median before averaging.
- **Recompute cadence:** monthly.
- **⚠️ DISCONTINUITY GUARD (added 2026-06-07, SPY walk):** the trailing window must NEVER blend across a **liquid-discontinuity boundary** — seasonal vintage changes AND **DK era-changes** (DK = "By Danny Khezzar": SPY had a massive recipe change at the DK switch; DIB's peach-tea started there). Window starts at the most recent discontinuity, even if that yields <3 brews (then take what exists; if 0, fall to ref_recipe). Same guard class for both consumers (BOM cost + drift aggregator).

**⚠️ COUPLING — THIS SAME STATISTIC FEEDS BOTH consumers (Kouros's explicit note):** (1) the `compile_sku_bom_liquid` BOM-cost basis (F2), AND (2) the recipe-vs-actual DRIFT aggregator. The drift view's prior **"median of last 5" basis is SUPERSEDED** — both records now use volume-weighted-trailing. Any drift-surface build must read the SAME aggregator, not a parallel statistic (a divergent basis between the two = a one-fact-two-stores smell). → also recorded in recipe-ingredients-drift-surface.md drift-view section.

**F2 IS NOW FULLY UNBLOCKED** — rule ① (composition precedence) + the basis aggregator are BOTH settled → `compile_sku_bom_liquid` can be dispatched. (This supersedes the old "median of last-5" that Rule #1 previously stated as settled, and resolves former open-call #1/#4.)

### ✅ DRY-HOP OBSERVED BRANCH — SHIPPED 2026-06-07 (commit `888473b`, maltyweb)
`compile_sku_bom_liquid` now includes a **third observed branch** for dry-hop additions:
- **Root cause fixed:** 100% of dry-hop was silently dropped because `bd_brewing_ingredients_parsed` `hops_dry` rows carry `source_table='bd_fermenting'` and their `source_id` → `bd_fermenting_v2.id` (NOT `bd_brewing_ingredients_v2.id`), so the §2b JOIN always missed them.
- **Two dry-hop source paths now loaded:**
  - §2b2 — v1 path: `bd_brewing_ingredients_parsed` `hops_dry` JOINed to `bd_fermenting_v2` via `bip.source_id = bf.id`; `GROUP BY (recipe_id, batch, mi_id)` anti-fan-out.
  - §2b3 — v2 path: `bd_fermenting_v2` `event_type='DryHop'` with `dh_mi_id_fk`; same GROUP BY.
  - §2b4 — merge: **v2 wins per batch** (never mix); v1 fills remaining batches.
- **Stage tags:** all brewhouse lines tagged `stage='brewhouse'`; dry-hop lines `stage='dry_hop'`. `proposedByRecipe[recipe_id]` is now `['brewhouse'=>..., 'dry_hop'=>..., 'coverage'=>...]`.
- **Composite keying:** `'bh:<mi_id>'` / `'dh:<mi_id>'` allows the same hop (e.g. CASCADE) in both kettle and dry-hop stages without PHP array collision.
- **Floor guard** extended to `$dhMerged` (same allowed-batch set as brewing branches).
- **Sanity check** per MI: 0–1500 g/HL warning via `error_log` (DOA/MOO total >1500 per recipe = 6 varieties summed, individual MIs all within range — verified clean).
- **HARD CONSTRAINT verified:** `ref_sku_bom` 2135 rows / 2250.003568 CHF byte-identical after dry-run.
- **Gates all passed:** (a) zero writes ✅  (b) CHF/HL present per recipe ✅  (c) no double-count ✅  (d) SPY floor 2025-10-07 ✅  (e) all lines tagged ✅  (f) DIV/EST=0 dry-hop ✅  (g) ALT PROC_PHOSPHORIQUE via recipe_gapfill ✅
- **Quantification (g/HL, volume-weighted trailing):** ALT 289 g/HL, EMB ~1118 g/HL (8 vars), DOA ~2103 g/HL (6 vars), MOO ~1068, STI ~670, SPY ~601, ZEP ~1098.
- **Coverage split per recipe:** ALT/DIV/EST/ZEP = v1-only or no-HL; EMB/DOA/STI/SPY/MOO = v2 data available; newer recipes = v2-only.
- **EST dry-hop:** 9 v1 rows exist but no `bd_brewing_cooling` HL data for those batches → correctly excluded (can't compute g/HL without volume).
- **NEXT STEP (operator):** review `data/sku-bom-liquid-preview.json` and gate the `--apply` (which will write the proposed liquid rows into `ref_sku_bom`). The apply path is still `--apply NOT IMPLEMENTED` in the CLI — must be implemented before live write.

**Composites:** each member's liquid derives from that MEMBER's OWN per-HL basis (rule above) × `slot_hl`, NOT a flat re-read of ref_recipe. This SUBSUMES the old F2 recommendation ("derive each member's per-HL from its own single-SKU liquid") — now there is ONE rule for solo + composite.

**REBUILD DECISION = YES, rebuild ref_sku_bom liquid for ALL SKUs + fill the 17 missing + retag `bom_source='liquid'`.** Justification:
- The 1280 existing liquid rows = BSF-import, observed-only, NO gap-fill → EVERY beer with a phosphoric/mineral/process-aid in its recipe has the ALT defect (systematic COGS understatement, not a one-off).
- 17 SKUs brewery-wide have packaging rows + ZERO liquid rows (-X cages, -B12s, -4PBs, a few -BU; ALT-X + ALTB12 among them) → silent COGS holes (sell at zero liquid cost).
- The existing brewing rows are mistagged `bom_source=''` not `'liquid'` → blocks a clean DELETE-target for a safe rebuild-in-place; retag is a prerequisite anyway.

**SEQUENCING GATE (load-bearing):** the rebuild needs a LIQUID COMPILER that DOES NOT EXIST yet. `compile_sku_bom_packaging` is packaging-only by HARD gate (`packagingOnly=true` throws otherwise; F2 deferred). So:
1. Settle the rule (this ruling; Kouros sign-off).
2. **Build `compile_sku_bom_liquid`** as a sibling in `app/sku-bom-compile.php` — reads the 2-source precedence, emits `bom_source='liquid'`, **refuse-don't-NULL on unresolved MI** (RQ row, NEVER a NULL line — the operator's `mi_id=NULL` defect must NEVER be re-created), mirrors the packaging compiler's per-SKU transaction + snapshot-before + parity gate. Equip ui+sql+coder.
3. **Dry-run full rebuild to errors=0**; prove cost-deltas explained (phosphoric/mineral RECOVERIES = expected POSITIVE deltas; malt/hops deltas should be ~0 since observed already won — flag any that aren't).
4. Apply, retag `'liquid'`, fill the 17.

⛔ **The walkthrough DEPENDS on this — do NOT walk on the current BSF-import liquid (you'd validate data you're about to replace). Order: settle rule → build liquid compiler → THEN walk SKUs.**

## 🟢 CANONICAL RULE #2 — THE THREE SCOTCH/STICKER BUCKETS (operator-authoritative, Kouros 2026-06-07 — CONFIRMED EXHAUSTIVE)
**Bucket question CLOSED by Kouros 2026-06-07: "there isn't branded eshop items — eshop items are all generic for now." NO format is both branded AND eshop → NO 4th bucket. The 3 buckets below are exhaustive.** (Resolves former open-call #3.)
Branded sticker + branded scotch are NOT "all bottle recipes" — they belong to specific formats. THREE buckets:
1. **BRANDED (sticker + branded scotch) → ONLY `-B` (24-pack BOTTLE box, fmt 1) + `-C` (24-pack CAN box, fmt 7).** `-C` is the can parallel of `-B` (ZEPC is the only `-C` today). Scotch A/B per recipe via `uses_branded_scotch` (see SCOTCH MODEL in packaging-bom-model.md): A=branded scotch alone, B=PKG_SCOTCH_TRANSP + PKG_STICKER_[beer].
2. **ESHOP SCOTCH (generic eshop scotch ONLY, NO branded sticker) → `-B12` (eshop/taproom pack, NOT wholesale), `-4PB` (4-pack-bottle), Pack-Découverte composites, + other eshop packs.** They're packed in mixed eshop boxes / renfort eshop. Carry **`LOG_SCOTCH_ESHOP` (id 201)**, NO branded sticker. **ALTB12 currently carrying `PKG_STICKER_ALT` is a DEFECT** (wrong bucket — clear it, replace with eshop scotch).
3. **NEITHER → everything else: `-4` (6×4 carton), `-X` (cage), `-BU`/`-CU` (singles), `-F` (keg), draft pours.** No scotch/sticker slot at all (4-carton's scotch slot was DELETED mig 141; -X carries contents only).

## 🟢 PACKAGING RESOLVER ALTITUDE MAP (where each component class lives — edit at altitude, NEVER the resolved ref_sku_bom row)
Resolver precedence per slot HIGHEST→LOWEST: `ref_sku_packaging_choices` (per-SKU Tier-1) → `ref_recipe_packaging_bindings` (per-recipe, role-keyed) → template default (`ref_packaging_bom_templates` + `ref_packaging_items`). (DIV33C is the cautionary tale: editing the resolved row alone gets clobbered by recompile.)

| Component class | Altitude | Table / mechanism |
|---|---|---|
| **Body + cap** (PKG_BOT_PIVO, PKG_BOT_CROWN_CAPS, can body/lid) | **Template default (FORMAT-generic)** | `ref_packaging_items` slot (`bottle`/`crown_caps`/`can`/`can_lids`) on the format's `ref_packaging_bom_templates` row; `default_mi_id_fk` fixed; container-layer VOLUME on this line. ← THIS is the "format layer" the body/cap resolve from. Admin-rare. |
| **Recipe art** (label, can-art, sticker, holder, outer_tray, scotch) | **Recipe binding** | `ref_recipe_packaging_bindings` role ENUM(`label,can,sticker,holder,outer_tray,scotch`); ONE row per (recipe,role) → fixes ALL the recipe's SKUs in that role |
| **Per-SKU divergence / white-label brand** | **Per-SKU choice (Tier-1)** | `ref_sku_packaging_choices` (sku_id, slot_name, mi_id_fk) — DIV33C can body, BLA4 brand, ALTB12 eshop scotch |
| **Composite overwrap** | **Per-SKU choice on the composite** | `ref_sku_packaging_choices` slot `outer_box`/`scotch_eshop`/`verre` (PD8→PKG_PACK_DEC; PAL/PAC→PKG_CARTON12_ESHOP+LOG_SCOTCH_ESHOP) |

**Heuristic (= the EDIT-ALTITUDE DECISION RULE on bom-review.php):** wrong for EVERY SKU of this recipe in this role → binding. Just THIS SKU → choice. Structural for the WHOLE format → template/items (admin-gate). MI price/identity → ref_mi.

### ⚠️ ESHOP-SCOTCH ALTITUDE — THE PACKAGING GAP TO CLOSE (bucket 2)
`LOG_SCOTCH_ESHOP` (id 201) is TODAY modeled as a **Stage-2 / sales-fulfilment concern, EXPLICITLY NOT in the production BOM** (packaging-bom-model.md §LOG_SCOTCH_ESHOP). Kouros's ruling **PROMOTES it INTO the production BOM** for the eshop-pack formats. The mechanism already exists (PD8/PAL/PAC carry `scotch_eshop → 201` as composite choices). Extend it to -B12 + -4PB.
- **PM RECOMMENDATION (decision to put to Kouros):** model eshop-scotch as a **FORMAT-LEVEL TEMPLATE DEFAULT** on format 2 (B12) + format 4 (4PB) — slot `scotch_eshop`, qty 1, `we_supply_only` — because it's format-uniform (every eshop pack of that format gets it), exactly like body/cap. This makes it AUTOMATIC + removes the per-SKU defect class (no ALTB12-type omissions). Per-SKU choice stays available for exceptions. Pack-Découverte composites keep their existing choice rows.
- **ALTB12 fix:** clear the wrong `PKG_STICKER_ALT` (bucket-1 leak into a bucket-2 SKU) AND ensure eshop scotch resolves (via the template default once added).

## 🟢 WALKTHROUGH FRAMEWORK — FORMAT-ARCHETYPE first, then recipe art, then liquid (3 nested loops, each maps to a DB altitude)
Validate by FORMAT ARCHETYPE (≈15-20 suffixes: F/BU/4/4PB/B/B12/X/C/CU/4C/12C/P25/P50/composites), NOT beer-by-beer — packaging is format-driven; the body/cap/scotch/sticker/outer structure is a property of the FORMAT, shared across all beers in it. Beer-by-beer re-validates the same structure 20× + misses format-wide gaps.
- **Phase A — per FORMAT archetype: validate STRUCTURE once.** Per archetype confirm at template/items altitude: container body + cap/lid slots (format-generic); which decoration roles the format carries (scotch? sticker? holder? outer_tray? outer_box?); which BUCKET (1/2/3) → set branded-vs-eshop-vs-neither scotch ONCE per format. Output = a clean per-format slot template. Fixes the skeleton for every beer in the format.
- **Phase B — per RECIPE: validate ART overrides.** Walk each recipe's bindings (label, can-art, sticker, scotch branded-vs-transp via `uses_branded_scotch`, holder, outer_tray). Where ALT's `PKG_STICKER_ALT` placement, white-label brand overrides get validated. ONE pass per recipe (a binding fixes all the recipe's SKUs in that role).
- **Phase C — per RECIPE: validate LIQUID (AFTER the liquid compiler is built).** Confirm composed liquid (observed malt/hops + ref_recipe gap-fill minerals/process-aids) per recipe; phosphoric recovery verified here. Per recipe, scaled to each SKU by hl_per_unit automatically.
- **Phase D — per SKU: spot-check the COMBINATION.** For a representative SKU of each (recipe × format), confirm liquid + packaging resolve together (resolved `ref_sku_bom` rows on BOM Review), parity 0, cost sane.

Nesting: **Format archetype (structure → template/items) → Recipe (art → bindings + liquid → recipe_ingredients/observed) → SKU (combination → resolved ref_sku_bom).** Three loops, each on a distinct altitude, least-repetitive + most rigorous.

**START ON ALTERNATIVE (recipe_id=6) — PACKAGING WALK STARTS NOW, LIVE (Kouros 2026-06-07):** the PACKAGING side (Phase A format-structure + Phase B recipe-art) needs NO build — walk it live, **simplest-format-first.** Walk ALT's formats (ALT4/ALT4PB/ALTB/ALTB12/ALT-X/ALTBU/ALTF) → fix ALTB12 bucket-2 eshop scotch → confirm bindings. **Phase C (LIQUID) is still gated on the F2 liquid compiler** (which is gated on Kouros's basis a/b pick) → settle ALT liquid (zero-observed → FULL ref_recipe incl. PROC_PHOSPHORIQUE 23.33 ml/hl) only after F2 lands.

## 🟢 DURABLE-PERSISTENCE SPLIT (confirmed = house architecture)
- **Per-SKU TRUTH → DB** (bindings / choices / templates+items / ref_recipe_ingredients / composite slots), reflected in the BOM Review surface. One fact one table. NEVER a parallel store, NEVER edit the resolved `ref_sku_bom` row.
- **The RULES → memory** (PM + operator). Canonical RULE doc = THIS file (liquid-composition rule #1 + the 3 buckets #2 + the altitude map) + packaging-bom-model.md (packaging scotch model detail) + sku-decomposition-tree.md F2 close. **For build agents, fold the SETTLED liquid-composition precedence + dimension discipline into the `sql` SKILL** (decided coding craft, where Sonnet loads it) — once Kouros signs off. The 3-bucket packaging rule stays in PM memory (domain data, not coding craft).

## 🟢 BOM REVIEW SURFACE — ADDITIONS NEEDED FOR THIS VALIDATION (no new compile path; fold into bom-review.php AFTER the liquid compiler lands)
1. **Liquid-source provenance per line** — tag each liquid line `observed | ref_recipe-gapfill | ref_recipe-full` (zero-observed beers). THE most important add — makes phosphoric-recovery visible + shows ALT-liquid-from-ref_recipe vs ZEP-malt-from-observed. (Today brewing rows are read-only-with-provenance but don't distinguish COMPOSITION source.)
2. **Missing-liquid flag** — surface the 17 packaging-but-zero-liquid SKUs as a defect feed (silent COGS holes today).
3. **Bucket indicator** — show which bucket (1/2/3) each SKU's format is in (branded / eshop-scotch / neither), at a glance.
4. **bom_source retag visibility** — once retagged `'liquid'`, show the clean liquid/packaging/composite_* split (the `''` mistag hides it today).
5. **Format-archetype pivot** — add a format-FIRST pivot to the browse tab so Phase-A (walk by archetype) is native, not a filter dance.

## 🚩 DIVERGENCE FLAGS to hold during the walkthrough
- mi_id=NULL liquid line = the operator's named defect — REFUSE, emit RQ, never NULL.
- Editing the resolved `ref_sku_bom` row directly = clobbered by recompile (DIV33C cautionary tale) — edit at binding/choice/template altitude.
- "Correcting" observed malt/hops toward ref_recipe = WRONG (drift is intentional; observed is canonical for malt/hops).
- Walking liquid on the CURRENT BSF-import rows = validating data about to be replaced — gate on the liquid-compiler rebuild first.
- Eshop scotch left as a Stage-2-only concern = the bucket-2 packaging gap; must be promoted into the production BOM for B12/4PB.
- Format-uniform facts (body/cap/eshop-scotch) modeled per-SKU = invites omissions (ALTB12) — prefer template default.

## 🟢 CANONICAL RULE #3 — INTRINSIC BOM vs CHANNEL-DEPENDENT FULFILLMENT LAYER (the Alternative walk's load-bearing architecture, operator-confirmed 2026-06-07)
**The per-SKU `ref_sku_bom` holds INTRINSIC content ONLY. Outer packaging + its MI consumption are a SEPARATE, CHANNEL-dependent Stage-2 FULFILLMENT layer — NOT in the per-SKU BOM.** This SHARPENS the existing intrinsic/fulfillment distinction and the §8.3 LOG_SCOTCH_ESHOP "fulfilment = Stage-2" line into a hard architectural boundary.

**What "intrinsic" means per SKU-state:**
- A loose bottle (in an `-X` CAGE) = bottle + cap + label + TEA + liquid. That is its intrinsic BOM. NO outer box, NO eshop carton, NO scotch.
- A `-B`/`-B12` BOX = the box's intrinsic contents (N bottles' intrinsic content) + the box's OWN intrinsic packaging (the cardboard box MI + branded sticker/scotch-or-label per bucket). The box IS a stock item; its box-MI is intrinsic TO the box.
- A `-X` cage is a stock STATE of loose bottles; a `-B` box is a DIFFERENT stock state of the same bottles. **A bottle exists in EITHER a cage OR a box — produced+consumed ONCE, never both.** (Reaffirms the cardinal cost-lives-once invariant.)

**What is FULFILLMENT-LAYER (NOT in per-SKU BOM — emitted by the tracker at sale time):**
- Eshop OUTER packaging is **ORDER-COMPOSITION dependent**, so it CANNOT live on a per-SKU BOM (a SKU has no knowledge of the order it ships in):
  - Single 12-pack order → `PKG_CARTON12_ESHOP` + 4× `PKG_RENFORTS_ESHOP` (bind 4/carton, price 0, warehouse-tracked) + `LOG_SCOTCH_ESHOP`.
  - **Multiple 12-packs in ONE order** (e.g. ALTB12+EMBB12 = 24 btl) → CONSOLIDATE into 1× `PKG_BOX_24_BTL_BLANC` (pack by TOTAL bottle count into FEWEST boxes — NOT per-SKU cartons).
  - Taproom 12-pack → `PKG_BOX_12_BTL_BLANC`.
- The cage-vs-box STOCK PULL itself (which physical stock a direct sale draws from) is fulfillment-layer routing.

**⚠️ RECONCILIATION WITH RULE #2 / eshop-scotch-into-production-BOM:** Rule #2 bucket-2 said eshop PACKS (`-B12`/`-4PB`/Pack-Découverte) carry `LOG_SCOTCH_ESHOP` as a real production-BOM line via a fmt-2/fmt-4 template default. Rule #3 REFINES the boundary: **the pack's OWN single-pack eshop scotch + carton (the wrap that makes a -B12 a sellable eshop unit on its own) is INTRINSIC to that SKU and stays a template default.** What moves to the FULFILLMENT layer is the ORDER-LEVEL consolidation (N×12 → fewest 24-boxes, renforts count, the order's shipping scotch) — that depends on the whole order, not the SKU. So: per-SKU template default = the single-unit eshop wrap; tracker = the multi-unit order consolidation + cage/box pull. NO double-count: if an order consolidates 2×12 into one 24-box, the tracker emits the 24-box and SUPPRESSES the two single-12 cartons. (Open item #2 below: confirm with Kouros whether a -B12 sold alone gets its carton from the template OR always from the tracker; PM rec = template for the single-unit baseline, tracker overrides on consolidation.)

### STOCK STATES (where cages and boxes sit)
| Stock state | Format | What it is | BOM altitude |
|---|---|---|---|
| **`-X` CAGE** | fmt 6 (1027 loose bottles) | intermediate WIP, NEVER sold direct; the default eshop/Shopify pull source while in stock | intrinsic: 3 contents lines (bottle+cap+label) per the X template |
| **`-B` / `-B12` BOX** | fmt 1 / fmt 2 | sellable box; the taproom pull source; the eshop FALLBACK when no cage stock | intrinsic: N×(bottle+cap+label) + the box's own outer MI (bucket 1/2/3 scotch/sticker) |
| **loose (eshop order)** | — | bottles drawn from a cage for a Shopify shipped order | NO new intrinsic; cage draw-down + tracker-emitted eshop outer pkg |

## 🟢 ④ DIRECT-SALES TRACKER — OPERATOR DECISIONS 2026-06-09 + MVP SPLIT (Phase 1 visual NOW / Phase 2 cage-COGS later)
**Operator answers 2026-06-09 (recorded — these RE-SHAPE the build into two phases):**
1. **Shopify pickup-vs-shipped signal → ON HOLD.** Team is actively changing Shopify's shipping-vs-pickup setup; we wait for the final mechanism before keying on it. → **Phase 0 (ingest pickup-flag) + cage-vs-loose COGS routing are DEFERRED** until that lands.
2. **Cage anchor → physical count starts Monday 2026-06-15** (NOT 06-08 as earlier — the cage leg slipped). Not loadable yet → the **cage draw-down engine is GATED on this.**
3. **MVP = VISUAL FIRST, and it is a NEW TOP-BAR PAGE, NOT inside Le Cockpit** (Cockpit = the settings side of sales/marketing/finance). The new page tracks **Shopify + taproom sales** and shows the **virtual consumption stock**, which **MUST be FULLY WIRED to the PF (FG) stock in Expéditions — it will be part of the weekly stocktake.** Operator wants a NAME (their placeholder "B2C").
4. **Keg-share (draft pours) → handled SEPARATELY** in the packaging-template batch (Rule #4), NOT in ④.

**⇒ THE TRACKER IS NOW A TWO-PHASE BUILD:**
- **④-MVP (Phase 1, BUILDABLE NOW — read-only visual page):** Shopify (`inv_sales_orders`/`inv_sales_order_lines`, channel='eshop') + taproom (`inv_sales_bc`, channel='taproom') sales per beer/SKU/period, + the **virtual consumption stock READ FROM `fg_stock_compute()`** (the SINGLE FG-stock derivation — do NOT build a 2nd). Charts via `kpi-charts.js` (`window.KpcCharts`). New top-bar page. **WRITES NOTHING to stock** (read-only on FG; consistent with the R1 single-homed-derivation boundary).
- **④-Phase 2 (DEFERRED, gated on pickup-flag + 06-15 cage anchor):** cage-vs-loose routing, the per-beer cage/box residual ledger (the one genuinely-new table), `inv_consumption` writes (`source_event='sales_derived'` + the cage/box draw-down disposition), COGS-into-COP. The MVP must leave HOOKS so Phase 2 slots in without rework.

**PAGE IDENTITY (PM recommendation):** new top-bar page, `ref_pages` row. **NAME — recommend «Ventes directes»** (matches the FR house naming: Expéditions/Approvisionnement/Saisies; "B2C" is operator placeholder, avoid the anglicism + it's really D2C). Alternatives: «Ventes & Boutique», «Boutique & Taproom», «Eshop & Bar». **Domain = `logistics`** (sibling of Expéditions, which it reads), `min_role='manager'` (commercial/finance view, not floor-operator), `sort≈105` (right after Expéditions sort=100). **NOT in Le Cockpit** (Cockpit = admin-domain settings cluster sort 150-180). The page is the visual front; Le Cockpit stays the settings side.

**DATA-READINESS (live, PM-verified 2026-06-09):** `inv_sales_orders` 2472 rows, channel='eshop' ONLY, 2025-12-01→2026-04-30; `inv_sales_order_lines` 4156 lines, **3893/4156 SKU-resolved (93.7%)**, 263 unresolved (merch/non-beer, NULL title — immaterial for a per-beer view, surface as an "non rattaché" bucket). `inv_sales_bc` 3071 rows 2025-12→2026-05, channels = **enum('b2b','taproom') ONLY** — taproom 208 rows (179/208=86% resolved), b2b 2863. **🔑 DOUBLE-COUNT GUARD IS STRUCTURAL: `inv_sales_bc` has NO 'eshop' channel** — the BC 'Eshop'/1080 mirror is already excluded at BC ingest; eshop comes SOLELY from `inv_sales_orders`, taproom SOLELY from `inv_sales_bc`. Same source split `fg_stock_compute()` already uses (see fg-stock.php header L21-30). MVP just mirrors that split — no new dedup logic needed. **GAP to flag operator:** eshop date coverage ends 2026-04-30 (no May/June eshop yet ingested); BC taproom runs to 2026-05. The page is meaningful on Dec-Apr history now; recency depends on the Shopify/BC ingest cadence.

**FG-STOCK WIRING (exact, PM-verified):** `app/fg-stock.php` → **`fg_stock_compute(PDO $pdo): array`** returns `{anchor_month, anchor_date, rows[], computed_at}`; each row carries `sku_id, sku_code, format, display_family, hl_per_unit, anchor_qty, prod_qty, expedie_qty, eshop_qty, taproom_qty, physique, open_week_qty, open_total_qty, live_semaine, live_futur, velocity_weekly, semaines_stock, flag_*`. Also `fg_stock_for_skus(PDO, array $skuIds)` for a filtered subset and `fg_stock_location_snapshot(PDO)` per-location. **The "virtual consumption stock" = `physique` (the derived live FG units) = the SAME number the weekly stocktake's expected-vs-counted board shows** (Expéditions `?view=stock` already renders it). So the ④ page surfaces the IDENTICAL `fg_stock_compute()` output the stocktake uses — it does NOT recompute, does NOT re-derive, does NOT write. The weekly stocktake remains the place stock is COUNTED (writes `inv_fg_stocktake`); ④ is a READER of the same derivation. R1 hazard (single-homed FG depletion) preserved: no 2nd FG-stock path.

## 🔴 ④-Phase 2 / "ESHOP CONSUMPTION MATRIX" — STRUCTURAL DRAWDOWN GAP PINNED (PM live-probe 2026-06-12)
Operator greenlit the "eshop consumption matrix" (bundle→component decomposition) 2026-06-12 — this IS ④-Phase 2 (the cage/COGS engine), narrowed to "wire bundle sales into FG depletion". Live findings (PM-verified, read-only):
- **`ref_skus.stocktake_scope` is a 4-VALUE ENUM `('base','cage','single','none')`** — NOT the 3-value I'd half-recorded. This column IS the materialized nesting-ladder/inventory-of-record decision. Active-SKU distribution: **base 52, none 61, cage 42, single 27.** Semantics from live rows (ZEP family): **base** = the packaged-output unit-of-record (ZEP4/ZEPB/ZEPC/ZEPF, upp=24 or keg upp=1, is_packaging_line=1) — what the packaging form produces + what stocktake counts; **cage** = the `-X` 1027-bottle WIP crate (ZEP-X, upp=1027, pl=1); **single** = loose unit (ZEPBU/ZEPCU, upp=1, pl=0); **none** = the BUNDLE/unboxing SKUs (ZEPB12/ZEP4PB/ZEP4PC/ZEP12C/ZEPP25/ZEPP50/ZEPV, pl=0) — derived sales handles, NOT independently inventoried.
- **🔴 THE GAP, EXACTLY: `fg_stock_compute()` depletes with `WHERE rs.stocktake_scope <> 'none'` on ALL THREE legs** (shipped L410/L1035, eshop L498/L1094, taproom L538/L1133). **⟹ a sale of a `none`-scope BUNDLE (ZEPB12, 4PB…) currently depletes NOTHING.** That is the scope-gate carried forward by d9f7976 — correct (bundles were wrongly hitting packaged-stock as if independently produced), but it leaves bundle sales drawing on air. The matrix's job = route a bundle sale to draw down its COMPONENT base/single units instead.
- **ANCHOR COVERAGE (the drawdown-target reality check):** with_anchor in `inv_fg_stocktake` — **base 38/52, single 16/27, cage 3/42, none 1/61.** So the BASE SKUs (the natural drawdown target) DO carry anchors + are counted; `none` bundles essentially do not (correct — they shouldn't). **There IS a real target to draw down** (base/single), but cage anchors barely exist yet (06-15 count fixes that).
- **NO bundle-BOM/component-map table exists for unboxing bundles.** `ref_sku_composite_slots` exists + is populated but ONLY for the 4 MIXED composites (PD8/PAL/XMAS/PAC — multi-recipe samplers); a single-recipe bundle (ZEPB12 = 12× ZEPBU of ONE recipe) has NO slot rows and needs none structurally — its decomposition is `units_per_pack` + recipe_id (ZEPB12 upp=12, recipe_id=ZEP → 12 ZEPBU-equivalents). The component map for unboxing bundles = **derivable from `units_per_pack` + the format's container, NOT a new table** (composite slots only earn their keep for MULTI-recipe packs).
- **PM READ:** the STRUCTURAL drawdown (bundle sale → component unit drawdown, wired into the 3 depletion legs by replacing `<>'none'` with a decomposition step) is SEPARABLE from + can ship BEFORE the cage/box-COGS VALUATION layer (gated on 06-15 cage anchor + pickup-flag). Full scoping returned to orchestrator 2026-06-12 — see below if recorded.

### 🔴 REPACK LOOSE-REMAINDER MODEL — PM DEFINITIVE RULING 2026-06-12 (Model B; PM live-probed)
**THE CORRECTNESS CRUX, RECONCILED. RULING = Model B (loose tracked as the scope='single' SKU), NOT Model A (separate "vrac restant" pool excluded from physique).** Decisive live facts (PM-probed 2026-06-12): **`inv_repack_events` ALREADY has `to_sku_id_fk`/`to_qty`/`loose_units`/`to_kind` ENUM('bundle','pd8','loose','adjustment') in schema (mig 336) AND IS EMPTY (0 rows live)** → Model B's structure is physically present; adopting it = NO migration, NO backfill, ZERO downstream risk. The compute leg (`app/fg-stock.php` Step 5.5 + snapshot ~L1266) currently reads ONLY `from_qty` as a raw physique subtraction (= the broken "loose discarded" leg: a ZEPB12 sale opens a 24-box but only 12 ship → over-depletes by 12 every bundle). **Model B fix:** box-open event nets HL 0 = −1 base box (scope='base', counted) + base_upp loose SINGLES (scope='single', counted) → nothing lost; the BUNDLE SALE decomposes in the eshop leg onto the (recipe,container) single (ZEPB12 → −12 ZEPBU), net physique −12 (the shipped bottles, exact). ONE accurate physique number (operator's "accurate stock" need = correctness, NOT a UX preference → PM RULES it, does not escalate the data model).
- **Why B over A (derivation-tree):** the repack fact = one physical conversion (box ceases / singles appear) = ONE fact, two legs, and the canonical table already holds both. Model A discards the `to`-leg at compute and rebuilds "vrac" as a derived aggregate OUTSIDE physique = a 2nd stock number for a fact the ledger already records = parallel-store smell at the arithmetic level; physique deliberately incomplete. A ALSO needs the same net-against-loose propose logic B needs, so A = strictly more surface for strictly less correctness.
- **VIOLATION CHECK — B clean:** R1 (Σcards==Σphysique) preserved (box-open nets 0; sale draws ONE counted single card; no phantom rows). Single-homed FG depletion preserved (eshop leg still the SOLE bundle-depletion path; B changes WHICH sku it lands on = the single, not how many paths). **d9f7976 gate: B works WITH it** — ZEPBU is scope='single' (≠'none') so the existing `<>'none'` gate already passes it; NO gate edit; the bundle SKU itself (scope='none') stays suppressed → no double-count (repack event = box→single net 0; sale = single-draw; two distinct facts).
- **`inv_side_stock_ledger` (mig 303) is NOT the Model-A precedent** — it's the PACKAGING-line loose-remainder accrual ledger (`prod MOD upp`→year-end-offered), a different fact at a different altitude, no box→single conversion. What it DOES set = loose tracked as its own per-SKU ledger line (NOT folded invisibly into a box pool) → precedent FAVORS B.
- **Auto-propose = RUNNING single-balance per (site, single_sku), NOT per-day aggregate, NOT ceil-per-line.** Process chronologically; box-open proposed only when running single balance would go negative for the next bundle draw; opens = `ceil(deficit/base_upp)`. Negative single balance = the "unbox more" signal; composes with the existing same-day tiebreak ordering. Per-day aggregation mis-orders intra-day (a morning sale should consume an afternoon-prior open) → reject it.
- **Loose pool grain = (site_id, single_sku_id); the single is per (recipe, CONTAINER) — NOT one-per-recipe.** Live: recipe 32 has BOTH EMBBU (bottle, id79) AND EMBCU (can, id94); recipe 25 DIVBU+DIV33C. Bottle bundle (…B12/…4PB)→BU single; can bundle (…4PC/…12C)→CU single. Do NOT collapse to (site,recipe) (merges bottle+can loose, mis-depletes). Site matters (repack is physical, singles counted per-site).
- **Single anchor coverage = 16/27** (11 unanchored). Unanchored single → running balance starts 0/from box-opens forward; a genuine negative = honest "unbox more / count me" signal, NOT a phantom (mirror base/cage anchor handling); surface, never NULL.
- **ONE thing to confirm with Kouros (physical-process, not data-model):** "box-open = a real shelf action; team picks from loose bottles until they run out, then opens another." B encodes exactly this. All evidence (bundles scope='none' virtual handles, zero bundle production events, engine = "unboxing") says bundles are virtual → STATE the assumption, PROCEED on B, only adjust if contradicted. Do NOT pause the build.
- **EQUIP: sql + coder + ui + webapp-testing.** Verify (Opus-independent, COGS-bearing): R1 gap=0 with synthetic box-open+bundle-sale fixtures; ZEPB12 sale moves physique −12 (not −24); box-open alone moves physique 0; single negatives only where anchor-absent; self-clean fixtures.

### 🔴 REPACK-ENGINE TARGET DECISION — PM RECONCILED RULING 2026-06-12 (PM live-probed; `app/repack.php`+mig336/337 BUILT, UNCOMMITTED)
The repacking-capture engine (`app/repack.php`, 596 lines, `?? untracked`; mig 336 `inv_repack_events`, mig 337 `rq_repack_unresolved_bundle`) decomposes eshop bundle sales (ZEPB12/4PB/composites) into a draw-down + loose-remainder. Phase-A brief chose **base box** as the draw target; Opus surfaced it as shaky. PM ruling after live re-probe:
- **🎯 TARGET = BASE BOX, NOT the cage. The cage is a TRAP for this engine.** Decisive live evidence (PM-probed 2026-06-12): the CORE `-X` cages (ZEP-X/ALT-X/EMB-X/MOO-X/SPY-X/STI-X/DIV-X/DOA-X, upp=1027 — the exact eshop pull target) have **~0 production events AND ~0 anchors** (DOA-X 2 evt, DIV-X/ZEP-X 1 anchor each, the rest 0/0). My 2026-06-07 "cages invisible" finding STANDS for the CORE cages — their production is still recorded inside `-B` box events (no cage option in the packaging form). **CORRECTION to a half-recorded claim: the 101-event/595k-unit "cage-scope production" I'd have cited is ENTIRELY contract/collab beers parked in scope='cage' (WEST/JASP/CROP/BAM/CAND/FEBR/LUME/DXV/MOSA, upp=24 or 1) — NOT the core eshop cages.** scope='cage' is HETEROGENEOUS (core 1027-crate vs re-scoped contract beers). The BASE box by contrast is fed (809 bot evt / 5.84M units) AND anchored (38/52). ⟹ cage-targeting = depleting an unfed/unanchored pool = guaranteed phantom negatives (the d9f7976 class). Base = a real, fed, counted pool. The rule-correctness of `-X` (operator ④#3) is a PHASE-2 truth (once the cage is fed by the 06-15 anchor + a packaging-form cage option); for the MVP capture engine, base is the only target with a pool behind it.
- **scope-gate / FG-leg arithmetic CONFIRMED intact (PM-read `app/fg-stock.php` L410/498/538 + L1035/1094/1133):** all 3 depletion legs gate `rs.stocktake_scope <> 'none'`. BOTH base AND cage pass the gate (neither is 'none'); the gate does NOT distinguish them — so the engine's CHOICE of target is what matters, not the gate. Σcards==Σphysique holds for EITHER target (the rewrite is a sale-side redirection, not a new card). The d9f7976 carry-forward (bundles wrongly hit packaged-stock) is what the engine FIXES — by redirecting the `none`-bundle sale onto a real base-box draw.
- **(B) string-prefix base-box tie-break (B12→B, 4PB→4) = ACCEPTABLE as an auto-PROPOSE default, NOT a silent commit.** The engine is auto-propose→human-confirm (writes `inv_repack_events` proposals; unresolved → `rq_repack_unresolved_bundle`). The house rule "never guess a COGS-impacting mapping" forbids SILENT inference; an operator-confirmed PROPOSAL with the inference shown is the SANCTIONED pattern (= triage). **GUARDRAIL: when the prefix rule yields ≠1 match, the engine MUST emit an RQ row, NEVER pick arbitrarily** (it already does — `_repack_find_base_box` returns null → `_repack_emit_rq` "base box ambiguous"). So the heuristic is fine PROVIDED ambiguity always routes to RQ + the proposal is operator-gated before any COGS write. Not a hard violation to remove; it IS a violation to let it auto-commit.
- **(C) composite aliasing (PACKDEC/PACKDECX8/FRPACKDEC → PD8 id 137):** there IS a canonical resolver — **`ref_sku_aliases`** (sales-SKU→canonical, already the mechanism for EPH/SPY folds). The engine should resolve a sold discovery-pack code through `ref_sku_aliases` to the canonical composite SKU FIRST, THEN fan out via `ref_sku_composite_slots`. If the alias rows don't yet exist for PACKDEC*, that's a SEEDING task (operator-explicit, from the canonical PD8=137 mapping already recorded — NOT a guess). Do NOT add a parallel alias map in the engine.
- **(D) recipe-NULL collab/FR SKUs (~25%: COLLAB12/24/4PACK, FRPACKCHEF/FRDOAB12/FRMOOB12…):** **RQ-and-manual-capture for MVP.** No recipe resolver should GUESS these (COGS+beer-tax bearing). FR-market SKUs are usually a known core beer re-listed (FRMOOB12 = MOO) → those ARE `ref_sku_aliases` candidates (operator-seeded, like the SPY folds). True collabs (COLLAB*) may be genuinely recipe-NULL until classified (the strain/validation arc). MVP: route both to `rq_repack_unresolved_bundle`, surface, never NULL-deplete.
- **(E) PROCEED, don't pause Kouros on the TARGET.** Base-box is the PM-confident target (it's the only fed+anchored pool; cage is provably a phantom-negative trap right now). The prefix heuristic is sound BECAUSE it's propose→confirm. What to RELAY (not block on): (1) target=base for MVP, cage deferred to Phase-2-post-06-15-anchor + packaging-form cage option; (2) PACKDEC*/FR-relist alias rows need an operator seeding pass (explicit mapping, ~10 codes); (3) the engine is uncommitted — commit-gate it through RULE 2 review. The ONE thing genuinely needing Kouros = whether the eventual Phase-2 cage feed (06-15 anchor + a cage option in form-packaging.php) is in scope now or later; that's his physical-process call, but it does NOT block the base-box MVP.

### 🎯 REPACK TARGET — FINAL RECONCILED RULING 2026-06-12 (operator ground-truth folded; PM live re-probed)
**Operator ground-truth (Kouros, exact):** Model-B physical assumption "team picks loose until empty then opens a box" — "it is right yes but not always the case. Sometimes unpacking a box is easier (split 2×12 into two orders). But usually yes, **priority is given to cages**." ⟹ eshop loose comes PRIMARILY from the **-X cage** (the loose pool), box-open is the SECONDARY/convenience path. So the operator-correct loose pool the bundle depletes is the **cage (-X)**, NOT the single (BU) Model-B ruled. This does NOT overturn the base-box MVP target — it confirms the **Phase-2 end-state**. The two are SEQUENCED, not in conflict.
- **TWO-HORIZON MODEL (canonical, reconciled):**
  - **MVP-NOW (target = BASE BOX):** unchanged. Cage is STILL an unfed/unanchored phantom-negative trap TODAY — PM re-probed live 2026-06-12: 8 core -X cages (upp=1027) have pkg_evt 0 (except DOA-X=2), anchors 0 (except DIV-X/ZEP-X=1). Base = 56 SKUs / 1776 evt / 162 anchors (fed+anchored). `scope='cage'` is HETEROGENEOUS — the event-rich rows (BAMB/WESTB/JASPF etc., upp=24/1) are CONTRACT/COLLAB beers parked in cage scope, NOT core eshop cages. So base is the only pool with stock behind it now.
  - **PHASE-2 END-STATE (target = CAGE -X):** the derivation-correct model Kouros's "cage-first" describes. bundle sale → deplete -X cage; box-open repack event → −1 base box + base_upp into -X cage (refill, the convenience/secondary path); cage FED by packaging once form-packaging.php gains a cage option (so -X production stops hiding in -B). **Single (BU) is RESERVED for a DIFFERENT pool** — taproom/bar loose pours (Lightspeed-grained), NOT the eshop loose pool. This corrects Model-B's "loose = single" identification: eshop loose = cage; taproom loose = single. Two distinct loose pools.
  - **GATE between them = the 06-15 cage anchor + the packaging-form cage option.** Until BOTH land, cage reads negative. After: flip the engine's draw target base→cage; box-open becomes a cage-refill leg.
- **🔧 ENGINE/FG-LEG REWORK ASSESSMENT (PM-read the code):** MOST of the built code SURVIVES; the change is a TARGET REDIRECTION + ONE NEW LEG, not a rewrite.
  - **SURVIVES as-is:** the whole auto-propose→human-confirm scaffold (`inv_repack_events` proposals + `rq_repack_unresolved_bundle`); `ref_sku_aliases` composite resolution (C); recipe-NULL RQ routing (D); the bundle→(recipe,container) decomposition; the `<>'none'` scope-gate (cage passes it too); Σcards==Σphysique invariant.
  - **CHANGES at Phase-2 flip:** (1) draw target `_repack_find_base_box` → `_repack_find_cage` (resolve bundle→ -X by recipe+container); (2) NEW box-open leg = the refill (−1 base box + base_upp → cage), which is the SECONDARY path the engine PROPOSES when cage runs low (mirrors the running-balance auto-propose, just one altitude up — cage-low instead of single-low); (3) the FG eshop leg decomposes none-bundle → deplete CAGE (not single). The single-balance auto-propose logic (Model-B §176) GENERALIZES to a cage-balance — same chronological running-balance shape, different pool grain.
  - **NET:** ~70-80% of `app/repack.php` is target-agnostic plumbing that stands. The base→cage swap is a localized resolver + one added leg. Building base-box MVP NOW is NOT throwaway — it builds the scaffold, the habit, and the capture surface; Phase-2 reuses it.
- **🔴 SEQUENCING RECOMMENDATION = OPTION (b), the capture/depletion split.** BUILD NOW: the CAPTURE (the `inv_repack_events` ledger + operator panel to log box-opens, the auto-propose, the RQ routing, the alias/recipe resolution) targeting BASE for the proposal arithmetic. HOLD: do not flip the live FG-depletion target to cage until 06-15 (anchor) + the packaging-form cage option land. Capture is immediately useful (builds the operator habit + the ledger of box-opens) and cannot phantom-negative because it's propose-not-deplete. Why NOT (a) "build cage-first now": it reads honest-negatives for 3 days on an unfed pool = operator sees broken stock, erodes trust, for no gain. Why NOT (c) "wait 4 days build all at once": loses the capture-habit head-start + bundles the cage-feed form change with the engine in one big-bang (harder to review/gate). (b) decouples cleanly: capture proves out on base, depletion-target flips to cage when cage is real.
- **🟡 form-packaging.php CAGE OPTION belongs in THIS arc but as a SEPARATE, PARALLEL build item — NOT inside the repack engine commit.** It is the thing that makes -X production REAL (stops cage hiding in -B; FORMAT_SUFFIXES lacks 'X' — §194 project arc). It is a packaging-FORM change (Salle des Machines / capacités altitude), architecturally distinct from the sales-side repack engine. Sequence it to LAND BY 06-15 alongside the anchor so the cage-feed is live when the Phase-2 target-flip happens. It can be built in parallel now; it does not gate the base-box MVP capture.
- **THE ONE THING TO PUT TO KOUROS (he owns the 06-15 timing):** "Confirm the 06-15 cage count proceeds, and approve sequencing: we build the repack CAPTURE now (proposes against base box, no live cage depletion), add the cage option to form-packaging.php in parallel to land by 06-15, then flip the FG-depletion target base→cage once the 06-15 anchor + cage-feed are live. Single (BU) stays the TAPROOM loose pool, not eshop — confirm." **What PM RULES without him:** Model-B's "eshop loose = single" is SUPERSEDED → eshop loose = cage (Phase-2), single = taproom loose; the engine is mostly target-agnostic so base-MVP is not throwaway; option (b) capture-now/depletion-later is the build call; the cage form-option is a separate parallel item by 06-15. Only the physical-process timing + the eshop-vs-taproom loose-pool split need his nod.

## 🟢 ④ LIGHTSPEED POS / TAPROOM INTEGRATION — PM INVESTIGATION 2026-06-11 (pre-build brief; nothing built)
**Context:** Kouros wants to wire maltyweb to the taproom's Lightspeed POS (paid custom API, monthly fee; access via a Monday.com intake form). PM-briefed the architectural fit. Live DB re-probed 2026-06-11.

**🔑 LOAD-BEARING FINDING — TAPROOM TODAY IS BC-DERIVED, MONTHLY, PACKAGING-SKU-GRAINED — NOT POS receipts.** `inv_sales_bc` channel='taproom' = **208 rows, period='YYYY-MM' (monthly), 2026-01→2026-05**, lines carry packaging-SKU descriptions (e.g. `sku_code='ALTB'`, `'Alternative Box 24 Btls 33cl'`, `qty_invoiced=31`, `unit_of_measure='24B'`, `sales_amount_chf`, `customer_no='3822' 'La Nébuleuse TAP/SHOP'`). It comes from the monthly BC GL export (`ventejanvier.xlsx`…), NOT from a POS feed. **So today's taproom signal is a coarse monthly aggregate in WHOLESALE packaging units (24-boxes), with a one-month lag** — exactly the R5 "taproom staleness" + the §0a6 single-unit-SKU stopgaps (DGDBU etc.) the team has been hand-patching. **Lightspeed would REPLACE this coarse monthly BC-taproom feed with per-receipt, per-day, single-unit-grained sales** — the real upgrade.

**WHERE LIGHTSPEED SALES LAND (PM ruling — SoT discipline):**
- **A NEW raw append-only table `pos_sales` (+ `pos_sales_lines`), one row per receipt / per receipt-line. RAW, append-only, idempotent.** This is the BC-taproom analogue but at receipt grain. It is NOT `ord_orders` (that's the B2B/WeeklyOrders booked-order lane — taproom is settled-at-till, not a booked order; mixing them = the same parallel-store/double-count smell). It is NOT `inv_consumption` (that's the computed produce/sale consumption ledger — a downstream WRITE target, never an ingest landing). Mirror the `inv_deliveries`/`inv_sales_ledger` discipline: `dedup_key` UNIQUE (= Lightspeed `saleLineID` or `saleID:lineIdx`), `row_hash`, `source_file`/`imported_at`, `sku_id_fk` resolved at ingest (refuse-don't-NULL → unresolved lands in a "non rattaché" bucket, surfaced, never dropped).
- **It then feeds the SAME computed views the BC-taproom channel feeds today** — `fg_stock_compute()` taproom leg + (Phase 2) `inv_consumption` sales_derived. **The cleanest cutover: make the existing taproom *channel* read from `pos_sales` instead of (or in addition to, by date) `inv_sales_bc` channel='taproom'.** This is a strangler swap at the seam, NOT a new derivation path. The Stage-2 cage/COGS engine (④-Phase 2) is UNCHANGED — it just gets a finer, daily, single-unit input.
- **Derivation-tree placement:** Lightspeed is a SALES-SIDE leaf feeding the FG-depletion + COGS legs. It derives DOWNSTREAM of: `ref_skus` (must map Lightspeed product → our SKU), `ref_recipes` (via sku→recipe_id_fk for liquid+packaging decomposition), `ref_customers` (taproom walk-ins → a generic taproom customer, like `customer_no='3822' TAP/SHOP`). It does NOT touch commissioning/capacities. It is the same altitude as the Shopify `inv_sales_orders` feed — a second direct-sales channel.

**WHAT WE ACTUALLY NEED FROM LIGHTSPEED (endpoints/objects — to close taproom COGS+sales):**
- **PRIMARY = Sales / receipts (READ).** Per-receipt line items: product/item ID, qty, unit price, tax, discount, timestamp, payment type, receipt/sale ID, completed/voided flag, refund linkage. This is the whole loop-closer — daily single-unit sell-through per SKU → FG depletion + COGS. (R-Series/X-Series: `Sale` + `SaleLine`; K/L-Series restaurant: orders/checks. Get LINE granularity, not just totals.)
- **SECONDARY = Products / catalog (READ, once + on change).** To build the Lightspeed-product-ID → `ref_skus.id` map. Pull `itemID`, `description`, `customSku`/`manufacturerSku`, `UPC/EAN`, price, category. The mapping table is the integration's spine (see next block).
- **Refunds / voids (READ).** Returns physically re-enter FG; must net (the `inv_sales_ledger` doc_type credit/return_receipt convention — a refund = a sign-flipped depletion). Do NOT ignore.
- **Inventory (SKIP / DO-NOT-SYNC).** maltyweb FG stock is canonical and single-homed (`fg_stock_compute()`); do NOT let Lightspeed's own inventory counts become a 2nd FG store (parallel-store divergence). We may later PUSH our number to Lightspeed for the staff's benefit, but that's a write-back nicety, not the loop.
- **Customers (MOSTLY SKIP).** Taproom = anonymous walk-ins → one generic taproom customer. Only relevant if they run loyalty/named tabs.
- **READ-ONLY for the loop. No writes needed to close taproom COGS.** (Same disarmed-by-default stance as the Shopify write-back worker, §0a11 Phase 2B.) A future inventory-push to Lightspeed would be the only write, and it's optional.

**SKU-MAPPING CONCERN (the spine — flag hard to Kouros):** Lightspeed product IDs must reconcile to `ref_skus` so a taproom sale decrements liquid + packaging and flows into COGS. **This is exactly the `ref_mi_aliases` / `ref_sku_aliases` discipline applied to a POS catalog.** Build a `pos_product_map` (Lightspeed itemID → `ref_skus.id`, with the Lightspeed customSku/description carried for audit). **NEVER guess the mapping from price or name pattern** (house rule — a wrong SKU map silently mis-costs COGS + mis-routes beer tax). Seed it from an explicit operator pass over the Lightspeed catalog (the same "surface unresolved, operator decides" pattern as triage). Taproom sells SINGLE units (BU bottle / CU can / draft pour P25/P50) and 24-boxes — the §0a6 single-unit-SKU work (DGDBU, scope='single', contract-beers-rescoped-out) was the GROUNDWORK for exactly this: those single-unit SKUs are the targets a Lightspeed line maps to. Draft pours map to `-P25`/`-P50` SKUs (keg-share BOM, Rule #4). **Unresolved POS lines = "non rattaché" bucket, visible, never a NULL sku_id_fk depletion.**

**WHICH LIGHTSPEED PRODUCT — UNKNOWN, MUST CONFIRM WITH KOUROS BEFORE THE MONDAY FORM.** Lightspeed sells Retail (R-Series legacy / X-Series new) vs Restaurant (K-Series / L-Series/Order Anywhere) vs Golf — **different APIs, different objects, different intake.** A brewery taproom that pours pints + sells bottles-to-go could be EITHER a retail POS or a hospitality/restaurant POS. **We do not currently know which one La Nébuleuse runs** — this determines the endpoints, the API tier to request on the Monday.com form, and the receipt object shape. **Confirm the exact product + version before requesting API access** (asking for the wrong series' API = paying a monthly fee for the wrong endpoints).

**⚠️ DOUBLE-COUNT / GATING WARNINGS (relay to Kouros):**
1. **Taproom XOR everything else (the cardinal R1 invariant).** A bottle sold at the till must NOT also be counted via BC-taproom (the monthly mirror) NOR via Shopify pickup NOR via WeeklyOrders/`ord_orders`. **At cutover, the BC-taproom channel and the Lightspeed feed cover the SAME physical sales** — pick ONE per date range (PM rec: Lightspeed from its go-live date forward; BC-taproom for history before). Do NOT sum both.
2. **Shopify pickup-at-brewery vs taproom till.** A Shopify order picked up at the brewery (the §0a11 pickup/delivery classification, `fulfilment_mode`) is NOT a taproom POS sale — but if the customer instead just walks in and buys, that's Lightspeed. Keep the channels disjoint; the pickup-signal hold (Phase 2 gate) is about Shopify, independent of Lightspeed.
3. **④-Phase 2 cage/COGS engine is still gated** on the cage anchor (count started 2026-06-15) + the Shopify pickup-signal. Lightspeed does NOT un-gate Phase 2 by itself, but it makes the taproom leg MUCH cleaner (daily single-unit vs monthly 24-box) and is arguably the better first integration to land — it directly improves the taproom COGS the cage engine will consume.
4. **Monthly→daily cadence is itself a win** — R5 "taproom staleness" (`inv_sales_bc` monthly → intra-month taproom depletion invisible, physique drifts up) is LARGELY SOLVED by a live POS feed.

**PM RECOMMENDATION (one-liner for Kouros):** Worth doing — Lightspeed is the right way to replace the coarse monthly BC-taproom aggregate with real daily single-unit sell-through. Before requesting API access: (a) confirm WHICH Lightspeed series the taproom runs (Retail vs Restaurant); (b) request READ on Sales/receipts (line grain) + Products + Refunds only; (c) land it as a raw `pos_sales` append-only table that feeds the EXISTING taproom channel (strangler swap at the seam), with a `pos_product_map` seeded by an operator catalog pass (never guess); (d) hold the XOR — Lightspeed forward, BC-taproom for history, never both. EQUIP for the eventual build: sql + coder (+ ui for the Ventes-directes/Tap&Shop surface; parser-coder NOT needed — this is a structured API, not OCR).

## 🟢 DIRECT-SALES STOCK CONSUMPTION TRACKER — HIGH-LEVEL DESIGN (= the Stage-2 sales/eshop consumption matrix, now sharpened by cage-vs-box channel routing; build AFTER all BOMs validated)
**Identity:** this is NOT a new concept — it IS the long-planned **Stage-2 sales/eshop SKU consumption matrix** (derivation-tree-and-schema.md §Stage-2; sku-decomposition-tree.md §-X-cage-interaction). The walk gives it its missing routing rule: **cage-pull-with-B-box-fallback**. Recorded as ONE build, not two. **NB 2026-06-09: SPLIT into ④-MVP (visual, now) + ④-Phase 2 (cage/COGS, deferred) per the operator-decisions block above — this high-level design IS ④-Phase 2.**

**INPUTS:**
- **Shopify orders** (direct — DISREGARD the BC 'Eshop' account; it mirrors Shopify → double-count). Each order = a set of (sales SKU × qty) + a **pickup-vs-shipped flag** (⚠️ STILL the load-bearing dependency — without it, cage-vs-box and consolidation can't be computed; the Shopify feed MUST carry it).
- **Taproom / BC 'privé' (acct ~1080)** sales lines.
- **Cage (`-X`) stock levels + Box (`-B`/`-B12`) stock levels** per beer (cage basis = packaging-form fills; box basis = packaging output less depletion).

**ROUTING LOGIC (per direct-sale line):**
1. **Eshop/Shopify shipped** → pull loose bottles from the beer's **`-X` CAGE while cage stock > 0** (draw-down, NO fresh bottle/cap/label — already booked in `-X`); **FALLBACK if cage empty → pull from a `-B` BOX** (consume the box + its box-MIs, unbox). + ADD eshop outer pkg (order-composition dependent, see below).
2. **Shopify pickup-at-brewery OR BC privé (1080)** → the existing "unboxed" path (loose-unit consumption); no eshop shipping carton.
3. **Taproom** → pull from **`-B`/`-B12` BOX** → consume the box + its MIs.
4. **Draft pour (P25/P50 at taproom)** → keg-share (Rule #4 below); channel-exclusive with -F keg sale.

**ORDER-CONSOLIDATION PACKING (eshop shipped only):** group the order's bottle count, pack into FEWEST `PKG_BOX_24_BTL_BLANC` (24) then `PKG_BOX_12_BTL_BLANC` (12) then carton12+renforts for a lone 12. Emit the consolidated outer-pkg MIs + `LOG_SCOTCH_ESHOP` ONCE per box, NOT per SKU. This is the order→box bin-packing the per-SKU BOM structurally cannot express.

**MI EMISSION / WRITE TARGET:** writes to **`inv_consumption`** (the canonical produce/sale consumption ledger) with `source_event='sales_derived'` for the eshop outer-pkg (beer-agnostic, mirrors today's `derive-eshop-consumption.ts`) and a NEW disposition for the cage/box draw-down of intrinsic content (beer-specific, per-recipe). The cage→0 transition is itself a packaging/RM event (opens a new cage). Month-close snapshots the residual matrix → exact RM consumption + COP/COGS. This is the SAME `inv_consumption` the brewing/packaging forms feed — ONE ledger, no parallel store.

**TABLES IT NEEDS (mostly EXIST — populate/wire, do NOT green-field):**
- `ref_sku_composite_slots` (the theoretical per-recipe map — built) + `ref_sku_aliases` (sales-SKU→canonical — built).
- A **per-beer cage/box residual state** table (the Stage-2 matrix's stock ledger) — NEW; keyed (period × beer × stock_state{cage,box} × measure), parallel to `cop_monthly`/`cogs_monthly`. This is the only genuinely new table.
- `inv_consumption` (write target — exists).
- `ref_skus.is_direct_sales` (mig 211 flag — exists; gates which SKUs the tracker routes).
- Shopify feed + pickup flag (sales-commercial-surface.md — the feed exists; the pickup flag is the gap to add).

**CARDINAL INVARIANTS (the things that silently corrupt COGS — hold them):** (a) a bottle is produced+consumed ONCE — cage XOR box, never double; (b) `-X`-pulled multipack = DRAW-DOWN, never re-adds bottle/cap/label; (c) order-level eshop pkg emitted per consolidated BOX, never per SKU; (d) a mixed deco pack draws from MULTIPLE per-beer cages or loose units, never one cage; (e) DISREGARD BC 'Eshop' acct (Shopify is the source).

## 🟢 RULE #4 ADDENDUM — DRAFT-POUR KEG-SHARE (deterministic, FOLD NOW into P25/P50 packaging BOM)
Draft pours `-P25`/`-P50` consume a SHARE of the keg hardware at pour level (the keg is TAPPED not SOLD, so collar/safe are consumed proportionally as the keg is drawn down). **Keg-share line = `(pour_HL / 0.2) × (PKG_KEG_COLLARS + PKG_KEG_SAFE)`** (0.2 HL = 20L keg). This belongs in the **packaging BOM of P25/P50** (template/items altitude, bucket-3 "neither" formats which today carry no scotch/sticker — this ADDS a keg-share slot). **NO double-count with ALTF:** the keg is either SOLD whole (`-F`, full collar+safe on the keg SKU) OR TAPPED for pours (`-P25/-P50`, fractional share) — **channel-exclusive**, a keg is one or the other, never both. **GUARD NEEDED:** the tracker/depletion must ensure a given physical keg's pours sum to ≤ 1 keg's hardware (Σ pour shares ≤ 1.0 per keg) and that a keg routed to pours is NOT also counted as an `-F` sale. Deterministic → fold into the format-template enrichment batch now.

## STATUS (Kouros 2026-06-07)
- **Rule #1 CONFIRMED** (with the malt/hops observed-ONLY refinement + finings-per-beer). ✅
- **Buckets CONFIRMED EXHAUSTIVE** (no branded eshop → no 4th bucket). ✅
- **BASIS AGGREGATOR DECIDED = volume-weighted trailing average** (option a; window ≥3 brews over last 5–8 brews OR 12mo, batch-HL weighted, ~2 MAD outlier-reject, monthly recompute). SAME stat feeds BOTH F2 BOM cost AND the drift aggregator. ✅
- **F2 liquid-compiler build FULLY UNBLOCKED** (rule ① + basis both settled) — ready to DISPATCH. ✅
- **PACKAGING walkthrough on Alternative STARTS NOW, live, simplest-format-first** — no build needed for Phase A/B. ▶️

## 🟢 DETERMINISTIC FIX-SET FROM THE ALTERNATIVE WALK (operator-confirmed — CAN go into ref_sku_bom / templates NOW)
These are the brain-state of the walk; each is unambiguous and FOLDS into the format-template-enrichment batch (NOT gated on F2 except where noted):
1. **Bottle cost ← WAC `v_mi_cost` 0.1466 EUR (bottle-only), NOT `ref_mi.price` 0.1684** (TEA-bundled + stale). + separate `PKG_TEA_BOT_CH` @ CHF 0.02 (GL 4201, regulatory fixed 33cl=small-beer=2ct) as a FORMAT-TEMPLATE line on ALL glass-bottle SKUs. Can CAR (IGORA 0.7ct) is EMBEDDED in can WAC (not invoiced) → NO line. ⟹ this elevates backlog #9 (WAC-costing root fix) — the bottle proved `ref_mi.price` vs `v_mi_cost` is a real ~15%-class divergence ON THE HIGHEST-VOLUME MI.
2. **WAC-costing compiler switch (= backlog #9, ELEVATED):** BOTH compilers (`compile_sku_bom_packaging` + the new `compile_sku_bom_liquid`) must cost from `v_mi_cost` NOT `ref_mi.price`. **HARD CROSS-DEP: this MUST precede the F2 liquid apply** (else the liquid rebuild bakes in stale catalog prices).
3. **Box-branding = ONE-OF-3 per (beer×format)** [bucket #1 sub-rule]: branded scotch (MOO/DIV/EMB/ZEP bottles → `PKG_SCOTCH_<beer>` REPLACES transparent, NO sticker/label) | sticker 1/box (ALT/DIB/SPY/STI) | label-on-box 1× (DGD/DIP/DIG/DOC/EPH). **SYSTEMIC STICKER DEFECT (34 box SKUs):** sticker qty set to BOTTLE-COUNT (24/12) instead of **1**; branded-scotch beers' boxes wrongly carry sticker+transparent; label beers carry unused stickers. Fix qty→1 + apply the 3-way at recipe-binding altitude. DOAB theoretical (skip).
4. **Scotch (carton-wrapper tape) = 0.92 m/box ÷ 990 m roll ≈ 0.00093**; transparent default unless a branded-scotch beer.
5. **`-B12` eshop:** DROP branded sticker + ADD `LOG_SCOTCH_ESHOP` (the ALTB12 bucket-2 defect fix; rides the fmt-2 template default).
6. **Draft pours `-P25`/`-P50`:** keg-share line per RULE #4 above. **FOLD NOW.**
7. **F2 liquid dry-run done+clean** (preview ready, NOT applied) — **needs the WAC-costing switch (#2) BEFORE apply.**

## 🟢 SEQUENCING RULING (Kouros's order question — answered)
**Walk all beers' BOMs first, THEN batch-build — with TWO deterministic carve-outs done now, and a HARD cross-dep.**

**Rationale:** the packaging walk (Phase A/B, simplest-format-first) needs NO build and is where the operator surfaces the per-beer bucket/branding facts — those are exactly the inputs the template-enrichment batch needs. Building the template fixes BEFORE finishing the walk = building on a partial fact-set (you'd re-touch templates per newly-walked beer). The walk is cheap (live, no build); finishing it first makes the template-enrichment a SINGLE clean batch instead of N incremental edits. So: **keep walking.**

**BUT two things are deterministic + cross-cutting enough to do in parallel NOW (they don't depend on the per-beer walk):**
- **(ii) WAC-costing compiler switch** — purely a cost-source swap, beer-agnostic, and it GATES the F2 apply. Do it now so F2 is unblocked the moment its dry-run is signed off.
- **(iii) F2 liquid build (compiler) + dry-run** — already done/clean; the BUILD of `compile_sku_bom_liquid` is beer-agnostic. Hold the APPLY until (ii) lands.

**ORDER OF OPERATIONS:**
1. **Continue the per-beer packaging walk** (live, no build) — accumulate per-beer bucket/branding/finings facts across all beers. ← operator's current activity, RIGHT call.
2. **(ii) WAC-costing compiler switch** (parallel, now) — both compilers cost from `v_mi_cost`. Beer-agnostic. **GATES step 3.**
3. **(iii) F2 liquid APPLY** — only after (ii). Rebuild ref_sku_bom liquid all SKUs + fill 17 missing + retag `bom_source='liquid'`. (Build of the compiler can happen in parallel with the walk; APPLY waits on WAC.)
4. **(i) Format-template enrichment BATCH** — AFTER the walk completes: sticker-qty→1 + 3-way branding + TEA line + eshop-scotch template default + keg-share. One clean batch over the full validated fact-set. (Keg-share + TEA are deterministic enough to fold early if a batch is dispatched sooner, but the sticker/branding 3-way wants the full walk.)
5. **(iv) DIRECT-SALES STOCK CONSUMPTION TRACKER (= Stage-2 matrix)** — LAST, AFTER all BOMs are validated (operator's explicit "after all BOMs"). Depends on: intrinsic BOMs correct (1-4), the Shopify pickup-vs-shipped flag (the one external dependency to line up early), cage/box residual ledger table.

**CROSS-DEPENDENCY FLAGS:** (a) **WAC-costing (ii) MUST precede F2 apply (iii)** — non-negotiable. (b) Keg-share + TEA (deterministic) can fold any time; sticker/branding 3-way wants the completed walk. (c) The Shopify **pickup flag** is the long pole for the tracker — start that feed change EARLY (parallel to everything) so it's ready when the tracker is. (d) The tracker double-count guards (cage-XOR-box, order-level-pkg-once, keg-share ≤1) are the correctness gates — bake them into the tracker spec, not as an afterthought.

## OPEN OPERATOR CALLS (remaining)
1. Eshop-scotch altitude: TEMPLATE DEFAULT on fmt 2 (B12) + fmt 4 (4PB) [PM rec] vs per-SKU choice each. (Surfaces during ALT packaging walk on ALTB12.)
2. **Single-unit vs order-level eshop carton boundary** (Rule #3 reconciliation): does a `-B12` sold ALONE get its carton/renforts/scotch from the SKU template, with the tracker only OVERRIDING on multi-unit consolidation? [PM rec = yes: template = single-unit baseline, tracker suppresses+consolidates on multi-pack orders.] Surfaces when the tracker is specced.
- ~~Rule #1 sign-off~~ ✅ confirmed. ~~3-bucket exhaustiveness~~ ✅ confirmed. ~~Basis aggregator a-vs-b~~ ✅ DECIDED = volume-weighted trailing (a).

## 📒 LIVING PER-SKU RECORD — append every validated SKU/format/recipe here (Kouros standing directive)
_This section is the append-only ledger of the walkthrough. As each format archetype / recipe / SKU is validated live, record: what was decided, at which altitude it was written in the DB, the resolved BOM lines confirmed, parity result, and any divergence flagged. Cross-ref each entry to the DB change (binding/choice/template id) + Kouros's project-bom-validation-arc.md._

- **2026-06-10 — ✅ CUV hl_per_unit REVERT + LINER-BRANCH PER-UNIT GENERALISATION SHIPPED → COGS-CUTOVER ZEPV GATE CLEARED (Opus live-verified VPS):** `ref_skus.hl_per_unit` 1.0→0.01 for EMBV/MOOV/ZEPV (27/46/61) + liquid recompile (`--apply --sku 27,46,61`, 0 parity) + COMPILER FIX in `app/sku-bom-compile.php` cuv liner branch (scale BOTH qty AND cost_direct by `$skuHlPerUnit`, generalising the hardcoded hl_per_unit=1.0 case; deployed, packaging recompiled, 0 parity). Live `Σref_sku_bom.cost`: ZEPV 0.096594/EMBV 0.224576/MOOV 0.121062, skuHL=0.01; ZEPV sales material now ≈937 CHF (was ~96k). The ~100× phantom GONE. DURABLE LESSON: a COGS-bearing compiler line gated only on the CHF/HL DISPLAY (cost/qty) silently broke in the SUMMED per-unit sales-material path; per-unit lines MUST scale by the SKU's own hl_per_unit, never hardcode a basis. **LOOSE END: `app/sku-bom-compile.php` deployed but UNCOMMITTED (dirty tree) → commit needed.** Full detail → §COGS COST-SOURCE CUTOVER (RESOLUTION block). Rollback snaps: `_snap_refskus_cuv_hlrevert_20260610` / `_snap_skubom_cuv_hlrevert_20260610`.
- **2026-06-09 — F2 FOLLOW-UPS (post-apply) — ⚠️ NOTE: the "COGS-CUTOVER GATE CLEARED" claim in this entry was REVERSED 2026-06-09 (the `92c9c13` hl_per_unit=1.0 choice BROKE sales-COGS) and only ACTUALLY cleared 2026-06-10 (see entry above + §COGS COST-SOURCE CUTOVER RESOLUTION):** (1) **Finings** (commit `f639632`) — NO real build: finings MIs (PROC_NAGARDO 79, CLAREX 76, DEHAZE 77) already pickable under the brewing form 'process' chip; `compile_sku_bom_liquid` observed-wins auto-retires the DIV gap-fill once NAGARDO is entered observed. Added a FR discoverability hint to `form-brewing.php` ("Collage/clarification → saisir sous Process"). **Racking-side finings DEFERRED** (bd_racking_v2 has no ingredient model — own arc if needed). (2) **-V cuv costing** (commit `92c9c13`, Opus-verified live) — `hl_per_unit` 0.01→1.0 for EMBV/MOOV/ZEPV; NEW `run_type='cuv'` observed branch in `compile_sku_bom_packaging` amortizes liner from `bd_packaging_v2` (`liner_cost_per_hl = Σ(new liners × resolved MI cost)/Σ(vendable_hl)` over F2 window; one `liner_amortized` line; template liner pair suppressed for cuv). RESULT CHF/HL: EMBV 22.46 / MOOV 12.11 / ZEPV 9.89 (was ~3400). **🏁 THE ~330k ZEPV PHANTOM IS GONE → COGS MySQL cost-source CUTOVER GATE CLEARED** (the NO-GO blocker in RESUME #0). Conservation proof: non-cuv ALL-SKU packaging 2001.4473→1908.9059, drop 92.54 = exactly the cuv liner delta. Opus independent recompute: ZEPV liner 0.9959 CHF/HL all-rows vs 0.9439 windowed (gap=trailing window, OK). (3) **warehouse.php $glSt** — CLEARED, not affected (per-HL absolute + ratio-only GL split). **RESIDUALS (non-blocking):** (a) **C1 cosmetic** — `bom-review.php` shows a misleading tier badge + MI-102 catalog price (EUR 18) for the `liner_amortized` line instead of the amortized cost; display-only, ZERO COGS error; fold into the deferred bom-review post-F2 polish. (b) `inv_tank_balances.brew_cost_per_hl` multi-brew correctness still lives in the Node `parse-tank-simulation` pipeline (RESUME #3) — separate arc.
- **2026-06-07 — BASIS DECISION (rule-level, not yet per-SKU):** volume-weighted trailing average chosen → F2 unblocked. No SKU validated yet.
- **2026-06-07 — ALTERNATIVE PACKAGING WALK (architecture-level, operator-confirmed):** established RULE #3 (intrinsic BOM vs channel-dependent fulfillment layer), RULE #4 (draft keg-share pour_HL/0.2), the DIRECT-SALES STOCK CONSUMPTION TRACKER design (= Stage-2 matrix + cage-pull-with-B-box-fallback + order-consolidation packing), the 7-item deterministic fix-set (bottle WAC 0.1466 / TEA line / sticker-qty→1 + 3-way branding / scotch tape 0.00093 / B12 eshop-scotch / keg-share / F2-needs-WAC), and the SEQUENCING RULING (walk-all-then-batch; WAC-switch + F2-build parallel-now; WAC GATES F2 apply; tracker LAST). Specific ALT SKU rows (ALT4/ALTB/ALTB12/…) to be appended as the live walk records each.

### 2026-06-07 — WALK PROGRESS BATCH (all operator-validated; PM-verified key facts against live DB)
**WALKED + VALIDATED:** ALT (architecture session above) + **EPH4 family, EPH1, EPH3 (closes the EPH family), EMB, MOO, Diversion family (DIV/DIB/DIG/DIP), STI, SPY, DOA.** ~~REMAINING: EST, DOC, DGD, collabs/contracts, composites~~ → **ALL walked same day; WALK COMPLETE — see §CLOSING BATCH below.**
- **mig 271 APPLIED + committed (maltyweb `2a37041`)** — `271_alt_batch25_reattribute_sku_fold.sql` (PM-verified live): EPH2 batch '25' full chain re-attributed → ALT (ALT brews 7→8, raw batch label kept); EPH24PB (sku 75) recipe→6 + BOM recompiled; folds EPHB12(305)→EPH4B12(67) + EPH4PB(304)→EPH44PB(76) via `ref_sku_aliases` **with rotating-listing caveat** (eshop listings rotate vintages); 28 audit rows; snapshot in maltytask data/snapshots. ⚠️ This CONSUMED the mig number previously reserved for P3 recap — **NEXT FREE = 272.**
- **NAMING RULE (durable): keg-paired stickers = `PKG_KEG_STICKER_*`.** PKG_STICKER_EMB renamed → **PKG_KEG_STICKER_EMB** (ref_mi 271): it is a KEG sticker (rond 45×45), used with keg collars when collars were branded; **DISCONTINUED at the Säuberlin generic-collar switch (2026-03-24)**; ~7.5k residual stock; alias kept.
- **inv_deliveries 572/573 backfilled** (Lenzlinger FA25-07798, 2025-09-30 — BSF-era rows never mirrored): 50k PKG_LABEL_DIV @0.10725 CHF + 7.5k keg stickers @0.07105.
- **Placeholder stickers DEACTIVATED:** PKG_STICKER_MOO/DIV/DIG/DIP (NULL price, zero purchases, zero BOM lines after fixes). Still-active placeholders DGD/DOA/DOC/EPH1-4 resolve at their beers' walks.
- **PKG_CAN_DIV_33 CREATED (ref_mi id 649)** — DIV33C was a preprinted-can SAMPLING run (2 events, given away as units; no box/sticker → minimal can+lid BOM is CORRECT); DIV33C re-pointed off generic PKG_CAN_ALU_33. **Price pending invoice** (invoice-feed list below).
- **DIG = Qrew CollabIn with Néb-PAID packaging** (Lenzlinger labels; invoice to feed). **BLA/BLO = Neb-OWN white label, DISCONTINUED, is_active=0** (BLA=MOO liquid, BLO=ZEP liquid).
- **SPY folds applied:** SPY12C50(109)→SPY12C(108) duplicate; SPYB24(303)→SPYB(49) ("Speakeasy By Danny Khezzar" Shopify listing = a -B with a bad name). **DK = "By Danny Khezzar"; DK era-change = LIQUID DISCONTINUITY** (→ BASIS discontinuity guard above). Non-DK consumables still in stock (esp. SPY); sales codes stay SPY/DIB.
- **DOAB re-pointed to DOA-X** (bd_packaging rows 2050/2179, audit rows written): partial-cage runs were entered as -B because no form ever had a cage option. DOAB back to 0 events = theoretical.
- **🔴 SYSTEMIC FINDING — CAGES INVISIBLE:** ALL -X SKUs have ZERO bd_packaging events ever (PM-verified: 0 suffix='X' rows; the only 2 sku→-X rows are the DOAB repoint). Cage production has always been recorded inside -B events — no cage option in ANY form incl. the current maltyweb one (`FORMAT_SUFFIXES` in form-packaging.php has no 'X'). Consequence: packaging-consumption OVERCOUNTS box/scotch/sticker for cage bottles, silently absorbed by drift.
- **🟢 OPERATOR DECISION — CAGE CUTOVER, NO RETRO-CORRECTION:** do NOT retro-repoint -B→-X (can't reach 100%; partial cages indistinguishable). Instead: (a) operator provides a **CAGE INVENTORY 2026-06-08 = opening anchor**; (b) packaging form gains the **-X option NOW** (build blessed, see §CAGE-FORM BUILD below); (c) forward-only correctness from the anchor date. Anchor+flows pattern, same as deliveries-driven inventory.
- **TEMPLATE-ENRICHMENT BATCH QUEUE (single apply AFTER the walk completes — refreshed):** sticker qty 24→1 for sticker-beers' -B; DELETE stickers on all -B12; branded-scotch swaps (EMBB/MOOB/DIVB/ZEPB: PKG_SCOTCH_TRANSP→PKG_SCOTCH_<beer>, delete sticker lines); label-beers' -B drop sticker + add 1× box-label (DGDB/DIPB/DIGB/DOCB/EPH*B); DROP PKG_INTERCAL_NOMOQ lines (EMB4C/MOO4C/STI4C/SPY4C/ZEP…); TEA `PKG_TEA_BOT_CH` @0.02 CHF template line on ALL glass SKUs; keg-share lines on all P25/P50 (1/80, 1/40 of collars+save).
- **INVOICE-FEED LIST (post-walk):** Lenzlinger DIG/DIP labels; PKG_CAN_DIV_33 cans; Wagner keg-saves; 2026 invoice catch-up generally.

### 🏁 2026-06-07 — CLOSING BATCH: WALK COMPLETE (every active SKU validated) + agreed build sequence
**CAGE FORM SHIPPED** — maltyweb commit `91ce8c0`, deployed: 'X (cage / vrac bouteilles)' added to FORMAT_SUFFIXES (the predicted 1-liner); resolution via the (recipe_id, format_code='X') fallback JOIN verified for all 8 -X SKUs; format tiles already rendered -X, tile sets the suffix (ONE entry path); units always operator-typed (no auto-1027); parallel legs support X; beers with no -X SKU hit a visible `sku_meta_missing` refusal (refuse-don't-NULL holds). NEXT (operator, 2026-06-08): cage inventory → create missing -X ref_skus → anchor rows into `inv_fg_stocktake` (qty in BOTTLES, source='manual-adjustment') per §CAGE-FORM BUILD below.

**WALKED + VALIDATED (closes the walk):**
- **EST ✅** — leanest beer: EST4/ESTBU/ESTF clean; NO -B/-B12/-4PB/cans/-X by design; P25/P50 + TEA ride the template batch.
- **DOC + DGD ✅** — both CollabIn, **Néb pays ALL materials**; 24-box = **BLANC** (durable rule: **CollabIn = white box; brown = contracts/external WL**). Sales names: DOC="Dockeuse", DGD="Galactic Drift". **Fixed live:** 4 NULL `sku_id_fk` packaging events linked (bd rows 1982/1983/2181/2182 → DOCB/DOCF/DGDB/DGDF, audit rows — resolver had missed exact sku_code present in `neb_beer`); `ipl=1` set on all 4; placeholder stickers DOC/DGD deactivated. To batch: drop sticker lines + add 1 box-label each (DGD label priced 0.245 from Lenzlinger FA26-03929; DOC label UNPRICED → invoice-feed, lands as no_basis until fed). **Remaining 240 NULL-fk packaging events = ALL contract-brew rows** (client beers, no Néb SKU — structurally expected; resolves in the contract-packaging arc, NOT a defect here).
- **MOO/DIV-family/STI/SPY addenda** (beyond the earlier batch): PKG_CAN_DIV_33 created ref_mi id **649** (DIV33C = preprinted sampling cans, can+lid only); placeholder stickers MOO/DIV/DIG/DIP deactivated; SPY folds SPY12C50(109)→SPY12C(108) + SPYB24(303)→SPYB(49); **DK = "By Danny Khezzar"; DK era-change = F2 basis-window discontinuity guard**; BLA/BLO = Neb-own WL discontinued (already inactive).
- **COMPOSITES ✅** (operator confirmed the decomposition-tree record): PAC="Pack du Chef" SPY×6+DIB×6 ✓; PAL="Pack Louis" 2× each of ALT/DIB/DOA/EMB/MOO/SPY ✓; both probably sold out (tracker will confirm — not 100%). **COLLAB12:** taproom = 12-box blanc (intrinsic default, ALTB12 treatment) / Shopify = carton12 eshop (fulfillment layer). **COLLAB4PACK:** 4 LOOSE bottles takeaway or mixed box — **NO holder MI**. Collab currently = Galactic Drift (rides `ref_sku_collab_temporal`).

**5 COMPOSITE GAPS → fold into the template batch (operator-approved; mirror in sku-decomposition-tree.md):**
1. **Member-container packaging missing from ALL composite folds** — PD8 lacks 8× btl+cap+label (≈1.60 CHF, >50% of pack packaging); same PAL/PAC/XMAS. **Compiler enhancement:** `compile_sku_bom_composite` must emit member packaging from slots (member_format_id=5 BU) × member recipe's label binding, + TEA per member glass bottle.
2. COLLAB12: DELETE PKG_STICKER_DGD×12 placeholder; keep 12× btl+cap+label(via temporal)+box12 blanc.
3. COLLAB4PACK: BOM EMPTY (compiler temporal-resolve gap, previously flagged) → build 4× btl+cap+label, no holder. **Compiler fix** (temporal-resolve in the compile path).
4. LOG_SCOTCH_ESHOP qty 1 @ 2.50 on PAL/PAC = full-roll-per-pack defect → fraction-of-roll.
5. Deactivate ref_skus 288 PACKDECX8 (already aliased→PD8; hygiene, EPHB12 pattern).

**🟢 AGREED BUILD SEQUENCE (operator-approved 2026-06-07) — state: ① ✅ ② ✅ FIX-7 ✅ ③-prereq ✅ ③-compiler ✅ NUMERICALLY VERIFIED (4 incidents fixed, dry-run only) → ③-apply (joint per-beer review) → ④:**
- **① WAC-costing switch — ✅ APPLIED 2026-06-07** (committed+deployed+recompiled, −13.5%).
- **② TEMPLATE-ENRICHMENT BATCH — ✅ EXECUTED 2026-06-07** (Dispatch A `c7d2c3e`+`052a8cb`; Dispatch B `c060b54` = migs 272+273; TEA-WAC incident root-fixed). Full record → §STEP-② EXECUTION RECORD.
- **FIX-7 keg-share — ✅ APPLIED 2026-06-07 (maltyweb `b65cfe3` = mig 274).** `derived_from_format_id BIGINT UNSIGNED NULL` FK on `ref_packaging_formats`; formats 16/17→15 (keg-F, verified live: format_code='F', hl_per_unit=0.2, catalog_id=13); 4 fractional `ref_packaging_items` rows (keg_collars+keg_safe at 0.0125/0.025, shape mirrors keg items 55/56); 2 `ref_packaging_bom_templates` rows (we_supply); compiler gate Arm B (generic derived-format inheritance, no hardcoded ids); compiler metaQuery unified UNION path + derived-format branch JOINing parent_f.catalog_id through the dbc chain; auto-detect Arm 4 (active derived-format SKUs always in cron scope). Opus-verified: 26 P25/P50 × 2 pkg rows each, ALTP25 = 0.001275+0.000820 CHF; non-P25/P50 byte-identical; idempotent; full auto-detect 0 errors. **Packaging total now 2001.45 CHF / 580 rows. ② = 100% COMPLETE; operator SIGNED OFF the aggregate delta ("ok validated, let the new costs flow").**
- **③-prereq F2 window guard — ✅ SHIPPED 2026-06-07 (maltyweb `5bbc601` = mig 275 + compiler).** Per-recipe floor-date guard in `compile_sku_bom_liquid`; SPY+DIB floored at 2025-10-07; EPH G1 (vintage-scoping) removed (structural — each EPH vintage = separate recipe_id, already 1:1). Dry-run: 0 errors / 0 unresolved MI / ref_sku_bom count+cost identical. See §F2-WINDOW-GUARD RULING for full findings + apply-gate checklist. **⚠️ SPY floor PENDING OPERATOR CONFIRMATION.** ⚠️ **DRY-HOP SYSTEMATIC FINDING must be reviewed before apply** (Gate 10 in apply checklist — see §F2-WINDOW-GUARD RULING).
- **③-compiler F2 LIQUID COMPILER — ✅ NUMERICALLY VERIFIED 2026-06-07 (commits `888473b` dry-hop branch + stage tags → `f99fa2a` inc#2 fix → `c9ff4ef` recipe-level window + `2a803d4` whirlpool → `2d59b4e` inc#4 fix; ALL DRY-RUN ONLY, ref_sku_bom untouched 2135/2250.003568 verified each time).** 4 incidents found+fixed; Opus hand-computed reference = preview EXACT (EMB basis {225,226,227,228,229,230,231,233} ΣHL 944.6; EMBF dry-hop 566.38 g/HL = Amarillo 264.66 n=7 + Cascade 153.50 n=7 + Simcoe 132.33 n=7 + Mosaic 15.88 n=1). Full incident log → §F2 COMPILER VERIFICATION RECORD below.
- **③-apply F2 liquid APPLY** — GATED on: (a) joint per-beer review of `data/sku-bom-liquid-preview.json` with operator (aggregate + per-recipe deltas); (b) SPY floor date confirmed; (c) dry-hop now INTEGRATED (incidents #2-#4 fixed) — review the per-recipe DH numbers + data-gap exclusions instead. Apply-gate checklist in §F2-WINDOW-GUARD RULING items 1-14 (11-14 added 2026-06-07 post-verification).
- **④ Stage-2 direct-sales consumption tracker — LAST** (Shopify pickup-vs-shipped feed = long pole; the 2026-06-08 cage anchor feeds it).

**§STEP-② BUILD PLAN (PM ruling 2026-06-07 — two dispatches, code-then-data) — ✅ EXECUTED 2026-06-07 (see §STEP-② EXECUTION RECORD below; only FIX 7 keg-share bounced, ruling issued):**
- **Altitude split:** COMPILER-CODE = (a) composite member-packaging emission (gap #1) + (b) COLLAB4PACK temporal-resolve (gap #3) — both in `app/sku-bom-compile.php`, dispatched ONLY after ① is committed (same file; never stack uncommitted edits). DATA-ALTITUDE (mig 272, binding/choice/template/ref_mi rows; NEVER direct ref_sku_bom writes) = everything else: sticker qty→1, -B12 sticker deletes, branded-scotch swaps, box-label adds, Nomoq intercal drops, **TEA as a per-container template line on glass-bottle formats** (rides the same per-container scaling as caps — fractional/per-container template qty is already the house pattern, cf. scotch 0.00093; price PKG_TEA_BOT_CH catalog 0.02 CHF in the same mig), **keg-share as fractional template lines** on P25/P50 (1/80, 1/40 — no compiler change), eshop-scotch fractions, COLLAB12 placeholder delete, PACKDECX8 deactivate, DGD label price 0.245.
- **Dispatch A (Sonnet, ui+sql+coder): compiler enhancements** — member-packaging emission + temporal-resolve. GATE A: dry-run on the 7 composites only — PD8 gains ≈8× btl+cap+label (+TEA once data lands), COLLAB4PACK non-empty 4× btl+cap+label, ZERO diff on non-composite SKUs, idempotent re-run, commit-stage review (RULE 2).
- **Dispatch B (Sonnet, sql+coder): mig 272 data batch + recompile ALL** — GATE B assertions post-recompile: 0 sticker lines qty>1 on -B; 0 stickers on any -B12; EMBB/MOOB/DIVB/ZEPB carry PKG_SCOTCH_<beer> only; label-beers' -B exactly 1 box-label; 0 PKG_INTERCAL_NOMOQ on listed SKUs; TEA present at >0 cost on all glass SKUs; P25/P50 carry fractional keg-share; LOG_SCOTCH_ESHOP fractional; PACKDECX8 inactive; parity_violations=0; snapshot-before + audit rows; DOC label = accepted no_basis (feed-2 lights up, refuse-don't-NULL satisfied). Re-run recompile = idempotent.
- **TEA fallback note:** if the format-template schema turns out NOT to support per-container multiplicity for a non-container MI, TEA moves to a small compiler rule keyed on container-material=glass — Dispatch A scope grows by one item; verify the schema in Dispatch B's pre-flight and bounce it to A if needed. *(Outcome: per-container multiplicity WORKED — no compiler bounce for TEA.)*

**§STEP-② EXECUTION RECORD (2026-06-07 — both dispatches DONE; verified-in-DB final state):**
- **②-A compiler (maltyweb `c7d2c3e` + `052a8cb`):** composite member-container packaging emitted DATA-DRIVEN from member format template+bindings — PD8 112 rows/2.80 CHF; PAL 83/6.99; PAC 28/6.91; XMAS 45/3.15. COLLAB4PACK temporal-resolve (4× btl+cap + PIVO + PKG_LABEL_DGD). Explicit-null-override handling added in the solo path. Composites added to cron auto-detect AND `sdc_recompile_recipe_packaging` (self-caught bug: `array_column('id')` vs `'sku_id'` made compositeIds empty). GATE A passed: zero diff outside 6 target SKUs, idempotent.
- **②-B data (maltyweb `c060b54` = mig 272 + mig 273):** mig 272 applied ALL data-altitude fixes — sticker qty 24→1 (-B/-C); sticker suppressed on all -B12 via explicit-null choices; branded-scotch bindings EMB/MOO/DIV/ZEP + §8.1 sticker auto-suppression; box_label slot on format 1 + 8 label-beer -B choices + explicit-null suppression for others; Nomoq intercal suppressed EMB4C/MOO4C/STI4C/SPY4C/ZEP4C; PKG_TEA_BOT_CH priced 0.02 + TEA template line on all 6 glass formats (slot `tea`); LOG_SCOTCH_ESHOP 1→0.000930 on PAL/PAC; COLLAB12 sticker explicit-null; PACKDECX8 (288) deactivated. **mig 273 (schema, forced by box_label): `ref_sku_bom.slot_name` VARCHAR(64); unique key now `(sku_id, ingredient_raw, source, slot_name)`** (same MI on two slots: bottle-label×24 + box_label×1); `idx_sku_id` added for the CASCADE FK; **`ref_sku_packaging_choices.qty_per_unit` widened to DECIMAL(14,6)** (0.000930 was silently rounding). All 3 compiler INSERT paths write slot_name. RQ 447/448 auto-resolved (EMBB via §8.1, EMBB12 via explicit-null).
- **🔥 POST-B INCIDENT — TEA WAC poisoning (fixed same hour, parent Opus):** Gate (f) passed only NOMINALLY — `v_mi_cost` WAC for PKG_TEA_BOT_CH was **1311.75 CHF/unit** (11 Univerre TEA `inv_deliveries` rows were qty=1 LUMP-SUMS ~1446 EUR each) → first full recompile poisoned ref_sku_bom packaging to 11.88M CHF. **Root-fix:** each TEA row re-granularized to qty = paired-bottle-line qty (exact invoice_ref+file_id_fk join; implied rates 0.0214–0.0219 ✓ = 2ct VetroSwiss), unit_price=lump/qty, qty_remaining scaled, 11 audit rows; WAC now 0.02033 CHF/bottle; full recompile (812 del/814 ins, parity 0). **VERIFICATION LESSON:** the Dispatch-B agent MISDIAGNOSED these rows as "wrongly assigned → reassign to PKG_BOT_PIVO" — they are LEGIT TEA rows (2026-05-13 TEA-split convention), only GRANULARITY was wrong. Gate assertions on "price > 0" are insufficient — assert the WAC is ORDER-OF-MAGNITUDE sane before recompiling a newly-priced MI. Detail → [billing-format-and-wac-unit-model.md](billing-format-and-wac-unit-model.md) §LUMP-SUM ECO-FEE ROWS.
- **Post-B defect (fixed):** `ref_recipes.uses_branded_scotch` is RECIPE-altitude → ZEP's flag gave the CAN box ZEPC branded scotch + §8.1 killed its sticker. Fixed via Tier-1 choice id 47 (sku 59, scotch→PKG_SCOTCH_TRANSP 179) → PKG_STICKER_ZEPC ×1 restored. Architectural flag recorded in packaging-bom-model.md (uses_branded_scotch must become FORMAT-scoped if a 2nd -C of a branded-scotch beer ever exists). BLO4C (inactive) still carries a Nomoq intercal row — historical-only per BLA/BLO ruling, INTENTIONAL.
- **❌ FIX 7 BOUNCED — P25/P50 keg-share NOT applied:** formats 16/17 have `catalog_id=NULL` → the dbc-commissioning gate in `compile_sku_bom_packaging` skips them ("not commissioned", 27 expected recompile errors). → §FIX-7 RULING below.
- **FINAL STATE (verified in DB 2026-06-07):** packaging 528 rows / 2001.37 CHF (from 1751.92 pre-②: TEA +185.82, box-labels +1.07, stickers 0.43 total, branded scotch 0.08); composite_packaging 83/11.92; Brewing 1472/236.64 BYTE-IDENTICAL; Gate B re-run post-TEA-fix ALL PASS; recompile idempotent. Spot BOMs: ALTB = PIVO 3.32 + box 0.65 + label 0.54 + TEA 0.49 + caps 0.17 + sticker×1 0.108 + transp scotch 0.014. ZEPC = can 6.85 + box 0.68 + lids 0.57 + sticker×1 0.108 + transp 0.014. Aggregate COGS delta +249.45 CHF (current-BOM altitude) — **operator SIGNED OFF 2026-06-07.**

**§FIX-7 RULING (PM, 2026-06-07 — P25/P50 keg-share vs the commissioning gate): COMPILER ARM via a `derived_from_format_id` discriminator; DO NOT commission pour formats in the dbc catalog. — ✅ EXECUTED EXACTLY AS RULED (maltyweb `b65cfe3` = mig 274; see build-sequence line above for the as-built record; seed verified live 16/17→15).**
- **Why not commission:** the dbc chain (`dbc_packaging_format_templates` × `dbc_container_types` × filler gating) models PHYSICAL packaging capability — container + filler. A draft pour has NEITHER (the keg is tapped, not filled); a fake catalog row would (a) corrupt the Le Zeppelin capability-gating semantics (formats-unlock UI would claim the brewery "makes" P25 on a filler), (b) contradict the compiler's own established design (`sku-bom-compile.php:469/677`: "catalog_id IS NULL → composites/draft-pours/6C/PAD by design"), (c) break the static volume chain math (units_per_format would need a fractional keg).
- **The durable shape (NOT a hardcoded id-16/17 bypass):** small mig — `ALTER ref_packaging_formats ADD derived_from_format_id INT NULL` + FK→`ref_packaging_formats(id)`; seed rows 16/17 → the F/keg 20L format row (VERIFY the keg format id at build time, don't guess). Gate rule becomes: pass if in-dbc-chain OR `is_composite=1` OR (`derived_from_format_id` set AND the PARENT format passes the gate) — pours inherit commissioning from the keg line, which is the true physical capability. The FK also encodes the channel-exclusive keg→pour derivation the Stage-2 tracker needs for its Σ-pour-shares ≤ 1.0-per-keg guard — a real downstream consumer, not decoration.
- **Scope:** 1 mig (next free, re-verify `--status`) + ~10-line gate arm in `app/sku-bom-compile.php` + the already-designed fractional keg-share template lines (1/80, 1/40 × collars+safe) + Gate assertion "P25/P50 carry fractional keg-share, 26 SKUs". Out of scope but FLAGGED: the volume resolver returns NULL for catalog_id=NULL formats even though `f.hl_per_unit` IS populated (0.0025/0.005) — a COALESCE-to-f.hl_per_unit improvement can ride a later pass, not this one. EQUIP: `coder`+`sql`.

**§F2-WINDOW-GUARD RULING (PM, 2026-06-07 — ③-prereq basis-window guard; build SHIPPED 2026-06-07 mig 275 + compiler commit `5bbc601`): design APPROVED; floor column is a legitimate durable fact, NOT just an SCD2 bridge. EPH seasonal scoping removed (structural — see note).**
- **Design as dispatched + EXECUTED:** mig 275 `ref_recipes.liquid_basis_floor_date DATE NULL` (NULL = no floor, semantic NULL; brews with event_date >= floor are IN — inclusive, since DIB's seed = the FIRST peach-tea brew). Seeded ONLY SPY + DIB at DK era-change boundaries, dates derived from canonical observed data. **EPH vintage scoping guard (G1) was designed but REMOVED during commit-review** after discovering the actual DB model: each EPH vintage has its own recipe row (ref_recipes), so each recipe_id already has exactly 1 batch — the "multi-vintage blending" risk doesn't exist structurally. G1 was dead code. Compiler implements G2 only (floor_date). ⚠️ SPY floor PENDING OPERATOR CONFIRMATION (grist-change evidence: batch 57 = 2025-10-07, durable MALT_OAT_FLAKES+MALT_PILSENER+MALT_WHEAT set vs pre-DK MALT_CARAPILS+MALT_PILSENER — adjust via UPDATE if operator picks a different boundary).
- **EPH data-model fact (durable):** `ref_recipes` has 19 EPH rows, one per (family×vintage). Active EPH SKUs point to the current-vintage row only (r62=EPH1-2026, r76=EPH2-2026, r71=EPH3-2024, r75=EPH4-2025). Each recipe_id has exactly 1 batch in `bd_brewing_cooling`. Floor date is NULL for all EPH rows (no discontinuity — each EPH is a NEW recipe, not a continuing one with a shift). EPH3 active SKUs still point to r71 (vintage=2024) — operator has not yet created a 2025/2026 row for EPH3 (this is fine; EPH3 is simply the 2024 vintage). **A zero-window EPH IS possible** if EPH3's vintage changes but no brew has happened yet for the new vintage; the compiler will skip it with a note (not an error) — correct behavior.
- **Dry-run final state:** 142 SKUs in scope, 0 errors, 0 unresolved MI. ref_sku_bom count=2135 cost=2250.003568 BEFORE=AFTER (no writes, hard constraint met). All EPH recipe_ids have exactly 1 brew in scope (their vintage's 1 batch). SPY (r51): 9 batches kept (57-65 post-floor), 45 excluded. DIB (r26): 3 kept (batches 4-6 post-floor), 3 excluded. ALT (r6): no guard, full trailing window. **Alternative validation: PASS** (PROC_PHOSPHORIQUE via recipe_gapfill; all hops from observed; no recipe-only hops leaked). Window guard report in `data/sku-bom-liquid-preview.json` (SPY r51 + DIB r26 as guarded recipes).
- **Dry-hop finding (SYSTEMATIC):** ALL 278 `hops_dry` rows in `bd_brewing_ingredients_parsed` have `source_table='bd_fermenting'`. The F2 JOIN on `bd_brewing_ingredients_v2` (which bridges only `source_table='bd_brewing_ingredients'`) produces 0 hops_dry rows for any recipe. Dry-hop data lives in `bd_fermenting_v2.event_type='DryHop'` (field `dh_mi_id_fk`) — NOT joined by F2. This is a SYSTEMATIC dry-hop undercount with no ref_recipe rescue (hops = observed-only; no gap-fill). DOA is the worst-affected (HOPS_MOSAIC up to ~45,000g/batch dry-hop). Quantification + operator decision required before applying F2 (Gate 10 in apply checklist). UPGRADING from report-only as specified.
- **(a) SCD2 verdict — NO CONFLICT, different fact.** SCD2 v1 (dormant effective_from/until) versions OFFICIAL reference rows on 5 tables — `ref_recipes` is NOT among them. The floor governs OBSERVED basis-window sampling; RULE #1 makes malt/hops observed-only with drift intentional, so observed discontinuities (DIB peach-tea appeared in brews, not in a ref edit) need not coincide with official version cuts. Reconciliation path when SCD2 v2 activates: a deliberate era-change SHOULD also cut ref_recipe_ingredients versions, and v3 may default the floor to MAX(effective_from) **with `liquid_basis_floor_date` kept as the explicit override** (observed discontinuities can precede official edits). Single scalar = MOST RECENT discontinuity only; on the next era-change OVERWRITE + log_revision. NEVER use the floor for historical valuation (that is SCD2-v3's job).
- **(b) Seeding in the mig — APPROVED with 3 provisos:** (1) LITERAL dates (no analytic SELECTs in mig files — migrate.php rule); (2) provenance comment in the mig citing the derivation query/evidence; (3) the operator gate is the F2 APPLY, not the seed — if the operator adjusts a date at review, correction = plain UPDATE + audit. DK-era-as-discontinuity is already operator-confirmed (2026-06-07 walk); deriving the DATE from canonical data = permitted seeding, not guessing. SPY's grist-change detection is the closer call — acceptable only because the build ends at dry-run; evidence pack MUST show the brews either side of the boundary.
- **(c) F2 APPLY-GATE CHECKLIST (additions beyond the dispatched design):**
  1. **WAC order-of-magnitude sanity per MI entering the liquid BOM** (the TEA-WAC lesson — assert sane per-kg/per-l bands, not merely >0; present the per-MI WAC table at review).
  2. **Unit-dimension consistency per MI across the window's brews** (kg vs g, l vs ml) — mixed units → refuse/RQ, never average across dimensions (unit-discipline hardening is still pending).
  3. **Volume denominator = canonical cooling-anchored `final_volume`** (event_type='Cooling', is_tombstoned=0); NULL/0-volume brews excluded from numerator AND denominator. **🔴🔴 CONFIRMED COGS-BEARING BUG (2026-06-08, operator-caught on EMB r32, PM-verified live) — NUMERATOR/DENOMINATOR BASIS MISMATCH on multi-brew batches → systematic malt/hops UNDERSTATEMENT by factor = #brews/batch (~÷3-4):** A combined "batch" at La Néb = N parallel ~30 HL brews. The malt/kettle numerator (§2b: `bd_brewing_ingredients_parsed`→`bd_brewing_ingredients_v2` via `source_id`) has **ONE ingredients record per BATCH** (e.g. EMB b181 = v2 id=4, single row, Munich 176 kg + Pilsener 347 kg = ONE brew's grain bill; `bd_brewing_ingredients_v2` has NO `brew` column). But the denominator (§2106/2323: `batch_hl = SUM(cool_final_volume_hl)` over `bd_brewing_cooling` GROUP BY recipe,batch) SUMS **all N brews' volumes** (b181 = 4 brews 30.6+30.3+30.2+33.4 = 124.5 HL). Result `176/124.5 = 1.41 kg/HL` Munich, `347/124.5 = 2.79` Pilsener → window-avg ≈ 2.03/3.77 = the dry-run's implausible ~5.8 kg/HL TOTAL grain bill (true ≈ 17 kg/HL ≈ 510 kg per 30 HL brew). **WHY THE EARLIER FAN-OUT CHECK LOOKED CLEAN:** a LEFT JOIN to the 4 cooling rows multiplies the single 176 kg malt row into 4 → `704/124.5=5.66` cancels; but the REAL compiler joins via `source_id` (no fan), so it divides 1 brew's malt by 4 brews' HL. **FIX OPTIONS (PM, operator to choose at review):** (A) denominator = volume of the SINGLE brew the ingredients record corresponds to — but there's no brew-link on the v2 record, so this needs a per-brew malt model OR (B) numerator × N_brews (multiply the per-batch grain bill by the cooling-row count for that batch) so both sides are batch-total — simplest, matches the "malt entered once = per-brew bill, brewed N times" reality; verify N = count of Cooling rows per batch. **✅ RESOLVED 2026-06-08 — operator CONFIRMED the grain bill is entered PER SINGLE BREW** (176 kg = per-brew charge). Fix B is canonical: **numerator × N_brews, N_brews = COUNT(bd_brewing_cooling rows for the batch)**, folded into the §2b v1 malt/hops_kettle qty BEFORE the MAD/aggregator (N varies per batch). Full ruling + apply-gate-16 + dispatch spec → **§MULTI-BREW BASIS BUG — SEMANTICS CONFIRMED + FIX RULING (apply-gate-16)** above. **BLOCKS F2 APPLY.** Single-brew batches (DIG/DIP/most EPH, N=1) are UNAFFECTED (×1; this is why DIG/DIP pilsener 0.6331 verified clean). Affects every multi-brew beer: EMB, MOO, DOA, STI, SPY, ZEP, DIV, EST, DOC, DGD, etc. — their digest malt/hops numbers are now VOID pending fix+regenerate.
  4. **Zero-empty-liquid invariant:** every active non-composite SKU ends with liquid rows OR an explicit skip-note/RQ (refuse-don't-NULL at SKU grain); the 17 missing-liquid SKUs gain rows (count assertion).
  5. **Packaging byte-identical (580 rows / 2001.45 CHF untouched); composite liquid via member-own-basis; NULL mi_id stays 0; idempotent re-run.**
  6. **Guard-interaction:** floor ∩ vintage ∩ ~2-MAD outlier-reject must leave ≥1 brew — assert MAD-rejection can't silently empty a window (0 after MAD → skip+RQ, never widen).
  7. **Vintage-parse validation:** validate year-coded-batch parsing against known messy cases (EPH2 batch '25' was reattributed to ALT; post-mig-263 consolidated EPH rows carry multi-vintage brews). **Verified live 2026-06-07: canonical seasonal marker = `ref_recipes.subtype='EPH'` (19 rows; 42 rows have EMPTY subtype — key on ='EPH' exactly); `lifecycle_hint` is ENUM('auto','historical') — NOT a seasonal marker. `ref_recipes.vintage VARCHAR(8)` is a SECOND "current vintage" source — the build must reconcile it with max(parsed batch vintage) or document which wins (two-sources = divergence smell).**
  8. **Drift-aggregator coupling:** the volume-weighted-trailing statistic must live in ONE shared home consumed by BOTH the F2 compiler and the future drift surface — no parallel stat.
  9. **Operator sign-off on aggregate + per-recipe COGS deltas**, with SPY/DIB shown before/after-floor split; closed-month COGS warning as in ②.
  10. **Dry-hop finding UPGRADED from report-only:** if the observed basis omits fermenting-side dry-hop additions, that is a SYSTEMATIC hop undercount with NO ref_recipe rescue (hops = observed-only, no gap-fill) — QUANTIFY CHF/HL per recipe; operator decides apply-with-ticket vs hold for hop-forward recipes at the review. *(RESOLVED in ③-compiler: dry-hop branch integrated `888473b`, incidents #2-#4 fixed — see §F2 COMPILER VERIFICATION RECORD; gate now = review the DH numbers, gates 11-14 below.)*
  11. **Same-MI-multi-stage uniqueness (PM-added 2026-06-07):** stage-tagged liquid rows of the SAME MI (real case: EMB Cascade whirlpool + Cascade dry-hop) must coexist under the mig-273 4-col unique key `(sku_id, ingredient_raw, source, slot_name)` — assert distinct slot_name per stage, NO silent merge/collision/last-write-wins. Verify on EMB specifically.
  12. **Basis-membership + outlier transparency:** per-recipe review table must show basis batches kept, batches EXCLUDED for missing-DH (the inc#4 data-gap rule, incl. their wasted brewhouse data e.g. b232), and MAD-rejected batches with value-vs-median + MAD/median ratio. Flag any recipe whose DH-gap exclusions shrink the basis below half the nominal 8-batch window. A recipe whose DH events ALL fail the batch join (DOC: 2 DryHop events batch='None') gets an EXPLICIT review line — its BOM ships with NO dry-hop until re-entered; never silent omission.
  13. **DH-alternation semantic guard:** inc#4's rule assumes a recipe either dry-hops consistently or never. List any recipe with MIXED DH/no-DH batches in-window as a data-gap for operator confirmation; if the operator says alternation is INTENTIONAL for any beer, HOLD that recipe and revisit with per-stage basis sets before apply.
  14. **Review-to-apply staleness:** regenerate the dry-run IMMEDIATELY before `--apply` and diff vs the operator-reviewed preview (production data + SDC stage re-attribution move daily; windows shift). Any delta since review → re-present, don't apply the stale sign-off.
  15. **Oldest-invoice costing transparency (added 2026-06-07, §DIGB item 7 — operator-binding rule):** per-recipe review table lists EVERY line costed `oldest_invoice` with basis-window end, MI's earliest delivery date, oldest CHF price vs current WAC, and CHF delta — the substitution is explicit, never silent. Reference check: DIG/DIP MALT_PILSENER = 0.633150 CHF/kg (not WAC 0.47061).

**§F2 COMPILER VERIFICATION RECORD (2026-06-07 — 4 incidents, all fixed; dry-run only throughout; ref_sku_bom 2135/2250.003568 verified untouched after every commit):**
- **Inc#2 (`f99fa2a`) — v1 dry-hop CROSS-RECIPE CONTAMINATION:** the v1 path joined `bip.source_id` against `bd_fermenting_v2.id` but source_id points into v1 `bd_fermenting` → fabricated dry-hop bills (MOO/ZEP from other recipes' rows, EMB double-counted 2×). FIX: v1 dry-hop path DELETED — `bd_fermenting_v2` DryHop events are recipe_id_fk-native AND complete for F2 scope (v1-only batches = 18 contract brews, out of F2 scope). Same family as the broken-row-index-bridge lesson: NEVER join v1 ids against v2 PKs.
- **Inc#3 (`c9ff4ef`) — PER-MI window let retired ingredients never age out:** each MI taking its OWN last ≤8 batches kept dead hops alive forever (EMB dry-hop 1119 g/HL vs ~533 real). FIX: RECIPE-LEVEL shared `basisBatches`; an MI absent from the shared window → NO line; per_hl = Σqty ÷ Σ(ALL basis HL). The window is a property of the RECIPE, not the ingredient.
- **Inc#4 (in `c9ff4ef`, fixed `2d59b4e`) — two faults:** (a) 2×MAD rejected NORMAL batches on highly-consistent data (EMB Amarillo MAD/median=0.007 → 2 good batches killed); FIX: skip MAD rejection when MAD/median < 0.05, keep 2×MAD otherwise (DOA Mosaic 151-492 g/HL still rejects correctly). (b) basis included batch 232 (brewing data present, NO dry-hop record = form gap) displacing b225 → Mosaic vanished + denominator off; FIX: for recipes with ANY dry-hop history, basis membership REQUIRES DH data — missing-DH batches are DATA GAPS, fully excluded incl. their brewhouse data (minor cost: b232's malt evidence wasted). **SEMANTIC ASSUMPTION (recorded):** beers either dry-hop consistently or never; intentional DH/no-DH alternation → revisit with per-stage basis sets (apply-gate 13).
- **Verified reference numbers (Opus hand-computed = preview EXACT):** EMB basis {225,226,227,228,229,230,231,233} ΣHL 944.6; EMBF dry-hop 566.38 g/HL (Amarillo 264.66 n=7, Cascade 153.50 n=7, Simcoe 132.33 n=7, Mosaic 15.88 n=1). DOC edge: its 2 DryHop events have batch='None' → excluded until re-entered with a valid batch (apply-gate 12 review line).
- **🔴 STANDING ARC DISCIPLINE (3 sub-agent gate-PASS failures in ONE day: TEA lump-sum, dry-hop contamination, per-MI window):** COGS-bearing sub-agent claims are VERIFIED BY OPUS against independently computed references BEFORE reaching the operator. A sub-agent's "gate PASS" is a claim, not a verification. Also in the index BUILD-LAUNCH CHECKLIST + conventions-and-helpers.md.
- **Whirlpool + boil-timing (operator-driven, `2a803d4` + mig 276):** `hop_addition_stage` ENUM gained `'whirlpool'` APPENDED LAST (index safety). **CONVENTION: any stage ORDERING must use explicit FIELD()/rank maps, never ENUM order.** Whirlpool = flameout addition, NO cooling, ≈100°C ≠ hop_stand = cooled to 80°C. `hop_boil_time_min` = minutes of boil REMAINING at addition (60=start, 0=flameout), UI-gated to stage='boil', non-boil forced NULL. Operator actively re-attributing hop stages in SDC (EMB already live: whirlpool Cascade + first_wort Herkules). Detail → recipe-ingredients-drift-surface.md §HOP-STAGE WHIRLPOOL.
- **IN FLIGHT (same dispatch, repo single-writer): SDC recette UX batch** — hops ALWAYS render g (kill g→kg auto-promote for hops; kg = inventory-only, operator rule); hop bill in PROCESS ORDER (mash→first_wort→boil 60→0→whirlpool→hop_stand→dry_hop, explicit order map per the convention above); **qty input per BRASSIN not per HL** (operator: "We never talk per hl in recette") — input ÷ brassin → `qty_per_hl` STORAGE UNCHANGED, brassin from the existing scaleLabel/commissioning source. ⚠️ PM flag: the ÷-brassin conversion MUST use the recipe's EFFECTIVE brassin (incl. the per-recipe Capacités override), not the global default — wrong denominator silently corrupts qty_per_hl (cost-affecting via non-malt/hops gap-fill).
Operator-specified: add 'X' to `FORMAT_SUFFIXES` in `public/modules/form-packaging.php` so cage runs are inputtable. **Operator inputs ACTUAL bottle count** (no auto-1027 — fill varies; revisit cages×1027 only if data shows constancy). PM-verified facts the build rides on:
- **ref_skus -X rows (8: ZEP/MOO/EMB/STI/SPY/DIV/DOA/ALT):** sku_code `<BEER>-X` WITH dash, format_id=6, units_per_pack=1027, hl_per_unit=3.38910, active. **format_code='X' + run_type='bot' ALREADY set on `ref_packaging_formats` id=6** → the (recipe_id, format_code) fallback JOIN in `resolve_packaging_sku_id()` resolves -X with NO dash special-casing (the literal step-2 path uses header neb_beer UNCHANGED, not beer+suffix composition — the fallback is the real resolver for suffix rows, as for -B today).
- **run_type='bot'** → vendable/beer-tax/loss formulas + the packaging dashboard (run_type-grouped) work UNCHANGED: vendable_hl = units/1027 × 3.3891 = 0.0033 HL/bottle, exactly the -B mechanics. Partial cages = fractional cages, fine (units are bottles).
- **-X BOM is per-CAGE-unit** (PM-verified ALT-X: 3 lines bottle+caps+label, qty_per_unit=1027 each) → `derive-packaging-consumption.ts` (units ÷ units_per_pack × qty_per_unit, maltytask `942431e`) yields bottles correctly. No box/scotch/sticker — already correct.
- **⚠️ ONLY 8 BEERS HAVE -X SKUs** (no DIB/EST/DOC/DGD/EPH*-X). A cage run for a beer without one → fallback returns NULL → `sku_meta_missing` refusal. Create missing -X rows from the 2026-06-08 inventory list (canonical seeding: recipe_id + format_id=6 + 1027/3.38910 + `<PREFIX>-X`) + recompile their packaging BOM.
- **ANCHOR HOME = `inv_fg_stocktake`** (exists: sku VARCHAR/sku_id_fk/month_closed/qty/source ENUM incl. 'manual-adjustment') — one row per -X SKU, **qty in BOTTLES**, counted_at=2026-06-08, source='manual-adjustment', sku_id_fk set. NO new table; the Stage-2 cage/box residual ledger later opens from here + X-event flows. Do NOT also write BSF StockTake (FG BSF-exit leg pending; monthly FG-form cage counts become the recurring reconcile, not a second store).
- **Build surfaces:** FORMAT_SUFFIXES const + `must_be_one_of` + window.FORMAT_SUFFIXES/JS picker (packaging-form.js) + parallel sub-form (main -B + parallel -X is THE expected usage, the DOAB scenario) + sticky/edit prefill + verify the format-tile path (tiles query `f.run_type <> ''` may already surface -X — align tile vs suffix-selector). NK `nebuleuse_format_suffix='X'` is a new value, no mig.

## §DIGB OPERATOR-REVISION INCIDENT + COSTING-BASIS RULING (2026-06-07, PM-verified live)
**Context:** operator BOM revision on DIGB (sku 12, fmt 1, r27; DIG = Qrew CollabIn label-on-box mechanism-3). 3 issues, live-DB verified:
1. **TEA = template altitude, operator-NOT-assignable — UI refusing it is CORRECT.** Mig 272 TEA template line slot=`tea` on all 6 glass formats; DIGB bom row tea ×24 = 0.487993 CHF compiler-emitted. Operator add = double-count. Optional UX: render template-tier lines "déjà inclus / read-only-with-provenance" in the revision flow.
2. **DIGB duplicate-label incident:** operator session wrote 6 choices sku 12 — id 25 box_label=632 (mig-272, CORRECT) + **id 53 sticker=632 (DUPLICATE — expired)** + ids 48/49/51/52 = redundant default-pins (numerically identical — compiler takes qty from template, set_sku_choice hardcodes 1.0 — but altitude noise; expired too). ✅ EXECUTED 2026-06-07 by orchestrator (PM-sanctioned): all 5 expired effective_until=CURDATE() + log_revision (claude-cli) + sdc_recompile_recipe_packaging(27) — parity_ok, 0 errors.
3. **UI GAP (small build, PM-blessed): bom-review.php has NO clear_sku_choice action** — set_sku_choice requires mi_id_fk>0 (replace-only, irreversible mis-clicks; operator confirmed "it doesn't let me"). Build: expire-active-choice (sku,slot) + log_revision + recompile. EQUIP: coder+sql+ui+webapp-testing.
4. **PKG_LABEL_DIG (mi 632) UNPRICED** — cost empty on all 3 DIGB label lines; rides the 2026-06-08 Lenzlinger invoice-feed checkpoint; feed-2 covers.
5. **🔴 COSTING-BASIS RULING (option c, PM):** ref_sku_bom = CURRENT STANDARD COST (one fact, v_mi_cost current WAC per ①) — stays. Time-of-production actuals = month-ledger chain (inv_deliveries FIFO × inv_consumption → monthly COP; COGS = prev-month COP) — DIG's Feb-2025 actual lives THERE. As-of-date WAC in compiler REJECTED (parallel valuation store; breaks recompile idempotency; same principle as "NEVER use liquid_basis_floor_date for historical valuation — SCD2-v3's job"). Quantity side already time-true for one-brew beers. Per-batch actual cost = read-only report post-F2 (RM-retro/Stage-2 fold) — NOT an F2 gate. **REFINED by item 7 below (operator-binding oldest-invoice rule) — option (c) holds, with a coverage-gap branch.**
6. **🔴 COMPILER DEFECT — inactive-MI resurrection (found post-expire recompile, PM-verified in code+live; FIX BLESSED, Dispatch D1):** `$miById` SELECT (sku-bom-compile.php ~324) loads ALL ref_mi with NO is_active (doesn't even SELECT the col); `_bom_resolve_slot` {beer}-pattern exact+LIKE (~1729-41) matches deactivated MIs → every label-beer -B recompile resurrects its deactivated placeholder sticker (635/636/637/640) at NULL cost; this — not the executed model — is why DOCB/DGDB/DIPB/DIGB carried sticker×1+box_label×1. Cost-invisible (NULL) so Gate B passed = the soft-delete-flag-not-enforced bug class (sql skill). **FIX:** SELECT adds `m.is_active`; {beer}-pattern path (exact + LIKE) requires is_active=1; `_bom_build_rq_row` candidate enumeration skips inactive. **EXEMPT (explicit references, unchanged):** Tier-1 choices, Tier-2 bindings, fixed-template default_mi_id_fk, `_bom_resolve_scotch`, AND all liquid/observed lines (Brewing rows legitimately cite inactive HOPS_TETRAHOP_GOLD — observed consumption is fact; F2 must NOT is_active-filter observed MIs).
   **§8.1 AMENDMENT (PM ruling — mechanism exclusivity, structural):** for a 24-box, box decoration = exactly ONE of 3 mutually-exclusive mechanisms: (1) box-sticker — REQUIRED iff scotch=TRANSP **AND no box_label resolves non-NULL**; (2) branded scotch → sticker intentionally absent (existing); (3) **box_label resolves a non-NULL MI → sticker intentionally absent, no line, no RQ (NEW)**. An ACTIVE Tier-1 sticker choice (mi_id_fk NOT NULL) overrides suppression (explicit beats structural — ZEPC-47 principle). Implementation: pre-resolve box_label before the slot loop (mirror the scotch pre-pass ~750-773); add the suppression branch in the §8.1 block (~792). Sticker unresolved + scotch=TRANSP + no box_label → RQ unchanged.
   **PURGE = recompile-all after fix** (compiler DELETE+reinsert is the purge; no manual DELETE). EXPECTED DELTA (PM census 2026-06-07): exactly 8 SKUs change, −1 sticker line each — DGDB/DIGB/DIPB/DOCB (inactive purge) + EPH1-4B (box_label suppression; their sticker MIs 641-644 stay ACTIVE serving EPH1-4C can-boxes, whose sticker lines must be UNCHANGED); all 8 lines cost NULL → aggregate CHF unchanged 2001.45 under the step-② metaQuery; 0 new RQ; 0 packaging rows referencing is_active=0 post-run; idempotent; parity 0. EPH*B sticker removal = executing the walk-ledger line ("label-beers' -B drop sticker incl EPH*B") — flag in operator report anyway. Opus verifies independently (standing COGS-claim discipline). clear_sku_choice (item 3) RIDES Dispatch D1 as commit 2.
7. **🔴 OLDEST-INVOICE COSTING RULE (operator-BINDING, Kouros verbatim 2026-06-07: "use the oldest possible invoice you have… 670 EUR/Tonne for the pilsener, not 498"):** refines ruling 5 — BOM stays current standard cost EXCEPT when the MI's delivery coverage postdates the recipe's liquid basis window. **Semantics (PM):** trigger **per-MI-line**: MI has ≥1 inv_deliveries row (exclusion_class NULL, qty_delivered>0, ANY status incl. Consumed) AND recipe basis-window END (latest basis batch date) < MI's earliest `date_received` → cost the line from the OLDEST delivery by date_received: `chf_unit = total_chf/qty_delivered` of that row (CHF-normalised at source; NEVER unit_price×hardcoded FX). Provenance tag `cost_basis/resolution='oldest_invoice'`. No deliveries at all → existing catalog/no_basis fallback. Coverage overlaps basis → current WAC, unchanged. **Home: F2-compiler-LOCAL — v_mi_cost UNTOUCHED** (shared, ②-signed-off; trigger needs per-recipe basis context a per-MI view can't carry; no new shared view — compiler-local query). Idempotency preserved (oldest delivery is append-only-stable, unlike as-of WAC). **Gate: NEW apply-gate 15** (appended, no renumber). **Applies to existing stale liquid rows automatically at ③ apply** (F2 replaces liquid wholesale; DIG/DIP 1-brew beers = the trigger class). **Opus reference number: MALT_PILSENER (mi 3) oldest = delivery id 113, 2026-01-14, 16461.90/26000 = 0.633150 CHF/kg (=0.670 EUR×0.945) vs current WAC 0.47061** — DIG/DIP pilsener lines must show 0.633150. Build INTO the F2 dry-run BEFORE the joint per-beer review (operator reviews final numbers), regenerate preview. Scope = F2 LIQUID ONLY; packaging stays current-standard (a packaging analog would be a separate operator decision).
6. **🔴 OPERATOR REFINEMENT (Kouros, 2026-06-07, same session — POST-dates ruling 5):** "when consumption is older than the prices shown, simply use the oldest possible invoice you have" — i.e. Pilsener for DIG (1 brew, Feb-2025 per walk ledger — predates ALL pilsener delivery rows, which start 2026-01) should cost **0.670 EUR/kg (oldest invoice, Jan-2026)** not 0.498 (current WAC from sole Active delivery). Proposed rule for F2: per-MI, if the recipe's basis-batch window predates the MI's earliest delivery coverage → cost basis = OLDEST delivery unit price (CHF-normalised), flagged cost_basis='oldest_invoice'. Needs PM integration ruling into the F2 10-point gate (see incident §5b below).

## §D1 SHIPPED + OPUS-VERIFIED (2026-06-07 night)
Commits `5186454` (sku-bom-compile: active-only {beer}-pattern + RQ-candidate filter + §8.1 box_label suppression w/ explicit-choice override) + `9fec7d5` (bom-review clear_sku_choice "retirer" button), deployed, not pushed. Recompile-all: 846 del/838 ins = net −8 = EXACTLY the predicted NULL-cost phantoms (DGDB/DIGB/DIPB/DOCB inactive-purge + EPH1-4B suppression); EPH1-4C/ZEPC/sticker-beers intact; idempotent (838/838 2nd pass); 0 RQ. **Total byte-stable: ALL-SKU bom_source='packaging' = 2001.4473 CHF, 580→572 lines; composites 11.9176 — this IS the metaQuery definition of the signed-off 2001.45 (includes inactive BLA/BLO 21 lines/13.64; active-only reads 1987.80/551 — population difference, not regression).** SKU 306 pre-existing format_id=0 skip unchanged. BLO4C inactive-MI outer_tray row = out-of-scope (inactive SKU). LESSON FOR BRIEFS: gate numbers must ship with their exact SQL — D1 agent couldn't reproduce 2001.45, proceeded on a self-derived invariant (held, but stop-rule was unenforceable). D2 (oldest-invoice + gate 15 into F2 dry-run) is next dispatch.

## §HISTORICAL-INVOICE BACKFILL (operator-announced 2026-06-07) — STANDING WATCH-ITEMS
Kouros will mass-upload PAST invoices after current dev settles, to replace oldest-invoice heuristic with actual time-of-production prices. (1) D2 rule self-improves (compile-time dynamic). (2) **PM MUST RE-RULE TRIGGER SEMANTICS at backfill time:** extended coverage makes the predates-coverage trigger stop firing on historical windows → silent flip to current WAC (wrong direction for old brews); as-of-window WAC over fixed basis window is append-only-stable and becomes viable then — the option-(a) rejection was for OPEN-ENDED as-of-date, revisit scoped to fixed windows. Gate-15 table = the detection surface. (3) **🔥 BACKFILL PRE-FLIGHT GATE: v_mi_cost reads Active-only — historical rows must land pre-Consumed/excluded or they poison current WAC brewery-wide.** Wire this into the backfill arc plan BEFORE first mass upload.

## §D2 SHIPPED + OPUS-VERIFIED + PUSHED (2026-06-07 night, `9fa60a7`)
Oldest-invoice rule + gate-15 live in F2 dry-run, exactly per ruling B (compiler-local; v_mi_cost untouched; 'oldest_invoice' provenance; preview JSON key gate15_oldest_invoice, 25 entries). Opus-verified: DIG/DIP pilsener 0.63315 dlv-113 ✓; clean recipes zero OI ✓; ref_sku_bom 2127/2250.0036 untouched, packaging 2001.4473 ✓; incident-#4 fix confirmed landed (EMB AMARILLO 264.66 n=7 / SIMCOE 132.33 n=7 = ground-truth). Known-minor: OI + null-conversion returns [null,'oldest_invoice'] (provenance slightly misleading; no current MI affected). F2 is now REVIEW-READY: next = joint per-beer review against the 15-point gate, then apply. Backfill watch-items (§HISTORICAL-INVOICE BACKFILL) unchanged.

## §MULTI-BREW BASIS BUG — ✅ FIX LANDED + OPUS-VERIFIED (2026-06-08, commit `9012c8d` NOT pushed) — apply-gate-16 RESOLVED-pending-operator-rewalk
**STATE: ✅ FIX LANDED + INDEPENDENTLY OPUS-VERIFIED 2026-06-08. apply-gate-16 (a)-(e) ALL PASS. F2 multi-brew digest REBUILT off corrected preview; operator resumes the walk on corrected numbers. apply-gate-16 RESOLVED-pending-operator-rewalk (no longer a build blocker; the remaining gate is the operator's multi-brew re-walk).**

**✅ FIX AS LANDED (maltyweb commit `9012c8d`, NOT pushed; file `app/sku-bom-compile.php` `compile_sku_bom_liquid`, no mig):** §2a GROUP BY now also computes `COUNT(*) AS n_brews` → `$batchNBrews`; §2b malt + hops_kettle accumulators multiply observed qty × n_brews per-batch BEFORE the MAD/averaging. Dry-hop, minerals/gap-fill, oldest-invoice, packaging UNTOUCHED. Defaults `n_brews=1` (single-brew identity). Built exactly to the canonical ruling below.

**✅ OPUS INDEPENDENT VERIFICATION (regenerated preview, generated_at 2026-06-08 13:03 UTC):** gate-16 (a)-(e) ALL PASS.
   - (a)/(b) **EMB total malt 21.03 kg/HL** (Munich 7.368 + Pilsener 13.662) vs recette 22.83 = **~8% real drift** ✓ (was 5.8 pre-fix); EMB batch-181 hand-check **347×4÷124.5 = 11.1486 kg/HL Pilsener** EXACT ✓.
   - (b) **STI 17.52 kg/HL** ✓ (second multi-brew, plausible).
   - (c) **DIG (single-brew, N=1) 10.27 kg/HL byte-UNCHANGED** (Pilsener 5.133 + Wheat 5.133) = REGRESSION GUARD PASS.
   - (d) **0 errors / 0 unresolved MIs.**
   - **Scope drift noted (NOT a fix artifact):** in_scope 142→147, total_active_skus 146→151 between runs = 5 new active SKUs created since yesterday (daily production drift — exactly why gate-14 requires dry-run regen at apply time). composite_excluded 4 / recipe_excluded 2 stable.

**✅ CHECK RESULTS (operator "check" request — all confirmed 2026-06-08):** saisie `form-brewing.php` enters ingredients per-batch = one-brew-charge (ONE ingredients header row per batch) + ONE cooling row per parallel brew ("Un brassin = un cycle… ajouter une ligne par cycle"). N_brews = cooling-row count is RELIABLE: 0 zero-cooling batches, every batch exactly 1 ingredient header, volumes confirm 1-brew-per-row; distribution 18.5% N=1 / 42% N=3 / 30% N=4. **The correct N_brews scaling ALREADY EXISTS in `warehouse.php` + `warehouse-export.php` WIP costing — F2 was the LONE consumer that omitted it** (resolves the §5 blast-radius question: ≥1 other consumer already does it right; no shared helper needed — F2 was the outlier, not the pioneer).

**🟡 TWO SEPARATE FOLLOW-UPS surfaced by this fix (NOT part of it; logged):**
   1. **DGD 18 kg/HL anomaly is NOT multi-brew** — DGD is single-brew (1 batch, n_cool=1); the 18 kg/HL Pilsener stands as a SEPARATE data issue (likely unit/typo in that one brew) → confirm vs brew record at DGD re-walk. (This is the MUST-DECIDE DGD MALT_PILSENER 18.04 item — now isolated as a data-entry issue, not a basis bug.)
   2. **`warehouse.php` `$glSt` FG-valuation query (~line 564)** may share the same one-brew ÷ batch-HL mismatch for GL cost ratios — SEPARATE review after F2 (the investigation flagged it; NOT confirmed bugged, needs a look). EQUIP coder+sql when picked up.

**RE-WALK CONSEQUENCE:** multi-brew lineup (EMB/MOO/DOA/STI/SPY/ZEP/DIV/EST/DOC + any ≥2-brew EPH) liquid malt/kettle-hop digest now VALID for re-walk; single-brew (DIG/DIP/single EPH) stood throughout. Digest REBUILT per-brassin off the corrected preview (multi-brew numbers updated, supersede note added; single-brew + dry-hop unchanged). Operator resumes the walk on corrected numbers.

---
**ORIGINAL RULING (kept for the record — the fix was built to this; now satisfied):**

**STATE (pre-fix, 2026-06-08): RESOLVED at the design level. Operator confirmed the grain-bill semantics; the open question on apply-gate-3 ("is 176 kg per-brew or whole-batch?") is now ANSWERED → fix mechanism is canonical. BLOCKS F2 APPLY until coded + dry-run regenerated.**

**CONFIRMED SEMANTICS (operator + maltyweb saisie, 2026-06-08):** A multi-brew batch = N parallel ~30 HL brews. The grain bill on the brewing-ingredients form is entered **PER SINGLE BREW** (each parallel brew uses that same charge). So `bd_brewing_ingredients_v2` = ONE per-brew record per batch; `bd_brewing_cooling` = N rows (one per brew). Triangulated on EMB: recette Pilsener 14.83 kg/HL; F2 wrongly 2.79; corrected 347 kg × 4 ÷ 124.5 = 11.15 kg/HL (≈ recette with real drift). **So 176 kg IS the per-brew charge** → Fix B confirmed (numerator × N_brews), NOT a denominator change.

**1. FIX MECHANISM — CANONICAL CHOICE = "numerator × N_brews where N_brews = COUNT(bd_brewing_cooling rows for the batch)".** This is the minimal, robust fix. Rejected the alternative ("denominator = batch_hl / N_brews = average single-brew volume") on edge-case robustness:
   - **Both are algebraically equivalent ONLY when all brews are equal-volume.** They DIVERGE on unequal brews and on a missing cooling row — and the divergence favors numerator×N.
   - **numerator × N** keeps the denominator as the TRUE total cooled volume (Σ real HL). The per-brew bill is physically repeated N times; multiplying by the count of actually-cooled brews is the faithful model of "this charge, brewed N times, into this much total beer". Unequal brews are handled correctly: the per-HL is (per-brew-qty × N) ÷ Σ(actual HL) = exactly the malt-mass-over-total-volume that COGS wants.
   - **denominator/N (average single-brew volume)** throws away the real per-brew volume distribution and re-introduces an averaging assumption; on a batch with one missing/zeroed cooling row it would compute the average over the SURVIVING rows but still divide a single-brew numerator, silently re-creating a partial version of the same mismatch. Worse failure mode.
   - **Edge — missing cooling row:** with numerator×N, N = count of *surviving* (volume>0) cooling rows (the §2a query already filters `cool_final_volume_hl > 0`); numerator scales by the same N that built the denominator, so the two stay consistent even when a brew's volume is absent. A genuinely missing cooling row degrades gracefully (under-counts N AND HL together → per-HL stays ~right; mass attribution is the only loss, and that's a data-gap, not a basis error). This self-consistency is the decisive reason.
   - **Edge — unequal brew volumes:** numerator×N ÷ Σ(real HL) is correct by construction (it's literally total grain ÷ total volume). The average-volume variant is NOT.
   - **CANONICAL: numerator × N_brews, N_brews = the count of cooling rows that built `batch_hl` for that (recipe,batch).** Compute N in the SAME §2a GROUP BY that produces `batch_hl` (add `COUNT(*) AS n_brews` alongside `SUM(cool_final_volume_hl)`), carry it in a `$batchNBrews[$rid][$batch]` index parallel to `$batchHl`. Do NOT recount elsewhere — single source of N.

**2. VOLUME-WEIGHTING INTERACTION — × N_brews goes on the PER-BATCH NUMERATOR, BEFORE the MAD/per-HL computation. Weighting stays correct.** The aggregator (`$computePerHl`, §3b) builds per-batch per_hl = qty ÷ hl for MAD, then sums surviving qty ÷ ΣHL. **The fix is to scale `qty` to batch-total (× N_brews) at the point the per-batch data enters the aggregator — i.e. fold N_brews into the numerator qty as it's read into `$batchQty`, so BOTH the per-batch per_hl (MAD input) AND the final Σqty÷ΣHL see the batch-total mass.** Consequences, all correct:
   - Per-batch per_hl for MAD becomes `(qty×N) ÷ batch_hl` = true kg/HL for that batch → MAD now compares real per-HL values across batches (today it compares ÷N values, which are internally consistent so MAD still "worked", but the fix makes them physically meaningful — no MAD-behavior regression because the relative dispersion within a recipe is unchanged by a per-batch scalar ONLY IF N is constant across batches; **N can VARY per batch** (a 4-brew batch and a 2-brew batch of the same recipe), so the per-batch scaling MUST happen before MAD, not after — confirmed this is the right place).
   - Final per_hl = Σ(qty_i × N_i) ÷ Σ(batch_hl_i) across the basis window. A 4-brew batch contributes (per-brew-qty × 4) to the numerator and its ~124 HL to the denominator; a 1-brew batch contributes (per-brew-qty × 1) and ~30 HL. **This IS the correct volume-weighting: each batch weights by its true total HL, and each batch's mass is its true total mass.** A 4-brew batch correctly weighs ~4× a 1-brew batch. ✓
   - **× N_brews on the numerator is the right place — NOT a post-hoc scale of the final per_hl, NOT inside the windowing/selection.** Windowing (basis-batch selection, ≤8, DH-gate) operates on batch identity, unaffected. MAD must see the scaled per-batch value (because N varies per batch) → scale at numerator-read. Confirmed.

**3. SCOPE — brewhouse branch ONLY. Confirmed.** Affected = §2b v1 path: malt + kettle/whirlpool/first-wort hops (`bd_brewing_ingredients_parsed` category IN ('malt','hops_kettle'), source_id → `bd_brewing_ingredients_v2`). **Dry-hop branch UNTOUCHED** — its numerator is already batch-total: §2b3 sums `dh_qty` over ALL DryHop events in the batch (`GROUP BY recipe_id_fk, batch, dh_mi_id_fk` with `SUM(dh_qty)`), so `dhV2[rid][batch][mi]` = whole-batch dry-hop mass ÷ whole-batch HL = correct (verified: Amarillo 265 g/HL < recette 333, sensible). **Minerals/process/adjunct (§2c v2 path + ref_recipe gap-fill) UNAFFECTED** — those are recipe-spec per-HL (`ref_recipe_ingredients.qty_per_hl`), never observed-divided. Confirm: the × N_brews scaling applies SOLELY to the §2b v1 malt/hops_kettle qty as it enters `$batchQty` for the brewhouse stage. Do NOT touch the dry-hop `$batchQty` build, the v2 build, or the recipe gap-fill.

**4. ACCEPTANCE GATE — NEW apply-gate-16 (appended, no renumber).** Before the operator sees the regenerated digest:
   - (a) **EMB total malt ≈ recette order-of-magnitude** — post-fix EMB brewhouse total malt must land ~14-21 kg/HL (recette Pilsener 14.83 alone + Munich; corrected Pilsener already hand-verified 11.15 kg/HL on b181). Pre-fix ~5.8 total is the failure signature.
   - (b) **Opus hand-verifies TWO multi-brew beers** — EMB + one other (pick DOA or STI, N≥3) — recomputing (Σ per-brew-qty × N) ÷ ΣHL by hand against the regenerated preview, EXACT match (not order-of-magnitude — exact, per the COGS-bearing-claims discipline).
   - (c) **Opus confirms a single-brew beer is BYTE-UNCHANGED** — DIG (N=1) malt/hops per-HL must be identical pre/post fix (N_brews=1 → ×1 → no change). This is the regression guard proving the fix is surgical.
   - (d) **0 errors / 0 unresolved MIs** in the regenerated dry-run; scope count unchanged (147 incl. contract / 142 Neb).
   - Gate-16 PASS is a precondition for the apply dispatch AND for the operator re-walk.

**5. BLAST RADIUS BEYOND F2 — fix lives in the F2 compiler; OTHER consumers get a SHARED helper IF they share the join, decided per-consumer after the parallel audit reports.** Architectural call:
   - The bug is **specific to the per-brew-record ÷ batch-total-HL division**. Any consumer that divides `bd_brewing_ingredients(_v2)` per-batch mass by `SUM(cool_final_volume_hl)` has it; any consumer that reads observed mass WITHOUT dividing by batch HL (e.g. raw consumption sums, deplete-deliveries which consumes absolute qty, packaging-consumption which keys on SKU/units not brewhouse HL) does NOT.
   - **Likely-clean (raw-mass, no HL division):** packaging-consumption (`derive-packaging-consumption.ts` — keys on `ref_skus.units_per_pack`, no brewhouse HL); deplete-deliveries (FIFO on absolute qty); COP brewing consumption (lot-tracked absolute consumption). These consume MASS, not mass-per-HL — unaffected. Confirm in the audit but expect clean.
   - **At-risk (per-HL derivations):** the drift aggregator (if it computes observed kg/HL to compare against recette per-HL), any COGS-per-HL or efficiency-per-HL builder, the recipe-profile per-HL table. If any of these re-divides per-batch mass by batch HL, it shares the bug.
   - **RULING:** do NOT pre-build a shared helper speculatively. The F2 compiler fix is self-contained (N_brews from its own §2a query). **IF the audit finds ≥2 other consumers with the same per-brew÷batch-HL division, THEN extract a single shared SQL view or PHP helper** — `v_observed_qty_per_hl` (or a `brewhouse_qty_per_hl(recipe,batch)` function) that bakes in `Σ(per-brew-qty × n_brews) ÷ Σ(cool_final_volume_hl)` ONCE — and repoint all consumers at it (one fact, one canonical derivation — the house rule). If the audit finds only F2 has it (the others are mass-based), leave the fix compiler-local. **Decide after the audit; don't build the helper on spec.** Either way: the N_brews count is the same scalar everywhere (count of volume>0 cooling rows per batch) — if a shared helper is built, that count is its single source.

**6. RE-REVIEW CONSEQUENCE — confirmed: ENTIRE multi-brew lineup's liquid digest is INVALID post-fix and must be rebuilt + re-walked. Single-brew beers' liquid STANDS.**
   - **INVALID (must rebuild + re-walk):** every multi-brew beer = EMB, MOO, DOA, STI, SPY, ZEP, DIV(+variants), EST, DOC, DGD, and any EPH family member brewed as ≥2 parallel brews. Their malt + kettle/whirlpool/first-wort hop per-HL numbers in the current digest (`reports/f2-liquid-review-digest-2026-06-08.md`) are ~÷N too low and are VOID. Their dry-hop numbers in the same digest STAND (dry-hop branch untouched).
   - **VALID (stands, no re-walk needed for the malt/hops basis):** single-brew beers (N=1 in `bd_brewing_cooling`) — DIG, DIP, and most EPH single-brews. Their brewhouse per-HL is ×1-unchanged. Gate-16(c) proves this. (Their non-basis decisions — oldest-invoice, DIG PROC_YEASTVIT scale fix, etc. — are orthogonal and still apply.)
   - **OPERATOR SEQUENCING:** the current digest walk should PAUSE on multi-brew beers (their numbers are about to change) and may proceed on single-brew beers (DIG/DIP) whose basis is final. After the fix + regenerate, rebuild the digest and re-walk ONLY the multi-brew set. This salvages the DIG/DIP/single-EPH walk already in progress.

**DISPATCH (when operator gives go): EQUIP `coder`+`sql`.** Files: `app/sku-bom-compile.php` ONLY (no mig — read-side compute change). Steps: (1) add `COUNT(*) AS n_brews` to §2a GROUP BY; index `$batchNBrews[$rid][$batch]`; (2) fold `× n_brews` into the §2b v1 malt/hops_kettle qty as it's read into the brewhouse `$batchQty` (NOT v2, NOT dry-hop, NOT gap-fill); (3) regenerate dry-run; (4) Opus gate-16 (a)-(d) BEFORE operator. This is COGS-bearing → Opus verifies independently per standing discipline; a sub-agent "PASS" is a claim, hand-verify EMB + one other + DIG-unchanged.

## §F2 LIQUID — ✅ APPLIED LIVE (2026-06-09) — KEYSTONE ③ DONE
**STATE: DONE. F2 liquid BOM is applied to `ref_sku_bom` on the VPS. The §F2 REVIEW HANDOFF CHECKPOINT below is now HISTORICAL (operator completed the walk + annotation; apply predicate built + shipped + run).**

**APPLY RUN (2026-06-09):** canary single-SKU first (EMBBU id=79), then FULL run = **147 SKUs applied, 1260 deleted → 1557 liquid rows inserted, 0 parity violations, 0 apply errors.** Post-apply `ref_sku_bom` population (healthy): Brewing/`bom_source='liquid'` **1557** (new F2, all `currency='CHF'`); Packaging/`packaging` 576 (untouched); Brewing/`composite_liquid` 204 (untouched); Packaging/`composite_packaging` 83 (untouched); Brewing/NULL 7 (out-of-scope SKUs — 2 recipe-missing + edges, not in the 147 in-scope set; cosmetic).

**CODE SHIPPED (committed + pushed, maltyweb commits `9012c8d` + `0956471`):**
- `app/sku-bom-compile.php`: `compile_sku_bom_liquid()` apply branch added (was dry-run-only). Per-SKU transaction; `DELETE source='Brewing' AND (bom_source IS NULL OR ='liquid') AND mi_id IS NOT NULL`; INSERT proposed lines; **packaging/composite PARITY GATE** via new helper `_bom_packaging_composite_snapshot()` (pre/post snapshot of `source='Packaging' OR bom_source IN('composite_liquid','composite_packaging')` must match within 1e-6 or rollback that SKU). New return key `apply_stats`. Skips zero-window (`skipped_zero_window`) + errored SKUs (no wipe). Idempotent (DELETE+INSERT, live recompute each call = "regenerate at apply").
- `scripts/sku-bom-liquid-cli.php`: `--apply` unblocked; apply summary + post-apply verification.

**🔒 CONVENTIONS LOCKED at apply (COGS-impacting — durable):**
1. **Liquid rows are CHF-canonical.** `cost`=CHF (from `v_mi_cost` WAC / oldest-invoice `chf_unit`), `currency`='CHF', `price`=round(cost/qty_per_unit,6). RATIONALE: ALL downstream readers (`sku-costs.php`, `sku-cost-detail.php`, `warehouse.php`) already `SUM(b.cost)` and label it "CHF". The OLD liquid rows stored cost in the MI's RAW currency (EUR for many) — a latent EUR-summed-as-CHF bug that this apply FIXES for liquid. (Packaging rows were already CHF.)
2. **`slot_name` stores the STAGE** ('brewhouse'/'dry_hop') for liquid rows. No stage column exists; this keeps the same MI in kettle + dry-hop distinct under UNIQUE key `uniq_sku_ing_src_slot (sku_id, ingredient_raw, source, slot_name)`. row_hash (no UNIQUE) also includes stage. Audited all 6 downstream readers: none require liquid `slot_name=NULL`.

**OPERATOR DECISIONS RATIFIED (AskUserQuestion, 2026-06-08/09):**
1. ✅ **DIG PROC_YEASTVIT FIXED** — `ref_recipe_ingredients` id=37 (r27) was 0.000354 kg/HL (unit slip, ~22× below standard); UPDATEd to 8.0 g/HL (house standard, matches all 10 other recipes). Direct SQL UPDATE. (= former MUST-DECIDE #1, RESOLVED.)
2. **DIV finings (PROC_NAGARDO) KEPT as gap-fill** for past events (60 g/brassin = 55.72 CHF/brassin). Going forward → finings will be OPERATOR-INPUTTED on the saisie forms (observed data). → **FORWARD BUILD ITEM: add a finings input field to the brewing and/or racking saisie forms** so finings flow as observed; until built, gap-fill stands.
3. ✅ **Floor dates 2025-10-07 RATIFIED** — SPY (9 DK brews kept, 45 excluded) + DIB (3 kept, 3 excluded). (= former apply-gate 9, RESOLVED.)
4. ✅ **Gate-15 oldest-invoice RATIFIED** (PM-ruled 2026-06-07; 27 substitutions triggered).

**CORRECTION to prior arc notes — DGD MALT_PILSENER 18 kg/HL is NOT an error** (= former MUST-DECIDE #2, CLEARED). Verified vs raw brew record (batch 1, 2026-03-25: Pilsener 635 + Wheat 135 + Oat 90 = 860 kg into 35.2 HL = 24.4 kg/HL total). Legitimate DrunkBeard "Galactic Drift" double-IPA grain bill; single-brew so unrelated to the multi-brew bug. Digest's "likely typo" flag was overcautious. NO action.

**FOLLOW-UPS (NOT blocking F2; logged for later):**
- **EMBV CHF/HL ≈ 3423 anomaly** — packaging cost 34.02 CHF on a tiny `hl_per_unit`; its LIQUID is correct (21.8 CHF/HL, consistent with EMBF). Packaging-BOM / `ref_skus.hl_per_unit` oddity for the -V cuv SKU, separate from F2. Investigate later. EQUIP coder+sql. **⬆️ ESCALATED 2026-06-09 — this is NOT cosmetic; it is the ZEPV litre-multiplication blocker (see §COGS COST-SOURCE CUTOVER below). ZEPV is the same defect AND is sold in May.**

## §F2 FOLLOW-UP DESIGN BRIEFS — schema-verified 2026-06-09 (PM, live DB)
**Two F2 follow-up builds briefed (operator "knock out F2 follow-ups"). Both grounded against live schema/code. warehouse.php $glSt FG-valuation follow-up = CLEARED by operator (reads inv_tank_balances.brew_cost_per_hl which is already per-HL; $glSt L564 computes only a GL-split RATIO, invariant to multi-brew scaling — NOT affected by the multi-brew bug. RESIDUAL: whether inv_tank_balances.brew_cost_per_hl is itself multi-brew-correct lives in the Node tank-sim pipeline = SEPARATE, still open).**

**BUILD 1 — FININGS AS OBSERVED INPUT (verified facts):**
- Finings MIs exist: **PROC_NAGARDO id=79, PROC_CLAREX id=76, PROC_DEHAZE id=77** (also PROC_SUPER_F 81, PROC_DWB 68, PROC_IMOBUM 69 are clarification-family) — ALL category `Process Chemical` (cat_id=5).
- `bd_brewing_ingredients_parsed_v2.category` ENUM = `('malt','hops_kettle','hops_dry','adjunct','mineral','process')` — **already has `'process'`. No enum/category migration needed.**
- **The brewing saisie form (`public/modules/form-brewing.php`) ALREADY exposes finings.** L937 `$miCatalog` WHERE c.name IN (...,'Process Chemical'); L953 `$catToSlug['Process Chemical'=>'process']`. NAGARDO/CLAREX/DEHAZE are pickable under the 'process' chip TODAY. CLAREX (15) + DEHAZE (10) already observed in parsed_v2; NAGARDO not yet.
- **The liquid compiler ALREADY reads observed finings.** `app/sku-bom-compile.php` §2c L2461-2474 reads `bd_brewing_ingredients_parsed_v2 WHERE category IN ('adjunct','mineral','process')`; merge L3141-3220 does observed-wins (`source='observed'`) THEN ref_recipe gap-fill (`source='recipe_gapfill'`, malt/hops excluded). **Observed finings → BOM automatically; once observed exists for DIV, the NAGARDO gap-fill retires for go-forward batches by construction.** NO compiler change.
- **`bd_racking_v2` has NO ingredient-capture columns** (verified full col list) — racking captures zero ingredients today. So "finings at racking" = NEW data model (header table + parsed table mirroring brewing, OR a lightweight columns-on-racking approach), and the compiler would need a 4th observed source. PM RULING: finings go on the BREWING form only for v1 (kettle/whirlpool + conditioning-via-brewday is the dominant case; the form already works; zero build). Racking-side finings = DEFER unless operator says fining is materially done at racking for specific beers; if so it is its own arc (new racking-ingredient table + compiler §2e), not a quick add.
- **NET: Build 1 is mostly a NO-CODE operator-training item + a small UX nudge.** Only real build = optional UX polish to make finings discoverable (e.g. a labelled "Collage / finings" hint or a dedicated chip alias for the process subset) — small, ui-only. The data path is already end-to-end.

**BUILD 2 — -V (CUVE DE SERVICE) COSTING (verified facts):**
- -V SKUs: **EMBV(27,r32), MOOV(46,r44), ZEPV(61,r57)**, all `format='Cuve de service'`, **format_id=18, hl_per_unit=0.01, units_per_pack=1**.
- Current packaging BOM = **template-fixed**: `ref_packaging_items` format 18 = 2 rows (id57 liner_client, id58 liner_transport), each qty=1, default_mi=102, scope=we_supply_only → compiler emits 2 fixed liner lines @ 17.01 CHF (18 EUR×0.945). Ignores observed entirely.
- **Observed data is ALL present in `bd_packaging_v2`**: `run_type` enum includes `'cuv'`; `vendable_hl` DECIMAL(8,3)=litres/HL filled per row; `new_liner_client` TINYINT + `liner_client_mi_id_fk`; `new_liner_transport` TINYINT + `liner_transport_mi_id_fk`. Operator inputs liner count/identity + fill volume PER ROW.
- **Empirical proof of operator ruling (254 cuv rows):** 196/254 rows have NEITHER new liner (topped up); only 36 client + 33 transport new liners. ZEPV(sku61,r57): 214 rows, 1457.24 HL filled, 36+28=64 new liners → amortized liner ≈ **~0.77 CHF/HL** vs template **3402 CHF/HL** (34.02÷0.01). DATA-QUALITY NUANCE: older rows have the `mi_id_fk` populated but the boolean NULL (and vice-versa) — the branch must treat "liner present" = (boolean=1 OR liner_*_mi_id_fk IS NOT NULL), resolving the MI from the *_mi_id_fk (fall back to default 102 only if a flag is set but mi NULL).
- **PM RULING (hl_per_unit for -V):** set **`ref_skus.hl_per_unit = 1.0`** for the 3 -V SKUs (cost expressed PER HL; sku-costs.php `SUM(cost)/hl_per_unit` then reads CHF/HL directly with denominator 1). This matches ZEPV being sold by the LITRE/HL, not by a fixed unit. The per-unit model does NOT meaningfully apply to a variable-fill cuve → make the "unit" = 1 HL.
- **PM RULING (costing model):** -V needs an OBSERVED branch in the PACKAGING compiler (parallel to F2 liquid), NOT the template. Per -V SKU: `liner_cost_per_hl = (Σ over cuv rows of (n_new_liners_on_row × resolved_liner_cost_chf)) ÷ (Σ vendable_hl over those rows)`, volume-weighted over the same trailing/floor window convention F2 uses. Emit ONE `liner_amortized` packaging line @ that per-HL cost (qty_per_unit = per_hl × hl_per_unit = per_hl × 1.0). Suppress the 2 template liner rows for cuv. Liquid lines unaffected (already correct per-HL).
- **Sequencing dependency:** the F2 liquid apply for the 3 -V SKUs computed `qty_per_unit = per_hl × 0.01` (the bad hl_per_unit). After hl_per_unit→1.0, the -V LIQUID rows MUST be re-run (re-compile_sku_bom_liquid for recipes 32/44/57's -V SKUs) so liquid qpu = per_hl × 1.0. So Build 2 = **data fix (hl_per_unit) + compiler change (observed liner branch) + targeted re-apply of liquid AND packaging for 3 SKUs.**
- **Build 2 directly UNBLOCKS the COGS cost-source MySQL cutover** (§COGS COST-SOURCE CUTOVER above): ZEPV is the lone gate; the observed-liner amortization is fix-option (a)/(b) combined and is the canonical fix. After Build 2 + regenerate, run `--cost-source=diff` to confirm ZEPV no longer explodes, then flip.
- **🔧 OPERATOR-CORRECTED PRINCIPLE (2026-06-09, via the PARALLEL F2/compiler session — supersedes the static per-HL approximation in the PM RULING above for the amortization BASIS): do NOT amortize the liner over the tank/liner NOMINAL capacity (10HL / 1000 L) NOR over a window-averaged static per-HL constant — because fills are frequently PARTIAL. Fold the liner(s) cost onto the EXACT packaged volume ANNOUNCED ON THE PACKAGING ROW (the `bd_packaging_v2` fill-event `vendable_hl`).** ⟹ per-litre liner cost = `(n_new_liners_on_event × resolved_liner_cost_chf) ÷ event_announced_fill_volume`, EVENT-DRIVEN, not a static per-SKU ref_sku_bom constant. This means liner cost is **fill-event-variable** and therefore CANNOT be a static `ref_sku_bom.qty_per_unit` — it belongs in the **packaging-consumption / `inv_consumption` layer per event** (option (a)), where the event + its announced volume are known. The static volume-weighted per-HL line in the PM RULING above is at best a fallback APPROXIMATION; the operator's canonical direction is the event-level path. (Static per-HL was correct as a first approximation; the partial-fill correction makes it event-driven.)
- **🔴🔴 ADJUDICATION REVERSAL (2026-06-09, Opus live-verified — SUPERSEDES the `hl_per_unit=1.0` PM RULING above):** the `92c9c13` per-HL choice (`ref_skus.hl_per_unit=1.0` for EMBV/MOOV/ZEPV) is the LONE OUTLIER and BREAKS sales-COGS material. **CANONICAL -V BASIS = PER-LITRE, `hl_per_unit = 0.01`** — matching (a) `ref_packaging_formats` id=18 `hl_per_unit=0.01` (the format is litre-denominated, '1L liner'), (b) `inv_sales_bc` ZEPV uom='L' + `hl_resolved`=litres/100, (c) JASPL (the contract -V sibling) at 0.01, and (d) the sales pipeline itself. **ROOT CAUSE of the inconsistency (live-traced):** `lib/repos/mysql-sku-bom.ts` builds `skuHL` from `ref_packaging_formats.hl_per_unit` (=0.01, line 119/142/179) but `skuCost` = `Σ ref_sku_bom.cost` where the liquid/liner rows were compiled at `qty_per_unit = basis_per_hl × ref_skus.hl_per_unit (=1.0)` → **same SKU, TWO bases.** `build-sales-cogs.ts` computes `units = Σ qty_invoiced` = **litres** (9,700) and `material = units × skuCost`. With skuCost=9.8873 (a PER-HL number) → **9,700 × 9.8873 = 95,907 CHF (~100× phantom).** Beer-tax stayed sane (12,025) because it uses `hl = units × skuHL` = 9700×0.01 = 97 HL ✓ — proving the split. Live ZEPV `ref_sku_bom`: 10 liquid rows (per-HL magnitudes qpu 5-76) + 1 `liner_amortized` 0.9439/HL, Σ=9.8873. **WHY MY PRIOR RULING WAS WRONG:** I optimized `hl_per_unit=1.0` for the `sku-costs.php` DISPLAY path (`SUM(cost)/hl_per_unit` reads CHF/HL with denom 1) and did NOT account for `build-sales-cogs` multiplying litre-units × per-unit cost. The `92c9c13` "phantom GONE" gate-PASS verified the CHF/HL display (9.89) + packaging conservation but NEVER verified the sales-COGS path — an insufficient COGS-bearing gate (the exact failure mode in the standing discipline). **RESOLUTION — REVERT to per-litre:** parallel/liner session sets `ref_skus.hl_per_unit = 0.01` for EMBV/MOOV/ZEPV and re-compiles BOTH the -V liquid AND the `liner_amortized` packaging line at the 0.01 basis (their lane: `app/sku-bom-compile.php` + targeted re-apply of the 3 SKUs). Then ZEPV skuCost ≈ 0.098873 (per-litre), and `units(litres) × skuCost = 959.07 CHF` = correct (= 97 HL × per-HL). After regenerate, cutover thread re-runs `--cost-source=diff`, confirms ZEPV sane, then the flip is a clean cutover (units=litres × per-litre cost). **CONSISTENCY REQUIREMENT (LOCKED): all -V/litre-denominated SKUs (cuv AND contract bulk -V/Vrac) carry `ref_skus.hl_per_unit = format.hl_per_unit` — NEVER diverge ref_skus from the format. JASPL=0.01 is canonical; EMBV/MOOV/ZEPV MUST match it.** The event-driven partial-fill liner principle (next bullet) is UNAFFECTED — the amortized liner per-litre cost is the same number whether expressed as qpu×0.01 or per-HL×1.0; only the `hl_per_unit` multiplier changes so the sales pipeline's litre-units land correctly. **Sequencing: this is in the LINER session's lane (owns `sku-bom-compile.php`); the cutover thread HOLDS the flip until the revert+recompile lands (it is right to hold — do NOT flip with the 100× ZEPV live).**
- **🚧 OWNERSHIP / COORDINATION BOUNDARY (2026-06-09): the PARALLEL Claude session OWNS the -V liner / announced-volume fix and is mid-design on the cuv liner cost.** It has NOT yet committed any compiler change (verified: `app/sku-bom-compile.php` byte-identical local↔VPS, all on main, no branch/stash/uncommitted). **The cutover-staging session (Kouros's other thread) MUST stay OFF `app/sku-bom-compile.php` and the packaging-consumption liner path** and consume the parallel session's result. Cutover thread's lane = the `--cost-source` MySQL loader + liquid-sum fix + the cutover flip (all committed, default still `sheet`); the liner fix lane = parallel session. The two meet at the §COGS COST-SOURCE CUTOVER GO-PATH gate: cutover thread may EITHER (i) HOLD the flip until the liner fix lands, OR (ii) cut over now with a NARROW guard that zeroes/excludes the static -V liner from `skuCost` so ZEPV stops exploding (9,700 L × 34.11 phantom) and the event-level liner path supplies -V liner COGS once it exists — provided the guard does not double-count (static liner OFF the moment the guard is in). Verified live 2026-06-09: ZEPV is the ONLY -V SKU sold in May (6 lines, 9,700 L, uom='L'); EMBV/MOOV no May sales → the guard only needs to cover ZEPV for May closeability.

## §COGS COST-SOURCE CUTOVER — ✅✅ FLIPPED + SHIPPED + COMMITTED 2026-06-10 (maltytask `0129bdb`) — CUTOVER COMPLETE
**🏁 FLIP DONE 2026-06-10 — maltytask commit `0129bdb` `feat(sales-cogs): cut over default cost-source sheet→mysql (post-F2)`.** `build-sales-cogs.ts` default flipped `COST_SOURCE ?? "sheet"` → `?? "mysql"` (~line 131); `--cost-source=sheet` retained as legacy fallback, `=diff` as audit. Same commit folded the in-tree `run-month-close.js` step-3 (build-sku-bom) drop from the critical-path parallel block. Depends on (already committed): maltyweb `6b8f6a0` (cuv liner per-unit fix) + `7fbeecb` (mig 302 DB-trigger lock cuv hl_per_unit=0.01) + the `0129bdb`-prior cuv revert+compiler-fix below. **POST-FLIP VERIFY (regression tripwire, per instruction): `build-sales-cogs --month 2026-05 --dry-run` with NO flag → header confirms `cost-source=mysql`; clean run, wrote `interfaces/sales-cogs-data.json`. May 2026 (mysql): 73 SKUs, COGS CHF 59,676.02, NonBeer CHF 31,617.70, BeerTax CHF 12,735.39 (sales 11,813.43 + invendables 212.03 + contract 709.93 on 46.74 HL); ZEPV 936.96 CHF confirmed. RULE-2 GO signed off; parity drift = expected benign post-F2 pattern.** TWO post-flip RESIDUALS logged below (not cutover-blockers). The pre-flip RESOLUTION narration is retained below for the record.

**🟢 RESOLUTION 2026-06-10 (Opus live-verified VPS — the ZEPV/cuv litre-explosion was FIXED via a TWO-PART change; this CLEARED the gate ahead of the flip above):**
1. **Data revert APPLIED:** `ref_skus.hl_per_unit` reverted 1.0→**0.01** for EMBV(27)/MOOV(46)/ZEPV(61) — the 3 cuv `run_type=cuv` SKUs (the only 3 divergent single-format rows; the 3 découverte composite packs PACKDEC/FRPACKDEC/FRPACKCHEF correctly LEFT ALONE — their hl_per_unit is a legitimate blended composite, not a format nominal). LIVE-VERIFIED: all three = 0.01000. Then `sku-bom-liquid-cli.php --apply --sku 27,46,61` recompiled liquid (34 lines del/ins, 0 parity violations); liquid per-unit dropped exactly 100× (ZEPV 8.943→0.0894 etc).
2. **🔴 COMPILER DEFECT found + fixed (the durable finding) — the §cuv observed-liner branch in `app/sku-bom-compile.php` HARDCODED a per-HL assumption.** It emitted the synthetic `liner_amortized` packaging row with `qty=1.0` + `cost_direct=$cuvLinerCostPerHl` directly (comment literally "with hl_per_unit=1.0, cost = liner_cost_per_hl directly"). After the revert, liquid was per-litre but the liner stayed per-HL → **unit mismatch INSIDE one BOM** → `build-sales-cogs.ts:612` does `materialCHF = units × SUM(bom.cost)`, so the summed cost MUST be fully per-sellable-unit, and ZEPV would still compute ~7,800 CHF (not the target). **FIX (deployed `bin/deploy.sh --apply`, packaging recompiled `--apply --sku 27,46,61`, 0 parity violations):** scale BOTH `qty => $skuHlPerUnit` (L815) AND `cost_direct => round($cuvLinerCostPerHl * $skuHlPerUnit, 6)` (L817), `$skuHlPerUnit = (float)($sku['hl_per_unit'] ?? 1.0)` (L811). Generalises the old `hl_per_unit=1.0` special case to ANY hl_per_unit, mirroring the file's own canonical per-unit pattern (~L485 liquid, ~L3462/3489). **The CHF/HL DISPLAY stays unchanged** (cost/qty still recovers `liner_cost_per_hl`, the gate that 92c9c13 had to pass) while the per-unit SUMMED cost is now correct.
   - **🔑 DURABLE LESSON (also fold into the `coder`/`sql` skill if it recurs): 92c9c13's liner-amortization was only EVER exercised at hl_per_unit=1.0 — it silently broke for any other hl_per_unit. The CHF/HL display gate it passed NEVER caught it because display divides cost/qty (so a per-HL qty=1.0 line reads correct on display). The COGS bug only surfaces in the SUMMED per-unit path (sales material = units × Σcost). This is the EXACT standing failure mode: a COGS-bearing change gated only on a display/conservation check, not on the sales-material path. ANY per-unit BOM line emitted by the compiler MUST scale by the SKU's own hl_per_unit, never hardcode a basis.**
3. **LIVE-VERIFIED post-fix (Opus, VPS www-data):** `Σ ref_sku_bom.cost` per cuv SKU = ZEPV **0.096594** / EMBV **0.224576** / MOOV **0.121062** (loader `loadSkuBomCostMap()` returns skuCost ZEPV=0.09659/EMBV=0.22458/MOOV=0.12106, skuHL=0.01 all). `liner_amortized` line now `qty=0.01`, cost scaled per-litre (ZEPV 0.007160 → cost/qty=0.716 CHF/HL display intact). ZEPV sales material = units(litres ≈9,700) × 0.0966 ≈ **937 CHF for ~97 HL** — the ~100× phantom (was ~9.89/unit → ~96k) is **GONE.** `build-sales-cogs --month 2026-05 --cost-source=diff` printed with NO per-SKU diff lines for the cuv SKUs (sheet and mysql agree). Rollback snapshots on VPS: `_snap_refskus_cuv_hlrevert_20260610` + `_snap_skubom_cuv_hlrevert_20260610`.

**CONSISTENCY RULE STANDS (LOCKED): all litre-denominated SKUs (cuv -V AND contract bulk -V/Vrac) carry `ref_skus.hl_per_unit = format.hl_per_unit` — NEVER diverge. JASPL=0.01 canonical; EMBV/MOOV/ZEPV now match it (0.01).**

**✅ ALL FLIP STEPS DONE 2026-06-10:** (1) compiler `app/sku-bom-compile.php` committed (maltyweb `6b8f6a0`) + DB lock mig 302 (`7fbeecb`); (2) fresh `--cost-source=diff` confirmed benign post-F2 liquid-higher drift; (3) default flipped `build-sales-cogs.ts` + `run-month-close.js` step-3 fold (maltytask `0129bdb`); (4) RULE-2 GO signed off; (5) verify dry-run header `cost-source=mysql`, May numbers landed (see FLIP DONE block at top of section). BLO/BLA not-sold-in-May = off critical path.

**TWO POST-FLIP RESIDUALS (surfaced by the verify run; NOT cutover-blockers — follow-up backlog):**
- **R-A. 3 unknown sales SKUs** (`sales-sku-unknown=3`) — sold in May, unresolved to a cost map. PRE-EXISTING (not flip-induced). Worth a triage pass. PM verdict 2026-06-10: **PARK** — pre-existing, doesn't block May close (COGS still computes; unknowns are surfaced, not silently dropped). Chase in a triage pass after close.
- **R-B. Contract beers UNTAXED** — BAMB/BAMF/JASPB/JASPF/JASPL show null beer-tax category → 0 CHF Biersteuer. These are Chien Bleu contract beers. Ties to the contract-Biersteuer categorization + accountant sign-off already flagged. **OPEN QUESTION (operator/accountant): should contract beers carry a beer-tax category (Cat per OG, like Neb lineup) or is 0 correct here (e.g. tax borne by the contract client, not La Nébuleuse)?** PM verdict 2026-06-10: **PARK pending accountant sign-off** — this is a tax-liability-routing decision, not a code defect; do NOT guess the category (COGS/tax-impacting). The 709.93 CHF contract beer-tax in the May verify run is the *Neb-side* contract lines that DO carry a category; the BAMB/JASP* nulls are the ones in question. See [contract beer-tax categorization] feedback note in MEMORY index.

---

## §COGS COST-SOURCE CUTOVER — ⚠️ HISTORICAL NO-GO (2026-06-09, SUPERSEDED by the RESOLUTION above)
**VERDICT (HISTORICAL): do NOT flip `build-sales-cogs` default to `--cost-source=mysql` for the May close yet. ONE May-sold SKU (ZEPV) is mis-costed by ~330k CHF in MySQL.** Default stays `sheet` until ZEPV is fixed. All Opus-verified live 2026-06-09. **← RESOLVED 2026-06-10, see section above.**

**THE THREE STRUCTURAL OUTLIERS — adjudicated vs the actual May-sold SKU set (83 distinct, `inv_sales_bc` period='2026-05'):**
1. **Cuv -V (mi_id=102 appearing 2×) is NOT a double-count bug.** `mi_id=102 = PKG_LINER_10HL_EDS25` (single-use plastic liner for the 10HL serving tank, 18 EUR → 17.01 CHF). The two rows have DISTINCT `slot_name` = `liner_client` + `liner_transport` = two physically separate liners consumed per fill (matches the "-V from PackagingData Liner cols" convention). 34.02 = legit 2×17.01. It is a CONSUMABLE (not a returnable deposit) → correctly in COGS. **HOWEVER:** of ZEPV/MOOV/EMBV, **only ZEPV is sold in May.** And ZEPV exposes the REAL bug ↓.
2. **🔴 ZEPV IS THE BLOCKER (May-sold, ~330k CHF phantom).** `ref_skus.ZEPV.hl_per_unit=0.01`, format='Cuve de service'. ZEPV is sold **by the LITRE** (`inv_sales_bc.unit_of_measure='L'`, 9,700 L in May across 6 lines). `build-sales-cogs.ts` computes `materialCHF = agg.qty × unitCost`, and `agg.qty = SUM(qty_invoiced)` = **litres** (`lib/repos/mysql-sales.ts:144`). MySQL skuCost(ZEPV)=34.109 (liq 0.0894 + 2×17.01 liner). Flip → 9,700 × 34.11 ≈ **330,500 CHF** of phantom liner COGS dumped on a 97 HL Cuv sale. The 34 CHF/fill liner is multiplied per-litre because there is NO amortization of the per-tank-fill liner across the tank's volume. The Sheet masks this (sheet skuCost(ZEPV)≈0.10, liquid-only, no liner) → today's `sheet` run reads a clean 55,147.83 ONLY because the sheet's near-zero ZEPV hides the litre-explosion. = the same defect logged as the "EMBV CHF/HL≈3423 / hl_per_unit oddity" follow-up, now proven COGS-bearing and live in May via ZEPV. **FIX BEFORE CUTOVER** (options for operator: (a) amortize liner cost across tank volume so ZEPV per-litre packaging ≈ 34.02/tank_litres; or (b) special-case litre-denominated -V SKUs so COGS = liner-per-fill × n_fills, not × litres; or (c) exclude -V from the sales-COGS material calc and cost the liner separately). EQUIP coder+sql. THIS is the lone gate.
3. **EST* — Sheet was stale/mis-assigned; MySQL is CORRECT. CLEARED.** ESTF NOT sold in May; **EST4/ESTBU/ESTP25/ESTP50 ARE sold.** MySQL: EST4 liq 1.2914 / pkg 6.8918 — normal, sibling-consistent (cf ALT4 1.29/6.74). The Sheet's wild Estafette values (75.40 etc.) are stale. No fix; MySQL trusted.
4. **BLO/BLA — NOT sold in May → does NOT block the cutover.** Neither BLO* nor BLA* appears in the May-sold set. BLA="Blanche des Romands" (MOO liquid), BLO="Blonde des Romands" (ZEP liquid) — both DISCONTINUED Néb-own white-labels (is_active varies). The missing-liquid gap (BLO/BLA recipes not F2-compiled) is real but DEFERRABLE — fix separately, off the May critical path.

**MAY-SOLD COST COVERAGE (Opus-verified): all 69 beer SKUs sold in May resolve to a ref_skus row WITH liquid+packaging BOM cost (except the legit zero-pkg contract kegs BAMF/JASPF/DGDF and the litre-bug ZEPV).** 14 May SKUs are UNRESOLVED non-beer (ART-*, CAUAL, CAUF deposits, CO2-, FO, HOODN, PIZZA, PLAN, SOFT, V50) — these have `sku_id_fk` NULL (or no ref_skus row) and `build-sales-cogs` routes them to NonBeer / unknown-SKU exclusion (lines 571-602), NOT into material COGS. Correct.

**CUTOVER GO-PATH (when ZEPV fixed):** (1) fix ZEPV liner amortization/unit-handling + regenerate; (2) re-run `--cost-source=diff` for May, confirm ZEPV no longer explodes + the systematic ~127-SKU liquid drift is all the benign "MySQL post-F2 higher than stale sheet" pattern (which it is — the sheet liquid builder is archived/never got F2 recalibration); (3) flip default in `build-sales-cogs.ts` (`COST_SOURCE` ?? "sheet" → "mysql") + fold the `run-month-close.js` step-3 fix per earlier spec; (4) RULE-2 review; (5) one fresh `diff` proving the flip is the intended delta, not a regression. The systematic liquid-higher drift is EXPECTED and APPROVED in principle — MySQL is the trusted post-F2 source; the sheet liquid estimate is stale. Only ZEPV stands between here and GO. EQUIP coder+sql.
- **`warehouse.php` `$glSt` FG-valuation query (~L564)** — check for the same one-brew÷batch-HL mismatch the F2 multi-brew bug had (GL-split uses `v_bip_canonical`, not `ref_sku_bom` directly → likely unaffected, but verify). EQUIP coder+sql. (This was already logged as a multi-brew follow-up.)
- **7 out-of-scope Brewing/NULL rows** remain in `ref_sku_bom` — legacy rows for SKUs the liquid compiler doesn't cover. Cosmetic.

---

## §F2 REVIEW HANDOFF CHECKPOINT (2026-06-08 — dry-run REGENERATED + digest BUILT; operator now DRIVES) — ⚠️ HISTORICAL, see §F2 LIQUID APPLIED above
**STATE: handoff. The build side is done through the dry-run; the ball is in the operator's court (walk + annotate the digest → ONE apply dispatch). The apply predicate is NOT YET BUILT.**

> **✅ MULTI-BREW FIX LANDED + OPUS-VERIFIED 2026-06-08 (commit `9012c8d`, see §MULTI-BREW above):** the digest's multi-brew malt/hops numbers were VOID under the pre-fix dry-run; they are now REBUILT off the corrected preview (generated_at 2026-06-08 13:03 UTC). Operator may walk the FULL lineup (multi-brew + single-brew) on the corrected digest. The handoff state below described the PRE-fix dry-run — multi-brew numbers in it are superseded by the rebuilt digest.

- **Dry-run REGENERATED on VPS post-`9fa60a7`** (oldest-invoice / gate-15 now included). Clean: **142 SKUs in scope; 79 gain / 49 lose / 14 no-change liquid; +800 / −588 / 668 changed lines; 0 errors; 0 unresolved MIs.** Canonical preview = `/var/www/maltytask/data/sku-bom-liquid-preview.json` — **REGENERATE at apply time per gate-14 staleness** (production data + SDC stage re-attribution move daily).
- **Engine RE-VERIFIED independently (Opus):** EMBF dry-hop 566.38 g/HL EXACT (Cascade 153.50 + Mosaic 15.88 + Amarillo 264.66 + Simcoe 132.33); DIG/DIP MALT_PILSENER oldest-invoice 0.6331 CHF/kg ✓ (gate 15); per-HL identical across all SKUs of each beer. No engine faults found.
- **Operator review artifact:** `/home/kluk/projects/maltytask/reports/f2-liquid-review-digest-2026-06-08.md` (1104 lines, 18 beers, PM walk order **EMB→DIG→DIP→DOA→DOC→SPY→ALT→rest**). Per beer: brewhouse + dry-hop per-HL tables, gap-fills, drift-exclusions, gate-15 subs, CHF/HL, per-beer decision checklist. Kouros walks it at his pace + annotates → then ONE apply dispatch.
- **APPLY PREDICATE — NOT YET BUILT (spec to dispatch when operator signs off):** per SKU, `DELETE source='Brewing' AND bom_source IN (NULL,'liquid') AND mi_id NOT NULL` + reinsert `bom_source='liquid'`; snapshot-before + log_revision; idempotent; gates 1-15. EQUIP: `coder`+`sql` (+`ui` ONLY if an on-page render touched). Regenerate dry-run immediately before apply + diff vs the operator-reviewed preview (gate 14).

**11 ANOMALIES SURFACED FOR REVIEW (NONE are engine faults — all recipe-spec data or known gaps).** Opus escalated **2 to MUST-DECIDE-BEFORE-APPLY:**
1. 🔴 **DIG PROC_YEASTVIT 0.00035 g/HL vs 8.0 standard = ~22000× recipe-spec scale error** — rides in as gap-fill; **fix `ref_recipe_ingredients` BEFORE apply** (data fix at the master-data root, not in the compiler).
2. 🟠 **DGD MALT_PILSENER 18.04 kg/HL vs 3.8-7.3 elsewhere = single-brew implausible grain bill** — confirm vs brew record before apply.

Digest-flagged others (review, not hard gates): DGD KRUSH_CRYO null-currency; **EPH3 r71 empty recipe spec** (all-observed, dormant since 2024-08 — oldest-invoice fires with large SIMCOE/MOSAIC deltas; confirm leave-on-OI for historical); **DOC EL_DORADO+MOSAIC 333 g/HL dry-hop EXCLUDED** (batch='None', apply-gate 12 review line — ships with NO dry-hop until re-entered); EST MALT_CASSONADE drift; EPH4 ADJ_BOURBON 666.67 ml/HL; MOO HALLERTAUER_BLANC+SPALTER observed-not-in-recipe; ALT n_window=3; EPH2 EPH24P hl_per_unit 0.0132; **no_basis CollabIn MIs** (DIG MIN_NACL, DIP ADJ_LEMON_JUICE) ride the Lenzlinger invoice feed.

**STILL-OPEN OPERATOR DECISIONS (the review's actual content):** SPY floor 2025-10-07 confirm (apply-gate 9 / §F2-WINDOW-GUARD); finings per-beer; the 2 escalated recipe-spec fixes (#1/#2 above); gate-15 oldest-invoice sign-off; dormant-EPH3 OI confirm; dry-hop data-gap acceptances (DOC + any MIXED-DH-alternation beers per apply-gate 13).
