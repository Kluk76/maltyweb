# UI/UX QUALITY REWORK ARC — commissioned 2026-06-07 (Kouros)

> Read when doing ANY site-wide UI/UX quality work, the P0–P3 audit, the SVG sketch upgrade (svg-tanks.php / salle-des-machines.js scene-painter), or adding motion/ease tokens. Companion: `ui` skill (upgraded 2026-06-07 — severity §11, personas §12, ux-quality-rules.md, motion.md, dataviz.md) + conventions-and-helpers.md §STANDING UI/UX DIRECTIVE.

> **🏁 ARC FULLY CLOSED 2026-06-07 (Phases 0+1+2+3 + C2 convergence + calibration addendum, all in one day; one carve-out = bom-review post-F2).** Commits: `9104273` (P1) + `01c99e3` (P1 review fixes) + `9dcd4ca` (P2 svg_cct/svg_bbt gravure) + `cd96cd4` (P2 SdM scene gravure) + `9ed0f6f` (C2 convergence) + `49981f4`/`c31163f` (calibration, operator-confirmed) + **`e05b18c`/`e679efa`/`aba178a` (Phase 3 bundles A/B/C, all pushed, PM-verified main==origin)**. **House sketch language = GRAVURE DENSE, everywhere a vessel renders; `app/svg-tanks.php` is the ONE canonical vessel library** (svg-vessels.php = tombstone stub, scrapping-backlog #31). Phase 3 = per-page polish EXECUTED per the gate-check dispatch (3 parallel Sonnet bundles + ONE combined-diff pre-commit RULE-2 review — done properly this time, 2 P1s caught+fixed BEFORE commit). What remains of this arc = §RESIDUAL BACKLOG below (4 items, incl. the explicit bom-review carve-out) — tracked there, arc otherwise complete.

## THE COMMISSION (operator, 2026-06-07)
1. Rework/improve overall UI/UX quality + usability across the app.
2. Improve the SVG sketches — Salle des Machines scene + CCT/BBT renderings (svg-tanks.php). Inspiration images CONFIRMED on Desktop: `/mnt/c/Users/Kouros/Desktop/CCTandBBTsketch.jpg` + `brewhousesketch.jpg` — review BEFORE any SVG build.

**This commission SUPERSEDES the 2026-05-26 "COLOURS ONLY" scope boundary** of the recolour directive — layout/component/sketch quality work is now in scope. The light-kraft PALETTE itself stays FROZEN (house standard; single global `:root` in app.css, Option (a) shipped `3c57e5b`).

## AS-BUILT STATE (verified 2026-06-07)
- **Tokens:** single global `:root` at app.css L1 (~75 lines): base kf surfaces/ink, functional accents (AA-tuned), steel set, --kf-* full palette, legacy dock tokens, admin fallbacks. **NO --ease-* tokens exist yet** — adding `--ease-out/--ease-in-out/--ease-drawer` to :root is clean (one place, site-wide). app.css = 12,379 lines; 131 transition/animation rules; **27 backdrop-filter occurrences in public/css/ = audit against the new perf hard-ban** (check scrolling containers); NO JS scroll listeners found (clean).
- **Page registry (ref_pages live, 20 rows / 18 active):** mon-tableau, sb-board, sb-guerre, zeppelin, triage, saisies, approvisionnement, wort, fermentation, packaging, sku-costs, warehouse, rm-comparison, settings(manager), ingest/charges-bc/db-browser/bom-review(admin); fulfilment + qa INACTIVE (is_active=0 — design-mock-grade, skip in audit). NB FS has MORE modules than ref_pages (reconcile CLI still open): tanks.php, sessions.php, salle-des-machines/salle-de-controle/salle-fournisseurs, sb-mother/sb-batch, form-* (linked from saisies), sku-cost-detail, warehouse-export, le-zeppelin hub, reglages-generaux + the 8 legacy `public/admin/settings/*` CRUD pages (scrap-tracked, do NOT polish).
- **THREE SVG sketch surfaces (the divergence to manage):**
  1. `app/svg-tanks.php` (447L, `svg_cct()/svg_bbt()`) → consumed by `tanks.php` (4 call sites incl. `<template id="cct-svg-N">` for JS re-render) + `packaging.php` L408 (BBT row). **This is the UGLIFIED one** — recolour `3c57e5b` mechanically inverted white speculars → faint blacks (geometry unchanged, 96 elements). Operator chose Option B (light-ground steel) but the flat result is the standing complaint. Lesson recorded: light ground needs light-source-aware near-white specular band + soft contact shadow, NOT a white→black flip.
  2. `app/svg-vessels.php` (`svg_vessel_cct/bbt/kettle/packaging_line`, namespaced to avoid collision) → sb-board.php + sb-mother.php. Built FROM SCRATCH for mother-shell Concept C diorama: light-on-kraft, upper-left key light, var(--token) fills, collision-safe defs UIDs. **The quality reference among the live libraries.**
  3. `public/js/salle-des-machines.js` (844L JS scene-painter, 38 svg refs) — the 400% sliding-stage brewhouse scene.
  - Fidelity target artifact: `public/_design/salle-des-machines.html` (94KB, 354 sketch elements — dark tokens, re-tone never copy). Old dark-theme sketch lineage: `595cd86:app/svg-tanks.php`.
  - **DIVERGENCE FLAG: two PHP vessel libraries model one fact (vessel rendering). → ✅ RESOLVED 2026-06-07 (C2, commit `9ed0f6f`):** svg-tanks.php is the ONE canonical library; svg-vessels.php tombstoned (see §C2 AS-LANDED). `cct-detail-modal.js` reads palette via getComputedStyle(:root) — token renames break it (verified unaffected at C2).
- **Contrast caveats (standing, from conventions-and-helpers.md):** --steel FAILS as text (use --steel-mid/--ink-mute); --oak passes on paper but FAILS on --bg-elev cards (use --oak-deep); mid-tone-as-text must be re-checked at 4.5:1 on #f1e8d4; --hop/--ember green-orange = deuteranopia danger pair (second channel required when sole discriminator).
- **Layout landmines:** `.home .main` align-items:center trap (full-bleed pages MUST set align-items:stretch explicitly); `body.home.salle-des-machines{overflow-x:hidden}` exemption MUST be preserved (ui skill #76/#77); fit-guard `.home .main > *{max-width:100%;min-width:0}`.

## NO-GO ZONES (as of 2026-06-07)
- **bom-review.php** — BOM arc ③ F2 joint per-beer review happens ON this surface NEXT; freeze until F2 apply lands.
- **salle-de-controle.php Recettes section** — SDC recette UX batch IN FLIGHT (hops-always-g / process-order / qty-per-BRASSIN) + operator re-attributing whirlpool stages live.
- **kpi-charts.js / mon-tableau / recap surfaces** — P3 recap-emails build IN FLIGHT (mig 277).
- **sb-board/sb-mother/sb-guerre/sessions interaction flows** — atom-11 operator smoke pending; visual polish OK, interaction changes NOT.
- **The 4 saisie forms' interaction flows** — operator in backlog-data-entry mode; styling fine, flow changes need his sign-off.
- **Legacy public/admin/settings/* pages** — scrap-tracked; do not invest polish.

## BLESSED SEQUENCING (PM, 2026-06-07 consult)
- **Phase 0 (one PR):** add --ease-* tokens to :root + run the P0–P3 audit (one representative per template group, persona-based, webapp-testing READ-ONLY screenshots) → prioritized findings doc. Template groups: hub/launcher (le-zeppelin, saisies, salle-des-machines stage) / dashboard (wort, packaging, mon-tableau, sb-board) / list+filter (sessions, triage, warehouse, charges-bc) / fiche (salle-fournisseurs) / PRG batch form (form-packaging as richest) / admin-settings (reglages-generaux) / detail-modal (cct-detail-modal).
- **Phase 1 (one PR):** P0/P1 fixes site-wide at the SHARED layer (app.css tokens/components, partials) — contrast, touch ≥44px, eight-states, backdrop-filter perf triage.
- **Phase 2 (own PR, preview-first MANDATORY):** SVG sketch upgrade — review Desktop inspiration images with operator FIRST → static preview (mock HTML or /tmp render) → operator sign-off → rebuild svg_cct/svg_bbt to svg-vessels lighting conventions → converge libraries (RULE-1 gate) → tanks.php/packaging.php/sb-* unchanged call signatures (FROZEN API: svg_cct(float,string,int,string,string,array)). salle-des-machines.js scene = separate sub-step, same preview-first.
- **Phase 3 (several small PRs):** per-template-group polish per the redesign priority order (font→color→states→layout→components→loading/empty/error→polish).
- **EQUIP:** audit dispatch = `ui` + `webapp-testing`; build dispatches = `ui` + `coder` + `webapp-testing` (+ `sql` only if a feeding query changes). frontend-design folded into ui.

## STATUS / PHASE LEDGER
- 2026-06-07: arc commissioned; planning consult delivered.
- **Phase 0 ✅ DONE (2026-06-07):** 3 `--ease-*` tokens in app.css `:root` (`--ease-out` cubic-bezier(0.23,1,0.32,1), `--ease-in-out` (0.77,0,0.175,1), `--ease-drawer` (0.32,0.72,0,1)). Full P0–P3 audit delivered → `/home/kluk/projects/maltyweb/reports/ui-ux-audit-2026-06-07.md`: 35 findings (1 P0, 14 P1, 13 P2, 7 P3); 10 shared-layer / 25 per-page. Scroll-listener ban: PASS clean app-wide.
- **Phase 1 ✅ CLOSED (2026-06-07, retroactive RULE-2 review done):** fresh-context adversarial review of `9104273` → 0 P0 / 0 P1 / 4 P2 / 3 P3; both PM-flagged hazards CLEAN (global `:active` scale doesn't disturb SdM stage or fixed children; focus-ring cascade resolves correctly). P2s fixed in commit `01c99e3` (app.css only, selective rsync): family-btn + sdm-zone focus rings → INSET `outline-offset:-2px` (outset rings were CLIPPED by `overflow:hidden` — rg-page switcher / Firefox on sdm-zone — **lesson: focus rings inside overflow:hidden containers must be inset**); unscoped `.db-flash__dismiss` rule added (form-resilience banner on non-.home pages had unstyled dismiss). Audit report committed same commit. DEFERRED → Phase 3 backlog: sb-zone__header frosted-glass restore (proper fix = make header STICKY, which legitimizes backdrop-filter under the perf ban — sb-* layout change, atom-11-gated); af-zoom-btn unintended :active scale (P3 cosmetic).
- **Phase 1 build detail (commit `9104273`, 34 files):** skip-to-content link in topbar.php + `id=main-content` on all 25 `<main>` (P0 cleared); global `:focus-visible` rings (2px --hop, broad selector + named classes); backdrop-filter REMOVED from 4 scrolling containers (.sdm-chrome, .sf-manifest, .rg-page .family-switcher, .sb-zone__header) — **salle-de-controle.css backdrop-filter DEFERRED to Phase 3 (frozen no-go arc)**; form-resilience.js keepalive → reglages-generaux.php; wort.php tab aria-labelledby + aria-pressed; SdM toast/flash live regions; 6 black shadows → warm tints; reglages nav-rail divs → real `<button>`; sessions.php "Recette #N" → recipe names (label-map rule); cct-detail-modal.js XSS audit clean (no change); global `touch-action:manipulation` + `:active scale(0.97)` w/ --ease-out + prefers-reduced-motion net + `overscroll-behavior:contain` on 4 panels; `brewery_identity()` replaced hardcoded "La Nébuleuse"/"Lausanne" in 4 pages (SDM footer said Lausanne — truth = Renens; the Neuchâtel-hardcode lesson AGAIN — grep remaining literals at Phase 3). Smoke: php -l clean, 200 on 10 pages.
- **Phase 2 — MAIN STEP LANDED (commit `9dcd4ca`; phase since FULLY CLOSED, see next entries).** Operator approved **Variante B (gravure dense)**; `svg_cct`/`svg_bbt` REBUILT in `app/svg-tanks.php` to the gravure style. API + state classes + uid mechanism (microtime md5 suffix) preserved EXACTLY; element counts 60–109/vessel (≤120 budget — well under the 300 cap even on a 10-vessel bank, 0 duplicate defs ids verified); state mapping: ferment = hop tint + dashed line + bubbles (CSS hook, reduced-motion gated), cold = cold tint, maint = ember cross-hatch + wrench, BBT full ≥0.9 = ok tick. php -l clean; smoke 302 on tanks/packaging/salle-des-machines; visually verified via local CLI-render screenshot. **LIVE for operators on tanks.php + packaging.php.** Preview metrics anonymized earlier (batch 99 / 42.0 HL). **`app/svg-tanks.php` is now the GRAVURE REFERENCE IMPLEMENTATION** (supersedes the §AS-BUILT note calling svg-vessels.php "the quality reference"). Original direction record: vintage engraving / etched line-art, oak-ink hatching on kraft, from Desktop inspiration (CCTandBBTsketch.jpg + brewhousesketch.jpg) — avoids the dead-specular problem.
- **Phase 2 ✅ FULLY CLOSED (2026-06-07, commit `cd96cd4` — SdM scene gravure integration):** `salle-des-machines.js` scene painter adopts gravure — `sceneDefs()` emits 3 hatch patterns (`bh_px`/`bh_pd`/`bh_pj`); `cylGravure()` replaces `cylShade` for SCENE vessels (**`cylShade` KEPT for the mini config icons — intentionally untouched**); strapShadow after every weldSeam; outline weights 1.85/1.1; CUITE calandre interne + reduced-motion-gated steam wisps; whirlpool vortex glyph; colour switch scoped to `svg.sdm-draw` ONLY (--oak-deep/--hairline-2 ink). brewScene ~227 elements, cellar ~130. Selective rsync (JS+CSS only), smoke 302. C1 gate = Kouros approved gravure for the scene → **gravure CONFIRMED as the house sketch language.**
- **C2 CONVERGENCE ✅ LANDED (commit `9ed0f6f`) — svg-tanks.php = the ONE canonical vessel library.** Pre-build inventory: only sb-board.php called `svg_vessel_kettle(1,'idle')` + `svg_vessel_packaging_line('idle')`; sb-mother needed the require as guard only. All four `svg_vessel_*` REIMPLEMENTED in gravure inside `app/svg-tanks.php`, signatures + CSS hooks verbatim (`.sb-vessel--pulsing`, `.sb-conveyor-lines`, etc.), element counts 50–110. `app/svg-vessels.php` = tombstone header + `require svg-tanks.php` + `function_exists` no-op stubs (zero-risk fallback) → **scrapping-backlog #31** (delete gate: BSF-exit Phase 6 landed AND `grep -rn "svg-vessels" public/ app/` = 0 hits). Bundled in same commit: **sb-zone__header restored to STICKY + FROSTED** — backdrop-filter is legitimate on a sticky header (composites once, not per scroll frame) → **the audit's deferred P2 (frosted-glass restore) is CLOSED.** cct-detail-modal token reads verified unaffected. Smoke: sb-board/sb-mother/tanks/packaging all non-500; php -l clean local+VPS.
- **TEARDOWN ✅ DONE:** `/_design/sketch-vessels-v2.html` + `/_design/sketch-brewhouse-v2.html` removed locally + VPS (404 confirmed). The gravure spec now lives in the canonical implementation (svg-tanks.php) + this arc file — no preview artifacts remain.
- **Phase 3 🟢 GATE RE-CHECK 2026-06-07 LATE — DISPATCHED with ONE carve-out.** Gates re-ruled against repo+live: (a) **kpi-charts/mon-tableau CLEARED** — mig 277 applied, recap cron activated `1d8672b` operator-confirmed, Mon-tableau v2 + KPI tranches landed+pushed (`e8f5336`/`f7fe151`/`b838cca`/`6f0e773`); future KPI tranches touch kpi-handlers.php only, non-colliding. CONSTRAINT: `window.KpcCharts` public API FROZEN (wort-kpis + mon-tableau both consume); empty-state text via consumer options. (b) **SDC Recettes CLEARED for visual-only** — the recette UX code dispatch LANDED (`2d4d419`+`2a803d4`+`9d169a4`, verified in git log); what remains is operator DATA-ENTRY (yeast strains, whirlpool re-attribution) = no code collision. salle-de-controle.css:88 family-switcher backdrop-filter (the deferred P1) now actionable. CONSTRAINT: zero interaction-flow change to the Recettes editor. (c) **bom-review STAYS FROZEN — the ONE explicit carve-out**; F2 joint per-beer review happens ON that surface next; polish logged as POST-F2 FOLLOW-UP (Phase 3 closes with this carve-out). (d) **sb-* visual-only permitted, MINIMAL** (only the 2 logged deferred items — sync-ts chip + smh-header check); every extra visual change enlarges atom-11's visual-acceptance scope. **Phase-1 coverage verified by grep 2026-06-07:** DONE = SH-01/02/03/05/06/07/09/10, WO-01/02/03 (wort has no "Recette #"), SE-01, SE-02 (named-class rings), SF-01/02, RG-01/02/04, SDM-01/02/05, LZ-03, CCT-01, BF-sdm/rg/sf/sbb. STILL OPEN = LZ-01/02/04(verify global :active covers .sh-card), SDM-03/04, WO-04/05/06, SE-03/04/05/06, SH-08 (dots have title= only, no aria-label), SF-03/04, PF-01..05, RG-03/05, CCT-02(verify net)/03/04 + deferred: sdc.css:88 backdrop, sb-board.css:1231 chip, smh-header sticky-check, af-zoom-btn :active opt-out, residual brewery literals (classification-appareil.php + salle-de-controle.php; app/settings*.php+mailer = canonical defs, verify don't blind-replace), hover:hover gating absent in le-zeppelin/sessions-dashboard/salle-des-machines CSS (grep=0). **+ mon-tableau.php light battery pass (landed POST-audit, now the DEFAULT LANDING page via mig 280 — never audited).** Dispatch shape = 3 parallel Sonnet agents (A hub/SdM/admin/fiche; B sessions/forms/SDC-visual; C dashboards/modal/sb-chips); **app.css owned by Agent C ONLY** (A/B per-page CSS only); RULE-2 review per bundle before commit; smoke via curl/on-VPS (Chromium-Tailscale gotcha). Atom-11 note unchanged: smoke = visual acceptance of sb-* gravure.
- **Phase 3 ✅ EXECUTED + LANDED (2026-06-07, commits `e05b18c` 3A / `e679efa` 3B / `aba178a` 3C — all pushed, PM-verified main==origin, origin..main=0). ARC CLOSES HERE (one carve-out).** Executed per the gate-check dispatch shape: 3 parallel Sonnet bundles, then **ONE fresh-context adversarial review over the COMBINED diff BEFORE commit** (RULE-2 done properly pre-commit, not retroactively — this is now the proven shape for multi-bundle bursts). **Review verdict: COMMIT AFTER FIXES — 2 P1s caught + fixed pre-commit:** (1) **PF-04:** bundle B had added a `preventDefault()` pre-submit gate on the novalidate packaging form — flow-change risk, could silently block legit submits with no error message → **REMOVED entirely**; a11y focus belongs POST-server-validation (logged → §RESIDUAL BACKLOG #2, NOT done). (2) **PF-03:** `role="listbox"`+`aria-live` on the tank grid was INVALID ARIA (button children) + would spam AT on every rebuild → replaced with `role="group"` + a separate sr-only `role="status"` element receiving summary strings ("N lots disponibles") from `buildTankCardsFromData`. (3) P3 fix: wort Filtrer submit min-height 32→44px (house floor). **Hazards cleared by review:** salle-fournisseurs duplicate main-content id (if/else exclusive — fine); CCT-02 reduced-motion (global `!important` net wins; local block comment slightly misleading but functional); WO-04 innerHTML (static strings only); wort double-submit (idempotent GET); aria-pressed/current sync (correct). **Coverage landed:** LZ-01/02/04(verified), SDM-03/04, RG-03/05, SF-03/04, SE-03/04/05/06, SH-08, PF-01/02(v)/03/05, WO-04/05/06, CCT-02/03/04; salle-de-controle.css:88 backdrop REMOVED (the deferred P1 — closed); sb-sync-ts backdrop removed; smh-header verified-sticky-KEPT; af-zoom-btn :active opt-out; **mon-tableau FIRST battery pass** (hover gating, focus rings, 44px remove btn, press feedback — structural leftovers → backlog #3); brewery literals fixed in classification-appareil + salle-de-controle. **Concurrent-session note:** visite-guidée session had live uncommitted WIP (visite-guidee.php/.css/.js) — left strictly alone, zero collisions; **the app.css-single-owner rule worked** (keep it for future parallel CSS bursts).

## RESIDUAL BACKLOG (post-arc-close, 2026-06-07 — the ONLY open items of this arc; log, do not lose)
1. **bom-review.php polish** — the explicit Phase-3 carve-out, FROZEN until F2 liquid apply + joint per-beer review complete; polish = post-F2 follow-up (gate mirrors RESUME #0(a)).
2. **PF-04 proper a11y** — focus-first-invalid on the PRG error redirect (server-knows-which-fields path) for the packaging form. The client-side preventDefault approach was REJECTED at review (flow-change risk on a novalidate form); the right altitude is post-server-validation. Small build, touches packaging-form submit/redirect + JS.
3. **mon-tableau structural leftovers** — aria-labels on kpc-card remove/drag icons; locale formatting on KPI readouts; picker empty-state copy. Needs `kpi-handlers.php`/`mon-tableau.js` edits → **COORDINATE WITH the KPI-tranche session (do not double-edit)**; fold into the next KPI tranche dispatch.
4. **Deuteranopia pass** on `--hop`/`--ember` chart series pairings (audit P3, never picked up) — chart-color audit across kpi-charts.js consumers; pure-visual, any future UI session can take it.

## CALIBRATION ADDENDUM — operator-feedback fixes (2026-06-07 evening, commits `49981f4` + `c31163f`, deployed + Opus-verified visually)
**Operator feedback:** (1) BBT fill level on packaging.php unclear; (2) ferment vs cold-crash CCTs indistinguishable on tanks.php.

**Root cause — DURABLE LESSON (now in the `ui` skill rendering-bug catalog):** the fill-tint rect in the `9dcd4ca` PHP port was emitted BEFORE the opaque paper-body fill polygon — SVG z-order = document order, so the tint was never *faint*, it was **100% INVISIBLE underneath an opaque sibling**. The `_design` HTML spec had the correct paint order; the port inverted it. Opacity tuning is meaningless when the element is under an opaque sibling. **When porting SVG markup between implementations, verify PAINT ORDER, not just element presence.** Also found same pass: `svg_vessel_cct` used `--cold` (blue) for its ferment state — wrong accent, fixed to `--hop`.

**Fixes (`49981f4`):** paint order corrected in ALL FOUR vessel fns (paper → tint → hatch); tint opacity 0.15→0.34-0.35; surface line sw 0.9→1.4 op 0.9 + meniscus wall ticks; **FERMENT = 5 static CO₂ bubbles** (visible under reduced-motion; animated class = enhancement only); **COLD = ✻ 6-arm frost glyph by the number + 4 frost wall ticks** — states now pass a **GRAYSCALE distinguishability test** (the ux-quality-rules §1 "color never the only signal" rule was being VIOLATED before this; now actually satisfied); BBT got 25/50/75/100% graduation ticks + labels. Element counts 59–73/vessel, budget fine. **(`c31163f`):** the "100" graduation label was clipped to "10" at the viewBox 80px edge — right-anchored at x=79.5 `text-anchor=end` (companion lesson, also in the skill entry).

Verification: selective rsync deploy, php -l clean, smoke 302, Opus verified visually (color + grayscale screenshots of VPS-rendered output). **Arc remains CLOSED — this was calibration, not a reopen. ✅ OPERATOR-CONFIRMED 2026-06-07 on the live pages ("yes much better") — the tanks.php/packaging.php confirmation loop is CLOSED; no open items remain on the addendum.**

## CONVERGENCE RECOMMENDATION — ✅ EXECUTED AS RECOMMENDED (C1 approved + C2 landed `9ed0f6f`, 2026-06-07)
The "ONE library, ONE style (gravure)" ruling held exactly as prepped: C1 gate passed (Kouros approved SdM gravure scene), C2 built same-day. One deviation from the prep: the sb-* swap was NOT bundled into a later Phase-3 sb-* PR — it landed immediately in `9ed0f6f` together with the sb-zone__header sticky+frost restore (which the prep had assigned to that bundle anyway, so the bundle happened, just at C2 time). Both tripwires checked: polling re-render uid/element budget OK (50–110/vessel); cct-detail-modal getComputedStyle reads unaffected. Atom-11 smoke = visual acceptance pass for the new sb-* look (see Phase 3 note).

## OPERATIONAL GOTCHA — headless Chromium can't reach app.maltytask.ch from the WSL box (2026-06-07)
app.maltytask.ch resolves to Tailscale `100.125.142.25`; curl traverses the tailnet fine, headless Chromium TIMES OUT. **Workaround for `_design`/static pages: curl the page to /tmp, screenshot via `file://` URL.** Authenticated-page smokes from this box need the same treatment or must run ON the VPS. Recorded durably in skills-and-build-tooling.md (webapp-testing entry) — applies to ALL webapp-testing smokes from this box, not just this arc.
