/* tankboard-pertes.js — Pertes par batch (C8).
 * Per-beer 6-month aggregate view has no client-side expand or "Tous" toggle.
 * File kept so the <script> tag in tanks.php continues to resolve without a 404.
 */
'use strict';
